---
title: v0.1.0 M1 Implementation Register
type: reference
audience: [A2, A4]
runs: no
verified_on: 2026-07-12
sources:
  - examples/enterprise_wecom_digital_employee/V0.1.0_M0_FREEZE.md
  - examples/enterprise_wecom_digital_employee/V0.1.0_INDUSTRY_REPORT_DIGITAL_EMPLOYEE_PLAN.md
  - contrib/agentseek-work/src/agentseek_work/models.py
  - contrib/agentseek-work/src/agentseek_work/state_machine.py
  - contrib/agentseek-work/src/agentseek_work/schema.py
  - contrib/agentseek-work/src/agentseek_work/migrations.py
  - contrib/agentseek-work/src/agentseek_work/repository.py
---

# v0.1.0 M1 实现登记

## M1 准入

| 项目 | 值 |
| --- | --- |
| M0 freeze version | `m0-1.0` |
| M0 decision | `GO_M1` |
| M0 baseline | `78865e5` |
| 首个开发分支 | `enterprise/v0.0.9-files-plugin` |
| 不变量 | v0.0.9 GA 能力不回归；MCP 两保护文件相对 `68d7b25` 零 diff。 |

## Slice M1-01：Work ledger contracts and state machine

### 已实现

| 能力 | 位置 | 合同 |
| --- | --- | --- |
| contrib package | `contrib/agentseek-work` | 独立 Bub entry point；不放入 `agentseek-enterprise`。 |
| `WorkStatus` | `models.py` | 覆盖 draft、queued、running、四类 waiting、succeeded、failed、cancelled。 |
| `WorkItem` | `models.py` | 不可变快照；固定 Profile/Pack/Playbook/runtime/角色/预算/idempotency/version。 |
| `WorkEvent` | `models.py` | 不可变状态事件；记录 actor、phase、from/to status、work version、digest 和 policy decision。 |
| `WorkBudget` | `models.py` | 固定模型、Token、外部查询、phase、任务时长和重试上限。 |
| 状态机 | `state_machine.py` | 只允许 M0 冻结状态边；非法边 fail closed。 |
| 乐观并发 | `state_machine.py` | 调用方必须提供 `expected_version`；陈旧版本拒绝。 |
| attempt | `state_machine.py` | 进入 `running` 才增加 `phase_attempt`；waiting 回队列不增加。 |

### 状态边

```text
draft -> queued | cancelled
queued -> running | failed | cancelled
running -> waiting_external | waiting_input | waiting_review | waiting_approval
running -> succeeded | failed | cancelled
waiting_external -> queued | failed | cancelled
waiting_input | waiting_review | waiting_approval -> queued | cancelled
succeeded | failed | cancelled -> terminal
```

### 已验证

| 检查 | 结果 |
| --- | --- |
| ruff | clean |
| ty | clean |
| unit tests | 12 passed |
| 时区 | naive timestamp 拒绝 |
| 不可变输入 | brief 复制为只读 mapping |
| 终态 | 不允许重新进入 queued/running |

## 本切片明确不包含

- PostgreSQL schema、迁移和 repository；
- WorkItem 与 WorkEvent 的原子事务提交；
- 幂等创建索引和 tenant scope；
- lease、claim、恢复、重试和取消 service；
- ApprovalRequest、outbox 和 notifier；
- DigitalEmployeeProfile、PackSnapshot 和 pack loader；
- Bub state/prompt hooks；
- 行业报告 Playbook、Source、Evidence、Claim 和 DOCX renderer。

## 下一切片

`M1-02 PostgreSQL ledger repository`：

1. 冻结 SQL schema 和枚举存储形式；
2. 实现 WorkItem/WorkEvent 原子事务和 append-only event；
3. 实现 tenant scope、idempotency unique key 和 optimistic update；
4. 增加 SQLite 语义单测和 PostgreSQL 集成 fixture；
5. 验证崩溃时不存在”状态已推进但事件未提交”的半事务。

---

## M1-01 Work Ledger Contracts 复核（Mac mini, 2026-07-12, commit `031330a`）— PASS

### 自动化与基础

- `make check-work`：**12 passed**（test_models 4 + test_state_machine 8），ty + ruff clean ✅
- `make test-wecom` **45** / `make test-enterprise` **79** / `make docs-test` 0.30s ✅
- Bub entry point `group=bub name=work value=agentseek_work.plugin:main` 发现 + 可导入 ✅
- MCP 两保护文件 vs `68d7b25` **byte-identical**（零 diff）✅

### 合同复核（models.py + state_machine.py，10 项）

| # | 检查 | 结论 |
| --- | --- | --- |
| 1 | WorkItem/WorkEvent/WorkBudget 作为 PG schema 基础 | ✅ 冻结 dataclass + slots；标量→列、`brief`→JSONB、tuple→ARRAY 或 junction、`version`→乐观锁 |
| 2 | M0 冻结状态边一致 | ✅ DRAFT→QUEUED→RUNNING→{WAITING_*, SUCCEEDED, FAILED, CANCELLED}；WAITING_*→QUEUED+CANCELLED；terminal→∅，全匹配 M0 freeze |
| 3 | succeeded/failed/cancelled 严格终态 | ✅ `ALLOWED_TRANSITIONS[terminal]=frozenset()`，任意 to_status 触发 `InvalidTransitionError` |
| 4 | expected_version 阻止陈旧更新 | ✅ `item.version != expected_version → OptimisticConcurrencyError`；version 每次 transition +1 |
| 5 | phase_attempt 只在进入 RUNNING 时 +1 | ✅ `next_attempt = ... +1 if to_status is RUNNING else ...`（L79） |
| 6 | WAITING_*→QUEUED 不误增 attempt | ✅ to_status=QUEUED（非 RUNNING）→不变 |
| 7 | 全部 timestamp timezone-aware | ✅ `_require_aware` 覆盖 created/updated/next_poll/lease_expires/due/occurred_at + transition 内再验 |
| 8 | MappingProxyType brief JSON/JSONB 序列化 | ⚠️ 可行但需注意：repo 边界须 `dict(item.brief)` 转换（`MappingProxyType` 不可 `json.dumps`）；`Mapping[str, Any]` 的 `Any` 不保证 JSON-serializable。**建议**：文档注明 brief 须 JSON-compatible，或 `__post_init__` 增加 JSON-roundtrip 校验 |
| 9 | WorkEvent 支撑审计 + 崩溃恢复 | ✅ from/to_status + work_version + actor + policy_decision + occurred_at 覆盖审计；`payload_digest`（非 payload 本体）是安全选择；WorkItem 为真相源（G4），WorkEvent 是不可变历史 |
| 10 | agentseek-work 通用，无域逻辑 | ✅ 无 wecom/files/report import；纯 WorkItem/Event/Budget/status_machine/plugin |

### PostgreSQL 建模建议

- **WorkItem 表**：标量字段 → 列（VARCHAR / TIMESTAMPTZ / INT / ENUM-or-VARCHAR）；`brief` → `JSONB`；`input_file_ids`/`source_ids`/`artifact_ids`/`skill_digests` → `VARCHAR[]` ARRAY（v0.1.0 首版简单）或 junction 表（需 FK 查询时）；`version` → 乐观锁 `UPDATE … WHERE work_id=$1 AND version=$expected`。
- **WorkEvent 表**：append-only；`event_id` PK（UUID）；`work_id` FK + index；`occurred_at` index / 按月分区（保留期到期删除）。`(work_id, work_version)` 唯一约束防重复事件。
- **WorkBudget 表**：`budget_id` PK；`WorkItem.budget_id` FK。
- **idempotency**：`(tenant_id, idempotency_key)` 唯一约束。
- **lease**：`lease_owner` + `lease_expires_at` → worker claim 后定时扫描过期 lease → 回收。

### 一项设计观察（非阻断）

- `transition_work_item` 的 `phase` 参数允许在**任意** transition 改 phase（含 WAITING_*→QUEUED）。M0 设计意图是 phase 只在进入新阶段（RUNNING）时变；当前允许在恢复（QUEUED）时改 phase。这依赖调用方自律，不是状态机约束。**建议**：M1-02 repository 层的 `resume` 方法不传 `phase` 参数（保持 `item.current_phase`），只有显式推进阶段才传。

### 结论：**PASS — 允许进入 M1-02**

10 项合同复核全 PASS（1 项 ⚠️ 非阻断建议）。models.py + state_machine.py 的状态边、终态、乐观锁、attempt 语义、timezone 约束和审计字段均与 M0 freeze 一致，适合作为 PostgreSQL schema 基础。
agentseek-work 保持通用，不含域逻辑。MappingProxyType brief 的 JSON 摩擦和 phase 参数自律建议已在上方记录，供 M1-02 repository 实现时注意。

## Slice M1-02：PostgreSQL ledger repository

### 已实现

| 能力 | 合同 |
| --- | --- |
| 初始 schema | `enterprise_work_budgets`、`enterprise_work_items`、`enterprise_work_events` 和 schema version 表。 |
| PostgreSQL 类型 | `brief`、skill/file/source/artifact id 快照使用 JSONB；状态使用 VARCHAR + CHECK，避免 native enum 迁移锁定。 |
| 初始 migration | revision 1 幂等创建 schema，并拒绝高于当前代码支持版本的数据库。 |
| tenant scope | WorkItem 读取、事件读取和状态更新都要求 `tenant_id`。 |
| 幂等创建 | `(tenant_id, idempotency_key)` 唯一；并发唯一约束竞争后读取并返回原 WorkItem。 |
| 乐观并发 | `UPDATE ... WHERE tenant_id AND work_id AND version=expected_version`；更新行数不是 1 时 fail closed。 |
| 原子事务 | WorkItem update 和 WorkEvent insert 使用同一事务；事件约束失败时状态更新回滚。 |
| append-only event | repository 不提供 WorkEvent update/delete；`(work_id, work_version)` 唯一。 |
| JSON 边界 | `MappingProxyType` 显式转 `dict`，所有 JSON 文档在写库前执行严格 round-trip；拒绝 object、NaN 等值。 |
| phase 约束 | phase 只能在进入 `running` 时改变；waiting 恢复到 queued 不能偷换 phase。 |

### 数据库约束

- WorkItem 主键：`work_id`；
- WorkItem 幂等唯一键：`tenant_id + idempotency_key`；
- WorkEvent 主键：`event_id`；
- WorkEvent 版本唯一键：`work_id + work_version`；
- WorkEvent `work_id` 外键：`ON DELETE RESTRICT`；
- WorkStatus、ActorType、非负 version/attempt 和 WorkBudget 数值范围使用 CHECK；
- tenant/status、next_poll_at、lease_expires_at、work_id/occurred_at 建索引。

### 本地验证

| 检查 | 结果 |
| --- | --- |
| ruff | clean |
| ty | clean |
| unit tests | 24 passed |
| SQLite migration | revision 1 重复执行保持幂等；数据库版本高于代码支持版本时拒绝 |
| SQLite transaction | 乐观失败不写事件；事件冲突回滚 WorkItem 更新 |
| PostgreSQL DDL compile | JSONB、幂等唯一键、事件版本唯一键、外键和状态 CHECK 均存在 |

### 待 Mac mini PostgreSQL 验证

- 对测试数据库执行 revision 1，不触碰 production schema；
- 验证 PostgreSQL 实际列类型、约束和索引；
- 两个连接并发使用相同 idempotency key，只产生一个 WorkItem；
- 两个连接以相同 expected version 更新，只有一个成功；
- 事件唯一约束失败后 WorkItem version/status 不变化；
- migration 重复执行无变化；高于支持版本时拒绝启动。

### 本切片不包含

- 自动接入 gateway 或 production 数据库；
- worker claim、lease 心跳、过期回收和恢复；
- ApprovalRequest、outbox、notifier 和任务工具；
- DigitalEmployeeProfile、PackSnapshot、pack loader 和 SkillResolver。

## 下一切片

`M1-03 lease and recovery service`：实现 queued claim、有限 lease、waiting 槽位释放、崩溃回收、取消、重试和预算预留。
