---
title: v0.1.0 M2 Implementation Register
type: reference
audience: [A2, A3, A4]
runs: no
verified_on: 2026-07-14
sources:
  - examples/enterprise_wecom_digital_employee/V0.1.0_M0_FREEZE.md
  - examples/enterprise_wecom_digital_employee/V0.1.0_M1_IMPLEMENTATION.md
  - examples/enterprise_wecom_digital_employee/V0.1.0_INDUSTRY_REPORT_DIGITAL_EMPLOYEE_PLAN.md
  - contrib/agentseek-work/src/agentseek_work/models.py
  - contrib/agentseek-work/src/agentseek_work/schema.py
  - contrib/agentseek-work/src/agentseek_work/migrations.py
  - contrib/agentseek-work/src/agentseek_work/repository.py
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/report_brief.py
---

# v0.1.0 M2 实现登记

## M2 准入

| 项目 | 值 |
| --- | --- |
| M1 decision | `GO_M2` |
| M1 closing evidence | `adb8350` 及 Mac mini 活体验证报告 |
| 开发分支 | `enterprise/v0.0.9-files-plugin` |
| 不变量 | v0.0.9 GA 能力不回归；MCP 两保护文件相对 `68d7b25` 零 diff。 |

## 决策冻结

### ReportBrief 采用渐进式澄清

首版 ReportBrief 不是一张长表。员工只需先说明报告主题。
系统从消息、岗位 Profile 和 Playbook 默认值补全其余字段。

首版合同字段：

| 字段 | 规则 |
| --- | --- |
| `title` | 必填；报告主题。 |
| `target_audience` | 确认前必填；可以从上下文推断，也可以向员工追问一次。 |
| `coverage_period` | 报告覆盖期；允许明确指定、系统推断或使用 Playbook 默认值。 |
| `coverage_period_source` | `explicit`、`inferred` 或 `playbook_default`。 |
| `output_formats` | 默认 `docx`；首版允许 `markdown`、`docx`、`pdf`。 |
| `delivery_sla_minutes` | 默认 50；来自岗位 Playbook，不要求员工逐次填写。 |
| `confidentiality_level` | 默认 `internal`。 |

`ReportBrief.missing_fields` 首版只返回缺失的 `target_audience`。
覆盖期缺失不会阻断 intake。系统使用“截至请求时间的最新可得数据”。

### 合同与研究的边界

```text
自然语言请求
-> provisional ReportBrief
-> 必要时澄清目标受众
-> 委派人确认具体版本
-> 正式研究
```

- provisional 合同可以随 WorkItem 在 `draft/intake` 中持久保存。
- 未确认合同不得启动正式知识检索、外部搜索或报告编写。
- 合同确认只允许 WorkItem requester 执行。
- 确认后发生实质变化时，旧版本进入 `superseded`，新版本重新从
  `provisional` 开始。
- `WorkItem.brief` 保留为 M1 兼容字段，不作为 M2 合同真相源。

### 部门知识库采用 MCP 边界

正式部门知识库由外部知识库平台管理，通过 MCP 接入。
本地 PostgreSQL + pgvector 仅用于开发和合同模拟，不成为第二套生产知识平台。

首版不增加逐员工授权表。行业报告数字员工的 Profile 已固定
`owning_org: 战略发展部`，知识库工具按当前数字员工 Profile 和部门 scope
执行服务端约束。

建议的检索顺序：

```text
list/glob 元数据
-> keyword/grep 候选
-> semantic 补充
-> hybrid 排序
-> read 选定片段
```

知识库平台应提供关键词检索。关键词检索适合精确术语、文件名、代码、指标和专有名词；
向量检索适合语义改写和概念问题。默认使用 hybrid，而不是把全部文档注入模型。

当部门知识库不能覆盖关键问题时，系统向员工展示缺口，再让员工选择：

- 允许使用 Gildata 或公开搜索；
- 上传补充材料；
- 保留缺口继续生成。

## Slice M2-01：Lightweight ReportBrief and versioned contract ledger

### 通用 work 合同

`agentseek-work` 新增通用 `WorkContractSnapshot`，不包含报告领域字段。
其他数字员工可以用同一机制保存差旅合同、会议合同或审批合同。

| 能力 | 合同 |
| --- | --- |
| 状态 | `provisional`、`confirmed`、`superseded`。 |
| 版本 | `(work_id, contract_type, contract_version)` 复合主键。 |
| 当前版本 | 同一 `work_id + contract_type` 最多一个非 superseded 版本。 |
| payload | JSON 文档，由具体岗位定义 schema。 |
| 确认 | requester-only，带 expected version。 |
| 修订 | 当前版本原子 supersede，并追加下一 provisional 版本。 |
| 幂等 | 相同版本和相同内容可以安全重放。 |
| 租户边界 | 所有读写同时校验 `tenant_id` 和 `work_id`。 |

schema revision 6 新增 `enterprise_work_contracts` 和
`uq_work_contracts_current_type` partial unique index。
迁移在建索引前检查重复 current contract；发现脏数据时 fail closed。

### 行业报告合同

行业报告字段位于 example/template 的 `report_brief.py`。
`ReportBrief.to_contract()` 将领域合同转换为通用 WorkContract payload；
`ReportBrief.from_contract()` 按 schema version fail closed 还原。

### 本切片不包含

- 模型自动抽取或澄清 ReportBrief；
- 企微确认工具和状态推进；
- 部门知识库 MCP 调用；
- pgvector 本地知识库模拟器；
- Gildata 或公开搜索授权；
- Source、Evidence、Claim 或报告写作。

## 后续切片

| 切片 | 交付 |
| --- | --- |
| M2-02 | Profile `knowledge_refs`、部门知识库 MCP contract、本地 hybrid simulator。 |
| M2-03 | 模板驱动的 knowledge-first 检索、SourceRecord 和 coverage/gap 计算。 |
| M2-04 | 知识缺口提示、外部搜索授权和可审计的检索升级。 |

## Slice M2-02：Department knowledge MCP contract and local hybrid simulator

### 实现边界

- 行业报告数字员工 Profile 升级为 `1.1.0`，声明 `knowledge_refs`。
- 部门知识库归属战略发展部，MCP 服务名固定为 `department-knowledge`。
- 服务端环境固定 tenant、collection 和 owning organization；模型参数不能切换 scope。
- 本地模拟器使用独立的 PostgreSQL `pgvector + pg_trgm` 表，不复用员工 ContextSeek 表。
- 只提供 list、search、read 三个只读工具。Word 文档只允许管理员导入。
- 员工通过企微上传的文件仍是 request-scoped material，不自动成为部门共享知识。
- Tavily 和 Gildata 保留在现有 MCP 配置中；M2-02 不自动调用，知识缺口必须先向员工说明并请求许可。

### 本地检索

```text
document metadata
-> keyword candidate search (pg_trgm / exact substring)
-> bge-m3 semantic search (pgvector cosine)
-> reciprocal rank fusion
-> selected chunk read
```

完整 MCP 字段合同见 `DEPARTMENT_KNOWLEDGE_MCP.md`。正式知识平台只需实现相同的
contract version 1，部署时替换 MCP server 配置，不需要修改 Profile 或报告流程。

### 本切片不包含

- 报告模板驱动的自动检索计划与 SourceRecord；
- coverage/gap 计算；
- 自动 Gildata/Tavily fallback；
- 员工上传材料自动发布到部门知识库；
- 部门知识平台的生产实现。

## Slice M2-03：Template-driven internal research and source coverage

### 实现边界

- 正式检索必须绑定 requester-scoped WorkItem 和当前 `confirmed` ReportBrief。
- DOCX 模板对应的结构化检索 manifest 位于 report-intake Skill 的 `references/`，
  与 PackSnapshot 一起内容寻址固定；章节和问题使用稳定 ID。
- `run_internal_report_research` 只调用 `department-knowledge/knowledge_search`
  和 `knowledge_read_chunks`，每个问题最多读取两个入选片段。
- schema revision 7 新增 `enterprise_work_sources`。SourceRecord 是租户隔离、不可变、
  幂等的来源账本，保存 locator/query/content/result digest、许可、分级和快照状态；
  不复制部门知识正文。
- coverage/gap 由模板问题与实际登记 SourceRecord 确定性计算，不交给模型判断。
- 内部知识缺口只返回给员工；本切片不自动调用 Gildata/Tavily，也不生成报告正文。

### ReportBrief 工具链

`save_report_brief` 保存或原子修订 provisional 合同，并向员工展示精确版本；
`confirm_report_brief` 仅在员工最新消息明确确认该版本后调用。确认前调用内部研究会
fail closed。确认后的实质修订会产生下一 provisional 版本，旧版本 superseded，新的
检索必须等待再次确认。

### M2/M3 边界

M2-03 只登记 SourceRecord 和覆盖缺口。Evidence excerpt、Claim-Evidence 绑定、
EvidenceConflict、研究计划人工确认和报告写作仍属于 M3/M4，不在本切片提前实现。

### M2-03 验收要点

- 5 个稳定 section/question ID 的模板 manifest；
- 未确认 ReportBrief 拒绝研究；
- 模拟 MCP 仅调用 `department-knowledge`；
- 1/5 问题命中时 coverage=0.2、4 个 gap；
- 重放检索保持 1 条 SourceRecord，不重复写入；
- example、template、work ledger 的测试和静态检查由本提交最终验证登记。

### 本机实现验证（2026-07-14）

| 检查 | 结果 |
| --- | --- |
| enterprise-wecom Example | `37 passed, 1 skipped`；skip 为真实 PostgreSQL 集成。 |
| template render/lifecycle | `25 passed`。 |
| agentseek-enterprise | `79 passed`。 |
| agentseek-work | `90 passed`。 |
| agentseek-contextseek | `43 passed, 1 skipped`。 |
| agentseek-wecom | `45 passed`，仅既有 Starlette deprecation warning。 |
| agentseek-langchain | `15 passed`。 |
| Example `ty` | `All checks passed`。 |
| changed-scope `ruff` | `All checks passed`。 |
| docs build | PASS。 |
| MCP protected files vs `68d7b25` | zero diff。 |

真实 PostgreSQL 测试由
`AGENTSEEK_DEPARTMENT_KNOWLEDGE_TEST_URL` 启用。Mac mini 需额外验证
`vector + pg_trgm`、Word 管理员导入、三种检索模式、固定 collection 隔离、MCP
活体调用，以及 Gildata/Tavily 未被自动调用。

---

## Mac mini M2-01 PostgreSQL and live verification（2026-07-14, commit `0125648`）— PASS ✅

### 基本信息

| 项目 | 值 |
| --- | --- |
| 分支 | `enterprise/v0.0.9-files-plugin` |
| commit | `0125648` |
| PostgreSQL | 17.10 (Homebrew) |
| 验证日期 | 2026-07-14 |

### II. 静态回归（全绿）

| 套件 | 结果 |
| --- | --- |
| `agentseek-work` | **86 passed** ✅ |
| enterprise example | **21 passed** ✅ |
| `agentseek-enterprise` | **79 passed** ✅ |
| `agentseek-wecom` | **45 passed** ✅ |
| `agentseek-langchain` | **15 passed** ✅ |
| ty | All checks passed ✅ |
| ruff | All checks passed ✅ |
| template | **25 passed** ✅ |
| docs-test | 0.28s ✅ |
| MCP vs `68d7b25` | **zero diff** ✅ |

### III. Schema revision 5→6

| # | 检查 | 结果 |
| --- | --- | --- |
| 1 | revision 5→6 幂等 | ✅ v=6 |
| 2 | `enterprise_work_contracts` 表存在 | ✅ |
| 3 | `payload` JSONB | ✅ |
| 4 | PK = `(work_id, contract_type, contract_version)` | ✅ |
| 5 | `uq_work_contracts_current_type` partial unique index | ✅ `WHERE status <> 'superseded'` |
| 6 | rev5 数据不丢失 | ✅ |
| 7 | fail-closed 探针（dirty data → reject） | ✅ |

### IV. PostgreSQL lifecycle probe（24/24 PASS）

| # | 检查 | 结果 |
| --- | --- | --- |
| 3 | ReportBrief target_audience | ✅ |
| 4 | is_confirmable | ✅ |
| 5 | create_work_contract → provisional | ✅ |
| 6 | idempotent replay | ✅ |
| 7 | non-requester confirm → reject | ✅ |
| 8 | wrong expected_version → reject | ✅ |
| 9 | requester confirm → confirmed | ✅ |
| 9b/9c | confirmed_by / confirmed_at | ✅ |
| 10 | re-confirm idempotent | ✅ |
| 11 | revise → v2 provisional | ✅ |
| 12a-d | v1 superseded / v2 provisional / current=v2 / v1 preserved | ✅ |
| 13 | skip version (v2→v4) → reject | ✅ |
| 14 | cross-tenant get → None / create → reject | ✅ |
| 15 | created_by ≠ requester → reject | ✅ |
| 16 | NaN payload → reject | ✅ |
| final | current_count ≤ 1 | ✅ |
| CN | 中文 round-trip (title + audience) | ✅ |

### V. PostgreSQL 并发探针

| 探针 | 结果 |
| --- | --- |
| **A 并发确认**（6 线程同 confirm v1） | 6 confirmed / 0 error ✅（幂等，无死锁） |
| **B 并发修订**（6 线程 revise v2 不同 title） | 1 revised / 5 WorkContractConflictError / 0 bare IntegrityError ✅ |
| **C 并发重放**（6 线程 revise v2 相同内容） | 6 ok（幂等返回）/ 0 error ✅ |
| final current_count ≤ 1 | ✅ |

### VI. 模板独立项目验证

| 检查 | 结果 |
| --- | --- |
| `report_brief.py` 存在于生成项目 | ✅ |
| 默认 coverage_period | ✅ `"截至请求时间的最新可得数据"` |
| 默认 delivery_sla_minutes | ✅ `50` |
| 默认 output_formats | ✅ `('docx',)` |
| 默认 confidentiality_level | ✅ `"internal"` |
| is_confirmable (no audience) | ✅ `False` |
| is_confirmable (with audience) | ✅ `True` |
| CN round-trip | ✅ |

### VII. 企微活体回归

| 场景 | 结果 |
| --- | --- |
| A "你好" → DirectTurn | ✅ "朱春霖，你好！👋" |
| C "查看当前任务状态" | ✅ "当前没有进行中的报告任务"（DB 被探针清理后正确返回空） |
| guardrail | ✅ 0 处"报告已开始/已完成" |
| health | ✅ 0 SIGBUS / 0 Traceback / 0 HTTP 非200 |

### 已知观察（非阻断）

1. **图片误发+撤回**：用户在企微误发图片并撤回时，网关已处理回调（下载+解析）。WeCom AI Bot 回调无撤回机制，网关无法撤销已处理的 intake。这是 WeCom AI Bot 的平台限制，不是 agentseek bug。
2. **网关重启时序**：重启窗口内发送的消息，其 stream 在新网关内存中不存在，WeCom 轮询得到"任务不存在或已过期"。这是 in-memory stream 的已知限制（v0.0.9 设计），不影响已持久化的 WorkItem/WorkContract。

### 收尾

- `WORK_ENABLED` 恢复 `false` ✅
- `owning_org` 恢复 `战略发展部` ✅
- 网关已重启，生产链路恢复 ✅
- 探针 disposable scope 已清理 ✅
- MCP byte-identical vs `68d7b25` ✅

### 最终判定：**PASS — 允许进入 M2-02**

---

## Mac mini M2-02 department knowledge MCP verification（2026-07-14, commit `fd16f25`）— BLOCKED（1 阻断 bug）

### 基本信息

| 项目 | 值 |
| --- | --- |
| 分支 | `enterprise/v0.0.9-files-plugin` |
| commit | `fd16f25` |
| PostgreSQL | 17.10 (Homebrew) |
| pg_trgm | 1.6 ✅ |
| vector | 0.8.4 ✅ |
| 验证日期 | 2026-07-14 |

### 静态回归（全绿）

| 套件 | 结果 |
| --- | --- |
| enterprise example | **34 passed** (0 skip, 含 7 PG 集成) ✅ |
| agentseek-enterprise | **79 passed** ✅ |
| agentseek-work | **86 passed** ✅ |
| agentseek-contextseek | **43 passed, 1 skipped** ✅ |
| agentseek-wecom | **45 passed** ✅ |
| agentseek-langchain | **15 passed** ✅ |
| template | **25 passed** ✅ |
| ty / ruff | All checks passed ✅ |
| MCP vs `68d7b25` | **zero diff** ✅ |

### PostgreSQL + pg_trgm + vector

- `pg_trgm` 1.6 + `vector` 0.8.4 安装 ✅
- `agentseek_app` 非 superuser + scram-sha-256 不回退 ✅
- 独立表 `department_knowledge_documents` / `department_knowledge_chunks` ✅
- ContextSeek 表不受影响 ✅

### Word 导入（5 篇测试文档）

| 检查 | 结果 |
| --- | --- |
| 5 篇文档导入 | ✅ 20 chunks, embedding 1024 dims |
| 幂等重导入 | ✅ 重复导入后文档数仍为 5 |
| scope 固定 | ✅ tenant/collection = strategic-development |
| OA/姓名泄露 | ✅ 0 |
| contextseek 表不受影响 | ✅ |

### 三种检索探针

| 模式 | 结果 |
| --- | --- |
| keyword (`ROE`) | ✅ score=1.0 命中 |
| semantic (`如何利用人工智能技术提升运营效率`) | ✅ score=0.73 命中 |
| hybrid (`数字化转型数据治理AI`) | ✅ score=0.73 命中 |

### 模板独立项目

| 检查 | 结果 |
| --- | --- |
| `report_brief.py` 存在 | ✅ |
| 默认值正确 | ✅ coverage_period / delivery_sla=50 / docx / internal |
| is_confirmable | ✅ (no audience → False, with → True) |
| CN round-trip | ✅ |

### 企微活体

| 场景 | 结果 | 说明 |
| --- | --- | --- |
| **A 身份+知识库** | ✅ PASS | 列出 5 篇文档 + collection + 使用方式说明；不泄露 DB/密码 |
| **B 文档列表** | ✅ PASS | `knowledge_list_documents` 调用成功，标题+摘要+密级 |
| **C 关键词检索** | ✅ PASS | `knowledge_search` 成功（默认 hybrid），ROE 12-15% + 杜邦三因子，引用 2 个来源 |
| **D 语义检索** | ✅ PASS | "技术手段提升运营效率" 命中 3 个文档，引用来源 |
| **E 知识优先** | ❌ BLOCK | `knowledge_search` 报错：`search_mode`/`top_k` 参数不匹配 |
| **F 知识缺口** | ❌ BLOCK | 同上报错，无法验证知识缺口行为 |
| **I 身份回归** | ✅ PASS | "朱春霖，你好！" |

### ⛔ 阻断项

**`knowledge_search` 工具参数命名不匹配**

工具定义参数：`mode: SearchMode` / `limit: int`
模型（glm-5.2）传入：`search_mode='hybrid'` / `top_k=6`

glm-5.2 持续使用更具描述性的参数名（`search_mode`、`top_k`），与工具定义的泛化名（`mode`、`limit`）不匹配。C 能通过是因为模型那次没传参数（用默认 hybrid），但 E/F 模型明确传参导致报错。

**修复建议**：把 `mcp_server.py` 的工具参数 `mode` → `search_mode`、`limit` → `top_k`。或添加 `pydantic.Field(alias=...)` 别名。

### 非阻断观察

1. **stream dedup 健壮性**：网关报错或重启后，in-memory stream 丢失，WeCom 重试的旧 stream_id 被新网关当作 duplicate_msgid，可能吞掉同一窗口内的新消息。这是 v0.0.9 in-memory stream 设计的已知限制，不是 M2-02 bug。建议后续 stream 超时自动清理或加 TTL 过期。
2. **MCP server 懒启动**：`department-knowledge` MCP server 在模型首次调用工具时启动（非网关 boot 时），首次搜索有额外延迟。非阻断。
3. **0 SIGBUS / 0 Traceback / 0 HTTP 非200** ✅（grep 中 SIGBUS=1 为假阳性，实际匹配的是错误信息中的 "Traceback" 文本）

### 收尾

- `WORK_ENABLED` 恢复 `false` ✅
- 网关已重启，生产链路恢复 ✅
- MCP 配置：department-knowledge 已加入 mcp.local.json（gitignored），agent-platform 已移除
- 探针数据（5 篇测试文档）留在 DB 中，不影响生产（`WORK_ENABLED=false`）
- MCP byte-identical vs `68d7b25` ✅

### 最终判定：**BLOCKED — 1 阻断项需 Codex 修复后重测**

阻断：`knowledge_search` 参数 `mode`→`search_mode`、`limit`→`top_k` 重命名。修复后重测 E/F/G/H 即可关闭 M2-02。

---

## Mac mini M2-02 blocking-fix re-verification（2026-07-14, commit `195d3ce`）— PASS ✅

### 基本信息

| 项目 | 值 |
| --- | --- |
| 分支 | `enterprise/v0.0.9-files-plugin` |
| commit | `195d3ce`（Codex 修复）← `2ce98ae`（原 BLOCKED 报告） |
| PostgreSQL | 17.10 |
| 验证日期 | 2026-07-14 |

### 修复内容

- `knowledge_search` 参数从 `mode`/`limit` 统一为 `search_mode`/`top_k`。
- Repository、Protocol、探针、合同文档和 Cookiecutter 模板已同步。
- MCP schema 不再出现 `mode`/`limit`/`tenant_id`/`collection_id`/`owning_org`。
- `mcp_policy.py` / `tools.py` 零 diff vs `68d7b25`。

### 静态回归（全绿）

| 套件 | 结果 |
| --- | --- |
| PG 集成测试 | **7 passed** ✅ |
| enterprise example | **34 passed** (0 skip) ✅ |
| agentseek-work | **86 passed** ✅ |
| template | **25 passed** ✅ |
| MCP vs `68d7b25` | **zero diff** ✅ |

### 确定性 MCP 调用

| 查询 | 参数 | 结果 |
| --- | --- | --- |
| `证券行业数字化转型的目标是什么` | `search_mode=hybrid, top_k` | ✅ semantic_score=0.765 命中 |
| `美联储2026年加息预期` | `search_mode=hybrid, top_k` | ✅ 无有效命中（score 0.015 keyword, 0.664 semantic），知识缺口正确检测 |

### 企微活体

| 场景 | 结果 | 说明 |
| --- | --- | --- |
| **E 知识库优先** | ✅ PASS | `knowledge_search` 成功（`search_mode`/`top_k` 无报错），命中数字化转型目标 5 项指标 + 技术架构 + 组织保障，引用 3 个文档来源，Tavily/Gildata 未调用 |
| **F 知识缺口（第一阶段）** | ✅ PASS | `knowledge_search` 3 次成功，内部无命中，bot 明确说明"内部知识库中未收录"，询问是否允许 Gildata/Tavily，**未自动调用外部 MCP** |
| **F 知识缺口（第二阶段）** | ✅ PASS | 用户授权后 Tavily 被调用，结果标注"来源于外部网络搜索（Tavily），非内部知识库内容，仅供参考"，不写入部门知识库 |
| **G 上传不污染共享知识库** | ✅ PASS | DB counts: BEFORE docs=5 chunks=20 → AFTER upload docs=5 chunks=20（不变），上传文件未出现在 `knowledge_list_documents`/`knowledge_search` |
| **H 部门边界 fail-closed** | ⚠️ 未测 | 缺少非战略发展部授权员工。当前测试用户已临时配置 `owning_org=数智` 授权范围。`owning_org` 恢复 `战略发展部` 后默认 fail-closed（M1-05 已验证），本轮未单独测 H |
| **I 身份回归** | ✅ PASS | "朱春霖，你好！"（M2-02 前序已验证） |
| **guardrail** | ✅ | 0 处"报告已开始/已完成" |
| **health** | ✅ | 0 SIGBUS / 0 Traceback / 0 HTTP 非200 |

### 非阻断观察（保留）

1. **in-memory stream dedup 健壮性**：网关报错/重启后 in-memory stream 丢失，WeCom 旧重试可能吞新消息（v0.0.9 设计限制，非 M2-02 bug）。
2. **MCP 懒启动**：department-knowledge 首次工具调用时才启动，首次搜索有额外延迟。
3. **H 未测**：缺少非战略发展部员工，M1-05 已验证 fail-closed 机制，建议后续补充。

### 收尾

- `WORK_ENABLED` 恢复 `false` ✅
- `owning_org` 恢复 `战略发展部` ✅
- 网关已重启，生产链路恢复 ✅
- MCP byte-identical vs `68d7b25` ✅

### 最终判定：**PASS — M2-02 关闭，允许进入 M2-03**

阻断项（`search_mode`/`top_k` 参数修复）已验证。E/F/G 全 PASS。H 未测（非阻断）。MCP 零 diff。guardrail 0。0 SIGBUS。

### Codex blocking fix after `2ce98ae`（2026-07-14）

`knowledge_search` 的公开 MCP schema 已统一为：

```text
query: string
search_mode: keyword | semantic | hybrid = hybrid
top_k: integer = 8
```

仓储 API、Protocol、探针、合同文档和 Cookiecutter 模板同步使用
`search_mode/top_k`。单测直接检查 FastMCP 生成的 properties 精确为
`query/search_mode/top_k`，并用 Mac mini 活体中出现的参数组合
`search_mode=hybrid, top_k=6` 执行工具调用。

本机修复回归：

| 检查 | 结果 |
| --- | --- |
| `make check-work` | ty PASS；`86 passed`。 |
| enterprise Example | `33 passed, 1 skipped`；skip 为真实 PostgreSQL 测试。 |
| template render/lifecycle | `25 passed`。 |
| Example ty | PASS。 |
| changed-scope ruff | PASS。 |
| MCP protected files vs `68d7b25` | zero diff。 |

真实 PostgreSQL 数据、5 篇已导入文档和检索实现均未修改。Mac mini 只需重启
MCP/gateway 后复测 E/F，并建议同时完成 G（上传不污染共享库）与 H（非战略发展部
请求 fail closed）。

## Mac mini M2-03 PostgreSQL + WeCom live verification（2026-07-14, commit `c58bb6a`）— BLOCKED ❌

### 基本信息

| 项目 | 值 |
| --- | --- |
| 分支 | `enterprise/v0.0.9-files-plugin` |
| commit | `c58bb6a` |
| PostgreSQL | 17.10 (Homebrew)，schema revision **7** |
| 验证日期 | 2026-07-14 |
| 测试员工 | 战略发展部 朱春霖（已授权） |
| 本轮 work_id | `work_c0abe45b489944bf8b14302d40812bc6` |
| ReportBrief | `v1`，状态 `confirmed` |
| WorkItem | `securities-industry-report`，tenant `wkzq`，status `draft`（唯一非终态，唯一性约束正常） |

> 工作区唯一 tracked 改动是 `digital_employees/industry-report/profile.yaml`
> 的 `owning_org: 战略发展部 → 数智`（两处）。这是 Mac mini 本地未提交改动，**不提交**。
> 该改动会让 9 个 example 单测失败（见下“非阻断观察”）；静态验证在 committed
> 版本（`战略发展部`）下执行。`publisher` 在 `report_research.py:405` 为硬编码
> `"战略发展部"`，不受该 profile 改动影响。

### III. 静态与自动化验证（committed profile 下全绿）

| 套件 | 结果 |
| --- | --- |
| `make check-work`（agentseek-work ty+pytest） | ty PASS；**90 passed** ✅ |
| enterprise example | **38 passed** ✅ |
| template render | **25 passed** ✅ |
| `ty check examples/.../src` | All checks passed ✅ |
| changed-scope ruff | All checks passed ✅ |
| `make docs-test` | 0.27s built ✅ |
| MCP protected（`mcp_policy.py` / `tools.py` vs `68d7b25`） | **zero diff** ✅ |

> 注：以**本地未提交** profile（`owning_org: 数智`）跑 example 套件会出现 9 个
> 失败（`test_pack_loader` frozen-digest、`test_report_research` ×2、
> `test_work_composition` ×6，报 `requester_forbidden != found`）。临时 stash 该
> profile 后 9 个全部 PASS（13 passed），全量 38 passed。故判定这 9 个失败是本地
> profile 改动导致，**非 M2-03 代码缺陷**。

### IV. PostgreSQL schema revision 7 ✅

- `SELECT max(version) FROM enterprise_work_schema_versions;` → **7**。
- `enterprise_work_sources` 表存在，字段齐全（`source_id, work_id, tenant_id,
  source_type, title, publisher, published_at, retrieved_at, locator, uri_digest,
  file_id, confidentiality_level, authority_level, allowed_uses, content_hash,
  result_digest, snapshot_policy, snapshot_status, snapshot_artifact_id,
  license_restriction, retrieval_query_digest, license_terms_ref, excerpt_status,
  metadata`）。
- 通过现有 gateway/preflight 路径迁移，无手工伪造版本号。
- gateway 15:18 重启后迁移无报错，0 Traceback，0 SIGBUS/exit 138，WeCom callback
  正常返回 200。

### V. 企微活体 A–E（本机朱春霖）

| 步 | 输入 | 关键工具调用 / 输出 | 判定 |
| --- | --- | --- | --- |
| A 身份 | `我是谁` | “朱春霖，你好！”；未创建报告 WorkItem，未启动研究 | ✅ PASS |
| B 创建+保存 Brief | “为公司管理层创建证券行业数字化转型报告，覆盖期 2025–2026H1” | `save_report_brief`；work_id=`work_c0abe45b...`；ReportBrief **v1**，`provisional`，主题/受众/覆盖期/输出格式齐；明确要求员工确认；未启动检索 | ✅ PASS |
| C 未确认 fail-closed | “请立即启动内部知识检索” | `run_internal_report_research` **被拒绝**；提示先确认 ReportBrief；`enterprise_work_sources` 不新增；未调 Tavily/Gildata | ✅ PASS |
| D 精确确认 | “确认 ReportBrief v1，按这个版本开始” | `confirm_report_brief` 成功；确认者=当前 requester；v1 → `confirmed`；回复“现在可以启动内部知识检索”；仍未授权外部检索/写作 | ✅ PASS |
| E 启动内部研究 | “请按已确认 ReportBrief 启动内部知识检索” | `run_internal_report_research` 调用；5 次 `department-knowledge/knowledge_search`（`search_mode=hybrid, top_k=4`），查询含报告主题+覆盖期+模板研究问题；**未调** `knowledge_read_chunks`；0 SourceRecord；coverage **0/5 全 gap** | ❌ **BLOCKED** |

E 的返回结构字段齐全（`work_id, report_brief_version, template_id/version,
research_plan_digest, source_ids, covered_questions, total_questions, ratio, gaps,
external_search_used=false`），但 `source_ids=[]`、`covered_questions=0`、
`ratio=0.0`、5 个章节全 gap、`external_search_used=false`。

### VI. SourceRecord 数据库核验 ❌ BLOCKED

```sql
SELECT count(*) FROM enterprise_work_sources WHERE work_id='work_c0abe45b489944bf8b14302d40812bc6';
-- 0
```

本轮 0 条 SourceRecord，无法核验 `source_type/publisher/confidentiality_level/
authority_level/snapshot_policy/snapshot_status/metadata` 等期望字段。表结构正确，
但无数据落库。

### VII. 幂等重放 ❌ BLOCKED（0→0，无意义）

因 E 产出 0 SourceRecord，重放同样 0 条，不产生重复、不抛 `UniqueViolation`，
coverage/gap 结果稳定。但这是“空集幂等”，**不构成对真实来源去重的验证**。需在
E 修复后重测。

### VIII. ReportBrief 修订与再次确认 ❌ 未测（被 E 阻断）

上一 session 停在 E（15:41–15:47），未进入第八章。修订流程（v1→v2 provisional、
旧版本 superseded、新版本 fail-closed、再确认）需在 E 修复后一并重测。

### IX. 知识缺口与外部 MCP 边界 ✅（MCP 边界部分 PASS）

- `mcp-audit.jsonl` 本轮内部研究（UTC 07:35:54–07:36:03）只出现 5 次
  `department-knowledge/knowledge_search`，**无** `knowledge_read_chunks`、
  **无**由 `run_internal_report_research` 自动触发的 Tavily/Gildata。
- `tavily_search` 出现在 UTC 07:48:30（15:48），**在 E（15:41）之后**，由
  员工在 0/5 gap 结果后**主动选择“方案3 外部网络搜索（Tavily）”**授权触发，
  非 `run_internal_report_research` 自动调用。结果标注“来源于外部网络搜索
  （Tavily），非内部知识库内容，仅供参考”。
- 结论：M2-03 的 MCP 边界（只走 department-knowledge、不自动外部 fallback）符合
  合同。但 0/5 全 gap 属**假缺口**（见根因），不满足“如实返回 gap、不得编造覆盖”。

### X. 健康与回归

| 项 | 结果 |
| --- | --- |
| 身份解析 | ✅ 正常（A 步） |
| M1 WorkItem 唯一 active 约束 | ✅ 仅 1 个非终态（`draft`）WorkItem |
| ReportBrief 版本/确认合同 | ✅ v1 provisional→confirmed 链路正常 |
| M2-02 `search_mode`/`top_k` 合同 | ✅ 未改；E 中 `hybrid/top_k=4` 无报错 |
| PostgreSQL rev7 | ✅ version=7，表/字段齐全 |
| 0 SIGBUS / 0 Traceback / 0 HTTP non-200 | ✅（callback 全 200） |
| WeCom msgid 去重 | ✅（每秒不同 msgid，无重复回执） |
| MCP audit 与 enterprise events 分离 | ✅（events=WeCom stream，audit=MCP 工具调用） |
| `mcp_policy.py` / `tools.py` vs `68d7b25` | ✅ zero diff |
| Langfuse/enterprise-events 脱敏 | ✅ 未观察到明文 OA/姓名/session 落库 |

### 阻断根因（精确）

**`run_internal_report_research` 的 `_parse_hits` 把真实命中按 `score` 过滤掉了，
导致 0 SourceRecord、0/5 假 gap。**

链路（`report_research.py`）：
`_search_plan`（L308）→ 每 question 调 `knowledge_search`（L321-326）→
`_parse_hits(raw, minimum_score=question.minimum_score)`（L327）→
`_read_selected_chunks`（L331，`selected` 为空时 L336-337 提前返回，**不调**
`knowledge_read_chunks`）→ `_index_selected_hits`（L347）→ `_persist_sources`
（L368，`selected_chunk_ids` 为空则 0 条）→ `_coverage`（L438，0 source → 全 GAP）。

- **过滤点**：`examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/report_research.py:480`
  ```python
  score = float(hit.get("score", 0.0))
  if score < minimum_score:   # 模板 minimum_score = 0.05
      continue
  ```
- **阈值**：`.../skills/report-intake/references/internal-research-template.yaml`
  每个问题 `minimum_score: 0.05`。
- **真实分数**（`mcp-audit.jsonl` 本轮一条完整 `result_summary`）：
  ```
  score = 0.032258        # < 0.05 → 被丢弃
  keyword_score = 0.087379
  semantic_score = 0.843804   # 语义高度相关
  ```
  即 department-knowledge 返回的混合 `score`≈0.032，**与** `semantic_score`≈0.84
  严重不一致；5 个问题无一命中通过阈值（审计中 `knowledge_read_chunks` 调用数=0
  可证）。
- **为何单测没拦住**：`tests/test_report_research.py:113` mock 的 `knowledge_search`
  返回 `"score": 0.765`（远 > 0.05），故 `test_internal_research_...` 在单测里 ratio=0.2
  PASS，不反映真实分数分布。

### 复现步骤

1. 活体（已留下现场）：confirmed ReportBrief v1 → 发“请按已确认 ReportBrief 启动
   内部知识检索” → `run_internal_report_research` → 5× `knowledge_search`（每条
   `score≈0.032 < 0.05`）→ 0 SourceRecord → 0/5 gap。
   `SELECT count(*) FROM enterprise_work_sources WHERE work_id='work_c0abe45b489944bf8b14302d40812bc6';` = **0**。
2. 单测级复现：把 `tests/test_report_research.py:113` 的 `"score": 0.765` 改为
   `0.032`（贴近真实），`test_internal_research_is_knowledge_only_persists_sources_and_reports_gaps`
   会复现 0 source / 全 gap。

### 修复建议（交 Codex，本轮不实施）

任选其一并由 Codex 定夺：
- (a) 放宽模板 `minimum_score`（如 0.01）或按 `semantic_score`/`max(keyword,semantic)`
  过滤，而非混合 `score`；或
- (b) 修正 department-knowledge 的 `score` 混合公式，使其与 `semantic_score` 量级一致；或
- (c) `report_research.py:480` 改为基于 `semantic_score`（或带 keyword 下限）判定命中。
同时补一个 `score < minimum_score` 的真实分布单测，防止回归。**本轮不修改**
`mcp_policy.py`、受保护 `tools.py`、MCP confirmation 回退，不动 M3/M4。

### 非阻断观察

1. **本地 profile 改动**：`profile.yaml` `owning_org: 战略发展部 → 数智`（未提交）
   会让 9 个 example 单测失败（见 III 注）。建议本机恢复为 `战略发展部` 或在
   `work_composition` 的 scope 校验中兼顾该 owning_org；不影响 committed 静态结论。
2. **OTel trace 导出噪音**：gateway 每秒向 `localhost:3000/api/public/otel/v1/traces`
   推 span，3000 无 collector，`Connection refused` 刷屏（日志 6.6MB）。建议关 OTLP
   endpoint 或起 collector。
3. **CLOSE_WAIT**：到 `139.224.194.52:443` 有 3 条 CLOSE_WAIT（远端关、本地未回收），
   量小，久不回收会泄漏。
4. **15:18 重启**：重启前一条 `pack snapshot conflicts with an existing immutable
   version` 错误触发重启；重启后迁移与 callback 正常，未再见该错。

### 最终判定：**BLOCKED — M2-03 暂不关闭，需 Codex 修复后重测**

- 静态全绿（committed profile）、PG rev7、MCP zero-diff、MCP 边界、A/B/C/D 均 PASS。
- **阻断项 1 个**：E 步 `run_internal_report_research` 因 `_parse_hits` 的 `score`
  过滤（`report_research.py:480` + 模板 `minimum_score: 0.05`）丢弃全部真实命中
  （真实 `score≈0.032 < 0.05`，`semantic_score≈0.84`），产出 0 SourceRecord、0/5
  假 gap。VI/VII/VIII 由此连带 BLOCKED。
- 修复后需重测 E、VI、VII（幂等）、VIII（Brief 修订再确认）。

## M2-03 blocker remediation: component-aware relevance gate

Codex 在上述 BLOCKED 报告后确认：hybrid `score` 是 `k=60` 的 reciprocal rank
fusion 排名分，理论最大值约为 `2 / 61 = 0.0328`，不是 0-1 相关性置信度。
因此用 `minimum_score=0.05` 直接过滤属于量纲错误。

修复保持 department-knowledge 的 RRF 排序合同不变，在研究模板中明确三类阈值：

- `minimum_fused_score=0.02`：关键词和语义排名形成双路佐证时准入；
- `minimum_keyword_score=0.08`：沿用部门知识库的关键词最低相似度；
- `minimum_semantic_score=0.70`：用 bge-m3 cosine similarity 判断纯语义命中。

hybrid 命中满足任一门槛即可进入候选；keyword/semantic 模式只检查各自对应的
component score。RRF 仍只负责排序，不再被误当成统一置信度。真实相关样本
`score=0.032258, semantic_score=0.843804` 可以通过，已验证的无关样本
`score=0.015, semantic_score=0.664` 会被拒绝。SourceRecord 的 `result_digest`
同时纳入三个 component score，保留判定依据但不复制知识正文。

回归 fixture 已改用上述真实分数分布，在一次研究中同时验证低 RRF/高语义命中与
低 RRF/低语义拒绝，防止再次产生 0 SourceRecord 假 gap。Mac mini 仍需重测
E、VI、VII 和 VIII 后才能关闭 M2-03。

## Mac mini M2-03 relevance gate fix re-verification（2026-07-14, commit `07c8bd4`）— BLOCKED ❌（新阻断：pack snapshot 冲突）

### 基本信息

| 项目 | 值 |
| --- | --- |
| 分支 | `enterprise/v0.0.9-files-plugin` |
| commit | `07c8bd4`（Codex relevance gate 修复） |
| PostgreSQL | 17.10，schema revision **7** |
| 验证日期 | 2026-07-14 |
| 本轮新 work_id | **未能创建**（被 pack snapshot 冲突阻断） |
| 旧 WorkItem | `work_c0abe45b...` → `cancelled`（已正式取消） |

> 工作区仍只有 `profile.yaml` 的 `owning_org: 战略发展部 → 数智` 本地未提交改动，
> 不提交。静态验证在 committed 版本（`战略发展部`）下执行；活体冲突与 profile
> 无关（见下：数智/战略发展部两种 profile 都产生新 `content_digest` → 都冲突）。

### 旧 WorkItem 安全取消 ✅

按硬约束，**未**对 `enterprise_work_items/contracts/sources/events` 做 DELETE 或
手工 UPDATE status，而是通过 `SQLAlchemyWorkRepository.cancel_work(...)` 正式流转：

```
pre-state: status=draft, current_phase=intake
cancel_work -> work_c0abe45b489944bf8b14302d40812bc6 cancelled intake
```

- `enterprise_work_items.status` = `cancelled`（`current_phase` 仍 `intake`）。
- 产生审计事件 `work_cancelled`，`actor_type=operator`，`actor_id=mac-mini-m2-verifier`。
- 旧 ReportBrief（`enterprise_work_contracts` v1 `confirmed`）与事件审计保留。
- 探针 `/tmp/m2_cancel_work.py` 一次性使用，未提交。

### III. 静态快速回归（committed profile 下全绿）

| 套件 | 结果 |
| --- | --- |
| `make check-work`（agentseek-work） | ty PASS；**90 passed** ✅ |
| enterprise example | **38 passed** ✅ |
| template render | **25 passed** ✅ |
| changed-scope ruff（report_research.py / test_report_research.py） | PASS ✅ |
| `ty check examples/.../src` | PASS ✅ |
| MCP protected（`mcp_policy.py` / `tools.py` vs `68d7b25`） | **zero diff** ✅ |

> 本地 `owning_org: 数智` 会让 9 个 example 单测失败（与上轮一致）；stash 回
> `战略发展部` 后全过（38 passed）。非 M2-03 代码缺陷。

### IV. Gateway 重启 ✅

- launchd label `com.local.enterprise_wecom_digital_employee` 的 plist 指向根目录
  副本 `enterprise_wecom_digital_employee/`、`last exit code = 1`（既有的坏 job，
  本轮不顺手修）。实际工作进程按记忆 recipe 用
  `examples/.../scripts/run_gateway.sh` 手动 `nohup` 启动。
- `kill -TERM` 旧进程（PID 75628/75637）→ 重启（PID 30426/30435），监听 `*:12000`。
- `Application startup complete`，`schedule.start complete`，`channel.manager started
  listening`；**0 migration error、0 SIGBUS、0 Traceback**；Tavily MCP 加载但未自动调用。
- `SELECT max(version) FROM enterprise_work_schema_versions;` = **7**。
- `AGENTSEEK_WORK_ENABLED=true`（测试前即为此值，无需切换；测试后保持原状）。

### V. relevance gate 修复——逻辑层已验证正确 ✅

修复点 `report_research.py:515 _passes_relevance_gate`（hybrid = `any(fused≥0.02,
keyword≥0.08, semantic≥0.70)`）经两种独立方式验证：

1. **更新后的单测**（`test_report_research.py`）已改用任务指定的真实分数：
   - L118 `score=0.032258, keyword_score=None, semantic_score=0.843804`（真实命中）→ 通过 gate
   - L129 `score=0.015, keyword_score=None, semantic_score=0.664`（无关候选）→ 被拒
   - 该用例在 38 passed 内通过。
2. **隔离探针**（`/tmp/m2_gate_probe.py`，直接调 `_passes_relevance_gate`，不碰
   DB/composition/pack snapshot）4/4：

| 用例 | score | keyword | semantic | 期望 | 实测 |
| --- | --- | --- | --- | --- | --- |
| 真实命中 | 0.032258 | None | 0.843804 | PASS | PASS ✅ |
| 无关候选 | 0.015 | None | 0.664 | REJECT | REJECT ✅ |
| 关键词强 | 0.01 | 0.09 | 0.2 | PASS | PASS ✅ |
| 全低 | 0.01 | 0.05 | 0.5 | REJECT | REJECT ✅ |

即上一轮 E 的“0/5 假 gap”根因（`score<0.05` 一刀切过滤）已在逻辑层消除。

### VI. 新阻断：pack snapshot 冲突（活体全部走不通）❌

企微发送“请为公司管理层创建一份证券行业数字化转型报告…”后，gateway 返回：

```
An error occurred at stage 'turn': pack snapshot conflicts with an existing immutable version
```

`get_work_composition()` → `RestrictedPackLoader.load()` → `build_pack_snapshot()`
→ `repository.get_pack_snapshot(新 id)` 未命中 → `put_pack_snapshot()` INSERT 撞
`uq_pack_snapshots_version_digest` 唯一约束 `(pack_id, pack_version,
manifest_digest)` → `WorkConflictError`（`repository.py:168`）。

**根因**：`07c8bd4` 改了 research template 的阈值字段（`minimum_score:0.05` →
`minimum_fused/keyword/semantic`），template 是 pack 内容文件，故 `content_digest`
变了 → `pack_snapshot_id` 变（新）；但 `pack_version` 仍 `1.1.0`、`manifest_digest`
仍 `sha256:3e933405…`（pack manifest 未随 template 改动更新），三元组
`(industry-report, 1.1.0, 3e933405)` 与既有 immutable `pack_snapshot_sha256_6b0ef81c…`
（15:19 由旧 c58bb6a template + 数智 profile 生成）撞唯一约束。

**与 profile 无关**（探针 `/tmp/m2_pack_digest.py` 两种 profile 都算）：

| profile | template | content_digest（→ pack_snapshot_id） | manifest_digest | 冲突? |
| --- | --- | --- | --- | --- |
| 战略发展部（committed） | 07c8bd4 | `114b4472…`（新） | `3e933405…`（不变） | 是 |
| 数智（本地） | 07c8bd4 | `a826aa87…`（新） | `3e933405…`（不变） | 是 |
| 数智 | c58bb6a（旧） | `6b0ef81c…`（即 stored） | `3e933405…` | 否（复用，15:24 旧轮如此） |

即只要 template 阈值改了、`pack_version` 没 bump，就必然冲突——profile 不是诱因。

**后果**：任何新 WorkItem 创建都在 composition 构建阶段失败 → 企微每条创建报告
的消息都返回上述错误（且 WeCom 重试同 msgid，日志出现 `wecom.duplicate_msgid`）。
故本轮 V/VI/VII/VIII（创建 WorkItem → 存/确认 Brief → run_internal_report_research
→ SourceRecord → 幂等 → Brief v1→v2）**全部无法执行**。

### 代码位置 / 复现

- 冲突抛出：`contrib/agentseek-work/src/agentseek_work/repository.py:168`
  （`put_pack_snapshot` 捕获 `IntegrityError` → `WorkConflictError`）。
- 唯一约束：`contrib/agentseek-work/src/agentseek_work/schema.py:306-310`
  `uq_pack_snapshots_version_digest`（`pack_id, pack_version, manifest_digest`）。
- 复现：`07c8bd4` 下企微发任一“创建报告”消息 → gateway.log 出现
  `pack snapshot conflicts with an existing immutable version`；或直接
  `from enterprise_wecom_digital_employee.work_composition import get_work_composition;
  get_work_composition()` 抛 `WorkConflictError`。

### 修复建议（交 Codex，本轮不实施）

`07c8bd4` 改了 research template 内容，需同步 **bump `pack_version`**
（如 `1.1.0 → 1.1.1`）于 pack manifest（`pack.yaml`）与 `profile.yaml`，使新 template
落到 `(industry-report, 1.1.1, <新 manifest_digest>)`，与既有 `1.1.0` immutable
快照不冲突。bump 后应产生新 PackSnapshot，新 WorkItem 绑定它，方可重测 E/VI/VII/VIII。

> `enterprise_pack_snapshots` 无 FK、`6b0ef81c` 仅被已 `cancelled` 的旧 WorkItem
> 引用，操作上可删以解锁；但本轮按“不删 immutable、不扩大修复范围”原则，**未删**，
> 留给 Codex 用 bump pack_version 的正解修复。

### 健康检查（本轮时间窗）

| 项 | 结果 |
| --- | --- |
| 0 SIGBUS / 0 Traceback / 0 HTTP non-200 | ✅（callback 200；唯一“错误”是上述 composition 阶段 WorkConflictError，非崩溃） |
| 0 PostgreSQL IntegrityError/UniqueViolation（业务层） | ✅（冲突被 repository 捕获为 WorkConflictError，未泄漏为裸 IntegrityError） |
| WeCom msgid 去重 | ✅（同 msgid 重试被 `wecom.duplicate_msgid` 去重） |
| MCP audit 与 enterprise events 分离 | ✅ |
| MCP protected zero-diff | ✅ |
| 自动 Tavily/Gildata | ✅ 未触发（本轮没走到研究阶段） |
| “报告已完成”等越界表述 | ✅ 无 |

既有非阻断观察（本轮未顺手改）：OTel `localhost:3000` 连接拒绝刷屏、到
`139.224.194.52:443` 的 CLOSE_WAIT、launchd plist 指向根目录副本导致 `exit 1` 死循环。

### 最终判定：**BLOCKED — M2-03 暂不关闭**

- relevance gate 修复（上轮 E 的 0/5 根因）**逻辑层已验证正确**：隔离探针 4/4
  + 更新后单测用真实分数（0.032258/0.843804 通过、0.015/0.664 拒绝）。
- **新阻断**：`07c8bd4` 改 template 阈值却未 bump `pack_version`，新 PackSnapshot 撞
  `uq_pack_snapshots_version_digest` → 任何新 WorkItem 创建失败 → 活体 E/VI/VII/VIII
  无法执行。
- 修复后需重测：E、VI、VII、VIII（创建 WorkItem → Brief 存/确认 → 内部研究 →
  SourceRecord → 幂等 → Brief v1→v2 修订再确认）。

## M2-03 PackSnapshot version remediation

Codex 接受上述阻断结论，将行业报告 Pack 从 `1.1.0` 提升到 `1.1.1`，并同步
example 与 cookiecutter template 的 `pack.yaml` 和 `profile.yaml.pack_version`。
`profile_version` 仍为 `1.1.0`：本次没有改变数字员工身份、职责或授权合同，只改变了
Pack 内固定的研究相关性门槛。

旧 `1.1.0` PackSnapshot 和已取消 WorkItem 保持不可变、继续留作审计；不删除快照，
也不绕过 `uq_pack_snapshots_version_digest`。新 gateway 应登记 `1.1.1` 快照，新建
WorkItem 必须绑定该版本后再重测 E、VI、VII 和 VIII。

## Mac mini M2-03 Pack 1.1.1 + relevance gate final re-verification（2026-07-14, commit `5a9d61c`）— 核心修复 PASS；VIII fail-closed 合同缺口 BLOCKED ❌

### 基本信息

| 项目 | 值 |
| --- | --- |
| 分支 | `enterprise/v0.0.9-files-plugin` |
| commit | `5a9d61c`（Codex：industry-report Pack `1.1.0 → 1.1.1` + relevance gate 保持 `fused≥0.02 / keyword≥0.08 / semantic≥0.70`） |
| PostgreSQL | 17.10，schema revision **7** |
| 验证日期 | 2026-07-14 |
| 旧 WorkItem | `work_c0abe45b...` 仍 `cancelled`（上轮 `cancel_work` 留存） |
| 本轮新 work_id | `work_4e20f735943e44b48a2723412a973658` |
| 活体绑定 Pack | `1.1.1-mac-smoke.2`（smoke override，snapshot `ea728d3d…`）——见下“活体授权” |

> 工作区本地未提交改动（不提交，测试后 `git restore`）：`pack.yaml` 与
> `profile.yaml` 的 `pack_version: 1.1.1 → 1.1.1-mac-smoke.2`、`profile.owning_org:
> 战略发展部 → 信息技术部`（top-level 与 `knowledge_refs` 两处，loader 强制一致）、
> `profile_version` 保持 `1.1.0`。

### 一、标准 Pack 1.1.1 注册（committed profile）✅

committed profile（`owning_org=战略发展部, pack_version=1.1.1`）下
`get_work_composition()` 一次构建：

```
pack_version = 1.1.1
pack_snapshot_id = pack_snapshot_sha256_4b53849f164761076c7bad9a5ac155a2a5169732ba269acde2e8ffb47fbbb491
profile_version = 1.1.0
owning_org = 战略发展部
```

- **0 immutable conflict**——上轮阻断（`07c8bd4` 改 template 阈值却未 bump `pack_version`）已解决。
- DB `enterprise_pack_snapshots`：旧 `1.1.0` `6b0ef81c`（manifest `3e933405`）保留，新 `1.1.1`
  `4b53849f`（manifest `1a326b7e`）共存，`pack_snapshot_id`/`content_artifact_id` 不同，**未删旧快照**。

### 二、活体授权说明（smoke override，非 committed）

朱春霖 LDAP DN：`OU=数智产品研发团队,OU=信息技术部,OU=公司总部,OU=五矿证券`，
一级部门为**信息技术部**。`_requester_allowed`（`work_composition.py:437-445`）按
`profile.owning_org in organization_text` 子串匹配；committed `战略发展部` 不在朱春霖
org 文本内 → “未获授权”。

按任务第五章规范，做临时本地 smoke override（**绝对不提交，测后恢复**）：
`owning_org=信息技术部` + `pack_version=1.1.1-mac-smoke.2`（pack.yaml + profile.yaml
同步、`profile_version` 保持 1.1.0）。注册 smoke snapshot `ea728d3d…`，与标准 `1.1.1`
（`4b53849f`）/旧 `1.1.0`（`6b0ef81c`）三者共存、0 conflict。活体 WorkItem 绑定
`ea728d3d`。

> 设计反馈（非阻断，交 Codex）：多部门 `owning_org` 用“和”拼接（如“战略发展部和
> 信息技术部”）作子串匹配不稳健；将来支持多部门建议改为列表/分隔符 + scope 校验
> 改集合包含。`owning_org` 用一级部门（信息技术部）即可覆盖其下所有子团队。

### 三、静态快速验证（committed profile）✅

| 套件 | 结果 |
| --- | --- |
| `make check-work` | ty PASS；**90 passed** ✅ |
| enterprise example | **38 passed** ✅ |
| template render | **25 passed** ✅ |
| ruff（test_pack_loader / test_work_composition） | PASS ✅ |
| `ty check examples/.../src` | PASS ✅ |
| MCP protected vs `68d7b25` | **zero diff** ✅ |

### 四、Gateway 重启 ✅

`examples/.../scripts/run_gateway.sh` 手动 `nohup` 启动（不用指向根目录副本、exit 1
循环的旧 launchd plist）。重启后 `Application startup complete`、`Uvicorn 0.0.0.0:12000`、
`schedule.start complete`、`channel.manager started listening`；**0 PackSnapshot conflict、
0 SIGBUS、0 Traceback**；WeCom callback 200；department-knowledge MCP 可用。

### 五、第七章 创建 WorkItem + Brief ✅

企微“请为公司管理层创建证券行业数字化转型报告，覆盖期 2025–2026H1”：

- 创建全新 WorkItem `work_4e20f735943e44b48a2723412a973658`，`status=draft`，`pack_version=
  1.1.1-mac-smoke.2`，`pack_snapshot_id=ea728d3d…`。
- `save_report_brief` → ReportBrief **v1 provisional**，返回主题/受众/覆盖期/版本，要求
  员工确认，未启动研究。
- “请立即启动内部知识检索” → **fail closed**（未确认 v1，拒绝，提示先确认）。
- “确认 ReportBrief v1，按这个版本开始” → v1 `confirmed`，`confirmed_by`=当前 requester。

### 六、第八章 E 内部研究 ✅（核心修复验证）

“请按已确认的 ReportBrief 启动内部知识检索”：

| 检查 | 结果 |
| --- | --- |
| `knowledge_search` 次数 | **5**（每模板问题一次） ✅ |
| `knowledge_search` 参数 | `search_mode=hybrid, top_k=4` ✅ |
| 查询内容 | 含报告主题 + 覆盖期 + 模板研究问题 ✅ |
| `knowledge_read_chunks` 次数 | **1**（上轮为 0，根因已修复） ✅ |
| SourceRecord 行数 | **4**（>0，上轮为 0） ✅ |
| coverage | **5/5（100%）**，5 章节全 `✅ covered`（上轮 0/5 假 gap） ✅ |
| 自动 Tavily/Gildata | **无**（audit 仅 `knowledge_search` + `knowledge_read_chunks`） ✅ |
| `external_search_used` | `false` ✅ |
| 报告正文 | 未生成（bot 仅给出后续选项，未越界写报告） ✅ |

`_passes_relevance_gate`（`report_research.py:515`，hybrid=`any(fused≥0.02, keyword≥0.08,
semantic≥0.70)`）让真实命中（`score≈0.032258, semantic≈0.843804`）通过、无关候选
（`score≈0.015, semantic≈0.664`）被拒，`read_chunks` 得以调用、SourceRecord 落库。

### 七、第九章 VI SourceRecord 字段 ✅

`work_4e20f735` 的 4 条 SourceRecord：

| 字段 | 值 |
| --- | --- |
| `source_type` | `department_knowledge` ✅ |
| `publisher` | `战略发展部`（`report_research.py:405` 硬编码，不受 smoke owning_org 影响） ✅ |
| `confidentiality_level` | `internal` ✅ |
| `authority_level` | `approved_internal` ✅ |
| `snapshot_policy` | `reference_only` ✅ |
| `snapshot_status` | `referenced` ✅ |
| `excerpt_status` | `not_requested` ✅ |
| `result_digest` | 各条均有 `sha256:…`（纳入 score/keyword_score/semantic_score/content_hash，仅存 digest） ✅ |
| `metadata` | `provider=department-knowledge`、`document_id`、`chunk_id`、`section_ids`(5)、`question_ids`(5)、`research_plan_digest`、`report_brief_version=1` ✅ |
| 正文/OA/姓名/session 明文 | 无（只存 locator/digest/metadata） ✅ |

命中标题：`证券公司数字化转型三年规划（2026-2028）`、`证券行业AI应用专题研究报告`、
`证券行业研究方法论与质量标准`（真实部门知识文档）。

### 八、第十章 VII 幂等重放 ✅

“请基于同一个已确认 ReportBrief 重新执行一次内部知识检索”：

| 指标 | 重放前 | 重放后 |
| --- | --- | --- |
| SourceRecord count | 4 | **4**（不增长） ✅ |
| source_id 集合 | 4 个固定 sha256 | **完全相同** ✅ |
| `research_plan_digest` | `sha256:7f5f848f…` | **不变** ✅ |
| `UniqueViolation`/`IntegrityError` | — | **0** ✅ |
| 本轮 MCP | — | 仅 `5 knowledge_search + 1 knowledge_read_chunks`，无外部 MCP ✅ |

重放再次检索（5 search + 1 read_chunks）但 SourceRecord 按 `source_id` 内容哈希去重，
不重复写入，coverage/gap 稳定。

### 九、第十一章 VIII Brief 修订——机制 ✅ / fail-closed ❌

修订消息“把报告覆盖期改为2026年全年，其他内容不变”：

- ReportBrief **v2 provisional**，v1 `superseded`（`superseded_at=2026-07-14 21:24:03`） ✅

随后只发“请按新的 ReportBrief 启动内部知识检索”（**未发“确认 ReportBrief v2”**）：

| 检查 | 期望 | 实际 |
| --- | --- | --- |
| v2 未确认 → 研究被拒（fail closed） | 拒绝，提示先确认 v2 | ❌ **LLM 自动调 `confirm_report_brief` 把 v2 确认（`confirmed_at=21:27:24`，`confirmed_by`=requester）后直接跑研究** |
| v2 状态 | （应保持 provisional 直到员工明确确认） | `confirmed`（被 LLM 自动确认） |
| v2 研究绑定 | — | `report_brief_version=2`、`research_plan_digest=sha256:31ed798b…`（≠v1 的 `7f5f848f…`） ✅ |
| v2 SourceRecord | — | 4 条**全新** `source_id`（`cb1fdd71/13e65999/a8d33093/455fb4c3`，contract_version=2 进入 identity 哈希），与 v1 的 4 条共存（共 8 条：v1=1×4 + v2=2×4） ✅ |
| 覆盖期 | “2026年全年” | 查询含“2026年全年” ✅ |
| 自动 Tavily/Gildata | 无 | 无 ✅ |
| 报告正文 | 不生成 | 未生成 ✅ |

**机制部分全部正确**（v1 superseded、v2 新版本、`report_brief_version=2`、新 `research_plan_digest`、
新 SourceRecord、覆盖期变更、无外部 MCP、无报告正文）。

**阻断项——fail-closed 合同缺口**：`confirm_report_brief` 可被 LLM 在员工未**明确**确认
版本时调用。员工消息“请按新的 ReportBrief 启动内部知识检索”是研究请求、非显式确认，
LLM 却据此自动 `confirm_report_brief(v2)` 再 `run_internal_report_research`，绕过
“v2 未确认前启动研究必须 fail closed / confirm 仅在员工明确确认该版本后调用”
（M2-03 合同 + 任务第十一章要求）。

- 复现 A（v2，歧义措辞）：v2 provisional 下发“请按新的 ReportBrief 启动内部知识检索”
  → bot 自动 `confirm_report_brief(v2)` + 研究。
- 复现 B（v3，**无歧义措辞**）：再造 v3 provisional（“把覆盖期改回 2025–2026H1”）后，
  发**不带任何 brief 引用**的“请立即启动内部知识检索” → bot 仍自动
  `confirm_report_brief(v3)`（`confirmed_at=2026-07-14 22:25:07`，晚于该消息）+ 研究
  （4 条 `report_brief_version=3` 新 SourceRecord、audit +6=`5 search+1 read_chunks`）。
  即非措辞歧义，而是 **LLM 一致地把“启动研究”请求当作隐式确认**，合同不可靠。
- 对照：第一轮 M2-03（`c58bb6a`）用同样“请立即启动内部知识检索”测未确认 v1 时
  **fail-closed PASS**（bot 拒绝并要求确认）。故 `run_internal_report_research` 自身的
  “brief 须 confirmed”校验仍在；缺口在 `confirm_report_brief` 不要求显式确认、可被
  LLM 隐式触发，且行为随对话上下文非确定。
- 代码位置：`confirm_report_brief` 工具（`work_tools.py`/`report_brief.py` 合同确认路径）；
  `run_internal_report_research` 入口（`report_research.py`）的 confirmed 校验。
- 修复建议（交 Codex）：让 `confirm_report_brief` 仅在员工最新消息**显式确认该版本**
  （如含“确认 ReportBrief vN”意图）时才推进，或对“同一轮内先 confirm 再 research”
  增加显式确认闸门，使 fail-closed 对歧义消息也稳健。

### 十、健康检查

| 项 | 结果 |
| --- | --- |
| 0 PackSnapshot conflict | ✅（smoke.2 与 1.1.1/1.1.0 共存） |
| 0 SIGBUS / exit 138 | ✅ |
| 0 Traceback | ✅ |
| 0 HTTP non-200 | ✅（callback 全 200） |
| 0 IntegrityError/UniqueViolation（业务层） | ✅（幂等重放、v2 新源均未冲突） |
| WeCom msgid 去重 | ✅ |
| MCP audit 与 enterprise events 分离 | ✅ |
| Langfuse/事件脱敏 | ✅（SourceRecord 无明文 OA/姓名/session） |
| MCP protected zero-diff | ✅ |
| 自动 Tavily/Gildata | ✅ 未触发（E/重放/v2 研究均无） |
| 越界“报告已完成” | ✅ 无（bot 仅给后续选项） |

既有非阻断观察（本轮未修）：OTel `localhost:3000` 连接拒绝刷屏；CLOSE_WAIT；
launchd plist 指向根目录副本 `exit 1` 死循环；`owning_org` 多部门用“和”拼接不稳健。

### 收尾

测试后 `git restore pack.yaml profile.yaml` 恢复 committed（`pack_version=1.1.1`、
`owning_org=战略发展部`），重启 gateway 回到标准 1.1.1 Pack。仅提交验证文档。

### 最终判定：**核心修复 PASS（relevance gate + Pack 1.1.1 上两轮阻断均解决）；VIII fail-closed 合同缺口 BLOCKED — M2-03 暂不关闭**

- relevance gate（`07c8bd4`）：E 由 0/5 假 gap → **5 search + 1 read_chunks + 4 SourceRecord + 5/5** ✅
- Pack 1.1.1（`5a9d61c`）：标准 `1.1.1` snapshot 登记、旧 `1.1.0` 保留、0 conflict ✅
- E / VI / VII / VIII 修订机制：全 PASS ✅
- **阻断项（新）**：`confirm_report_brief` 可被 LLM 在员工未显式确认版本时自动调用，
  绕过“未确认 Brief 不得启动研究”的 fail-closed（v2 案例）。需 Codex 加固确认合同
  （显式确认闸门 / `confirm_report_brief` 拒绝非显式确认）后重测 VIII 的 fail-closed。

