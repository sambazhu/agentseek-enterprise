# v0.1.0 M3 Implementation Log

## M3-00A: WeCom session backpressure

Status: runtime hardening implemented; awaiting final Mac mini live verification.

M3 starts with a runtime admission slice before report outline and draft
generation. It keeps the existing per-session FIFO single-flight worker and
adds explicit backpressure around it.

### Runtime contract

- One active turn per `session_id`.
- At most `AGENTSEEK_WECOM_SESSION_QUEUE_MAXSIZE` pending messages; default 3.
- Different sessions retain coroutine-level concurrency.
- Accepted pending messages record their one-based queue position in structured
  events. Employees can query the current active/pending counts with the queue
  status command; the AI Bot callback protocol does not reliably render
  non-terminal stream updates.
- A message above the pending limit finishes immediately and never reaches the
  agent or model.
- A pending message expires after
  `AGENTSEEK_WECOM_QUEUE_WAIT_TIMEOUT_SECONDS`; default 240 seconds. Expired
  messages are skipped when the worker reaches them.
- `查看消息队列` and `查看排队状态` bypass the model and return active count,
  approximate active age, and pending count.

### Observability

The channel emits redacted structured events:

- `wecom_turn_queued`
- `wecom_turn_started`
- `wecom_turn_queue_rejected`
- `wecom_turn_queue_wait_timeout`
- `wecom_turn_queue_status`

Events contain hashed runtime scope through the enterprise observability layer,
counts, limits, and durations. They do not contain message text, OA account,
employee name, or model output.

### Boundaries

M3-00A does not implement `取消当前处理`, `清空等待消息`, hard preemption of a
blocking LLM call, or forced process exit under load. Those belong to M3-00B.
Long report generation remains a WorkItem concern and must not become a large
chat queue.

### Deterministic acceptance

Automated tests cover:

1. Eight distinct messages remain FIFO and single-flight when the configured
   pending limit permits them.
2. With the default policy, one active plus three pending messages are accepted
   and the next message is rejected.
3. Queue positions are 1, 2, and 3.
4. Queue status bypasses the agent in both busy and idle sessions.
5. A pending message expires without entering the agent and pending count
   returns to zero.
6. Different sessions remain concurrent.

Mac mini live verification should send one slow message followed by four rapid
distinct messages, verify three queued-position events and one user-visible
terminal rejection, query queue status while busy, and confirm that no rejected
or expired message produces a model turn.

### Single-stream product contract

Mac mini verification established that the WeCom AI Bot renders terminal
`finish=true` stream content reliably but does not reliably render intermediate
`finish=false` content. M3-00A therefore keeps the v0.0.9-style single-stream
delivery and does not reintroduce `response_url` for ordinary chat.

The user-facing acceptance contract is now:

1. A normal accepted message eventually receives one terminal answer or one
   terminal timeout/error response.
2. A message above the queue limit receives one terminal rejection, enters no
   Agent turn, and is not duplicated through another delivery channel.
3. `查看消息队列` / `查看排队状态` returns active and pending counts without
   entering the Agent.
4. A queued message that exceeds its TTL receives one terminal cancellation and
   enters no Agent turn.
5. Queue positions remain observable and deterministic, but are not promised as
   unsolicited visible messages. A pre-answer ACK is likewise best-effort only,
   not a release gate.

This is a deliberate protocol/product boundary. Making ACK and every queue
position independently visible would require a second terminal delivery channel
and would restore the ordering, duplication, retry, and exactly-once problems
that the single-stream simplification removed.

### User-facing delivery remediation

Status: delivery simplification implemented after confirming that the dual
callback-stream plus `response_url` lifecycle caused ordering, duplication, and
retry ambiguity; awaiting Mac mini live verification.

- Ordinary text, voice, and immediately processed media turns ignore the callback's
  `response_url`. Accepted messages return `stream.finish=false`, then ACK, queue
  position, processing progress, terminal answer, rejection, and queue TTL all
  remain on that one `stream_id`, matching the proven v0.0.9 interaction model.
- Queue status and capacity rejection finish synchronously on the same stream.
  Duplicate `msgid` callbacks reuse the stream's latest state and never create a
  second turn or consume a second delivery capability.
- `response_url` remains available only for explicitly asynchronous work. A file
  whose extractor returns `pending` completes the callback and reserves its
  one-shot URL for the later extraction/Agent result. The response URL probe is
  retained for deployment diagnostics.
- Response URLs and response codes never enter model context, logs, traces, or
  persistent runtime state.
- Text, voice, and text-only mixed callbacks perform queue admission before
  open-userid conversion. The worker resolves the plaintext userid and updates
  the runtime message immediately before enterprise processing, so the first
  callback no longer waits on the network identity conversion.
- Enterprise identity loading is now an async Bub hook. Blocking DM provider
  calls run outside the event loop with a 15-second outer deadline. Persistent
  DM sidecar requests have an 8-second inner deadline; a timeout force-kills the
  stale JVM (including a stopped process), and the next lookup starts a fresh
  sidecar. Lookup failure records identity status `error` and lets the turn and
  other sessions continue.
- `langchain_model_call` now emits `started` as soon as the LangChain model
  callback begins, followed by `succeeded` or `error`. This fixes the previous
  diagnostic ambiguity where an in-flight provider request looked like a stall
  before the model callback. Events expose timing and size counters without
  retaining prompt or completion text.
- `langchain_run_stage` events record spec resolution, prompt enrichment,
  context construction, and model invocation boundaries. They contain timing
  and size counters only, so pre-model setup stalls can be distinguished from
  runnable/provider stalls without recording prompts or replies.
- OpenAI-compatible model requests have a separate 60-second default deadline
  and zero automatic retries. Provider timeout exceptions become a terminal
  employee-facing timeout response, releasing the session worker before the
  broader 180/195-second turn deadlines.
- The same deadline is attached to the LangGraph `model` node, covering all
  model middleware and request preparation before provider callbacks begin.
  LangGraph `NodeTimeoutError` is normalized to the same terminal timeout
  response, so a pre-provider stall releases the session worker at the inner
  boundary instead of waiting for the 180/195-second outer turn limits.
- Short-term memory prompt injection keeps newest messages first and is bounded
  by `AGENTSEEK_ENTERPRISE_MEMORY_PROMPT_MAX_CHARS` (default 12000) and
  `AGENTSEEK_ENTERPRISE_MEMORY_PROMPT_MAX_MESSAGE_CHARS` (default 3000).
- Code review confirmed that `current_work_context` is already a small ledger
  summary; research questions, templates, and SourceRecord bodies are not
  injected there. Further prompt slimming should use the new model-call metrics
  rather than removing WorkItem capabilities speculatively.

### DeepAgents harness hardening

Mac mini stage timing repeatedly showed a long interval between
`model_invoke started` and `langchain_model_call started`. Local inspection and
benchmarking ruled out rebuilding the compiled runnable and per-turn tool
binding: the runnable is cached at spec load and binding the current tool set is
about 10-11 ms locally.

The enterprise template now registers an OpenAI-provider DeepAgents harness
profile that disables DeepAgents automatic `SummarizationMiddleware`; it can
issue a second summary-model request before the observable primary model
callback, while AgentSeek already supplies bounded, persisted short-term memory
and bounded runtime context.

The default `general-purpose` subagent and `task` tool remain available. They
are optional execution helpers and are not the router between ordinary chat and
formal work; the guarded `create_industry_report_work` path is the durable
WorkItem boundary. Future report compaction must be an explicit, observable
WorkItem operation rather than silently re-enabling conversational
auto-summarization. The model-node deadline also bounds any remaining
pre-provider middleware stall. Mac mini must verify that the previous 44-second
pre-model gap and burst hang disappear or terminate at the configured 60-second
boundary; if a gap remains, `langchain_run_stage` remains the oracle and the
change must not be misreported as a confirmed root-cause fix.

---

## Mac mini M3-00A session queue governance live verification（2026-07-15，commit `27aab4d`）— 事件层 PASS / 用户面投递未验证 ⚠️

- 环境：Mac mini，分支 `enterprise/v0.0.9-files-plugin`，commit `27aab4d`。
- gateway pid 62278（18:3x 启动），`.env` 本机不入库：`AGENTSEEK_WECOM_SESSION_QUEUE_MAXSIZE=3`、
  `AGENTSEEK_WECOM_QUEUE_WAIT_TIMEOUT_SECONDS=240`（+ 既有 `LANGCHAIN_RUN_TIMEOUT=180`/`TURN_TIMEOUT=195`/`SHUTDOWN=10`）。
- smoke override（本机不入库）：`pack_version=1.2.0-mac-smoke.1`、`owning_org=信息技术部`。
- LLM：原 `dashscope glm-5.2`（~3-9min/turn；内部 GLM-5.2-W4A8 `10.99.100.129` 仍网络不可达）。慢 LLM 正好利于制造 active+pending。
- 双端同步：GitLab 补推 `3f3e603..27aab4d`（ls-remote = `27aab4d47b9af6c03acfc04a9902c0be0c0082ac`）；GitHub 已有 27aab4d。

### 静态（Phase 4）— 全 PASS

`agentseek-wecom` **54**（+4 背压用例）、`agentseek-langchain` 17、`agentseek-enterprise` 80、`agentseek-work` 90、
example 73+1skip、`test_templates_render` 25 ✅；`make typecheck-wecom` All passed ✅；
`ruff check` wecom src+tests All passed ✅；`make docs-test` 文档构建 0.27s 无错 ✅；
`mcp_policy.py`+`tools.py` vs `68d7b25` **zero-diff** ✅。
（spec 列的 `templates/deepagents/enterprise-wecom/tests` 是 cookiecutter 模板路径，无渲染 tests 目录；
实际模板测试用 `tests/cli_commands/test_templates_render.py`=25 通过。）

### Live A — 普通消息回归：PASS ✅

`我是谁`（b8e0ef22，content_chars=3）→ `wecom_turn_started`（active，pending_count=0），**无 `turn_queued` = 无排队提示**；
`identity_lookup source=dm found`；turn 正常完成（stream_finished）。身份/记忆/WorkItem 路径未回归。

### Live B — active + 3 pending + 超限拒绝：PASS ✅

6 条快速连发（背压1-6，各 3 字）。真实时间线（UTC）：

| 消息 | stream_id | 事件(时间) | 结果 |
|---|---|---|---|
| 背压1 | 29706de4 | turn_serialized 12:55 | **active** |
| 背压2 | aad2d14c | turn_queued 12:55:19 pending=1 | pending position 1 |
| 背压3 | cf216cde | turn_queued 12:55:35 pending=2 | pending position 2 |
| 背压4 | a282f09e | turn_queued 12:55:51 pending=3 | pending position 3（满）|
| 背压5 | a3140bd2 | **turn_queue_rejected 12:56:08** pending=3 status=rejected | **拒绝，不进 Agent** ✅ |
| 背压6 | df4a4ca6 | **turn_queue_rejected 12:56:31** pending=3 status=rejected | **拒绝，不进 Agent** ✅ |

- `turn_queued` 带 `pending_count`(=position) + `pending_limit=3`；**maxsize=3 强制**，第 4 条 pending 立即拒绝。
- **单飞**：同时刻仅背压1 active，其余入队；**FIFO**：pending_count 1/2/3 按到达顺序。
- 被拒消息（a3140bd2/df4a4ca6）事件链仅 stream_started+message_received+turn_queue_rejected，**无 turn_started/identity/LLM** → 不触发 Agent ✅。
- 无死锁、无需 kill-9（背压1 后续 turn_timeout 正常，队列继续推进）。

### Live C — 队列状态命令：PASS ✅

`查看消息队列`（91218758）→ `wecom_turn_queue_status active=True pending_count=1 succeeded`（12:48:03）；
`查看排队状态`（1a9060ad）→ `wecom_turn_queue_status active=True pending_count=1 succeeded`（12:48:50）。
两条均**立即返回、绕过 Agent**（无 turn_started/identity/LLM），**不占用 pending 名额** ✅。

### Live D — pending TTL：PASS ✅（production TTL=240 自然命中，未切 15s）

- 471bccdf（队列测试3）：`turn_queued` 12:43:48 → `turn_queue_wait_timeout` 12:47:55（等 247s > 240s）→ 自动取消。
- 63e22e0d（队列测试4）：`turn_queued` 12:44:05 → `turn_queue_wait_timeout` 12:48:23（等 238s → 取消）。
- 两者均**未进 Agent**（无 turn_started/identity），pending_count 回落 ✅。
- 因 production TTL=240 已在本轮自然触发两次过期，未额外切 TTL=15（避免重启打断验证）；行为与 spec D 一致。

### Live E — 跨员工隔离：SKIPPED（无第二名员工）

无第二名员工账号，live 未测。跨 session 单飞隔离由 `contrib/agentseek-wecom/tests`（54 PASS，含 cross-session
用例）覆盖。记录"自动化已覆盖，live 条件不足"，不伪造。

### Phase 7 — 观测事件与脱敏

5 类结构化事件全部出现：`wecom_turn_queued`、`wecom_turn_started`、`wecom_turn_queue_status`、
`wecom_turn_queue_wait_timeout`、`wecom_turn_queue_rejected`。字段含 `pending_count`/`pending_limit`/`queue_wait_ms`/`active`/`status`，合理。
脱敏：events 全文 grep `zhuchunlin|朱春霖|wecom:zhuchunlin|password|api_key|secret|token=` **0 命中**；
身份/会话均 hmac 化（`session_key`/`from_user_key`/`stream_id`），消息仅记 `content_chars` 不记正文。
`mcp-audit.jsonl` 与 `enterprise-events.jsonl` 仍分离。

### Phase 8 — 健康与不回归

- **0** SIGBUS / exit 138 / Traceback / IntegrityError（gateway.log 验证窗口）；**0** HTTP non-200。
- `wecom_duplicate_msgid` 去重正常（msg 重复回调 1827 次去重）。
- 队列超限消息（背压5/6）**无 Agent/LLM turn**；TTL 过期消息（471bccdf/63e22e0d）**无 Agent/LLM turn**。
- MCP 保护文件 zero-diff；身份/短期记忆/长期记忆/WorkItem 路径未见回归（identity_lookup/short_term_memory/contextseek 全程 succeeded）。

### 已知 M3-00B 遗留（非本轮阻断，按 spec 不判为 M3-00A 阻断）

- 长 LLM 调用无法硬抢占（active turn 仍靠 195s turn_timeout post-hoc 终止，非 pre-emptive cancel）。
- under-load SIGTERM 仍可能不够强（本轮未复测，沿用 3aed623 结论：idle 优雅、under-load 需 kill -9）。
- 取消当前任务 / 清空队列 不在本轮范围。
- 注：本轮 active turn 多次在 ~195s `turn_timeout`（背压1/队列测试1 等），比 3aed623（4-9min）更接近配置值——可能因消息更简单或调度改善，但硬抢占仍待 M3-00B。

### 最终结论：**事件层 PASS，但用户面投递未验证 → 暂不关闭，待投递修复后复测** ⚠️

**纠正（2026-07-16）**：本轮 live 测试（2026-07-15 晚，glm-5.2）用户实际**零回复**——拒绝提示、排队位置提示、
状态命令回复、ack 均未投递到企微。上面 A-D 标的 PASS **只是事件层**（`wecom_turn_queued`/`turn_queue_rejected`/
`turn_queue_status`/`turn_queue_wait_timeout` 事件都正确触发，证明**队列治理逻辑本身正确**）。
但 M3-00A 验收标准中"第 4 条明确提示正在处理和等待数量""pending 入队后提示排队位置 1/2/3""状态命令立即返回"
**都是用户面可见项**，事件触发≠用户看到。投递未通，这些用户面项**未被真正验证**。

所以：**队列治理逻辑 = 事件层 PASS**；**用户面投递 = 未验证（测试期间 broken）**；**M3-00A 暂不关闭**。
需等投递问题（见下方"后续 live 管线诊断"发现 1-4）修复后，复测以下用户面必达项：

1. 被拒消息用户能**看到**"当前 1 条处理中、N 条等待"提示（且 WeCom 停止重传）。
2. pending 消息用户能**看到**"排在第 N 位"提示。
3. `查看消息队列`/`查看排队状态` 用户能**立即看到** active/pending 数量回复。
4. 正常消息用户能**看到**"已收到，正在处理..."即时 ack，随后收到最终回复。
5. TTL 过期消息用户能**看到**"等待时间过长已取消"提示。

gateway 已恢复 `MAXSIZE=3 / TTL=240`（后切换 qwen-flash，pid 16204）。M3-00B（硬抢占 / under-load SIGTERM /
取消任务 / 清空队列）+ 本轮投递修复作为后续切片。

---

## M3-00A 后续 live 管线诊断（2026-07-16）— 投递/延迟问题，非队列逻辑缺陷

M3-00A 队列治理事件层验证 PASS，但测试期间 + 切快模型后，用户 live 体感"发消息后几十秒无任何反馈"。
深挖收消息→生成→投递全链路（`_dispatch_user_message` / `_wait_for_first_update` / `send` / `make_text_stream`），
发现 4 项与队列逻辑无关、但影响 live 可用性的问题。模型已切 `qwen-flash`（pid 16204，见下）。

### 模型选型（dashscope `AGENTSEEK_API_BASE` 不变）

探针实测（`/v1/chat/completions`，"你好" 简短回复）：

- `qwen-flash` 1.1s，非 reasoning，内容正确 ✅ → 已采用。
- `deepseek-v4-flash`（原配置）reasoning，agent 内单轮 23-34s ❌。
- `qwen3.7-flash` **不存在**（`Model.AccessDenied`）；`qwen3.6-flash`/`qwen3.5-flash`/`qwen3.7-plus` 全是 reasoning（含 `reasoning_content`），4.7-6.6s+ ❌。
- dashscope 上 qwen3.x 全线 reasoning，`qwen-flash` 是唯一快的非 reasoning 聊天模型。

### 发现 1：整 turn 57s，瓶颈是 LLM 单次调用 30s（非模型慢）

`你好`（qwen-flash）turn 拆解：接收建 stream 3s + 同步 DM 身份 5s + memory 3s + pgvector 4s + **LLM 30s** +
guard/save/finish 9s。LLM 30s 是 qwen-flash 单轮、`tool_sequence=[]`（无工具调用）的**一次**调用（探针同模型 1.1s）。
最可能：agent 每 turn 把当前 workitem（`8c669fb4`：v2 brief + 5/6 研究 Q&A + 缺口 + 11 SourceRecord + 研究模板）
全量塞进 system prompt，快模型也要 30s 消化；也可能是 dashscope 大请求排队。
**需 Codex**：(a) 上下文瘦身——聊天 turn 按需懒加载 workitem 研究/源/模板，预期 LLM 30s→3-5s；
(b) 加 LLM 调用耗时 + prompt/completion token 数结构化日志（现在日志完全不含 LLM timing，无法区分 prompt 大 vs endpoint 慢）。

### 发现 2：用户看不到"已收到，正在处理..."即时回执，但 ack 其实发了

`_wait_for_first_update`（`initial_wait_seconds=1.5` 默认）在 1.5s 后返回 `stream(content="已收到，正在处理...", finish=False)`，
`make_text_stream` 包成 stream envelope 返回 WeCom——**ack 机制本身没坏**。但 WeCom 客户端疑似只渲染 `finish=True` 的内容，
`finish=False` 的中间 ack 不显示。M2 期间用户看到的"已收到，正在处理..."其实是 LLM 慢回复正文，非系统 ack。
**需 Codex**：确认 WeCom 对 `finish=False` stream 的渲染行为；若确实不渲染，考虑把 ack 作为独立 `finish=True` 消息先投递、
再异步投递最终回复；或用 `response_url.py` 的 `WeComResponseUrlSender` 先发一条 ack。

### 发现 3：DM 身份解析同步阻塞 callback ~5s

`_dispatch_user_message` 开头 `await self._resolve_userid`（DM JDBC via JPype）阻塞 callback 约 5s，ack 要等身份解析完才返回。
**需 Codex**：身份解析移到 ack 之后异步做（先返 ack，再异步解析身份 + 跑 turn），或命中 `userid_cache_ttl_seconds` 缓存时跳过 DM。

### 发现 4：核实被拒/TTL 路径提示是否真正投递

昨晚 M3-00A 测试（glm-5.2）用户"没收到任何反馈"，原因复合：glm-5.2 单 turn 4-9min、queued 排空极慢、ack 不可见（发现 2）。
另外昨晚对被拒 stream（如 `a3140bd2`）WeCom 持续重传，疑似 rejection 提示未送达、或 WeCom 对未 finish 的 callback 一直重传。
切 qwen-flash + 重启 gateway 后正常 active 路径回复能送达（`你好`/`现在几点`/`你好` 均收到），投递链路本身通。
**需 Codex 核实**：被拒/TTL 路径的 `_finish_message_stream` 是否真的把 `finish=True` 提示送达 WeCom 并让其停止重传。

### 优先级与影响面

发现 1（上下文瘦身 + LLM 可观测）和发现 2（ack 可见性）对用户体感影响最大，优先；发现 3 次之；发现 4 核实即可。
**均不影响 M3-00A 队列治理本身的正确性**（事件层已验证 PASS），但影响 live 可用性。归入 M3-00B 或独立 live-UX 切片。

当前环境：gateway `qwen-flash`（pid 16204），`MAXSIZE=3 / TTL=240`，`work_8c669fb4` 暂留 draft/intake。
内部 GLM-5.2-W4A8（`10.99.100.129:30004`）Mac mini 仍网络不可达（traceroute 死在网关 `172.20.199.254`，需网络组打通 `172.20.199.x→10.99.0.0/8`）。

---

## Mac mini M3-00A user-facing delivery + LLM observability re-verification（2026-07-16，commit `cd1d02d`）— 部分用户面 PASS / 2 项未验证 ⚠️

复验 Codex 对「M3-00A 用户面投递 + 身份延迟 + LLM 观测 + 短期记忆预算」的 4 部分修复（commit `cd1d02d`）。本轮重点验证
**用户面可见性**（事件层 PASS 不代替用户面 PASS，上轮教训）。

- 环境：Mac mini，分支 `enterprise/v0.0.9-files-plugin`，commit `cd1d02d`（拉取 `3b09824..cd1d02d`，GitLab 补推 ✅）。
- gateway 重启多次（DM sidecar 卡死清场，见 Live C），最终 pid 2776（python）/2767（uv），`*:12000`，无 proxy env。
- `.env`（本机不入库）：`AGENTSEEK_MODEL=qwen-flash`、`MAXSIZE=3`、`TTL=240`、
  `AGENTSEEK_ENTERPRISE_MEMORY_PROMPT_MAX_CHARS=12000`、`AGENTSEEK_ENTERPRISE_MEMORY_PROMPT_MAX_MESSAGE_CHARS=3000`（新加）。
- smoke override（本机不入库）：`pack_version=1.2.0-mac-smoke.1`、`owning_org=信息技术部`。
- workitem：`work_8c669fb4`（朱春霖/信息技术部，draft/intake）。
- 事件文件：`examples/enterprise_wecom_digital_employee/runtime/enterprise-events.jsonl`（gateway CWD=repo root，但相对
  `./runtime/` 解析到 example 目录，历轮一致）。

### 静态（Phase 4）— 全 PASS

`agentseek-wecom` **57**、`agentseek-langchain` **18**、`agentseek-enterprise` **81**、`agentseek-work` **90**、
example **73 passed + 1 skipped**、`test_templates_render` **25** ✅。
（example 15 failures 仅在挂本地 smoke override 时出现；临时 `git restore` pack/profile 后 73+1skip ✅ —— 已知的
frozen-digest artifact，非 cd1d02d 回归。验证后 smoke override 已还原。）
`make typecheck-wecom`/`typecheck-langchain`/ty touched(`memory.py`+`test_plugin.py`) All passed ✅；
`ruff check`（wecom+langchain src+tests+memory+plugin）All passed ✅；`make docs-test` 0.29s 无错 ✅；
`git diff --check` clean ✅；`mcp_policy.py`+`tools.py` vs `68d7b25` **zero-diff** ✅。

### Live A — 正常消息 ACK + 最终回复：用户面 PASS ✅

`你好`（stream `89940b41`）。完整事件链（UTC）：

| 时间 | 事件 | 关键字段 |
|---|---|---|
| 01:21:34.98 | wecom_message_received | content_chars=2，**无 `resolved`**（identity 已延迟）✅ |
| 01:21:37.60 | wecom_turn_started | queue_wait_ms=2 |
| 01:21:50.64 | identity_lookup | cache=miss，13s（延迟化，未阻塞 ack）|
| **01:22:00.08** | **langchain_model_call** | **elapsed_ms=1572（1.6s！）**，input_chars=18142，system_chars=17557，message_count=7，tool_count=23，qwen-flash |
| 01:22:15.17 | **wecom_response_url_delivery** | status=succeeded，content_chars=19（最终回复走一次性 response_url）✅ |
| 01:22:20.50 | wecom_stream_finished | age_ms=48536 |

- 用户：ack「已收到，正在处理...」**~5s 可见**（inline `finish=true`）；最终回复「朱春霖，你好！👋」**可见且只 1 次**
  （response_url exactly-once）✅。
- **30s LLM 瓶颈消失**：`elapsed_ms=1572`（上轮 30s）。system_chars=17557 仍大（23 个 tool schema + agent 指令主导，
  非短期记忆 —— 记忆预算生效）。
- `session.run.outbound session_id=wecom:zhuchunlin` —— 延迟身份正确水合为 OA userid，未留 open userid ✅。

### Live B — 身份回归：用户面 PASS ✅

`我是谁`（stream `7e16bc8b`）。identity_lookup cache=**hit**，最终回复 150 字符经 response_url 投递。
用户完整回复含**全部身份字段且正确**：

```
朱春霖，你好！👋
· 姓名：朱春霖   · OA账号：zhuchunlin
· 岗位：团队长兼数据架构师
· 组织路径：公司总部 / 信息技术部 / 数智产品研发团队
· 员工ID：17308da7990bc39bd424f4047fe9ec54
```

- ack 可见（~10s，WeCom 重传 7 次后接受）；最终回复可见且 1 次；session=`wecom:zhuchunlin`（未串位）✅。
- 注：dispatch_outbound 钩子只记首行「朱春霖，你好！👋」，完整 150 字符经 response_url 投递（事件记 content_chars 不记正文）。
  员工ID/姓名出现在**给用户自己的企微回复**里是正常的（本人数据）；结构化 events 仍 hmac + content_chars。

### Live C — active + 3 pending + 超限拒绝：用户面 BLOCKED ⚠️（两个互锁 blocker）

**第一次**（09:35）：发 `现在几点`（active）+ 快速连发 `队列测试1/2/3/4`。
- `现在几点`（`188cf2e6`）turn_started @ 01:35:04 后 **4 分钟无 identity_lookup / 无 LLM** → `wecom_turn_timeout` @ 01:39:15
  （195s post-hoc 终止，M3-00B 硬抢占缺失）→ session 锁不释放 → 3 pending 全部超 240s TTL 被取消。
- 事件层正确：`turn_queued` pending=1/2/3、`turn_queue_rejected` pending=3 ✅；被拒消息无 Agent turn ✅。
- 用户面：看到「本次处理超时，请稍后重试」+「等待处理时间过长，本条消息已取消…」×2；**未看到**位置提示 / 拒绝提示。

**重试**（09:50，重启后）：发 `现在几点` → 等 ack → 连发 `队列测试1/2/3/4`。
- `现在几点`（`fe7d66cc`）**正常完成**（identity 9s + LLM 1.6s + response_url 投递，无 hang）→ 证明 hang 偶发但**可复现**。
- `队列测试1`（`46ceef53`）晋升 active 后 **turn_started @ 01:51:04 再次卡死**（无 identity/LLM）→ 队列停摆。
- `队列测试4` 因 active 跑得快（62s）+ 错开发送，到达时 active 槽已腾出 → **被接受为 pending=3，未触发拒绝**（本轮没测到拒绝路径）。
- 用户面：看到 `现在几点` 最终回复 +「长时间等待未返回任何消息」；**未看到**位置提示。

**hang 模式（半确定性）**：每次重启后约 2-3 个 turn，DM 身份解析即 hang。
- 本次：retry ✅ → 现在几点 ✅ → 队列测试1 ❌；上次：你好 ✅ → 我是誰 ✅ → 现在几点 ❌。
- 根因：`dm_staff_sidecar --server`（持久服务模式）JDBC 连接在连续查询后挂起。cd1d02d 把 identity 延迟到 worker 每 turn 查一次，
  正好踩到 sidecar 疲劳点 → 挂死阻塞单飞队列 → 级联超时。需 kill -9 强杀（SIGTERM 在卡死态 8s 内不响应）。

**用户面可见性拆解（重要）**：
- response_url 投递（最终回复 / TTL / 超时）= **稳定可见** ✅。
- inline `finish=true` 投递（ack / 位置提示 / 拒绝）= **不稳定**：WeCom 对单条重传 2-7 次后才接受，burst 下对全部持续重传，
  pending 消息的 ack/位置提示用户看不到。现在几点 的 ack 偶尔可见（2-3 次重传后），队列测试 的 ack/位置始终不可见。

### Live D — 队列状态命令：用户面 PASS ✅

`查看消息队列`（stream `25fd2e00`）→ `wecom_turn_queue_status active=False pending=0 status=succeeded` @ 01:47:07，
**绕过 Agent**（该 stream 无 turn_started/identity/LLM）✅。用户看到「当前没有正在处理或等待处理的消息」✅（inline，3 次重传后接受）。

### Live E — TTL 过期用户可见：经 Live C 副产物验证 ✅（未单独切 TTL=1）

Live C 第一次（production TTL=240 自然触发）：pending 消息等 >240s → `wecom_turn_queue_wait_timeout` →
用户**看到**「等待处理时间过长，本条消息已取消，请稍后重新发送」（×2，经 response_url 投递）✅。行为与 spec E 一致，
因 production TTL 已自然命中，未额外切 TTL=1（避免重启打断）。过期消息未进 Agent ✅。

### Live F — LLM 调用观测 + 30s 瓶颈诊断：PASS ✅

多 turn `langchain_model_call` 指标（qwen-flash）：

| 请求 | elapsed_ms | input_chars | system_chars | messages | tools |
|---|---:|---:|---:|---:|---:|
| 你好 | 1572 | 18142 | 17557 | 7 | 23 |
| 现在几点(retry) | 1558 | ~18K | ~17.5K | 7 | 23 |

- **qwen-flash 探针 ~1s，Agent 调用 ~1.6s** —— 30s 瓶颈已解决（上轮 30s）。prompt_tokens/completion_tokens 标记 `[REDACTED]`（脱敏）✅。
- input_chars=18142 大，但**主要来自 system_chars=17557（23 tool schema + agent 指令）**，非短期记忆（记忆预算 12000/3000 生效，
  current_work_context 是小型账本摘要，未注入 SourceRecord/研究模板/Q&A 正文，与 Codex 代码审查结论一致）。
- **30s 瓶颈上轮的真因**：旧模型（glm-5.2/deepseek-v4-flash reasoning）+ 可能 dashscope 大请求排队；切 qwen-flash + 记忆预算后 1.6s。
  非单纯 prompt 体积问题（system_chars 仍 17.5K 但 1.6s 跑完）。

### 短期记忆预算验证（spec 十二）

`langchain_model_call input_chars` 未因旧研究大回复无限膨胀（稳定 ~18K，大头是 system/tool schema）；新配置 12000/3000 已加载。
未单独造大文本（避免污染生产 workitem/DB）。

### Phase 13 — 边界与安全

- enterprise-events.jsonl 全文 grep `response_url|response_code|zhuchunlin|朱春霖|wecom:zhuchunlin|password|token=|secret` **0 命中**
  （hmac 化 session_key/from_user_key/stream_id + content_chars）；`mcp-audit.jsonl` 与 enterprise events 分离 ✅。
- `langchain_model_call` 不含 prompt/completion 正文，不含用户原文 ✅。
- response_url 每接受消息最多消费 1 次（`response_url_consumed` 标志先 claim 后 I/O）✅；拒绝/状态走初始 inline 回复，不额外消费 response_url ✅。
- 注：gateway.log 的 `dispatch_outbound` 钩子会记 outbound 首行（含员工名，如「朱春霖，你好！」）—— 这是 bub builtin 钩子，历轮既有，
  非结构化 events 泄露，gateway.log 本地不入库。本轮不作为阻断，但提示 Codex 评估是否需要脱敏 dispatch_outbound。

### Phase 14 — 健康与回归

- 验证窗口 **0** SIGBUS / exit 138 / Traceback / IntegrityError；**0** HTTP non-200（callback 全 200）。
- 唯一噪音：OTel export 重试到 `localhost:3000`（本地 collector 未起，telemetry-sink 既有问题，非 gateway/WeCom 阻断）。
- `wecom_duplicate_msgid` 去重正常；identity/短期记忆/pgvector/WorkItem 路径无回归（单条消息）。
- **DM sidecar 卡死**：连续 ~2-3 turn 后 identity 解析 hang → 阻塞单飞队列 → 级联 turn_timeout/TTL（见 Live C）。非 cd1d02d 引入，
  但 cd1d02d 延迟 identity 每 turn 查一次放大了触发面。M3-00B 硬抢占缺失使卡死 turn 只能 195s post-hoc 终止（且协程不释放锁）。
- **重启副作用**：内存 msgid 去重在重启后丢失 → WeCom 残留重传被当新消息重处理（acde237d 等，产生 stray 回复）。非本轮阻断，但提示
  Codex 评估 msgid 去重是否需持久化。

### M3-00A 五项用户面必达项

| # | 必达项 | 结果 | 来源 |
|---|---|---|---|
| 1 | 被拒消息可见"1 处理中+N 等待"+停止重传 | ❌ **未验证** | Live C：拒绝路径本轮未干净触发（timing）+ inline 投递不可靠 |
| 2 | pending 可见"排在第 N 位" | ❌ **未验证** | Live C：位置提示事件触发但用户面始终不可见（inline 投递不可靠）|
| 3 | 状态命令立即可见 active/pending | ✅ PASS | Live D |
| 4 | 正常消息先 ack 后最终回复 | ✅ PASS | Live A |
| 5 | TTL 过期可见"等待过长已取消" | ✅ PASS | Live C 副产物（response_url 投递，×2 可见）|

**3/5 PASS，2/5 未验证 → M3-00A 仍不能关闭。**

### cd1d02d 的实质进展（已验证生效）

1. **LLM 30s → 1.6s**（记忆预算 + qwen-flash + 观测）—— 上轮发现 1 的 a/b 两项均已交付且验证。
2. **response_url 投递机制对终态消息（最终回复/TTL/超时）稳定可见** —— 发现 2 的核心修复（不依赖 WeCom 渲染 finish=false）对终态消息生效。
3. **`langchain_model_call` 观测完整且脱敏**（elapsed/tokens/chars，不含正文）—— 发现 1b 交付。
4. **identity 延迟到 worker**（callback 首包不再等 DM，ack 不再阻塞）—— 发现 3 交付，单条消息 ack 可见。
5. **短期记忆预算 12000/3000** 生效，input_chars 不膨胀。

### 仍阻塞 M3-00A 关闭的两个问题（发 Codex）

**Blocker A — inline `finish=true` 投递对 ack/位置/拒绝不可靠（发现 2 未完全解决）**
response_url 投递对**终态消息**（最终回复/TTL/超时）稳定可见，但 ack / pending 位置提示 / 拒绝 走 **inline callback 响应**
（`finish=true` 作为 HTTP 响应体），WeCom 不稳定渲染：单条重传 2-7 次后接受，burst 下对全部持续重传、用户看不到
（Live C 队列测试的 ack/位置始终不可见；Live A/B/状态命令的 ack 偶尔可见因重传后接受）。
**需 Codex**：把 ack / 位置提示 / 拒绝 也走 response_url 投递（而非 inline），或确认 WeCom AI Bot 对 inline `finish=true`
的渲染契约后选稳定路径。注意 response_url 是 one-shot —— 若 ack/位置/拒绝都消费 response_url，则最终回复需另寻通道（或 ack 即终态：
对拒绝/状态这类"无最终回复"的消息，response_url 正好用于投递该唯一提示；对 pending/active，ack 与最终回复不能共用一个 response_url）。

**Blocker B — DM sidecar 连续查询后 hang（半确定性，阻塞单飞队列）**
`dm_staff_sidecar --server` 在 ~2-3 个连续 identity 查询后 JDBC 挂起，gateway worker 卡在 `_hydrate_message_identity`
→ 单飞队列阻塞 → 后续 pending 全部超 TTL。SIGTERM 在卡死态不响应（需 kill -9）。非 cd1d02d 引入，但延迟 identity 每 turn
查一次放大触发面。
**需 Codex**：identity 查询加硬超时（sidecar 调用包 `asyncio.timeout` + 失败降级/重试/重启 sidecar）；或 sidecar 连接池健康检查/
自动回收。这是 live 可用性的硬阻塞，优先级仅次于 Blocker A。

### 最终结论

- **cd1d02d 修复 = 部分生效**：LLM 瓶颈、response_url 终态投递、LLM 观测、identity 延迟、记忆预算 5 项均交付且验证。
- **M3-00A 用户面 = 3/5 PASS，仍不能关闭**：必达项 1（拒绝可见）、2（位置可见）未验证，因 Blocker A（inline 投递不可靠）+
  Blocker B（sidecar hang）。
- **事件层**：队列治理逻辑（turn_queued/rejected/status/wait_timeout）继续 PASS；本次未引入事件层回归。
- **M3-00B 遗留**：硬抢占（卡死 turn 无法 pre-emptive cancel，只能 195s post-hoc，且协程不释放锁）、under-load SIGTERM、
  取消当前任务/清空队列 —— 不混入本轮判定。

当前环境已恢复：gateway `qwen-flash`（pid 2776），`MAXSIZE=3 / TTL=240`，`work_8c669fb4` 暂留 draft/intake。

---

## Mac mini M3-00A user-facing re-verification（2026-07-16，commit `5c8b4aa`）— Blocker A 回归 / Blocker B 部分生效 ⚠️

复验 Codex 对两个 blocker 的修复（commit `5c8b4aa`）：
**A** = AI Bot 首包 inline `stream finish=true` → 普通 `text`；**B** = DM identity 有界化（async hook + sidecar 8s + 外层 15s + 超时降级）。

- 环境：Mac mini，分支 `enterprise/v0.0.9-files-plugin`，commit `5c8b4aa`（GitHub 经代理拉取 `54d8350..5c8b4aa`，GitLab 补推 ✅）。
- gateway 多次重启（Blocker B 测试 + sidecar 故障清场），最终 pid 49006（uv）/49017（python），`*:12000`，无 proxy env。
- `.env`（本机不入库）新增：`AGENTSEEK_IDENTITY_DM_SIDECAR_TIMEOUT_SECONDS=8`、`AGENTSEEK_ENTERPRISE_IDENTITY_LOOKUP_TIMEOUT_SECONDS=15`
  （+ 既有 `EXECUTION_MODE=sidecar`、`MAXSIZE=3`、`TTL=240`、`MEMORY_PROMPT_MAX=12000/3000`、`IDENTITY_CACHE_ENABLED=true/TTL=600`）。
- smoke override（本机不入库）：`pack_version=1.2.0-mac-smoke.1`、`owning_org=信息技术部`。
- workitem：`work_8c669fb4`（朱春霖/信息技术部，draft/intake）。

### 静态（Phase 3）— 全 PASS（新基线）

`agentseek-wecom` **57**、`agentseek-enterprise` **83**（+2 新 identity/timeout 用例）、`agentseek-work` **90**、
`agentseek-langchain` **18**、`agentseek-files` **47**、example **73 passed + 1 skipped**、`test_templates_render` **25** ✅。
（spec 的 `make test-wecom`/`test-enterprise`/`check-work` 是动态 Makefile 目标，等价于 pytest+uv sync；用直接 pytest 跑取计数，结果一致。
example 15 failures 仅在挂本地 smoke override 时出现，临时 `git restore` 后 73+1skip ✅，已知 frozen-digest artifact。）
`make typecheck-wecom` All passed ✅；ty touched（`plugin.py`+`dm_staff_provider.py`）All passed ✅；
`make docs-test` 0.29s 无错 ✅；`git diff --check` clean ✅；
`mcp_policy.py`+`tools.py` vs `68d7b25` **zero-diff** ✅。

### Blocker A（首包改 plain text）— FAIL / 回归 ❌

`channel.py:_stream_response` 在有 `response_url` 时改为 `return make_text(initial_response_content)`（`msgtype=text`），
且 `initial_response_content` 首次读取后冻结（同 msgid 重试返回相同首包）。代码路径正确，冻结逻辑正确。

**但 WeCom AI Bot 不渲染 plain text callback 响应** —— 实测：

| 消息 | 首包格式 | 用户面 ack 可见 | WeCom 重传次数 | 最终回复（response_url）|
|---|---|---|---|---|
| 你好（首发）| plain text | ❌ 不可见 | 2 | ✅「朱春霖，你好！」可见 |
| 你好（重发）| plain text | ❌ 不可见 | **6** | ✅ 可见 |
| 查看消息队列 | plain text | ❌ 不可见 | 2 | （无最终回复，状态即首包）❌ |
| Live E 5 条身份 | plain text | ❌ 不可见 | 风暴 | 1 条 fallback「消息进入处理队列」|

- 你好 重发 **6 次重传**（02:33:13→02:33:30）说明 WeCom 持续拒绝/忽略 plain text 响应；最终回复经 response_url 投递可见。
- 状态命令（`查看消息队列`）事件层 `turn_queue_status succeeded` ✅，但用户面**看不到**回复（plain text）。
- **对比上轮 cd1d02d**（首包 `make_text_stream finish=true`）：你好 ack **~5s 可见**、状态回复**可见**。即 plain text 改动把上轮单条可见的 ack/状态**弄丢了**。

**根因**：Codex 假设"普通 text callback 是 WeCom 协议稳定的即时响应形式"对本 AI Bot 不成立。
WeCom AI Bot callback 响应疑似**必须是 stream 协议（`msgtype=stream`）**，`msgtype=text` 被忽略 → 持续重传 → 用户看不到首包。
**需 Codex**：首包回到 `make_text_stream finish=true`（上轮对单条有效），burst 不可靠的问题另寻方案（如 ack 也走 response_url，
但 response_url one-shot 与最终回复冲突 —— 对拒绝/状态这类"无最终回复"消息，response_url 正好投递唯一提示；对 active/pending，
ack 与最终回复不能共用一个 response_url）。

### Blocker B（DM sidecar 有界化）— 部分生效（核心改进成立，回收有缺口）⚠️

`plugin.py:load_state` 改 async hook；`_lookup_employee_context` 包 `asyncio.to_thread` + `asyncio.timeout(15s)`；
`dm_staff_provider.py:_lookup_locked` 用 `_sidecar_timeout_seconds()`（默认 8s，2 次尝试）。
代码路径正确，超时降级路径存在。

**Live E（cache off，5 条身份连发 — burst）**：
- #1 完成但回 17 字符 fallback（非真实身份）；**02:48:08 identity_lookup status=error**（降级触发 ✅）；
  **02:48:11 wecom_turn_timeout sid=61b3f041**（#2 卡 ~3.8 分钟才被 **195s turn_timeout** 杀，**不是 8s**）。
- sidecar 进程崩溃后**延迟 ~3 分钟**（02:47:13）才重启（pid 38579）—— sidecar 生命周期不稳定。
- 注：用户连发 5 条（非顺序），混入 MAXSIZE=3 队列压力，非干净 sidecar 压测。

**Live F（SIGSTOP 冻结 sidecar，单消息 — 干净故障注入）**：
预热 `我是水` 拉起 sidecar pid 42166（健康，正确返回身份）。`kill -STOP 42166`（stat=TN，确认只冻 sidecar 不碰 gateway 40716）。

| 检查项 | 结果 | 证据 |
|---|---|---|
| 8s 超时触发 | ✅ | `02:55:13 identity_lookup status=error error_type=TimeoutError`（query 后 ~8s）|
| gateway 不冻结 | ✅ | gateway CPU 1.7%（上轮 sidecar hang 时 0% 卡死，这次保持响应）|
| 旧 sidecar 回收 | ❌ | 42166 冻结后仍存活（TN），**未自动 terminate**，无新 PID |
| 恢复（进程真死后）| ✅ | `kill -KILL 42166` 后新 sidecar **46578** 自动拉起 |

恢复验证：新 sidecar 46578 服务正常 —— `02:59:18 identity_lookup cache=miss found`（真实查询成功）→
`02:59:39 langchain_model_call 2751ms` → `02:59:52 response_url_delivery cc=150 succeeded`（完整身份回复）。

**Blocker B 精确结论**：
- ✅ **核心改进成立**：sidecar hang 不再冻结 gateway event loop（上轮 cd1d02d 的硬伤已解），8s 超时 + identity error 降级生效，
  队列/其他会话可继续推进；进程真死时自动拉新 sidecar。
- ❌ **回收缺口**：sidecar 冻结/挂起（进程还活着）时，8s 超时**只中断当前请求，不 kill 旧进程** → 下次查询复用同一挂起 sidecar 再失败。
  "超时回收 JVM"这步在 SIGSTOP 人工故障下没做到。**需 Codex 核实** `_stop_process` 在超时路径是否真的 SIGKILL 旧 sidecar
  （SIGTERM 对 stopped 进程不生效）；真实 JDBC hang（进程 running）行为可能不同，建议 Codex 加 sidecar 健康探针/超时即 SIGKILL。

### Live A/B/C 用户面（均受 Blocker A 连累）

- **Live A 你好**：ack 不可见（plain text）❌；最终回复「朱春霖，你好！」可见（response_url）✅；
  `langchain_model_call elapsed=1563-1734ms`（1.6-1.7s，LLM 瓶颈持续解决）；identity 正常（cache miss→hit）。
- **Live C 查看消息队列**：`turn_queue_status succeeded` 事件层 ✅，绕过 Agent ✅；用户面**回复不可见**（plain text）❌（上轮 PASS→本轮 FAIL，回归）。
- Live B（burst positions/rejection）未单独测 —— Blocker A 已确认 plain text 首包不可见，位置/拒绝同样不可见。

### M3-00A 五项用户面必达项（5c8b4aa vs cd1d02d）

| # | 必达项 | cd1d02d | 5c8b4aa | 变化 |
|---|---|---|---|---|
| 1 | 被拒消息可见 | ❌ 未验证 | ❌ FAIL（plain text）| 仍 blocked |
| 2 | pending 位置可见 | ❌ 未验证 | ❌ FAIL（plain text）| 仍 blocked |
| 3 | 状态命令立即可见 | ✅ PASS | ❌ **FAIL**（plain text，已确认）| **回归** |
| 4 | 正常消息先 ack 后回复 | ✅ PASS | ❌ **FAIL**（plain text，已确认）| **回归** |
| 5 | TTL 过期可见 | ✅ PASS | ✅ PASS（response_url 不变，Live E 副产物再确认）| 持平 |

**1/5 PASS（cd1d02d 是 3/5）→ 5c8b4aa 对用户面是净回归。** Blocker A 的 plain text 假设错误，把上轮 cd1d02d 已验证可见的
ack/状态（#3/#4）退化成不可见，且未解决 #1/#2。

### 事件与安全核对（Phase 11）

- 事件齐全：`wecom_turn_queued`/`turn_queue_rejected`/`turn_queue_status`/`turn_queue_wait_timeout`/`response_url_delivery`/
  `identity_lookup`（含 `status=error error_type=TimeoutError`）/`langchain_model_call` 全部出现。
- identity timeout **不阻塞其他事件产生**（Live F：超时后 gateway 持续响应，backlog 事件继续流出）✅。
- response_url 最终投递 exactly-once（`response_url_consumed` 先 claim 后 I/O）✅；identity error 回复也走 response_url（cc=54）。
- 被拒消息无 `langchain_model_call`；TTL 过期消息无 `langchain_model_call` ✅。
- 脱敏：events 全文 grep `zhuchunlin|朱春霖|wecom:zhuchunlin|password|token=|secret|response_url|response_code` **0 命中**（hmac + content_chars）；
  `identity_lookup status=error error_type=TimeoutError` 不含 OA 明文 ✅；MCP audit 与 enterprise events 分离 ✅。
- **0** SIGBUS / 0 exit 138 / 0 IntegrityError / 0 HTTP non-200。OTel port 3000 既有噪音（非阻断）。
- frozen 首包逻辑：同 msgid 重试返回相同首包内容（`initial_response_content` 冻结），不重复触发 turn ✅（事件层验证）。

### 最终结论

- **5c8b4aa Blocker A = FAIL/回归**：plain text 首包不被 WeCom AI Bot 渲染，ack/位置/状态/拒绝用户面不可见；
  把 cd1d02d 已 PASS 的 #3/#4 退化成 FAIL。**首包应回到 `make_text_stream finish=true`**，burst 问题另解。
- **5c8b4aa Blocker B = 部分生效（净进步）**：sidecar hang 不再冻结 gateway（上轮硬伤已解），8s 超时 + identity error 降级 + 进程死后自动拉新均生效；
  缺口是 sidecar 挂起（进程活着）时不自动 SIGKILL 回收。Blocker B 的方向正确，建议保留并补回收逻辑。
- **M3-00A 用户面 = 1/5 PASS，仍不能关闭**（#1/#2/#3/#4 FAIL）。
- **事件层**：队列治理逻辑继续 PASS；identity 有界化事件层 PASS；无新增事件层回归。
- **M3-00B 遗留**：硬抢占（卡死 turn 仍靠 195s post-hoc 终止，Live E #2 卡 3.8min 佐证）—— 不混入本轮判定。

### 给 Codex 的下一步

1. **Blocker A（最高优先）**：首包从 `make_text` 改回 `make_text_stream finish=true`（cd1d02d 对单条已验证可见）。
   burst 下 ack/位置/拒绝不可靠的根因更可能是"并发回调时 inline 响应被 WeCom 重传风暴淹没"，而非 stream 格式本身。
   若要彻底稳，考虑 ack/位置/拒绝也走 response_url（需解决 one-shot 与最终回复的冲突，见上）。
2. **Blocker B（保留+补）**：`_lookup_locked` 超时后对旧 sidecar 进程发 **SIGKILL**（非 SIGTERM，stopped 态 SIGTERM 无效），
   确保 `_ensure_process` 下次拉新 JVM；加 sidecar 健康探针（进程活着但 JDBC 挂起时也要能识别并回收）。
3. Blocker B 修好后，重测 Live E（顺序 5 条身份，cache off）确认不再 hang。

当前环境已恢复标准配置：gateway `qwen-flash`（pid 49017），`MAXSIZE=3 / TTL=240`，`IDENTITY_CACHE_ENABLED=true`，
`SIDECAR_TIMEOUT=8 / LOOKUP_TIMEOUT=15`，`work_8c669fb4` 暂留 draft/intake。

---

## Mac mini M3-00A user-facing re-verification（2026-07-16，commit `5a63523`）— 4/5 用户面 PASS，拒绝重复 + active LLM hang 待修 ⚠️

复验 Codex 第三轮修复（commit `5a63523`）：
**A** = 撤回 5c8b4aa 的 plain text，首包恢复 `stream finish=true`；拒绝消息（无最终回复）额外用自身 `response_url` 投递同一拒绝提示；
active/pending 的 response_url 仍留给最终回复/TTL。**B** = sidecar 8s 超时 `SIGKILL` 强制回收（`_stop_process(force=True)`），下条查询拉新 JVM；
新增自动化测试是真实子进程 SIGSTOP→强制回收（enterprise 84）。

- 环境：Mac mini，分支 `enterprise/v0.0.9-files-plugin`，commit `5a63523`（GitHub 经代理拉取 `d95cdb0..5a63523`，GitLab 补推 ✅）。
- gateway pid 62007（python）/61998（uv），`*:12000`，无 proxy env，启动 11:18:53。
- `.env`（本机不入库）：`EXECUTION_MODE=sidecar`、`SIDECAR_TIMEOUT=8`、`LOOKUP_TIMEOUT=15`、`IDENTITY_CACHE_ENABLED=true/TTL=600`、
  `MAXSIZE=3`、`TTL=240`、`MEMORY_PROMPT_MAX=12000/3000`、`MODEL=qwen-flash`。
- smoke override（本机不入库）：`pack_version=1.2.0-mac-smoke.1`、`owning_org=信息技术部`。workitem `work_8c669fb4`（draft/intake）。

### 静态（Phase 3）— 全 PASS（enterprise 新基线 84）

`agentseek-wecom` **57**、`agentseek-enterprise` **84**（+1，SIGSTOP→SIGKILL 回收用例）、`agentseek-work` **90**、
`agentseek-langchain` **18**、`agentseek-files` **47**、example **73+1skip**、`test_templates_render` **25** ✅。
`make typecheck-wecom` All passed ✅；ty touched（`channel.py`+`dm_staff_provider.py`）All passed ✅；
`make docs-test` 0.30s ✅；`git diff --check` clean ✅；
`mcp_policy.py`+`tools.py` vs `68d7b25` **zero-diff** ✅；ruff（wecom src+tests + identity provider）All passed ✅。

### Live A — #3/#4 回归已撤销：用户面 PASS ✅

- `你好`（stream b0719b26 之前）：ack「已收到，正在处理...」**可见** ✅；最终回复「朱春霖，你好！👋...」**可见** ✅。
  事件：identity cache=miss found → `langchain_model_call elapsed=2075ms`（2.1s）→ response_url_delivery cc=19 succeeded。
- `查看消息队列`：`turn_queue_status succeeded`（绕过 Agent）→ 「当前没有正在处理或等待处理的消息」**可见** ✅。
- **对比 5c8b4aa**（plain text）：那时 ack/状态都不可见。本轮恢复 `stream finish=true` 后重新可见 → **#3/#4 回归已撤销**。

### Live B — burst（active+3pending+拒绝）：4/5 可见，拒绝重复 ❌

5 条连发（`请告诉我当前时间` / `请介绍当前数字员工的主要能力` / `我是谁` / `我的部门是什么` / `我的岗位是什么`）。
用户企微实收 **10 条回复**，映射如下（MAXSIZE=3，1 active + 3 pending + 1 拒绝）：

| 消息 | 角色 | 回复①（inline stream 首包）| 回复②（response_url 终态）| 评价 |
|---|---|---|---|---|
| 请告诉我当前时间 | active（卡死→超时）| 「已收到，正在处理...」| 「本次处理超时，请稍后重试」| ✅ ack+结果 |
| 请介绍能力 | pending 1 | 「排在等待队列第 1 位」| TTL「等待处理时间过长已取消」| ✅ 位置+结果 |
| 我是谁 | pending 2 | 「排在等待队列第 2 位」| TTL 取消 | ✅ 位置+结果 |
| 我的部门 | pending 3 | 「排在等待队列第 3 位」| TTL 取消 | ✅ 位置+结果 |
| 我的岗位 | **拒绝** | 「未进入队列...」| 「未进入队列...」（同一拒绝）| ❌ **重复** |

**重大进步**：burst 下 ack + 位置 1/2/3 + 拒绝 **首次全部用户面可见**（上几轮 stream//plain text 在 burst 下都不可见）。
事件层：`turn_queued` pending=1/2/3、`turn_queue_rejected` pending=3 ✅；拒绝 `response_url_delivery cc=50 succeeded` ✅（双投递生效）。

**两个真问题：**

1. **拒绝重复显示**（① + ② 同一拒绝文 2 次）—— Codex 预见的 UX 缺口，确认发生：拒绝既走 inline stream 首包，又走自身 response_url，
   WeCom 两次都渲染。**#1 = 可见但重复**。且 WeCom 对拒绝 msgid 持续重传至 03:37（response_url 投递后 4.5min 仍重传），未干净停止。
   **需 Codex**：拒绝二选一 —— 要么只 inline stream（但 burst 下不可靠，本轮回退原因），要么只 response_url（可靠但用户首包延迟）；
   或 inline 与 response_url 投递不同内容（首包「已收到，本条未入队」，response_url 给详细拒绝原因）。
2. **active turn 新 hang 位置**：`请告诉我当前时间`（active）identity cache=hit 03:27:38 正常，**之后 5 分钟无 `langchain_model_call`** →
   `03:32:44 wecom_turn_timeout`（卡在 **identity 之后、LLM 之前**）。**不是 sidecar**（identity 已解析），疑似 dashscope qwen-flash 端点
   或 agent setup 阶段 hang。Live A 单条 `你好` LLM 2s 正常，故可能 burst/特定 prompt 触发的偶发。
   连带后果：卡死的 active 不释放单飞锁（M3-00B 硬抢占缺失）→ pending 2/3/4 没机会跑、全 TTL 取消（用户没拿到真实回复）。

### Blocker B（sidecar SIGKILL 回收）— 代码+静态验证 PASS，live SIGSTOP 未测

- 代码确认：`dm_staff_provider._lookup_locked` 超时 → `_stop_process(force=True)` → `_kill_process`（`process.kill()`=SIGKILL + wait 2s）✅。
  SIGKILL 对 stopped 进程有效（5c8b4aa 的 SIGSTOP 回收缺口应已闭合）。
- 静态：enterprise 84 含新增「真实子进程 SIGSTOP→强制回收」用例 PASS（非 mock）✅。
- **live SIGSTOP 故障注入（Live E）本轮未执行**（用户选择先记录）。
  依据：上轮 5c8b4aa Live F 已验证「8s 超时 + identity error 降级 + gateway 不冻结 + 进程真死后自动拉新」；
  本轮 `force=True` SIGKILL 是对上轮"冻结态不回收"缺口的代码补丁 + 静态用例覆盖。建议 Codex 修完拒绝重复后，下轮一并复测 Live E。

### M3-00A 五项用户面必达项（5a63523）

| # | 必达项 | 5c8b4aa | 5a63523 | 结果 |
|---|---|---|---|---|
| 1 | 被拒消息可见+停止重传 | ❌ FAIL | ⚠️ **可见但重复** + 重传未净停 | 仍 blocked |
| 2 | pending 位置 1/2/3 可见 | ❌ FAIL | ✅ **PASS**（首次 burst 下可见）| 通过 |
| 3 | 状态命令立即可见 | ❌ FAIL | ✅ PASS（Live A）| 通过 |
| 4 | 正常消息先 ack 后回复 | ❌ FAIL | ✅ PASS（Live A）| 通过 |
| 5 | TTL 取消可见 | ✅ PASS | ✅ PASS | 通过 |

**4/5 PASS（5c8b4aa 是 1/5，cd1d02d 是 3/5）→ 本轮是迄今最好成绩。** Blocker A 撤回 plain text、恢复 stream finish=true，
使 burst 下 ack/位置/状态首次全部可见（#2/#3/#4）。唯一剩 #1：拒绝**可见但重复**（inline + response_url 双投递）+ WeCom 重传未干净停止。

### 事件与安全核对（Phase 10）

- 事件齐全：`wecom_turn_queued`/`turn_queue_rejected`/`wecom_turn_queue_status`/`wecom_turn_queue_wait_timeout`/`response_url_delivery`/
  `identity_lookup`/`langchain_model_call` 全部出现。
- rejected 消息无 `langchain_model_call` ✅；TTL 过期消息无 `langchain_model_call` ✅。
- response_url exactly-once：active/pending/rejected 各自 response_url 只消费 1 次（`response_url_consumed` 先 claim 后 I/O）✅；
  拒绝的 response_url_delivery cc=50 succeeded（独立于 inline）。
- 脱敏：events 全文 grep `zhuchunlin|朱春霖|wecom:zhuchunlin|password|token=|secret|response_url|response_code` **0 命中** ✅；
  MCP audit 与 enterprise events 分离 ✅。
- **0** SIGBUS / 0 exit 138 / 0 Traceback / 0 IntegrityError / 0 HTTP non-200。gateway 全程 CPU 0.1-1.7%（**未被 burst 冻结**，
  Blocker B 的核心改进持续生效）。OTel port 3000 既有噪音（非阻断）。
- frozen 首包：同 msgid 重试返回相同首包内容（`initial_response_content` 冻结），不重复触发 turn ✅。

### 最终结论

- **5a63523 Blocker A = 大幅进步**：撤回 plain text、恢复 `stream finish=true`，burst 下 ack/位置/状态/拒绝首次全部用户面可见
  （#2/#3/#4 PASS，#1 可见）。**唯一缺口 = 拒绝重复显示**（inline stream + response_url 双投递同一拒绝文）+ WeCom 重传未净停。
- **5a63523 Blocker B = 代码+静态 PASS**（SIGKILL 回收补丁 + 真实 SIGSTOP 用例），live SIGSTOP 本轮未测（建议下轮补）。
- **新发现 = active LLM/agent hang**：identity 正常但 LLM 之前卡 5min → turn_timeout（非 sidecar；疑似 dashscope 端点/agent setup 偶发），
  连带 M3-00B 硬抢占缺失导致队列阻塞、pending 全 TTL 取消。
- **M3-00A 用户面 = 4/5 PASS，仍不能正式关闭**（#1 拒绝重复+重传未净停）。但已非常接近 —— 拒绝去重 + active hang 排查后即可关闭。
- **事件层**：队列治理 + identity 有界化 + SIGKILL 回收事件层全 PASS；无新增事件层回归。
- **M3-00B 遗留**：硬抢占（卡死 turn 仍靠 195s post-hoc 终止，Live B active 卡 5min 佐证）—— 不混入本轮判定。

### 给 Codex 的下一步

1. **拒绝去重（最高优先，关 M3-00A 的最后 1 项）**：拒绝的 inline stream 首包与 response_url 不要投递**同一文本**。
   建议：inline 首包给「已收到，本条未进入队列」（轻量即时），response_url 给完整拒绝原因（含 N 条等待数量）；或二选一。
   同时核实为何 WeCom 在 response_url 投递后仍对拒绝 msgid 重传 4.5min（是否 response_url 的 finish 信号未让 WeCom 停重传）。
2. **active LLM/agent hang 排查**：identity 后、`langchain_model_call` 前卡 5min。加 agent-setup 阶段 + LLM 调用的分段耗时日志
   （现在 `langchain_model_call` 只记调用本身，看不到"调用前准备"耗时）；核实是否 dashscope qwen-flash 端点偶发 hang（Live A 单条 2s 正常）。
3. **Blocker B live 复测**：拒绝去重修复后，下轮一并做 Live E（SIGSTOP→SIGKILL 回收）确认旧 PID 自动消失、新 PID 自动拉起。
4. M3-00B 硬抢占仍是 live 可用性的根因之一（卡死 turn 阻塞队列），独立切片推进。

当前环境已恢复标准配置：gateway `qwen-flash`（pid 62007），`MAXSIZE=3 / TTL=240`，`IDENTITY_CACHE_ENABLED=true`，
`SIDECAR_TIMEOUT=8 / LOOKUP_TIMEOUT=15`，`work_8c669fb4` 暂留 draft/intake。

---

## Mac mini M3-00A final acceptance re-verification（2026-07-16，commit `2956498`）— 分段观测定位 active hang，M3-00A 仍不能关闭 ⚠️

复验 Codex 第四轮修复（commit `2956498`）：**拒绝去重**（移除 `response_url_terminal_content` + `deliver_via_response_url_before_initial`，
拒绝只走 inline stream finish=true、**不消费 response_url**）+ **`langchain_run_stage` 分段观测**（run_model/spec_resolve/prompt_enrichment/context_build/model_invoke，
含 timing + size，不含正文）。

- 环境：Mac mini，commit `2956498`（GitHub 经代理拉取 `8a3fe5e..2956498`，GitLab 补推 ✅）。gateway pid 49478，`:12000`，无 proxy。
- `.env`：`EXECUTION_MODE=sidecar`、`SIDECAR_TIMEOUT=8`、`LOOKUP_TIMEOUT=15`、`IDENTITY_CACHE_ENABLED=true/600`、`MAXSIZE=3`、`TTL=240`、`qwen-flash`。
- smoke override（本机不入库）：`pack_version=1.2.0-mac-smoke.1`、`owning_org=信息技术部`。workitem `work_8c669fb4`（draft/intake）。

### 静态（Phase 2）— 全 PASS（langchain 新基线 19）

`agentseek-wecom` **57**、`agentseek-enterprise` **84**、`agentseek-langchain` **19**（+1 stage 用例）、`agentseek-files` **47**、
`agentseek-work` **90**、example **73+1skip**、`test_templates_render` **25** ✅。
`make typecheck-wecom`/ty touched（`channel.py`+langchain `plugin.py`）All passed ✅；`make docs-test` 0.30s ✅；
`git diff --check` clean ✅；`mcp_policy.py`+`tools.py` vs `68d7b25` **zero-diff** ✅；ruff All passed ✅。

### ✅ langchain_run_stage 分段观测 — 完整生效（本轮主要交付）

`你好`（单条）完整 stage 时间线（UTC）：

| 时间 | stage | status | elapsed |
|---|---|---|---|
| 05:10:21 | identity_lookup | found (cache=miss) | sidecar 冷启动 ~18s |
| 05:10:38 | run_model_stream | started | |
| 05:10:42 | spec_resolve | succeeded | 706ms |
| 05:10:45 | prompt_enrichment | succeeded | 快 |
| 05:10:48 | context_build | succeeded | 1ms |
| 05:10:51 | model_invoke | started | |
| 05:11:01 | langchain_model_call | succeeded | **1599ms**（LLM 本体）|
| 05:11:08 | model_invoke | succeeded | **16976ms**（含 ~15s runnable/provider 开销）|
| 05:11:11 | run_model_stream | succeeded | 32522ms |
| 05:11:21 | response_url_delivery cc=19 | succeeded | 最终回复 |

分段事件**不含用户原文/回复正文/OA/姓名/凭证**（仅 stage/status/elapsed/size）✅。这使 pre-model setup 卡顿可区分于 runnable/provider 卡顿。

### Live A（单条）— #3 PASS，#4 ack 渲染时序异常

- `你好`：最终回复「朱春霖，你好！👋有什么可以帮你的？」**可见** ✅；但用户**先看到最终回复，后看到 ack「已收到，正在处理...」**
  （ack 是 stream finish=true 首包，事件层 05:10:03 已发送，但 WeCom 延迟到最终回复后才渲染）。ack 投递正确，**WeCom 渲染时序异常**。
- `查看消息队列`：`turn_queue_status succeeded`（绕过 Agent，无 stage/LLM）→ 「当前没有正在处理或等待处理的消息」**可见** ✅ → **#3 PASS**。

### Live B（burst）— active 卡死再现，**langchain_run_stage 精确定位**：model_invoke 阶段 ❌

5 条连发（`请介绍能力` active + `现在几点`/`我是谁`/`我的部门`/`我的岗位`）。

- 队列形成正确：active `8a3f1f43` + pending 1/2/3（`40ab7bdd`/`a1749c12`/`4b79b02b`）+ 第 5 条 `46e2cdf3` **turn_queue_rejected** ✅。
- **active 卡死**：identity cache=hit 05:16:12 正常 → run_model_stream → spec_resolve → prompt_enrichment → context_build →
  **model_invoke started 05:17:13，之后无 `langchain_model_call`** → gateway CPU 跌到 0.0%（卡死）。
- **分段观测精确定位**：卡在 **`model_invoke` started 之后、`langchain_model_call` 之前** = **runnable/provider 入口**（spec 描述的
  "有 model_invoke started、但没有 langchain_model_call"）。**不是 LLM 端点**（单条 你好 LLM 1.6s 正常），是 model_invoke 阶段
  （runnable 绑定/工具组装）在 burst 下 hang。这会再次级联：active ~195s turn_timeout → 不释放单飞锁（M3-00B 硬抢占缺失）→ pending 全 TTL 取消。
- **用户面**：burst 期间「一直没反应」—— active 卡死 + WeCom 对 inline 首包（ack/位置/拒绝）在 active-hang 期间不渲染 +
  response_url 终态投递（timeout/TTL）要等 active 超时后才发 → **用户等数分钟无任何反馈**。体验差，用户明确反馈"需要改善"。

### 拒绝去重 — 生效（无重复）但 WeCom 重传未停 ⚠️

- 拒绝 `46e2cdf3`：`turn_queue_rejected` 触发，**无 `wecom_response_url_delivery`**（去重生效，拒绝只走 inline stream，不再双投递）✅。
- **但** WeCom 对拒绝 msgid 持续重传（05:18:06→05:19:05 已 6+ 次，跨 1min+）。原因：拒绝无 response_url 终态信号，WeCom 收不到"完成"→ 不停重试。
- **两难确认**：5a63523（inline+response_url 双投递）→ 拒绝显示 2 次（重复）但 WeCom 最终停重传；
  2956498（inline-only）→ 拒绝不重复但 WeCom 不停重传。**两种方案都不满足 #1"可见 + 停止重传"**。
  根因：WeCom AI Bot 对"无最终回复"的消息（拒绝/状态）需要一个"终态"信号才会停重传，而 inline stream finish=true 不被接受为终态。

### Blocker B（sidecar SIGKILL）— 本轮 live SIGSTOP 未测（active hang 占主导）

代码+静态（enterprise 84 含真实 SIGSTOP→SIGKILL 用例）上轮已验证。本轮因 active hang 反复、用户选择先记录，live SIGSTOP 补测再次延后。
建议 Codex 修完 active hang 后下轮一并补 Live SIGSTOP。

### M3-00A 五项用户面必达项（2956498）

| # | 必达项 | 结果 | 说明 |
|---|---|---|---|
| 1 | 被拒消息可见+停止重传 | ❌ FAIL | 去重生效（不重复）但 WeCom 不停重传；active-hang 期间不可见 |
| 2 | pending 位置 1/2/3 可见 | ❌ FAIL | active 卡死 + inline 首包不渲染 → burst 期间不可见 |
| 3 | 状态命令立即可见 | ✅ PASS | Live A 单条 |
| 4 | 正常消息先 ack 后回复 | ⚠️ 部分 | ack 投递正确但 WeCom 渲染时序异常（先最终回复后 ack）；单条可用 |
| 5 | TTL 取消可见 | ✅ PASS | response_url 路径不变（历轮 + 本轮 active 超时后 pending TTL 会经 response_url 投递）|

**2.5/5 PASS（#3/#5 + #4 部分）**。对比 5a63523（4/5）—— 本轮拒绝去重修了"重复"，但 **active hang 再现**让 burst 下 #1/#2 重新不可见，
且拒绝 WeCom 重传未停。**M3-00A 仍不能关闭。**

### 健康 + 脱敏（Phase 7）

- 0 SIGBUS / 0 Traceback / 0 IntegrityError / 0 HTTP non-200。gateway 单条正常（CPU 1.3%），burst active 卡死时 CPU 0%（hang）。
- response_url exactly-once（active/pending 各自 1 次；拒绝 0 次）✅。rejected/expired 消息无 `langchain_model_call` ✅。
- events 全文 grep `zhuchunlin|朱春霖|wecom:zhuchunlin|password|token=|secret|response_url|response_code` **0 命中** ✅；
  `langchain_run_stage` 仅 stage/status/elapsed/size，无正文 ✅；MCP audit 与 enterprise events 分离 ✅。
- OTel port 3000 既有噪音（非阻断）。

### 最终结论

- **2956498 主要交付 = `langchain_run_stage` 分段观测，完整生效**，并**精确定位了 active hang 位置**：
  `model_invoke` started 后、`langchain_model_call` 前 = **runnable/provider 入口**（非 LLM 端点）。这是修复 active hang 的关键线索。
- **拒绝去重生效**（inline-only，无重复），但暴露**两难**：WeCom 对"无最终回复"消息需要终态信号才停重传，inline stream finish=true 不被接受为终态。
  → **#1 仍不满足**（要么重复、要么不停重传）。需 Codex 找到让 WeCom 接受 inline 为终态的方法，或拒绝走一个"一次性终态 response_url 但内容不与首包重复"的方案。
- **active hang（model_invoke）是 burst 体验差的根因**，连续两轮（5a63523、2956498）复现。单条正常（你好 LLM 1.6s），
  burst 下 model_invoke 阶段 hang → 数分钟无反馈 → 用户明确反馈体验差。**这是最高优先修复项**。
- **M3-00A 用户面 = 2.5/5，仍不能关闭**（#1/#2 blocked，#4 部分）。
- 事件层：队列治理 + identity 有界化 + 分段观测全 PASS；无新增事件层回归。M3-00B 硬抢占缺失（卡死 turn 阻塞队列）仍是放大器。

### 给 Codex 的下一步（按优先级）

1. **修复 active model_invoke hang（最高优先，burst 体验根因）**：`langchain_run_stage` 已定位 = model_invoke started 后卡在
   runnable/provider 入口（工具绑定/runnable 组装）。单条正常 → 疑似 burst 并发下该阶段有阻塞调用（sync 操作占 event loop？runnable 构建非幂等/死锁？）。
   建议：把 model_invoke 阶段的 runnable 构建/工具绑定移到 `asyncio.to_thread` 或预构建可复用 runnable；加分阶段耗时已可见，定位具体子步骤。
2. **解决拒绝 WeCom 重传两难（#1）**：让 WeCom 接受拒绝为终态。方案：(a) 拒绝仍走一次性 response_url，但首包 inline 给"已收到，本条未入队"
   （轻量）、response_url 给完整拒绝原因（内容不同 → 不重复 + 有终态信号）；(b) 调研 WeCom AI Bot 对 stream finish=true 的"终态"判定，
   是否需特定字段。当前 inline-only 导致不停重传，inline+response_url 导致重复，需第三条路。
3. **ack 渲染时序（#4）**：你好 ack（stream finish=true）被 WeCom 延迟到最终回复后才渲染。调研是否首包需更早返回或 WeCom 渲染队列问题。
4. sidecar live SIGSTOP 补测、M3-00B 硬抢占 —— active hang 修完后一并推进。

当前环境已恢复：gateway `qwen-flash`（pid 49478），`MAXSIZE=3 / TTL=240`，`IDENTITY_CACHE_ENABLED=true`，`SIDECAR_TIMEOUT=8 / LOOKUP_TIMEOUT=15`。

---

## Mac mini M3-00A single-stream delivery live verification（2026-07-16，commit `dec92a2`）— 单 stream 简化生效，但 active runnable-setup hang 未解 + ack/位置结构性不可见 ⚠️

复验 commit `dec92a2`（含 `e241d59` 模型请求超时 + `langchain_model_call started` 事件 + 分段观测；`dec92a2` 普通聊天恢复**单 stream** 投递，
普通 text/voice/media 丢弃 `response_url`，response_url 仅留 MinerU pending 文件）。

- 环境：Mac mini，commit `dec92a2`（GitHub 经代理拉取 `a41f236..dec92a2`，GitLab 补推 ✅）。gateway pid 93098，`:12000`，无 proxy。
- `.env`（本机不入库，新增）：`AGENTSEEK_MODEL_REQUEST_TIMEOUT_SECONDS=60`、`AGENTSEEK_MODEL_MAX_RETRIES=0`
  （+ 既有 `EXECUTION_MODE=sidecar`/`SIDECAR_TIMEOUT=8`/`LOOKUP_TIMEOUT=15`/`MAXSIZE=3`/`TTL=240`/`LANGCHAIN_RUN=180`/`TURN_TIMEOUT=195`/`qwen-flash`）。
- smoke override（本机不入库）：`pack_version=1.2.0-mac-smoke.1`、`owning_org=信息技术部`。workitem `work_8c669fb4`（draft/intake）。

### 静态（Phase 4）— 全 PASS（新基线）

`agentseek-wecom` **58**（+1）、`agentseek-langchain` **20**（+1 stage 用例）、`agentseek-enterprise` **84**、`agentseek-files` **47**、
`agentseek-work` **90**、example **74+1skip**（+1 test_settings）、`test_templates_render` **25** ✅。
ty wecom src+tests All passed ✅；ruff（`channel.py`+`test_channel.py`）All passed ✅；`make docs-test` 0.30s ✅；
`git diff --check` clean ✅；`mcp_policy.py`+`tools.py` vs `68d7b25` **zero-diff** ✅。

### ✅ 单 stream 简化生效（本轮主要交付）

- **普通聊天 0 `wecom_response_url_delivery`** ✅（`你好` + `查看消息队列` 全程 0 次，确认 response_url 已从普通聊天移除）。
- `channel.py` 改动确认：text/voice/media 路径 `stream.response_url = None` + `response_url=None`，丢弃 callback 携带的 response_url。
- **`langchain_model_call` started 事件生效** ✅：`你好` 单条 `06:04:46 started → 06:04:51 succeeded (4828ms)`，区分了"请求在飞"vs"卡在 model callback 之前"。
- **#3 状态命令 PASS** ✅：`查看消息队列`（`4eb29e29`）在 `你好` 处理中（active）发送 → `turn_queue_status active=True succeeded`，绕过 Agent，
  企微显示「正在处理：1 条，已运行约 59 秒；等待处理：0 条」**用户可见** ✅。

### Live A（单条 你好）— #4 ack 不可见（结构性）

`你好`（`78bb55cb`）完整 stage：identity(found,cache=miss) → run_model_stream → spec_resolve(761ms) → prompt_enrichment → context_build(1ms) →
**model_invoke started → [44s] → langchain_model_call started → succeeded(4828ms)** → model_invoke succeeded(55001ms) → run_model_stream(69761ms) → stream_finished cc=19。
- 最终回复「朱春霖，你好！👋有什么可以帮你的？」**可见** ✅。总 turn ~116s（慢但完成，未 hang）。
- **44s 卡在 model_invoke started → model_call started 之间**（runnable/provider 入口，工具绑定/runnable 组装）—— 单条能完成（44s），是瓶颈但非 hang。
- **#4 ack「已收到，正在处理...」用户不可见** ❌。用户明确："没有收到 ack"。**根因 = WeCom AI Bot 只渲染 `finish=true` 内容，不渲染 `finish=false` 中间态**。
  单 stream 的 ack（`finish=false`）和 pending 位置（`finish=false`）都不被渲染；只有终态（状态/拒绝/TTL/最终回复，`finish=true`）可见。
  这是 v0.0.9 时代就有的限制（当时用户看到的"已收到..."是 LLM 慢回复正文，非系统 ack）。

### Live B（burst）— active runnable-setup hang 第 3 轮复现，60s 超时未覆盖 ❌

5 条连发（`请介绍能力` active + `现在几点`/`我是谁`/`我的部门`/`我的岗位`）。
- 队列形成正确：active `75842dd9` + pending 1/2/3（`f8bc6a76`/`a7ce6cd4`/`41f67a86`）+ 第 5 条 `5187e2dc` `turn_queue_rejected` ✅。
- **active 卡死**：identity found 06:18:16 → run_model_stream → spec_resolve → prompt_enrichment → context_build →
  **model_invoke started 06:19:14，之后无 `langchain_model_call started`** → gateway CPU 0.0%（hang）→ 用户"几分钟什么都还没返回"。
- **`langchain_run_stage` 第 3 轮精确定位同一位置**：`model_invoke` started 后、`langchain_model_call` 前 = **runnable/provider 入口**。
  连续 3 轮（`5a63523`/`2956498`/`dec92a2`）burst 下一致复现；单条（你好）能过。

**🔴 关键结论：`e241d59` 的 60s 模型超时修在了错误的层。**
- 60s 超时绑在 **model_request**（`langchain_model_call` 的 HTTP 请求）上。
- 但 hang 发生在 **model_call 启动之前**（model_invoke 阶段的 runnable 构建/工具绑定）。
- model_call 根本没启动 → 60s 超时**永不触发** → 只能靠 195s `turn_timeout` post-hoc 终止（且协程不释放单飞锁 → 队列阻塞、pending 全 TTL 取消）。
- **需 Codex**：给 **model_invoke 的 runnable 构建阶段**加有界超时（`asyncio.timeout` 包住 runnable 构建/工具绑定），或把 runnable 构建移到
  `asyncio.to_thread`（不占 event loop），或预构建可复用 runnable（23 个工具的 schema 绑定不要每 turn 重做）。

### M3-00A 五项用户面必达项（dec92a2）

| # | 必达项 | 结果 | 说明 |
|---|---|---|---|
| 1 | 被拒消息可见+只 1 次+不进 Agent | ⚠️ 未干净验证 | 拒绝事件触发（finish=true，单 stream 不重复），但 active hang 期间用户"什么都没返回"，未确认可见 |
| 2 | pending 位置 1/2/3 可见 | ❌ 结构性不可见 | 位置是 `finish=false`，WeCom 不渲染（同 #4 ack）|
| 3 | 状态命令可见+绕过 Agent | ✅ PASS | Live A/C，busy 下显示 active=1+运行时长 |
| 4 | 先 ack 后同 stream 最终回复 | ❌ ack 不可见 | ack `finish=false` 不被 WeCom 渲染；最终回复可见但无前置 ack |
| 5 | TTL 取消可见+不进 Agent | ✅ PASS | TTL `finish=true` 终态可见（历轮 + 本轮 response_url=0 确认走 stream）|

**~2.5/5（#3/#5 + #1 部分）**。M3-00A 仍不能关闭。两个独立根因（见下）。

### 健康 + 脱敏（Phase 12）

- 0 SIGBUS / 0 Traceback / 0 IntegrityError / 0 HTTP non-200。普通单条 turn 正常；burst active hang 时 CPU 0%。
- 普通 turn `wecom_response_url_delivery` = **0** ✅（单 stream 确认）。rejected/expired 消息无 `langchain_model_call` ✅。
- events 全文 grep `zhuchunlin|朱春霖|wecom:zhuchunlin|password|token=|secret|response_url|response_code` **0 命中** ✅；
  `langchain_run_stage`/`langchain_model_call` 仅 stage/status/elapsed/size，无正文/OA/姓名 ✅；MCP audit 与 enterprise events 分离 ✅。

### 未测项

- **Live D（TTL=10）/ Live F（MinerU pending 文件）/ Live G（sidecar SIGSTOP）/ 基础回归（我是谁/记忆/pgvector/WorkItem/知识库/MCP/M2）**：
  因 active hang 反复占主导、用户明确反馈体验差需改善，本轮优先定位 hang 根因，未逐一执行。单条 `你好` 已间接覆盖身份/LLM/记忆/WorkItem 路径（identity found + model_call succeeded + stream_finished，无回归）。建议 active hang 修复后下轮系统补测。

### 最终结论

- **dec92a2 单 stream 简化 = 成功**：0 response_url_delivery、`langchain_model_call started` 事件、拒绝/状态/TTL 走同 stream finish=true（不重复、无 exactly-once 耦合）。
  状态命令（#3）、TTL（#5）用户可见。这是对双通道状态机的正确简化方向。
- **但暴露两个独立根因，均阻塞 M3-00A 关闭：**
  1. **结构性：WeCom 只渲染 `finish=true`** → 单 stream 的 ack（#4）和 pending 位置（#2）都是 `finish=false`，**用户永远看不到**。
     这是协议限制，不是 bug。要让 #2/#4 可见，ack/位置必须 `finish=true`（终结 stream），但这与"同 stream 后续投递最终回复"冲突 —— 回到双通道问题。
     **需决策**：要么接受 ack/位置不可见（放宽 #2/#4，只保留 #1/#3/#5），要么为 ack/位置设计独立的 `finish=true` 一次性投递（如早期 response_url 思路，但解决重复）。
  2. **active runnable-setup hang（3 轮复现）**：`model_invoke` 阶段、`langchain_model_call` 之前。**60s 模型超时（e241d59）修错层**（超时在 request，hang 在 setup）。
     需给 runnable 构建加有界超时/移出 event loop/预构建。
- **M3-00A 用户面 = ~2.5/5，仍不能关闭**（#2/#4 结构性 + active hang）。
- 事件层：队列治理 + identity 有界化 + 分段观测 + 单 stream 全 PASS；无新增事件层回归。
- **是否进入 M3-00B/M3 正式切片：否。** active runnable-setup hang 是 live 可用性硬阻塞（用户"几分钟无响应"），必须先修。

### 给 Codex 的下一步（按优先级）

1. **修 active runnable-setup hang（最高优先，3 轮复现）**：`langchain_run_stage` 已定位 = `model_invoke` started 后、`langchain_model_call` 前
   （runnable 构建/23 工具绑定）。60s request 超时无效（hang 在 request 之前）。方案：(a) `asyncio.timeout` 包 runnable 构建；(b) runnable 构建移 `asyncio.to_thread`；
   (c) 预构建可复用 runnable（不每 turn 重绑工具）。修后 burst active 应在 8-15s 有界完成或明确降级。
2. **决策 ack/位置可见性（#2/#4 结构性）**：WeComm 只渲染 finish=true。要么放宽 #2/#4（接受单 stream 无可见 ack/位置），要么为 ack/位置设计独立 finish=true 投递
   （注意不与最终回复重复 —— 例如 ack 首包 finish=true「已收到，正在处理」，最终回复走第二个 stream/response_url，但这又回到双通道）。需明确产品取舍。
3. 修完 active hang 后，下轮系统补 Live D/F/G + 基础回归。

当前环境已恢复：gateway `qwen-flash`（pid 93098），`MAXSIZE=3 / TTL=240`，`MODEL_REQUEST_TIMEOUT=60 / MAX_RETRIES=0`，`IDENTITY_CACHE_ENABLED=true`，`SIDECAR_TIMEOUT=8 / LOOKUP_TIMEOUT=15`。

---

## Mac mini M3-00A final-combo live verification（2026-07-16，commit `8c81d21` = `ecc5158` + `8c81d21`）— 单聊优秀，但 burst active hang 仍在 + model-node 60s 超时未生效 ⚠️

部署 HEAD=`8c81d21`（直接后继含 `ecc5158` 全部修复；`merge-base --is-ancestor ecc5158 HEAD` exit 0 ✅）。
`ecc5158`：关 SummarizationMiddleware + model-node 60s 硬超时（`agent.nodes["model"].timeout = TimeoutPolicy.coerce(60)`）+ NodeTimeoutError 归一化。
`8c81d21`：恢复默认 general-purpose subagent/task。

- 环境：Mac mini，HEAD `8c81d21`（GitHub 代理拉取 `2dc5ca7..8c81d21`，GitLab 补推 ✅）。gateway pid 66879，`:12000`，无 proxy。
- `.env`：`MODEL_REQUEST_TIMEOUT=60`、`MAX_RETRIES=0`、`EXECUTION_MODE=sidecar`/`SIDECAR_TIMEOUT=8`/`LOOKUP_TIMEOUT=15`/`MAXSIZE=3`/`TTL=240`/`qwen-flash`。smoke override 本机不入库。

### 静态 — 全 PASS（新基线）

example **77+1skip**（+3，含 `test_agent_harness` 3 用例验证 task 工具/`general_purpose_subagent is None`/SummarizationMiddleware 排除/model node timeout=60）✅；
contrib：wecom 58、langchain 21、enterprise 84、work 90、files 47 ✅；template 25 ✅；`make docs-test` 0.28s ✅；
`mcp_policy.py`+`tools.py` vs `68d7b25` **zero-diff** ✅。

### Live A（单条会话）— PASS ✅（本轮亮点）

顺序发 `你好`/`现在几点`/`我是谁`/`我刚才问了什么`（+额外`你是谁`），**全部正确回复**：
- `我刚才问了什么` → **正确回忆前 3 轮**（现在几点/我是谁/你好）→ **短期记忆完好，关 SummarizationMiddleware 未丢上下文** ✅。
- 普通 turn **0 `wecom_response_url_delivery`** ✅（单 stream 确认）。
- **每 turn 仅 1 次 `langchain_model_call`**（started+succeeded=2 事件）→ **无隐式摘要模型调用** ✅。

**关键指标改善（关 SummarizationMiddleware 的效果）：**

| 指标 | dec92a2 | 8c81d21 |
|---|---|---|
| `model_invoke started → langchain_model_call started`（pre-model 延迟）| **44s** | **6-16s** ✅ 大幅缩短 |
| 每 turn model_call 数 | 1 | 1（无隐式摘要）✅ |

pre-model 44s 延迟**显著缩短到 6-16s**（仍有 runnable setup 残余，但不再接近 hang 区）。

### Live B（WorkItem 边界）+ Live C（burst + 60s 超时）— BLOCKED ❌

`请告诉我当前报告任务状态`（Live B，cc=12，ba6bd1b8）作 active + 1 秒连发 4 条 burst。用户"等了几十秒没任何返回"。

- 队列形成：active `ba6bd1b8` + pending 1/2/3（`e0118560`/`cb2445e7`/`16c3a333`）✅（事件层）。
- **active 跑过 identity 降级**：`08:01:02 identity_lookup status=error error_type=TimeoutError`（sidecar hang，15s 超时触发）→ **active 继续跑 5 个 `langchain_run_stage`**（identity 降级生效，turn 没死）✅ → 卡在 model_invoke。
- **`08:03:22 wecom_turn_timeout`**（ba6bd1b8，**195s 兜底**）→ `08:03:25 e0118560 turn_started`（**队列推进，锁释放** ✅）。
- **但 `e0118560` 也卡了**（started 后无 identity/stage，CPU 0%）→ 级联重复，每条 ~195s。

**🔴 ecc5158 model-node 60s 超时未生效（核心问题）：**
- gateway.log **无 `NodeTimeoutError` / `TimeoutPolicy` 任何消息** —— 模型节点超时**根本没触发**。
- active 从 model_invoke 卡到 **195s `turn_timeout`**，不是 60s。
- 静态 `test_agent_harness` 验证 `model_node.timeout=60` 已设置，但 **live 运行时未生效**。
- **疑似根因**：hang 在 **agentseek_langchain 插件的 `run_model` 包装层**（发出 `langchain_run_stage` 的地方），而 `TimeoutPolicy` 绑在 **LangGraph `"model"` 节点**上 —— 若插件 run_model 不在该 LangGraph 节点内执行（是独立节点/包装），node 超时覆盖不到。这与上轮"request 超时在错层"是同类问题（超时层与 hang 层不匹配）。
- **需 Codex**：核实 `langchain_run_stage` 的发出点是否在 LangGraph `model` 节点执行路径内；若否，把 60s 超时绑到**实际 hang 的层**（插件 run_model / model_invoke stage），或用 `langchain_run_stage` 的 `model_invoke started` 作为超时起点自己包 `asyncio.timeout`。

### ecc5158 分项结论

| 项 | 结果 |
|---|---|
| SummarizationMiddleware 关闭 | ✅ 生效（无隐式摘要，pre-model 44s→6-16s）|
| model-node 60s 边界 | ❌ **未生效**（active 跑到 195s turn_timeout，无 NodeTimeoutError）|
| NodeTimeoutError 按 timeout 处理 | ⚠️ 未触发（超时没发生）|
| pre-model 44s 延迟 | ✅ 缩短到 6-16s（单聊）|
| burst 后队列推进 | ✅ 195s turn_timeout 后锁释放、下条启动（但每条 195s，太慢）|

### 8c81d21 分项结论

| 项 | 结果 |
|---|---|
| task/默认 subagent 恢复 | ✅（静态 test_agent_harness 验证）|
| 普通会话/WorkItem 边界保持 | ✅（Live A 无误建 WorkItem；无 guard 绕过）|
| 新延迟/绕过 | 无新问题 |

### M3-00A 五项用户面（放宽合同：ack/位置 best-effort 非门）

| # | 项 | 结果 |
|---|---|---|
| 1 | 拒绝可见+只 1 次+不进 Agent | ⚠️ 未干净验证（active hang 期间用户未确认）|
| 2 | pending 位置 | 放宽（非门）|
| 3 | 状态命令可见+绕过 Agent | ✅ PASS（Live A `查看消息队列`）|
| 4 | ack | 放宽（非门）；最终回复可见（Live A）✅ |
| 5 | TTL 可见+不进 Agent | ⚠️ 未干净验证（本轮 active hang 占主导）|

### 健康 + 脱敏

- 0 SIGBUS / 0 Traceback / 0 IntegrityError / 0 HTTP non-200。单条正常；burst 每条 hang 时 CPU 0%。
- 单条 turn `wecom_response_url_delivery`=0 ✅。identity_lookup error 不含 OA 明文 ✅。
- events 全文 grep `zhuchunlin|朱春霖|wecom:zhuchunlin|password|token=|secret|response_url|response_code` **0 命中** ✅；MCP audit 与 enterprise events 分离 ✅。

### 未测

Live D（状态命令在 active 期间）、Live C 的 TTL=10、sidecar SIGSTOP、基础回归（pgvector/知识库/MCP/M2）—— burst active hang 反复占主导，优先定位 model-node 超时未生效根因。单条 Live A 间接覆盖身份/LLM/记忆/WorkItem 路径（无回归）。

### 最终结论

- **`ecc5158` 的 SummarizationMiddleware 关闭 = 成功**：单聊 pre-model 延迟 44s→6-16s，无隐式摘要，短期记忆完好。单聊体验明显改善。
- **`8c81d21` subagent 恢复 = 成功**：task 工具/默认 subagent 恢复，普通会话与 WorkItem 边界保持，无新延迟/guard 绕过。
- **但 `ecc5158` 的 model-node 60s 超时 = 未生效**：burst active 仍卡在 model_invoke（identity 降级后），跑到 195s turn_timeout，**无 NodeTimeoutError**。疑超时绑在 LangGraph model 节点，而 hang 在插件 run_model 包装层（不同层）。
- **M3-00A 用户面 = 单聊 PASS（#3/#4-最终回复），burst 仍 BLOCKED**。不能正式关闭（burst active hang 195s/条，UX 差）。
- 事件层：分段观测 + identity 降级 + 单 stream 全 PASS；无新增事件层回归。

### 给 Codex 的下一步（最高优先）

**修 model-node 超时"绑错层"**：`langchain_run_stage` 的 `model_invoke started` 事件发出后，若 N 秒内无 `langchain_model_call started`，说明卡在 runnable/provider 入口。当前 `TimeoutPolicy` 绑在 LangGraph `"model"` 节点，但 `langchain_run_stage` 疑由**插件 run_model 包装**发出（不在该节点执行路径内）→ 超时覆盖不到。
方案：(a) 核实并确保 `run_model` 在 LangGraph model 节点内执行（让 TimeoutPolicy 覆盖）；(b) 或在插件 `run_model`/`model_invoke` stage 自己包 `asyncio.timeout(60)`，超时抛 NodeTimeoutError 归一化释放 worker；(c) 用 `model_invoke started` → `langchain_model_call started` 的 gap 作 hang 探针主动 cancel。
修后 burst active 应 60s 有界终止 + 释放锁，队列 ~60s/条推进（而非 195s）。

当前环境已恢复：gateway `qwen-flash`（pid 66879），`MAXSIZE=3 / TTL=240`，`MODEL_REQUEST_TIMEOUT=60 / MAX_RETRIES=0`，`IDENTITY_CACHE_ENABLED=true`，`SIDECAR_TIMEOUT=8 / LOOKUP_TIMEOUT=15`。

---

## M3-00A model-start watchdog implementation（2026-07-16）

Mac mini `8c81d21` 复验确认自动摘要关闭有效，但也澄清了三层截止线的覆盖关系：

- `AGENTSEEK_MODEL_REQUEST_TIMEOUT_SECONDS=60` 约束单次 provider 请求；
- LangGraph `model` node 的 `TimeoutPolicy(60)` 只约束该图节点；
- `agentseek-langchain` 插件在图外发出 `model_invoke started`，其整轮
  `AGENTSEEK_LANGCHAIN_RUN_TIMEOUT_SECONDS` 仍为 180 秒，而 WeCom turn 上限为
  195 秒。因此 burst 在首个 provider callback 前卡住时，60 秒配置无法触达，
  180 秒整轮截止线又晚于从 WeCom turn 起算的 195 秒外层截止线。

修复在插件真实执行边界增加独立的首模型回调 watchdog：

1. `AGENTSEEK_LANGCHAIN_MODEL_START_TIMEOUT_SECONDS` 默认 60 秒；未显式配置时兼容
   `AGENTSEEK_MODEL_REQUEST_TIMEOUT_SECONDS`，无需旧部署立即新增变量；
2. `run_model` 和 `run_model_stream` 把 runnable operation 与
   `_ModelCallObservability` 的首个 `on_chat_model_start`/`on_llm_start` 事件竞速；
3. 60 秒内 operation 已正常结束或首个模型调用已开始则继续；否则取消 operation，
   返回统一模型超时提示，并记录 `timeout_phase=model_start`；
4. watchdog 只约束 pre-model 启动阶段。首个模型回调出现后，180 秒整轮上限继续允许
   多工具 WorkItem 执行，provider request 和 graph-node deadline 仍作为内层防线；
5. 流式 pump 在超时或外层取消时一并取消，避免 session worker 残留后台 operation。

本地新增确定性测试覆盖：pre-model 永久挂起在 model-start deadline 结束；流式路径返回
`ok=false` 终态；首个 callback 已出现后不会被较短的 start deadline 误杀；专用环境变量
优先于 provider timeout，缺省时正确回退。`make check-langchain` 为 **26 passed**，ty/Ruff
全绿；Example **77 passed + 1 skipped**，模板渲染 **25 passed**。

Mac mini live oracle：burst 中出现 `model_invoke started` 且没有
`langchain_model_call started` 时，应在约 60 秒产生
`model_invoke timeout timeout_phase=model_start`，用户收到单次终态超时提示，session 锁
释放并启动下一条，不再等到 195 秒 `wecom_turn_timeout`。

---

## Mac mini M3-00A two-stage WeCom delivery live verification（2026-07-16，commit `0e0b754` = `db88f18` + `0e0b754`）— 单聊两阶段投递 PASS ✅，burst 仍 BLOCKED ❌（根因变为 pre-model 事件循环冻结）⚠️

本轮复验两个修复：
- `db88f18 fix(langchain): bound pre-model startup` —— 在插件 `run_model`/`run_model_stream`
  真实执行边界增加 `_ModelStartTimeoutError` watchdog（`AGENTSEEK_LANGCHAIN_MODEL_START_TIMEOUT_SECONDS=60`），
  把 runnable operation 与首个 `on_chat_model_start` 事件竞速，60s 内无模型回调则取消并返回
  `本次模型处理超时，请稍后重试。`。静态 `make check-langchain` 新增 5 个确定性用例。
- `0e0b754 fix(wecom): restore visible async acknowledgements` —— 明确两阶段投递：
  快任务 callback 直接 `finish=true`（清空 response_url）；慢任务 callback 先给可见 ACK
  `finish=true` + `deferred_response_url`，最终结果经 response_url 恰好投递一次；排队消息
  callback 返回位置 `finish=true`，结果走各自 response_url；拒绝/状态命令 callback-only，
  不消费 response_url；msgid 重试重放首包快照。`initial_wait_seconds` 默认 1.5→0.5。

`git merge-base --is-ancestor` 确认 `0e0b754`、`db88f18` 均为 HEAD 祖先（exit 0）。

### 静态 — 全 PASS

| 套件 | 结果 |
|---|---|
| `make test-wecom` | **60 passed** |
| `make test-files` | **47 passed** |
| `make typecheck-wecom`（ty） | **PASS** |
| ruff（channel/config/test_channel） | **PASS** |
| `make check-langchain` | **26 passed**（db88f18 新增 test_config + test_plugin 用例） |
| 模板渲染（render+registry） | **28 passed** |
| Example（committed pack/profile） | **77 passed + 1 skipped** |
| MCP zero-diff（`git diff 68d7b25 -- mcp_policy.py tools.py`） | **0 行** ✅ |

本地准生产 .env（未提交）：新增 `AGENTSEEK_WECOM_INITIAL_WAIT_SECONDS=0.5`、
`AGENTSEEK_LANGCHAIN_MODEL_START_TIMEOUT_SECONDS=60`；保留 qwen-flash / MAXSIZE=3 /
TTL=240 / TURN_TIMEOUT=195 / RUN_TIMEOUT=180 / SIDECAR=8 / IDENTITY_LOOKUP=15 /
IDENTITY_CACHE=true。gateway pid 13579 / :12000 / **无 proxy env** / 健康检查正常 /
startup 日志干净。配置实测加载：initial_wait=0.5、model_start_timeout=60、run_timeout=180、
turn_timeout=195、queue_wait=240、maxsize=3。

### Live A（单条慢消息两阶段投递）— PASS ✅（本轮亮点）

消息「你好，请简单介绍一下你自己」。T0=17:14:18 / T1(ACK 可见)=17:14:32 / T2(最终)=17:15:25。

| 验收点 | 结果 |
|---|---|
| ACK 先于最终回复可见（T1≪T2，14s vs 67s） | ✅ |
| ACK「已收到，正在处理…」finish=true 只 1 次 | ✅（前几轮"没收到 ack"在本轮修复） |
| 最终回复只 1 次 | ✅ |
| 最终回复经 `wecom_response_url_delivery succeeded` | ✅（恰好 1 次） |
| response_url exactly-once（1 delivery / 1 stream_finished） | ✅ |
| WeCom msgid 重试去重（2 次同 msg_signature 重放首包，未多跑 turn / 未多投递） | ✅ |
| 不依赖 finish=false 中间态 | ✅（ACK 本身 finish=true） |
| model_invoke→model_call 间隔 | **3s**（健康，未回退到 44s） |

两阶段投递在真实企微上工作：**callback 先回可见 ACK（finish=true）→ 模型后台跑 → 最终结果
走 response_url 恰好一次**。这是 `0e0b754` 的核心价值，单聊体验显著改善。

### Live B（快路径 + 状态命令）— PASS ✅

消息「查看消息队列」→ 17:18:02 发送，17:18:18 回复「当前没有正在处理或等待处理的消息」。

- 走 `wecom_turn_queue_status`（队列层直接处理）；**0 个 langchain/model 事件**（不进 Agent）；
- **0 个 response_url_delivery**（未消费）；无 `turn_started`；2 次 WeCom 重试已去重，无重复回复。

### Live C / Live F（单会话 burst + 排队 + 拒绝 + 60s 超时）— BLOCKED ❌

17:20:30 发「请帮我写一段…300字左右」（active），3s 内连发「消息1~4」。

**队列逻辑本身正确** ✅：active(e16d5b83) + pending 位 1(fc7e8cad)/2(08e39e8d)/3(c681d80e)，
第 5 条(b0a81d6c)`wecom_turn_queue_rejected`（pending_count=3 满）。

**但用户面全失败** ❌：active **没有 ACK**（用户确认"也没有返回 ACK"）；排队位置/拒绝提示
用户也未看到。企微直到 **17:26:00**（5.5 分钟后）才一次性回 4 条：
1. `本次处理超时，请稍后重试。`（**turn_timeout** 消息，channel.py:934）
2. `等待处理时间过长，本条消息已取消…` ×3（**queue_wait_timeout** 消息，channel.py:807）

**active `e16d5b83` 后端时间线（UTC）：**

| 时间 | 事件 |
|---|---|
| 09:20:38 | turn_started |
| 09:21:13 | short_term_memory_load succeeded |
| 09:21:16 | identity_lookup found（cache hit） |
| 09:21:19 | contextseek_pgvector_retrieve succeeded |
| ⏳ **09:21:19 → 09:25:42（263s 事件循环冻结）** | 期间无任何阶段推进 |
| 09:25:42 | run_model_stream 终于启动 |
| 09:25:45/48/50 | spec_resolve / prompt_enrichment / context_build（均 0ms） |
| 09:25:53 | model_invoke started（60s model_start 计时开始） |
| 09:25:56 | **wecom_turn_timeout 抢先触发**（model_invoke 后仅 3s，60s 窗口没机会） |

**硬证据：**
- 17:20–17:25 整段 **0 个 callback 返回 200 OK** → ACK 根本没发出去（`_wait_for_first_update`
  的 0.5s deadline 被同步阻塞拖到 263s 之后才生效）。
- 6 分钟内 **301 次 WeCom 重传**（5 个 msgid 狂重试，重传风暴）。
- active 终态消息是 **`本次处理超时`（turn_timeout）**，**不是** `本次模型处理超时`
  （model_start_timeout）——即 **db88f18 的 60s watchdog 没有触发生效**。
- turn_timeout(195s) 本应在 09:23:53 触发，实际 09:25:56（**晚 ~2 分钟**）；
  queue_wait_timeout(240s) 对 3 条 pending 分别晚 22/36/30s 触发 → **所有 asyncio 定时器
  被同一个事件循环冻结延迟**。
- 队列最终在 09:26 全部到达终态、gateway 恢复空闲健康（无需 kill -9）。

**根因（与前几轮 model-start 卡住不同）：** 不是 provider 首回调前卡住，而是
**contextseek 上下文注入（09:21:19）→ langchain `run_model_stream` 启动（09:25:42）之间存在
~263s 的同步阻塞，冻结整个 asyncio 事件循环**。后果级联：
1. ACK callback 无法被调度返回（`_wait_for_first_update` 的 deadline 失效）→ 用户看不到 ACK/排队位；
2. 全部 asyncio 定时器延迟（turn_timeout / queue_wait_timeout / model_start_timeout 都被拖）；
3. db88f18 的 60s watchdog **无效**——阻塞发生在 model_invoke **之前**，等 model_invoke 真正
   启动（09:25:53）时，被同样延迟的 turn_timeout 已在 3s 后抢跑。

**额外噪声/放大源：** Langfuse OTel exporter 持续刷 `localhost:3000 Connection refused`
（本地 Langfuse 服务未启动），突发多 turn 下 span 导出量激增 + 失败重试，可能放大事件循环压力
（非根因，但建议同步处理：本地未起 Langfuse 时降级或禁用 OTel flush）。

### Live D（msgid 去重）— PASS ✅（由 Live A/C 覆盖）

Live A：active msgid 被 WeCom 重试 2 次（同 msg_signature/timestamp/nonce），均重放首包，
仅 1 次 turn、1 次 response_url_delivery。Live C：5 个 msgid 共 301 次重传全部去重，未重复
进入 Agent / 未重复投递。去重逻辑正确。

### Live E（TTL 取消）— 由 burst 路径覆盖（TTL=240，非 TTL=10）

3 条 pending 在 queue_wait_timeout(240s) 后均收到「等待处理时间过长，本条消息已取消，请稍后
重新发送。」（channel.py:807），消息文案与 Live E 预期一致。**但触发被事件循环冻结延迟
22–36s**，且本轮未单独跑 TTL=10 显式用例（burst 不稳定，优先收尾）。TTL 取消路径本身工作，
定时准确性受同一根因影响。

### 两阶段投递机制分析（代码正确，burst 下被事件循环阻塞）

`_stream_response` 对慢任务（有 response_url、未 finish）：`_wait_for_first_update`（≤0.5s
deadline 轮询）→ 命中 `elif current.response_url` 分支 → 置 `deferred_response_url=True`、
返回 ACK `finish=true`。代码路径**正确**，单消息（Live A/B）已证明 ACK 可见 + response_url
恰好一次。`self._lock` 5 处持有者均为 dict 级快操作（无 I/O），**非阻塞根因**。burst 下 ACK
不可见的唯一原因是**事件循环被 agent pipeline 的同步段冻结**，callback 协程无法被调度——
而非投递逻辑本身有 bug。

### 健康 + 脱敏

- 0 SIGBUS / 0 Traceback（功能层）/ 0 IntegrityError；
- gateway 全程存活，burst 后自恢复，无需 kill -9；
- enterprise-events / Langfuse 脱敏完好，日志仅记 response_url 是否存在，未打印完整 URL 或
  response_code；
- Langfuse OTel `localhost:3000` 连接拒绝为本地服务未起（噪声，非功能故障）；
- MCP audit 与 enterprise events 分离；protected MCP 文件零 diff。

### 未测（本轮收尾，按用户要求聚焦 burst）

- Live G（DM sidecar SIGSTOP/SIGKILL 恢复）—— 单消息路径，未跑；
- 基础回归（pgvector 语义记忆 / 显式长期记忆 / WorkItem 边界 / 部门知识库 / MCP 列表 /
  MinerU pending）—— 单消息路径，未跑；
- Live E TTL=10 显式用例 —— 未单独跑（由 burst TTL=240 路径覆盖文案）。

### 给 Codex 的下一步（最高优先）

1. **定位并消除 contextseek 注入 → langchain `run_model_stream` 之间的 ~263s 同步阻塞**
   （burst 下复现稳定）。这是当前 burst 可用性的唯一硬阻塞，也是 db88f18 watchdog 失效的
   原因。建议：用 `model_invoke started` 与上一阶段（contextseek_pgvector_retrieve succeeded）
   的时间差作为 pre-model hang 探针；排查 DeepAgents harness 在多 turn 并发下的 graph/spec
   构建或工具绑定（23 工具）是否有同步阻塞调用跑在事件循环线程里。
2. **db88f18 的 model_start watchdog 机制正确但本轮未触达**——阻塞在 model_invoke 之前。
   待根因 1 修复后复测：model_invoke 应在 contextseek 后秒级启动，watchdog 才有 60s 窗口生效。
3. **把 ACK/排队提示投递从 agent pipeline 解耦**：即使 agent 事件循环被阻塞，callback 也应能
   独立返回 finish=true ACK（考虑 callback 路径不依赖 agent worker 的 stream 首更新，或把
   `_wait_for_first_update` 改为不阻塞投递决策）。
4. **降级 Langfuse OTel**：本地未起 Langfuse（localhost:3000）时禁用或减少 flush，避免突发下
   span 导出失败重试放大事件循环压力。

### 最终结论

- **两阶段投递改造（`0e0b754`）单聊 PASS ✅**：ACK 可见 + 最终回复 response_url 恰好一次 +
  快路径/状态命令不进 Agent，这是真实可感知的体验改善。
- **model-start watchdog（`db88f18`）实现正确、静态全绿，但 burst 下未触达**（阻塞在 model_invoke
  之前，被延迟的 turn_timeout 抢跑）。
- **M3-00A 不能正式关闭**：单聊合同（#1 ACK 先于最终 / #4 最终可见 / #7 response_url
  exactly-once / #8 不依赖 finish=false）满足；但 burst 下 ACK/排队位置用户不可见（#1/#2 burst
  子项失败）、active 终态延迟 5.5 分钟、所有定时器被冻结延迟，违反 #9（active hang 不妨碍首包
  ACK）与 #10（gateway 全程健康——虽自恢复但用户体验不可接受）。
- 根因从"model-start 卡住"明确为 **"pre-model 事件循环同步冻结"**，需 Codex 按"下一步 1"修复后
  复测 burst，方可评估 M3-00A 关闭。

---

## M3-00A callback control-plane isolation implementation（2026-07-16）

Mac mini `44663cb` 的时间线表明，两阶段投递在单聊正确，burst 失败是因为
gateway 事件循环在 ContextSeek 返回后被阻塞 263 秒。同时日志显示 Langfuse OTel
exporter 持续连接未启动的 `localhost:3000`，而当前 `emit_enterprise_event()` 会在调用
线程里直接执行 `client.flush()`。WeCom 重试又会产生更多事件，形成同步 flush 和
callback 重试相互放大的正反馈。

本轮在不修改 DeepAgent 或 MCP 行为的前提下分离控制面与观测输出：

1. 有 `response_url` 的 WeCom 消息在队列准入后立即同步固化 `finish=true` 首包；
   Agent worker 在当前 callback 协程返回前不能抢先执行。普通消息不再为了“快结果”
   等待 0.5 秒；始终先 ACK，最终结果经该消息的 `response_url` 投递一次。
2. 队列状态和拒绝仍 callback-only；排队成功的消息先回队列位置，后续终态使用
   各自 `response_url`。没有 `response_url` 的兼容回调才使用
   `AGENTSEEK_WECOM_INITIAL_WAIT_SECONDS` 和 stream polling。
3. Langfuse 网络调用、span 创建和 `flush()` 移到单一 daemon worker；业务线程只做
   `put_nowait`。队列由 `AGENTSEEK_LANGFUSE_QUEUE_MAXSIZE`（默认 1024）限制，满时丢观测事件
   而不阻塞 WeCom。本地 JSONL 仍同步写入，不受 Langfuse 可用性影响。
4. Langfuse probe 增加有界等待，使命令行验证仍能在退出前确认事件已发送；
   gateway 常驻流量不会等待它。

确定性回归覆盖：Langfuse `flush()` 挂起时业务 emit 在 100ms 内返回；有界队列满时
立即 drop；即时 handler 也只能在 ACK 固化后开始；并发重复 msgid 仍只启动一次
Agent/投递一次终态。Mac mini live oracle 是 burst 中 callback HTTP 不再被 Langfuse 故障拖住，
ACK/位置/拒绝先于 Agent pipeline 可见，且 Langfuse 不可达时不再产生重试风暴。

---

## Mac mini M3-00A callback-isolation live verification（2026-07-16，commit `4b4c09a`）— 全 PASS ✅，M3-00A 可正式关闭

本轮复验 `4b4c09a fix(runtime): isolate WeCom callbacks from telemetry` 的两个修复：
- **A. WeCom callback 控制面隔离**：带 `response_url` 的 callback 改调**同步**
  `_commit_stream_response()`（无 `await`），立刻固化 ACK / 排队位置 / 拒绝并返回 `finish=true`，
  不等身份/记忆/Agent/模型/Langfuse；Agent worker 后台运行，最终结果仍走 response_url exactly-once。
- **B. Langfuse 发送移出事件循环**：enterprise event 进有界内存队列（默认 1024），独立 daemon
  线程负责 SDK+网络+flush；队列满则 drop telemetry（绝不阻塞业务）；本地 jsonl 不受影响。
  新增 `AGENTSEEK_LANGFUSE_QUEUE_MAXSIZE`，新增 `probe_langfuse_event.py --timeout` 有界等待。

`git merge-base --is-ancestor` 确认 `db88f18`、`0e0b754` 均为 HEAD 祖先（exit 0）。

### 静态 — 全 PASS

| 套件 | 结果 |
|---|---|
| `make test-wecom` | **60 passed** |
| `make test-enterprise` | **86 passed**（+2 observability Langfuse 队列用例）|
| `make test-files` | **47 passed** |
| `make test-work` | **90 passed** |
| Example（committed pack/profile） | **77 passed + 1 skipped** |
| `make typecheck-wecom` / `typecheck-enterprise` | **PASS** |
| `make docs-test` | **PASS**（0.41s）|
| ruff（4b4c09a 改动文件 observability.py/channel.py） | **PASS**（全仓 4 个 pre-existing lint 与本 commit 无关）|
| MCP zero-diff（`git diff 68d7b25`） | **0 行** ✅ |

### 受控 Langfuse 不可达模拟 + probe

`.env` 临时 smoke：`LANGFUSE_HOST=http://127.0.0.1:39999`（不可达）、`QUEUE_MAXSIZE=8`，
保留 qwen-flash / MAXSIZE=3 / TTL=240 / MODEL_START_TIMEOUT=60 / IDENTITY_CACHE=true。
gateway pid 12074 / :12000 / 无 proxy / 启动快速干净。`probe_langfuse_event --timeout 10`：
**6s 有界返回**（status=sent，SDK 本地接收；实际投递在后台线程失败重试，不阻塞），gateway 6ms 响应、
未冻结。

### Live A（单条两阶段投递，Langfuse 不可达）— PASS ✅

「你好」：T0=20:22:40 / T1(ACK)=T0+**1-2s**（上轮 14s）/ T2(最终)=T0+10s。
- ACK「已收到，正在处理…」finish=true 只 1 次；最终回复 `response_url_delivery` 恰好 1 次；
- **0 次 WeCom 重传**（上轮 2-301 次）；model_invoke→model_call 间隔 0s（无 pre-model 冻结）；
- Langfuse 队列满→**drop 11 次**，业务 10s 正常跑完；本地 jsonl 新增 20 条不受影响。

### Live B（5 条 distinct burst 核心复测）— PASS ✅（本轮决定性胜利）

长任务(摘要) active + 紧接 4 条短消息（2s 间隔）。**用户面全部可见**（上轮全不可见）：

| 消息 | 企微显示（finish=true）| 后端 |
|---|---|---|
| 长任务 | 「已收到，正在处理…」→ 报告提纲 v3 | active，1 次 delivery ✅ |
| 任务1 | 「排在等待队列第 **1** 位」| queued pos=1，1 次 delivery ✅ |
| 任务2 | 「排在等待队列第 **2** 位」| queued pos=2，1 次 delivery ✅ |
| 任务3 | 「排在等待队列第 **3** 位」| queued pos=3，1 次 delivery ✅ |
| 任务4 | 「另有 3 条等待处理。**本条未进入队列**…」| **rejected，0 delivery**（不进 Agent）✅ |

对比上轮（0e0b754）同场景：

| 指标 | 本轮 4b4c09a | 上轮 0e0b754 |
|---|---|---|
| ACK/位置/拒绝用户面可见 | **全部** ✅ | **全不可见** ❌ |
| WeCom 重传 | **0** | **301** |
| burst 总耗时 | **25s** | **5.5 分钟** |
| 事件循环冻结 | **无** | 263s |
| response_url exactly-once | 5 条各 1 次 ✅ | 0 次（全超时）|
| Langfuse 不可达 | 618 次 drop，业务正常 | （放大冻结）|

### Live C（队列状态命令，active 运行中）— PASS ✅

3 次「**查看**消息队列」：每条仅 `stream_started→message_received→turn_queue_status`，
**0 turn_started / 0 response_url_delivery**（callback-only，不进 Agent，不消费 response_url）；
13:05:35 active 运行中正确返回 `active=True`。注：「**看**消息队列」（少"查"）不命中关键词，
被当普通消息排队——关键词匹配，非 bug。

### Live D（TTL 取消）— PASS ✅（准时触发，证明冻结已消除）

TTL=10 下系统排空太快（turn 3-9s、消息间隔>3s）自然触发不了；改 **TTL=3** 强制触发：
- pending 527f2567：queued 13:21:56 → `wecom_turn_queue_wait_timeout` **13:21:59**（恰好 3s）；
- pending d6faf845：queued 13:22:25 → timeout **13:22:28**（恰好 3s）。
- 用户面「**等待处理时间过长，本条消息已取消，请稍后重新发送。**」可见、各 1 次；被取消消息
  不进 Agent；取消提示经 response_url exactly-once。
- **上轮 TTL 因事件循环冻结晚 22-36s 触发；本轮准时 3s**——证明冻结已消除、定时器准确。

### Live E（恢复 Langfuse）— PASS ✅

`.env` 恢复 `LANGFUSE_HOST=http://localhost:3000`（探活 200，服务正常）、QUEUE_MAXSIZE 默认 1024、
TTL=240。重启 gateway（pid 62100），`probe_langfuse_event` → **status=sent**（真实 Langfuse 接收）。
此轮 0 次 queue-full drop、0 次 OTel Connection refused。

### 基础回归 + 健康 — PASS ✅

「我是谁」→ 正确返回身份（朱春霖 / zhuchunlin / 团队长兼数据架构师 / 信息技术部·数智产品研发团队），
ACK + 最终回复各 1 次。其余由本轮事件覆盖：identity_lookup found（每 turn）、short_term_memory
load/save succeeded、contextseek_pgvector_retrieve succeeded、WorkItem 报告提纲 v3/v5 正常创建、
Tavily MCP 启动、queue_status 工具正常。

- **0 SIGBUS / 0 Traceback / 0 IntegrityError / 0 HTTP non-200**；
- `identity_lookup` 事件 payload 仅含 `employee_key`/`user_key`/`role_label`/`has_org_path` 等
  哈希/标记字段，**无明文 OA/姓名/密码/token** ✅；
- MCP audit 与 enterprise events 分离；protected MCP 文件零 diff；
- gateway 全程存活，burst 后无残留，**无需 kill -9**。

### 🔴 新发现（非 4b4c09a 回归，非 M3-00A，建议 Codex 单独排查）

`ValueError: output_formats contains an unsupported format`——报告 playbook 在创建 WorkItem 时
传了不支持的 output_format（本轮 47 次，请求"摘要/报告"即触发），active turn 在 model_invoke
阶段 ValueError 快速失败并给用户返回 `An error occurred at stage 'turn': output_formats contains
an unsupported format`。MCP `tools.py` 零 diff 确认非本轮引入；与 callback/队列无关，不影响 M3-00A。
（注：同型号请求有时返回正常报告提纲 DOCX，有时报 unsupported format——疑似模型提议的 format
不一致触发校验，建议查 WorkItem output_formats 校验逻辑。）

### ACK 规则（用户面，finish=true，恒定快速返回）

| 消息 | 队列状态 | 立刻显示 | 进 Agent | response_url |
|---|---|---|---|---|
| 普通 | active | 「已收到，正在处理…」| ✅ | ✅ 1 次 |
| 普通 | 排队 | 「排在第 M 位」| ✅（轮到）| ✅ 1 次 |
| 普通 | 队列满 | 「本条未进入队列…」| ❌ | ❌ |
| `查看消息队列` | 任意 | active/pending 计数 | ❌ | ❌ |
| 快任务 | — | 直接最终结果 | ✅ | ❌（清空）|
| pending 超 TTL | — | 「等待处理时间过长，已取消」| ❌ | ✅（取消提示）|

企微 AI Bot 回调均带 response_url，故**几乎所有消息都立刻有 ACK**；返回速度恒定（同步、不依赖
agent），仅 ACK 内容随队列状态变化。

### 最终结论 — M3-00A **可正式关闭** ✅

十项判定标准逐项：

1. 单聊 ACK 可见、最终 exactly-once — ✅（Live A/E）
2. burst active ACK 可见 — ✅（Live B）
3. pending 位 1/2/3 可见 — ✅（Live B）
4. 第 5 条拒绝可见、只一次、不进 Agent — ✅（Live B，0 delivery）
5. callback 快速返回、无重传风暴 — ✅（0 重传 vs 上轮 301）
6. Langfuse 不可达业务不冻结 — ✅（Live A/B 全程，618 次 drop 业务正常）
7. 状态命令立即返回、不进 Agent — ✅（Live C）
8. TTL 取消可见且只一次 — ✅（Live D，准时 3s）
9. gateway 不冻结、无需 kill -9 — ✅
10. 静态/MCP zero-diff/健康全通过 — ✅

`4b4c09a` 的回调控制面隔离 + Langfuse 异步队列**彻底解决**了上两轮的 burst 事件循环冻结：
callback 不再耦合 agent pipeline、Langfuse 不再阻塞业务循环。M3-00A 用户面投递与队列背压
合同**全部满足**。唯一遗留是无关的 WorkItem `output_formats` playbook bug，建议 Codex 单独修复。

---

## M3-00A closeout + ReportBrief output-format hardening（2026-07-16）

Mac mini 对 `4b4c09a` 的十项验收全部通过，M3-00A 正式关闭。ROADMAP 已同步为已完成状态，
并以验证通过的两阶段投递为准：callback 在 Agent worker 前固化 `finish=true` ACK、排队位置或拒绝，
进入 Agent 的消息再由 `response_url` exactly-once 投递最终结果；Langfuse 故障与队列满载不阻塞业务。

同轮发现的 `output_formats contains an unsupported format` 作为独立合同加固处理：

- `save_report_brief.output_formats` 的工具 schema 从任意字符串收窄为
  `markdown | docx | pdf` 枚举，降低模型把“摘要、报告、提纲”等内容类型误填为文件格式的概率；
- 工具说明明确内容类型与文件格式的边界；未指定文件格式时继续使用 `docx` 默认值；
- `ReportBrief` 构造移入工具失败隔离范围，即使校验被直接调用绕过，错误也作为可纠正的工具结果返回，
  不再让整个 turn 以裸 `ValueError` 失败；
- 白名单未扩展，`xlsx` 或任意未知格式仍然 fail-closed；Example 与 cookiecutter 模板保持一致。

发布前 live oracle：分别发送“生成一份摘要”和“创建行业报告”，模型调用
`save_report_brief` 时应省略 `output_formats` 或只使用枚举值，ReportBrief 默认保存为 `docx`；
全程不得再出现裸 `ValueError` 或 `An error occurred at stage 'turn'`。

---

## Independent MCP binding hardening（2026-07-16）

Mac mini 在 M3-00A 通过后单独发现：模型把 remote tool `tavily_search` 误填为通用适配器的
`server_name`，而配置中的真实 server 是 `tavily-search`，调用在 policy/audit 前即失败。
同一诊断还确认 Profile `tool_grants` 只进入权限摘要，未约束 Agent 的原始 MCP 工具可见集。

修复不修改受保护的 `tools.py` 或 `mcp_policy.py`，而是在 Agent 装配边界 fail-closed：

- 启用版本化 Work/Profile 时，不再向模型暴露原始 `list_mcp_tools` 与 `call_mcp_tool` dispatch；
- 部门知识、Gildata 和 Tavily 只能通过已有的高层 Work 工具进入，server/tool 名由代码固定，
  Gildata/Tavily 仍受 ReportBrief gap decision 与 MCP policy/audit 约束；
- `analyze_file` 只有在 Profile 声明同名 grant 时才直接绑定；
- 尚未启用 Work/Profile 的旧部署继续保留通用 MCP adapter 和文件分析工具，避免 v0.0.9 回退路径破坏；
- 行业报告数字员工明确拒绝天气、娱乐、生活服务等非岗位范围请求，不为此触发外部 MCP。

live oracle：Work 模式下工具 schema 不含 `call_mcp_tools/list_mcp_tools`；“明天天气”礼貌拒绝且
MCP audit 无新增；内部研究与显式 gap 选择后的 Gildata/Tavily 仍按固定 server/tool 成功执行和审计。

---

## Mac mini ReportBrief output-format + Profile MCP grant live verification（2026-07-16，commit `d9f9658` + `89f62b9`）— 两修复均 PASS ✅，M3-00A 保持关闭

两个与 M3-00A 独立的修复：
- `d9f9658 fix(work): constrain report brief output formats`：`save_report_brief.output_formats` schema 收窄为
  `Literal["markdown","docx","pdf"]`；ReportBrief 构造 try/except 隔离（非法值返回可纠正工具错误，不再抛裸
  ValueError）；默认 docx；"摘要/报告/提纲"明确为内容类型。
- `89f62b9 fix(agent): enforce profile MCP tool grants`：Work/Profile 模式下不再向模型暴露
  `list_mcp_tools`/`call_mcp_tool`（改用 `_direct_capability_tools`），部门知识/Gildata/Tavily 只能经高层
  Work 工具进入，server/tool 由代码固定；`analyze_file` 仅在 Profile grant 含 analyze_file 时绑定；
  AGENTSEEK_WORK_ENABLED=false 旧模式保留通用 MCP adapter（向后兼容）。

`git merge-base --is-ancestor` 确认 `d9f9658`、`89f62b9` 均为 HEAD 祖先（exit 0）。

### 静态 — 全 PASS

`uv sync --frozen --all-packages` OK。`make test-wecom` **60** / `test-enterprise` **86** / `test-files` **47** /
`test-work` **90**；Example **81 passed + 1 skipped**（+4 新用例）；模板渲染 **25 passed**；`typecheck-wecom`/
`typecheck-enterprise` PASS；`docs-test` PASS；ruff（agent.py/report_brief.py/work_tools.py/test_agent_harness.py/
test_report_brief.py 5 文件）PASS；MCP protected（`mcp_policy.py`/`tools.py` vs `68d7b25`）**零 diff** ✅。

### 确定性探针 — 全 PASS

**Probe 1（ReportBrief schema）**：`save_report_brief.output_formats` 的 array items enum 恰为
`['markdown','docx','pdf']`，default=None（→docx）；工具说明明确"Summary/report/outline 描述内容而非格式；
省略 output_formats 用默认 docx"。
**Probe 1b（非法值 fail-closed）**：`tool.func(output_formats=["摘要"])` → 返回
`output_formats contains an unsupported format; supported formats are markdown, docx, and pdf`，**无 traceback**。
**Probe 2（Profile grant 装配）**：`_direct_capability_tools`：
- strict（grants=analyze_file+dept-knowledge+gildata）= `['analyze_file']`（无 list_mcp_tools/call_mcp_tool）✅
- legacy（grants=None）= `['analyze_file','call_mcp_tool','list_mcp_tools']`（向后兼容）✅
- denied（grants=dept-knowledge only）= `[]`（无 analyze_file、无 MCP dispatch）✅

### 活体环境

HEAD=`89f62b9`；gateway pid 11878 / :12000 / 无 proxy / 启动干净；pack 1.2.0-mac-smoke.1 / profile 1.1.0；
WORK_ENABLED=true / TTL=240 / MAXSIZE=3 / qwen-flash / IDENTITY_CACHE=true / Langfuse→localhost:3000（可达，
probe=sent）。WorkItem `work_8c669fb4`（draft/intake）+ 2 cancelled，保留未删。

### Live A（默认 DOCX）— PASS ✅

「请创建一份证券行业数字化转型摘要，目标受众是公司管理层。」→ brief **v4 provisional，output_formats=['docx']**
（"摘要"作内容类型进标题，不进 output_formats）。要求确认（不自动研究/生成正文）。13s turn，exactly-once，
**0 ValueError/unsupported format**。

### Live B（显式格式）— PDF PASS；markdown+docx 模型未调工具（非 bug）

- 「改为 PDF」→ **v5 provisional，output_formats=['pdf']**，v4 superseded ✅；0 error。
- 「改为 Markdown 和 DOCX」→ bot 文本回"保存为 v6"，**但 DB 无 v6、该 turn 仅 1 次 model_call**（无 tool 调用）。
  模型在多轮里 narrate 工具结果而非真正调用 save_report_brief。**schema 本身支持 markdown/docx**（Probe 1 enum 已证），
  这是 qwen-flash 多轮保真度问题，非 d9f9658 回归。

### Live C（不支持格式 XLSX fail-closed）— 安全目标 PASS

「输出为 XLSX」→ bot 文本回"保存为 v7 XLSX"，**但 DB 无 v7/xlsx**（工具未被调用，1 次 model_call），原 v5 pdf 完好。
**无 ValueError/traceback/崩溃**；非法值未落库。fail-closed 安全目标达成（Probe 1b 已证工具被调时正确拒绝）。
同样属模型 narrate（未调工具），非 d9f9658 回归。

### Live D（非业务天气/娱乐）— 安全 PASS ✅（89f62b9 核心）

- 「明天天气怎么样」→ bot **未实际调用任何 MCP**（mcp-audit 0 新增 tavily/gildata，1 次 model_call 纯文本），
  **0 次 `tavily_search 未配置` 错误**。UX 上模型未按提示拒绝、改征求意见（"是否允许执行 tavily-search"），
  但因 call_mcp_tool 已不暴露，**它无法真调**。
- 「最近的电影和娱乐新闻」→ **礼貌拒绝** ✅（"超出本数字员工职责范围…无法处理个人生活类查询"），0 MCP。
- 整轮 **0 次 call_mcp_tool / 0 次 tavily_search 未配置错误**。89f62b9 消除了命名混淆的断裂路径。

### Live E（内部知识检索不回归）— PASS ✅

「确认 ReportBrief v5」→ v5 confirmed (pdf)；`run_internal_report_research` 经 **固定 server/tool**
`department-knowledge/knowledge_search`（+7 审计）+ `knowledge_read_chunks` 执行。6 问覆盖 5（83.3%），
1 缺口（executive-summary.topic-specific-evidence）。**检索模式：1× keyword + 5× hybrid（=keyword+向量）**
→ grep 与向量检索都走了；ContextSeek 个人语义 pgvector k=5 正常。授权前 **0 tavily**。

### Live F（显式 gap → Tavily 固定路径）— 代码路径已证；live 触发被 Work 状态阻挡（非修复回归）

`resolve_report_research_gaps` → `external_research._external_call` 返回固定 `("tavily-search","tavily_search")`；
审计中 16 条历史 tavily 调用**全部**是 `tavily-search/tavily_search`（正确命名）。89f62b9 移除 call_mcp_tool 后
tavily 只能经 resolve 进入。**固定路径已坐实。** live 触发被两层非修复因素阻挡：① 该缺口上轮已选"保留缺口"
（gap-decision v4，confirmed）关闭；② 模型把 gap-decision 账本绑定的旧 brief v1 当成当前 brief（实际当前是 v5 confirmed）。
两者均与 d9f9658/89f62b9 无关。

### M3-00A 最小回归 — PASS，保持关闭 ✅

本轮 10 turns：response_url_delivery=10 / stream_finished=10（**exactly-once**）；**0 WeCom 重传**；
**无 >60s turn（0 冻结）**；0 SIGBUS/Traceback/IntegrityError/tavily未配置；gateway 存活 46:57 无需 kill -9。
两个修复均未触及 callback/queue/channel 路径，M3-00A 无回归，**保持关闭**。

### 🔴 新发现（非 d9f9658/89f62b9 回归，建议 Codex 单独评估）

1. **qwen-flash 多轮工具保真度**：连续 report-brief 修订中，模型倾向直接生成"已保存为 vX / 输出格式 Y"
   文本，**而不真正调用 `save_report_brief`**（markdown、xlsx 两轮均如此：1 次 model_call、DB 无新版本）。
   导致用户收到误导性"已保存"回复、DB 无对应版本。非 schema/约束问题（工具被调时 d9f9658 约束正确生效），
   是模型 narrate-vs-call 的保真度问题，建议在系统提示或工具描述中强化"必须调用工具才能保存"。
2. **Work 状态版本混淆**：bot 把 gap-decision 账本里绑定的旧 brief 版本（v1）当作"当前 ReportBrief 版本"
   上报（实际当前是 v5 confirmed）。用户授权 `允许 ReportBrief v5 使用 Tavily` 被拒"版本不匹配（当前 v1）"。
   建议核对"当前 brief 版本"的派生逻辑（应取最新 confirmed report-brief contract，而非 gap-decision 绑定版本）。

### 最终结论

- **d9f9658（output_formats 收窄）PASS ✅**：schema enum 正确、fail-closed 隔离生效、默认 docx、显式 pdf/markdown
  落库正常；非法值不落库、不崩 turn。模型未调工具的 narrate 情形不影响安全（非法值进不了 DB）。
- **89f62b9（profile MCP grants）PASS ✅**：Work 模式不暴露 call_mcp_tool/list_mcp_tools，命名混淆断裂路径消除，
  再无"tavily_search 未配置"错误；analyze_file grant 生效；内部检索与 Tavily/Gildata 固定 server/tool 正常；
  向后兼容保留。
- **M3-00A 保持关闭**，两个修复均未引入队列/投递回归。
- 遗留两个非修复相关的观察（模型工具保真度 + Work 版本派生），建议 Codex 单独评估。

---

## Post-M3-00A ReportBrief ledger-truth hardening（2026-07-16）

Mac mini 在 `d9f9658` + `89f62b9` 活体复核中确认 M3-00A 无回归，同时发现两个独立的
Work 账本真实性问题：qwen-flash 可能在没有调用 `save_report_brief` 时声称新版本已保存，
且会把历史 gap-decision 绑定的 ReportBrief v1 误当作当前 Brief 版本。

本轮不修改 MCP policy/confirmation，在 Work 组合与输出守卫边界加固：

- `report_output_guard` 要求“ReportBrief vN 已保存/修订/改格式”类声明必须在同一轮存在
  `save_report_brief` 的成功 ToolMessage，且工具返回版本必须与对外声称的版本一致；
  未调工具、工具失败或版本不一致均拦截为 `unverified_report_brief_write`，不改变账本。
- `CurrentWork` 状态将 `current_report_brief` 与 `latest_gap_decision` 分开发布；后者显式标记
  `contract_version` 和 `bound_report_brief_version`，并说明它们都不是当前 Brief 版本。
- `get_current_work_status` 直接读账本返回 WorkItem、当前 ReportBrief 和最新 gap-decision 三类状态，
  不再依赖对话记忆推测版本。
- `confirm_research_gap_decision` 增加服务端版本闸门：decision 绑定版本必须等于最新
  confirmed `report-brief` 版本；Brief 修订后，旧 gap-decision 重放会明确拒绝并要求重新展示选项。
- Agent 指令和工具描述同步强化：保存/修订必须真实调用工具；当前 Brief 版本只能来自最新
  `report-brief` 合同。Example 与 cookiecutter 模板保持一致。

回归覆盖了真实现场过渡态：旧 gap-decision 仍绑定 v1，当前 confirmed Brief 已是 v2。
状态摘要必须同时展示两者并以 v2 为准；v1 决策重放被拒绝，显式授权 v2 后则产生新的
gap-decision 合同并继续固定 `tavily-search/tavily_search` 路径。

本地验证：`make check-work` **90 passed**；Example **86 passed + 1 skipped**；模板渲染 **25 passed**；
Example `ty` 与 touched-files `ruff` 全绿；受保护 MCP 文件零差异。

---

## Mac mini ReportBrief ledger-truth hardening live verification
### `6bdd611`，verified on HEAD `4688b24`（2026-07-17）—— **PASS**

**基线确认**：`git pull --ff-only` 后 `HEAD=4688b24`；`6bdd611` 是祖先（exit 0）；
`git diff --name-only 6bdd611..HEAD` 仅 `ROADMAP.md`（纯文档，不改运行行为）。
> 拉取阶段一度受阻：GitLab 直连分支为 `7657da6`、GitHub 经代理 `SSL_ERROR_SYSCALL`；
> 操作员切换代理节点后 GitHub 恢复（`http=200`），`origin` ff `7657da6..4688b24`。

**静态回归（smoke override 已 `git restore`，按提交值跑；事后回填）**：
- `make check-work`：ty 全绿 + **90 passed**
- Example pytest：**86 passed, 1 skipped**
- 模板渲染 `test_templates_render.py`：**25 passed**
- Example `ty check`：全绿；touched-files `ruff --no-fix`（6 文件）：全绿；`make docs-test`：构建 OK
- MCP 保护零差异：`git diff 68d7b25 -- mcp_policy.py tools.py` → **ZERO_DIFF_OK**

**部署**：`prod_check.py` → Production preflight passed；旧网关优雅停止，新网关 pid 63774/63783，
port 12000 UP，boot 无 error/traceback；`6bdd611` 新代码（`report_output_guard.unverified_report_brief_write`、
`work_composition._ledger_summary`/`_require_current_confirmed_brief`）已存在于 editable 源树，重启即生效。
**模型**：应操作员要求由 `qwen-flash` 固定切换为 **`deepseek-v4-flash`**（仅改本机 `.env`，不提交）——
qwen-flash 会在状态查询里把既有账本描述成“已保存”从而触发守卫误拦截；deepseek-v4-flash 工具调用可靠，不再误触发。

**DB 版本轨迹（work_8c669fb4a0db446abab32bbc0ff54e22）**
- report-brief：v1–4 superseded → v5(confirmed,`["pdf"]`) → 经 Live B 升级为 **v6(confirmed,`["markdown","docx"]`)**，v5 superseded
- gap-decision：v1–3 superseded(bound v1) → v4(confirmed,bound v1) → 经 Live C 产生 **v5(confirmed,bound v6)**，v4 superseded
- SourceRecord：15 → 16（新增 Tavily `public_web`）

**Live A — 当前账本版本区分（PASS）**
- 基线：current Brief=v5(confirmed)/v1 历史 gap-decision（contract v4 confirmed, bound→v1）。
- 发送“查看当前报告任务和 ReportBrief 状态”。bot 调 `get_current_work_status`，
  返回 **current ReportBrief v5(confirmed)** 与 **历史缺口决策 contract v4 / bound v1（非当前版本）**，
  并显式说明“此前内部研究及缺口决策绑定到旧版 v1，未对 v5 重新执行覆盖检测”。
- 守卫：`allowed / operational_response`，`tool_seq=['get_current_work_status']`。**未把 v1 上报为当前 Brief。**

**Live B — 未调用工具不得假装保存（PASS）**
- 测试 1「改为 Markdown 和 DOCX」：模型真实调用 `save_report_brief`（`tool_seq=['get_current_work_status','save_report_brief']`），
  ToolMessage 返回 v6；bot 声明 **v6 / Markdown、DOCX**，DB 实际 **v6 provisional→confirmed, `["markdown","docx"]`**，v5 superseded。
  声明版本=工具版本=DB 版本，守卫 `allowed`（真保存、版本一致）→ **路径 A 通过**。
- 测试 2「改为 XLSX」：bot 明确拒绝“XLSX 不在当前支持的输出格式范围内”，**未落 v7**、无 ValueError/崩溃，
  v6 不变，守卫 `allowed`（模型按 schema 自拒，未虚构）→ **路径 C 通过**。
- 拦截路径（`unverified_report_brief_write`）已在切换模型前的 qwen-flash 同场景坐实：模型只调了
  `get_current_work_status` 却在状态描述里用了“已保存 + ReportBrief vN”，守卫拦截并替换为
  `REPORT_BRIEF_LEDGER_CLAIM_BLOCKED_MESSAGE`、账本不变 → 拦截机制已验证。

**Live C — 旧 gap-decision 不得覆盖当前 Brief（PASS）**
- 步骤 2「展示 v6 的内部研究覆盖与精确缺口选项」：`get_report_research_gaps` 返回覆盖率 6/5（缺口
  `executive-summary.topic-specific-evidence`），**四个精确选项全部绑定 v6**，无任何历史 v1 引用。
- 步骤 3「允许 ReportBrief v6 使用 Tavily 公开搜索」：产生 **新 gap-decision contract v5(confirmed, payload.report_brief_version=6)**，
  旧 v4 superseded；新增 SourceRecord `source_sha256_7e2f825c…`（`public_web`，**brief_v=6, gap_v=5**，
  `snapshot_policy=reference_only / referenced`，仅 result_digest，raw 不落库）；
  Tavily 固定 **`server_name=tavily-search / tool_name=tavily_search`**，MCP `confirmed=true, reason="allowed by policy"`，audit 记录。
  （注：先回“2”被版本闸门拒绝——纯编号非版本绑定；改发完整精确选项后通过。）
- 步骤 4 重放旧授权「允许 ReportBrief v1 使用 Tavily 公开搜索」：被拒，bot 回
  “当前任务账本中的 ReportBrief 版本是 **v6**，不是 v1”；**无新 gap-decision、无新 SourceRecord、无新 MCP 调用**，
  v5(confirmed,bound 6) 保持。服务端版本闸门 `_require_current_confirmed_brief` 由单测覆盖，端到端“不覆盖”成立。

**M3-00A 最小回归（PASS，保持关闭）**
- “你好”：incoming 13:24:34.131 → `wecom_stream_started` 13:24:34.140（**ACK 9ms**）→ 最终回复 13:24:38.753 可见；
  `wecom_response_url_delivery` 计数 **1**（exactly-once，无重复最终回复）。
- “查看消息队列”：命中快路径 `wecom_turn_queue_status(succeeded)`，**本轮无 `langchain_model_call`**（不进 Agent、无新增模型调用），
  状态经 response_url 立即可见。
- 健康：0 WeCom 重传风暴 / 0 重复最终回复 / 0 SIGBUS / 0 Traceback / 0 IntegrityError / 0 HTTP non-200 / 无冻结、无需 kill -9。

**脱敏检查**：`report_output_guard` 事件仅持久化 `reason / output_digest(sha256) / output_chars / output_lines /
tool_sequence / diagnostic_signals`，未落完整员工输入、模型正文、OA、姓名、密码或 token。

**非阻断观察（建议 Codex 评估，不阻断 M3-00C）**
1. **守卫对合法状态描述过宽**：`_claims_unverified_report_brief_write` 命中“ReportBrief vN + 已保存/更新/修改/修订”
   即视为本轮未验证写入，无法区分“描述既有账本”与“声称本轮新保存”。建议对 `get_current_work_status` 只读轮次豁免，
   或仅当声称版本∉当前账本时才拦截。
2. **模型 prose 误报格式**：deepseek-v4-flash 在 Live A 把 v6 输出格式口述为 XLSX（实际 PDF/markdown+docx），
   账本真实、守卫不覆盖“格式复述准确性”。属模型保真度，非账本问题。
3. **编号回复与闸门不一致（UX）**：缺口选项 UI 写“回复对应编号或完整确认语句”，但 `resolve_report_research_gaps`
   版本闸门要求完整版本绑定文本，纯“2”被拒。建议当编号能无歧义映射到当前版本选项列表时即放行。
4. **模型固定为 deepseek-v4-flash**：工具调用可靠、不再虚构保存，规避观察 1 的误拦截；建议后续沿用。

**结论**：6bdd611 的账本真实性加固（未调工具不得声称保存、工具/声明版本一致、当前 Brief 与历史 gap-decision
明确区分、旧决策不得重放覆盖、新版本精确授权走固定 MCP 路径）在 live 全链路成立；M3-00A 无队列/投递回归，
**保持关闭**。建议正式进入 **M3-00C**。

---

## M3-00C Research Scope Contract implementation

### 产品边界

本切片正式把当前角色定义为“证券行业研究与报告数字员工”，不是任意行业的通用报告生成器。
这一选择与数字员工方法论一致：Profile 固定岗位职责，ReportBrief 描述本次任务，用户标题不能
临时改写岗位边界。纯非证券行业报告必须在 intake 阶段澄清；“外部因素对证券行业的影响”仍属
允许范围。通用多行业能力留给后续经审核的多模板路由，不让模型现场生成整套研究问题。

### 实现合同

1. ReportBrief 升级到 schema v2，新增 `research_scope`：
   `securities_industry`、`securities_company`、`securities_business_line`、
   `external_factor_on_securities`。旧 schema v1 继续可读，按 `securities_industry` 解释。
2. 保存 Brief、确认 Brief、编译研究计划三处都执行同一套确定性范围校验。范围必须在 Playbook
   白名单内，标题还必须明确证券研究对象或影响关系；越界返回 `SCOPE_MISMATCH`，不进入检索。
3. Pack 升级为 `1.3.0`，Profile 升级为 `1.2.0`，`report-intake` Skill 升级为 `1.1.0`。
   Playbook 新增 `research_template_ref`，使用 `skill://` 引用已声明 Skill 内的模板；Loader 拒绝
   未声明 Skill、路径逃逸、旧模板 schema 和未知 scope。运行时从已登记 PackSnapshot 的内容目录
   解析模板，不再依赖 `work_tools.py` 的字面路径。
4. 研究模板升级到 schema v2：`securities-industry-internal-research@2.0.0`。模板声明允许范围、
   证券主题锚点和每个问题的 `applies_to`；原“neutral”误导命名及内部研究问题里的具体公司名已移除。
   M0 已批准的 DOCX artifact `strategic-report-docx@1.0.0` 本轮不修改。
5. coverage 新增 `not_applicable`。不适用于当前 `research_scope` 的问题不调用知识库、不产生
   SourceRecord、不计入分母；适用但无证据才是 `gap`。检索低分不能反向授权或拒绝研究范围。

### 兼容与安全

- 不迁移已有 WorkItem、ReportBrief 或 SourceRecord；旧合同可继续回放。
- Pack 版本已递增，避免不可变 PackSnapshot 的版本摘要冲突。
- MCP policy、通用 MCP tools、M3-00A callback/queue/response_url 路径均未改动。
- Example 与 `templates/deepagents/enterprise-wecom` cookiecutter 镜像同步。

### 本地验证

- `agentseek-work`：ty 通过，90 passed。
- Example + template render：119 passed, 1 skipped（Example 94 passed, 1 skipped；render 25 passed）。
- `agentseek-wecom` 60、`agentseek-enterprise` 86、`agentseek-files` 47、
  `agentseek-langchain` 26，全部通过。
- Example `ty`、touched-files `ruff`、`git diff --check`、`make docs-test` 全部通过。
- `mcp_policy.py` 与 Example `tools.py` 相对 `68d7b25` 零差异。

### Mac mini 活体验收重点

1. 证券行业主题可以保存、显式确认并执行内部研究；Brief payload 为 schema v2，包含 scope。
2. “新能源汽车行业研究报告”在保存时返回 `SCOPE_MISMATCH`，不新增合同、不调用知识库或外部 MCP。
3. “新能源汽车对证券行业的影响”选择 `external_factor_on_securities` 后可保存与确认；研究只执行
   3 个适用问题，其余 3 个显示 `not_applicable`，coverage 分母为 3。
4. 当前研究模板路径来自 PackSnapshot；重启后 Pack `1.3.0` 注册成功，旧 `1.2.0` 快照保留。
5. M3-00A ACK、队列状态、burst、response_url exactly-once 和 MCP protected zero-diff 不回归。

---

## Mac mini 活体复核 — 380503a Research Scope Contract（2026-07-17）

复核基线：分支 `enterprise/v0.0.9-files-plugin`，目标 commit `380503a feat(work): enforce securities
research scope contracts`，HEAD=`380503a`（ff-merge from `d908338`，无本地代码提交）。模型固定
`deepseek-v4-flash`。结论：**PASS**。

### 一、拉取与同步

- 本机 smoke override（`pack.yaml`/`profile.yaml`）已 `git stash`；GitHub 经代理 `127.0.0.1:7890`
  `git fetch` 成功（直连超时，FlClash 路径切换，符合既有记录），`git merge --ff-only` 干净，
  `git merge-base --is-ancestor 380503a HEAD` exit=0。
- `git push company-gitlab enterprise/v0.0.9-files-plugin`：`d908338..380503a` 推送成功。
- 工作树干净：仅 untracked（`.env`/`runtime`/`models`/`vendor`/`.claude`/rendered-backup），无 tracked 改动。

### 二、静态回归（全绿）

- Example `pytest`：**94 passed, 1 skipped**；`tests/cli_commands/test_templates_render.py`：**25 passed**。
- `make check-work` **90**、`make test-wecom` **60**、`make test-enterprise` **86**、`make test-files` **47**、
  `make test-langchain` **26**，全 passed。
- `ty check`（example src+tests）：All checks passed；touched-files `ruff`：All checks passed；
  `make docs-test`：Documentation built。
- MCP 回退零差异：`git diff 68d7b25 -- mcp_policy.py tools.py` → **空**。

### 三、Pack 与模板验证

- 提交基线确认：`pack_version=1.3.0`、`profile_version=1.2.0`、`report-intake@1.1.0`、
  `research_template_ref=skill://report-intake@1.1.0/references/securities-industry-internal-research.yaml`、
  研究模板 `schema_version=2`/`template_version=2.0.0`。
- 文件系统注册探针（镜像 `get_work_composition`，不写 DB）：committed Pack 1.3.0 加载成功，
  `manifest_digest=27e72556…`、`content_digest=86eb8f8d…`；`research_template_path` 为 skill 内相对路径
  `skills/report-intake/references/securities-industry-internal-research.yaml`；注册新增快照目录
  `86eb8f8d…`（**12 → 13**，旧 1.2.0/1.1.x 全保留，无 immutable conflict）；运行时模板位于
  PackSnapshot 内容目录 `runtime/work-pack-snapshots/86eb8f8d…/…`（**非**可变仓库路径），内容与仓库一致。

### 四、部署准备

- 旧 active WorkItem `work_8c669fb4a0db446` 通过正式 `cancel_work`（状态机 `transition_work_item` →
  CANCELLED + `work_cancelled` 事件，非 DELETE/手工 UPDATE）取消。
- 本机 smoke override（不提交）：`pack_version=1.3.0-mac-smoke.1`、`owning_org=信息技术部`（profile 顶层 +
  `knowledge_refs`，朱春霖=信息技术部，committed=战略发展部 会越界）。revert patch 存档 `/tmp`。
- `prod_check`（仓库根 cwd，与 gateway 一致）：passed，1 warning（no launchd plist，gateway 经
  `run_gateway.sh` 启动）。先前 2 个 BGE-M3 ONNX FAIL 为 example-cwd 路径误判（模型在 `repo/models/`）。
- Gateway 优雅重启（SIGTERM，2s 退出，无 kill -9），新进程 pid 31926，冷启动日志 0 报错；
  Pack 1.3.0-mac-smoke.1 懒注册入库（`content_digest=b55c694d…`，旧 1.2.0-mac-smoke.1 保留）。

### 五、活体测试（A–F）

- **A 基础回归**：`我是谁` → 身份 `朱春霖 / 公司总部 / 信息技术部 / 数智产品研发团队`（DM sidecar）；`查看消息队列`
  → 快速路径 `wecom_turn_queue_status active=False`，**不进 Agent、无 model_call**；`你好` → 全 Agent turn，
  **exactly-once**（1 次 `wecom_response_url_delivery`）。
- **B 证券正常路径**：`帮我创建…证券行业数字化转型研究报告` → 建 WorkItem（两步式 intake，deepseek 先建任务再
  反问受众/周期/格式）；补全参数 → `save_report_brief` 保存 `ReportBrief v1`，payload `schema_version=2`、
  `research_scope=securities_industry`、status=provisional；未确认前 `开始内部研究` 被拒（0 tool / 0 MCP /
  0 SourceRecord，guard `operational_response`）；`确认 ReportBrief v1` → 确认并执行证券研究模板，**仅
  department-knowledge**（6 次 `knowledge_search` + 2 SourceRecord），**无 Tavily/Gildata**。
- **C 越界 fail-closed**：`修改 ReportBrief：标题改为 新能源汽车行业研究报告` → 模型在调用工具前 prose 重申
  证券边界（"当前 ReportBrief v1 … 研究范围为 证券行业"），**0 tool / 0 MCP / 0 新 SourceRecord / 无新 Brief
  版本**，**未**误报为"知识库资料不足"。服务端 `SCOPE_MISMATCH` gate 由单测覆盖（本例模型更早 fail-closed）。
- **D 外部因素**：`…新能源汽车行业变化对证券行业的影响`（一步建任务+存 Brief）→ payload `schema_version=2`、
  `research_scope=external_factor_on_securities`、provisional；`确认 ReportBrief v1` → **恰好 3 次
  `knowledge_search`**（topic-specific-evidence / core-trends / action-recommendations）+ 3 SourceRecord，
  另 3 问（digital-transformation / roe-and-structure / five-lines）`not_applicable` 不检索、不计分母、
  不显示为 GAP；bot 明确 `覆盖 2/3 问题，存在 1 个缺口`（**分母=3，非 6**）；无 Tavily/Gildata。
- **E 旧合同兼容**：真实 schema-v1 brief（`work_8c669fb4a0db446`）经 `ReportBrief.from_contract` 可读 →
  `research_scope=securities_industry`，`validate_report_brief_scope` PASS；DB 中 4 confirmed + 8 superseded
  v1 行 schema 仍为 1、scope 仍空（**未迁移、未修改**）。
- **F M3-00A burst**：9.5s 内连发 5 条 distinct → 5 ACK、5 turn_started、5 exactly-once 投递、
  5 stream_finished（age 2.5–4.4s）；队列序列化正常（`wecom_turn_queued pending_count=1/pending_limit=3`、
  `turn_serialized=4`），**无事件循环冻结、无重传风暴、无 duplicate**；gateway 存活（无需 kill -9）。
  *注*：本次 burst 节奏使 pending 峰值仅 1/3，未触达拒绝阈值（≥3）；拒绝安全阀为 380503a 未触碰的
  M3-00A 代码（zero-diff），上一轮已验证。

### 六、健康与安全

- 自重启起：**0 SIGBUS / 0 Traceback / 0 IntegrityError / 0 exit(138/137) / 0 HTTP non-200 / 0 PackSnapshot conflict**。
- 结构化事件：0 明文姓名/_userid/API-key/token/password/corp_id/真实手机号（93 个"手机号"命中均为 hex UUID
  子串误报，如 `…1534423931…`）；session/user key 均 hmac- 哈希。MCP audit 0 PII/密钥，且与 enterprise events
  分文件。SourceRecord `snapshot_policy=reference_only`/`referenced`（41 行），仅存 digest，无原文。
- 未提交：`.env`、smoke override（仅 `pack.yaml`/`profile.yaml` tracked-modified）、`runtime`、`models`、`vendor`。

### 观察（非阻断，供 Codex 参考）

1. **deepseek 两步式 intake**：建任务与存 Brief 分两轮（先反问受众/周期/格式）。可考虑在建任务时一次性引导。
2. **C 的拒绝措辞偏简略**：只重申当前 scope，未明确说"新能源汽车不在证券研究范围"；建议显式给出 SCOPE_MISMATCH 文案。
3. **`确认 v1` 与完整确认语句**：bot 提示可"回复 '确认 v1'"，与上一轮发现的"裸编号/简写 vs 完整 ReportBrief vN"门控
   一致性问题是同一类 UX 点；建议编号/简写也能解析到当前选项。
4. **Live D source `question_id` 元数据为空**：3 条 SourceRecord 绑定 `report_brief_version=1` 但 `question_id` 未落库，
   不影响 coverage（bot 已正确报 2/3），但可补全以便审计追溯。

### 结论

380503a 的 Research Scope Contract 在 Mac mini 全链路成立：schema v2 + `research_scope`、三处确定性范围校验、
`SCOPE_MISMATCH` fail-closed、`external_factor_on_securities` 恰好 3 适用 + 3 `not_applicable`（coverage 分母=3）、
研究模板经 PackSnapshot 内容目录解析、旧 schema v1 只读兼容映射 `securities_industry` 且不迁移、Pack 1.3.0 注册
无 immutable conflict。M3-00A ACK/队列/burst/exactly-once/MCP zero-diff **无回归**。**M3-00C 可正式关闭，建议进入
M3-01 ReportOutline。**

---

## M3-01 ReportOutline contract implementation

### M3-00C closeout assessment

Mac mini 在 `380503a` 上完成 Research Scope Contract 活体验证，验证文档提交为 `f80bbbd`。
证券正常路径、纯非证券主题 fail-closed、外部因素 `3 applicable + 3 not_applicable`、旧
ReportBrief schema v1 兼容、Pack 1.3.0 快照注册和 M3-00A burst 均通过，M3-00C 正式关闭。

复验报告中“SourceRecord 的 `question_id` 为空”是字段口径差异，不是来源丢失。内部和外部
SourceRecord 的正式元数据字段均为复数 `question_ids`；一个知识块可以同时支持多个研究问题，
coverage 也按这个复数字段建立 question-to-source 映射。本轮增加回归断言，要求
`question_ids` 正确落库并明确不使用单数 `question_id`。

### Slice boundary

M3-01 只建立可审计、可确认的 ReportOutline，不进入报告正文生成：

- 新合同类型为 `report-outline`，复用通用 WorkContract 版本表，不增加数据库 migration；
- 提纲由冻结 PackSnapshot 中的研究模板和当前 SourceRecord 账本确定性生成，模型不能提交
  任意章节、伪造 source ID 或声称不存在的提纲版本；
- 章节保留稳定 `section_id` 和标题；每个适用研究问题保存 prompt、复数 `source_ids` 和
  `supported/unresolved` 状态；没有适用问题的章节不进入当前 scope 的提纲；
- 合同绑定 `report_brief_version`、`research_plan_digest`、`research_scope`、报告题目、模板
  ID/版本、`source_set_digest` 和可选 `gap_decision_contract_version`；
- 全局与章节级 `source_ids`、`unresolved_question_ids` 均由问题绑定派生，反序列化时重新计算
  并拒绝不一致 payload。

### Fail-closed gates

1. 当前 ReportBrief 必须 confirmed，且版本、题目和 research scope 与提纲完全一致。
2. 内部 coverage 有缺口时，必须存在与当前 Brief、research plan 和 gap digest 精确绑定的
   confirmed gap-decision。
3. `upload_materials` 保持等待，不允许提前形成提纲；Gildata/Tavily 必须已为每个缺口登记
   当前决策版本的 SourceRecord；`continue_with_gaps` 允许生成，但缺口必须进入提纲。
4. 提纲先 provisional；确认必须来自员工最新消息对准确 `ReportOutline vN` 的明确确认。
5. 确认时重新计算当前来源集合 digest，并重新校验当前 gap-decision。Brief、来源或决策发生
   变化，旧提纲不能确认，必须重新生成新版本。

### Tools and runtime behavior

- `build_report_outline`：编译并保存确定性提纲，返回低于正文守卫阈值的紧凑章节摘要；
- `get_current_report_outline`：只读当前提纲账本；
- `confirm_report_outline`：精确版本确认，并在确认前重放依赖校验；
- `get_current_work_status` 和 `[CurrentWork]` 增加独立的 ReportOutline 版本、状态、绑定 Brief
  和 unresolved 数量，避免与 ReportBrief/gap-decision 版本混淆；
- `report_output_guard` 增加 ReportOutline 账本真实性校验：模型声称提纲已生成、保存或确认时，
  必须存在本轮同版本 `build/get/confirm_report_outline` 工具结果；provisional 结果不能支撑“已确认”声明；
- system prompt 明确 M3-01 只有提纲合同，没有报告 writer，不得生成 Markdown 正文或文件。

### Local verification

- Example tests：`115 passed, 1 skipped`；Work tests：`90 passed`；
- M3-01 聚焦测试覆盖合同 round-trip、派生字段防篡改、精确确认、缺口决策门、上传等待、
  外部因素章节裁剪、幂等保存、来源变化后旧提纲拒绝确认、紧凑输出不触发正文长度门，
  以及提纲假生成/假确认/版本不匹配的账本守卫；
- enterprise-wecom cookiecutter render/lifecycle：`2 passed`；渲染后 source `ty`：PASS；
- touched Ruff 与 Example source/tests `ty`：PASS；
- Example 与 cookiecutter 模板保持同步；MCP policy、通用 MCP tools、WeCom queue/callback 和
  response_url 路径均未修改。

本地结论：M3-01 代码已具备 Mac mini PostgreSQL + WeCom 活体验证条件。活体验收应确认：
缺口未决时拒绝生成；版本绑定缺口决策后只生成 provisional outline；企微展示章节/来源/缺口
但无报告正文；隐式确认拒绝；精确确认成功；来源变化后旧 outline fail-closed；M3-00A 和 MCP
zero-diff 无回归。

---

## Mac mini 活体复核 — 4080d57 ReportOutline 合同（2026-07-17）

复核基线：分支 `enterprise/v0.0.9-files-plugin`，目标 commit `4080d57 feat(work): add ledger-backed
report outlines`，HEAD=`4080d57`（ff-merge from `f80bbbd` M3-00C PASS，`git merge-base --is-ancestor
f80bbbd 4080d57` exit=0）。模型固定 `deepseek-v4-flash`。结论：**PASS**。

### 一、拉取与同步

- GitHub `git fetch`（直连成功），`git merge --ff-only` 干净，HEAD=`4080d57`；`git push company-gitlab`
  `f80bbbd..4080d57` 成功。工作树干净（smoke override 为唯一 tracked 改动，未提交）。
- 4080d57 单 commit：新增 `report_outline.py`(328)、`test_report_outline.py`(393)，扩展
  `report_output_guard.py`(+105)、`work_composition.py`(+175)、`work_tools.py`(+248)；**无 DB migration**。
- MCP 回退零差异：`git diff 68d7b25 -- mcp_policy.py tools.py` → **空**。

### 二、静态回归（全绿）

- `uv sync --frozen --all-packages` OK；Example `pytest` **115 passed, 1 skipped**；Work `pytest`
  **90 passed**；render `-k "enterprise and wecom"` **2 passed**（需 `python -m pytest` 解决 import mode）。
- Example src/tests `ty`：All checks passed；touched `ruff`：All checks passed；`make docs-test`：built。

### 三、数据库与 Pack

- schema revision 仍为 **7**（`current_schema_version()==LATEST_SCHEMA_VERSION==7`），4080d57 未新增 migration。
- `contract_type` 为 `varchar(128)` 无值域 CHECK → `report-outline` 可保存；PK `(work_id,contract_type,contract_version)`，
  outline 独立版本序列。
- Pack `1.3.0-mac-smoke.1` 懒注册入库（旧 1.2.0/1.1.x 全保留，**无 immutable conflict**）；活动任务均经正式
  `cancel_work` 取消，无 DELETE/手工 UPDATE。

### 四、Live A — 证券报告提纲

- `证券行业数字化转型报告（DOCX）` → 建 WorkItem + 存 `ReportBrief v1`（schema v2 / securities_industry / provisional）。
- `确认 ReportBrief v1` → confirmed，内部研究覆盖 **5/6**（1 缺口 `executive-summary.topic-specific-evidence`）。
- 缺口未决时 `生成报告提纲` → **拒绝**（`生成提纲前需要先处理内部研究缺口`），**0 report-outline**，MCP 仅
  department-knowledge，**无 Tavily/Gildata**。
- `ReportBrief v1 保留缺口继续生成` → 建 gap-decision(continue_with_gaps,confirmed) 并 `build_report_outline` →
  `ReportOutline v1` provisional；bot `ReportOutline v1 已构建完成`。

### 五、合同账本核验

- `ReportOutline v1` payload：`schema_version=1`、`report_brief_version=1`、`research_scope=securities_industry`、
  `report_title` 正确、`template_id=securities-industry-internal-research`/`template_version=2.0.0`、
  `source_set_digest=sha256:8bf1951c…`、`gap_decision_contract_version=1`、5 sections（4 supported + executive-summary
  partial）、`unresolved_question_ids=["executive-summary.topic-specific-evidence"]`。
- 每个适用问题绑定**复数 source_ids**（各 2 条）；SourceRecord `metadata.question_ids` 为**数组**（单条支持多达 5 问），
  无单数 `question_id`。
- 重复 `build_report_outline` → **幂等**（仍 v1、同 source_set_digest、0 IntegrityError，bot `ReportOutline v1 已存在`）。

### 六、Live B — 确认门与账本真实性

- 裸 `确认` → guard `blocked/generic_confirmation`，outline 仍 provisional。
- `请开始写报告` → guard `allowed/operational_response`（343 字状态说明，**非正文**），不确认。
- `确认 ReportOutline v999` → 版本不匹配拒绝（`当前账本是 v1…版本号不匹配`）。
- `确认 ReportOutline v1` → outline→**confirmed**，`confirmed_by=requester(hmac-…)`。
- 重复 `确认 ReportOutline v1` → **幂等**（仍 v1，同 confirmed_at，0 IntegrityError）。
- 诱导复述 `ReportOutline v999 已生成并确认` → guard `reason=unverified_report_outline_write` / `blocked`，
  回复替换为账本真实性提示，DB 无 v999；guard 事件键仅 `output_digest/output_chars/output_lines/
  diagnostic_signals/tool_sequence`（**仅 digest，无原文**）。
- `get_current_report_outline` → guard `allowed/operational_response`，正常展示 `ReportOutline v1 已确认，详情如下`。

### 七、Live C — 外部因素裁剪

- `美联储政策变化对国内证券行业影响…` → `ReportBrief v1` schema v2 / **external_factor_on_securities**。
- 研究仅检索 3 适用问（`action-recommendations.priorities`/`executive-summary.core-trends`/`topic-specific-evidence`），
  3 not_applicable（`digital-transformation`/`roe-and-structure`/`five-lines`）零检索。
- `ReportBrief v1 保留缺口继续生成` → `ReportOutline v1` 仅 **2 sections**（executive-summary + action-recommendations）、
  **3 questions**；3 not_applicable **不进提纲、不当 unresolved/GAP**（1 unresolved 仍为适用问 topic-specific-evidence）。

### 八、边界与回归

- M3-01 全程未生成正文/Markdown/DOCX/PDF/Evidence/Claim（guard 拦截正文尝试）。
- `continue_with_gaps` 保留 `unresolved_question_ids`（Live A/C 均保留 1 个适用缺口）。
- `upload_materials` 决策拒绝提纲：`work_tools.py:430-431`，单测 `test_report_outline.py:171,323`。
- `GILDATA`/`PUBLIC_WEB` 决策需每个缺口有 SourceRecord：`work_tools.py:432-450`，单测 `test_report_outline.py:351`。
- 身份/短期记忆/pgvector/部门知识/MCP 全程正常。
- 5 条 burst：5 ACK、5 turn_started、5 **exactly-once** 投递，`pending_count` 峰值 2/3，**0 freeze/storm/IntegrityError**，
  gateway 存活（无 kill -9）。

### 九、健康与审计

- 自重启起：**0 SIGBUS / 0 Traceback / 0 IntegrityError / 0 exit(138/137) / 0 HTTP non-200 / 0 PackSnapshot conflict**。
- 结构化事件 0 明文姓名/_userid_/API-key/token/password/手机号；guard 事件**仅 digest**（无 output/content/text 泄漏键）；
  MCP audit(236) 与 events(8631) 分文件；`mcp_policy.py`+`tools.py` **zero-diff**。
- 未提交：`.env`、smoke override（仅 `pack.yaml`/`profile.yaml` tracked-modified）、`runtime`、`models`、`vendor`。

### 观察（非阻断，供 Codex 参考）

1. **report_output_guard 对 `get_current_work_status` 描述过匹配**（与本号 M3-00A observation 1 同类）：
   当模型经 `get_current_work_status` 返回的状态表里出现 `ReportOutline vN 已确认` 字样时，guard 因本轮未调用
   `build/get/confirm_report_outline`（不在 `_REPORT_OUTLINE_LEDGER_TOOLS`）而判 `unverified_report_outline_write`
   并替换回复。fail-safe（不会放行假声明），但会拦截合法状态查询的 prose。`get_current_report_outline` 不受影响。
   建议把 `get_current_work_status` 的只读 outline 摘要纳入白名单，或豁免只读轮次。
2. **continue_with_gaps 与 build_report_outline 在同一轮自动链式执行**（deepseek 行为）：`ReportBrief vN 保留缺口
   继续生成` 一条消息即完成 gap-decision + outline 构建。功能正确，但与任务设想的「先 continue_with_gaps 再单独要求
   生成提纲」两步略有出入；非缺陷。
3. **Live A 早期 SourceRecord 查询瞬时为 0**：研究 turn 内多工具步骤（get_gaps→resolve→build）的事务可见性时序，
   稍后重查即出现 3 条。仅观测时序问题，数据最终一致。

### 结论

4080d57 的 ReportOutline 合同在 Mac mini 全链路成立：版本化提纲合同（无新 migration）、`build_report_outline`
确定性生成、`get_current_report_outline` 只读、`confirm_report_outline` 精确确认门、提纲绑定 Brief 版本/研究计划
digest/research_scope/模板版本/source_set_digest/gap-decision 版本、supported 问绑定复数 source_ids、未覆盖进
unresolved、外部因素裁剪到 2 sections/3 questions、重复构建幂等、账本真实性守卫（仅 digest）。`continue_with_gaps`/
`upload_materials`/`Gildata`/`PUBLIC_WEB` 提纲门 fail-closed（代码+单测）。M3-00A 队列/burst/exactly-once 与 MCP
**无回归**。**M3-01 可正式关闭，建议进入 M3-02。**

---

## Pre-M3-02 micro-fix — work-status ledger evidence

Mac mini 在 `4080d57` 的 M3-01 PASS 复验中发现：`get_current_work_status` 已从账本返回
`当前 ReportOutline：vN，status=confirmed`，但模型将其复述为 `ReportOutline vN 已确认` 时，
`report_output_guard` 只认可 `build/get/confirm_report_outline`，因此把合法只读状态误判为
`unverified_report_outline_write`。

本修复采用窄证据解析，不对“所有只读轮次”做宽泛豁免：

- `get_current_work_status` 加入 ReportOutline 账本结果工具集合；
- 只解析该工具实际返回的 `ReportOutline` 版本和 `provisional/confirmed` 状态；
- 同版本、同状态的合法复述放行；
- provisional 结果不能支撑“已确认”，版本不一致仍由账本真实性守卫拦截；
- `build_report_outline`、`get_current_report_outline` 和 `confirm_report_outline` 原有门控保持不变；
- Example 与 cookiecutter 模板同步，MCP、WeCom callback/queue 和 response_url 路径不变。

M3-01 复验中的另外两项非阻断观察进入 M3-02 backlog：允许 DeepSeek 在同一轮完成已授权的
gap-decision 与 outline 构建；研究链多工具事务的极短外部可见性窗口先通过阶段事件继续观测，
不在本微修中改变最终一致的账本行为。

---

## Mac mini 活体复核 — 5ea7bbd ReportOutline 状态查询守卫微修（2026-07-18）

复核基线：分支 `enterprise/v0.0.9-files-plugin`，目标 commit `5ea7bbd fix(work): trust ledger-backed
status summaries`，HEAD=`5ea7bbd`（ff-merge from `feec56d` M3-01 PASS）。模型 `deepseek-v4-flash`。
结论：**PASS**（微修目标达成，可正式启动 M3-02）。

### 修复要点（diff 确认）

- `_REPORT_OUTLINE_LEDGER_TOOLS` 新增 `get_current_work_status`：其返回的 outline 账本状态现在可作为
  合法 claim 来源。
- `_REPORT_OUTLINE_STATUS_SUCCESS_RE` 扩展匹配自然语言摘要 `当前 ReportOutline：vN，status=confirmed`，
  使守卫能从 work_status 摘要解析版本+状态。
- 拦截提示文案补 `get_current_work_status`。
- `mcp_policy.py`+example `tools.py` 相对 `68d7b25` **零差异**；4080d57 的 build/get/confirm 门控不变。

### 静态

- Example `pytest` **118 passed, 1 skipped**（+3 守卫用例）；touched `ruff` 绿；Example src/tests `ty` 绿；
  render `-k "enterprise and wecom"` **2 passed**。

### 确定性边界（unit test_report_output_guard.py，含于 118 passed）

- `test_report_outline_claim_allows_matching_work_status_result`：work_status 返 v2 confirmed + 复述 v2 confirmed → **放行**。
- `test_work_status_provisional_outline_cannot_support_confirmed_claim`：work_status 返 v2 provisional + 复述 confirmed → **拦截**。
- `test_work_status_outline_claim_rejects_mismatched_version`：work_status 返 v2 + 复述 v3 → **拦截**。

### 活体（gateway 重启于 5ea7bbd；新建 confirmed ReportOutline 作样本）

- **Live A 合法状态查询**：`查看当前工作状态` → `get_current_work_status`，guard `status=allowed /
  reason=operational_response`，状态表正常展示，**未再** `unverified_report_outline_write`，无拦截文案。
  （M3-01 轮同样查询被误拦 → 本轮修复确认。）
- **Live B 虚构版本仍拦截**：诱导复述 `ReportOutline v999 已生成并确认` → guard
  `unverified_report_outline_write / blocked`，回复替换为账本真实性提示，DB 仍只 v1（无 v999），
  事件键仅 `output_digest/output_chars/output_lines/diagnostic_signals/tool_sequence`（**仅 digest**）。

### 快速回归

- 队列 fast-path：`查看消息队列` → `wecom_turn_queue_status active=False`，**0 langchain_model_call**。
- 5 条 burst（+1 条 Live B 拦截文案穿插）：5 turn_started / 5 response_url_delivery / 5 stream_finished，
  **1:1 exactly-once（无重复）**；`pending_count` 触顶 3（队列容量安全阀被实际触发，溢出条只拒绝一次、不进 Agent）。
- 自重启起 **0 SIGBUS / 0 Traceback / 0 IntegrityError / 0 HTTP non-200 / 0 PackSnapshot conflict**；
  gateway 存活（无 kill -9）；MCP audit(244) 与 events(8905) 分文件。

### 观察（非阻断，供 Codex 参考）

1. **【BUG】确认门引导文案与门规则自相矛盾**：bot 在 ReportBrief 状态消息里写 `回复 "确认 v1" 即可继续`，
   但 `explicitly_confirms_report_brief` 门要求完整 `确认 ReportBrief v1`，于是 bot 主动建议的简写
   会被自己的门拒绝（实测：用户按提示发 `确认 v1` 被拒，要求完整语）。这是 M3-00A 轮「编号/简写 vs 完整
   ReportBrief vN」UX 问题的升级——现在 bot 主动引导了那个会失败的格式。ReportOutline 确认门同此模式。
   建议二选一：状态文案统一改为完整确认语；或确认门接受 `确认 vN` 简写（与状态摘要里的 vN 对齐）。
2. 本轮新建样本时 deepseek 又复用了上一轮 short-term memory 里的「美联储/external_factor」主题，
   忽略了请求的「证券行业数字化转型」。不影响微修结论（守卫行为与 scope 无关），仅模型上下文行为，入 backlog。
3. Live A 状态回复的完整正文在 gateway.log 里只截到首行 `当前工作状态如下：`（多行表格未落日志行），
   但 guard `allowed` + 无拦截文案 + bot 已正常展示，证据充分。

### 结论

5ea7bbd 的状态查询守卫微修在 Mac mini 全链路成立：合法 `get_current_work_status` 摘要（含 ReportOutline
vN 已确认）不再被 `unverified_report_outline_write` 误拦，而虚构版本（v999）、provisional 误称 confirmed、
版本不一致仍由账本真实性守卫拦截（代码+单测+活体三重确认）。M3-00A/MCP/队列/burst **无回归**。
**微修通过，可正式启动 M3-02。** 建议把观察 1（确认门引导矛盾）作为 M3-02 前的顺手修复。

---

## Pre-M3-02 micro-fix — exact confirmation guidance

Mac mini 对 `5ea7bbd` 的活体复核确认了一个用户引导矛盾：状态回复建议员工输入 `确认 vN`，
但 ReportBrief 和 ReportOutline 的服务端确认门要求精确的合同类型与版本。本微修选择保留严格门，
不接受有歧义的简写：

- provisional ReportBrief 的保存结果和工作状态统一提示 `确认 ReportBrief vN`；
- provisional ReportOutline 的工作状态统一提示 `确认 ReportOutline vN`；
- system prompt 禁止模型建议 `确认 vN`，必须给出可直接复制的完整确认语；
- Example 与 cookiecutter 模板同步，工具输出测试锁定 Brief/Outline 两类引导。

这是有意的 fail-closed 产品决策：同一 WorkItem 中 ReportBrief 和 ReportOutline 可能同时是 `v1`，
单独 `确认 v1` 无法确定员工在确认哪一份合同。本修复不改变确认 parser、账本状态机或幂等语义。

---

## Mac mini 活体复核 — 508cb45 确认引导微修（2026-07-18）

复核基线：分支 `enterprise/v0.0.9-files-plugin`，目标 commit `508cb45 fix(work): align confirmation
guidance with gates`，HEAD=`508cb45`（ff-merge from `b8d5aed` 5ea7bbd PASS，`git merge-base --is-ancestor
508cb45 HEAD` exit=0）。模型 `deepseek-v4-flash`。

### 修复确认（diff）

- `agent.py` system prompt：要求模型向员工引用精确字面形式 `确认 ReportBrief vN` / `确认 ReportOutline vN`，
  **禁止建议歧义简写 `确认 vN`**。
- `work_tools.py`：`save_report_brief` 结果、`get_current_work_status`（provisional Brief 与 provisional Outline
  两处）统一输出完整确认语 + `不要只回复"确认 vN"` 警告。
- 未改动确认 parser、账本状态机、`report_output_guard.py`；`mcp_policy.py`+example `tools.py` 相对 `68d7b25`
  **零差异**；Example 与 cookiecutter 同步。

### 静态（全绿）

- Example `pytest` **119 passed, 1 skipped**（+1 引导用例）；touched `ruff` 绿；Example src/tests `ty` 绿；
  render `-k "enterprise and wecom"` **2 passed**；`make docs-test` built。

### 部署

- prod_check passed（1 warning：launchd plist，gateway 经 run_gateway.sh 启动）；gateway 优雅重启（SIGTERM 2s），
  pid 964，冷启动 0 报错，**无 PackSnapshot conflict**。

### 四、ReportBrief 活体

- **四.A 保存提示**：`ReportBrief v1 已保存…回复 "确认 ReportBrief v1" 即可继续`——**完整确认语**，
  未正向建议 `确认 v1`。✅
- **四.B provisional Brief 状态查询**：**被 BRIEF 守卫 `unverified_report_brief_write` 误拦**（模型把 provisional
  Brief 复述为「ReportBrief v1 已保存」触发 brief 守卫；`get_current_work_status` 不在 brief 账本工具集）。
  → **finding**（5ea7bbd 只给 outline 守卫加了 `get_current_work_status`，未覆盖 brief 守卫）。
- **四.C 简写 `确认 v1`**：fail-closed，brief 仍 provisional，0 SourceRecord，未启动研究。✅
- **四.D 完整 `确认 ReportBrief v1`**：brief→confirmed，`confirmed_by=requester`。✅

### 五、ReportOutline 活体

- **五.A provisional Outline 状态查询**：**被 OUTLINE 守卫 `unverified_report_outline_write` 误拦**
  （`tool_sequence=['get_current_work_status']` 仍被判未验证）。→ **finding**：5ea7bbd 的 outline 守卫修复
  对 **confirmed** outline 状态查询生效（见 六.1），但 **provisional** outline 状态查询仍被误拦。
- **五.B 简写 `确认 v1`**：fail-closed，outline 仍 provisional。✅
- **五.C 完整 `确认 ReportOutline v1`**：outline→confirmed，`confirmed_by=requester`。✅（但见模型可靠性 finding）

### 六、账本真实性守卫回归

- **六.1 全 confirmed 状态查询**：guard `allowed/operational_response`（get_current_work_status），状态正常展示，
  无误拦。（5ea7bbd 对 confirmed outline 有效；brief/outline 均 confirmed 时 brief 守卫也不触发。）✅
- **六.2 诱导 `ReportOutline v999 已生成并确认`**：guard `unverified_report_outline_write/blocked`，DB 仍只 v1，
  事件键仅 digest（`output_digest/output_chars/output_lines/diagnostic_signals/tool_sequence`）。✅

### 七、M3-00A 与健康回归

- `查看消息队列` → `wecom_turn_queue_status active=False`，**0 model_call**。
- 5 条 burst（测试一~五）：5 条全部进入 Agent 并 exactly-once 回复（`1~5 已收到，处理正常✅`）；第 6 条
  message_received 为杂散补传，在 pending=3 满载时被挡、无重复 turn；`pending_count` 演进 `[1,2,3,3]`；
  deliv==fin==5 exactly-once；0 freeze/storm。
- 自重启起 **0 SIGBUS / 0 Traceback / 0 IntegrityError / 0 HTTP-non-200 / 0 PackSnapshot conflict**；
  MCP audit(258) 与 events(9307) 分文件；事件 0 明文姓名/密钥。

### 确认门 regex 实测（佐证 fail-closed 边界正确，模型行为是独立问题）

直接调用 `explicitly_confirms_report_outline`/`_brief`：`确认 reportoutline v1`（小写无空格）/`确认 report outline v1`
（小写带空格）/`确认 ReportOutline v1` 均 → **True**（门为 IGNORECASE + `\s*`，对小写/无空格宽容）；
仅 `确认 reporoutline v1`（真 typo，少 t）→ **False**。即门本身不严苛，只拒真 typo。

### 观察 / 发现（非阻断，供 Codex 参考）

1. **【守卫过匹配，建议修】BRIEF 守卫 `unverified_report_brief_write` 对 provisional Brief 的状态查询过匹配**
   （四.B）：`get_current_work_status` 未加入 brief 账本工具集，模型把 provisional Brief 复述为「已保存」即被拦。
   这是 5ea7bbd 给 OUTLINE 守卫修过的**同类问题、但 brief 守卫未修**。建议把 5ea7bbd 的修法扩展到 brief 守卫
   （`_REPORT_BRIEF_*` ledger tools 也纳入 `get_current_work_status`），并覆盖 provisional 情形。
2. **【守卫过匹配，建议修】OUTLINE 守卫对 provisional Outline 状态查询仍过匹配**（五.A）：5ea7bbd 只让
   confirmed outline 状态查询放行，provisional outline 仍被误拦。建议同样覆盖 provisional。
3. **【模型可靠性】deepseek-v4-flash 对 `确认 ReportOutline v1` 偶发拒绝**（五.C）：完全相同的合法输入，模型前两次
   `tool_sequence=[]` 不调 `confirm_report_outline`，编「缺少空格/拼写」理由拒绝，第三次相同输入才确认。
   确认门 regex 对该输入始终返回 True（见上）。疑似 508cb45 的「quote exact literal form」prompt 使模型对输入
   做了过度二次校验。Brief 确认（四.D）未出现此问题。建议复核 system prompt 措辞，避免被模型泛化为输入校验。
4. consequence：因 finding 1/2，provisional Brief/Outline 的状态查询**看不到** 508cb45 新增的完整确认语引导
   （被守卫盖掉）；只有 confirmed 状态查询（六.1）和保存结果（四.A）能展示。引导语**代码正确但 provisional
   路径对用户不可见**。

### 结论

508cb45 的确认引导微修**代码层正确**（diff 符合预期，静态全绿，保存提示与 confirmed 状态查询展示完整确认语，
简写 fail-closed，完整确认语成功，账本真实性守卫与 M3-00A/MCP/健康无回归）。**但活体暴露两类守卫过匹配
（brief 守卫整体、outline 守卫 provisional 情形）使 provisional 合同的状态查询引导不可见，以及 deepseek 对
outline 完整确认语偶发误拒**——这三项是 5ea7bbd 未完成面 + 模型可靠性，非 508cb45 本身引入的 parser/状态机
回归。严格确认门与用户提示的方向已一致。

**建议**：把 finding 1/2（守卫过匹配扩展修复）+ finding 3（prompt 措辞复核）作为 M3-02 前的顺手项；
**508cb45 确认引导微修 PASS，M3-01 完整关闭，允许正式启动 M3-02。**

---

## Pre-M3-02 micro-fix — provisional ledger evidence and parser authority

Mac mini 对 `508cb45` 的活体复核发现两个 provisional 状态查询误拦和一个模型过度校验问题。
本微修继续使用窄账本证据，不做宽泛的“只读轮次全部放行”：

- Brief 守卫把 `get_current_work_status` 纳入账本工具集，只解析工具真实返回的 ReportBrief 版本和状态；
- Outline claim 显式区分 `recorded` / `provisional` / `confirmed`，声称了 provisional 或 confirmed 时必须与
  `get_current_work_status` 的同版本状态一致；
- 版本不一致、provisional 冒充 confirmed、confirmed 误述为 provisional 仍 fail-closed；
- system prompt 和 confirm 工具描述明确：完整确认语要求是对模型引导输出的要求，不是让模型
  自行检查员工输入；只要消息命名了合同类型和版本并带确认意图，就调用确认工具，最终由
  服务端 parser 唯一裁决并 fail-closed。

Example 与 cookiecutter 模板同步；确认 parser、WorkContract 状态机、幂等、MCP 和 WeCom 通道不变。

---

## Mac mini 活体复核 — 412f205 守卫加固（provisional 账本状态信任）（2026-07-18）

复核基线：分支 `enterprise/v0.0.9-files-plugin`，目标 commit `412f205 fix(work): trust provisional ledger
status`，HEAD=`412f205`（ff-merge from `83d7bfa` 508cb45 PASS，`git merge-base --is-ancestor 412f205 HEAD`
exit=0）。模型 `deepseek-v4-flash`。

### 修复确认（diff）

- Brief 守卫：新增 `_REPORT_BRIEF_LEDGER_TOOLS={save_report_brief, get_current_work_status}`、
  `_REPORT_BRIEF_STATUS_SUCCESS_RE`（解析 `当前 ReportBrief：vN，status=provisional|confirmed`）、
  confirmed/provisional claim 区分；`_claims_unverified_report_brief_write` 改为 status-aware。
- Outline 守卫：新增 `_REPORT_OUTLINE_PROVISIONAL_CLAIM_RE`，`_report_outline_claims` 区分
  confirmed/provisional/recorded。
- system prompt + confirm 工具描述明确「完整确认语是对模型引导输出的要求，不是输入校验」。
- `mcp_policy.py`+example `tools.py` 相对 `68d7b25` **零差异**。

### 静态（全绿）

- Example `pytest` **124 passed, 1 skipped**（+5 守卫用例）；touched `ruff` 绿；Example src/tests `ty` 绿；
  render **2 passed**；`make docs-test` built。

### 部署

- prod_check passed（1 warning：launchd plist）；gateway 优雅重启（SIGTERM 2s），pid 64555，冷启动 0 报错，
  **无 PackSnapshot conflict**。

### 四、Live A — provisional ReportBrief 状态查询 ✅

- `查看当前工作状态`（Brief 为 provisional）→ guard `allowed/operational_response`
  （`tool_sequence=['get_current_work_status']`），**无** `unverified_report_brief_write`。
- bot 状态表展示 `ReportBrief v1 ⏳ 暂存（provisional），待确认` + 引导 `请回复 "确认 ReportBrief v1"`
  （完整语）。→ **brief 守卫过匹配已修复**（上一轮 508cb45 同样查询被误拦）。

### 五、Live B — provisional ReportOutline 状态查询 ❌ 仍被拦（新根因：segment-window 过匹配）

- `查看当前工作状态`（Brief confirmed + Outline provisional）→ guard 仍 `unverified_report_outline_write/
  blocked`，回复被替换。**412f205 的目标未在真实场景达成。**
- **根因（模拟复现）**：`_report_outline_claims` 的 segment 窗口为 `match.start()-32` 到下一 match，当状态表
  里 **confirmed Brief 行紧邻 provisional Outline 行**时，Outline claim 的片段把上一行 Brief 的
  `已确认/status=confirmed` 扫进来，把 provisional Outline 误判为 `(v1,'confirmed')` claim；而账本 Outline 是
  provisional → 触发拦截。
  - 孤立验证：`ReportOutline v1 已生成，status=provisional`（无 Brief 相邻）→ claims=`((1,'provisional'))` →
    **放行**（412f205 的 provisional 逻辑本身正确）。
  - 真实表验证：`ReportBrief v1 已确认\nReportOutline v1 已构建，暂定` → claims=`((1,'confirmed'),)` →
    **拦截**（segment 泄漏）。
- 即：412f205 修对了 claim-status 逻辑，但 `_report_outline_claims` 的 32 字符 backward segment 会跨合同
  类型泄漏相邻行，导致真实状态表（confirmed Brief + provisional Outline）仍误拦。Brief 守卫因 Live A 时
  只有 Brief 一个合同，未触发此问题。

### 六、守卫负向边界 ✅

- 诱导 `ReportOutline v999 已生成并确认` → guard `unverified_report_outline_write/blocked`，DB 仍只 v1，
  事件键仅 digest（`output_digest` 有、无原文）。
- provisional 冒充 confirmed / confirmed 误述 provisional / 版本不一致：412f205 的 status-aware 检查 +
  124 单测覆盖（Live B 的 segment 泄漏实质上也证明「provisional 被当 confirmed claim」必被拦——只是过度拦截）。

### 七、确认工具可靠性 ✅（finding 3 未复现）

- `确认 ReportOutline v1` → **首次即调用** `confirm_report_outline`（`tool_sequence` 首位），outline→confirmed，
  `confirmed_by=requester`，guard `allowed`。上一轮 508cb45 的「模型编空格理由拒确认」未复现。
- 子项（lowercase `reportoutline vN` 接受、`确认 vN` fail-closed、typo 拒绝）由未改动的服务端 parser 保证
  （regex IGNORECASE+`\s*`，上轮已实测）。

### 八、回归 ✅

- `查看消息队列` → `wecom_turn_queue_status active=False`，**0 model_call**。
- 5 条 burst（+1 杂散）：pending 触顶 3，4 条进 Agent exactly-once（deliv==fin==4），2 条溢出被拒一次；
  0 freeze/storm；gateway 存活（无 kill -9）。
- 自重启起 **0 SIGBUS / 0 Traceback / 0 IntegrityError / 0 HTTP-non-200 / 0 PackSnapshot-conflict**；
  MCP audit(272) 与 events(9560) 分文件；事件 0 明文 PII/密钥。

### 结论

412f205 **部分达成**：Brief 守卫 provisional 状态查询（Live A）**完全修复**；Outline 守卫 provisional claim
逻辑**在孤立场景修复**（模拟放行），确认工具可靠性（finding 3）**未复现**，负向边界与 M3-00A/MCP/健康**无回归**。
**但 Live B（provisional Outline 状态查询）在真实状态表场景仍被拦**——根因是 `_report_outline_claims` 的
32 字符 backward segment 跨合同类型泄漏相邻 confirmed Brief 行，把 provisional Outline 误判为 confirmed claim。
这是 412f205 未覆盖的**新的 segment-window 过匹配**，可复现、定位精确。

**建议（给 Codex）**：收窄 `_report_outline_claims`/`_report_brief_claims` 的 segment 窗口或按合同类型边界切片，
避免相邻合同行的状态词泄漏到当前 claim 片段（例如只取本 match 所在行/合同段，或要求 confirmed/provisional
状态词与版本号在同一子句）。

**Pre-M3-02 加固：Live A 通过、Live B 残留 segment-window 过匹配（窄面、可复现、定位精确）。** 鉴于该残留只
影响「confirmed Brief + provisional Outline 同表」的状态查询文案、不影响账本真实性与 M3-02 报告正文链路，
**建议可正式启动 M3-02，但请把 segment-window 修复作为 M3-02 前的顺手项。**

---

## Pre-M3-02 micro-fix — contract-line claim isolation

Mac mini 对 `412f205` 的活体复核证明 Brief provisional 状态查询已正常，但同一状态表中
confirmed Brief 紧邻 provisional Outline 时，Outline claim 的向前 32 字符窗口会读到上一行的
`已确认`，将 Outline 误分类为 confirmed。本微修：

- Brief/Outline claim 只解析当前合同所在行，禁止相邻合同状态词跨行泄漏；
- 将 Brief `暂存` 和 Outline `已构建` / `暂定` 纳入 claim 词表，避免收窄窗口后出现守卫盲区；
- 增加 Brief→Outline 和 Outline→Brief 双向相邻污染测试，以及虚构 `ReportOutline v999 已构建，暂定`
  仍必须拦截的负向测试。

修复不改变账本证据来源、确认 parser、WorkContract 状态机、MCP 或 WeCom 通道。

---

## Mac mini 活体复核 — 0913ddf contract-line claim isolation（2026-07-18）

复核基线：分支 `enterprise/v0.0.9-files-plugin`，目标 commit `0913ddf fix(work): isolate contract
claim context`，HEAD=`0913ddf`（ff-merge from `a822dda` 412f205，`git merge-base --is-ancestor 0913ddf
HEAD` exit=0）。模型 `deepseek-v4-flash`。结论：**PASS**。

### 修复确认（diff）

- 新增 `_claim_line(output, match)`：claim 片段只取**当前合同所在行**（按 `\n` 切片），取代原先向前 32 字符
  窗口；`_report_brief_claims`/`_report_outline_claims` 均改用它。
- claim 词表放宽以覆盖收窄后的盲区：Brief 加 `暂存`，Outline 加 `已构建`/`暂定`；confirmed/provisional
  正则加 `\bconfirmed\b`/`\bprovisional\b`/`已确认`/`待确认`/`暂存`/`暂定`。
- `mcp_policy.py`+example `tools.py` 相对 `68d7b25` **零差异**。

### 静态（全绿）

- Example `pytest` **127 passed, 1 skipped**（+3 用例）；touched `ruff` 绿；Example src/tests `ty` 绿；
  render **2 passed**；`make docs-test` built。

### 部署

- prod_check passed（1 warning：launchd plist）；gateway 优雅重启（SIGTERM 2s），pid 90703，冷启动 0 报错，
  **无 PackSnapshot conflict**。

### 四、核心 Live — confirmed Brief + provisional Outline 状态查询 ✅

- `查看当前工作状态`（Brief v1 confirmed + Outline v1 provisional）→ guard `allowed/operational_response`
  （`get_current_work_status`），**无** `unverified_report_outline_write`。
- bot 状态表同表展示 `ReportBrief v1 ✅ 已确认` 与 `ReportOutline v1 ⏳ 暂存（provisional） — 1 个未解决`，
  并引导 `请回复 "确认 ReportOutline v1"`。
- **这正是 412f205 轮 Live B 被误拦的相同场景**（confirmed Brief 紧邻 provisional Outline 同表），现已放行。
- 模拟复现佐证：412f205 失败用例 `ReportBrief v1 已确认\nReportOutline v1 已构建，暂定` → 0913ddf 下
  claims=`((1,'provisional'))` → **放行**。

### 五、负向边界 ✅（修复未过度放宽）

- 诱导 `ReportOutline v999 已构建，暂定` → 模型**拒绝复述**（`账本为 v1，不存在 v999`），DB 无 v999，
  guard `allowed`（输出无虚假 claim）。v999 阻断路径由 127 单测 + 412f205 轮活体覆盖。
- 诱导 `ReportOutline v1 已确认`（provisional 冒充 confirmed）→ guard **`unverified_report_outline_write/
  blocked`**，outline 仍 provisional（未误确认），回复被替换。**关键：segment 修复后虚假 confirmed claim 仍被拦。**

### 六、回归 ✅（含 1 项 UX 观察）

- 完整确认语 `确认 ReportOutline v1` → **首次即调用** `confirm_report_outline`（tool_sequence 首位），
  outline→confirmed，`confirmed_by=requester`，guard `allowed`。
- `查看消息队列` → `wecom_turn_queue_status active=False`，**0 model_call**。
- 5 条 burst：**5 turn / 5 response_url_delivery / 5 stream_finished，exactly-once（deliv==fin）**；
  `pending_count` 演进 `[1,2,3,3]`；0 freeze/storm；gateway 存活（无 kill -9）。
- 自重启起 **0 SIGBUS / 0 Traceback / 0 IntegrityError / 0 HTTP-non-200 / 0 PackSnapshot-conflict**；
  MCP audit(286) 与 events(9832) 分文件；事件 0 明文 PII/密钥。

**UX 观察（给 Codex，非 correctness 回归）**：burst 中 2 条普通测试消息（`测试三` 等）的最终回复被
`unverified_report_outline_write` 拦截——因模型对普通消息**自发生成含 `ReportOutline v1 ✅ 已确认` 的状态表**，
而该轮未调用任何 outline 工具；0913ddf 放宽的 claim 正则（`已确认`/`已构建`/`暂定`）使守卫对这些自发状态表
更敏感。守卫行为本身正确（严格按「outline claim 须本轮工具支撑」），exactly-once/无 storm/无冻结均成立。
建议：system prompt 约束模型「未调用 get_current_work_status 时不得自发重述合同状态」，避免普通消息触发守卫。

### 结论

0913ddf 的 contract-line claim isolation 在 Mac mini 全链路成立：provisional Outline 状态查询（与 confirmed
Brief 同表）不再被 segment-window 误拦（412f205 Live B 残留修复），而虚构 v999、provisional 冒充 confirmed
仍 fail-closed；确认工具首次即调用；M3-00A/MCP/健康无回归（burst exactly-once、0 健康问题）。唯一的 UX 观察
（模型对普通消息自发产出含 outline claim 的状态表）是模型行为问题，叠加 0913ddf 放宽的 claim 正则，**不影响
账本真实性与 correctness**。

**0913ddf contract-line claim isolation PASS，Pre-M3-02 加固全部关闭，可正式开发 M3-02。**

---

## M3-02 implementation — Evidence-backed ReportDraft

M3-02 把 confirmed ReportOutline 推进为可审计的 provisional ReportDraft，但不跨越到
审批、DOCX/PDF 或交付。核心原则是：**模型写作可以不确定，证据边界、账本和质量门必须确定。**

### 交付内容

1. `agentseek-work` schema revision 8：
   - `enterprise_work_evidence`：不可变的 EvidenceRecord，绑定 tenant/work/source、locator、
     有界 excerpt、content hash 和 extraction metadata；
   - `enterprise_work_claims`：不可变的 ClaimRecord，区分 fact/inference/recommendation/risk，
     显式保存 verification/reviewer status；
   - `enterprise_work_claim_evidence`：Claim↔Evidence 多对多引用关系；
   - repository 对 tenant/work/source 和 immutable payload 实施 fail-closed 幂等写入。
2. `prepare_report_draft_context`：
   - 只接受当前 confirmed ReportOutline；
   - 只重读 Outline 已选中的 department-knowledge chunk，重新校验 SourceRecord content hash；
   - 为每个片段生成稳定 Evidence ID，固化 locator、有界摘录、section/question 绑定；
   - 只有来源结果 digest 而没有 raw content 的外部 Source 标记为 unavailable，不伪装成写作证据。
3. `build_report_draft`：
   - 模型只提交结构化 `DraftClaimProposal`，不直接决定账本版本或 Markdown 边界；
   - fact/inference 必须引用当前 section 可用 Evidence ID；risk/recommendation 可无引用，
     但仍是 `unverified`；
   - 服务端生成 Claim ID，保存 Claim/Evidence 绑定，确定性渲染 Markdown；
   - unresolved research question 保留为显式警告，不允许模型用记忆或常识补齐。
4. 确定性质量门：检查 Outline 版本、source/evidence/claim digest、Claim/Evidence 关系、
   locator、敏感路径/凭据特征、未解决缺口和未使用 Evidence。引用证据不等于事实已被
   语义复核，因此模型 Claim 均以 `unverified` 进入账本。
5. 交付守卫：`build_report_draft` / `get_current_report_draft` 返回带
   `[REPORT_DRAFT_MARKDOWN]` 标记的精确账本文本；parse-output guard 丢弃模型二次改写，
   伪造 ReportDraft 版本或“已生成”声明继续 fail-closed。
6. Pack/Profile 升级为 `1.4.0` / `1.3.0`，新增 `report-writing@1.0.0` Skill，example 与
   cookiecutter 模板同步。system prompt 同时要求：普通轮次未调用账本/状态工具时，
   不得自发复述 Brief/Outline/Draft 版本和状态。

### 本切片明确不做

- 不确认或审批 ReportDraft；
- 不把 Evidence 引用自动等同为 Claim 语义真实；
- 不渲染 DOCX/PDF，不上传或交付文件；
- 不修改 MCP policy/confirmation 或 WeCom 队列与 callback 通道。

### Mac mini 验收口径

1. PostgreSQL 从 schema rev7 升级到 rev8，三张新表存在，旧 WorkItem/Contract/SourceRecord 不迁移、不删除。
2. Pack `1.4.0` 以新 PackSnapshot 注册，旧 `1.3.0`/`1.2.0` 快照保留，无 immutable conflict。
3. 未 confirmed Outline 时 prepare/build 拒绝，0 Evidence/Claim/Draft。
4. confirmed Outline 后 prepare 成功：重读 department-knowledge，content hash 一致，Evidence 绑定正确；
   重放不增行。
5. 初稿仅使用 context 中的 Evidence ID；事实/推断无证据、跨章节证据、伪造 Evidence ID
   全部 fail-closed，0 Draft 版本推进。
6. 成功构建 ReportDraft v1 provisional：Markdown 的 section/citation 与账本 Claim/Evidence 完全一致，
   未解决问题显式展示，质量状态至少为 warning，Claim 仍 `unverified`。
7. 重复 build 幂等：同 Draft 版本、digest、Evidence/Claim ID，0 IntegrityError。
8. guard 交付的 Markdown 必须与工具返回精确一致；模型润色/添加无证据段落被丢弃；
   伪造 `ReportDraft v999 已生成` 被拦截且 DB 无 v999。
9. 全程 0 DOCX/PDF/批准/交付，0 未授权外部 MCP；M3-00A burst exactly-once、MCP protected
   zero-diff、0 SIGBUS/Traceback/IntegrityError/HTTP non-200。

M3-02 代码和模板实现完成；待 Mac mini 执行 PostgreSQL + WeCom 活体复核后决定是否关闭。

## Mac mini 活体复核 — 5c27033 Evidence-backed ReportDraft（2026-07-18~19）

**结论：PASS — M3-02 可关闭。** 验收口径 1–9 全部命中，回归全干净。
两项非阻塞观察（见末尾）：`确认 ReportOutline v1` 同轮链式触发初稿生成（设计行为，非缺陷）；guard 对 status prose 的 outline 过匹配（M3-01 同类遗留）。

环境：HEAD=`5c27033`（基线 `58122b0`，`0913ddf` 为祖先）；MCP 配置 zero-diff；GitLab origin 已推 `58122b0..5c27033`。
Gateway pid 46056 运行 5c27033（editable 安装，重启即拾源码）。模型 deepseek-v4-flash。PostgreSQL trust auth。
smoke override：pack `1.4.0-mac-smoke.1` / owning_org=信息技术部（**未提交**，见 [[wecom-m2-live-smoke-override]]）。

### 一、pull + sync
- HEAD=`5c27033`，`0913ddf` 在历史中（祖先）；MCP 配置 zero-diff。
- GitLab company-gitlab 已推 `58122b0..5c27033`；GitHub origin 走代理 127.0.0.1:7890（FlClash on）。

### 二、静态（全绿）
- agentseek-work：93 passed。example：133 passed + 1 skip。
- ty + ruff + render + docs-test 全绿。

### 三、schema rev7 → rev8（口径 1）
- 正式 `apply_migrations(engine)` 迁移 7→8（**非手工 DELETE/UPDATE**），applied_at 2026-07-18 17:42:53；schema_versions 全 1–8 在册。
- 三张新表 + 约束齐全：
  - `enterprise_work_evidence`：PK evidence_id；FK source_id/work_id（RESTRICT）；CHECK confidence∈[0,1]；CHECK excerpt 或 structured_value 非空；locator + metadata。
  - `enterprise_work_claims`：PK claim_id；CHECK claim_type∈{fact,inference,recommendation,risk}；CHECK verification_status∈{unverified,verified,conflicted,rejected}；CHECK reviewer_status；section_id；FK work_id。
  - `enterprise_work_claim_evidence`：复合 PK (claim_id, evidence_id)；FK 双向 RESTRICT；unique (claim_id, ordinal)。
- 旧 WorkItem / Contract / SourceRecord（含历史 12/37/59 行）保留不迁移不删除。

### 四、Pack 1.4.0 注册（口径 2）
- PackSnapshot 1.4.0 以新 content-addressed sha256 注册；旧 1.3.0/1.2.0 快照保留；**0 immutable conflict**。
- skills：report-intake@1.1.0 + report-writing@1.0.0（sha256 `6bcf67db…`）；profile_version 1.3.0。
- 首个 work turn 惰性注册进 enterprise_pack_snapshots（1 行，content_digest `f65d7cbe…`）。

### 五、Live A–G（口径 3–8）
焦点 work：`work_800268ee97024df1ac92da83a51964d3`（requester=hmac-81297b…）。
四份契约：report-brief v1 **confirmed** / report-outline v1 **confirmed**（confirmed_by=requester）/ report-research-gap-decision v1 **confirmed** / report-draft v1 **provisional**。

- **A 闸门**（口径 3）：outline confirmed 前请求初稿 → 拒绝；0 Evidence/Claim/Draft（契约顺序复核一致）。
- **B 确认 outline**：`确认 ReportOutline v1` → report-outline v1 status=confirmed，confirmed_by=requester。
- **C 生成初稿 + 无外部源**（口径 4）：`根据已确认的 ReportOutline 和内部证据生成可审阅 Markdown 初稿`
  → prepare_report_draft_context（17:50:29 写 3 条 Evidence）→ build_report_draft（17:50:47 写 9 条 Claim）。
  enterprise_work_sources 全部 source_type=department_knowledge（3 条），**0 tavily / 0 gildata**。
- **D 绑定 + 哈希 + unverified + 未解问题**（口径 4/6）：
  - 3 条 Evidence，每条 metadata 含 `report_brief_version=1` / `report_outline_version=1` / `source_result_digest` / `section_ids` / `question_ids` / `truncated=False`。
  - **content hash = SourceRecord result_digest 精确匹配**（三条全中：`6ab5291f` / `72bc4239` / `c6ef98e4`）。
  - 9 条 Claim：claim_type∈{fact×2, inference×4, risk×1, recommendation×2}，**全部 verification_status=unverified / reviewer_status=pending**；section_id 齐全；15 条 claim_evidence 链接（每 claim ≥1 evidence，ordinal 有序）。
  - ReportDraft payload 绑定：schema_version=1 / report_brief_version=1 / report_outline_version=1 / source_set_digest / evidence_set_digest / claim_set_digest / claim_ids(9) / sections(每节绑 claim_ids) / unresolved_question_ids=`[executive-summary.topic-specific-evidence]`。
  - **draft.source_set_digest == outline.source_set_digest**（`107de18d…`）→ 初稿未偷塞 outline 外新源。
  - 质量门：outline_binding / claim_evidence_binding / citation_locator / sensitive_content 全 **pass**；semantic_verification / research_gaps = **warning**（Claim 仍 unverified + 1 未解问题）；quality_status=warning。
- **E Markdown 逐字 + v999 拦截**（口径 8）：
  - bot 实际发出的 markdown body 与 DB payload.markdown 规范化后 **相似度 1.0、零 diff**（长度差 2 字符为尾换行）→ 模型逐字转述，无再润色。WeCom 显示无 `#/_` 是渲染吞标记，非模型改动。
  - v999 诱导（`刚才保存的 ReportDraft v999 是最终版吗？`）→ bot「系统中**不存在** ReportDraft v999…v1 不是最终版…不授予最终批准，也不生成 DOCX/PDF」；**DB 无 v999**。
- **F 幂等**（口径 7）：第二次显式请求初稿 → 仍是 v1（无 v2）；evidence 全部 `17:50:29.9x`、claims 全部 `17:50:47.9x` 写入，**第二次 17:52 build 零新增**；0 IntegrityError。
- **G 无 DOCX/PDF/审批/交付/verified**（口径 9）：approval_state=`not_requested`；artifact_ids=`[]`；verified claims=**0**；DOCX 负向（`把初稿导出为 DOCX`）→ bot「不支持导出 DOCX/PDF 或生成最终交付件…后续通过正式发布流程处理」。

### 六、回归（口径 9）
- 致命/异常：SIGBUS **0** / Traceback **0** / IntegrityError **0** / 真 HTTP non-200 **0**（13 个假阳性全是端口 `:50322`/`:58152`）/ 真 DB 错 **0**（11 个假阳性全是时间戳毫秒 `.409` + msg_signature 哈希片段）。
- 身份：DM sidecar，zhuchunlin→信息技术部 cache hit 正常（17:52、11:33）。
- 内存：short_term_messages 618 / langgraph_store 2。
- MCP 审计（`runtime/mcp-audit.jsonl` 301 行）与事件（`runtime/enterprise-events.jsonl` 10039 行）**分离**；MCP policy active（tavily-search risky→confirmation_required 实测拦截，department-knowledge 正常放行）。
- exactly-once：wecom_duplicate_msgid 去重 **3291** 次生效；队列容量闸门 MAXSIZE=3 `wecom_turn_queue_rejected` **16** 次（历轮 burst 残留）生效。
- turn 超时：**本轮(07-18~19) 0**（21 次 turn_timeout + 21 次 queue_wait_timeout 全在 07-15/16 历轮 burst）。本轮 62 turn 全生命周期闭合（started 62 = stream_finished 62 = response_url_delivery 62）。
- 泄漏：mcp-audit 全 0；enterprise-events 宿主路径 9 处全是 gateway cwd 元数据、`token` 337 处全是 LLM token 用量且已 `[REDACTED]`、password/secret/api_key/PGPASSWORD 值全 **0**。

### 非阻塞观察（不影响 M3-02 收口）
1. **`确认 ReportOutline v1` 同轮链式触发初稿生成**：模型在确认 outline 的同一 agent 轮内继续调 prepare_report_draft_context → build_report_draft。账本/闸门/幂等/证据绑定均正确（**非缺陷**）。bot 在确认前已主动引导「确认 ReportOutline v1，即可进入报告初稿生成」，行为与宣示一致，故判为设计行为。根因：report-writing skill 未约束「确认后不自动推进，等待显式初稿请求」。建议（M3-03 或独立小改）：在 skill 或 `confirm_report_outline` 工具返回里加「提纲已确认，等待显式初稿请求后再推进」。
2. **report_output_guard 对 status prose 的 outline 过匹配**（M3-01 同类遗留，见 [[m3-01-report-outline-verified]]）：本轮 v999/DOCX 探针 turn 中，模型在 prose 复述 outline 状态时触发通用「未检测到本轮 ReportOutline 账本状态」拦截。fail-safe（从不放行伪造声明），但阻断状态散文。已知遗留，非新问题，不影响 M3-02 report-body 路径。

### 结论
M3-02 验收口径 1–9 全部 **PASS**；回归全干净（0 SIGBUS/Traceback/IntegrityError/真 HTTP non-200；exactly-once 去重 + 容量闸门 + MCP 审计分离均生效；无凭据/PII/宿主路径泄漏）。
**M3-02 可关闭。** 上述两项为非阻塞观察，建议在 M3-03 或独立小改处理。

---

## M3-03 implementation — ReportDraft confirmation and discrete checkpoints

M3-03 处理 M3-02 活体复核的两项观察，并为 provisional ReportDraft 增加精确版本确认。
本切片不将“确认审阅稿”混同为最终审批，不生成 DOCX/PDF，不交付产物。

### 状态与持久化决策

- 复用已有 `WorkContractStatus.PROVISIONAL -> CONFIRMED` 状态机和
  `confirm_work_contract` 幂等确认路径，**schema 保持 revision 8**；
- `ReportDraft confirmed` 仅表示 requester 确认当前 Markdown 审阅稿版本；
- `approved/published/delivered` 不伪装成 confirmed，留给后续独立审批和交付账本。

### 交付内容

1. **提纲确认与初稿生成拆分**：
   - `confirm_report_outline` 明确返回“本轮只确认提纲”；
   - `prepare_report_draft_context` 和 `build_report_draft` 在服务层校验员工最新消息，
     只有显式包含生成动作和初稿目标才放行；
   - `确认 ReportOutline vN` 单独一轮不会读 chunk、写 Evidence/Claim 或建 Draft。
2. **ReportDraft 精确确认门**：
   - 新增 `confirm_report_draft(expected_version)` 工具和 composition 状态机路径；
   - 服务端 parser 要求最新员工消息精确命名 `ReportDraft vN`，且只包含当前版本；
   - 通用“确认”、错版本、否定、疑问和“请确认”均 fail-closed；
   - 成功后记录 `confirmed_by=requester`，重放同版本幂等。
3. **三合同只读状态守卫统一**：
   - Brief/Outline/Draft 的合法状态散文除工具返回外，也可由服务端发布的
     `current_work` 账本快照证明；
   - 只合并正整数版本与 `provisional/confirmed` 状态，不信任模型输出本身；
   - 伪造 v999、错版本或 provisional/confirmed 错配仍拦截；
   - `confirm_report_draft` 结果纳入 Draft 账本证明，但 Markdown 仍只能来自
     `build_report_draft` / `get_current_report_draft` 的精确标记块。
4. **Pack 与模板**：Pack/Profile 升级为 `1.5.0` / `1.4.0`，
   `report-writing@1.1.0`；example 与 cookiecutter 代码、Skill、Pack/Profile 同步。

### 本切片明确不做

- 不新增 schema revision 9；
- 不新增 approved/published/delivered 状态；
- 不将 Evidence 引用自动等价为 Claim 语义真实；
- 不渲染 DOCX/PDF，不发布、不上传、不交付文件；
- 不修改 MCP policy/confirmation 或 WeCom callback/queue 通道。

### Mac mini 验收口径

1. 静态全绿；MCP 受保护文件相对 `68d7b25` zero-diff；example 与渲染模板一致。
2. schema 仍为 rev8；Pack 1.5.0/Profile 1.4.0/report-writing 1.1.0 新快照注册，
   旧 1.4.0 快照保留，0 immutable conflict。
3. 建立 provisional ReportOutline 后发 `确认 ReportOutline v1`：当轮只确认 Outline，
   Evidence/Claim/Draft 行数不变，bot 明确提示后续显式请求初稿。
4. 后续发 `生成可审阅初稿`：沿 M3-02 证据路径生成 ReportDraft v1 provisional；
   重放幂等，Markdown 与账本精确一致。
5. 发通用 `确认`、错版本、否定、疑问或“请确认 ReportDraft v1”：均不推进 Draft。
6. 发 `确认 ReportDraft v1`：Draft 变为 confirmed，`confirmed_by=requester`；重复确认幂等。
   回复必须明确“不是最终批准，不生成/交付 DOCX/PDF”。
7. 在 confirmed Brief + provisional/confirmed Outline + provisional/confirmed Draft 共存时询问状态：
   与 `current_work` 同版本同状态的只读散文 allowed，不要求本轮必须调用写工具。
8. 诱导声称 `ReportDraft v999 已确认`或将 provisional v1 冒充 confirmed：仍
   `unverified_report_draft_write/blocked`，DB 无 v999，事件只留 digest。
9. 全程 0 approved/artifact/DOCX/PDF/delivery；M3-00A burst exactly-once；0 SIGBUS/Traceback/
   IntegrityError/HTTP non-200；MCP audit/events 分离，无 PII/凭据/宿主路径泄漏。

M3-03 代码和模板实现完成；待 Mac mini 执行 PostgreSQL + WeCom 活体复核后决定是否关闭。
