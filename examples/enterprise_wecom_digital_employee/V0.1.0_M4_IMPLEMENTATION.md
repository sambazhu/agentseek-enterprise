# v0.1.0 M4 Implementation Record

## M4-00 WeCom outbound protocol freeze

M3 ends at an immutable, approved, content-addressed DOCX Artifact. M4 is the
first milestone allowed to expose that file outside the WorkItem runtime. The
transport contract is frozen before publication or delivery tables are added.

### Verified official protocol boundary

The deployment uses the WeCom AI Bot HTTP callback mode:

- [Receive messages](https://developer.work.weixin.qq.com/document/path/100719)
- [Active replies](https://developer.work.weixin.qq.com/document/path/101138)
- [Template cards](https://developer.work.weixin.qq.com/document/path/101032)
- [AI Bot long connection](https://developer.work.weixin.qq.com/document/path/101463)

The official active-reply documentation, last updated 2026-03-24, states that a
callback `response_url` is usable once and expires after one hour. Its documented
message types are `markdown` and `template_card`; `file` is not supported.

The official long-connection mode supports file replies, proactive messages and
chunked temporary-media upload. It uses BotID plus a dedicated Secret and is
mutually exclusive with callback mode for one AI Bot. The current plugin does not
implement that transport, so enabling it in configuration must fail closed.

The group-robot webhook API at
[message push configuration](https://developer.work.weixin.qq.com/document/path/91770)
does support file upload, but it requires a separate group webhook key and sends
only to that configured group. It is not the current AI Bot `response_url` and is
not an acceptable substitute for employee-scoped delivery.

### v0.1.0 decision

Keep the callback transport that already passed identity, memory, MCP, file
intake, queue and exactly-once response testing. Deliver an Artifact as a
`text_notice` template card whose action opens a short-lived signed HTTPS
download link.

The M4 ledger will distinguish:

1. publication of an immutable current Artifact;
2. exactly-once delivery of the template card to the requester;
3. download attempts and successful downloads, which may retry during the
   signed URL lifetime and are not the same as message delivery.

Tokens, response codes, media IDs, plaintext employee IDs and absolute storage
paths must never enter model context or structured events. The future download
endpoint will resolve only an immutable Artifact ID plus a hashed, expiring,
recipient-bound grant.

### Implemented controls

- `agentseek_wecom.outbound` publishes callback and long-connection capability
  records.
- callback supports only `markdown` and `template_card`; requesting `file`
  raises `UnsupportedWeComOutbound`.
- long connection is documented as file-capable but marked unimplemented.
- `WeComResponseUrlSender.send_template_card` emits the official payload shape
  and retains the existing trusted-host and response-code validation.
- an exact, default-disabled template-card probe consumes one callback
  `response_url` without entering the Agent, enabling a real WeCom UI oracle.
- `WeComSettings` accepts only `AGENTSEEK_WECOM_TRANSPORT_MODE=callback`.
- `scripts/probe_wecom_outbound.py` reports the effective capability without
  printing credentials.
- production preflight rejects `direct_file` in callback mode and validates a
  `signed_link` base URL as HTTPS with no credentials, query or fragment.
- example and cookiecutter configuration default Artifact delivery to
  `disabled`; M4-02 must explicitly enable it after the endpoint and ledger are
  present.
- the deterministic DOCX renderer strips Markdown blockquote markers and
  normalizes unordered and ordered list prefixes before writing Word text.

### M4-00 acceptance

1. callback capability reports only Markdown and template card, one-shot and
   3600-second response URL lifetime;
2. callback `file` request fails closed before network I/O;
3. long connection reports file capability but `implemented=false`;
4. template-card sender produces the official envelope;
5. probe default is successful with delivery disabled;
6. callback + `direct_file` probe/preflight fails;
7. `signed_link` requires a clean HTTPS base URL;
8. example and rendered cookiecutter remain synchronized;
9. the default-disabled live trigger delivers one visible `text_notice` card,
   emits a terminal probe event and never enters the Agent;
10. protected MCP files remain unchanged.

## Mac mini 活体复核 — 8b629ca（2026-07-20）

**裁定：PASS。M4-00 可正式关闭，下一步进入 M4-01 publication ledger。**

HEAD `8b629ca`（含 `08b1bbb` 出站能力/preflight/协议探针/DOCX 标记规范化 + `8b629ca` 默认关闭的企微模板卡片活体探针），
GitHub origin + GitLab company-gitlab 双端一致。本轮仅冻结协议并验证模板卡片，未发布、未交付真实 Artifact、未启用 signed_link。

### 静态与受保护文件

- Work 96、WeCom **73**（+outbound/response_url/channel 用例）、Enterprise 86、Example **194 passed + 1 skipped**
  （+prod_check/report_artifact 用例）、Files+LangChain 73、template render 2；typecheck-wecom / ty / docs-test 全绿。
- `mcp_policy.py` 与 `tools.py` 相对基线 `68d7b25` **zero-diff**。

### 能力矩阵探针（`scripts/probe_wecom_outbound.py`）

- **默认（callback + disabled）**：`reply_message_types=[markdown, template_card]`、`response_url_one_shot=true`、
  `response_url_ttl_seconds=3600`、`direct_file_delivery=false`、`implemented=true`，exit 0，`configuration_ready=false`，
  decision “Artifact delivery is disabled until M4 publication and delivery ledgers exist.”
- **callback + direct_file**：exit 1，`WeCom transport 'callback' does not support outbound message type 'file'`。
- **long_connection + direct_file**：exit 1，`implemented=false`，“documented but not implemented”。
- **callback + signed_link + 干净 HTTPS**：exit 0，`configuration_ready=true`，base URL 经 `validate_artifact_download_base_url` 通过。
- 额外核验：signed_link + 非 HTTPS（`http://`）→ exit 1；signed_link + URL 内含凭据（`user:pass@`）→ exit 1（HTTPS-only、无凭据/query/fragment 强制生效）。

### 生产 preflight（`scripts/prod_check.py`）

从仓库根目录运行 `prod_check.py --env-file examples/enterprise_wecom_digital_employee/.env`（与 `run_gateway.sh` 的 cwd 一致，
`.env` 的 `./models/...` 为仓库根相对路径）：**Production preflight passed.**（“WeCom AI Bot callback transport selected”
+ “outbound Artifact delivery is disabled”）。`.env` 默认即 `AGENTSEEK_WECOM_TRANSPORT_MODE=callback` /
`AGENTSEEK_WORK_ARTIFACT_DELIVERY_MODE=disabled` / 探针 trigger 为空，未改动。

### 企微模板卡片活体探针（默认关闭，临时启用）

临时在 `.env` 设 `AGENTSEEK_WECOM_RESPONSE_URL_TEMPLATE_CARD_PROBE_TRIGGER=M4-00模板卡片探针`，经 `run_gateway.sh`
重启网关，测试员工精确发送 `M4-00模板卡片探针`：

1. callback 首包：员工收到“模板卡片探针已启动，请等待第二条消息。” ✓
2. 随后**恰好一张** `text_notice` 模板卡片，标题“AgentSeek M4-00 出站协议探针” ✓（员工实机确认）
3. 卡片经 response_url 发送成功（`_run_response_url_template_card_probe` 走 success 分支，事件 `wecom_response_url_template_card_probe=succeeded`
   发往已配置 sink Langfuse；无 error 分支、0 probe-handler error）
4. **不进入 Agent**：0 `langchain_model_call`；DB delta 全 0（work_items 16 / contracts 57 / sources 72 / artifacts 1 /
   work_events 16，与基线逐项一致）—— 0 WorkItem/合同/Source/Artifact 变更
5. **脱敏**：probe 相关日志行 0 处出现 response_url 明文/OA/姓名/access_token/app_secret/凭据
6. **0 重复卡片 / 0 callback 重传风暴**：probe msgid 仅 1 次 callback、员工实机仅见 1 张卡片
7. 探针命中于 `_handle_text` 精确匹配、在 `_dispatch_user_message` 之前短路（transport-level，不依赖 owning_org/identity scope）

完成后已立即清空该环境变量（`.env` grep 0）并经 `run_gateway.sh` 重启网关（PID 13855，startup complete）。

### M3 Artifact 回归

- **schema 仍 rev9**（DB `max(version)=9`、代码 `LATEST_SCHEMA_VERSION=9`，无 rev10）；M4-00 未触碰 work schema。
- **Pack 仍标准 1.7.0**（smoke override 已 restore，未提交）；PackSnapshot 保留 11 条（含 M3-04B smoke 与历史标准快照）。
- 现有 M3-04B DOCX Artifact 未受影响：`artifact_ffc3a9b0166d02e…`、source_contract_version=1、
  **content_sha256=`sha256:4a91a203…`（不变）**、`not_published` / `not_delivered`、0 publication、0 delivery。
- **DOCX 渲染器修复（08b1bbb，对应 M3-04B 观察 1）经确定性 probe 证实**：对含 `>` blockquote、`-`/`*`/`+` 无序、`1)`/`2.`
  有序的 Markdown 重新渲染，document.xml 中 **0 处字面 `>`/`&gt;`**；`-/*/+` → `•` 项目符号；`1)`/`2.` 规范为 `1.`/`2.`
  有序文本；blockquote 正文保留。（注：M3-04B 既有 Artifact 由修复前代码渲染，仍含历史 `>`；新渲染产物已清洁。）

### 基础回归与健康

- 探针回合本身已验证 inbound + response_url 路径正常（0 model_call、0 风暴、0 DB 变更）；
  identity / memory / 工作状态 / MCP / burst-queue 沿用 M3-04B（M4-00 未改这些子系统）。
- 健康：0 SIGBUS / 0 Traceback / 0 IntegrityError·UniqueViolation / 0 真 HTTP non-200 / 0 PackSnapshot conflict /
  0 queue·event-loop freeze / 0 probe-handler error；MCP 审计（enterprise-events.jsonl 仅 `department_knowledge_*`）
  与 work 账本事件（`work_cancelled` + `artifact_registered`）分离；probe 事件无 PII/凭据/宿主路径泄漏。

### 非阻塞观察

1. **prod_check cwd 依赖**：`.env` 的 `./models/...` 为仓库根相对路径，`prod_check.py` 须从仓库根运行
   （与 `run_gateway.sh` 一致）；从 example 目录运行会在 bge-m3 模型路径上报 FAIL。属部署说明，非代码缺陷。
2. **probe 事件 sink**：`wecom_response_url_template_card_probe` 经 `emit_enterprise_event` 发往 Langfuse；
   `enterprise-events.jsonl` 仅沉淀 MCP 审计（`department_knowledge_*`），与 M3-04B 一致。员工实机所见卡片 + 0 error
   + success 分支为权威证据。

### 结论

M4-00 协议冻结全部验收口径达成：callback 仅 markdown/template_card、one_shot/TTL 3600、direct_file=false；
long_connection documented but unimplemented；direct_file 在 callback 下 fail-closed；signed_link 需干净 HTTPS；
preflight 拒绝 direct_file；模板卡片活体探针默认关闭、精确触发后只发一张 text_notice 卡片、不进入 Agent、0 账本变更、
脱敏、无风暴；schema 仍 rev9、Pack 1.7.0、M3 Artifact 与 digest 不变；渲染器 `>`/列表规范化修复经 probe 证实。
**M4-00 正式关闭**，下一步进入 **M4-01 publication ledger**（发布账本；交付 ledger、signed_link 下载端点仍随后）。

## M4-01 ReportPublication ledger

M4-01 introduces publication as a durable business fact while leaving every
outbound delivery capability disabled. The checkpoint is:

```text
current confirmed ReportDraft
  + current approved ReportApproval
  + current immutable DOCX ReportArtifact
  + exact requester action: 发布 ReportArtifact vN
  -> immutable ReportPublication
  -> WorkItem.status=published
  -> delivery=not_delivered
```

Publication does not create a template card, signed URL, download grant, PDF or
direct-file message. Those remain M4-02 work.

### Schema revision 10

`enterprise_work_publications` stores one immutable publication record per
Artifact:

- `publication_id` and monotonically increasing `publication_version`;
- tenant/work/Artifact ownership binding;
- Artifact content SHA-256, Draft version, Approval contract version and
  trusted template digest;
- policy ID, authenticated publisher and timezone-aware publication time;
- metadata containing `delivery_status=not_delivered` and PackSnapshot ID.

The database enforces unique `(work_id, publication_version)` and
`(work_id, artifact_id)` bindings. Revision 10 also expands PostgreSQL WorkItem
and WorkEvent status constraints to admit `published`; existing rows and schema
history are retained.

### Atomic repository behavior

`put_publication_record` validates the registered Artifact binding and writes
the Publication, WorkItem transition and `publication_registered` WorkEvent in
one transaction. The first publication advances `draft -> published`. An exact
replay returns the existing publication without incrementing the publication or
WorkItem version and without adding an event. A later current Artifact may
create the next publication version while the WorkItem remains published.

### Server-side publication gate

`publish_report_artifact` accepts only the complete exact action
`发布 ReportArtifact vN`. Before touching the ledger it revalidates:

1. the exact current confirmed Draft version;
2. the current approved Approval and its Draft digest binding;
3. exactly one current DOCX Artifact with matching Draft, Approval and template
   digests;
4. the physical content-addressed file and its SHA-256;
5. the authenticated enterprise actor as the WorkItem requester.

Generic confirmation, request forms such as `请发布`, wrong versions,
negations, questions, stale approvals, missing files and tampered bytes all fail
closed before publication.

### Read model and output guard

The current-work summary exposes Artifact publication state and every
ReportPublication with a computed `current` flag. Any upstream Draft, Outline or
Brief revision makes an older Publication `current=false` without deleting its
history.

`get_current_work_status` and `get_current_report_publications` are the only
read paths for publication prose. The output guard treats publication and
delivery as separate claims:

- an unbacked current publication claim is replaced with
  `unverified_report_publication`;
- a stale publication may be described only as stale;
- any claim that a file/card/link was delivered remains blocked by the Artifact
  delivery guard;
- guard events retain only output digests, never the blocked prose.

### Pack and template

The example and cookiecutter Pack/Profile/report-writing versions advance to
`1.8.0` / `1.7.0` / `1.4.0`. The immutable Skill requires a later exact
publication request, a ledger tool call, and an immediate stop with an explicit
`not_delivered` statement.

### M4-01 acceptance

1. schema revision 10 installs idempotently and preserves revision 9 data;
2. the new publication table, constraints and PostgreSQL published-status
   checks are present;
3. no publication is possible before the exact current Approval and Artifact
   gates pass;
4. exact publication creates publication v1, advances the WorkItem and records
   one event atomically;
5. exact replay returns v1 with no new row, event or WorkItem version;
6. upstream revision makes the old publication non-current without deleting it;
7. forged current/stale publication claims fail closed and remain digest-only;
8. `artifact_ids` remain unchanged and delivery mode remains disabled;
9. no template card, signed link, download endpoint, file send, PDF, delivery
   row or delivery event is created;
10. example/template, static tests, MCP protected files and M3/M3-00A behavior
    remain unchanged.

## Mac mini 活体复核 — de550c7（2026-07-20）

**裁定：BLOCKED（阻断项 1）。M4-01 暂不可关闭——活体发布路径被 message-extraction/parser 不一致阻断；
账本机制本身经静态单测与代码审查确认正确，修复范围窄（仅 live 消息解析）。**

HEAD `de550c7`（GitHub origin + GitLab company-gitlab 双端一致）。schema rev9→rev10、publications 表、状态机、
账本逻辑均正确；但**活体精确 `发布 ReportArtifact vN` 经 WeCom 不可达**。

### 阻断项 1（BLOCKER）：活体发布被 envelope + fullmatch 阻断

- 现象：员工经 WeCom 多次精确发送 `发布 ReportArtifact v2`（含手输/复制黏贴/带。），**全部**被
  `服务器校验未通过，未精确匹配` 拒绝；WorkItem 始终 draft、0 publication。Live B/C/E 因此无法活体验证。
- 根因（经临时 instrumentation 捕获 `latest_user_message` 真实 codepoints 证实）：`_latest_user_message_text(runtime)`
  返回的并非纯用户文本，而是 **WeCom 渠道元数据信封 + 末尾真实文本**：
  `from_userid=...|userid=...|oa_account=...|msgtype=text|wecom={...JSON incl. content...}|channel=$wecom|chat_id=...\n---Date: ...---\n<真实文本>`。
  真实命令 `发布 ReportArtifact v2` 在最末尾。
- `explicitly_requests_report_publication` 使用 `_PUBLISH_REQUEST_RE.fullmatch(message)`（整串精确匹配），
  信封前缀使整串不匹配 → 拒绝。**这与所有其它门不一致**：render/artifact/approve/brief/outline/draft 解析器
  均用 `.search()` + 空白归一化，容忍信封。
- 复现（确定性 probe）：`publish(enveloped)=False`（活体阻断）、`artifact/render(enveloped)=True`（可用，解释
  为何 M3-04B render 活体通过而 publish 失败）。修复方向 probe：剥离信封（取末行）后 `publish(stripped)=True`。
- 修复建议（任选其一，二选一即可，范围窄）：
  1. `explicitly_requests_report_publication` 改为容忍信封——先剥离渠道信封/取真实文本（如 `message.rsplit("\n",1)[-1]`
     或与其它解析器一致的 `" ".join(message.split())` + `.search()`），保持精确版本绑定；
  2. 或修正 `_latest_user_message_text` 让其返回干净用户文本（不含渠道信封），使所有门一致受益。
  推荐方案 1（局部、低风险，与现有解析器风格对齐）。

### 已通过（机制层正确）

- 静态：Work **103**、Example **207 passed+1 skipped**、Enterprise 86、WeCom 73、Files+LangChain 73、render 2、
  typecheck/ty/ruff/docs 全绿；**含 publication 单测全绿**（test_report_publication / test_repository publication /
  test_work_composition / test_report_output_guard）；mcp_policy.py+tools.py 相对 68d7b25 zero-diff。
- schema rev9→rev10：`apply_migrations` 正式迁移；`enterprise_work_publications` 14 列齐全（publication_id PK、
  publication_version、work_id/tenant_id、artifact_id FK RESTRICT、content_sha256、source/approval_contract_version、
  template_digest、policy_id、status、published_by、published_at、metadata），约束 `uq_work_publications_version`、
  `uq_work_publications_artifact`、`ck_work_publication_version`、`ck_work_publication_contract_versions`、
  `ck_work_publication_status`，索引 tenant_work/content；**PostgreSQL `ck_work_items_status` / `ck_work_events_from/to_status`
  CHECK 已含 `published`**（rev10 迁移 drop+re-add）。旧数据 0 删除（items 16/contracts 57/artifacts 1/events 16 迁移前后一致）。
- Pack 1.8.0 / Profile 1.7.0 / report-writing 1.4.0（SHA 7683cc89… 与 pack.yaml 一致）；smoke override
  `1.8.0-mac-smoke.1`+`信息技术部` 仅本机临时、已 `git restore`，未提交。
- 焦点工作 `work_ed04971ebf644513aaaf27aecc207d5c` 经正式工具链产生 **current Artifact v2** `artifact_79e1f9395a28a…`
  （docx，40007B，content `sha256:cccd10a8…`，绑定 draft v2 / approval v2 / outline v2 / brief v3；approval current=true、
  artifact current=true、publication_status=not_published）。
- **Live A 负向发布门（活体）PASS**：6 例（`发布`/`请发布…`/`是否…`/`不要…`/`v999`/`确认`）+ 解析器确定性 probe 全部
  0 publication、WorkItem 保持 draft、0 publication_registered、0 投递。
- **Phase 12 无交付 PASS**：`delivery_status=not_delivered`、`ARTIFACT_DELIVERY_MODE=disabled`、0 template_card/file/
  signed_link/download 投递、0 delivery 事件、publication 工具未消费 response_url、M4-00 探针未触发。
- **Live D 守卫（确定性 probe）**：伪造“ReportArtifact v999 已正式发布”→ BLOCKED；伪造“DOCX 已发送给员工并附带下载链接”
  → BLOCKED（unverified_report_artifact/publish）。
- 健康：0 SIGBUS/Traceback/IntegrityError/真 HTTP non-200/PackSnapshot conflict/freeze/publication-delivery error；
  MCP audit 与 work events 分离；debug instrumentation 已移除（0 残留）。

### 非阻塞观察（NON-BLOCKING）

1. **守卫 publication claim 正则偏窄**：`_REPORT_PUBLICATION_CLAIM_RE` 要求 `已/已经…发布` 前缀；迂回措辞
   “历史旧版 ReportArtifact 仍是当前正式发布版本”（无 `已/已经`）未被拦截（确定性 probe 显示该例未 BLOCKED）。
   常见“已正式发布/已发布并交付”已拦截。建议扩正则以覆盖“…当前…发布版本/…发布版本…当前”类 current-claim。
2. （沿用 M4-00）`prod_check` 须从仓库根运行；probe/guard 事件发往 Langfuse，jsonl 仅 MCP 审计。

### 结论与后续

- **M4-01 不可关闭**：阻断项 1（活体 publish 不可达）未解决前，Live B/C/E 无法活体通过。账本机制（schema rev10、
  publications 表/约束、状态机、publish 逻辑、幂等、stale current=false）经静态单测与代码审查确认正确——
  修复仅限 live 消息解析（envelope 容忍），不需动 schema/账本/状态机。
- **未进入 M4-02**：M4-02 signed-link delivery 须在 M4-01（含活体 publish）通过后方可启动。
- 建议 Codex 修复 `explicitly_requests_report_publication`（或 `_latest_user_message_text`）后，我在 Mac mini
  仅需重跑 Live B/C/D/E（A、Phase 12、健康已过）即可关闭 M4-01。
### M4-01 WeCom publication-command correction

The first Mac mini live pass verified the revision-10 schema, publication
ledger, state transition, idempotency and guard design, but found that the
exact publication action was unreachable from WeCom. The runtime HumanMessage
contains a channel metadata envelope and dated separator before the employee's
literal text, while the publication parser intentionally uses `fullmatch`.

The parser now removes only a recognized WeCom channel envelope ending at its
standard `---Date:...---` separator, then applies the unchanged exact-action
and exact-version `fullmatch` to the remaining employee text. Messages without
that recognized envelope are never stripped. Wrong versions, extra delivery
instructions and user-authored date separators remain fail-closed.

The publication output guard also rejects a contradictory claim that a
historical or stale Artifact is still the current formal publication version.
Legitimate `current=false` history remains reportable. Focused publication and
guard tests pass (`58 passed`), as do the complete example suite
(`212 passed, 1 skipped`), Work suite (`103 passed`), touched ruff/ty checks and
the protected MCP zero-diff check. Mac mini live B/C/D/E remains the authority
for closing M4-01.

## Mac mini 活体复核 — 0491846（2026-07-20，修复后复测）

**裁定：PASS。首轮 f85e586 的阻断已关闭——envelope 解析修复使活体 publish 可达；Live B/C/D/E 全部通过。
M4-01 可正式关闭，允许进入 M4-02 signed-link delivery。**

HEAD `0491846`（提交链 de550c7 → f85e586 BLOCKED 文档 → 0491846 fix(work): accept exact publication from wecom envelope），
GitHub origin + GitLab company-gitlab 双端 ls-remote 确认 `049184650f9e`。本轮仅修两处（envelope 剥离 + 守卫
old-current 拦截），未动 schema rev10 / 账本 / 状态机 / Artifact / Approval / MCP 受保护文件 / 交付机制。

### 静态与受保护文件

- Work **103**、Example **212 passed+1 skipped**（+envelope/guard 新增用例）、Enterprise 86、WeCom 73、
  Files+LangChain 73、render 2；ty（须带 `PYTHONPATH=…/src`，裸跑会误报 41 个 import 诊断）/ ruff / docs-test 全绿。
- `mcp_policy.py` + `tools.py` 相对 `68d7b25` **zero-diff**。

### 修复边界核验（代码审查 + 确定性探针）

- `report_publication._publication_command_text`：仅在存在 `---Date:…---` 标记 **且** `channel=$?wecom`
  出现在该标记之前时，剥离最后一个标记之后的内容；否则原样返回。剥离后**仍用严格 `_PUBLISH_REQUEST_RE.fullmatch`**。
- 确定性 envelope 探针（8 例全 PASS）：标准 envelope+`发布 ReportArtifact v2`→True；v3/v999/`并交付`/`请发布`→False；
  **无 `channel=$wecom` 的伪造 `---Date:…---` → False（不剥离，整串 fullmatch 失败，无法绕过）**；纯文本→True。
  安全边界未因修复放宽。

### 部署与状态

- `run_gateway.sh` 启动（PYTHONPATH）；schema **rev10**（DB max=10、代码 LATEST=10）；`enterprise_work_publications`
  表与约束齐全；旧 PackSnapshot 保留；0 immutable conflict；gateway 干净启动。
- 焦点工作 `work_ed04971ebf644513aaaf27aecc207d5c` 复用首轮已验证 current Artifact v2 `artifact_79e1f9395a28a…`
  （docx，40007B，content `sha256:cccd10a8…`，绑定 draft v2 / approval v2 / outline v2 / brief v3，current=true，
  not_published）；smoke override `1.8.0-mac-smoke.1`+`信息技术部` 仅本机临时、已 `git restore` 未提交。

### Live B — 精确发布（PASS）

- 员工精确发送 `发布 ReportArtifact v2`：**envelope 被正确解析**，`publish_report_artifact` 调用成功，
  **不再出现“服务器校验未通过，未精确匹配”**。
- 新增 `ReportPublication publication_45a7412d9423e9e16aea4817dd2f6f86e16b0980399b5a95f8a3afee0188fea2` v1，
  status=published，current=true。
- 绑定逐项正确：`artifact_id=artifact_79e1f9395a28a…`；**content_sha256=`sha256:cccd10a8…` 与 Artifact 物理文件
  SHA-256 完全一致**；source_contract_version=2 / approval_contract_version=2 / template_digest=`sha256:fb4340bd…` /
  policy_id=industry-report-v1 / **published_by=`hmac-81297b…`==WorkItem.requester** / published_at 带时区；
  metadata.delivery_status=not_delivered、pack_snapshot_id 与 WorkItem 一致。
- WorkItem **draft→published**，version 2→3；**恰好 1 条 `publication_registered` WorkEvent**（actor=requester，
  payload_digest=content_sha256，policy=industry-report-v1）；artifact_ids 不变（仍 2）。
- 0 文件 / 卡片 / 链接 / response_url 交付。

### Live C — 幂等重放（PASS）

再次精确发送 `发布 ReportArtifact v2`：**同一 `publication_45a7412d…`**、publication_version 仍 1、
content_sha256 不变；`enterprise_work_publications` 行数不增长（仍 1）、`publication_registered` event 不增长（仍 1）、
WorkItem.version 不增长（仍 3）；0 IntegrityError/UniqueViolation；0 重复交付。bot 回复“已在上一轮成功发布，无需重复发布”。

### Live D — 状态与守卫（PASS）

- 状态查询（服务端摘要 + bot 回复）：WorkItem **published**、ReportPublication v1 status=published **current=true**、
  绑定 ReportDraft v2、delivery=not_delivered；artifact v1 current=false/not_published（历史）、v2 current=true/published。
- 守卫确定性 probe（基于真实 publication 账本状态）：伪造“ReportArtifact v999 已正式发布”→ BLOCKED；
  **伪造“历史旧版 ReportArtifact v1 仍是当前正式发布版本”→ BLOCKED**（0491846 守卫新增的 STALE∧CURRENT 矛盾行
  检测生效）；伪造“DOCX 已发送给员工并附带下载链接”→ BLOCKED；**合法“历史 v1 已失效、当前非发布版本”→ ALLOWED**
  （无误拦）。blocked 事件只存 output_digest，不落原文。

### Live E — 上游修订导致 publication stale（PASS）

- `PUBLISHED` 不在 `TERMINAL_WORK_STATUSES`（={succeeded,failed,cancelled}），`find_active_work` 视 published 为
  active，故正式 `save_report_brief` 修订覆盖期可执行：Brief v3→superseded、**v4 provisional**（current）。
- 复算 `_ledger_summary`：**publication v1 current=false**、artifact v2 current=false、approval v2 current=false；
  原 publication 行与物理 DOCX 保留不删；WorkItem 仍 `published`（可审计）；0 自动重新发布 / 0 自动交付。
- 再次 `发布 ReportArtifact v2`：0 新 publication、WorkItem.version 不变（活体 fail-closed）；
  确定性 probe `_require_current_report_approval` 直接复算 → 抛出“当前 ReportApproval 已失效或未绑定最新 ReportDraft，
  不能渲染 Artifact。”（publish 路径共用此 stale gate，确认不能绕过重新研究/审批/渲染/发布）。

### 健康、审计与无交付边界

- 0 SIGBUS / 0 Traceback / 0 IntegrityError·UniqueViolation / 0 真 HTTP non-200 / 0 PackSnapshot conflict /
  0 queue·event-loop freeze；MCP 审计（enterprise-events.jsonl 仅 `department_knowledge_*`）与 work 账本事件
  （`artifact_registered` + `publication_registered`）分离；无 OA/姓名/凭据/宿主路径泄漏；artifact_ids 不被重复修改。
- **0 template card / 0 signed URL / 0 download endpoint / 0 direct file send / 0 PDF / 0 delivered 状态 / 0 delivery event**；
  publication 工具未消费 response_url；M4-00 模板卡片探针未触发。Publication 仅代表正式发布账本事实，不代表文件已交付。

### 非阻塞观察

1. **A6/B 类确定性文案由模型改写**：publish 工具结果含标准“发布不等于交付”，bot 回复简写为“已成功发布”——语义保持、
   账本支持（publication.delivery_status=not_delivered）。与 M3-04B/M4-01 既有的 deepseek 转述类别一致。
2. **ty 须带 PYTHONPATH**：`uv run ty check …/src …/tests` 裸跑误报 41 个 import 诊断；须
   `PYTHONPATH=…/src uv run ty check`（本轮规范漏写 PYTHONPATH）。

### 结论

首轮 f85e586 的阻断项 1（活体 publish 被 envelope+fullmatch 阻断）**已关闭**：0491846 的 envelope 剥离修复使
活体 `发布 ReportArtifact vN` 经 WeCom 可达，安全边界未放宽；守卫 old-current 拦截生效。Live B/C/D/E 全部 PASS，
schema rev10 / 账本 / 状态机 / 幂等 / stale / 守卫 / 无交付边界均确认。**M4-01 正式关闭**，**允许进入 M4-02
signed-link delivery**（交付 ledger + 短时签名 HTTPS 下载端点 + 模板卡片交付）。

## M4-02 signed-link delivery implementation

M4-02 adds schema revision 11 and keeps publication, delivery and download as
three separate facts. An exact `交付 ReportArtifact vN 给我` command is
parsed after authenticated WeCom-envelope removal. Arbitrary recipient text is
rejected; the recipient ledger key is always the authenticated requester key.

The delivery tool prepares a `text_notice` card carrying an HTTPS URL of the
form `.../delivery_<digest>#<one-time-token>`. The URL fragment is cleared by
the browser and POSTed to the redemption endpoint, so it does not enter the
HTTP path, query string, access log or referrer. PostgreSQL stores only the
SHA-256 grant digest. Card send success atomically registers the Delivery and
advances `published -> delivered`; failed sends leave no Delivery row.
The volatile card intent deliberately does not persist WeCom `response_url`
credentials. If the gateway exits before card send succeeds, delivery remains
unregistered and the employee must replay the exact delivery command; this is
fail-closed crash recovery, not a durable outbox guarantee.

Redemption row-locks and consumes the grant once, enforces its maximum 3600
second lifetime, resolves only the ledger-bound content-addressed Artifact and
revalidates byte count plus SHA-256 before returning DOCX bytes. Restart-safe
resolver initialization happens during the standard gateway bootstrap when
signed-link mode is enabled. `unverified_report_delivery` blocks unsupported
claims that a file was delivered, sent or downloaded while retaining only an
output digest in observability events.

The first M4-02 product boundary is requester-only bearer delivery. The grant
is recipient-bound in the business ledger and distributed only through that
requester's WeCom response URL. The callback transport does not provide a
browser SSO assertion at download time, so the endpoint does not claim a
second browser-side identity check; adding authenticated corporate download
SSO is a later hardening slice.

## Mac mini 活体复核 — 5a7a122（2026-07-21）

**裁定：BLOCKED（2 项阻断）。M4-02 暂不可关闭——活体员工浏览器下载 + Live C 被“一次性下载后不可重发”阻断；
Live F 交付守卫存在正则/逻辑缺口。交付/下载/解析/端点/健康机制本身已验证通过。**

HEAD `5a7a122`（GitHub origin；GitLab 由本文档 push 补齐），父基线 `dc4767f`。schema rev10→**rev11**，
Pack 1.9.0 / Profile 1.8.0 / report-writing 1.5.0。焦点工作复用 `work_ed04971ebf644513aaaf27aecc207d5c`（经正式工具链
重新流转至 current publication v2 + artifact v3）。`/ai-server` 反向代理（preserve-prefix，用户确认并修复）+
`AGENTSEEK_WORK_ARTIFACT_DELIVERY_MODE=signed_link` + `PUBLIC_BASE_URL=https://test.wkzq.com.cn:9443/ai-server/artifacts`
+ TTL=3600（Live C 临时降到 180s）。prod_check signed_link PASS。

### 已通过（机制层）

- 静态：Work **106**、Example **225 passed+1 skipped**、Enterprise 86、WeCom **78**、Files+LangChain 73、render 2；
  ty（须带 `PYTHONPATH=…/src`）/ ruff / docs 全绿；mcp_policy.py + tools.py 相对 68d7b25 zero-diff。
- schema rev11：`enterprise_work_deliveries` 17 列（delivery_id PK、delivery_version、work_id/tenant_id/artifact_id/
  publication_id FK RESTRICT、content_sha256、size_bytes、recipient_key、grant_hash、grant_expires_at、grant_consumed_at、
  status、delivered_by、delivered_at、metadata）+ 约束（PK、3 FK RESTRICT、`uq_work_deliveries_grant` grant_hash 唯一、
  `uq_work_deliveries_version`、4 checks: version>0/size>0/expiry>delivered/status）+ 3 索引；PG `ck_work_items_status`
  CHECK 含 `delivered`；旧数据 0 删除。
- **Live A 精确交付（PASS）**：`交付 ReportArtifact v3 给我`（envelope 被安全剥离，复用 M4-01 的
  `authenticated_user_command_text`）→ `deliver_report_artifact` 调用成功，发一张 `text_notice` 模板卡片
  （card_action.url = 签名下载链接）。delivery `delivery_11d82167…` v1，status=delivered，绑定 artifact_0973c21d544(v3) +
  publication_8777019a(v2)，content_sha256 `sha256:99c405414906…` == 文件 SHA，**recipient_key==requester**、
  **grant_hash=sha256 摘要（非 raw token）**、grant_expires_at=delivered_at+TTL、delivered_by==requester、metadata 干净
  （无路径/token/OA）、WorkItem published→delivered（v5→6）、**1 条 delivery_registered event**（digest-only）、0 leak。
- **Live B 下载机制（PASS，curl + 真实 token）**：GET `/ai-server/artifacts/{id}` → 200 no-store HTML（含 token/replaceState JS）；
  POST `/redeem`（真实 token）→ 200 DOCX，**SHA `99c405414906…`==content_sha256**；redeem 后 grant_consumed_at 置位；
  **重放同 token → 410 Gone**；错误 token→404；不存在 delivery→404；path traversal→404；超长 token(>512B)→404。下载端点
  在 gateway 启动即注册（Phase 12：restart 后无 model turn 仍可用 GET 200/POST 404）。
- **Live D 负向命令门（PASS，parser probe）**：v999/无`给我`/给张三/疑问/否定/并发布/确认/下载报告 全 False；伪造
  `---Date:---` 无 `channel=$wecom` 不剥离。安全边界未放宽。
- **Live E stale（PASS）**：正式 `save_report_brief` 修订（Brief v4→v5 provisional）→ approval v3 current=false、
  publications v1/v2 current=false、**delivery v1 current=false**（grant_state=consumed，仍历史保留）；WorkItem 仍
  `delivered` 可审计；0 自动重发/重交付。
- **Live F 部分（v999 已交付 → BLOCKED ✓）**；**Phase 13 健康 PASS**：0 SIGBUS/IntegrityError/真 HTTP non-200/
  PackSnapshot conflict/freeze（日志中 1 Traceback 为迁移前 rev10-vs-rev11 旧崩溃，当前 gateway 正常）；
  delivery_registered event digest-only 无 PII/路径/token；MCP 审计 vs work events 分离。

### 阻断项 1（BLOCKER）：一次性下载被消耗后，模型拒绝重发 → 活体浏览器下载 + Live C 不可达

- 现象：v1 grant 被本次 curl 机制核验消耗（grant_consumed_at 置位）后，员工多次精确重发 `交付 ReportArtifact v3 给我`
  （含显式“请调用 deliver_report_artifact”指令），模型**始终拒绝**，回复“此前已完成交付（grant_state=consumed），
  无法重复发放。如需重新获取文件，请创建一个新的报告流程”——调用 `get_current_report_deliveries` 后自行拒绝，
  **不调用 deliver_report_artifact**。
- 关键：**composition 允许重发**——`prepare_report_delivery` 的 `existing` 仅匹配 `grant_is_active`（未消耗且未过期）的
  delivery；grant 被消耗后 `existing=None` → 会创建新 delivery（新 grant + 新卡）。SKILL L15 也指示“对当前 published
  Artifact 调用 deliver_report_artifact”，**未指示“consumed 即拒绝”**。故这是 **deepseek 过度谨慎 + `get_current_report_deliveries`
  的 grant_state=consumed 误导**，非代码能力限制。
- 影响：员工一次下载后若丢失文件，**无法经模型重发同一版本的下载卡片**（被要求“创建新报告流程”）；这与用户期望的
  “允许多次下载”冲突，且阻塞 Live B 活体浏览器点击 + Live C（幂等/过期重发）+ Phase 12（访问有效卡片链接）的活体验证。
- 建议（给 Codex，二选一或组合）：
  1. **SKILL/agent.py 明确指示**：员工再次精确 `交付 ReportArtifact vN 给我` 时**总是调用 `deliver_report_artifact`**，
     由服务端决定（活跃 grant → 幂等返回既有；消耗/过期 → 生成新的一次性 grant + 新卡）。模型不应预查 `get_current_report_deliveries`
     并因 `consumed` 拒绝。一次性下载安全仍保留（每 grant 仍 redeem 一次即 consumed）。
  2. **`get_current_report_deliveries` 输出去掉/弱化 `grant_state=consumed` 的“终态”暗示**，避免模型误判为“不可重发”。
  3. 产品语义明确：**“允许多次下载”=允许多次一次性 grant**（每次新 grant 仍 one-time），**不是复用已消耗 grant**——安全不降级。
- 注：本阻断不阻碍“下载机制”结论（curl 已全链路验证 GET no-store + POST redeem→DOCX + hash + 重放410 + fail-closed）；
  `/ai-server` 反代（preserve-prefix）用户已修复——仅缺一张未消耗的新卡片供员工实机点击。

### 阻断项 2（BLOCKER）：Live F 交付守卫正则/逻辑缺口（5 例仅 1 例被拦）

- 现象（确定性 probe，真实 delivery 账本状态）：“ReportArtifact v999 已交付”→BLOCKED ✓；但“报告已发送给张三”/
  “报告已经下载完成”/“历史 stale Artifact 仍是当前交付版本”/“未发布 Artifact 已经完成交付”**4 例均 ALLOWED**（未拦）。
- 根因（`_claims_unverified_report_delivery` + `_REPORT_DELIVERY_CLAIM_RE`）：
  1. **无版本号的交付声明**（如“已发送给张三”“已经下载完成”）走 `if not claims: return not any(True in states.values())`
     分支——只要账本有**任一** current delivery 即放行，**不校验接收人/声明细节**（张三≠requester 也不拦）。
  2. **缺 M4-01 风格的“current 交付版本”claim 正则 + STALE∧CURRENT 矛盾行检测**（0491846 已为 publication 守卫加了，
     delivery 守卫未同步）→“仍是当前交付版本” evade。
  3. “已经完成交付”（“完成”介于“已经”与“交付”之间）不匹配 `(已|已经)(交付|发送|下载)`。
- 影响：伪造“已交付/已发送/已下载/仍是当前交付版本/未发布已交付”可绕过守卫，与 spec Live F 期望不符。
- 建议（给 Codex）：参照 0491846 对 publication 守卫的修法，对 delivery 守卫：
  1. 新增 `_REPORT_DELIVERY_CURRENT_RE`（current 交付版本类 claim）+ 逐行 STALE∧CURRENT 矛盾检测 → 拦“仍是当前交付版本”；
  2. 无版本号交付声明时，**校验接收人**（声明中的接收人 vs 账本 recipient_key），不匹配即拦；
  3. 扩 `_REPORT_DELIVERY_CLAIM_RE` 覆盖“已经完成交付/已经下载完成/已发送给<人>”等措辞。

### 非阻塞观察

1. **bot 回复含内部卡片 marker**：交付回合 outbound 含 `[[agentseek-wecom-template-card:…]]` marker（卡片经 response_url
   发送的内部占位）；员工实机收到的是 text_notice 卡片（marker 是内部流表示）。建议确认 marker 不泄漏到员工可见文本。
2. **ty 须带 PYTHONPATH**（沿用 M4-01）：裸跑 `uv run ty check` 误报 41 个 import 诊断。
3. **GitLab 缺 5a7a122**：Codex 本轮代码只推 GitHub；GitLab 由本文档 push 补齐（48e379f 模式）。建议 Codex 每提交双端推送。

### 结论

M4-02 交付链路代码本身（schema rev11 + delivery 账本 + 卡片 + 签名授权 + 下载端点机制 + stale）已验证通过；
但**两项阻断未解决前不可关闭**：(1) 一次性下载消耗后模型拒绝重发（阻塞活体浏览器下载 + Live C + Phase 12 活体路径），
(2) Live F 交付守卫 5 例仅 1 例被拦。建议 Codex 修复上述两点后，我在 Mac mini 仅需重跑 Live B（员工点击新卡片下载）+
Live C（幂等/过期重发）+ Live F（5 例全拦）即可关闭 M4-02。下载机制（curl 全链路）+ Live A/D/E + Phase 12/13 已过。

## M4-02 blocker fixes after Mac mini verification

The Pack/Profile/report-writing versions advance to `1.9.1` / `1.8.1` /
`1.5.1` so the immutable Skill snapshot records the corrected replay contract.
Every exact `交付 ReportArtifact vN 给我` request now instructs the model to call
`deliver_report_artifact` without treating `consumed` or `expired` as terminal.
The service remains authoritative: an active grant is idempotent and sends no
second card, while a consumed or expired grant creates a new Delivery version,
new one-time token and new card. Multiple downloads therefore mean multiple
one-time grants; no consumed token is ever revived or reused.

`unverified_report_delivery` now applies line-isolated current/stale semantics
matching the publication guard. It also rejects unsupported named recipients,
claims that a browser completed a download, and contradictory claims that an
unpublished Artifact was delivered. The requester-only ledger can support
generic self/requester delivery prose, but it cannot support delivery to a
named third party. Legitimate historical `current=false` and
`grant_state=consumed` summaries remain readable.

Focused regressions cover active-grant idempotency, consumed-grant reissue with
a distinct token and Delivery version, all five Live F false claims, and the
legal historical-delivery control case. Example and cookiecutter Pack, Skill,
agent instructions, tool output and guard logic remain synchronized.

## Mac mini 活体复核 — 69dca85（2026-07-21，修复后复测）

**裁定：PASS。两项阻断已修复，M4-02 可正式关闭。下一切片建议 M4-03 浏览器侧下载 SSO 认证。**

HEAD `69dca85`（GitHub origin + GitLab company-gitlab 双端 ls-remote 确认 69dca850f702）。父基线 5a7a122（M4-02）+
7b0aa90（首轮 BLOCKED 文档）。本轮修复 consumed/expired 重发 + delivery 守卫 5 例；未动 schema rev11 / 下载端点 /
publication·artifact·approval 状态机 / WeCom response_url·card 协议 / MCP 受保护文件。Pack 1.9.1 / Profile 1.8.1 /
report-writing 1.5.1。

### 静态与受保护文件

- Work 106、Example **231 passed+1 skipped**（+delivery 守卫/consumed 重发用例）、Enterprise 86、WeCom 78、
  Files+LangChain 73、render 2；ty（须带 `PYTHONSEEK=…/src`）/ ruff / docs 全绿；mcp_policy.py + tools.py zero-diff。
- schema 仍 **rev11**；enterprise_work_deliveries 结构不变；旧数据 0 删除（7 条 delivery 历史全保留）。
- prod_check signed_link PASS（含 grant TTL bounded）。

### 阻断 1 修复验证（consumed/expired/active 三态 + 活体浏览器下载）—— PASS

焦点工作 `work_ed04971ebf644513aaaf27aecc207d5c` 经正式工具链重新流转至 current publication v3（publication_705b076db7e9…）
+ current DOCX Artifact v4（artifact_c379a597fbd65…，content `sha256:4dc36f777…`，size 39740B）。

- **Live B 活体浏览器下载（PASS）**：`交付 ReportArtifact v4 给我`（envelope 经 `authenticated_user_command_text` 剥离）→
  **模型直接调用 deliver_report_artifact**（不再先 get_current_report_deliveries 后拒绝）→ delivery v2（grant sha256:81ee…）+
  text_notice 卡片。员工实机点击卡片：GET `/ai-server/artifacts/{id}`→200 no-store 页（`/ai-server` preserve-prefix 反代生效）→
  JS 读 fragment POST `/redeem`→**200 DOCX**，grant_consumed_at 置位；下载文件 SHA `4dc36f777…`==content_sha256；
  **重放同链接→410 Gone**；marker `[[agentseek-wecom-template-card:…]]` 仅 channel 消费，**员工可见文本无 marker**。
- **Live B consumed 重发（PASS，修复核心）**：v2 grant 被下载消耗后，再次 `交付 ReportArtifact v4 给我`→**模型再次调用
  deliver**（不拒绝）→ delivery v3（新 grant sha256:188…≠81ee）+ 新卡；旧 v2 保留 consumed 历史。复测 v3→v4（同样 consumed→新 grant+卡）。
- **Live C1 active 幂等（PASS）**：v5 grant 仍 active 时再次 `交付 ReportArtifact v4 给我`→模型调用 deliver→服务端判 active→
  **返回同一 delivery_v5，不重发卡、不新增 delivery_registered、WorkItem.version 不增**；bot 回复“delivery_v5 仍在有效期内，
  不重复发送卡片”。即“模型总是调用工具、服务端负责幂等”，非模型自行拒绝。
- **Live C2 过期重发（PASS）**：TTL 临时降到 15s，v5（3600s TTL）自然过期后再次 `交付`→**模型调用 deliver→v6（新 grant c4d，15s TTL）**；
  v6 过期后点击→**410 Gone**；再次 `交付`→**v7（新 grant 514）**；点击 v7→**200 DOCX**（consumed 16:29:54）。
  consumed/active/expired 三态均由模型调用 deliver、服务端按 grant_is_active 决定幂等或新发。

### 阻断 2 修复验证（delivery 守卫 5 例 + 员工张三）—— PASS

确定性 probe（真实 delivery 账本，v7 current=true/consumed）：
- “ReportArtifact v999 已交付”→BLOCKED ✓
- “报告已发送给张三”→BLOCKED ✓（recipient≠requester；新增 `_REPORT_DELIVERY_RECIPIENT_RE`+`_claims_delivery_to_other_recipient`）
- “报告已经下载完成”→BLOCKED ✓（新增 `_REPORT_DOWNLOAD_COMPLETION_RE`）
- “历史 stale Artifact 仍是当前交付版本”→BLOCKED ✓（新增 `_REPORT_DELIVERY_CURRENT_RE`+逐行 STALE∧CURRENT 矛盾检测，对齐 0491846 publication 守卫修法）
- “未发布 Artifact 已经完成交付”→BLOCKED ✓（新增 `_REPORT_UNPUBLISHED_DELIVERY_RE`）
- “报告已发送给员工张三”→BLOCKED ✓（“员工”前缀不绕过 recipient 校验）
- 对照：current=true requester delivery“已交付”→ALLOWED ✓；历史 delivery current=false/consumed→ALLOWED ✓（不误拦）。

### 健康、脱敏与回归

- 0 SIGBUS / 0 IntegrityError·UniqueViolation / 0 真 HTTP non-200 / 0 PackSnapshot conflict / 0 freeze；
  日志中 1 Traceback 为 M4-02 首轮迁移前（PID 44267，rev10-vs-rev11）旧崩溃，当前 gateway 正常。
- 0 direct_file / 0 PDF / 0 publish·deliver 合并；下载端点启动即注册（restart 后无 model turn GET 404/POST 可用）；
  delivery_registered event digest-only（无 PII/token/路径/OA）；MCP audit vs events 分离；MCP protected zero-diff。
- response_url/card exactly-once（每次交付 1 张卡，幂等不重发）。

### 非阻塞观察（给 Codex 后续）

1. **render 后 bot 下一步指引用错版本**：`生成 ReportDraft v4 DOCX` 后 bot 回复的下一步指引写“发布 ReportArtifact **v1**”
   （stale 旧版），误导员工发了 v1（被 guard 正确拦截）。工具结果返回的是正确 v4，属 deepseek 对指引文案的版本幻觉
   （同 M3-04A/M4-01 “确定性文案依赖模型”类）。建议：render/publish 工具返回的下一步指引由服务端确定性地拼接当前版本。
2. **卡片副标题“下载授权为一次性且有时限”**：当前每 grant 确为 one-time（redeem 即 consumed），但 69dca85 已支持
   consumed/过期后重发新 grant——即“可多次下载”已实现（每次新 one-time grant）。卡片文案“一次性”可能误导员工以为不可重下载。
   建议：卡片文案改为“下载授权为一次性，可重新请求获取新下载卡片”之类，与服务端能力一致。
3. **ty 须带 PYTHONPATH**（沿用）；**GitLab 本轮已有 69dca85**（Codex 双端推送已修复）。

### 结论

两项阻断均修复并活体通过：consumed/active/expired 三态模型均调用 deliver（不拒绝）、服务端按 grant_is_active 决定幂等或
新 grant+卡；delivery 守卫 5 例 + 员工张三全 BLOCKED、对照 ALLOWED。schema 仍 rev11、账本/状态机/下载端点/publication·artifact·approval
未回归、MCP zero-diff、健康清洁、0 direct_file/PDF、marker 不泄漏。**M4-02 正式关闭**。
下一切片建议 **M4-03 浏览器侧下载 SSO 认证**（当前 callback 模式无浏览器 SSO，下载端点无二次身份校验——M4-02 文档已标注的后续硬化项）。

## Post-M4-02 deterministic guidance patch

After Mac mini closed M4-02, two non-blocking user-facing ambiguities were
removed before starting M4-03. `render_report_docx_artifact` now returns the
ledger-derived exact `发布 ReportArtifact vN` command, and
`publish_report_artifact` returns the same-version exact self-delivery command.
Both results require verbatim relay so the model cannot substitute a stale
Artifact version from memory. The delivery card now states that this card's
grant is one-time and time-limited while clearly explaining that the employee
may request a new one-time card for another download.

Pack/Profile/report-writing advance to `1.9.2` / `1.8.2` / `1.5.2` to keep the
Skill snapshot immutable. Focused tests call both tools and assert the current
version in their next-step text, reject a stale v1 suggestion, and verify the
card's one-time-plus-reissue wording. Example and cookiecutter remain mirrored.

M4-03 has an explicit identity-provider decision gate. This repository has no
existing OIDC or SAML integration. The configured WeCom Corp ID/App Secret are
currently used for server-side API access and identity resolution; their
presence alone does not establish that a WeCom web-OAuth application and
callback domain are authorized. Implementation must wait for a real corporate
OIDC/SAML or WeCom web-authorization choice and configuration. A second bearer
cookie is not accepted as fake SSO.

## Mac mini 活体复核 — 73d2016（2026-07-21，确定性文案补丁）

**裁定：PASS（两项小修正确并验证）。M4-02 保持关闭；v0.1.0 功能范围冻结，进入 RC 可靠性收口。
另发现 1 项 pre-existing latent bug（render 幂等重渲染冲突，非 73d2016 回归），建议 Codex 后续修复。**

HEAD `73d2016`（GitHub origin + GitLab company-gitlab 双端 ls-remote 确认 73d20168f622）。父基线 5361442（M4-02 PASS）。
本轮仅修两项确定性文案：render/publish 下一步指引绑定当前账本版本 + 卡片文案说清“一次性 grant + 可重新申请”。
未动 schema rev11 / 下载端点 / publication·artifact·approval 状态机 / WeCom response_url·card 协议 / MCP 受保护文件。
**产品决策：M4-03 浏览器 SSO/OIDC/SAML 暂缓；当前一次性 signed-link 是 v0.1.0 正式交付方式；后续有明确企业 IdP 条件时再作安全加固。**

### 静态与受保护文件

- 聚焦测试 37 passed（test_report_delivery/pack_loader/work_composition）；Example **233 passed+1 skipped**；
  template render 2；ty（须带 `PYTHONPATH=…/src`）/ ruff / docs 全绿；mcp_policy.py + tools.py zero-diff。
- schema 仍 **rev11**；enterprise_work_deliveries 结构不变；8 条 delivery 历史全保留。
- Pack 1.9.2 / Profile 1.8.2 / report-writing 1.5.2（SHA `312e8190…` 与 pack.yaml 一致）；新 PackSnapshot 注册，旧 1.9.1/1.9.0 保留，0 conflict。

### 两项小修验证

1. **render/publish 下一步指引绑定当前版本（PASS，代码确认 + 单测）**：
   - `render_report_docx_artifact` 工具返回新增 `f"如需发布，请精确回复'发布 ReportArtifact v{artifact.source_contract_version}'。"`
     + `"请原样转达本工具的完整结果，不得改写版本号。"`（work_tools.py:639，服务端确定性地用当前 Artifact 版本，不依赖模型）。
   - `publish_report_artifact` 工具返回新增 `f"如需交付，请精确回复'交付 ReportArtifact v{publication.source_contract_version} 给我'。"`（:693）。
   - 单测覆盖：`test_report_delivery.py:70 assert "交付 ReportArtifact v4 给我" in published`（publish 指引）✓。
   - render 指引代码确认（:639 用 `artifact.source_contract_version`，与 publish 同模式）✓。
2. **卡片文案说清“一次性 + 可重新申请”（PASS，代码 + 单测 + 活体确认）**：
   - `_delivery_card_description(expected_version)` = "本卡下载授权为一次性且有时限；如需再次下载，请重新回复'交付 ReportArtifact vN 给我'获取新卡片。"
     （work_tools.py `main_title.desc` 改用该函数，替换旧"下载授权为一次性且有时限，请及时下载。"）。
   - 单测：`test_report_delivery.py:80 assert "本卡下载授权为一次性" in description` ✓。
   - **活体确认**：`交付 ReportArtifact v4 给我`→新 delivery v8（新 grant a42）+ 新 text_notice 卡；员工实机看到卡片描述
     “本卡下载授权为一次性且有时限；如需再次下载，请重新回复'交付 ReportArtifact v4 给我'。”（新文案，非旧"请及时下载"）✓；
     点击下载成功（DOCX 下载，回归未退化）✓。

### 活体指引版本验证（Live A render / Live B publish）—— 受 pre-existing 阻塞

- **Live A render 指引活体受阻**：`生成 ReportDraft v4 DOCX`（幂等重渲染）→ 报错 "artifact record artifact_c379a597fbd65… already exists with different values"
  （详见下"pre-existing latent bug"）。render 指引文案未触达（composition 在工具返回前抛 WorkConflictError）。**render 指引本身代码正确（:639 确定性用当前版本）**，仅活体路径被 latent bug 阻塞。
- **Live B publish 指引活体受阻**：`发布 ReportArtifact v4`（幂等重发）→ 模型调用 `get_current_report_publications`（展示既有 publication_v3）而非
  `publish_report_artifact`→指引文案未返回（bot 回复"已经发布…publication_v3…status=published"）。**publish 指引本身代码正确 + 单测覆盖（:70）**，
  仅模型在幂等重发时选择只读工具（deepseek 可靠性，非 73d2016）。
- 故 Live A/B 的活体指引版本未直接验证；render 指引代码确认、publish 指引代码确认 + 单测覆盖。

### pre-existing latent bug（非 73d2016 回归，建议 Codex 后续修复）

**render 幂等重渲染冲突 + 未捕获 WorkConflictError → traceback**：
- `render_report_artifact` 无 skip-if-exists（不像 publish 的 `if existing is not None: saved=existing`、delivery 的 `already_delivered`），总是重新渲染 + 调 `put_artifact_record`；
  `ArtifactRecord` 是 `@dataclass(frozen=True)` 默认 eq 含**全部字段含 created_at**，重渲染新记录 created_at≠既有→`existing != artifact`→`_raise_artifact_record_conflict`（WorkConflictError）。
- `render_report_artifact` 工具 `except (…, WorkCompositionError)` **未捕获 WorkConflictError**（不同异常类）→ 传播到 `bub.framework.process_inbound`→
  "Error processing inbound message" + Traceback（活体确认：2 条 traceback 均为 "WorkConflictError: artifact record … already exists with different values"）。
- 该 latent bug 此前未暴露（M3-04B/M4-02 轮模型幂等重发时用 `get_current_report_artifacts` 而非 render）。本轮模型首次调用 render 幂等重发→暴露。
- 影响：员工重复发 `生成 ReportDraft vN DOCX`（幂等重渲染）会失败 + traceback。
- 建议 Codex：① `render_report_artifact` 加 skip-if-exists（artifact_id 已存在且 content/digest 一致→返回既有，不重新渲染 + put）；
  ② 或 `put_artifact_record` 比较时排除 `created_at`（与 publish/delivery 一致地处理幂等）；③ render 工具的 except 增加 WorkConflictError（避免 traceback 传播）。

### 健康、负向与回归

- 0 SIGBUS / 0 IntegrityError·UniqueViolation / 0 真 HTTP non-200 / 0 PackSnapshot conflict / 0 freeze；
  3 Traceback = 1 旧（M4-02 首轮迁移前 init 崩溃）+ 2 render-conflict WorkConflictError（pre-existing latent，上节）。
- 0 direct_file / 0 PDF；schema 仍 rev11；8 delivery 历史保留；MCP protected zero-diff；download 回归（v8 点击→200 DOCX，重放→410）✓。
- 错版本 render/publish/deliver 仍拒绝（M4-02 轮 parser + 守卫已验证，本轮单测覆盖）；不自动 publish/deliver。

### 结论

两项确定性文案小修正确并验证（render/publish 指引代码确认 + publish 单测 + 卡片文案代码/单测/活体确认）。
M4-02 保持关闭。**产品决策：v0.1.0 功能范围冻结**（一次性 signed-link 交付；M4-03 浏览器 SSO 暂缓，待明确企业 IdP 条件），
**进入 RC 可靠性收口**。pre-existing latent render 幂等冲突（非 73d2016）建议 Codex 后续修复（render skip-if-exists + 排除 created_at + 捕获 WorkConflictError）。

## Post-M4-02 Artifact replay reliability fix

The RC reliability patch closes the latent render replay failure found during
the `73d2016` Mac mini verification without changing schema revision 11,
Artifact identity, publication/delivery state machines, or the download
protocol.

- `render_report_artifact` still renders deterministic bytes and computes the
  content-addressed Artifact ID. Before registration it now returns an existing
  record when all immutable business fields match. `created_at` and contextual
  metadata are deliberately excluded from replay identity; a different content,
  Draft, Approval, template, storage key, or actor remains a hard conflict.
- A concurrent registration conflict is re-read and accepted only when the same
  immutable identity won the race. Every other `WorkConflictError` remains
  fail-closed. The render tool also contains `WorkConflictError` as a final
  framework-isolation boundary so a ledger conflict cannot become an inbound
  processing traceback.
- Exact `生成 ReportDraft vN DOCX` and `发布 ReportArtifact vN` requests are
  documented as action-tool commands. The agent must call the corresponding
  render/publish tool and let the service decide idempotency instead of replacing
  the action with a read-only current-state tool.
- The regression test advances the render clock by five minutes before replay
  and verifies the same Artifact ID/hash/storage key/created_at is returned with
  no extra Artifact row, `artifact_registered` event, or WorkItem version.

Because `report-writing` changed, Pack/Profile/Skill advance immutably to
`1.9.3` / `1.8.3` / `1.5.3`; example and cookiecutter stay mirrored. Browser
SSO remains deferred beyond v0.1.0, and the verified one-time signed-link flow
remains the frozen delivery mechanism for RC.

## Mac mini 活体复核 — e8deb3b（2026-07-21，RC 可靠性修复）

**裁定：PASS。latent render 幂等冲突已修复；render/publish 幂等重放返回既有、0 conflict/traceback。v0.1.0 功能冻结完成，进入 RC 全链路回归。**

HEAD `e8deb3b`（GitHub origin + GitLab company-gitlab 双端 ls-remote 确认 `e8deb3bbd8f2`；GitLab 由本轮验证推送补齐——Codex 仅推 GitHub，已同步）。
父基线 `905e8d0`（73d2016 补丁 PASS）。本轮修复 73d2016 复测发现的 pre-existing latent render 幂等冲突 + WorkConflictError 未捕获 + 模型幂等重发用 get_current。
未动 schema rev11 / 下载端点 / publication·artifact·approval 状态机 / WeCom 协议 / MCP 受保护文件。Pack 1.9.3 / Profile 1.8.3 / report-writing 1.5.3。

### 静态与受保护文件

- Work 106、Example **234 passed+1 skipped**、Enterprise 86、WeCom 78、Files+LangChain 73、render 2；ty/ruff/docs 全绿；mcp_policy.py + tools.py zero-diff。
- schema 仍 **rev11**；enterprise_work_deliveries 结构不变；8 条 delivery 历史全保留。新 PackSnapshot 注册，旧 1.9.2 及更早保留，0 conflict。

### 修复验证

**Live A — render 幂等重渲染（PASS，latent bug 修复核心）**：
- 焦点工作 `work_ed04971ebf644513aaaf27aecc207d5c` current Artifact v4 `artifact_c379a597fbd65…`（content `sha256:4dc36f777…`，created_at 14:03:39）。
- `生成 ReportDraft v4 DOCX`（幂等重渲染）→ **模型调用 `render_report_docx_artifact`**（bot 回复匹配 render 工具措辞"DOCX 已生成"）→
  `_put_artifact_idempotently` + `_same_artifact_identity`（按 content_sha256 + source_digest + approval_digest + template_digest 比较不可变身份，**忽略 created_at + 非 identity metadata 如 pack_snapshot_id**）→
  **返回既有 Artifact v4**（同 artifact_id/content_sha256/storage_key/created_at）。
- **0 新 Artifact**（仍 4）、**0 新 artifact_registered event**（仍 4）、**WorkItem.version 不增**（仍 15）、**0 WorkConflictError、0 Traceback**。
- 修复前（73d2016 复测）：render 无 skip-if-exists → 总重新渲染 + put → `ArtifactRecord` eq 含 created_at → `existing != artifact` → WorkConflictError → render 工具 except 未捕获 → framework traceback。
  修复后：skip-if-exists（`get_artifact_record(artifact_id)` → `_same_artifact_identity` → 返回既有，不 put）+ WorkConflictError 兜底（并发竞态后重读账本，仅相同不可变身份才作幂等重放）。

**Live B — publish 幂等重发（PASS）**：
- `发布 ReportArtifact v4`（幂等重发）→ 模型调用 `publish_report_artifact`（skip-if-exists 既有 publication_id → 返回既有 publication v3）→
  **0 新 publication**（仍 3）、**0 新 publication_registered event**（仍 3）、**WorkItem.version 不增**（仍 15）、**0 WorkConflictError、0 Traceback**。

### 指引文案 relay（非 e8deb3b 范围，deepseek 转述可靠性）

- Live A bot 回复"ReportDraft v4 DOCX 已生成 ✅"（**未逐字 relay 工具结果中的"发布 ReportArtifact v4"指引**）；Live B bot 回复"已经发布…publication_v3"（**未 relay "交付 ReportArtifact v4 给我"指引**）。
- 工具返回字符串含确定性当前版本指引（73d2016 :639/:693，代码确认 + 单测 :70），但模型 paraphrase 而非逐字转达（deepseek 转述可靠性，与 69dca85 delivery 同类的"模型不总调用动作工具/不逐字 relay"问题）。
- **e8deb3b 的修复目标（render 幂等 + 0 traceback）已达成**；指引 relay 是独立的 deepseek 可靠性问题（非 e8deb3b 回归，也非其范围）。

### 边界与最小回归

- 错版本 render/publish fail-closed：`生成 ReportDraft v999 DOCX`→False、`发布 ReportArtifact v999`→False（parser 确定性 probe）；`生成 ReportDraft v4 DOCX`/`发布 ReportArtifact v4`→True ✓。
- 卡片文案 `_delivery_card_description` 未被 e8deb3b 触及（73d2016 已活体验证"本卡下载授权为一次性且有时限；如需再次下载，请重新回复'交付 ReportArtifact v4 给我'"）✓。
- stale Artifact 不得重渲染/发布/冒充 current（render/publish 要求 current approval/artifact，M4-02 已验证）✓。

### 健康、脱敏与回归

- 0 SIGBUS / 0 Traceback（e8deb3b session）/ 0 IntegrityError·UniqueViolation / 0 真 HTTP non-200 / 0 freeze；
  0 direct_file / 0 PDF / 0 SSO；schema 仍 rev11；MCP audit 与 enterprise events 分离；MCP protected zero-diff；
  无 OA/姓名/凭据/token/grant_hash/storage_key/宿主路径泄漏。

### 结论

e8deb3b 修复验证 PASS：render 幂等重渲染（skip-if-exists + `_same_artifact_identity` 忽略 created_at/非 identity metadata）+
WorkConflictError 兜底（并发竞态重读）+ render 工具 except 捕获 WorkConflictError（0 framework traceback）+ publish 幂等。
latent render 幂等冲突（73d2016 复测发现的 pre-existing bug）已根治。**v0.1.0 功能范围冻结完成**（一次性 signed-link 交付；
M4-03 浏览器 SSO 暂缓待企业 IdP），**进入 RC 全链路回归**。指引文案 relay（模型 paraphrase）是独立 deepseek 可靠性问题，
建议后续在 RC 回归中观察或考虑服务端确定性拼接关键引导（如 nudge 一样下沉到工具返回文本被 guard 放行的通道）。
