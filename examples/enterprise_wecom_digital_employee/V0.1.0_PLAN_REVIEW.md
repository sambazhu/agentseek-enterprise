---
title: v0.1.0 行业报告数字员工规划 — 评审与遗漏
type: explanation
audience: [A2, A3, A4, A5]
runs: no
verified_on: 2026-07-12
sources:
  - examples/enterprise_wecom_digital_employee/V0.1.0_INDUSTRY_REPORT_DIGITAL_EMPLOYEE_PLAN.md
  - examples/enterprise_wecom_digital_employee/ROADMAP.md
  - examples/enterprise_wecom_digital_employee/DEPLOYMENT_NOTES.md
  - contrib/agentseek-files/src/agentseek_files/content_analysis.py
  - contrib/agentseek-enterprise/src/agentseek_enterprise/observability.py
---

# v0.1.0 规划评审

> 本文评审对象是 `V0.1.0_INDUSTRY_REPORT_DIGITAL_EMPLOYEE_PLAN.md`（commit 88bf45a）。
> 评审基于 v0.0.9 GA（`8128aac4`）的全链路复测结论与本会话在 Mac mini 上积累的活体证据。
> 文末保留 Codex 逐项处置表，记录接受、部分接受和对应修正。

## 总体结论：**合理，架构判断高质量**

这份规划把 `enterprise-wecom` 从"对话+工具调用助手"升级为"组织任务完成型数字员工"，
核心判断都对：

- **以 `WorkItem` 而非对话 session 为中心实体** —— 长任务、跨重启、可审计的基础。正确。
- **`WorkEvent` 不可变事件账本，明确"不能只存在于 Langfuse"** —— 业务审计与模型观测分层。正确。
- **新增 `contrib/agentseek-work` 通用插件，报告业务留模板** —— 复用边界清晰；
  "为什么不放进 agent.py"（channel 可移植、异步 worker、迁移、测试）的理由站得住，
  与 v0.0.9 的 files/enterprise/wecom 分层一脉相承。
- **权限不来自记忆、工具不信模型传入的 requester_id**（来自 runtime context + repository 二次校验）。安全正确。
- **Profile 先于 WorkItem 解析、版本固定、无 Profile 不回退为宽权限 Agent** —— 可复现 + 不越权。正确。
- **确定性检查 vs 模型辅助检查分离，模型检查不替代人工评审** —— 报告可靠性的关键。正确。
- **交互 turn 与异步 worker 分离（lease / 幂等 / 单阶段提交 / 从最后成功阶段恢复）** —— 正确的异步模型。
- **Agent 编排用"确定性状态机 + 受约束模型节点 + 策略保护 MCP + 人工评审"，而非多自治 subagent** —— 对企业报告场景是正确取舍。

M0–M6 里程碑按依赖排序、验收 A–D 覆盖（行业报告 / 内部安全 / 跨重启恢复 / 证据冲突）都到位。
这是一份可以拿去冻结的产品+工程合同级文档。问题不在"设计错"，而在
**几项前置依赖被放到"待确认"而非"M0 阻断"**，且 **MVP 范围偏大、若干恢复/成本/egress 工程细节没画死**。

## 突出优点（保持，不要改坏）

1. 报告合同 `ReportBrief` + 启动条件（模型不能自行补全业务条件）。
2. `Source / Evidence / Claim` 三层可追溯 + 来源优先级 + 低优先级不能独立支撑关键结论。
3. 三审批节点绑定版本（合同 / 提纲 / 终稿），"实质变化需重新批准"。
4. `agentseek-work` vs 报告 Playbook 的协议分离（`WorkPlaybook` 协议由模板实现）。
5. State 是组合边界、非业务库；完整数据始终从 PostgreSQL + `agentseek-files` 按授权读。
6. 质量指标含"重复执行率=0""敏感数据泄露数=0""越权阻断率"——把不可妥协项写成了目标。

---

## 关键遗漏与风险（按严重度）

### 🔴 M0 必须先解（否则 v0.1.0 无法定验收）

**G1. 企微"自建应用主动消息"是硬依赖，却被列为"建议/待确认 #11"。**
报告任务的核心 UX（waiting_input/review/approval 通知、完成交付）全靠它。规划自己说
降级查询模式"不是目标体验"。但自建应用要企微管理后台配置、callback URL、可能要公网入口 —— 
这是最容易卡壳的一项。应升为 **M0 阻断项**：先确认能否拉通主动消息通道，再谈 Playbook。
拉不通，v0.1.0 只做"任务可创建+查询+跨轮推进"，不做"自动通知+审批流"。

**G2. 公司 Word 报告模板（待确认 #13）是 DOCX 交付的前置条件。**
规划说"DOCX 是必要交付"，但模板没定 → 要么发无品牌通用 DOCX（对战略发展部不算"交付"），
要么用 python-docx 套公司样式（需模板+章节规范+logo+字体）。必须 M0 拿到，否则 M5 交付做不出
真正可用的报告。同时关联 DOCX 生成选型（见 G14）。

**G3. 来源快照（待确认 #9）应从"待确认"升为"必须交付"。**
报告数字员工的全部价值是可追溯。`SourceRecord` 只有 `uri_digest`/`content_hash`，没有
"已存原文快照"。公告/网页 URL 会失效/改版，半年后无法复核 = Claim 失效。对"行业报告"岗位，
**来源原文快照是必须的**。建议 `SourceRecord` 加 `snapshot_artifact_id`（指向 `agentseek-files`
存的原文副本），受限来源一律快照。

### 🟠 设计阶段要补（写代码前定清楚）

**G4. LangGraph checkpoint 与 WorkItem 状态机的"恢复真相源"未定。**
规划说"checkpoint 保存图执行状态，但不能替代企业任务账本"——那崩溃恢复时听谁的？
两个都可能说"phase 4 跑到一半"。必须明确：**`WorkItem` 状态机是唯一真相源**，
checkpoint 只做 phase 内部的 step 级续跑；恢复时先按 `WorkItem.current_phase` + 最后成功
`WorkEvent` 定位，再让 checkpoint 接续 step。否则"从最后成功阶段恢复"会歧义，且可能与
"已批准版本仍能准确识别"（验收 C）冲突。

**G5. `work_id` 贯穿 Langfuse 依赖一个未修的 v0.0.9 缺口。**
v0.0.9 已确认：runtime trace 顶层 `release=None`（`_LangfuseEmitter` 运行时路径没调
`update_current_trace`，见 `observability.py`）。规划说 work_id 要贯穿 Langfuse —— 但同一
emitter 缺口会让 runtime trace 顶层也拿不到 `work_id`（只进 metadata）。要么 v0.1.0 先修
emitter（运行时路径补 `update_current_trace(release=…, work_id=…)`），要么明确"work_id 在
Langfuse 只在 metadata"。建议合并到一处修，并把这条加进 M0/M1 的 emitter 修复项。

**G6. 模型/Token 成本边界完全没提。**
一个 WorkItem 跑 10 阶段，每阶段可能多次模型调用 + pgvector 召回（v0.0.9 实测单次语义召回
~27–53s）。没有：每阶段/每 WorkItem 的最大调用数、token 上限、超时、成本帽。既影响 worker
lease 大小，也影响"报告卡住烧钱"。至少给每阶段一个 `max_model_calls` + `max_tokens` +
`phase_timeout`，触发即转 `waiting_input` 或 `failed`。

**G7. 单实例 Mac mini 的并发模型未定。**
lease/optimistic version 都是标准件，但没说：几个 worker？Mac mini 单实例是 1 worker 串行？
若某 WorkItem 的 phase 在等 MinerU（v0.0.9 实测可几分钟到 ~15min），其他 queued 任务是否饿死？
需明确单实例下：phase 级让出（等 MinerU 时释放 lease、让其他任务插队）还是任务级串行。直接影响
"任务完成率"指标和用户体验。

**G8. 保密数据外发缺少确定性卡点（enforcement point）。**
规划列了"能否发送到外部大模型/MinerU"分级 + "每次外部调用记录"，但记录是事后。要达到
"敏感数据泄露数=0"这个**已声明指标**，必须在报告 Playbook 的工具层、调外部 provider 之前有一个
**硬 egress 策略检查**（按 file 的 `confidentiality_level` + 目标 provider 白名单），不通过直接拒绝，
不靠模型自觉。这条线没画出来，"零泄露"就是个愿望。

### 🟡 范围风险

**G9. v0.1.0 MVP 偏大，建议拆分发布。**
"必须交付"16 项：WorkItem/WorkEvent/PG schema/状态机/幂等恢复取消/ReportBrief/完整 10 阶段
Playbook/Source-Evidence-Claim/提纲评审+最终批准/跨重启恢复/Markdown+DOCX/outbound/企微查询/
主动通知或降级/work_id 贯穿/管理员运维查询。M1→M6 串行，现实里 3–6 个月。建议拆成两到三个 GA：
- **v0.1.0**：M1+M2+M5 轻量版 —— 任务账本 + ReportBrief + 材料绑定 + 跨重启恢复 + Markdown 交付 +
  企微查询（"可委派、可追踪、可恢复"的最小闭环）。
- **v0.1.1**：M3+M4 —— 研究/证据/Claim/提纲评审/初稿/质量门。
- **v0.1.2**：完整 DOCX + 主动通知 + 最终批准 + 审计闭环。

否则 v0.1.0 GA 太远，期间 v0.0.9 在生产跑着没有任何任务治理层。

### 🟢 细节遗漏（不阻塞，建议补）

**G10. 运维（operator）工具缺位。** 工具列表（create/get/list/cancel/provide_input/approve）
都是委派人视角。运维的"人工接管"动作（force_fail / requeue / force_transition / 查看 WorkEvent）
没进 tools，但"人工接管率"是声明指标。补一组 admin tools。

**G11. 证据冲突（验收 D）没有数据结构。** `Claim.verification_status` 没有 `conflicted` 态 +
冲突记录实体。验收 D 要"不静默选择" → 需要一个 `EvidenceConflict` 或 Claim 上的冲突标记 + 解决记录，
否则验收 D 落不了地。

**G12. "实质变化需重新批准"无阈值。** 每次修订都新版本；什么算"实质变化"（结构改 vs 错别字）？
不定规则 → 要么每版都重新批准（慢），要么都不批（险）。给个判定：如章节/关键 Claim 变化→重批；
措辞→不批。

**G13. v0.0.9 已知项的承接没写明。** 规划说"不重写 v0.0.9 能力"，但几个 v0.0.9 遗留应在 v0.1.0
显式承接/修复：
- MinerU 慢/不稳定 → worker 的 `phase_timeout` 要按 MinerU 变速设计（已知会卡 ~15min）。
- pptx 图形化数值 OCR 不出 → 报告里图表数字会过不了确定性校验，要有"标记为不可校验、要求人工
  提供源数字"的兜底。
- CurrentFiles 异步刷新 → WorkItem 绑定 file_id 后，材料盘点要能"等文件解析完再取证据"
  （worker 主动等，而不是让用户 ask again —— 这正好把 v0.0.9 的 ask-again UX 升级掉，值得在
  规划里点一句）。

**G14. DOCX 生成技术没选型。** python-docx（库、无 shell、好做）vs LibreOffice headless/pandoc
（要装、要 sandbox allowlist）—— 影响 sandbox 设计和公司模板能否套。M5 前要定，且与 G2 联动。

**G15. v0.1.0 自身的回滚没写。** v0.0.9 有 v0.0.8-ga 回滚 tag。v0.1.0 是加表+加插件+改 agent.py，
回滚 = production 退回 v0.0.9-ga（新表留着不用、新插件不注册）—— 建议在 `PRODUCTION_FREEZE.md`
写明 v0.1.0 回滚路径，延续 GA-readiness 纪律。

**G16. 跨重启恢复的"故障注入"测试 harness 没写。** M6 列了"故障恢复演练"但没说怎么做。v0.0.9
没有持久任务、不需要这个；v0.1.0 必须有：worker 跑到 phase 中途被 kill → 重启 → 断言不重复执行、
MCP 写不重复、从正确 phase 恢复。加进测试分层。

**G17. 评审意见的企微录入 UX（待确认 #15）可能阻塞修订流。** 在企微对话里"指到某章节、写一条评审
意见"很别扭。`ReviewComment` 数据结构有了，但录入路径（企微文本 vs Word 批注导入）不定，提纲评审+
修订就做不顺。M0 一起定。与 G1 联动（都依赖企微通道能力）。

---

## 建议（优先级排序）

1. **M0 先冻结**：自建应用主动消息可行性（G1）、公司 Word 模板（G2）、来源快照升级为必须（G3）、
   评审意见录入 UX（G17）。四项不定，后面都悬。
2. **M1 前补设计**：checkpoint vs WorkItem 真相源（G4）、模型/token 边界（G6）、并发模型（G7）、
   egress 硬卡点（G8）、emitter 修 release+work_id（G5）。
3. **考虑拆 v0.1.0 为两到三个 GA**（G9），让"任务账本+可恢复+Markdown 交付"先上线，避免大爆炸。
4. **把 v0.0.9 已知项（MinerU 延迟/图表数值/异步刷新）显式写进 v0.1.0 承接清单**（G13）——
   尤其是"worker 等文件解析完再取证据"，顺手干掉 ask-again UX。
5. 补运维工具（G10）、冲突数据结构（G11）、重批阈值（G12）、DOCX 选型（G14）、回滚路径（G15）、
   chaos harness（G16）。

## 一句话结论

**架构判断对、分层干净、可审计性是真正的设计目标，不是口号。** 主要问题不在"设计错"，而在
**几项前置依赖（自建应用主动消息 / Word 模板 / 来源快照）被放到"待确认"而非"M0 阻断"**，
且 **MVP 范围偏大、若干恢复/成本/egress 的工程细节没画死**。把这五项前置和工程细节在 M0/M1 钉死，
这份规划就能落地。

---

## Codex 逐项处置（2026-07-12）

Codex 已将接受项和修正后的部分接受项落实到
`V0.1.0_INDUSTRY_REPORT_DIGITAL_EMPLOYEE_PLAN.md` 与 `ROADMAP.md`。
部分接受项保留评审目标，但修正不安全、违反许可或不符合当前 SDK 的具体实现。

| ID | 严重度 | 摘要 | 处置 |
| --- | --- | --- | --- |
| G1 | 🔴 M0 | 自建应用主动消息升为 M0 阻断，降级模式范围明确 | 接受：增加 M0 文本/文件消息探针；失败则缩减范围或推迟完整 GA。 |
| G2 | 🔴 M0 | 公司 Word 模板 M0 拿到，否则 DOCX 交付不成立 | 接受：正式模板未就绪时也必须取得战略发展部批准的中性模板。 |
| G3 | 🔴 M0 | 来源原文快照升为必须交付（`SourceRecord.snapshot_artifact_id`） | 部分接受：快照策略必须交付；受许可限制的来源不强制复制全文。 |
| G4 | 🟠 设计 | WorkItem 为恢复真相源，checkpoint 只做 phase 内 step 续跑 | 接受：WorkItem/WorkEvent 是 phase 级唯一真相源。 |
| G5 | 🟠 设计 | emitter 运行时路径补 `update_current_trace(release/work_id)` | 部分接受：work_id 使用 metadata/tags；当前 SDK 无 release/work_id 顶层参数，需按实际签名验证。 |
| G6 | 🟠 设计 | 每阶段/每 WorkItem 模型调用/token/超时上限 | 接受：增加 WorkBudget，超限进入预算扩展批准。 |
| G7 | 🟠 设计 | 单实例并发模型 + phase 级让出（等 MinerU 时不饿死队列） | 接受：默认单 phase worker；外部等待转 waiting_external 并释放 lease。 |
| G8 | 🟠 设计 | 外发前硬 egress 策略检查（不靠模型自觉） | 接受：所有外部 provider、消息和文件交付前确定性检查。 |
| G9 | 🟡 范围 | 拆 v0.1.0 为多个 GA（账本先行） | 部分接受：采用 alpha/beta/RC 分段；GA 仍交付一个窄场景端到端报告闭环。 |
| G10 | 🟢 细节 | 运维 admin tools（force_fail/requeue/force_transition/view_events） | 部分接受：增加有限修复工具；不提供任意 force_transition。 |
| G11 | 🟢 细节 | 证据冲突数据结构（`EvidenceConflict` 或 Claim.conflicted） | 接受：增加 EvidenceConflict 和 Claim conflicted 状态。 |
| G12 | 🟢 细节 | "实质变化需重批"阈值规则 | 接受：范围、章节、Claim、数字、Evidence、结论变化需重批。 |
| G13 | 🟢 细节 | v0.0.9 已知项承接（MinerU 超时/图表数值/异步刷新→worker 等解析） | 接受：worker 主动轮询、Scheme C 失效传播、图表数值 unverified。 |
| G14 | 🟢 细节 | DOCX 生成选型（python-docx vs LibreOffice/pandoc） | 接受：v0.1.0 选择 python-docx；LibreOffice/Pandoc 后置为可选 adapter。 |
| G15 | 🟢 细节 | v0.1.0 回滚路径写进 PRODUCTION_FREEZE | 接受：发布时回滚到 v0.0.9 GA，保留新表并停止新 worker。 |
| G16 | 🟢 细节 | 跨重启故障注入测试 harness | 接受：覆盖 phase/MCP/artifact/lease/checkpoint/outbox/DB/MinerU 边界。 |
| G17 | 🟢 细节 | 评审意见企微录入 UX（与 G1 联动） | 部分接受：MVP 使用企微章节编号结构化意见；Word 批注导入后置。 |

---

## 二次评审（cf4da7c, 2026-07-12）

> Codex 在 cf4da7c 对 17 条全有处置，并新增 `DigitalEmployeePack` 复用模型。
> 本节复核处置是否落地、评估 pack 模型、列出新发现。二次评审基于已核实证据，
> 非凭空质疑。

### A. G1–G17 处置复核（已逐条核实写入规划）

12 条"接受"均**真写进规划**，非仅表格表态：

| ID | 处置 | 核实落点 | 评审 |
| --- | --- | --- | --- |
| G1 | 接受 | M0 增加文本/文件消息探针，失败则缩减范围 | ✅ |
| G2 | 接受 | 模板未就绪时也要取得批准的"中性模板" | ✅ |
| G3 | 部分接受 | `snapshot_artifact_id` + `snapshot_status` 区分许可来源（plan L831/861-874） | ✅ 建议补：受许可来源的 SourceRecord 必填 query+excerpt+timestamp+license_terms（仅"不复制全文"不够） |
| G4 | 接受 | WorkItem/WorkEvent 为 phase 级唯一真相源 | ✅ |
| G5 | 部分接受 | work_id 走 metadata/tags | ⚠️ 见下 G5-R |
| G6 | 接受 | `WorkBudget`（max_model_calls）+ `budget_extension_required`（L718-734） | ✅ |
| G7 | 接受 | 新增 `waiting_external` 状态，外部等待释放 lease（L649/655/665/1711） | ✅ 直击 MinerU 饿死队列 |
| G8 | 接受 | `check_data_egress()` 确定性卡点（L1077） | ✅ |
| G9 | 部分接受 | alpha1/alpha2/beta1/rc1/ga 分段（ROADMAP L527-531） | ✅ 合理妥协（GA 仍端到端，但分段降险） |
| G10 | 部分接受 | 有限修复工具，不提供任意 force_transition | ✅ 安全取舍 |
| G11 | 接受 | `EvidenceConflict` + Claim `conflicted` 态（L901-908） | ✅ |
| G12 | 接受 | 实质变化规则：章节/Claim/数字/Evidence/结论→重批，措辞→不批（L1129-1140） | ✅ |
| G13 | 接受 | worker 主动轮询 file_id，自动续材料盘点，不要求员工再问（L1725） | ✅ 顺手干掉 ask-again UX |
| G14 | 接受 | v0.1.0 用 python-docx；LibreOffice/Pandoc 后置 adapter | ✅ |
| G15 | 接受 | 回滚到 v0.0.9-ga，保留新表、停新 worker（L2268-2274） | ✅ |
| G16 | 接受 | 故障注入 harness 覆盖 phase kill/lease/outbox/MinerU（L2180/2242-2244） | ✅ |
| G17 | 部分接受 | MVP 企微章节编号结构化意见 | ⚠️ 见下 G17-R |

### B. G5 复核（SDK 事实已验证）

Codex 的部分接受**理由成立**：实测装好的 Langfuse SDK `update_current_trace` 参数为
`name/user_id/session_id/version/input/output/metadata/tags/public`，**确无 `release` 参数**；
`work_id` 也不是标准 trace 字段。所以 work_id 走 metadata 正确。

**但**：probe trace 顶层**确实带 `release=enterprise-wecom-v0.0.9-rc1`**（本会话活体见过）——
说明顶层 release 是可达的（probe 走 `Langfuse(release=...)` 构造器默认值）。runtime trace
顶层 `release=None` 的根因是**logfire/OTel 导出路径不继承 client 的 release 默认值**，不是 SDK
完全不支持。建议 Codex 不要把"OTel 路径不传播 release"等同于"SDK 无 release 顶层参数"，
再查一次：`LANGFUSE_RELEASE` 环境变量、OTel resource attribute、或让运行时 trace 也走 client
创建而非纯 OTel 导出。即便最终只能 metadata-only，也把结论写成"OTel 路径不继承 client release
默认值"，更准确，便于日后 SDK 升级时 revisit。**work_id 走 metadata 的结论不变。**

### C. G17 复核

"企微章节编号"在修订时会重排（章节号变），评审意见对不回旧章节。建议用 `ReviewComment.section_id`
（规划已有此字段、来自提纲稳定 ID）而非章节号。MVP 即可做对，不增加成本。

### D. DigitalEmployeePack 模型评审 —— **高质量扩展，建议保留**

新增的 pack 模型不是为 v0.1.0 加功能，而是为"从同一模板派生智能办公数字员工"打底。判断全对：

1. **Pack = 项目内纵向角色包，不是新 Bub 插件**（L213）—— 避免插件膨胀，通用能力留 contrib。
2. **所有权分层清晰**（L271-278）：`agentseek-work` 只存 Profile/pack_id/version/digest，不读 Skill 正文；
   模板解析 manifest 装载当前 Profile 的 Skills/Playbooks；二进制 asset 走 `agentseek-files`/可信目录。
3. **Skill/Playbook/Tool/Policy/WorkItem 边界**（L303-333）：Skill=方法/Playbook=流程/Tool=动作/
   Policy=边界/WorkItem=状态。"Skills 不能替代确定性能力（授权/状态/审批/egress/事务/幂等/隔离/outbox/
   预算/审计恢复）" —— 安全正确。
4. **"Skill 被 Profile 引用不等于获得工具权限"**（L375）—— 权限不靠 Skill 授予，正确。
5. **Shared vs 岗位 Skills 分层**（L377-404）+ "员工上传文档不能变成 Skill" —— 信任边界对。
6. **渐进式读取 + `references/`**（L406-425）+ 显式提醒"文件工具默认读取行数有限，需足够 limit"
   —— 直击 v0.0.9 截断问题，承接到位。
7. **版本固定**（L427-459）：WorkItem 创建时固定 `pack_id/pack_version/skill_set_version/skill_digests/
   profile_version/playbook_version/runtime_release`；在跑任务不自动切 Skill；"只保存 digest 不足以复现，
   必须保留不可变 tag/内容快照/可取回 artifact" —— 与 G3 来源快照同原则，正确。
8. **二进制模板不是 Skill**（L461-479）：DOCX 模板走 `asset_refs` + 受控存储，python-docx 读 artifact ——
   顺带解决了 G2/G14 的模板存放位置。
9. **DirectTurn vs WorkItem**（L481-512）+ "是否建 WorkItem 由 runtime 规则和 tool contract 决定，
   不完全交给模型" —— 正确（不是每条消息都该变持久任务）。
10. **v0.1.0 装载范围固定**（L514-547）"一项目一 Bot 一 Profile 一 pack 一组 skill"；未来多员工单 gateway
    需 scoped SkillResolver + agent registry，"不能靠提示词隔离" —— 正确。

### E. 二次评审新发现（pack 模型 + 处置细节）

**P1. Pack manifest 格式与 loader 未定义。** 规划给了 pack 目录结构（`profile.yaml/skills/playbooks/
assets/policies/evals/`），但 manifest schema（模板如何发现+装载一个 pack、谁解析 `profile.yaml`、
skill_refs 如何解析成实际 Skill 路径）没定。M1 前需定一个最小 manifest schema + loader。

**P2. Pack 版本"可取回"机制未定。** L458 说"必须保留不可变 tag、内容快照或可取回 artifact，只保存 digest
不足以复现"——原则对，但怎么存/取没说。pack 源在项目 repo（`skills/` 目录）时可用 git tag/commit 取回；
若 pack 是生成项目时复制进来的，版本就模糊。建议明确：`pack_version` = pack 来源的 git commit/tag，
或 pack 内容作为 versioned artifact 存进 `agentseek-files` 受控目录。否则历史报告无法复现 Skill 正文。

**P3. DirectTurn vs WorkItem 的判定规则本身没给。** L511 说"由 runtime 规则和 tool contract 决定"，
但规则是什么没写。需一条具体规则：如"该轮会产生/修改 artifact、或需审批、或跨系统写 → WorkItem；
只读/单轮/无 artifact → DirectTurn"。否则落地时还是模型自由裁量。

**P4. smart-office pack 的归属未明。** 规划把 `digital_employees/industry-report/` 和
`digital_employees/smart-office/` 放在同一树，又说 v0.1.0"一项目一 pack"、智能办公是"后续独立项目"。
需说明：smart-office pack 是 **cookiecutter 模板里的示例**（生成新项目时可选），还是**独立 repo**。
否则两 pack 同树但不同项目会造成混淆。

**P5. pack 的 `evals/` 未描述。** pack 含 `evals/` 但结构/用法没写。GA-readiness 纪律下，pack 级 eval
（回归集、验收场景）应定义 —— 至少 industry-report pack 的 eval 要在 M6 前定型。

**P6. pack 测试未加进测试分层。** 原测试分层（work unit/hook/playbook/composition/live）没含
"pack 测试"（manifest 装载、skill_refs 解析、版本固定、二进制 asset 不进 Skill 的断言）。补一层。

**P7. `runtime_release` 固定在 WorkItem（L450）与 G5 顶层 release 缺失的交叉点没说明。**
WorkItem 固定 `runtime_release` 很好（DB 可查、可复现），但 Langfuse runtime trace 顶层 release=None
（G5）。建议在规划里点一句：`runtime_release` 以 WorkItem 为权威源；Langfuse 仅作辅助观测，缺失不阻断。
避免日后审计时"Langfuse 看不到 release"被误判为版本未固定。

### F. 二次结论

- **G1–G17 处置质量高**：12 接受项已核实真落地，5 部分接受理由均合理（G3 许可限制、G5 SDK 事实已验证、
  G9 分段、G10 安全、G17 可改进）。仅 G5 建议把结论措辞改准、G17 建议改用 section_id。
- **DigitalEmployeePack 是高质量架构扩展**，非范围膨胀（v0.1.0 只 ship industry-report pack，
  框架最小实现 + 版本固定 + 装载范围）。建议保留。
- **7 条新发现（P1–P7）**全是"落地前要画死"的工程细节，非设计错误。P1/P2/P3 建议在 M0/M1 定，
  P4/P5/P6/P7 可稍后。

### Codex 请回应（round 2）

| ID | 摘要 | 处置 |
| --- | --- | --- |
| G5-R | "OTel 路径不继承 client release 默认值"措辞改准；再查 LANGFUSE_RELEASE env / OTel resource 是否能补顶层 release | ☐ |
| G17-R | 评审意见企微录入改用 `ReviewComment.section_id`（稳定）而非章节编号 | ☐ |
| G3-R | 受许可来源的 SourceRecord 必填 query+excerpt+timestamp+license_terms | ☐ |
| P1 | pack manifest schema + loader 定义 | ☐ |
| P2 | pack 版本"可取回"机制（git tag/commit 或 versioned artifact） | ☐ |
| P3 | DirectTurn vs WorkItem 的具体判定规则 | ☐ |
| P4 | smart-office pack 归属（cookiecutter 示例 vs 独立 repo） | ☐ |
| P5 | pack `evals/` 结构与 industry-report pack eval 定型 | ☐ |
| P6 | pack 测试分层补入（manifest/skill_refs/版本固定/asset 隔离） | ☐ |
| P7 | `runtime_release` 权威源 + Langfuse 辅助的说明 | ☐ |

