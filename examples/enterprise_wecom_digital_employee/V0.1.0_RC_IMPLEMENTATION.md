# v0.1.0 RC reliability closeout

## Frozen product scope

v0.1.0 RC adds no product capability. The delivery contract remains the M4-02
callback-mode `text_notice` card with a short-lived, recipient-bound, one-time
signed-link grant. Browser OIDC/SAML or WeCom web authorization is deferred
until a real enterprise identity provider and HTTPS callback domain are chosen.
PDF and direct-file delivery remain disabled.

The frozen schema is revision 11. The current Pack/Profile/report-writing
versions remain `1.9.3` / `1.8.3` / `1.5.3`.

## Deterministic employee guidance

The render and publication tools already return exact ledger-derived next-step
commands. The output guard now provides the final deterministic boundary when a
model paraphrases or omits them:

- a successful `render_report_docx_artifact` turn appends the exact current
  `发布 ReportArtifact vN` command when absent;
- a successful `publish_report_artifact` turn appends the exact current
  `交付 ReportArtifact vN 给我` command when absent;
- an already relayed exact command is not duplicated;
- the version is parsed only from the successful tool result in the latest
  human turn, never from model prose or conversation memory.

This behavior is mirrored in the enterprise-wecom cookiecutter template and
does not weaken Artifact, Publication, or Delivery ledger guards.

## Automated RC gates

The following tests form the RC reliability gate:

1. The report lifecycle integration test creates the WorkItem and ReportBrief,
   performs internal research and gap resolution, confirms the Outline and
   Draft, approves content, renders, publishes, delivers, redeems once, rejects
   replay with 410 semantics, and makes Approval/Artifact/Publication/Delivery
   stale after an upstream revision.
2. Artifact render and publication replays return the existing immutable
   records without adding rows, events, or WorkItem versions.
3. A deterministic two-thread race forces one Artifact registration conflict;
   both callers receive the same immutable identity and no duplicate survives.
4. A cold process composition boot registers the download resolver before any
   model turn. Clearing the composition cache and rebuilding against the same
   database repeats registration without PackSnapshot conflict.
5. A clean database migrates from no schema to revision 11 and contains the
   delivery ledger. The latest schema constant is asserted as 11.
6. Artifact, publication, and delivery events are digest-only. The lifecycle
   assertion rejects grant tokens, storage keys, employee names, and OA account
   material in those event records.
7. Output-guard tests cover deterministic nudge insertion, no duplication,
   ledger-backed allowance, fake versions, stale-current contradictions, and
   unverified delivery claims.

## Mac mini RC live gate

Automated tests do not replace the final quasi-production pass. Mac mini must
run one fresh end-to-end WeCom lifecycle, confirm visible nudge text, click and
hash-check the DOCX, observe a 410 replay, revise an upstream contract, verify
all downstream records become non-current, restart the gateway, and repeat a
download endpoint probe before the first model turn. Logs and ledgers must show
zero unhandled traceback, duplicate side effect, plaintext identity, credential,
grant token, storage key, or host path.

## Mac mini 活体复核 — 097a9ef（2026-07-21，RC reliability gate）

**裁定：PASS。nudge 兜底（B/C/D）经单测验证；幂等/下载/stale/健康回归通过；v0.1.0 功能冻结完成，进入 RC。**

HEAD `097a9ef`（GitHub origin + GitLab company-gitlab 双端 ls-remote 确认 `097a9ef3af91`；GitLab 由本轮验证推送补齐——Codex 仅推 GitHub，已同步）。父基线 `0ae64e7`（e8deb3b RC 修复 PASS）。
本轮新增：guard `_append_deterministic_nudges`（render/publish 成功 + 模型 paraphrase → 服务端确定性拼接"发布/交付 ReportArtifact vN"指引；已含则不重复）+ e2e/integration 测试（test_report_draft +93、test_report_output_guard +95、test_work_composition +37）。未动 schema rev11 / 状态机 / 下载端点 / MCP 受保护文件。Pack 1.9.3 / Profile 1.8.3 / report-writing 1.5.3。

### 静态与受保护文件

- Work 106、Example **239 passed+1 skipped**、Enterprise 86、WeCom 78、Files+LangChain 73、render 2；ty/ruff/docs 全绿；mcp_policy.py + tools.py zero-diff。
- schema 仍 **rev11**；enterprise_work_deliveries 结构不变；8 条 delivery 历史全保留。

### nudge 兜底验证（B/C/D — 单测覆盖）

- **B render nudge**：`test_render_action_appends_ledger_version_nudge_when_model_paraphrases`（test_report_output_guard:945）——
  render 成功 + 模型 paraphrase "DOCX 已生成" → guard 追加 `如需发布，请精确回复"发布 ReportArtifact v4"`。✓
- **C publish nudge**：`test_publish_action_appends_ledger_version_nudge_when_model_paraphrases`（:1065）——
  publish 成功 + paraphrase → 追加 `如需交付，请精确回复"交付 ReportArtifact v4 给我"`。✓
- **D 不重复**：`test_render_action_does_not_duplicate_existing_exact_nudge`（:979）——输出已含指引则不追加。✓
- guard `_append_deterministic_nudges`（report_output_guard.py）：
  `_successful_artifact_render_version_in_turn`（检测 render_report_docx_artifact 工具成功）→
  `if f"发布 ReportArtifact v{version}" not in output: append`；
  `_successful_artifact_publication_version_in_turn` → `if f"交付 ReportArtifact v{version} 给我" not in output: append`。
  与 `_append_outline_confirmation_nudge` 同模式（服务端确定性，不依赖模型 relay）。

### 活体 nudge 验证——受 deepseek 模型可靠性阻塞（非 097a9ef 回归）

- `生成 ReportDraft v4 DOCX`（幂等重渲染）→ **模型未调用 render_report_docx_artifact**（0 次出现，含显式"请调用 render"指令），
  bot 回复 "ReportDraft v4 DOCX 已生成 ✅"（从对话记忆回复，非工具结果）。guard 运行（report_output_guard 事件）但 nudge 不触发
  （`_successful_artifact_render_version_in_turn` 返回 None——无 render 工具成功）。**publish 同理**（模型用 get_current 展示既有）。
- 这是 deepseek 在幂等重发时不调用动作工具的已知可靠性类别（M4-02/73d2016/e8deb3b 轮反复出现）。
  **nudge 机制本身正确**（单测 B/C/D 覆盖精确场景：render 成功 + paraphrase → nudge 追加）；活体验证需模型调用 render/publish，
  在幂等场景模型不调用。活体 full lifecycle（新 Artifact → render → nudge）约 12+ 消息且受模型可靠性影响，本轮未执行；
  建议后续在模型配合时或经确定性 e2e 测试补验活体 nudge。

### 回归验证（E/F/G/H）

- **E 下载**：下载端点启动即注册（restart 后无 model turn GET 200 no-store + POST 错误 token→404 + 不存在→404）✓；
  410 重放（M4-02 已活体验证）✓。
- **F 幂等/并发**：artifacts 4→4（skip-if-exists 返回既有，0 新）；e8deb3b 活体验证（0 conflict/traceback）；
  并发 render 不产生 duplicate（test_work_composition +37 单测覆盖 + `_put_artifact_idempotently` 兜底 WorkConflictError 重读）✓。
- **G stale**：M4-02 已活体验证（Brief 修订→approval/artifact/publication/delivery 全 current=false，旧版本不能重发）✓。
- **H 健康/脱敏**：0 SIGBUS / 0 Traceback（097a9ef session）/ 0 IntegrityError·UniqueViolation / 0 真 HTTP non-200 / 0 freeze；
  0 direct_file / 0 PDF / 0 SSO；MCP audit 与 enterprise events 分离；MCP protected zero-diff；无 token/grant_hash/storage_key/OA/姓名/宿主路径泄漏 ✓。

### 结论

097a9ef RC nudge 兜底验证 PASS：guard `_append_deterministic_nudges` 在 render/publish 成功 + 模型 paraphrase 时服务端确定性拼接
"发布/交付 ReportArtifact vN"指引（B/C/D 单测覆盖，含不重复）。幂等/下载/stale/健康回归通过（e8deb3b/M4-02 + 本轮确定性检查）。
**v0.1.0 功能范围冻结完成**（一次性 signed-link 交付；M4-03 浏览器 SSO 暂缓），**进入 RC 全链路回归**。
活体 nudge/full-lifecycle 受 deepseek 幂等不调用动作工具阻塞（非 097a9ef 回归）；nudge 机制单测验证正确，建议后续模型配合时补活体验证。

## Mac mini 活体复核 — 097a9ef full-lifecycle 补验（2026-07-22，glm-5.2）

**裁定：PASS。活体 full lifecycle 走通，nudge 机制活体生效（guard 实际运行 + D case 活体）；DOCX sha 匹配；下载 grant 一次性消费；健康/脱敏回归通过。**

切模型 `AGENTSEEK_MODEL=glm-5.2`（deepseek-v4-flash 幂等不调动作工具，glm-5.2 在 fresh flow 配合）。本轮发现框架设计约束并验证修订路径正确，最终在既有 work 上重走全链路触发 fresh render → nudge。

### 框架设计约束确认（M1-06，非回归）

`create_industry_report_work` 受 `ActiveWorkConflictError` 阻塞（`work_tools.py:92`）。根因：`_find_active_work`（`repository.py:1806`）用 `status.not_in(_terminal_status_values())` 判活，`TERMINAL_WORK_STATUSES={succeeded,failed,cancelled}`（`models.py:97`）——**`delivered` 不算终态**，故 delivered 的 work 持续占用 `(tenant,requester,digital_employee,playbook)` scope，阻止新建同类 work。而 `state_machine.py:38` `WorkStatus.DELIVERED: frozenset()`——delivered 无出边，不可 cancel/回退。

→ **by-design**（M1-06 方案 B，commit `7c64d2f`，doc `adb8350`）：一个员工一个 playbook 一个活跃报告；delivered 持续锁 scope 是为了让修订走同一 work（追加新 Brief 版本），而非新建 work。render（`work_composition.py:785`）不检查 work status，只看当前已确认 draft + 当前 approval，故 delivered work 上重走可达 fresh render。

### 活体 full lifecycle（既有 work_ed04971 上追加版本，状态机 delivered 不变）

work_ed04971（tenant wkzq，zhuchunlin/信息技术部），原 delivered v15（artifact v4 sha `4dc36f777…`，publication v3，8 deliveries）。本轮：

| 阶段 | 契约版本 | work_version | 关键证据 |
|---|---|---|---|
| ReportBrief | v6（量化交易新方向）confirmed | 15 | v5 superseded |
| internal research + gap | gap-decision v5 confirmed | - | 15 sources / 11 evidence，1 gap 保留继续 |
| ReportOutline | v5 confirmed | - | v4 superseded |
| ReportDraft | v5 confirmed | - | 新 markdown（首行"# 证券行业量化交易发展研究报告"） |
| ReportApproval | v5 confirmed（approved） | - | v4 superseded |
| **render** | **artifact v5** | **16** | 全新 content_sha256 `sha256:05d955bf4bb83e210156d6798438df0bcca79a4db14a63d7fd286f9fa5b51499`（≠ v4），40772 bytes，绑定 brief v6/outline v5，`artifact_registered` 事件 |
| publish | publication v4 published | 17 | 绑定 artifact v5，`publication_registered` 事件 |
| deliver | delivery v9 delivered | 18 | grant_hash `sha256:898410c…`，active（未消费），TTL 3600s 至 01:36，`delivery_registered` 事件 |
| download | - | - | GET no-store HTML 200 + POST redeem 200 + **grant_consumed_at=00:36:56** |

### nudge 机制活体生效（关键）

- **render turn guard 实际运行**：日志 `00:27:30.790 report_output_guard`（render 成功 outbound 00:27:30.772 之后）。guard 检测 `_successful_artifact_render_version_in_turn`（render_report_docx_artifact 成功，v5）→ 检查输出是否含 `发布 ReportArtifact v5` → bot 回复已含"如需发布，请显式回复：**发布 ReportArtifact v5**"（模型 relay 了 render 工具结果，版本号正确）→ 不重复追加（**D case 活体**）。
- publish 回复含"如需交付，请显式回复：**交付 ReportArtifact v5 给我**"（交付指引，版本正确 v5）。
- **B case（模型省略→guard 追加）未触发**：glm-5.2 两轮（render/publish）都 relay 了命令，故 guard 走"已含则不重复"分支。B case 由单测 `test_render_action_appends_ledger_version_nudge_when_model_paraphrases`（:945）覆盖。活体确认了 guard 在真实 model turn 中**实际运行 + 正确判活 + 不重复**，nudge 机制非死代码。

### 下载 + sha 匹配 + 410 重放

- delivery v9 `content_sha256` = `sha256:05d955bf4bb83e210156d67…` == artifact v5 `content_sha256`（**MATCH**，redeem 的 DOCX 即 artifact 内容）。
- 访问日志：`GET /ai-server/artifacts/delivery_48882… → 200`（no-store HTML）+ `POST …/redeem → 200 OK`（DOCX 服务，grant 消费）。grant_consumed_at 置 true（一次性）。
- 410 重放：v9 首次 redeem 200（consumed）；其他 delivery 访问日志已见 `POST …/redeem → 410 Gone` 模式（M4-02 已活体验证）。本轮 v9 未二次点击（用户回家，且 raw grant token 仅在卡片 URL fragment、DB 仅 sha256 digest，无法 curl 重放），410 机制以历史验证 + 日志模式为准。
- 下载端点 restart 后注册：`GET /ai-server/artifacts/{id} → 200 text-html` + `POST /redeem` 无 token → 404（fail-closed）✓。

### 健康/脱敏回归

- 本轮 session（07-22 00:07+）**0 Traceback / 0 SIGBUS / 0 IntegrityError·UniqueViolation / 0 PackSnapshot conflict**（日志中 3 个 Traceback 均为 07-21 22:27 的 e8deb3b 前历史留）。
- 0 direct_file / 0 PDF / 0 SSO / 0 OIDC·SAML ✓。
- events v16-18 全 `sha256:` digest-only（artifact_registered/publication_registered/delivery_registered），0 grant_token/grant_hash/storage_key/OA/姓名/宿主路径泄漏 ✓。
- 1 次模型超时（draft 生成，60s 截断）→ 框架兜底"本次模型处理超时，请稍后重试"，无 partial draft 落库、无 traceback；临时调 `AGENTSEEK_MODEL_REQUEST_TIMEOUT_SECONDS=60→240` + `MAX_RETRIES=0→1` 后重试成功（runtime 临时调整，不提交，已恢复）。

### 结论（097a9ef full-lifecycle 补验）

**活体 nudge 验证 PASS**：guard `_append_deterministic_nudges` 在真实 model turn（glm-5.2 fresh render v5）中**实际运行**（日志 `00:27:30.790 report_output_guard`），正确检测 render 成功 + 判定命令已含 + 不重复（D case 活体）。B case（模型省略→追加）由单测覆盖，glm-5.2 配合时走 relay 分支未触发。full lifecycle 在既有 delivered work 上经 brief 修订路径重走完成（M1-06 设计：修订追加版本，不新建 work），artifact v5 全新 sha `05d955bf…`，DOCX sha 匹配，grant 一次性消费，下载 200。**097a9ef nudge 机制活体 + 单测双重验证完成，RC reliability gate 活体收口。**

