---
title: v0.1.1 M2 Playbook Registry 实现与验收
type: reference
audience: [A3, A4, A5]
runs: no
verified_on: 2026-07-22
sources:
  - examples/enterprise_wecom_digital_employee/V0.1.1_DEPARTMENT_DIGITAL_EMPLOYEE_PLAN.md
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/playbook_registry.py
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/reports/playbook.py
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/pack_loader.py
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/work_composition.py
---

# v0.1.1 M2 Playbook Registry 实现与验收

## 一、切片目标

M2 在不改变 v0.1.0 报告账本和企微协议的前提下，把原来直接绑定在 Agent 上的
证券报告流程适配为一个版本化 `PlaybookBinding`，并由部门级 `PlaybookRegistry`
统一加载和委派。

本切片只启用一个生产 Playbook：

```text
securities-industry-report@1
```

多 Playbook 的确定性路由和第二个测试 Playbook 属于 M3。M2 允许 Registry 保存多个
Binding，但没有选择结果时禁止调用任何一个 Binding，避免隐式选择第一个流程。

## 二、实现合同

### 2.1 Registry

`PlaybookRegistry` 负责：

- 按版本化 `playbook_ref` 登记 Binding；
- 校验 Binding 集合与 Profile `supported_playbooks` 完全一致；
- 拒绝重复、缺失和额外 Binding；
- 委派消息状态、状态增强、工具、输出守卫、当前任务和服务介绍；
- 单 Playbook 阶段返回唯一活动 Binding；
- 多 Playbook 阶段在 M3 路由选择前 fail-closed。

动态入口只允许从模板自身 Python package 加载，Pack 不能导入任意外部模块。

### 2.2 权限边界

Pack 中每个 Playbook 显式声明：

- `skill_refs`；
- `policy_refs`；
- `tool_grants`；
- `data_scopes`。

每组声明必须是 Profile 对应权限的子集，并且 Skill 和 Policy 还必须由 Pack 声明。
Profile v2 缺少任一 Playbook 权限字段时加载失败。Profile v1 可以缺少这些字段，
Loader 会继承原 Profile/Pack 上限，保持 v0.1.0 单 Playbook 兼容。
显式空 `policy_refs`、`tool_grants` 或 `data_scopes` 是合法的最小权限子集。

研究模板是报告 Playbook 的可选扩展元数据，不是通用 `PlaybookSpec` 的必填字段。
证券报告 Binding 会单独要求它存在，并校验模板所属 Skill 已被该 Playbook 声明。

有效权限仍由运行时继续求交：

```text
Profile 上限 ∩ Playbook 声明 ∩ 员工授权 ∩ 当前 Policy
```

M2 没有修改 MCP Policy、确认回退或服务端动作工具的既有授权校验。

### 2.3 报告 Binding

`IndustryReportPlaybookBinding` 封装现有 `IndustryReportWorkComposition`，拥有：

- 报告消息状态和 WorkItem 上下文；
- 报告专用工具集合；
- 报告账本真实性 Output Guard；
- 当前报告任务查询；
- 服务目录介绍。

Agent 不再直接导入报告工具和报告 Guard，而是只从 Registry 的活动 Binding 获取。
原 `get_work_composition()` 保留为兼容 API，报告生命周期实现没有重写。

### 2.4 WorkItem 绑定

创建 WorkItem 时使用已选择 Playbook 的：

- `playbook_id` 和 `playbook_version`；
- Skill digest 子集；
- Profile 与 Playbook 权限摘要。

`digital_employee_id=industry-report`、Playbook `securities-industry-report@1`、活动任务
并发键和历史 WorkItem 查询语义均不变。

## 三、版本与迁移

| 项目 | M2 值 |
| --- | --- |
| Pack | `1.11.0` |
| Profile | `1.10.0` |
| Pack schema | `1` |
| Profile schema | `2`，兼容 `1` |
| Work schema | revision `11`，无迁移 |
| 生产 Playbook | `securities-industry-report@1` |

Pack/Profile 升版是因为不可变 Pack 内容和权限摘要发生变化。旧 PackSnapshot 必须保留。

## 四、M2 不做

- 不增加第二个生产 Playbook；
- 不做 LLM-first 意图分类；
- 不实现活动 WorkItem、显式服务和确定性规则路由；
- 不改变 ReportBrief 到 ReportDelivery 的合同；
- 不改变 schema revision 11；
- 不改变 MCP 受保护文件；
- 不释放 delivered Work 对 M1-06 scope 的既有占用。

## 五、Mac mini 验收 Oracle

### 5.1 静态与 Pack

- example、Work、Enterprise、WeCom、Files、LangChain、模板、ty、ruff、docs 全绿；
- Pack `1.11.0` / Profile `1.10.0` 注册新 PackSnapshot；
- 旧 `1.10.0` PackSnapshot 保留，零 immutable conflict；
- schema 仍为 revision 11；
- MCP protected files 相对 GA 与 `68d7b25` 均 zero-diff。

### 5.2 Registry 与权限

- Profile v2 的单生产 Playbook 成功注册；
- Binding 提供现有报告工具、Guard、当前任务和服务介绍；
- Profile v1 继承兼容探针通过；
- skill、policy、tool、data scope 任一越界都 fail-closed；
- 构造多 Binding Registry 可以登记和查询，但未选择时执行必须失败。

### 5.3 企微活体

1. `你是谁`、`你能做什么`、`怎么使用你` 继续走 M1 确定性路径，零模型调用；
2. `我是谁` 继续返回认证人类员工身份；
3. `查看当前报告任务状态` 命中原 `work_ed04971`，不创建新 WorkItem；
4. 走一次现有报告动作或精确状态命令，确认报告工具经 Registry 可达；
5. 确认 ReportBrief、Outline、Draft、Approval、Artifact、Publication、Delivery 历史仍可读；
6. 不发生工具全集泄漏、跨 Playbook 上下文或新路由事件。

### 5.4 健康与安全

- 零 SIGBUS、Traceback、IntegrityError、PackSnapshot conflict 和真实 HTTP non-200；
- 队列、去重、response_url 和 signed-link 下载不回归；
- 事件与 MCP audit 分离；
- 不记录员工原文、明文 OA、姓名、凭据、Prompt 或宿主路径。

## 六、退出条件

M2 只有在 Mac mini 确认现有证券报告 Playbook 经 Registry 后保持完整兼容，且新
PackSnapshot、权限子集和 Profile v1 兼容均通过后关闭。关闭后进入 M3：确定性内部
路由和测试型第二 Playbook。

## 七、Mac mini 活体复核 — 964d477（2026-07-22，glm-5.2）

**裁定：PASS。** Registry 单 Playbook 适配保持 v0.1.0 行为；确定性服务发现经 Registry.profile 仍 0 model call；报告 Binding 只读工具可达（get_current_work_status）；Output Guard 正确拦截无账本依据的正文；M1-06 delivered scope 保持，无第二 WorkItem；健康/脱敏回归通过。M2 关闭，允许进入 M3。

HEAD `964d477`（GitHub origin + GitLab company-gitlab 双端 `964d47740b3`）。父基线 `f6bcf49`（M1 PASS）。schema 仍 rev11；Pack `1.11.0` / Profile `1.10.0`；`digital_employee_id` 仍 `industry-report`；Playbook 仍 `securities-industry-report@1`。

### 静态回归

- Work 106、Example **275 passed+1 skipped**、Enterprise 86、WeCom 78、Files+LangChain 74、template render 2；ty/ruff/docs 全绿；MCP protected（mcp_policy.py + tools.py）zero-diff（vs GA `d6f3868` 与 `68d7b25`）。
- 0 schema migration；新 PackSnapshot `1.11.0` 注册成功，旧 `1.10.0`/`1.9.3` 及更早全保留，0 immutable conflict。

### Registry 确定性探针（committed 标准 Pack 1.11.0）

- `pack_version=1.11.0`、`profile_version=1.10.0`、`profile_schema=2`；`playbook_refs=('securities-industry-report@1',)`（仅一个）；`active_playbook=securities-industry-report@1`；`effective_tool_grants=('analyze_file','department-knowledge-read','gildata-read')`（Profile 子集）；`tool_count=23`；create/outline/render/delivery 四关键工具均可达；`introduction` 来自"证券行业正式报告"服务目录。
- Profile v1 兼容 + 权限越界 fail-closed 9 项由静态测试覆盖（test_playbook_registry: v1 继承 / v2 缺声明 fail-closed / 越界 exceeds / research_template 属 skill 子集 / 空子集 / 通用 spec 不要求 research_template / entrypoint package-scoped / factory 非法拒绝 / 多 Binding 未路由 selection-required）。

### 部署

Mac mini smoke override（zhuchunlin 属信息技术部，committed profile 为战略发展部）：`pack_version 1.11.0 → 1.11.0-mac-smoke.1`、`owning_org 战略发展部 → 信息技术部`，仅本机、测试后 `git restore`。`scripts/run_gateway.sh` 启动（PYTHONPATH + JPype + JayDebeApi），PID 99532，health OK，0 conflict/traceback。

### Live A — M1 Job Charter 回归（0 model call + 措辞优化）

M2 已将 `behavior_principles` 改为业务口语（M1 非阻塞反馈落实）："优先使用部门已授权资料，使用外部信息前会先征得你的同意；重要事实和结论会保留来源，资料不足时会明确说明；需求、提纲和初稿等关键版本会请你确认后再继续；审批、生成文件、发布和交付分别确认，不会替你自动越级操作"——无"模型记忆/合同版本/外部检索"技术词。

- A1 `你是谁`：战略发展部数字员工 + DE-SD-001 + mission + 服务 + 准则；**0 model call**（1 秒响应）；service_discovery @12:20:26（intent=identity）。✓
- A2 `你能做什么`：仅"证券行业正式报告"+ 工作过程 + 示例 + "关键版本需你确认"；**0 model call**；service_discovery @12:22:45。✓
- A3 `怎么使用你`：普通/正式 Playbook 区分 + 示例 + 步骤 + 执行边界；**0 model call**；service_discovery @12:23:41。✓
- A4 `我是谁`：返回认证人类员工身份（朱春霖/zhuchunlin/信息技术部/岗位/组织路径），不介绍 DE-SD-001，无 service_discovery。✓

### Live B — Registry 下现有任务状态

`查看当前报告任务状态` → 返回 `work_ed04971` delivered v18 全生命周期（Brief v6/gap v5/outline v5/draft v5/approval v5/artifact v5 published+delivered/delivery v9 consumed）；`get_current_work_status` 经 Registry 报告 Binding 可达；**work 数 17，0 新 WorkItem，0 新 event**；无跨 Playbook 状态泄漏。✓

### Live C — 报告 Binding 工具可达性（只读）

`查看当前 ReportArtifact 和发布交付状态` → 模型未调用 `get_current_report_artifacts` 等只读工具（glm-5.2 将"查看 ReportArtifact"误判为生成请求，模型可靠性问题，**非 M2 回归**）；Output Guard 正确拦截无账本依据的正文（"未检测到本轮账本支持的 ReportDraft…模型正文已被运行时守卫拦截"）；**0 新 Artifact/Publication/Delivery**；无 `call_mcp_tool` 越权。Registry Binding 工具可达性已由 Live B（`get_current_work_status`）+ 探针（create/outline/render/delivery 均可达）证明。✓

### Live D — 服务发现与正式请求边界

`你是谁，并帮我编写一份报告` → **未被 fast-path 截获**（`match_job_charter_intent` 对该串返回 None，M1 已单测验证）；bot 先简述身份，随即转入报告请求处理（指出既有任务已交付、要求补充新报告主题/受众/覆盖期）；**M1-06 delivered scope 保持**——未创建第二个同 Playbook WorkItem（work 17、0 新 event），沿用现有 WorkItem/修订语义；无 service_discovery 事件。✓

### 健康/脱敏

本轮 session（12:16 重启后）：**0 Traceback / 0 SIGBUS / 0 IntegrityError·UniqueViolation / 0 PackSnapshot conflict / 0 真 HTTP non-200**；0 direct_file / 0 PDF / 0 SSO；3 个 `digital_employee_service_discovery` 事件（A1/A2/A3，仅标识/摘要，无员工原文/OA/姓名/Prompt/凭据）；gateway 重启后 0 新 work event；MCP protected zero-diff。✓

### 非阻塞（供 Codex 后续）

1. **身份回复可补充 Skills/MCP 能力概览**（员工反馈）：当前 `你是谁` 仅列 Playbook 正式服务，建议在能力层补充 Skills（文件分析、知识检索）和 MCP（部门知识库、外部数据源经员工同意启用）的口语化概览，使员工更全面了解数字员工。需遵守 M0 方案 §9.1（不暴露隐藏工具名）+ §5.3（工具可见性≠授权）——在能力层描述，不暴露 tool ID/grant。
2. **`我是谁` 员工 ID hash 对用户不友好**（员工反馈）：`17308da7990bc39bd424f4047fe9ec54` 形式的 hash 展示给业务人员无意义，建议隐藏或改为工号/姓名为主、hash 仅审计层保留。
3. **Live C 模型未调用只读 `get_current_*` 工具**（glm-5.2 可靠性）：模型把"查看 ReportArtifact"误判为生成请求，Output Guard 正确拦截但未走只读工具路径。建议 SKILL/agent 引导在"查看/当前 X"查询时优先调用 `get_current_*`（与 M3 路由的确定性触发规则协同）。
4. model call 日志 sink 跨 turn 不一致（与前序轮一致，功能正常）。

### 结论

**M2 PASS — 关闭，允许进入 M3 确定性内部路由。** PlaybookRegistry 单 Binding 适配保持证券报告全生命周期行为；Agent 统一从活动 Binding 获取工具与 Output Guard；Playbook 权限是 Profile 子集（fail-closed）；Profile v1 兼容；确定性服务发现经 Registry.profile 仍 0 model call；M1-06 delivered scope 保持，无第二 WorkItem；v0.1.0 账本零回归。M3 才引入确定性内部路由 + 测试型第二 Playbook。
