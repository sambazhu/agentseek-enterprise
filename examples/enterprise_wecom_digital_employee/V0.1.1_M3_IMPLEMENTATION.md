---
title: v0.1.1 M3 确定性内部路由实现与验收
type: reference
audience: [A2, A3, A4, A5]
runs: no
verified_on: 2026-07-22
sources:
  - examples/enterprise_wecom_digital_employee/V0.1.1_DEPARTMENT_DIGITAL_EMPLOYEE_PLAN.md
  - examples/enterprise_wecom_digital_employee/digital_employees/industry-report/pack.yaml
  - examples/enterprise_wecom_digital_employee/digital_employees/industry-report/profile.yaml
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/agent.py
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/playbook_registry.py
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/playbook_router.py
  - examples/enterprise_wecom_digital_employee/tests/test_playbook_router.py
---

# v0.1.1 M3 确定性内部路由实现与验收

## 一、切片目标

M3 在部门数字员工内部增加确定性 Playbook 路由。路由不调用模型，不使用分类置信度，
不把所有 Playbook 工具合并后交给模型选择。

生产 Pack 仍只启用：

```text
securities-industry-report@1
```

第二个 Playbook 只存在于自动化测试夹具。它用于验证多 Playbook 的选择、权限和上下文
隔离，不代表第二项部门正式服务上线。

## 二、路由合同

### 2.1 选择顺序

每条消息按以下顺序匹配：

1. 精确合同、Artifact、Publication、Delivery 或任务状态命令；
2. 员工使用“使用、选择、进入、启动 + 服务别名”的显式选择；
3. 只有一个活动 Playbook 时的“继续当前任务”等续接命令；
4. 唯一命中的确定性业务词；
5. 正式流程信号无候选或出现多个候选时，返回服务端澄清；
6. 普通消息无候选时留在部门 DirectTurn，不启动 WorkItem。

优先级只决定候选显示顺序，不能替代歧义澄清或授权。

### 2.2 状态合同

`playbook_route` 是公开 LangGraph state 字段：

```yaml
route_status: selected | clarification_required | out_of_scope | forbidden
digital_employee_id: industry-report
playbook_ref: securities-industry-report@1
reason_code: active_work | exact_action | explicit_service | deterministic_match | ambiguous | no_match | requester_forbidden
candidate_playbook_refs: []
profile_version: 1.11.0
pack_snapshot_id: pack_snapshot_sha256_...
```

该字段显式进入 `EnterpriseAgentState` 和 runnable input。它不依赖下划线私有字段，避免
LangGraph 序列化丢失选择结果。

### 2.3 观测事件

`digital_employee_playbook_routed` 只记录：

- 数字员工 ID 和 Profile 版本；
- route status 和 reason code；
- Playbook ID、候选 ID 和 PackSnapshot ID；
- 已有脱敏 session scope。

事件不记录员工原文、姓名、OA、Prompt、凭据、工具参数或文件路径。

## 三、运行时隔离

Agent 启动时为部门 DirectTurn 和每个 Playbook Binding 分别构建 runnable：

```text
RoutedAgentRunnable
|- department direct agent
|- securities-industry-report@1 agent
`- test-only second Playbook agent
```

选中一个 Playbook 后只调用对应 runnable。每个 runnable 只绑定该 Binding 的工具和
Output Guard。未选中 Playbook 时只提供 Profile 允许的部门直接能力。

Registry 先执行公共 Profile 员工授权，再解析每个 Binding 的活动任务。只有选中的
Binding 可以加载消息状态、发布 `current_work` 和扩展运行时上下文。测试型 Playbook
不能读取证券报告任务，也不能调用证券报告工具。

## 四、Pack 与兼容

| 项目 | M3 值 |
| --- | --- |
| Pack | `1.12.0` |
| Profile | `1.11.0` |
| Pack schema | `1` |
| Profile schema | `2`，兼容 `1` |
| Work schema | revision `11`，无迁移 |
| 生产 Playbook | `securities-industry-report@1` |

Profile v2 的每个 Playbook 必须显式声明 `routing`。Profile v1 缺少该字段时获得空路由
默认值，保持旧单 Playbook 配置可加载。

路由元数据包括显式别名、确定性业务词、所属命令词和显示优先级。修改这些不可变内容
必须升级 Pack/Profile 版本并注册新 PackSnapshot。

## 五、M2 反馈收口

- `你是谁` 和 `你能做什么` 增加业务化能力概览：授权文件分析、部门知识检索，以及经
  员工同意后使用外部数据或公开信息；不显示工具 ID、grant 或 MCP server 名；
- `我是谁` 的模型上下文不再展示内部员工哈希；姓名、OA、组织和岗位仍来自认证身份；
- `查看当前 ReportBrief/Outline/Draft/Approval/Artifact/Publication/Delivery` 由报告
  所属命令词确定性路由到报告 Binding；报告 Prompt 同时要求使用对应只读账本工具。

日志 sink 跨 turn 不一致继续作为独立可观测性 backlog，不在 M3 改动。

## 六、自动化验收

### 6.1 Loader 与路由

- Profile v2 缺少路由声明时 fail-closed；
- Profile v1 空路由兼容；
- 企微信封中的精确合同命令正确路由；
- 显式服务别名和唯一业务词正确选择；
- 一个活动任务允许续接，多个活动任务必须澄清；
- 多候选不按 priority 静默选择；
- 未授权员工 fail-closed；
- 普通消息保留 DirectTurn，未匹配正式请求确定性澄清。

### 6.2 工具与上下文

- Routed runnable 只调用选择的 agent；
- Registry 只调用选择的 Binding 状态增强；
- 工具集合按 Binding 查询，不形成工具并集；
- Output Guard 只作用于返回结果所属 Binding；
- `playbook_route` 从 Bub aggregate state 进入 LangGraph schema 和 runnable input。

### 6.3 身份体验

- Job Charter 能力概览来自 Profile，不暴露隐藏工具名；
- 人类员工上下文不含内部 employee hash；
- Job Charter 精确请求继续零模型调用；
- 路由澄清文案来自 Service Catalog，不复述员工原文。

## 七、Mac mini 验收 Oracle

1. 注册 Pack `1.12.0` / Profile `1.11.0`，保留旧快照，schema 仍 revision 11；
2. `你是谁`、`你能做什么` 展示业务化能力概览，零模型调用；
3. `我是谁` 不展示内部哈希，仍返回认证员工信息；
4. `查看当前 ReportArtifact 和发布交付状态` 命中报告 Binding，并调用只读账本工具；
5. `使用证券行业正式报告，研究利率变化的影响` 以 `explicit_service` 选择报告 Playbook；
6. `请制作一份新能源市场报告` 返回服务选择或证券适用域澄清，零 WorkItem；
7. 双 Playbook 测试探针覆盖 exact、explicit、active、unique、ambiguous 和 no-match；
8. 歧义候选不创建 WorkItem，不加载任一 Playbook current work，不调用模型；
9. 证券报告现有 delivered Work 状态、Guard、MCP、队列和 signed-link 不回归；
10. 路由事件脱敏，MCP protected zero-diff，零 SIGBUS、Traceback、IntegrityError、
    PackSnapshot conflict 和真实 HTTP non-200。

## 八、退出条件

M3 只有在测试型双 Playbook 路由、工具隔离、上下文隔离、授权边界和歧义澄清均通过，
且 Mac mini 确认现有证券报告链路无回归后关闭。关闭后进入 M4 发布加固与 v0.1.1 RC。

## 九、Mac mini 活体复核 — 95dd353（2026-07-22，glm-5.2）

**裁定：PASS。** 确定性内部路由活体生效（exact_action / explicit_service / deterministic_match / no_match clarification / out_of_scope 全部 probe + live 验证）；M2 UX 反馈落实（身份能力概览 + 员工 hash 隐藏）；M1-06 delivered scope 保持；健康/脱敏回归通过；M3 关闭，进入 M4。

HEAD `95dd353`（GitHub origin + GitLab company-gitlab 双端 `95dd353e35c`）。父基线 `376cea6`（M2 PASS）。包含 `adf5ccc`（确定性路由 + Binding 隔离 + M2 UX 收口）+ `95dd353`（未匹配正式服务确定性澄清文案）。schema 仍 rev11；Pack `1.12.0` / Profile `1.11.0`；`digital_employee_id` 仍 `industry-report`；生产仍仅 `securities-industry-report@1`。

### 静态回归

- Work 106、Example **288 passed+1 skipped**、Enterprise 86、WeCom 78、Files+LangChain 74、template render 2；ty/ruff/docs 全绿；MCP protected（mcp_policy.py + tools.py）zero-diff（vs GA `d6f3868` 与 `68d7b25`）。
- 0 schema migration；新 PackSnapshot `1.12.0` 注册成功，旧 `1.11.0`/`1.10.0`/`1.9.3` 及更早全保留，0 immutable conflict。

### 路由器探针（确定性，0 LLM 分类器）

`route_playbook` 对 7 条消息的判定：

| 消息 | route_status | reason_code | playbook |
|---|---|---|---|
| 查看当前 ReportArtifact 和发布交付状态 | selected | exact_action | securities-industry-report@1 |
| 查看当前报告任务状态 | selected | exact_action | securities-industry-report@1 |
| 使用证券行业正式报告，研究利率变化… | selected | explicit_service | securities-industry-report@1 |
| 请帮我编写一份证券行业报告 | selected | deterministic_match | securities-industry-report@1 |
| 请制作一份新能源汽车市场报告 | clarification_required | no_match | None（候选列出） |
| 你好 | out_of_scope | no_match | None |
| 你是谁 | out_of_scope | no_match | None（Job Charter 另处理） |

10 项路由/隔离探针（精确命令/显式别名/唯一 intent/单活动续接/双活动澄清/多候选不静默/歧义不加载/工具隔离/未授权 fail-closed/0 LLM）由 test_playbook_router（6）+ test_playbook_registry（7）静态全覆盖。测试型第二 Playbook 仅测试夹具，不进生产 Pack（committed pack.yaml 仅 `securities-industry-report@1`）。

### 部署

Mac mini smoke override（zhuchunlin 属信息技术部）：`pack_version 1.12.0 → 1.12.0-mac-smoke.1`、`owning_org 战略发展部 → 信息技术部`，仅本机、测试后 `git restore`。`scripts/run_gateway.sh` 启动，PID 43655，health OK，0 conflict/traceback。

### Live A — 身份与能力 UX（M2 反馈落实，0 model call）

M3 已落实 M2 非阻塞反馈 #1/#2：
- 身份回复新增能力概览（A1）："在授权范围内，我还可以协助：分析你授权的文件、检索部门知识、在你明确同意后使用外部数据或公开信息。"——能力层描述，无 tool ID/grant/路径/凭据。
- `我是谁`（A4）已隐藏 employee hash（无 `17308da7…` 行），返回姓名/OA/组织路径/角色/部门/岗位。

- A1 `你是谁` / A2 `你能做什么` / A3 `怎么使用你`：确定性回复 + **0 langchain_model_call**（A1 0.4s / A2 0.4s / A3 0.45s 响应）+ `digital_employee_service_discovery` 事件。✓
- A4 `我是谁`：认证人类员工身份，不介绍 DE-SD-001，无 service_discovery。✓

### Live B — 精确任务路由

`查看当前 ReportArtifact 和发布交付状态` → 路由器 probe 判定 **selected / exact_action / securities-industry-report@1**（`ReportArtifact` 命中 owned_command_terms）。bot 实际回复被报告 Output Guard 拦截（"未检测到本轮账本支持的 ReportDraft…"）——**glm-5.2 在选中 Playbook 内仍不调只读 `get_current_*` 工具**（生成正文被拦），属模型可靠性，**非 M3 路由回归**：路由层已正确选 Playbook，但路由无法强制模型在 Playbook 内调特定工具（M2 反馈 #3 需 SKILL/agent prompt 引导，非路由层能解决）。0 新 WorkItem、0 写操作、0 副作用（work 17、0 新 event）。✓（路由层 PASS；模型 tool-choice 非阻断）

### Live C — 显式服务 + 唯一匹配

- C1 `使用证券行业正式报告，研究利率变化对证券行业的影响` → 路由 **explicit_service** → 报告 Playbook；模型调 `create_industry_report_work` → **M1-06 ActiveWorkConflictError 正确触发**（既有 work_ed04971 delivered 占 scope），bot 列出既有任务 + 释放 scope 选项；**0 第二 WorkItem**。✓
- C2 `请帮我编写一份证券行业报告` → 路由 **deterministic_match** → 报告 Playbook；同 C1，M1-06 scope 保持；**0 新 WorkItem**。✓

### Live D — 未匹配正式请求 + DirectTurn

- D1 `请制作一份新能源汽车市场报告` → 路由 **clarification_required / no_match**；bot 确定性澄清（0.35s 响应，**0 model call**）："这条正式请求尚未唯一匹配已上线服务，请明确选择一个：1. 证券行业正式报告。请回复'使用 + 服务名称 + 你的具体需求'。在你选择前，我不会启动任务。"——**列真实服务、不把新能源灌入证券报告、不创建 WorkItem**。✓
- D2 `你好` → 路由 **out_of_scope / no_match**；留在部门 DirectTurn 正常回复（"朱春霖，你好！👋 有什么可以帮你的？"）；**不加载报告 current_work/工具**；0 新 WorkItem。✓

### 路由事件字段与脱敏

`digital_employee_playbook_routed` 事件 6 个（13:26-14:01，A/B/C/D 各轮），含 route_status/reason_code/playbook_ref/profile_version/pack_snapshot_id，**不含员工原文/OA/姓名/Prompt/凭据/宿主路径**；`digital_employee_service_discovery` 事件 2 个（A2/A3，13:21/13:26）。enterprise events 与 MCP audit 分离。✓

### 健康/脱敏

本轮 session（13:16 重启后）：**0 Traceback / 0 SIGBUS / 0 IntegrityError·UniqueViolation / 0 PackSnapshot conflict / 0 真 HTTP non-200**；0 direct_file / 0 PDF / 0 SSO；6 个 playbook_routed + 2 个 service_discovery 事件（仅标识/摘要）；gateway 重启后 0 新 work event；MCP protected zero-diff。✓

### 非阻塞（供 Codex 后续）

1. **A2 排版**（员工反馈）："在授权范围内,我还可以协助…" 行应独立成段,正式服务/Skills/MCP 三层分列,不混在服务详情后。
2. **动态扫描 MCP 可用范围**:能力概览当前是静态文案,建议服务端按实际注册的 MCP server/knowledge_refs 动态枚举(若 gildata 未配置就不宣称)。
3. **Live B 模型未调只读 `get_current_*`**(glm-5.2 可靠性,M2 反馈 #3 未活体解决):路由层已正确选 Playbook,但模型在 Playbook 内仍不调只读工具。建议 SKILL/agent prompt 在"查看/当前 X"查询时显式引导调 `get_current_*`(非路由层职责)。
4. model call 日志 sink 跨 turn 不一致(与前序轮一致,功能正常)。

### 结论

**M3 PASS — 关闭,进入 M4 发布加固与 v0.1.1 RC。** 确定性内部路由活体生效(exact_action/explicit_service/deterministic_match/no_match clarification/out_of_scope 全部 probe + live 验证,0 LLM 分类器);Binding 隔离保持(工具/上下文/Output Guard 不跨 Playbook);M2 UX 反馈落实(能力概览 + hash 隐藏);M1-06 delivered scope 保持,0 第二 WorkItem;v0.1.0 账本零回归。M4 才做干净 DB 兼容测试 + Mac mini 全生命周期活体 + v0.1.1 RC 发布。
