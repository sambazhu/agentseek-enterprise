---
title: v0.1.1 M4 发布加固实施记录
type: explanation
audience: [A2, A3, A4, A5]
runs: no
verified_on: 2026-07-22
sources:
  - examples/enterprise_wecom_digital_employee/V0.1.1_DEPARTMENT_DIGITAL_EMPLOYEE_PLAN.md
  - examples/enterprise_wecom_digital_employee/V0.1.1_M3_IMPLEMENTATION.md
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/agent.py
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/capability_registry.py
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/channel_command.py
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/job_charter.py
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/playbook_registry.py
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/playbook_router.py
  - examples/enterprise_wecom_digital_employee/src/enterprise_wecom_digital_employee/report_status.py
---

# v0.1.1 M4 发布加固实施记录

## 一、目标

M4 不增加新的业务 Playbook，不重做 v0.1.0 已完成的报告账本、审批、发布和交付。
它负责在 v0.1.1 RC 前收紧部门数字员工的员工体验和确定性边界，并完成全生命周期兼容回归。

M3 Mac mini 复核发现三个需要在 RC 前关闭的问题：

1. 精确的“查看当前 ReportArtifact/发布/交付状态”已经路由到报告 Playbook，但模型可能不调用
   `get_current_*` 工具，随后被 Output Guard 正确拦截；
2. Job Charter 把正式服务、辅助能力和执行边界混排；
3. Job Charter 的能力概览来自静态文案，可能宣称部署中并未配置的 MCP 能力。

模型调用日志跨 turn 丢行属于独立可观测性问题，不与本轮员工功能改动耦合。

## 二、M4-00：确定性状态与运行时能力目录

### 2.1 只读报告状态下沉到 Playbook Binding

新增受限的只读命令识别器。它只接受带“查看/查询/显示/告诉我”和“当前”的精确状态请求，
并复用已认证的企微信封剥离逻辑。发布、交付、渲染等动作命令不会被截获。

```text
企微消息
-> 确定性 Playbook 路由
-> 已选 Binding.direct_response
-> WorkComposition.current_work_summary
-> 服务端账本状态渲染
-> 直接回复员工
```

该路径不调用模型，也不依赖模型选择 `get_current_*` 工具。响应只展示选择的合同状态字段，
不展示 ReportDraft 正文、storage key、grant hash、下载 token 或宿主路径。普通工具调用仍复用同一渲染器，
避免直接路径与工具路径产生两套状态口径。

支持的首批状态包括 ReportBrief、缺口决策、ReportOutline、ReportDraft、ReportApproval、
ReportArtifact、ReportPublication 和 ReportDelivery。完整任务状态返回全部部分；员工只询问 Artifact、
发布和交付时，只返回对应部分。

新增脱敏事件 `digital_employee_playbook_direct_response`，只记录数字员工、Profile、Playbook、session
和响应类别，不记录员工原文或账本正文。

### 2.2 Job Charter 分段渲染

“你是谁”和“你能做什么”按以下业务结构服务端渲染：

```text
数字员工身份 / 正式服务

协助能力

工作准则 / 执行边界
```

员工能够区分正式 Playbook 服务、辅助能力和治理边界，不再看到混排的技术说明。

### 2.3 能力目录按部署配置求交

身份响应中的辅助能力由以下事实求交生成：

```text
Profile 声明
∩ Playbook 有效 tool grants / data scopes
∩ 当前 MCP 配置中的 server 标识
```

能力目录只读取 MCP 配置的 server 名，不读取、输出或记录命令参数、URL、token 和凭据，也不会为一次
身份查询主动连接 MCP。这里表达的是“当前部署已配置且已授权”的能力快照，不是实时健康检查；实际调用
仍需通过 MCP policy、员工授权和工具级错误处理。

对员工只展示“文件分析”“部门知识”“外部数据”“公开搜索”等业务能力名称，不显示工具 ID、grant、
MCP server 名或内部路径。配置缺失或 JSON 无效时 fail-closed，不宣称对应能力。

## 三、兼容边界

- schema 保持 revision 11，无迁移；
- Pack `1.12.0` / Profile `1.11.0` 不变；
- `digital_employee_id=industry-report` 不变；
- 生产仍只启用 `securities-industry-report@1`；
- 不修改报告合同、状态机、MCP policy、文件交付和下载协议；
- Profile v1 与单 Playbook 兼容路径保持；
- cookiecutter 模板与 example 使用相同实现。

## 四、自动化验收

- 企微信封中的组合状态查询确定性识别；
- 动作命令和伪造日期信封不进入只读路径；
- 只读响应不含正文、storage key、grant hash 或 token；
- 已选 Binding 才能生成直接响应，其他 Binding 不读取该任务；
- Job Charter 三段排版稳定；
- 未配置或未授权的 MCP 能力不出现在身份响应；
- example、Work、Enterprise、WeCom、Files、LangChain、模板渲染、ty、ruff 和文档测试全绿；
- MCP protected zero-diff；
- Mac mini 确认状态请求零模型调用且 v0.1.0 生命周期无回归。

## 五、独立 Backlog

`langchain_model_call` / `report_output_guard` 日志在少数 turn 中缺行的问题继续作为独立可观测性
工单。M4-00 新增的确定性路径本来就不会产生 model-call 日志，因此应以
`digital_employee_playbook_direct_response` 区分正常零模型路径与模型链路日志缺失。

## 六、Mac mini 活体复核 — a5c7f03（2026-07-22，glm-5.2）

**裁定：PASS。** 确定性服务响应加固活体生效：报告状态查询由 Playbook Binding 直读账本确定性响应（0 model call、绕过 Output Guard，M3 Guard 拦截问题完全解决）；Job Charter 三段排版 + 动态 MCP 能力求交（M2/M3 UX 反馈落实）；动作命令不被只读解析器截获；健康/脱敏回归通过。M4-00 关闭，进入 v0.1.1 RC 全生命周期复验。

HEAD `a5c7f03`（GitHub origin + GitLab company-gitlab 双端 `a5c7f032e6b`）。父基线 `abe5487`（M3 PASS）。schema 仍 rev11；Pack `1.12.0` / Profile `1.11.0`（本轮未升版）；`digital_employee_id` 仍 `industry-report`；生产仍仅 `securities-industry-report@1`。

### 静态回归

- Work 106、Example **302 passed+1 skipped**、Enterprise 86、WeCom 78、Files+LangChain 74、template render 2；ty/ruff/docs 全绿；MCP protected（mcp_policy.py + tools.py）zero-diff（vs GA `d6f3868`）。
- 0 schema migration；0 PackSnapshot immutable conflict（Pack/Profile 未升版，沿用 1.12.0 快照）。

### 部署

Mac mini smoke override（zhuchunlin 属信息技术部）：`pack_version 1.12.0 → 1.12.0-mac-smoke.1`、`owning_org 战略发展部 → 信息技术部`，仅本机、测试后 `git restore`。`scripts/run_gateway.sh` 启动，PID 14183，health OK，0 conflict/traceback。

### Live A — Job Charter 排版 + 动态能力（M2/M3 反馈落实，0 model call）

M4-00 落实 M3 非阻塞反馈 #2（分段排版）+ #3（动态 MCP 能力）：
- **三段分列**：`你是谁` → 身份段 / **"协助能力"** 独立段 / **"工作准则"** 独立段；`你能做什么` → **"正式服务"** / **"协助能力"** / **"执行边界"** 三段。
- **动态 MCP 能力求交**：协助能力文案为"检索**已配置的**部门知识、在你明确同意后使用**已配置的**外部数据或公开搜索"——按实际注册 MCP server/knowledge_refs + effective_tool_grants 求交（`capability_catalog.resolve_runtime_capabilities`），未配置不宣称。
- A1/A2/A3 均**0 langchain_model_call**（A1 0.3s / A2 0.34s / A3 0.45s 确定性响应）+ `digital_employee_service_discovery` 事件（14:52/14:54/14:55）。
- A4 `我是谁`：返回人类员工身份（姓名/OA/岗位/组织路径/角色/部门），**employee hash 已隐藏**（无 `17308da7…`），不介绍 DE-SD-001。

### Live B — 确定性只读账本查询（M3 反馈 #1/#3 功能性解决，核心突破）

M3 中 `查看当前 ReportArtifact 和发布交付状态` 被 Output Guard 拦截（模型未调 `get_current_*`）。M4-00 由 `report_status.py` 直读账本确定性响应：
- **B1** `查看当前 ReportArtifact 和发布交付状态` → `match_report_status_sections` 匹配 ARTIFACT+PUBLICATION+DELIVERY；`_deterministic_direct_response` → `registry.direct_response_for_state` → 报告 Binding 直读账本；**0 langchain_model_call**（3.6s 确定性）；**0 get_current_* 模型工具调用**（直读，非模型工具）；**不触发 report_output_guard**（确定性响应绕过 guard，M3 拦截问题完全解决）；`digital_employee_playbook_direct_response` 事件 @15:02:11.771（response_kind=ledger_status）；返回 work_ed04971 delivered + 全部 5 Artifact / 4 Publication / 9 Delivery 账本事实；回复无 Draft 正文/storage_key/grant_hash/token/宿主路径。✓
- **B2** `查看当前报告任务状态` → 匹配全 8 section（Brief/Gap/Outline/Draft/Approval/Artifact/Publication/Delivery）；同样 0 model call、绕过 guard；含下一步提示（"提交 ReportDraft v5 审批"/"生成 ReportDraft v5 DOCX"）。✓
- 首次 B2 曾返回 no_match 澄清（瞬时异常，疑似首条文本差异/状态残留），重发后正常；probe 确认 `查看当前报告任务状态` 应 exact_action（`报告任务状态` ∈ owned_command_terms）。

### 边界探针（纯函数 probe）

- **动作命令不被截获**：`发布 ReportArtifact v1` / `交付 ReportArtifact v1 给我` / `生成 ReportDraft v1 DOCX` / `确认 ReportBrief v6` / `批准 ReportDraft v5` → `match_report_status_sections` 全部返回 None（不被只读解析器截获），`route_playbook` 全部 selected/exact_action（正确路由到报告 Playbook 走动作工具）。✓
- **伪造 envelope**（只有 `---Date:---` 无 `channel=$wecom`）：`authenticated_user_command_text` **正确不剥离**（返回完整伪造串）；`match_report_status_sections` 返回 None（确定性状态读取器正确拒绝，不泄露状态）。✓
- **非阻断发现**：`route_playbook` 的 exact_action 用子串匹配（`term in normalized_command`），未剥离的伪造串末尾"查看当前reportartifact"含 `reportartifact` → router 命中（selected/exact_action）。实际无安全影响（WeCom msg_signature 网关 + identity 网关，伪造消息无法到达 gateway；且确定性状态读取器已拒绝），但 router 偏离 spec"不得命中"。建议 Codex 让 router exact_action 只在 envelope 已剥离（合法 `channel=$wecom`）时匹配，或改严格匹配。

### 回归

- 查看消息队列仍走 fast-path；普通"你好"保留 DirectTurn（M3 已验证，本轮未变）。
- M1-06 delivered scope 不变：B1/B2 查询 work_ed04971 不创建新 WorkItem（work 17、0 新 event）。
- 现有 delivered 工作可继续查询（B1/B2 返回完整历史）。
- MCP、身份、记忆、文件、signed-link 交付不回归。

### 健康/脱敏

本轮 session（14:51 重启后）：**0 Traceback / 0 SIGBUS / 0 IntegrityError·UniqueViolation / 0 PackSnapshot conflict / 0 真 HTTP non-200**；0 direct_file / 0 PDF / 0 SSO；3 个 `digital_employee_service_discovery`（A1-A3）+ 1 个 `digital_employee_playbook_direct_response`（B1）事件（仅 ID/版本/Playbook/response_kind，无员工原文/OA/姓名/Prompt/凭据/宿主路径）；gateway 重启后 0 新 work event；MCP protected zero-diff。✓

### 非阻断（供 Codex 后续）

1. **report_status 呈现是原始账本 dump**（员工反馈，M3 #1/#3 功能性已解决但呈现待优化）：`_append_artifacts/publications/deliveries`（report_status.py:175-205）`for record in records:` 遍历**全量** records（B1: 5+4+9=18 行；B2: 全 8 section 更多），每行带全 64 字符 `artifact_id` sha256，且每行前缀"当前 ReportArtifact"即使 `current=false`（历史记录也标"当前"）。建议：突出 `current=true` 当前版本 + 历史用 1 行概要（"历史 N 个,均非当前"）+ ID 缩略/隐藏（同 M2 员工 hash 反馈）+ 中文标签表格化。
2. **router exact_action 子串匹配对伪造 envelope 也命中**（见边界探针）：建议 router 只在 envelope 已剥离时匹配，或改严格匹配。
3. **B2 首次 no_match 瞬时异常**：疑似首条文本差异/状态残留，重发正常；建议 Codex 复看 `enrich_state` → `route_playbook` 是否存在首条消息状态初始化竞态。
4. model call 日志 sink 跨 turn 不一致（前序轮已记录，M4-00 确定性路径本就不产生 model-call 日志，应以 `digital_employee_playbook_direct_response` 事件区分）。

### 结论

**M4-00 PASS — 关闭，进入 v0.1.1 RC 全生命周期复验。** 确定性服务响应加固活体生效：报告状态查询 0 model call + 绕过 guard + 直读账本（M3 反馈 #1/#3 功能性解决）；Job Charter 三段排版 + 动态 MCP 能力求交（M3 反馈 #2/#3 落实）；动作命令不被截获；健康/脱敏回归通过。非阻断：report_status 呈现为原始 dump（待摘要化）、router 子串匹配伪造 envelope（待严格化）。

## 七、M4-01：统一 Capability Registry

### 7.1 产品模型

M4-01 不再把 Skills/MCP 人为分成“普通对话能力”和“Playbook 专属能力”。一名部门数字员工只有
一个由 Profile 声明的能力池：

```text
Profile Capability Pool
|- Skills
|- Files
|- Department Knowledge
`- configured MCP-backed data/search

DirectTurn = 共享能力 + 普通对话
Playbook   = 共享能力 + WorkItem/合同/状态机/审批/审计
```

Playbook 不复制能力，只增加正式工作的治理约束。普通对话可以分析文件、查部门知识、在员工明确
请求后使用已配置的外部数据；进入正式 Playbook 后，仍使用同一底层能力，但必须经由版本化
流程工具登记 SourceRecord、Evidence、授权和审批事实。

### 7.2 运行时实现

新增 Profile 级 `CapabilityRegistry`：

- 以 Profile `tool_grants` / `data_scopes` / `knowledge_refs` 为授权上限；
- 与当前 MCP 配置的 server 标识求交，缺失配置时 fail-closed；
- 同一组业务化工具同时注入 DirectTurn 和所有 Playbook Agent；
- 不暴露通用 `call_mcp_tool`，不让模型拼接 MCP server/tool 名；
- 部门知识、聚源和公开搜索使用固定的服务端映射；
- 外部数据和公开搜索要求员工在最新消息中明确提出；
- 正式 Playbook 路由中，直接检索工具 fail-closed，改由 Playbook 工作流包装同一 MCP invoker。

Playbook Manifest 的 Skill/tool/data 字段仍保留为依赖声明、向后兼容和 fail-closed 校验，但不再
创建与 Profile 并行的第二个能力池。

### 7.3 M4-00 非阻断收口

- 状态回复只展示 `current=true` 当前记录，Artifact 不再展示 64 位 ID，历史记录合并为一行数量摘要；
- 带 `---Date:---` 但不带认证 `channel=$wecom` 前缀的伪造信封在路由前直接拒绝；
- 路由命令先做 Unicode NFKC 规范化并移除零宽控制字符，降低企微输入差异造成的瞬时 `no_match`。

### 7.4 版本与边界

- Pack `1.13.0` / Profile `1.12.0`；
- schema 保持 revision 11，无数据迁移；
- `digital_employee_id=industry-report` 和生产 Playbook 不变；
- 不修改 v0.1.0 的账本、状态机、审批、发布、交付和下载协议；
- MCP protected 文件保持 zero-diff；
- example 与 cookiecutter 模板同步。

## 八、Mac mini 活体复核 — 2998b9c（2026-07-22，glm-5.2）

**裁定：PASS。** M4-01 统一 Capability Registry 活体生效：单一 Profile 级能力池由 DirectTurn 与
正式 Playbook 共享；外部能力经显式授权门固定映射到已配置 MCP；正式 Playbook 直连能力 fail-closed；
M4-00 三项非阻断收口全部 live 修复。**M4-01 关闭，v0.1.1 RC 全生命周期可进入收尾复验。**

HEAD `2998b9c`（GitHub origin + GitLab company-gitlab 双端一致）。父基线 `3091e5b`（M4-00 PASS）。
schema 仍 rev11；Pack `1.13.0` / Profile `1.12.0`；`digital_employee_id=industry-report`；生产仅启用
`securities-industry-report@1`。

### 静态回归

Work 106、Example **309 passed+1 skipped**（新增 `test_capability_registry.py`）、Enterprise 86、
WeCom 78、Files+LangChain 74、template render 2；ty/ruff/docs 全绿；MCP protected
（`mcp_policy.py` + `tools.py`）zero-diff（vs `68d7b25` 与 `3091e5b`）。schema `LATEST_SCHEMA_VERSION=11`，
无 rev12 迁移；新 PackSnapshot `1.13.0` 注册，旧 `1.12.0` 及历史保留，0 immutable conflict。

### 部署

Mac mini smoke override（zhuchunlin 属信息技术部，committed profile 为战略发展部）：
`pack_version 1.13.0 → 1.13.0-mac-smoke.1`、`owning_org 战略发展部 → 信息技术部`（top-level + knowledge_refs），
仅本机、测试后 `git restore`。`scripts/run_gateway.sh` 启动（PYTHONPATH + JPype + JayDeBeApi），PID 75303，health OK。

### Capability Registry 确定性探针（真实 profile + 本机 mcp）

`get_settings().resolved_mcp_config_path()` 解析到 `examples/.../industry-report/.agents/mcp.local.json`
（注：含 department-knowledge；与根目录 `.agents/mcp.local.json` 不同）。availability =
`file_analysis=True / department_knowledge=True / licensed_external_data=True / public_search=True`。
注入工具 `analyze_file`、`list/search/read_department_knowledge`、`search_licensed_external_data`、
`search_public_information`；**不含** `call_mcp_tool` / `list_mcp_tools`。固定映射：`search_public_information`→
`tavily-search/tavily_search`（confirmed=True）；`search_department_knowledge`→`department-knowledge/knowledge_search`；
`search_licensed_external_data`→`gildata_datamap-data/FinGeneralQuery`。正式 Playbook（`route_status=selected`）
下调 public/ licensed 工具返回"当前消息已进入正式 Playbook…"，**0 MCP 调用**（fail-closed）。
Playbook `work_tools` 经 `capability_registry.invoke_mcp` 复用同一 invoker。

### Live — DirectTurn 普通能力（5A–5D，glm-5.2）

- **5A 部门知识**：`请从部门知识库检索证券行业数字化转型的目标…` → `tools/call knowledge_search` →
  `department-knowledge/knowledge_search`，返回《证券公司数字化转型三年规划（2026-2028）》核心目标表
  （95% 数字化覆盖 / 90% 客户触达 / 30% 运营效率等真实种子数据）。0 新 WorkItem。
- **5B 无来源（负向）**：`美联储最近的政策变化是什么？` → **0 外部调用**，从本会话历史结果答，附"非部门授权知识库"声明。
- **5C 公开搜索**：`请使用公开搜索查询美联储最新政策并给出来源。` → `tools/call tavily_search` →
  `tavily-search/tavily_search`，5 条带 URL 来源（CNBC/U.S.Bank/St.Louis Fed/Fed 官网/Livemint）。
- **5D Gildata**：`请使用 Gildata 查询证券行业最新经营数据。` → `tools/call FinGeneralQuery` →
  `gildata_datamap-data/FinGeneralQuery`，返回聚源 EDB 真实数据（营收 5,411.71 亿 / 净利润 2,194.39 亿，
  指标代码 1530030002 / 1530030011 / 1530097220）。
- 授权门实证：用户回复菜单"2"（非显式请求）→ 模型欲用 Gildata → 门返回"需在最新消息中明确提出使用 Gildata/聚源"（`_direct_external_refusal`）。授权 = 最新消息显式动作词 + 来源词。

### Live — M4-00 三项收口复测

- **首次 no_match（6.1）**：重启后首条 `查看当前报告任务状态` → `digital_employee_playbook_routed`→
  `digital_employee_playbook_direct_response`，**0 `langchain_model_call`**，无 no_match。M4-00 首次 no_match 回归已修复。
- **状态呈现（6.2）**：`查看当前 ReportArtifact 和发布交付状态` → 仅 `current=true` 记录，Artifact 不显 64 位 ID，
  历史"4 个/3 个/8 个"数量摘要，历史不再标"当前"，中文状态标签（已交付/已发布/已下载）。0 model call。
- **纯函数（6.3）**：`test_exact_action_normalizes_invisible_unicode_and_rejects_forged_envelope` 覆盖——零宽 `报​告任务状态`
  NFKC/Cf 规范化后 SELECTED/EXACT_ACTION；`---Date:---` 无 `channel=$wecom` 伪造信封 OUT_OF_SCOPE/NO_MATCH。

### Live — 身份四问 + burst

`你是谁` / `你能做什么` / `怎么使用你` 均 `digital_employee_service_discovery`、**0 model call**；
能力概览按 runtime availability 动态生成（部门知识已配置即列出）。`我是谁` 返回认证人类员工身份
（朱春霖 / zhuchunlin / 团队长兼数据架构师 / 信息技术部），**不**介绍 DE-SD-001、**不**显 employee hash。
burst（8 条连续消息，含 Gildata ~3min 长任务）：**exactly-once**（8 in ↔ 8 out），队列顺序背压，无冻结/重传风暴。

### 健康/脱敏/回归

本 session（16:13 重启后）：**0 Traceback / 0 SIGBUS / 0 IntegrityError·UniqueViolation / 0 PackSnapshot conflict /
0 ERROR·CRITICAL**；0 direct_file / 0 PDF / 0 SSO；回复与事件无 OA 原文、他人姓名、token、storage_key、
grant_hash、宿主路径、隐藏工具名（`call_mcp_tool`/`list_mcp_tools`）泄漏。`enterprise_work_items` 总数 17 不变，
近 40 分钟新建 0；已交付 `work_ed04971` 仍 `delivered` 可查；v0.1.0 账本/状态机/审批/Artifact/Publication/
Delivery/signed-link 协议零回归。MCP audit 与 enterprise events 分离。

### 非阻断（供 Codex 后续）

1. **stdout 块缓冲（运维项，影响日志可观测性）**：`run_gateway.sh` 以 `>> log 2>&1` 重定向，非 TTY 时 Python
   块缓冲，每 turn 结构化事件需等下一条消息推进才 flush，实时日志不可靠（亦为 M4-00 复核中 15:14"缺收尾日志"同源）。
   建议加 `python -u` 或 `PYTHONUNBUFFERED=1`（或 logging 用 line-buffered StreamHandler）。
2. **身份/能力回复排版（UX）**：员工反馈希望 section 标题与正文区分——`正式服务`/`协助能力`/`执行边界`/`工作准则`
   加粗成小标题、正文分点。WeCom markdown 支持 `**加粗**` 与列表，可在 `job_charter` 渲染层实现。
3. **Pydantic 序列化 UserWarning**：每个 model turn 出现 `EnterpriseAgentRuntimeContext.context` 字段
   `Expected none` 序列化告警（`functional_validators.py:835` / `main.py:475`）。非阻断，但建议复看该字段类型声明。
4. **model call 日志 sink**（前序 backlog 延续）：确定性路径无 model-call 日志属预期，以
   `digital_employee_playbook_direct_response` 事件区分；跨日紧凑时间戳格式（`HH:MM:SS` 无日期）会与历史混读，
   建议结构化事件统一带日期前缀。

### 结论

**M4-01 PASS — 关闭，v0.1.1 RC 全生命周期可进入收尾复验。** 统一 Capability Registry 活体生效：
DirectTurn 与正式 Playbook 共享同一 Profile 能力池（含 Skills/文件/部门知识/已配置 MCP），外部能力经显式
授权门固定映射、正式 Playbook 直连 fail-closed；不暴露通用 MCP 工具、不让模型拼接 server/tool 名。
M4-00 三项收口 live 修复。账本/状态机/审批/交付零回归，健康与脱敏通过。
