# 阶段 A 离线静态门禁 — 结果记录（2026-09-10 05:30–05:36 UTC）

执行者：Linux CC（用户授权代跑）；环境：production worktree /root/agentseek-enterprise @ b339228，
`.venv/bin/pytest`（3.13.14，零依赖变更），网关运行态未受影响。

| # | 用例 | 结果 | 实测 | GA 基线对照 |
| --- | --- | --- | --- | --- |
| A1 | contrib/agentseek-wecom/tests | **PASS** | 154 passed, 3 warnings (15.8s) | =154 ✓ |
| A2 | contrib/agentseek-files/tests | **PASS** | 47 passed (3.1s) | =47 ✓ |
| A3 | contrib/agentseek-enterprise/tests | **PASS** | 88 passed (7.1s) | =88 ✓ |
| A4 | contrib/agentseek-contextseek/tests | **PASS** | 47 passed, 1 skipped (2.7s) | =47+1skip ✓ |
| A5 | examples/enterprise_wecom_digital_employee/tests | **PASS** | 335 passed, 1 skipped (15.1s) | =335+1skip ✓ |
| A6 | 模板渲染定向（-k enterprise and wecom） | **PASS** | 5 passed, 50 deselected | 基线记录 2，测试有增补，全过 |
| A7 | ruff --no-fix + bash -n run_gateway.sh | **PASS** | All checks passed / OK | — |
| A8 | prod_check --env-file .env --strict | **FAIL→修复→PASS** | 见下 | — |

## A8 发现与处置（回归测试的第一个真实捕获）

- 首跑 1 FAIL：`AGENTSEEK_WECOM_LONG_CONNECTION_LOCK_PATH is missing or still a placeholder`
- 根因：2026-09-10 传输切换时该键被有意省略走代码默认（`runtime/wecom-long-connection.lock`），
  但生产预检合同要求显式配置；
- 处置：`.env` 追加 `AGENTSEEK_WECOM_LONG_CONNECTION_LOCK_PATH=runtime/wecom-long-connection.lock`
  ——与运行中进程实际使用的默认值**完全相同**，纯显式化，无行为变化，无需重启；
- 复跑（A8-prod-check-rerun.txt）：**exit 0，0 FAIL 0 WARN，"Production preflight passed"**。

证据文件：本目录 A-agentseek-{wecom,files,enterprise,contextseek}.txt、A-examples.txt、
A6-template-render.txt、A7-ruff.txt、A8-prod-check.txt（FAIL 原始）、A8-prod-check-rerun.txt（PASS）。

## 顺带修正的事实（供阶段 C 使用）

- `AGENTSEEK_MCP_CONFIG_PATH` 指向 `examples/enterprise_wecom_digital_employee/.agents/mcp.json`
  （**存在但 `mcpServers` 为空**）——C0 补建位置应是此文件（非 DE 级 mcp.local.json），
  条目内容照抄仓内 `.agents/mcp.department-knowledge.example.json`；
- 其余 prod_check OK 项确认：MinerU token/base URL 已配、pgvector 1024 维 bge-m3、
  部门知识 PG URL 已配、ONNX/tokenizer 路径有效。

**阶段 A 结论：8/8 PASS（A8 含一次真实配置缺口修复）。**
