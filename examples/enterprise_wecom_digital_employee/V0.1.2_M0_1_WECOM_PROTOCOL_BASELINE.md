# v0.1.2 M0.1：企微协议基线

## 为什么插入 M0.1

v0.1.2 M0 已完成平台路线冻结，原计划下一步是 M1 组织授权与
Identity Provider 抽象。在进入跨部门身份工程前，需先修正企微渠道的会话
和消息语义基线，否则群聊会话可能按发送人串联，并且引用消息会在
进入 Agent 前丢失。这两个问题会直接影响 M1 的授权边界，因此将 M0.1
作为 M1 的前置切片。

## 官方协议基线

- [AI 机器人回调与消息回复](https://developer.work.weixin.qq.com/document/path/100719)：
  区分单聊与群聊，群聊以 `chatid` 定位会话；回调包含文本、混合、
  引用和媒体等语义。
- [response_url 异步回复](https://developer.work.weixin.qq.com/document/path/101138)：
  URL 仅可使用一次，有效期一小时，仅支持 Markdown 和模板卡片。
- [AI 机器人长连接](https://developer.work.weixin.qq.com/document/path/101463)：
  `aibot_respond_msg` 是对入站消息的回复；`aibot_send_msg` 的主动发送仅支持
  Markdown 和模板卡片，且需要用户曾在对应会话中联系机器人。
- [open_userid 转 userid](https://developer.work.weixin.qq.com/document/path/101521)：
  用户身份解密是成员身份层能力，不得覆盖群聊会话边界。
- [自建应用发送消息](https://developer.work.weixin.qq.com/document/path/90236)
  与[接收消息](https://developer.work.weixin.qq.com/document/path/90238)：
  自建应用可按成员、部门或标签主动推送多种消息，但不支持流式回复。

| 渠道 | 入站 | 对当前会话回复 | 主动发送 | 当前实现 |
| --- | --- | --- | --- | --- |
| AI 机器人 callback | 单聊、群聊；文本、混合、引用、媒体等 | 回调流式回复；`response_url` 一小时内一次 Markdown/模板卡片 | 无任意成员主动推送 | 已实现，本切片修正会话和引用语义 |
| AI 机器人长连接 | 单聊、群聊与媒体 | `aibot_respond_msg` 支持流、卡片、Markdown 和媒体 | `aibot_send_msg` 仅 Markdown/模板卡片，且需用户先联系 | 未实现，仅冻结能力声明 |
| 企微自建应用 | 应用消息回调 | 无流式回复 | 可按成员、部门、标签发送多种消息 | 未实现，后续独立立项 |

## 本切片范围

1. 群聊 session 使用 `aibotid + chatid` 隔离，不再使用发送人作为群聊
   会话主键。
2. 明文 `userid` 回填只更新成员身份，不改写群聊 session 和
   `chat_id`。
3. 引用文本、混合消息和语音转写进入 Agent 正文；引用媒体只保留
   安全元数据，不重新下载历史签名 URL。
4. 安全回调上下文保留 `chatid` 和引用语义，仍排除 `response_url`
   和媒体签名 URL。
5. 修正长连接能力矩阵，明确“回复类型”和“主动发送类型”不同。

## 不在本切片

- 不实现 AI 机器人长连接 transport；
- 不新增企微自建应用 channel；
- 不改造持久化 inbox/outbox、租约和多实例恢复，这些仍属于 M4；
- 不改变 M1 的组织授权与 Identity Provider 目标。

## 验收门槛

- 同一发送人在不同群聊中产生不同 session，同一 `chatid` 在不同
  `aibotid` 下也不共享 session；
- 单聊仍使用 `wecom:{userid}`，现有记忆兼容；
- userid 解密后群聊 session 不变；
- 引用消息进入 Agent 可见正文，且上下文中无 response capability
  和签名 URL；
- callback 与 long-connection 能力声明有协议测试；
- `contrib/agentseek-wecom` 和企微数字员工样例回归通过。

## 后续顺序

M0.1 通过后回到原路线 M1。长连接实现和自建应用渠道分别立项，
先补齐持久化 inbox/outbox、幂等、重启恢复和多实例所有权，再打开
新 transport 的生产开关。

## Mac mini Claude Code 复验交接

代码基线：

- 分支：`enterprise/v0.1.2-upstream-core-sync`；
- 代码 commit：`6861744a5d18b0e584d31b95a9930110335248e9`；
- 父基线：`afcfe3e`，必须是代码 commit 的 ancestor；
- 仅部署该研发分支复验，不合入 `production`，不创建或移动 GA tag。

保护项：

- 保留 Mac mini 本机 `.env`、`.agents/mcp.local.json`、JDBC/模型文件、
  runtime 数据和 LaunchAgent 配置；
- 不提交密钥、业务文件、日志原文或临时部署覆盖；
- 先记录当前 commit、PID 和健康状态，仅在静态门禁通过后重启。

拉取与静态门禁：

```bash
git fetch company-gitlab enterprise/v0.1.2-upstream-core-sync
git switch enterprise/v0.1.2-upstream-core-sync
git merge --ff-only company-gitlab/enterprise/v0.1.2-upstream-core-sync
git diff --exit-code 6861744 HEAD -- \
  contrib/agentseek-wecom/src contrib/agentseek-wecom/tests
git merge-base --is-ancestor afcfe3e 6861744
uv run ruff check contrib/agentseek-wecom/src contrib/agentseek-wecom/tests
uv run ty check contrib/agentseek-wecom/src contrib/agentseek-wecom/tests
uv run pytest -q contrib/agentseek-wecom/tests
PYTHONPATH=examples/enterprise_wecom_digital_employee/src \
  uv run pytest -q examples/enterprise_wecom_digital_employee/tests
```

`HEAD` 可以包含后续纯文档交接 commit，但其 tree 中的代码必须与
`6861744` 一致。预期企微组件 `82 passed`，数字员工样例
`325 passed, 1 skipped`。

部署与活体 Oracle：

1. 执行本机 `prod_check` 并记录经脱敏的 PASS/WARN/FAIL；不为了过检查
   修改或提交 `.env`。
2. 按现有 `scripts/run_gateway.sh`/LaunchAgent 流程停旧起新，确认只有
   一个网关监听 `:12000`，健康检查通过，无 traceback/port conflict。
3. 单聊发送“`M0.1单聊基线：请回复当前会话类型`”，确认有且仅有
   一条终态答复，无异常。
4. 由同一测试成员在群 A 和群 B 各发一条唯一文本；从脱敏结构化事件
   确认两者 session 都是 `wecom:<aibotid>:group:<chatid>` 且不同，解密
   userid 后 session 不变。再回到群 A 追问群 A 文本，不得引用群 B 内容。
5. 在单聊或群聊中引用一条唯一文本，发送“`请复述我引用的内容`”；
   确认 Agent 能复述引用文本，且不展示 URL、response code 或内部字段。
6. 检查本轮日志和 trace 的脱敏结果：不得出现 `response_code=`、签名
   媒体 URL、Token、EncodingAESKey、API key 或用户消息原文。

PASS 需同时满足静态门禁、部署健康、单聊兼容、跨群隔离、引用语义
和脱敏检查。若缺少第 4/5 项的真实企微交互，必须标记 `BLOCKED`，
不能以单测代替活体 PASS。

反馈要求：将时间、主机、精确 commit、每项结果、脱敏证据和最终
`PASS/BLOCKED` 追加到本文档。只提交该验证文档；若发现缺陷，记录
复现消息、时间和代码位置，不直接修改生产代码。将验证 commit 推送
到 GitLab 和 GitHub 同名分支，并用 `git ls-remote` 确认双端一致。

---

## Linux 验证机复验记录（2026-08-27）

### 环境

- 日期/时区：2026-08-27，服务器时区 UTC（日志时间均为 UTC）；
- 发行版/架构：Ubuntu 22.04.5 LTS，x86_64；hostname `agent01-hh`
  （192.10.50.171/24）；
- 验证 HEAD：`da559fe8cd26bd0b6219e1e74509e8ebd2bec5df`，分支
  `enterprise/v0.1.2-upstream-core-sync`，工作区 clean；
- ancestry：`afcfe3e` 与 `6861744a5d18b0e584d31b95a9930110335248e9`
  均为 HEAD 祖先；HEAD 相对 `6861744` 在
  `contrib/agentseek-wecom/{src,tests}` 零差异；
- Python 3.13.14（uv 管理，`uv sync --locked --all-packages --all-extras
  --group plugins`）；系统 uv 0.11.29；
- Java：OpenJDK 11.0.32（`AGENTSEEK_IDENTITY_DM_JDBC_JAVA_HOME` 指向，
  sidecar 实际使用）；主机另有 JDK 26.0.1（本机旧部署曾长期使用，
  作为兜底）；
- 旧实例处理：本机原有 `industrydev` 网关（用户 `iemp`，:12188，
  uv+python PID 1062381/1062384，nohup 守护、无 systemd）已按规程
  SIGTERM 优雅停止（1 秒退出），端口释放；该实例服务的机器人
  （callback `/industrydev/callback/demo/industrydev`）与本轮验证的
  机器人不同，不构成双实例冲突。

### Linux 环境适配（.env 相对 Mac mini 基线，密钥逐字节未动）

1. 显式新增 `AGENTSEEK_WECOM_TRANSPORT_MODE=callback`（默认即 callback）；
2. `AGENTSEEK_IDENTITY_DM_JDBC_JAVA_HOME` 由 macOS homebrew 路径改为
   `/usr/lib/jvm/java-11-openjdk-amd64`（apt 安装 openjdk-11-jdk-headless）；
3. `AGENTSEEK_IDENTITY_DM_JDBC_JAR`、`AGENTSEEK_MCP_CONFIG_PATH`、
   `AGENTSEEK_CTX_BGE_M3_ONNX_MODEL_PATH/TOKENIZER_PATH` 改为本机绝对
   路径（JDBC 用仓库 vendor 驱动；ONNX/tokenizer 复用本机已有
   `industrydev` 模型目录；MCP 用提交版空服务清单 `mcp.json`）；
4. 六个 PostgreSQL URL 由 Mac mini `localhost`（本机不可达，且其
   `agentseek_app` 凭据在本子网共享 PG 上认证失败）改为本机旧部署
   验证过的 `192.10.50.60:5432/ai_memory`（PG 17.10 + pgvector，
   `contextseek_pgvector_items` 已存在）；未修改该库任何 schema；
5. `AGENTSEEK_WORK_ARTIFACT_DELIVERY_MODE=signed_link → disabled`：
   README 要求仅在 HTTPS base 已路由到本网关时启用 signed_link，
   反代切换前不满足；且共享库无 work 台账表（见已知问题 2）；
6. `AGENTSEEK_WORK_ENABLED=true → false`（项目默认值）：共享库无
   work 台账（schema revision 0，期望 11），按轮加载失败会令每轮
   回复变成错误文案（见已知问题 2）；验证规程禁止自动迁移外部
   共享库，M0.1 验收项不依赖 work 运行时；
7. `AGENTSEEK_LANGFUSE_ENABLED=true → false`：本机无 Langfuse
   （localhost:3000 拒连），OTel 导出重试在日志刷 SDK Traceback；
   本地 JSONL 事件不受影响（README 规定的降级路径）。

### 静态门禁与预检

- `ruff check contrib/agentseek-wecom/{src,tests}`：通过；
- `ty check contrib/agentseek-wecom/{src,tests}`：通过；
- `pytest contrib/agentseek-wecom/tests`：82 passed；
- `PYTHONPATH=… pytest examples/enterprise_wecom_digital_employee/tests`：
  325 passed, 1 skipped；
- `prod_check`：初始配置 46 OK / 0 WARN / 0 FAIL；最终配置（适配 5–7
  后复跑）41 OK / 0 WARN / 0 FAIL。

### 部署与健康

- 启动方式：`AGENTSEEK_GATEWAY_LOG=$PWD/runtime/gateway.log nohup
  scripts/run_gateway.sh`（Linux 显式日志路径，避免 macOS
  Library/Logs 默认），`runtime/gateway.pid` 记录 supervisor PID；
- 网关进程 PID 1338564（supervisor 1338554），唯一监听 `0.0.0.0:12000`；
- `GET /health` → `{"ok":true,"channel":"wecom","enabled":true}`；
- 回调路由本地探测：无签名 GET→422、空 POST→400（路由挂载正常）；
- 外部链路：企微后台回调 URL（`test.wkzq.com.cn:9443` +
  `/ai-emp/callback/demo/<botid>`）经外部反向代理转
  `192.10.50.171:12000`，由维护者切换后生效；日志确认真实回调
  POST 来源 `192.10.50.60`（反代出口），全部 200。

### 活体验证

A. 单聊（08:57 UTC 前后）：消息「M0.1单聊基线：请只回复“单聊正常”」。
1 次回调 POST→200，无重试、无重复 msgid；DM sidecar（Java 11）启动，
`open_userid→userid` 解析成功并缓存；终态回复恰 1 条「单聊正常」
（response_url 一次性投递 succeeded，11.5s）；会话
`wecom:<userid>` 与既有单聊记忆兼容。**PASS**

B. 群聊隔离（同一测试成员，群 A / 群 B）：

| 步骤 | 会话 key（事件哈希） | chat key（哈希） | 回复 |
| --- | --- | --- | --- |
| 群 A 首条（ALPHA-M01） | `hmac-c625fe…` | `hmac-9e5d02…` | 确认仅当前群 |
| 群 B 首条（BETA-M01） | `hmac-e575eb…`（≠A） | `hmac-a7bd3a…`（≠A） | 确认仅当前群 |
| 群 A 追问口令 | `hmac-c625fe…`（与首条一致） | `hmac-9e5d02…` | 只答 ALPHA-M01，全文无 BETA-M01 |
| 群 B 反向追问 | `hmac-e575eb…`（与首条一致） | `hmac-a7bd3a…` | 只答 BETA-M01，日志中 ALPHA 0 命中 |

群会话格式 `wecom:<aibotid>:group:<chatid>`；同一发送人两群会话独立、
回群后延续；userid 解密回填未改变群会话边界。**PASS**

C. 引用消息（单聊）：先发「引用测试原文：REFERENCE-M01-ONLY」，再以
企微“引用”该消息发送「请复述我引用的消息内容。」。Agent 终态回复
复述出 `REFERENCE-M01-ONLY`，恰 1 条终态回复；回复与日志均未出现
raw payload、URL、response code 或内部字段；引用文本未被当作系统
指令执行。**PASS**

D. 稳定性与脱敏（第二次重启后的验证窗口）：

- 回调 HTTP：7 次 POST 全部 200，无 5xx、无重试风暴、无
  `wecom_duplicate_msgid`；
- 稳定性：Traceback 0、端口冲突 0、SIGBUS 0、进程退出 0；
  网关存活、唯一监听；
- 敏感扫描（gateway.log / enterprise-events.jsonl / mcp-audit.jsonl，
  仅统计）：企微 Token、EncodingAESKey、App Secret、Tavily/Langfuse/
  MinerU 密钥、数据库密码、namespace secret、签名媒体 URL 均 0 命中；
  `response_url` 字样 9 处命中均为事件名 `wecom_response_url_delivery`
  而非 URL 值；事件文件仅含哈希 key 与字符计数。**PASS（含已知
  问题 1 的窗口外泄漏记录，见下）**

### 最终判定

**PASS** —— commit/ancestry、静态门禁、示例测试、prod_check、部署
健康（单实例）、单聊兼容、群聊双向隔离、引用语义、脱敏检查全部
通过。

### 已知问题与复现

1. **（较高优先级，安全）turn 阶段未处理异常会把模型 API Key 明文写入
   gateway.log。**
   - 复现：`AGENTSEEK_WORK_ENABLED=true` 且共享库无 work 台账时，任一
     消息触发 `work ledger schema revision 0 is not ready; expected 11`
     （`examples/enterprise_wecom_digital_employee/src/
     enterprise_wecom_digital_employee/work_composition.py:1742`），
     `bub.framework` `process_inbound` 以 rich 回溯打印局部变量，
     `ProjectSettings(api_key='sk-…')` 明文出现（本轮 gateway.log
     第 244/253 行，共 2 次，时间为适配修复前的 07:54:40 UTC 窗口）；
   - 代码位置：`examples/enterprise_wecom_digital_employee/src/
     enterprise_wecom_digital_employee/settings.py:37-44`（`api_key`
     为普通 `str`，未用 `SecretStr`/repr 排除），暴露面为框架异常
     局部变量打印；
   - 影响：任何未处理 turn 异常都可能重复该泄漏；当前
     `runtime/gateway.log` 权限 644 且含 2 处明文 key，建议验证后
     轮转/收紧权限并轮换该 key；
   - 本轮仅记录，未修改生产代码。
2. **（环境项）共享 PG 无 work 台账表**：`ai_memory` 库 schema
   revision 0。属于部署环境与 Mac mini 本机库的差异，非代码缺陷；
   如需在本机验证 work/artifact 流程，需先由 DBA 决定建表方式。
3. **（观察项）`session.run.outbound` INFO 行打印回复全文**：为 bub
   框架既有行为；回复可能回显用户口令类内容。事件层已哈希、不含
   明文；如需收紧应改框架日志级别，不在本切片范围。

### 验证提交

- 本记录为验证文档追加；仅提交本文档，不含配置、日志或业务数据；
- Linux 验证提交 `bac573c` 初始仅推送到 GitLab `origin`（该机
  尚未配置 GitHub 远端）；后续集成提交必须将本验证记录
  同步到 GitLab/GitHub 同名分支，并核验双端为同一 commit。
