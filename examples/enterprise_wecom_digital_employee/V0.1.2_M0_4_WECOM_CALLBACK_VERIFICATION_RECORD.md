# v0.1.2 M0.4 Callback 加固 Linux 复验记录（2026-08-31，UTC）

## 最终判定：PASS

## 环境摘要

- 主机 `agent01-hh`（Ubuntu 22.04.5 x86_64，192.10.50.171/24）；
- Python 3.13.14（uv 管理），OpenJDK 11.0.32（DM sidecar）；
- 反代链路：企微 → `test.wkzq.com.cn:9443`（192.10.50.60）→ 本机
  `:12000`（bot 精确路径 location）；
- Linux 唯一网关实例运行，Mac mini 同机器人实例保持停止；
- M0.3 五项 durable 配置沿用，未生成或替换任何加密材料。

## commit 链

- 分支：`enterprise/v0.1.2-wecom-callback-hardening`（GitHub 拉取，
  fast-forward，无历史改写；已补推 GitLab 同名分支）；
- HEAD：`201f37a03cf2693d532fb658f2c0c4f86e525cc8`
  （`feat(wecom): harden callback media and card recovery`）；
- 父生产基线：`05e8086ec2bf47329ffea6362c7757abe83b000e` 为 HEAD 祖先 ✓；
- `git diff --check 05e8086..HEAD` 干净 ✓；
- 双端 `ls-remote`：GitLab 与 GitHub 同名分支均指向 `201f37a` ✓。

## 静态门禁（脱敏）

| 门禁 | 结果 |
| --- | --- |
| `pytest contrib/agentseek-wecom/tests` | **113 passed**（1 条允许的弃用警告） |
| 数字员工样例（PYTHONPATH） | 329 passed, 1 skipped |
| Ruff（wecom + 示例） | 通过 |
| ty（wecom src/tests + 示例 src，带 PYTHONPATH） | 通过 |
| `make docs-test` | 文档构建通过 |
| 模板渲染（`test_templates_render.py -k enterprise and wecom`） | 5 passed |
| `prod_check`（Linux 最终 .env） | 43 OK / 0 WARN / 0 FAIL |

四个关键确定性 Oracle 显式复跑通过：
`test_media_io_starts_only_after_first_callback_and_reserves_session_order`、
`test_pending_template_card_is_sent_and_finalized_after_restart`、
`test_sent_template_card_recovers_business_action_without_resending`、
`test_ambiguous_sending_template_card_is_blocked_without_resending`
（4 passed）。

## 部署健康

- `:12000` 唯一监听；`/health`
  `{"ok":true,"channel":"wecom","enabled":true}`；
- `runtime/wecom-messages.sqlite3` 权限 **600**，`gateway.log` **600**；
- 启动窗口 Traceback/ERROR 0 条。

## 活体时间线与结果

### 1. 媒体首包与会话顺序（单聊，PDF + 后续文本）

| 时间 (UTC) | 事件 |
| --- | --- |
| 11:47:51.240 | file 回调入站 |
| 11:47:51.287 | **首包立即确认接收**（到达后 46ms，message_received 先于任何媒体 I/O） |
| 11:47:52.041 | MinerU v4 提交（**在首包之后**；下载/解密/OCR 未阻塞回调） |
| 11:47:53.549 | 后续文本入站，同会话排队 |
| 11:48:02.578 | 文件解析完成（extract done / media intake succeeded） |
| 11:48:19.287 | 文件回合终态（400 字符） |
| 11:48:21.521 | 文本回合终态（4 字符「顺序正常」） |

文件在前、文本在后；无 `file-poll` 绕过 session 队列的直接调度。
**PASS**。

### 2. M0.3 回归

单聊恰一条终态「正常」；群 A `hmac-c625fe…` 只答 ALPHA-M04R、群 B
`hmac-e575eb…` 只答 BETA-M04R（双向追问零交叉）；引用复述
`REFERENCE-M04R-ONLY`；身份解析（identity cache hit）不改写群/单聊地址。
**PASS**。

### 3. SIGTERM 释放 lease 并立即恢复（自动触发）

首轮手动执行时慢任务在 SIGTERM 前已完成（未截获在途回合）；改用
durable 库轮询自动触发：processing 记录出现后 **0.5s 内**发出 SIGTERM
（11:54:32）。关停日志含 `channel.manager stopped`，旧进程 13s 完整退出；
停止后 processing 记录 **lease_owner=NULL**（优雅释放）；新进程
11:54:47 启动后约 30s 内恢复完成，最终 Markdown **恰好投递 1 次**
（员工确认只收到一次），inbox **27/27 completed**、outbox **27/27
delivered**。**PASS**。

### 4. 模板卡片事件探针（临时配置，已清除）

- 临时设置 `AGENTSEEK_WECOM_TEMPLATE_CARD_EVENT_PROBE_TRIGGER`（非密钥）
  并重启；
- 首次尝试因消息含多余空格未命中触发词（员工输入问题，非代码缺陷）；
  精确发送后命中；
- 12:11:44.357 探针卡片投递 succeeded（恰 1 张 `button_interaction`，
  按钮 key=`M04_CONFIRM`，见 channel.py 按钮 key 定义）；
- 员工点击「确认交互」→ 12:11:50.419 **msgtype=event 回调入站，回调
  首包 35ms 内返回**（满足 5s 要求，企微不重试卡片事件）；
- **恰一个 card 事件回合**（1× message_received(msgtype=event) + 1×
  stream_finished），Agent 结构化解析出 `button_interaction` 与
  **操作标识 M04_CONFIRM**，单聊地址保持，回复不含 payload/URL/响应码；
- 验证后已清空探针配置并重启（配置行数复核为 0）。**PASS**。

### 5. 卡片重启恢复状态机

`WORK_ENABLED=false`，未迁移共享库、未开启 Work。三态由确定性 Oracle
覆盖（pending 重启发送一次并完成提交；sent 重启不重发只补提交；
sending 结果不明 blocked 不重发不提交），均已通过（含于 113 passed 与
显式复跑）。报告交付卡片恢复未活体验证（需 signed-link + Work schema，
当前环境不满足，按文档不强行开启）。**PASS（确定性覆盖）**。

## 脱敏检查（只报命中数）

- `gateway.log`（M0.4 窗口）：模型 Key、企微 Token/EncodingAESKey/
  AppSecret、PG/DM 密码、namespace secret、`response_code`、下载/媒体
  URL、MinerU JWT **全部 0 命中**；
- durable SQLite：卡片 action payload（`M04_CONFIRM`、
  `button_interaction`、按钮文案）、`response_url`、签名、session 明文、
  消息正文（含本轮散文与引用文本）、凭据 **全部 0 命中**（全密文）；
- enterprise-events.jsonl：同上模式 0 命中。

## 共享 schema 只读核验

`ai_memory` 表与扩展清单与既往完全一致，本轮零写入、零迁移。

## 当前运行状态与回滚

- 运行 HEAD：`201f37a`（分支 `enterprise/v0.1.2-wecom-callback-hardening`）；
- 网关单实例 `:12000`，`/health` 正常，durable SQLite（600）持续工作，
  收官状态 inbox/outbox 全终态（29/29）；
- 探针配置已清除，`.env` 其余内容与 M0.3 复验时一致；
- 回滚：`git switch production && git merge --ff-only origin/production`
  后用现有 `run_gateway.sh` 重启；不删除、不手工修改 durable SQLite
  （如需处置移入权限 700 受限归档）；
- 验证提交：仅本记录文档，推送 GitHub 与 GitLab 并 `ls-remote` 核验。
