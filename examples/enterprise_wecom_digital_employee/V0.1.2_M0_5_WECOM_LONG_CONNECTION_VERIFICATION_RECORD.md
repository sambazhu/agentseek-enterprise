# v0.1.2 M0.5 AI Bot 长连接 Linux 复验记录（2026-09-01，UTC）

## 最终判定：BLOCKED（fb6e851 后语义层隔离已证生效；残留违例来自上轮泄漏回复污染的群 A 自省会话记忆，oracle 字面仍不通过——需换净群或授权清理后复验）

## 环境摘要

- 主机 `agent01-hh`（Ubuntu 22.04.5 x86_64）；Python 3.13.14（uv），OpenJDK 11.0.32；
- 拓扑：Callback（production `0d16633`，`:12000`，全程未动）+ M0.5 canary
  （独立 worktree `/root/agentseek-m05-long-connection` @ `b097e87`，`:12001`）；
- 新机器人：BotID 脱敏指纹 `sha256[:12]=f2cbdeb2fda2`（前缀 `aib3eId…`），
  长连接模式，专用 Secret（已写入 canary `.env`，未输出）；
- 映射：BotID 指纹 `f2cbdeb2fda2` → deployment_role=M0.5 transport canary →
  digital_employee_id=industry-report → Profile=战略发展部数字员工
  （单部门数字员工的 Transport canary，非第二名数字员工）。

## commit 链

- 双端功能分支 tip：GitLab = GitHub = `b097e870392fc8c5f62c29cca5216b20bb09885d` ✓；
- M0.5 代码 HEAD `d05432a` 在链上（`f979438`→`d05432a`→`a698472`→`b097e87`）；
- 父 production `0d16633` 为祖先 ✓；`diff --check` 干净 ✓；`uv lock --check` ✓。

## 静态门禁

| 门禁 | 结果 |
| --- | --- |
| 企微组件 | **127 passed**（注：4 个长连接测试默认绑 `:12000`，与运行中的 Callback 冲突报 Errno 98；以 `AGENTSEEK_WECOM_PORT=12100` 环境别名重跑后全过——环境冲突，非代码缺陷，未改代码） |
| 数字员工样例 | 332 passed, 1 skipped |
| Ruff / ty / docs-test / `bash -n` | 全过 |
| 模板渲染（enterprise+wecom） | 5 passed |
| 企微集成 | 3 passed |
| prod_check（canary .env，启动前/收官后各一次） | 0 WARN / 0 FAIL |

## 隔离部署

- 独立 worktree + 独立 `.env`（600）；durable/namespace 两个新 Secret 本机生成，未复用任何既有凭据，未输出；
- 路径审计：全部可写路径位于 worktree `runtime-m05`（wecom 侧按 cwd 解析到 worktree 根，enterprise 侧按项目根解析到 `examples/…/runtime-m05`，两者均在 worktree 内）；与 Callback 零重叠，无 `BLOCKED_DEPLOYMENT_CONFIGURATION`；
- Callback 的 `.env`、日志、runtime、durable SQLite 零变化（文件 mtime 停留在 08-31 12:12）。

## 活体结果

1. **单聊**：恰一条终态「长连接正常」，session 仍为 `wecom:{plaintext_userid}`
   （不含 BotID/transport）✓；inbox/outbox 终态 ✓。
2. **活动流**（1000 字散文）：首包（message_received）先于模型调用约 1.05s；
   同一 stream.id 分片流式（员工目视确认），无重复消息，终态 delivered
   （outbox message_type=`long_connection_stream`）✓。
3. **员工 A/B**（zhuchunlin / xiongjijian）：各自只回收各自标识，零交叉；
   两个独立 session（`hmac-55de…` / `hmac-1405…`）；B 的 DM 身份解析正常 ✓。
4. **群 A/B：❌ 失败（本 BLOCKED 项）**：
   - 群 A 追问首轮未召回（回复称"只收到当前提问"）；重测后群 A 回复同时列出
     GROUP-ALPHA-M05 与 **GROUP-BETA-M05** 并引用原始 chat_id —— 违反
     "双向追问零交叉"；
   - 排查证据链：会话地址两群各自稳定且不同 ✓；短期记忆 SQL 严格
     `WHERE session_id=?` 且实测群 A 加载仅含 ALPHA 行 ✓；canary 无任何
     contextseek 检索事件，但 seekdb 存储实测含 GROUP-BETA×4、GROUP-ALPHA×2
     （同一员工全部轮次）→ 泄漏源为 **ContextSeek `SCOPE_MODE=enterprise_user`
     的员工范围语义检索**（跨会话/跨群注入，README 明示 scoped to employee
     rather than session）；
   - 定性：既有语义记忆架构在"同一员工多群"场景的设计缺口；canary 全新小存储
     使 k=5 必然命中同日跨群轮次，确定性暴露（生产 Callback 大存储仅降低命中
     率，并非不存在）。修复方向建议：群聊场景按
     `tenant_id+digital_employee_id+conversation_id` 收紧语义检索范围或对
     检索命中做会话过滤（与任务书第三节未来作用域一致）；
   - 群 B 侧追问正常（只答 GROUP-BETA-M05）。
5. **引用**：复述 REFERENCE-M05-ONLY，无 payload/URL/req_id/回复能力进入
   Agent ✓。
6. **媒体**（PDF+文本）：首包 46ms 先于 MinerU 提交；文件回合（332 字符）先于
   文本回合（"顺序正常"）✓；长连接媒体同级 aeskey 语义由确定性测试覆盖。
7. **主动消息与卡片事件**：探针命中后恰好 1 条主动 Markdown + 1 张
   button_interaction（key=`M05_CONFIRM`）；点击后恰 1 个 msgtype=event 回合，
   终态经 `wecom_long_connection_proactive_delivery`（aibot_send_msg Markdown，
   80 字符）到达同一会话；无 respond_update 残缺消息；outbox 计数
   `long_connection_proactive_markdown` delivered=2（探针 1 + 卡片终态 1）、
   `…_template_card` delivered=1 ✓；探针配置已清除并重启（复核 0 行）。
8. **SIGTERM 生命周期**（durable 轮询自动触发，poll#1 即 SIGTERM）：
   Callback `:12000` PID 1357387 全程不变 ✓；canary 21s 完整退出
   （manager cancelled/stopped 齐备）；processing 记录 `lease_owner=NULL`
   （lease 释放）；lock 释放；重启后 9s 重新订阅（subscribed=true）；恢复
   立即认领，终态恰一次投递（892 字符，员工确认只收到一次）；inbox 18/18
   completed、outbox 全 delivered ✓。

## 脱敏扫描（只报命中数）

- canary gateway.log：模型 Key、长连接 Secret、Callback Token/AESKey/AppSecret、
  PG/DM 密码、`response_code`、`req_id`、下载/媒体 URL 全部 **0 命中**；
  `M05_CONFIRM` 2 次命中均为框架 `session.run.outbound` INFO 行的**回复正文**
  （M0.1 起既有观察项，非卡片 action payload 泄漏）；
- durable SQLite：上述全部模式 **0 命中**（全密文）；
- enterprise events：**0 命中**；
- 授权会话存储（短期记忆/seekdb）：仅含预期会话正文，凭据类 **0 命中**，
  路径均位于 runtime-m05，权限 600/700；
- 共享 PostgreSQL：表清单与既往一致；canary 语义层走 seekdb（未使用 pgvector
  客户端），canary 对共享库零写入；Callback 侧文件与 durable SQLite 零变化。

## 结论表述（按任务书边界）

单个部门数字员工**可以通过**独立 AI Bot 使用长连接 Transport，并与现有
Callback 实例并行运行（transport 层全部 Oracle 通过）；但**群聊隔离 Oracle
失败**——同一员工的跨群语义记忆泄漏使群 A 回复包含群 B 内容，判 BLOCKED，
待语义检索作用域修复后重验该 Oracle（其余项无需重测）。

## 当前运行状态与回滚

- Callback：production `0d16633`，`:12000`，PID 1357387，健康，未重启未切换；
- Canary：`b097e87` + 本记录，`:12001`，subscribed=true，durable（600，
  schema revision 2）持续工作；探针配置已清除；
- 回滚：优雅停止 `:12001` → 确认端口/lock/WebSocket 释放 → runtime-m05 移入
  权限 700 受限归档（不删除 SQLite/日志）→ 由管理员停用新机器人或保留不连；
  不触碰 Callback；
- 验证提交：仅本记录文档，推送 GitLab 与 GitHub 同名功能分支并 ls-remote 核验。

## 修复候选（待 Linux 单项复验）

- 修复 commit：`fb6e851` (`fix(contextseek): isolate semantic memory across group chats`)；
- 修复边界：单聊保持匿名 `tenant_key + user_key` 语义连续性；群聊追加
  已有的匿名 `session_key`，形成员工与当前群会话双重隔离；
- fail-closed：群聊缺失合法 `session_key` 时不检索、不写入，不退回员工级
  大作用域；raw userid/chatid/BotID/transport 不进入 scope；
- 数据边界：不修改 SeekDB 或 pgvector schema，不迁移、删除旧员工级记忆。
  修复后的群聊不再读取旧大作用域；
- 本地确定性门禁：定向 `3 passed`，ContextSeek `46 passed, 1 skipped`，
  WeCom + Enterprise `214 passed`，数字员工样例 `332 passed, 1 skipped`，
  改动范围 Ruff/ty、`make docs-test` 通过；
- 待验 Oracle：保留现有 `runtime-m05` 语义库，由同一员工对群 A/B 写入
  `GROUP-ALPHA-M05R` / `GROUP-BETA-M05R` 并双向追问。零交叉后才可将本记录
  改判 PASS；其余 M0.5 活体项无需重测。

---

## fb6e851 Linux 群隔离修复复验（2026-09-01，UTC）

### 判定：BLOCKED（残留违例；语义层修复本身已验证生效）

- 复验 HEAD：`adca4ffefff9da9b5dd748a2cf61d8bc9c3e812b`（双端同名分支
  tip 一致）；`2e4f3b3`（前次 BLOCKED 记录）为祖先 ✓；`fb6e851`
  在链 ✓；canary worktree fast-forward 更新，未 force push；
- 静态定向门禁：`enterprise_group_scopes_are_isolated` /
  `enterprise_direct_chat_keeps_employee_scope` /
  `enterprise_group_scope_fails_closed` **3 passed**；
  ContextSeek 全量 **46 passed, 1 skipped**（与预期一致）；
- 部署：仅优雅重启 `:12001` canary（停止 13s，重启后
  `transport=long_connection, subscribed=true`，单实例，启动 0 错误）；
  **Callback `:12000` PID 1357387 全程未变**；`.env`（600）、
  runtime-m05、durable SQLite（600）、SeekDB 全部保留未清理；
  共享 PostgreSQL 未修改。

### 群 A/B Oracle（同一员工 zhuchunlin，两群，新标识 -M05R）

| 步骤 | 群 A（session `hmac-654baaa8…`/chat `hmac-b9f9924d…`） | 群 B（session `hmac-f545995e…`/chat `hmac-8cbec088…`） |
| --- | --- | --- |
| setup | 确认 GROUP-ALPHA-M05R ✓ | 确认 GROUP-BETA-M05R ✓ |
| 追问 | 答出 GROUP-ALPHA-M05R ✓，但列出"本群历史 3 个标识"含 **GROUP-BETA-M05**（历史污染）❌ | **只答 GROUP-BETA-M05R**，无 ALPHA、无历史混入、无 chatid ✓ |

- 两群 session/chat key 各自两轮稳定、互不相同 ✓；未改写成
  `wecom:<userid>` ✓；每条消息恰一次终态（本轮 5 轮 5 终态）✓；
  durable inbox 23/23 completed、outbox 无未交付 ✓。

### 残留违例根因（证据链）

- 群 A 本轮回复中的 GROUP-BETA-M05 **来自群 A 自己的会话短期记忆
  row22**——即上一轮（BLOCKED 轮）泄漏的那条错误回复本身被保存进了
  群 A 的 session 记忆；本轮 row45 的"3 个标识按时间顺序"叙事与其
  派生关系吻合；
- 语义层修复已验证生效：
  1. 代码：群聊作用域改为 `…/conversation/{session_key}/semantic`，
     结构上不再命中旧员工级大作用域记录（不迁移、不删除旧数据）；
  2. **群 B 阴性对照**：与群 A 完全相同的追问文本未检索到群 A 的
     任何内容（若语义泄漏仍活，B 应命中高相似的新 ALPHA-M05R 轮次）；
  3. 定向确定性测试 3 passed（群隔离/单聊连续性/fail-closed）。
- 本轮按规程未清理现场数据（含 row22 污染行），故 oracle 字面
  "群 A 不得出现历史 GROUP-BETA-M05"仍不满足 → 维持 BLOCKED。

### 复验路径建议（不扩大修复范围）

1. 换一对**全新群**（无污染会话记忆）重跑本 Oracle——预期直接通过；
2. 或由维护者授权精确清理群 A 会话中的 row22 污染行后原群重跑；
3. 修复代码本身无需返工。

### 变化确认

无 schema 变化（SeekDB/pgvector/共享 PG）、无 `.env` 变化、无
production/tag 变化、未提交任何 runtime-m05/日志/SQLite。

---

## 全新群最终复验（2026-09-01，UTC）

### 判定：BLOCKED（隔离目标全部达成、零交叉；群 C 自标识召回失败×2，PASS 字面未满足）

- 基线：双端 tip `db87cc3` ✓（`fb6e851→adca4ff→db87cc3` 同链 ✓，无
  force push，production/tag 未变）；`db87cc3` 仅文档，运行代码与
  `adca4ff` 相同，canary 未重启（健康保持 subscribed=true）；
- 新群：群 C、群 D 均为**新建群**（短期记忆基线 4 个会话指纹之外，
  证实"此前从未与该机器人交互"）；同一员工 zhuchunlin 操作；
  标识带本轮随机后缀（报告脱敏为 GAMMA/DELTA 逻辑名）；
- 服务端：群 C 两轮 session `hmac-ec01a3d7…`/chat `hmac-727cadda…`
  稳定；群 D 两轮 session `hmac-56d7d6bd…`/chat `hmac-737b6fc6…`
  稳定；两群互不相同；未改写为 `wecom:<userid>`；5 轮（含群 C 重问）
  各恰一次终态；durable inbox 28/28 completed、outbox 零未交付；
  Callback `:12000` PID 1357387 全程不变。

### 隔离结果（本轮核心目标：全部达成）

| 检查 | 群 C | 群 D |
| --- | --- | --- |
| 回复含对方新标识 | **0** | **0** |
| 回复/记忆含历史 ALPHA/BETA（M05/M05R） | **0** | **0** |
| 引用他群 chatid | 无 | 无 |
| 本群短期记忆标识命中 | GAMMA×1，DELTA/ALPHA/BETA=0 | DELTA×3，GAMMA/ALPHA/BETA=0 |

### 群 C 自召回失败（唯一未满足项）

- 群 D 完美：setup 回显 DELTA，追问精确答 `GROUP-DELTA-M05R2-<后缀>`；
- 群 C：setup 回复为通用问候（未回显标识）；两次相同追问
  （规程允许的重问一次已用）分别返回两个**不同的 32 位十六进制串**
  ——两值不同证明是模型**编造**而非引用固定内部 ID；
- 服务端排除系统缺陷：记忆加载事件以正确 session key 成功执行，
  GAMMA 行（该会话 row46）在加载窗口内、内容完整；
- 定性：模型对"只列标识"未映射到会话记忆中的 GAMMA（与群 D 的
  setup 回显形成对照），属模型理解/质量项，非隔离缺陷、非 fb6e851
  回归；未改提示词诱导（遵守规程）。

### 结论与建议

- 跨群隔离（fb6e851 目标）在无污染新群中**零交叉验证通过**；
- PASS 字面条件"群 C 只回复 GAMMA"未满足 → 维持 BLOCKED；
- 建议 Codex 复核两点后决定：① 群聊 setup 回复是否应稳定回显收到的
  标识（提升召回锚点）；② 记忆注入对"标识"类内容的呈现方式；
- 原群 A 污染数据（row22 等）保持未修改；本轮未清理任何数据。

### 变化确认

无 schema/.env/production/tag 变化；未提交 runtime/日志/数据库；
Callback/canary 双健康。

---

## 短期记忆字面量修复候选（待 Linux 单项复验）

- 修复 commit：`e81b779` (`fix(enterprise): preserve literal identifiers in short-term recall`)；
- 维护者决策：群聊 setup 不承诺稳定回显员工输入，不在渠道层硬编码测试标识；
- 提示合同：短期记忆显式标注“历史用户原文/历史助手回复”。员工自带
  字面量不是 runtime 内部 ID；复述时逐字复制最相关用户原文，不编造
  UUID/哈希/内部 ID；
- 冲突规则：历史助手回复可能错误，它与历史用户原文冲突时以用户原文
  为准。该规则直接覆盖群 C 现有“GAMMA 原文 + 两条编造十六进制回复”数据；
- 边界：不更换模型，不改 ContextSeek scope，不改 WeCom Transport，不清理群 C/D
  记忆，不修改 schema；
- 本地门禁：短期记忆定向 `8 passed`，Enterprise `88 passed`，
  ContextSeek + WeCom `173 passed, 1 skipped`，数字员工样例
  `332 passed, 1 skipped`，改动范围 Ruff/ty 通过；
- 待验 Oracle：重启 canary 后，在原群 C/D 各重发一次相同追问。群 C 必须
  逐字回复 GAMMA 用户原文且不输出历史编造十六进制串；群 D 必须逐字
  回复 DELTA；两群仍保持零交叉。
