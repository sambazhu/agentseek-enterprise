# v0.1.2 M0.6 企微自建应用 Transport Linux 复验记录（2026-09-02，UTC）

## 最终判定：BLOCKED（应用入站与主动探针各有一个代码缺陷；部署/回调/健康/安全边界正常）

## 第一阶段：静态复验（✅ STATIC_PASS）

- 双端功能分支 tip：GitHub = GitLab = `b28102aa50597924ae5d1eb7eb7e384a95e3e749` ✓；
- `2cff5c0`（production）为 HEAD 祖先 ✓；`12a6bbc`（feat: add self-built
  application transport）在链 ✓；范围内零 merge commit ✓；
  `diff --check` 干净 ✓；production 双端保持 `2cff5c0` 未动 ✓；
- 门禁：WeCom **146 passed**（`AGENTSEEK_WECOM_PORT=12100`）、样例
  **335 passed+1 skipped**、Enterprise **88**、ContextSeek **47+1**、
  模板 **5**、集成 **3**、Ruff（`--no-fix`）、ty、docs-test、bash -n、
  `uv lock --check` 全过；显式 Oracle：test_application_transport +
  test_outbound **28 passed**、example test_prod_check **13 passed**。

## 第二/三阶段：应用创建、配置与部署（✅）

- 应用"数字员工（测试）"（AgentID 1000119，脱敏）；企业可信 IP 含本机
  出口 `58.60.106.62`；Secret 经 gettoken 验证有效；
- department/list 成功：信息技术部 id=407（探针部门目标取其子部门
  数智产品研发团队 433，缩小影响面）；标签列表 4 个，测试标签取
  `53 营销人员（测试）`；探针成员 zhuchunlin；
- 公网回调：`https://test.wkzq.com.cn:9443/wecom/app/app-m06/callback`
  →（192.10.50.60 nginx `location /wecom/app/`）→ `:12001/ai-emp/*`
  （路由探测确认映射正确）；
- M0.6 canary `.env`（600）：13 项覆盖（transport/callback token+aeskey/
  探针三目标/文件路径等），Secret/Token/AESKey 未回显未提交；
  探针文件 runtime-m06/m06-probe.txt（138 字节，600）；
- `prod_check --strict`：**通过（0 WARN / 0 FAIL，exit 0）**；
- 部署：旧 M0.5 canary 优雅停止（11s，:12001 释放）；M0.6 canary 自
  独立 worktree `/root/agentseek-m06-app-transport` 启动；
  **`:12000` Callback PID 1357387 全程未变**；
- 健康：`:12001/health` long_connection subscribed=true（双传输并行）；
  **`:12001/health/wecom-app` =
  `{"ok":true,"transport":"application","enabled":true,"visibility_loaded":true}`**；
  启动窗口 0 Traceback/ERROR；
- 企微后台回调保存：验证 GET → **HTTP 200 OK**（1 秒内）。

## 第四阶段：活体 Oracle

### Oracle 1 应用入站文本：❌ BLOCKED（缺陷一）

- 测试成员在应用发送「M0.6 应用回调：请只回复“应用回调正常”」；
- 回调 POST → **HTTP 500**；异常链：
  `application.py:192 handle_message → channel.py:626
  _handle_application_plain_message → :650 _handle_text →
  :1124 _dispatch_user_message → :1386 _commit_inbound_stream_response`；
- 异常：`RuntimeError("long-connection callback did not include req_id")`
  ——应用入站消息被路由进**长连接流式提交路径**，而应用回调无
  `req_id`（应用消息本无流帧），fail-closed 报缺失；
- 结果：员工未收到任何回复（使用方确认）；inbox 记录 `blocked`
  （error_type=`long_connection_req_id_missing`）；企微未重试（仅 1 次
  POST，无重试风暴）；
- 定性：应用传输与 channel 流提交机的集成缺陷；确定性测试
  （28 passed）未覆盖该集成点。

### Oracle 2 主动消息探针：❌ BLOCKED（缺陷二，安全边界本身按设计工作）

- 长连接机器人发送「M0.6自建应用主动探针」→ 回复"探针已启动" ✓
  （触发正常，该回合 inbox completed）；
- 探针执行：`wecom.application proactive probe failed
  error_type=WeComAppVisibilityDenied` ——**在 API 调用前**被可见范围
  检查拒绝（fail-closed 行为符合设计）；outbox `wecom_app_text`
  → `blocked`（未盲目补发 ✓）；
- 鉴别证据：同凭据**直连 `message/send` 到 zhuchunlin 返回 errcode 0**
  （真实可见范围包含该成员、凭据与可信 IP 正常）→ 拒绝源于网关侧
  可见范围模型：启动时 `agent/get` 的 `allow_user/allow_party/allow_tag`
  对本应用**返回空**（企微对部分自建应用不回填该组字段），被按
  "全部拒绝"解释；
- 定性：**空 allow_* 误判为 deny-all** 的可见范围模型缺陷（应按企微
  语义处理空集——回退为允许发送并按 API 级错误分类，或用
  department/user 解析真实范围）；
- 四类目标投递（成员 text/file、部门 markdown、标签 textcard）全部
  未执行；文件上传未触发。

### Oracle 3-5：未执行（依赖 1/2 的通路）

- 可见范围/局部投递 fail-closed 的确定性行为已由静态测试覆盖（含于
  146），但活体 @all/越界/invaliduser 场景依赖可用的探针通路；
- SIGTERM 恢复与脱敏扫描部分执行：脱敏扫描见下。

## 脱敏扫描（只报命中数）

runtime-m06/gateway.log：应用 Secret、回调 Token、EncodingAESKey、
模型 Key、access_token、PG/DM 密码、userid 明文 **全部 0 命中**。
（诊断直连消息为管理使用方本人目标，内容为标注的诊断文本。）

## 零变化确认

- Callback `:12000` PID/health/durable 零变化；
- 共享 PostgreSQL 未触碰；M0.5 现场数据未清理（m05 worktree 原样，
  m06 使用独立 runtime 存储）；
- 无 .env/日志/runtime/SQLite 提交；production/tag 未动。

## 缺陷汇总（供修复）

1. **应用入站被长连接流提交路径接管**（channel.py `_dispatch_user_message`
   → `_commit_inbound_stream_response` 对 application transport 抛
   `req_id` 缺失，HTTP 500、inbox blocked、员工无回复）；
2. **可见范围空集误判 deny-all**（`agent/get` allow_* 对本应用为空，
   网关拒绝一切发送；直连 API errcode 0 证明真实范围有效）。
   两缺陷修复后在本 canary 重跑 Oracle 1-5（配置与现场保留）。

---

## 78a3e6a 修复复验（2026-09-02，UTC）

### 判定：BLOCKED（缺陷①已修复并活体验证；Oracle 2 因权威范围"字面集合 vs 企微子树语义"差异被拦——按使用方决定 B 记录待 Codex 决策）

- 同步：双端 tip `78a3e6a` ✓（`598601f` 祖先 ✓，ff 更新，diff --check
  干净，production/tag 未动）；Callback `:12000` 基线：PID 1357387、
  durable mtime 08-31 12:12、日志 359 行，全程零变化（+2 行为本验证
  的健康检查探测）。
- 静态门禁全过：定向 **31 passed**、WeCom **149 passed**、样例
  **335+1**、Enterprise **88**、ContextSeek **47+1**、模板 5、集成 3、
  Ruff（--no-fix）/ty/docs-test/bash -n/uv lock --check。
  六项新合同均有命名测试：无 req_id 入站恢复、流事件仅累积、
  空 allow_* 回退 API（`test_empty_agent_visibility_snapshot_defers_
  authorization_to_send_api` 等）、权限错误 blocked、@all/畸形 API 前
  拒绝、ambiguous 不补发。
- 部署：仅重启 `:12001`，双传输健康（long_connection subscribed +
  /health/wecom-app ok/application/visibility_loaded=true），启动
  0 错误。

### Oracle 1 应用入站：✅ PASS（缺陷①修复验证）

- 回调 POST → **HTTP 200**；`req_id` 错误 **0 次**；
- 恰一条终态「应用回调正常」（员工确认），outbox `wecom_app_text`
  delivered；session `wecom:<userid>`。
- 附加佐证：信息技术部 3 名真实同事自发使用应用并均正常获得单条
  回复（多用户入站正常）。

### Oracle 2 主动探针：⚠️ 部分通过 → 本轮 BLOCKED 项

- **成员 text ✅**：空 `allow_userinfos` → 按新合同回退 API 判定 →
  送达（缺陷②——空集 deny-all——修复验证）；
- **部门 markdown ❌ 被 API 前拒绝**（`visibility_denied` → blocked，
  探针中止，标签/文件未测）；
- 根因（本轮查明，含对上轮诊断的更正）：企微 `agent/get` 实际字段为
  `allow_partys/allow_tags`（上轮我误用 `allow_party/allow_tag` 读取
  显示为空——上轮"缺陷②空 allow_*"的真实集合是
  users=[] / parties=[407] / tags=None）；权威部门集合为字面 `{407}`
  （信息技术部），探针目标 433 为其**子部门**——按"非空权威范围之外
  的目标在 API 前拒绝"合同字面拦截；
- 语义差异证据：与网关 payload **字节级一致**的直连 `message/send`
  markdown → party 433/407 均 errcode 0（企微按"可见范围含子部门全部
  成员"语义放行）；
- 定性：实现比企微语义更保守（fail-closed 方向，无安全问题），但
  拦截了"已授权部门的子部门"目标。使用方决定路径 B：不改企微后台、
  不改探针目标，记录差异待 Codex 决策（建议：用 department/list 解析
  子树后放行，或维持保守并文档化；或管理员在可见范围追加 433）。

### Oracle 3 安全失败关闭：✅（确定性覆盖）

六项合同由 149 基线中的命名测试覆盖（API 前拒绝/空集回退/权限错误
blocked/invaliduser/party/tag blocked/部分成功不算成功/ambiguous 不
补发/@all 拒绝）。

### Oracle 4 SIGTERM 与持久化恢复：✅ PASS

- 后台轮询截杀器在 application inbox 进入 processing 后 **0.5s 内**
  SIGTERM（04:07:09）；manager cancelled/stopped 齐备；
- 优雅停止后 processing 记录 **lease_owner=NULL**（lease 释放）；
- 重启后**立即恢复**（无 req_id/response_url 依赖，未 blocked）；
- 终态经应用 outbox 恰好投递一次：inbox **completed**；2000 字终态
  因企微单条文本上限按设计拆分 2 条送达，**员工确认无重复**。

### Oracle 5 回归与脱敏：✅（一项既有观察项如实记录）

- 凭据类（应用 Secret/回调 Token/AESKey/长连接 Secret/模型 Key/
  access_token/PG/DM 密码/media_id）在 log/events/durable 全部
  **0 命中**；
- userid 明文在 gateway.log 命中 7/3/8（三名成员）——均为 M0.1 起
  既有框架日志行（`session_id=wecom:<userid>`、身份缓存），非 M0.6
  新增；按手册"目标 ID 0 明文"的更严措辞与既有框架行为的差异记录
  待维护者决策；
- Callback PID/health/durable mtime 零变化；共享 PG 表清单零变化；
  长连接 subscribed 全程保持。

### 收尾

- 五个 `APP_PROACTIVE_PROBE_*` 已清空（复核 0 行）；无敏探针文件移入
  `runtime-m06/restricted-archive`（600/700）；canary 优雅重启后
  双传输健康；Callback 未动。

### 复验路径

Codex 决策子树语义（或管理员在可见范围追加 433）后，仅需恢复探针
五项配置并重跑 Oracle 2（其余 Oracle 已全部通过，无需重测）。
