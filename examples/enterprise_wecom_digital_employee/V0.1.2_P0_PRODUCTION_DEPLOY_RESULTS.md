---
title: v0.1.2 P0 企业记忆隔离修复生产部署报告
type: how-to
audience: [A4]
runs: yes
verified_on: 2026-09-11
sources:
  - V0.1.2_P0_DURABLE_LINUX_REGRESSION_RESULTS.md
  - contrib/agentseek-enterprise/src/agentseek_enterprise/runtime.py
---

# v0.1.2 P0 修复生产部署报告（Linux CC 执行，用户确认）

## 0. 结论

部署成功：production b339228 → **503c846**（ff-only，含 08f87a4 关闭报告+9045b1e
语义修复+a22d6d5 durable 修复）；本地定制原样保留；最小生产复验 9/9 PASS+
短期/语义无回归；未回滚；运行态健康。

## 1. 发布提交核对

| 项 | 结果 |
| --- | --- |
| 部署目标 | 503c84632b4e（GitHub production=Codex 推进值，唯一目标） |
| 包含性 | 08f87a4 ✓ / 9045b1e ✓ / a22d6d5 ✓（merge-base --is-ancestor 逐项） |
| 祖先 | b339228 后代 ✓；08f87a4..503c846 仅 +1 docs 提交 |
| 旧 tag | enterprise-wecom-v0.1.2-ga 未动（tag 对象 ae5a22b→commit 1952303，仅读取未修改） |
| GitLab 同步 | 普通推送 production+feature 两 ref（b339228→503c846 / 08f87a4→503c846），无 force |
| 双端核对 | production 与 feature 两 ref 双端 SHA 完全一致（503c846） |

## 2. 现场保护（部署前记录）

- 部署前：production @ b339228；脏跟踪文件 2 个（industry-report 的 pack.yaml/
  profile.yaml=信息技术部本地定制，仅记录文件名）；
- **ff 触碰检查**：`git diff --name-only b339228..503c846 -- digital_employees/` 为空
  →ff-only 可带脏树无损执行（不 stash、不 reset）；
- 进程：单网关（uv+python 一对），:12001 唯一监听，lock 唯一；
- 部署前模块加载路径：contextseek/enterprise/example 三类均 ←生产工作树（.pth）；
- 停止/启动/回滚步骤（预写）：优雅停止=SIGTERM python（`pgrep -f 'scripts/bub_gatewa[y]'`，
  括号防自匹配）；启动=标准 run_gateway.sh+显式 GATEWAY_LOG；
  **回滚=反向 ff `git merge --ff-only b339228`+重启**（同样不触碰脏路径；
  不移动双端 production ref；不删新群 namespace 数据）。无 schema migration→
  无数据库结构变更（执行确认：零 SQL DDL）。

## 3. 运行态变更（用户确认后执行）

- 用户明确确认后执行；变更=ff-only 代码更新+一次优雅重启（停机 14s）；
- 部署后：HEAD=503c846；单实例；:12001 唯一；lock 唯一；
  /health=ok subscribed ✓；/health/wecom-app=ok visibility_loaded ✓；
- **三类模块实际加载路径=目标工作树**（inspect 实测）；运行代码含
  `conversation_type`（EnterpriseIdentityContext 注解实测 True）；
- 启动验证窗口：0 Traceback/0 ERROR/0 凭据输出；.env/MCP/pack 本地定制/数据库/
  运行目录全部未动（脏文件原样在列）。

## 4. 最小生产复验（新合成标记 D5DM-3308 / D5GA-4419 / D5GB-5521；不复用旧标记）

判据三重：工具事件 namespace（期望键构造性计算）+数据库落点+手机端回答。

| # | 用例 | 结果 | 证据 |
| --- | --- | --- | --- |
| 1-2 | dm durable 写+召回 | PASS | write/recall=员工 5 段（31652c15…=计算键）；答"D5DM-3308" |
| 3 | 群A durable 写 | PASS | write=群 7 段（667c7488…=计算键） |
| 4 | 群B durable 写 | PASS | write=群 7 段（b60608c3…=计算键） |
| 5-6 | 两群正向召回 | PASS | 各自 7 段 recall entries=1；答各自标记 |
| 7 | dm 问两群标记（六向关键向） | **PASS** | dm recall=员工 5 段 entries=9（=清理后画像，无群标记）；答"查了当前群与单聊的永久工作记忆，没有找到任何 D5GA 或 D5GB 标记" |
| 8 | 群A 问 D5GB | PASS | 群A 7 段内查；答"只存有 D5GA-4419…没有 D5GB" |
| 9 | 自建应用问 D5DM | PASS | 员工 5 段；答 D5DM-3308（跨传输共享） |
| 回归 | 短期记忆键+语义 scope | PASS | 群回合=各自群键/群会话桶（b1f9…/ad97…），dm=canonical 键/员工桶（d92a…）——与验收期确立映射完全一致，无回归 |

模型口头"已记住"未单独采信——每项均以事件+落点+手机回答三重判定。

## 5. 本轮合成数据处置（按工单：未获清理批准→仅登记范围保留待处理）

- 登记范围：长期 Store——员工 5 段画像新增 1 行（D5DM-3308）、群 7 段两行
  （D5GA-4419 / D5GB-5521，各自 namespace）；短期记忆与语义层各会话若干行
  （含标记文本）；均在案可定向清理，待用户批准。
- 未触碰：报告所列 6 条 Mac 时代口令槽位（未获批）。

## 6. 逐项汇总

| 项 | 结果 |
| --- | --- |
| 发布提交核对+双端同步 | PASS |
| 现场保护/ff 无损性/回滚预案 | PASS |
| 用户确认（强制门） | 已获（"确认部署"） |
| 部署+加载验证 | PASS（模块路径/SHA/单实例/健康/无错窗口） |
| 最小生产复验 | PASS 9/9+回归核验 |
| 回滚 | 未执行（无需） |
| 本轮合成数据清理 | NOT RUN（未批准，范围已登记 §5） |
| tag/合并/M3 | 未创建/未合并/未推进（按工单） |

## 7. 回传

- 部署 production SHA=503c846；报告提交后 production ref=本报告 HEAD（文档推进，
  运行代码仍=503c846 内容）；双端一致以推送后核对为准；
- 本地定制保持原样（pack/profile 脏文件在列未动）；
- 后续建议：维护者决定 hotfix tag 名称（工单留权）；v0.1.2 回归阶段 D/E/F
  续测时机待用户安排。
