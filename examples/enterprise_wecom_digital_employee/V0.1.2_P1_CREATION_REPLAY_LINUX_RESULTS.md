---
title: v0.1.2 P1 历史创建消息重放隔离 Linux 复测结果
type: how-to
audance: [A4]
runs: yes
verified_on: 2026-09-13
sources:
  - V0.1.2_P1_CREATION_REPLAY_HANDOFF.md
  - V0.1.2_P1_CREATE_CONSENT_LINUX_RESULTS.md
---

# P1 历史创建消息重放隔离 Linux 复测结果（Linux CC）

## 0. 结论摘要

- 部署 8cc35c4（双端一致；基线 6e7a0f9 祖先核对）；schema 12 沿用；
  双路径加载验证；
- **核心修复验证通过**：原消息 ID 重放→只读返回原任务（协议层注入驱动：
  请求键逐字节复现+find 命中+零写入+零模型调用）；同文新消息 ID→不误判重放
  （手机实测走活动续接，零新建）；
- 上一轮偏差（重放被活动任务吸收）就此闭环；
- 恢复完成：开关全关、双健康、**错误增量 0**。

## 1. 部署

| 项 | 结果 |
| --- | --- |
| SHA | 8cc35c44…（GitHub=GitLab 双端一致；含 6e7a0f9） |
| schema | 12 沿用（无迁移、不倒库、不改备份） |
| 加载 | repository/work_composition/agent ←P1 双路径 |
| 基线 | 任务 5、错误 24 |

## 2. 重放隔离验证（方法说明+结果）

**方法**：同消息 ID 重放无法经手机实现（重发必产生新 msgid）——按交接要求采用
**协议层注入驱动**：以 P1LU 原创建消息的原始 msgid（ef1f6f25…，日志固定）构造
合成信封，驱动生产代码链路（`load_message_state→_message_scope_key→_request_key→
find_created_work`），全部只读，配合全账本快照断言。

| # | 断言 | 结果 |
| --- | --- | --- |
| 1 | 原 msgid+dm 身份复现幂等键 | **MATCH**（与 P1LU 存储键逐字节一致） |
| 2 | find_created_work（四维完整授权范围） | **返回 P1LU（work_0ed6d63，published）** |
| 3 | 确定性响应 | 代码路径三级验证：has_id=True→find 命中→`work_creation_replay_response` 文案设置→`_deterministic_direct_response` 先于任何模型路由返回 |
| 4 | 负向四例（同文新 ID/跨请求人/跨 DE/跨 playbook） | **全拒** |
| 5 | 账本零写入 | **9 表 IDENTICAL**（items/contracts/sources/evidence/claims/artifacts/publications/deliveries/events） |
| 6 | 模型调用 | **0**（驱动不经模型；生产网关无该消息到达） |

## 3. 手机实测（同文新 ID+复验组）

| # | 消息 | 回复 | 判定 |
| --- | --- | --- | --- |
| 1 | 新建报告任务（创建任务 B） | "已创建新报告任务（work_70efdfd…）并沿用既有信息录入 Brief v1" | ✅ 恰一条（5→6） |
| 2 | P1LU 原文重发（**新 msgid**） | "当前任务（work_70efdfd…）已在 intake…无需重复创建或保存" | ✅ **无重放提示**——不凭文本误判；走活动续接+占用规则；零新建 |
| 3 | 新建报告任务（再发，新 msgid） | "当前已有一个进行中的报告任务（work_70efdfd…）" | ✅ 零新建+引用既有 B |
| 4 | 请新建一份报告 | 同上 ActiveWorkConflict | ✅ 第三活动拒绝复验 |
| 5 | 取消当前报告任务 | "已取消当前报告任务，历史记录保留" | ✅ B（draft 态）正式取消 |

说明：#3 的手机重发具有新 msgid，不构成同 ID 重放——返回占用提示引用 B（功能等价
于只读告知）；同 msgid 字面重放路径已由 §2 协议层验证覆盖。

## 4. 消息身份核验（脱敏）

- 重放身份：dm 会话、requester=user_key（短哈希 81297b56…）、tenant=wkzq 域、
  DE=industry-report、playbook=securities-industry-report；
- 原始 msgid：ef1f6f25…（仅此处引用，完整值不入回传正文）。

## 5. 账本终态与登记

- 任务 6 条（历史全保留）：P1REG published / P1LU published /
  4 条 cancelled（含本轮任务 B=work_70efdfd，draft 取消，Brief v1 provisional 留档）；
- 零活动任务；旧任务 A 内容零变化（未续接、未改 Brief、未新建）。

## 6. 恢复与终态

| 项 | 结果 |
| --- | --- |
| 开关 | WORK=false / PRODUCTION_MCP=false（进程环境确认）；模型 DashScope 保留 |
| 健康 | LC subscribed+app 双 OK；:12001 唯一 |
| 错误增量 | **基线 24 → 终值 24，增量 0** |
| 标签/production | GA/hotfix1/production 未动；P1 分支未合 |

## 7. 回传清单

- 部署 SHA=8cc35c4；报告=本文件（推送后 HEAD）；
- 重放返回原任务（协议层）/零模型调用/零账本写入/活动任务不变：**全部验证**；
- 同文新 ID 不误判：手机实测 ✓；
- NOT RUN 沿用：跨身份活体负向（无第二账号——协议层四维范围隔离已代偿）；
  交付正例、篡改活体；
- 内容完整性不结案；P1 发布时机待 Codex/用户决定；裸"同意"文案错配仍为独立 UX 观察。
