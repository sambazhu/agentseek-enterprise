---
title: v0.1.2 P1 宽口径证据用途复测 + 平台 MCP 冒烟结果
type: how-to
audience: [A4]
runs: yes
verified_on: 2026-09-14
sources:
  - V0.1.2_P1_EVIDENCE_USE_HANDOFF.md
  - V0.1.2_P1_SENTENCE_REVIEW_SHEET.md
  - V0.1.2_P1_PLATFORM_KB_CONTRACT_VERIFICATION.md
---

# P1 证据用途复测与 MCP 冒烟（f033e59，Linux CC，2026-09-14）

## 0. 结论摘要

- **用途协议（securities-evidence-use-v1）验证通过**：评审表 22 句重放全部按
  交接单方向归类（A 类→background、C1 导语→明确拒绝、D1→company_case、
  B1/B2→direct@roe 等）；六问检索 five-lines 3 direct + roe direct +
  **core-trends 新增 direct**（词表扩充 行业周期/高贝塔）。
- **全链初稿保存率仍为 0**（本轮 2 次尝试×2 生成=4 调用，每批 7 fact，全卡
  逐字/用途双门）——**证据池已修好、生成层未跟上**，内容完整性不结案的核心
  实证再+1。
- **平台 MCP 冒烟**：tavily ✅、聚源 ✅（两跳工具链真实数据）；IT 知识库 ❌、
  OA ❌——**均为平台侧 schema 缺陷**（服务端不应用默认值/required 错名），
  模型侧行为合理。

## 1. 对照段（固定语料 p1-acc4-a，176 chunks 同构）

| 评审表样本 | 旧判定 | 新判定 |
| --- | --- | --- |
| A1/A2/A4/A7（词法不共现实质句） | REJ | **background**（可引用不关缺口） |
| A3/A5（域词在邻句） | REJ | 单句 REJ / **同行带上文=background**（继承生效；竞争主体黑名单+触发词受限） |
| B1 量化利润句 | PASS@roe | **direct@roe** |
| B2 杠杆/权益乘数句 | REJ@five-lines | **direct@roe + background@five-lines**（问题定位修复） |
| C1 结构导语（上轮唯一活体 claim） | PASS | **明确 REJ**（噪声排除） |
| D1 IT 治理边界句 | PASS@digital/core | **company_case**（带标签不证行业） |
| A2/A3 换 core-trends 问 | REJ | **direct**（新词 行业周期/高贝塔） |

六问检索：topic/digital/priorities 仍零准入（语料限制如实）。

## 2. 全链（任务 09:28 创建→09:36 取消）

| 项 | 结果 |
| --- | --- |
| 诊断新事件 | `admitted`×4 + **`admitted_background`** + **`binding_limit`**（名额限制≠无关）均在流 |
| Source | 带 use 版本标记；core-trends/five-lines/roe 三问 direct 源绑定（对照段预期一致） |
| 初稿尝试 1 | 7 fact→`not_complete_sentence`→`repair_requested`→修复 7 fact→`sentence_not_relevant`→干净退出 |
| 初稿尝试 2（用户"生成初稿"） | 7 fact→`sentence_not_relevant`→修复→`not_complete_sentence`→干净退出 |
| 守卫 | 零放宽；F4b 上限内（每尝试恰 2 次生成） |
| 待观察项 | **"带用途标签的已保存初稿"活体样本仍未取得**（保存率 0 的直接后果） |

## 3. 平台 MCP 冒烟（生产态，4+1 项）

| MCP | 结果 | 证据 |
| --- | --- | --- |
| tavily-search | ✅ | 确认门→用户确认→今日证券新闻返回 |
| 聚源企业数据接口 | ✅ | 模型自主两跳：企业首页搜索(keyword)→工商基本信息查询(enterprise_code)→华为真实工商数据 |
| 信息技术部知识库 | ❌ | 3 次调用全 `1001 kbIndexCode不存在`——模型按 schema 省略 kbCode，**服务端不应用默认值**（合同核验已登记缺陷的活体复现） |
| OA流程助手 | ❌ | 3 次调用 args={}——required 写不存在的 `login_name`+top_count 默认不生效（已登记缺陷活体复现） |
| 人力资源知识库 | （未用户测；直连探活 OK） | — |

**平台组修复清单（用户可见故障，建议优先）**：①KB 工具服务端应用 schema 默认值
（kbCode/score/limit）或改 required；②OA 工具 required 字段名修正（oa_account）
+top_count 默认落地；③（前已登记）tavily 占位 key、MySQL 间歇 2013、战略库
kbIndexCode 缺失、kbCode 越权。

## 4. 收尾终态

任务已取消（今日累计 7 测试任务全 cancelled）；WORK=false/skill-mcp 同步恢复/
平台 6 MCP（.58 直连）；双健康 OK；production/标签未动。**p1-acc4-a 集合与
acc4-a 目录保留待批清理**。

## 5. 回传 Codex

- 用途协议判定通过（对照+全链+新诊断事件）；C1 导语明确拒绝实证；
- 生成层瓶颈最终定性：证据池（F5+F1/F2+用途分层）已修复到"三问 direct+背景
  可引用"，但模型 7 句提案的逐字纪律×全批拒绝=活体保存率 0（今日 6 尝试
  12 生成调用全部如此；唯一成功=单句提案）——**下一步建议评估：引文强约束
  提示/逐 Claim 反馈/部分保存**（均上游裁定，本轮未实施）；
- 平台 MCP 两项用户可见故障=平台侧 schema 缺陷（修复清单已列）。
