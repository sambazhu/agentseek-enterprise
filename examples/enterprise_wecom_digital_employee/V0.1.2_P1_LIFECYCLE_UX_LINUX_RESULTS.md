---
title: v0.1.2 P1 生命周期与初稿协议 Linux 复测结果
type: how-to
audience: [A4]
runs: yes
verified_on: 2026-09-13
sources:
  - V0.1.2_P1_LIFECYCLE_UX_HANDOFF.md
  - V0.1.2_P1_LIFECYCLE_UX_LOCAL_RESULTS.md
---

# P1 生命周期与初稿协议 Linux 复测结果（Linux CC）

## 0. 结论摘要

- 部署 0778fc0（双端一致，基线 0c4060f 祖先核对）；schema **11→12 迁移成功**
  （停写窗口+一致性备份+行数不变+索引谓词核验）；双路径加载验证
  （agentseek-work 与 examples 均←P1 工作树，LATEST=12）；
- **交接七项全部实测通过**（1 项 NOT RUN：跨请求人负向无第二账号）；
- 核心行为全部按设计落地：published 不占名额（两 published 并存实证）、
  发布后冻结完整、draft 态取消真实可用（上轮死局修复）、初稿程序补齐
  （一次生成成功，无模型重试循环）、中文研究问题 UX；
- 恢复完成（WORK=false/PRODUCTION_MCP=false，双健康，**错误增量 0**）。

## 1. 部署与迁移证据

| 项 | 结果 |
| --- | --- |
| 代码 | 0778fc0（GitHub=GitLab 普通推送）；evidence 链完整 |
| 停写窗口 | 网关停止（WORK=false 双保险）→ 迁移 → 重启 |
| 备份 | pg_dump（容器内 pg_dump 17 直连，主机 14 版本不匹配改道）→ 独立容器 p1lu_backup（127.0.0.1:54331）；**可读性验证**：items=2/contracts=21/sources=6/claims=23 与生产一致 |
| 迁移 | 新代码 apply_migrations：11→12，行数 2→2 不变 |
| 索引 | uq_work_items_active_playbook 谓词=排除 succeeded/failed/cancelled/**published**/**delivered** ✓ |
| 加载路径 | migrations/repository（←P1 contrib/agentseek-work）+draft_generation（←P1 examples）；LATEST_SCHEMA_VERSION=12 运行在位；WORK=true 进程环境确认 |

## 2. 逐项结果

### §1 新建与历史名额

| 项 | 结果 |
| --- | --- |
| published 不阻止新建 | ✅ P1REG published 状态下 P1LU 新任务直接创建（独立 work_id） |
| 第三活动同类拒绝 | ✅ ActiveWorkConflict+二选一引导 |
| 原消息重放幂等 | ✅ 返回既有任务零新建（总数恒 3） |
| 终态 | **两条 published 并存**（P1REG+P1LU）=schema 12 核心语义实证 |

### §2 发布后冻结（P1LU published 后 5 项）

| 项 | 结果 |
| --- | --- |
| 取消 | ✅ "该报告已发布，不能取消"+直接新建引导（死局文案修复确认） |
| Brief 修订 | ✅ "内容冻结，不能原位修订" |
| Draft 重复确认 | ✅ 守卫纠正过度声称，零新版本 |
| 新增检索 | ✅ 拒绝（路由正确） |
| 账本前后对照 | ✅ 完整不变（6 合同各 1/sources 1/evidence 1/claims 7/artifact 1/publication 1） |
| ⚠️ 观察项 | 修订请求被拒时**自动创建了新空任务**（work_088a3fe，回复明示）——设计上属于"直接新建"路径，但修订话术→新任务未经用户确认，UX 上可能意外；该任务已按 draft 态正式取消（顺带复验 schema 12 下 draft 取消可用） |

### §3 本人历史访问

| 项 | 结果 |
| --- | --- |
| 查看已发布（P1LU/P1REG） | ✅ 各自正确读取（work_id/状态/服务），零状态变化 |
| 未知编号 | ✅ "未找到当前身份可访问的已发布报告"（身份域限定措辞） |
| 跨请求人/跨租户 | **NOT RUN**（无第二请求人账号；本地测试已覆盖） |

### §4 历史交付

| 项 | 结果 |
| --- | --- |
| 交付已发布报告 … ReportArtifact v1 给我 | ✅ 拒绝："当前未启用 signed_link Artifact 交付"（未启用配置路径；未启 signed_link） |
| 已配置交付正例 | **BLOCKED**（交付配置未启用，按工单不冒称通过） |

### §5 初稿协议（P1LU 全链，真实 DashScope）

| 项 | 结果 |
| --- | --- |
| 全链 8 步 | ✅ Brief 确认→缺口门→保留缺口→初稿→确认→审批→DOCX→发布（8 类合同 v1 全 confirmed） |
| **一次生成成功** | ✅ **初稿单次模型调用直接通过**（上轮需 3 次收敛——程序补齐章节+固定待确认声明的修复生效） |
| 正例绑定 | ✅ sources=1（正例文档）、claims=7、evidence 引用链 [E1] |
| 反例零绑定 | 本轮未重跑独立反例（知识库含正例文档）；引用上轮 0c4060f 报告的反例结论（0/6 全缺口）作为基线 |
| 空稿/缺章节 | 程序补齐验证（一次成功即为补齐路径实证）；无模型补写章节调用 |

### §6 守卫与 UX

| 项 | 结果 |
| --- | --- |
| 中文研究问题 | ✅ P1LU 初稿"待确认问题"与风险节全部中文问题正文（"战略行动建议的优先级、适用条件、依赖、风险和验证指标是什么？"等），零英文 question_id |
| 拒绝原因+下一步 | ✅ 本轮各拒绝消息均含背景与指引（取消/修订/交付/未知编号） |
| Draft v999 | 本轮未重发（上轮 8b90938 已确认修复；代码未变） |
| WORK off 三层介绍 | 本轮收尾恢复后未重问（8b90938 已确认；代码未变） |
| 篡改负向活体 | NOT RUN（同前：无法经自然话术可靠诱导特定篡改句；离线回归在案） |

## 3. 模型/MCP 调用计数

- 模型：P1LU 链约 12 次（创建/路由/初稿生成 1/审批链/冻结测试若干），**初稿生成 1 次 1 成**；
- MCP：department-knowledge 若干搜索（P1LU 研究轮）；OA/知识库/tavily 0。

## 4. 残留账本与测试数据登记

- 任务终态：P1REG published / P1LU published / work_c1ed3 cancelled / work_088a3fe cancelled（意外创建后正式取消，历史保留）；
- 备份容器 p1lu-pg（127.0.0.1:54331，p1lu_backup 库）保留=迁移前一致性快照（**已有 schema 12 新写入，回滚路径=保留新库关闭 Work**，备份供审计）；口令文件 /root/.p1lu-pg-pw；
- P1CF-POS 合成文档、全部历史数据未动（未清理，按工单）。

## 5. 恢复与终态

| 项 | 结果 |
| --- | --- |
| 开关 | WORK=false / PRODUCTION_MCP=false（进程环境确认）；模型 DashScope 保留 |
| 健康 | LC subscribed+app 双 OK；:12001 唯一 |
| 错误增量 | **基线 24 → 终值 24，增量 0**（本轮全流程零新错误） |
| 标签/production | GA/hotfix1/production 未动；P1 分支未合 |

## 6. 新发现/观察

1. **观察（UX）**：published 任务上的修订请求→拒绝原位修订的同时**自动创建新空任务**（回复有说明但未经确认）——建议后续在创建前明确征求用户意图；
2. 备份操作教训：主机 pg_dump 14 对 PG 17 不匹配→改用备份容器内 pg_dump 17 直连（记录方法）。

## 7. 回传清单

- 部署 SHA=0778fc0；报告=本文件（推送后 HEAD）；迁移 11→12 证据在 §1；
- PASS：名额释放/第三拒绝/幂等/发布后冻结 5 项/历史访问 3 项/交付拒绝/初稿一次成功/中文 UX；
- NOT RUN：跨请求人负向（无账号）、篡改活体（不可诱导）；
- BLOCKED：已配置交付正例（signed_link 未启用）；
- 内容完整性仍不结案；MCP 后端补测待窗口。
