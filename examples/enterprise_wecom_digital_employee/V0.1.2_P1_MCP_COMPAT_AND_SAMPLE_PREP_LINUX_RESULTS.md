---
title: v0.1.2 P1 MCP 兼容复测 + 样例发布前只读核对
type: how-to
audience: [A4]
runs: yes
verified_on: 2026-09-14
sources:
  - V0.1.2_P1_MCP_COMPAT_HANDOFF.md
  - V0.1.2_P1_SAMPLE_ACCEPTANCE.md
  - V0.1.2_P1_SAMPLE_RELEASE_HANDOFF.md
---

# P1 MCP 兼容复测与样例发布准备（3ad174e/63a6186，Linux CC，2026-09-14）

## 一、MCP 客户端兼容受控复测——判定：通过（战略库独立 BLOCKED）

| # | 项 | 结果 | 活体证据 |
| --- | --- | --- | --- |
| 1 | OA 本人报销 | **PASS** | bot："已查到你名下的最近费用报销记录（OA账号：zhuchunlin），共 5 条"；required 修正+**可信身份注入生效**（模型侧出参 {}=账号字段隐藏，注入在下游；审计结果摘要脱敏符合"不打印账户值"边界） |
| 2 | IT 知识库 | **PASS** | 3 次调用全 success=true（默认参数显式补齐+kbCode 钉死本工具声明值）；"3 份制度文件"真实数据 |
| 3 | HR 知识库 | **PASS** | success=true，考勤制度真实数据 |
| 4 | 战略库 | **BLOCKED（后端，如实）** | 正确传参后错误从"kbIndexCode不存在"变为"无法找到对应的知识:[urzi…]"（kbCode 已正确送达）——**索引缺失如实上抛、未冒充无资料、未转查其他库** |
| 5 | tavily | **不适用覆盖（如实）** | 平台同步下发真实 key（tvly-fzq…，此前冒烟活体可用）；**AGENTSEEK_MCP_LOCAL_OVERRIDES 未启用**——平台凭据可用即不满足"平台无法持久保存"的覆盖前置；认证位置=stdio 子进程 env（env_from_env 可达位置已核对） |
| 6 | 依赖/锁文件 | 如实登记 | 本轮零安装零升级（jsonschema 4.26.0 已在 venv）；锁文件既有元数据漂移维持登记未动；未升级模型 SDK |
| 7 | 身份负向/审计边界 | 保留 | 注入走可信运行时（模型不可传账号）；审计摘要脱敏实测；他人账号/群聊拒绝路径离线覆盖未动 |

**现场恢复**：测试后已重启回生产代码路径（兼容层属 P1 分支待发布——**生产路径上 OA/IT/HR 仍会按坏 schema 失败**，恢复 MCP 可用性依赖 P1 发布，回传项）；开关全程未动（WORK=false）、skill-mcp 同步在位、双健康 OK、错误增量 0；单实例。

## 二、样例模板发布前只读核对（不执行发布）

### 候选与差异

| 项 | 值 |
| --- | --- |
| 候选分支 | enterprise/v0.1.2-p1-workflow @ **63a6186**（双端一致） |
| 实测代码 | ab4698c（选择式生成活体验证）+ 3ad174e（MCP 兼容，本文活体验证） |
| 文档链 | ab97bbe→63a6186 docs-only（评审表/各复测报告/样例验收/发布交接） |
| 与 production(4f7cf7f) 差异 | 30 提交；代码面 48 文件 +2928/-347（examples src+templates+contrib/skill-mcp+third wheel）；含 schema 11→12 迁移代码 |
| 兼容性 | schema 12 已在生产库应用（无回退需求=纯前进合并）；带背景绑定新提纲需新代码读（回退纪律=关 WORK 保账本） |

### 现场登记（只读快照）

- production=4f7cf7f（运行代码=生产路径）；标签 ga/hotfix1 未动
- 运行态：WORK=false/平台 6 MCP（域名形态经 hosts→.58）/skill-mcp 同步 on/Langfuse on（events-only）/双健康/单实例
- 历史登记：P1ACC5=work_7d01c706 cancelled（保留）；p1-acc4-a 集合+acc4-a 目录保留；今日 8 测试任务全 cancelled

### 已验证 vs NOT RUN（沿用样例验收口径，不外推）

- 已验证：选择式生成一次保存 6 fact/幂等零生成/用途标签/缺口保留/守卫不放宽（离线+活体）；MCP 兼容四项（本文）
- NOT RUN/限制：选择修复与二次失败活体路径；signed_link 正例；topic/digital/priorities 零候选（语料层）；战略库平台索引；平台侧 schema 服务端修复；内容完整性未结案

### 发布/回退方案（待用户批准后执行）

1. 仅样例代码归并：production ← 63a6186（普通合并/推送，不强推不移标签；新标签名称另行授权）
2. 部署窗口：停机重启换载入路径→健康核对→OA/IT/HR/聚源/tavily 五项冒烟（不启 WORK 不建任务）
3. 回退：保留账本+关 WORK+git detach 前一 production 提交重启（新提纲不回旧代码处理）
4. 启用 WORK/交付能力=单独授权

## 回传结论

- **一（MCP 兼容）：通过**——OA/IT/HR 三项活体 PASS+身份注入与脱敏边界保持+战略库独立 BLOCKED（平台索引缺失）+tavily 覆盖不适用如实；恢复生产路径后 MCP 可用性待 P1 发布
- **二（样例发布准备）：信息齐全待批**——候选/diff/兼容/限制/方案如上；人工业务审核按用户决定跳过不记 PASS；未执行任何 production 发布动作
- 约束遵守：未清理资料/任务/备份；未合并 production/未动标签；两项分别回传
