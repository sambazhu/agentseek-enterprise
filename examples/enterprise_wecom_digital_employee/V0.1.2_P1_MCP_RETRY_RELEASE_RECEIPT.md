---
title: v0.1.2 P1 MCP 同步冷却重试限定范围发布回执
type: how-to
audience: [A4]
runs: yes
verified_on: 2026-09-15
sources:
  - V0.1.2_P1_MCP_RETRY_HANDOFF.md
---

# MCP 同步冷却重试发布回执（Linux CC，2026-09-15）

## 一、发布前核对（全过）

| 项 | 结果 |
| --- | --- |
| 候选 | b226413 双远端一致；production=b35d0c9 为候选祖先（ff 可行） |
| 备份 | `release-retry-20260915/`（0700）：env/三配置/live mcp.json/itdept profile/回退步骤，均 0600 |
| 差异 | 单提交仅 agent.py 重试块+loader 写失败标识+测试+HANDOFF；无 digital_employees/无依赖/无 DB 变更 |

## 二、发布（已完成）

- **production：b35d0c9 → b226413**（fast-forward；GitHub/GitLab ls-remote 一致；未强推、未动标签）
- 受控停 overlay 实例（优雅停）→ 生产树标准 `scripts/run_gateway.sh` 启动（无 UV_PROJECT_ENVIRONMENT/无路径导出）
- 加载核验：cwd=生产根、PYTHONPATH=生产 examples src+skill-mcp contrib（脚本设置）、**agent/merge/loader=生产树、SDK=venv site-packages**、SKILL_DOCS_DIR=生产树、统一 runtime 绝对路径不变、零安装零 .pth、WORK=false、同步开启、三配置未跟踪+已忽略+0600

## 三、发布后验证（同一终态实例，无热身）

| 项 | 结果 | 证据 |
| --- | --- | --- |
| 首条消息进程内真实同步 | ✅ | `agent_config = AgentConfig`(6 mcps) → `sync_mcp_config result: 6 servers`；dmcp 与生效 mcp.json **同秒 14:58:32** 写入（非旧缓存冒充） |
| 合并 | ✅ | 生效=7 服务（含 department-knowledge），0600 |
| OA 本人报销（新问法"最近半年"） | ✅ 真调用 | 审计 14:58:51 succeeded（身份注入，回复按半年窗口） |
| IT 制度库（新问法"信息系统变更管理"） | ✅ 真调用 | 审计 14:59:33 succeeded |
| HR 知识库（新问法"产假"） | ✅ 真调用 | 审计 15:00:10 succeeded（避开会话记忆：此前年假/婚假问答不入账） |
| tavily 确认门 | ✅ 真调用 | 确认门→confirmed succeeded ×3（15:01:21-34） |
| 额外用户问题 | ✅ | HR 库 13 次连续调用全 succeeded（15:02:19-15:03:03） |
| 实例/健康 | ✅ | 单实例、:12001 单监听、/health 200 |
| 错误增量 | **0** | 平台故障 0、department-knowledge 启动异常 0（本实例未出现）、新增应用错误 0 |

## 四、回退

**未触发。** 备档：停网关→`git switch --detach b35d0c9`→标准脚本重启（三层配置与统一 runtime 保留）；回退后须注明——三层配置与标准启动仍在，但恢复旧版"失败后进程内不重试"行为。不强推远端。

## 五、结论与 NOT RUN（口径不扩大）

**"MCP 同步失败冷却重试"发布完成**：production 运行 b226413（三层配置+标准启动+冷却重试全链），发布实例真实同步与四项真调用冒烟全过、错误增量 0。

NOT RUN（沿用复验轮标注）：
- 网关进程内嵌的失败后重试活体（同进程重试证据=协议层驱动进程 b226413 同代码路径；网关进程自身为成功缓存态）
- 三次上限/并发去重活体（离线用例覆盖）
- WORK 研究窗口、department-knowledge 真实调用（另批；其子进程启动瞬时异常为存量观察项，本实例未出现）
