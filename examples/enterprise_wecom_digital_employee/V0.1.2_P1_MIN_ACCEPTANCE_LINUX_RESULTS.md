---
title: v0.1.2 P1 最小验收 Linux 结果
type: how-to
audience: [A4]
runs: yes
verified_on: 2026-09-14
sources:
  - P1_ACCEPTANCE_INVENTORY.md
  - P1_ACCEPTANCE_SUPPLEMENT.md
  - V0.1.2_P1_CREATION_REPLAY_LINUX_RESULTS.md
---

# P1 最小验收 Linux 结果（Linux CC，2026-09-14）

## 0. 结论摘要

- 三组材料（A/B/C）+ 跨账号四例全部完成实测；隔离方案（独立 collection + 临时
  MCP 切换）按用户批准的 A 方案执行，测后已完全还原；
- **核心验证结论**：relevance 门在无关域（IT 制度）上 100% 拒绝（C 组零绑定）；
  在有证券内容的域上保守但正确（A/B 组仅绑定实质相关 chunk）；跨请求人隔离
  完整（四例全拒+双方账本零变化+协议层重放隔离验证）；管道级联选择度极高
  （真实材料一次成功率低——如实记录，不结案内容完整性）；
- 全部测试数据已清理（三 collection + 测试任务）；开关已恢复关闭。

## 1. 执行环境

| 项 | 结果 |
| --- | --- |
| 代码 | 8cc35c4（P1 分支 HEAD=a523ad4 时部署，schema 12，双路径） |
| 隔离 | 3 测试 collection（p1-acc-a=5篇/b=2篇/c=4篇）+ mcp.local.json 临时 COLLECTION_ID 切换（A 方案） |
| 模型 | DashScope deepseek-v4-flash-0731 |
| 账号 | zhuchunlin（主）+ xiongjijian（跨请求人，DM 解析验证 RESOLVED） |

## 2. A 组（相对完整=全部 5 份可提取文本）

| 项 | 结果 |
| --- | --- |
| 新建授权门 | 自然表达被拦→明确"新建报告任务"后创建（✅ b545d8a 行为） |
| 源绑定 | 3 源（投行→five-lines、自营→core-trends、资管→five-lines） |
| 覆盖 | 业务线对标=supported；执行摘要=partial；其余 3 问=unresolved |
| 初稿 | draft v1 provisional，5 claims **全部程序待确认占位**（无证据章节不调模型） |
| 人工评审 | 绑定正确（3 源真证券材料+对应问题实质相关）；**管道选择度极高**（176 chunks→仅 3 绑定）——top2 搜索→relevance→原句提取级联筛选 |

## 3. B 组（明确缺口=投行+资管）

| 项 | 结果 |
| --- | --- |
| 干净重跑 | 首轮消息误路由到 A 组活跃任务（测试流程错误）→取消后重跑 |
| 源绑定 | 2 源（投行→five-lines、资管→five-lines）——**仅 five-lines 一问** |
| 覆盖 | **4/5 问=unresolved**（topic/core-trends/digital/roe/priorities 全缺） |
| 初稿 | 0 claims / draft 未持久化（观察项：程序空稿或模型调用未完成） |
| 人工评审 | 绑定正确；4 缺口符合预期（投行/资管无核心趋势/数字化/ROE/优先级的词法匹配） |

## 4. C 组（无关/混合=行业总览+3 篇 IT 制度）

> 后续勘误（依据 `V0.1.2_P1_ACC_ROOT_CAUSE_DIAGNOSIS.md`，原记录保留）：
> §3 的“draft 未持久化”不准确。干净 B 组 draft v1 已于 03:02:48 持久化，含 5 条程序占位 Claim；
> 未保存的是后来混入行业总览尝试的 v2，不能作为干净 B 组结论。v2 被原句/相关性校验整批拒绝，无部分写入。
> 02:56–02:58（B 被 A 吸收）与 03:05–03:08（C 被 B 吸收）单列为混合无效样本。
> 原文“4/5 问”与运行时六问不一致，不再作为问题覆盖率依据；应按六个 question_id 单列结果，不能混用章节数。
> §2 全占位稿不能单独证明模型返回空 claims，也不能证明三源正文实质充分。后续诊断已指出标题桩被准入的问题；
> 模型原始参数未留存，空批与非事实类型被程序替换等路径无法仅由最终稿区分。本文末尾对 B 未持久化的总结同样按本注修正。

| 项 | 结果 |
| --- | --- |
| **IT 制度拒绝** | ✅ **零绑定**——《数据分类分级管理办法》《信息技术管理制度》《网络和信息安全管理办法》在 sources 中完全不存在 |
| 行业总览 | ✅ 绑定到 five-lines（追加绑定到 B 组任务，因路由问题） |
| 人工评审 | IT 制度全部拒绝=正确（域词不匹配证券域）；行业总览绑定=正确 |

## 5. 跨账号测试（xiongjijian，4/4 PASS）

| # | xiongjijian 操作 | 回复摘要 | 判定 |
| --- | --- | --- | --- |
| 1 | 查看已发布报告 work_0ed6d63… | "未找到当前身份可访问的已发布报告…归属其他请求人" | ✅ 跨请求人历史访问拒绝 |
| 2 | 修改覆盖期 | "当前身份（熊积健）名下没有…无法对不存在的任务修改" | ✅ 跨请求人 Brief 修改拒绝 |
| 3 | 取消当前报告任务 | "当前没有可取消的…「取消」仅作用于取消者本人" | ✅ 跨请求人取消拒绝（含显式所有权范围声明） |
| 4 | 新建报告任务 | 创建成功（work_642b4da3）→证实 xiongjijian 有服务权限 | ✅ 授权用户可正常操作 |
| 账本 | zhuchunlin P1REG（16 合同）/P1LU（6 合同）**零变化** | | ✅ |
| 协议层 | xiongjijian 身份+zhuchunlin 的 msgid→键不同→find=None | | ✅ 跨请求人重放隔离 |

## 6. 管道行为总结（如实，不结案）

1. **级联选择度**：真实证券材料在"top2 搜索→relevance 词法→原句提取"级联下一次
   成功率极低（A 组 176→3 绑定、初稿全占位）——设计保守正确但不适合直接作为
   业务生产力管道使用；提示词/检索策略优化是上游决策项。
2. **relevance 门**：在域外内容上 100% 拒绝（C 组零绑定）——核心安全目标达成。
3. **新建授权**：自然表达→询问；明确命令→恰一条；重放→幂等——三轮一致。
4. **跨请求人**：四例+协议层全拒——隔离完整。

## 7. 清理（已执行，用户授权）

| 项 | 结果 |
| --- | --- |
| 测试 collection | p1-acc-a/b/c 全清（documents 11 行/chunks 级联）；分组目录已删 |
| 测试任务 | A/B/C 组任务+ xiongjijian 任务全部正式取消，历史保留 |
| 生产数据 | P1CF-POS/P1REG/P1LU/IT 制度 46 篇未动 |

## 8. 恢复终态

| 项 | 结果 |
| --- | --- |
| 开关 | WORK=false / PRODUCTION_MCP=false（标准脚本 production 代码） |
| MCP | department-knowledge → strategic-development（生产） |
| 模型 | DashScope deepseek-v4-flash-0731 |
| 健康 | LC subscribed + app 双 OK |
| 标签/production | 未动 |

## 9. 回传

- 三组人工预期 vs 实际：A=部分绑定（低于预期）/B=正确缺口/C=完美拒绝；
- 隔离证据：collection 级物理隔离（搜索+读块全 WHERE 约束）；
- 初稿一次成功率：A=全占位/B=未持久化——**不适合直接生产使用**；
- xiongjijian 配合窗口=2026-09-14 下午（已完成，账号已验证有服务权限）；
- 内容完整性不结案；P1 发布时机待 Codex/用户决定。
