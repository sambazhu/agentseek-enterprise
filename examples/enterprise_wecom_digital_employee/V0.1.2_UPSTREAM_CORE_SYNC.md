---
title: v0.1.2 upstream v0.1.1 core integration 复验记录
type: explanation
audience: [A2, A3, A4, A5]
runs: no
verified_on: 2026-08-03
sources:
  - contrib/agentseek-wecom/pyproject.toml
  - templates/langchain/default
  - templates/deepagents/enterprise-wecom
  - src/agentseek/__main__.py
  - src/agentseek/env.py
  - examples/enterprise_wecom_digital_employee/scripts/run_gateway.sh
---

# v0.1.2 upstream v0.1.1 core integration 复验记录

## 裁定：PASS

`enterprise/v0.1.2-upstream-core-sync` @ `897d14c22c21c222813a1a5c53cf3a33615500b2`
（父：企业 `e9e216e` v0.1.2 roadmap reset + 上游 `2d91d5e` AgentSeek v0.1.1，两者均为 ancestor）。
吸收上游 Lifecycle v2 / 服务发现 / 安全路径规范化 / 锁定版本模板 catalog / dotenv 不隐式注入 /
DeepAgents MCP·Sandbox·Hybrid RAG 模板 / WeCom 基础模板能力 / Windows 进程治理 / CLI 改进；
**企业边界全保留**（企业模板 lifecycle v1、schema rev11、Pack/Profile、Work 状态机、MCP 受保护文件均未改）。
**建议可进入企业模板 Lifecycle v2 迁移切片**（未合入 production、未打 GA tag）。

GitHub origin + GitLab company-gitlab 双端同步：`main=007233eb`、`production=e9e216e`、
集成分支 `enterprise/v0.1.2-upstream-core-sync=897d14c`（ls-remote 双端一致）。

## 一、同步与双端推送

- `main`：GitHub→GitLab 镜像 `refs/remotes/origin/main:refs/heads/main`，双端 `007233eb5aae1ba26759365d18ba4059a2c28da8`。
- `production`：GitHub `e9e216e` ff-only 同步到 GitLab（`8e89f0a..e9e216e`，merge-base=8e89f0a，未 force），双端 `e9e216ef1943…`。
- 集成分支：`897d14c` 推 GitLab（new branch），双端 `897d14c22c2…`。
- 两父 ancestor：`e9e216e` YES、`2d91d5e` YES。

## 二、静态回归

| 套件 | 结果 |
|---|---|
| `uv lock --check` | 237 包一致 ✓ |
| Core `pytest tests` | **1141 passed, 1 skipped** ✓ |
| Work | 106 ✓ |
| Enterprise | 87 ✓ |
| WeCom | 80 ✓ |
| Files + LangChain | 75 ✓ |
| Example | **325 passed, 1 skipped** ✓ |
| template render（全量） | 55 ✓ |
| ty（core `make typecheck` + 企业） | All checks passed ✓ |
| docs-test | built ✓ |
| ruff（touched: src/test_env/test_wecom_integration/test_templates_render/test_deepagents_mcp_template/test_docs_lifecycle） | All checks passed ✓ |

**OPS 注记**：Core 套件首次构建时 `pdm_build_skills` 后端 `git clone github.com` 失败（curl 28，无代理）。
需为 uv 构建设代理 env（`HTTP_PROXY/HTTPS_PROXY/ALL_PROXY=http://127.0.0.1:7890`）才能完成 build → 测试。
非代码问题，是本机 github 需走 FlClash 代理（[[github-needs-local-proxy]]）。

## 三、边界探针

1. **单一 WeCom entry point**：entry point group `bub` 下 `wecom = agentseek_wecom.plugin:main`
   **恰好一个**，来自 agentseek-wecom；根工作区**无 bub-wecom**（bub-wecom 仅在通用 langchain/default 模板）。✓
2. **langchain/default 用 bub-wecom**：`templates/langchain/default/.../pyproject.toml` 依赖 `bub-wecom`
   （git rev `d9f1771…`），README 注明 upstream `bub-wecom` adapter。✓
3. **企业模板兼容**：`templates/deepagents/enterprise-wecom` 仍 `.agentseek/lifecycle.toml`（lifecycle v1）；
   `.env.example` `AGENTSEEK_WORK_ENABLED=false`（默认 false）；`bub_gateway.py:82` 门控 work runtime；
   render 测试（`test_templates_render.py:74-75`）覆盖 Windows `D:\source trees\agentseek` + 引号 POSIX
   `/workspace/agent"seek` 路径 → 合法 TOML（tomllib 解析）。✓
4. **dotenv 安全**：`src/agentseek/__main__.py:69-81` `apply_agentseek_env_aliases()` 在
   `PYTHON_DOTENV_DISABLED=1` 包裹下执行 → 核心**不隐式读 .env**；企业 gateway 仍经
   `scripts/run_gateway.sh` `--env-file` 显式加载本机 .env（边界保留）。✓
5. **MCP protected zero-diff**：`mcp_policy.py` + `tools.py` vs `68d7b25` = **0 diff**。✓

## 四、企微最小活体（glm-5.2，run_gateway.sh python -u，PID 83339）

- **A 你是谁**：`digital_employee_service_discovery`、**0 model**、Markdown 排版
  （`**数字员工身份**`/`**协助能力**`/`**工作准则**` + 列表）。✓
- **B 冷启动第一条 `查看当前报告任务状态`**：gateway 重启后**首条** → routed→direct_response →
  业务化摘要（`**当前报告任务**` 主题/进度/覆盖期/`**已完成**`…），**0 model、无 no_match**（c9db64e fix #3 在 v0.1.2 保留）。✓
- **C `查看当前 ReportArtifact 和发布交付状态`**：direct_response（0 model）、**不 Guard 误拦**、
  有界详细账本（artifact v7 DOCX 已发布已交付 / publication v6 / 历史 6-5）。✓
- **D `请检索部门知识…数字化转型目标`**：`tools/call knowledge_search` + `knowledge_read_chunks`
  （统一 Capability Registry → department-knowledge），**无 call_mcp_tool/list_mcp_tools 泄漏**；
  返回部门知识结果；0 新 WorkItem。✓
- **E 5 条 distinct burst**：exactly-once（5 in ↔ 5 out），0 重传风暴、0 freeze、0 duplicate。✓
- A–D 全程 **0 新 WorkItem、未改任何报告合同/Artifact/Publication/Delivery**（work 总数 17 不变）。

## 五、健康与数据边界

- schema **rev11**（`LATEST_SCHEMA_VERSION=11`，0 migration）；Pack `1.13.0` / Profile `1.12.0`（未变）；
  0 PackSnapshot conflict。
- 本 session（21:27 起）：0 Traceback / 0 SIGBUS / 0 IntegrityError·UniqueViolation / 0 真 HTTP non-200 / 0 freeze / 0 重传风暴。
- **0 泄漏**：回复无 OA 原文、token、storage_key、grant_hash、宿主路径、隐藏工具名（call_mcp_tool/list_mcp_tools）。
- MCP audit 与 enterprise events 分离；MCP protected zero-diff。
- work 总数 17 不变，已交付 `work_ed04971` 仍可查询（数据边界保留）。

## 六、结论

**v0.1.2 upstream v0.1.1 core integration PASS。** 上游核心（Lifecycle v2 / 服务发现 / 安全路径 / 锁定模板 catalog /
dotenv 不隐式注入 / DeepAgents MCP·Sandbox·Hybrid RAG / WeCom 基础模板 / Windows 进程治理 / CLI）正确吸收，
企业边界全保留（lifecycle v1、schema rev11、Pack/Profile、Work 状态机、MCP 受保护文件、单一 agentseek-wecom entry、
run_gateway.sh 显式 .env）。静态全绿、边界探针通过、Live A-E 通过、健康脱敏通过。**建议进入企业模板 Lifecycle v2 迁移切片。**
未合入 production、未打 GA tag。

---

# Lifecycle v2 迁移复验（57cb925）

## 裁定：PASS — 可关闭 Lifecycle v2 迁移切片

`enterprise/v0.1.2-upstream-core-sync` @ `57cb9251c35438a7a3d07c57d69976c3a6dcaa10`
（父验证基线 `9ea952e`，确认为 ancestor）。本轮**仅迁移企业模板与仓库内企业示例的 Lifecycle 契约**
（lifecycle.toml v1→v2），**未修改生产业务逻辑、schema、Pack/Profile、Work 状态机、MCP 策略或企微消息链路**。
GitHub origin + GitLab company-gitlab 双端 `enterprise/v0.1.2-upstream-core-sync=57cb9251…`（ls-remote 双端一致）。
未合入 production、未打 tag。

### 迁移范围（纯元数据）

`git diff 9ea952e..57cb925 --stat`：仅 `examples/.../.agentseek/lifecycle.toml`、
`templates/deepagents/enterprise-wecom/{{cookiecutter.project_slug}}/.agentseek/lifecycle.toml`、
模板 `README.md`、`tests/cli_commands/test_templates_render.py`、若干 `docs/*.md`。
**0 运行时 `.py` 变更**（消息链路 / 队列 / Guard / Work / MCP 代码与基线逐字节一致）。

## 一、Lifecycle v2 静态契约

两个 `lifecycle.toml` 均 `version=2`、`template="deepagents/enterprise-wecom"`、`guide="README.md"`、
`env_file=".env"`：

- 唯一主服务 `wecom-gateway`：`url=http://127.0.0.1:12000/health`（模板用 `{{cookiecutter.wecom_port}}`）、
  `kind=api`、`display=advanced`、`primary=true`、`tech=wecom`。
- `[processes.gateway]` `provides=["wecom-gateway"]`；`[checks.wecom-gateway]` 绑定同名服务，
  `timeout=2.0`、`attempts=10`。
- `[tasks.sync]`/`[tasks.prod-check]` 保持不变；`AGENTSEEK_WORK_ENABLED` 默认 `false`；
  `run_gateway.sh` 仍 `--env-file` 显式加载本机 `.env`（line 16）+ `python -u`（line 17）。
- **脱敏**：env 仅暴露**名称**+描述+占位默认（`AGENTSEEK_WECOM_CALLBACK_PATH` 示例为 `/ai-bot/callback/demo/<botid>`，
  模板为 `{{cookiecutter.wecom_callback_path}}`）；无 Token/EncodingAESKey/API key 的**值**、无宿主绝对路径、
  无进程完整命令参数。✓

## 二、静态回归

`uv lock --check`(237) / Core **1141+1skip** / Work 106 / Enterprise 87 / WeCom 80 / Files+LangChain 75 /
Example **325+1skip** / template render **55** / `make typecheck` + 企业 `ty` / ruff(touched) / docs-test / `git diff --check`
全绿。MCP 受保护文件 `mcp_policy.py` + `tools.py` **vs 68d7b25 = 0 diff**。

## 三、Lifecycle CLI（info / doctor）

`agentseek info --json`：`ok=true`、`lifecycle_version=2`、`metadata_complete=true`、`project.guide.path=README.md`、
仅一个 service `wecom-gateway`、`provider=process:gateway`、`check_ids=["wecom-gateway"]`、
actions 恰好 `project:start_dev` + `service:wecom-gateway:copy`、`warnings=[]`、无凭据值/回调实际路径/宿主路径/进程命令。
`agentseek doctor --json`（非 live）：`passed=true`，lifecycle spec/工具/路径/环境检查全 pass，`warnings=[]`，
不打印任何凭据值（仅 env 名称 + "is configured"）。✓

## 四、Live health（run_gateway.sh + doctor --live）

`agentseek doctor --live --json`：`passed=true`、`service-check:wecom-gateway` state=**pass**
（`http://127.0.0.1:12000/health is reachable`，/health 返回 `ok=true`）、`warnings=[]`。
纯 HTTP 探测，未触发模型/WorkItem/MCP/报告合同变化，未泄露 callback path 与凭据。✓

## 五、最小企微活体（Live A–E）

> 活体配置：本机 smoke override（**仅本地、未提交**）—— `owning_org: 信息技术部`（profile + knowledge_ref，
> pack_loader 强制一致）+ `pack_version: 1.13.0-mac-smoke.2`（避免与已提交 `1.13.0` 内容寻址快照冲突，
> 沿用历史 `-mac-smoke.N` 约定）。committed Pack/Profile 仍为 `1.13.0`/战略发展部。
> 模型 `deepseek-v4-flash-0731`、`AGENTSEEK_WECOM_SESSION_QUEUE_MAXSIZE=8`（见六）。

- **A `你是谁`**：`digital_employee_service_discovery`、**0 model**、`**数字员工身份**` Markdown。✓
- **B `查看当前报告任务状态`**：direct_response、**0 model**、`**当前报告任务**` 业务摘要、**无 no_match**。✓
- **C `查看当前 ReportArtifact 和发布交付状态`**：direct_response（**0 model**）、**不 Guard 误拦**、未改合同。✓
  （非阻塞：该查询回退为 raw 状态文案 `work_id=…/状态=已交付/阶段=intake/服务=…`，未走业务化排版——同历史 finding #2 类。）
- **D `请检索部门知识…`**：`tools/call knowledge_list_documents` + `knowledge_search`（统一 Capability Registry
  → department-knowledge），**无 `call_mcp_tool`/`list_mcp_tools` 泄漏**；返回 5 份内部资料；0 新 WorkItem。✓
- **E 5 条 burst**：`SESSION_QUEUE_MAXSIZE=8` 下 **5 in ↔ 5 turn_started ↔ 5 stream_finished**、
  `wecom_turn_queue_rejected=0`、exactly-once（回复 1–5「已收到」各一）、0 重传风暴、0 freeze。✓

A–E 全程 **0 新 WorkItem、未改任何报告合同/Artifact/Publication/Delivery**（账本 items=17/contracts=92/
artifacts=8/publications=6/deliveries=11 全不变；今日 0 新 work_item、0 新 work_event）。

## 六、非阻塞观察（非迁移回归）

1. **会话队列容量与 burst 上限**：`agentseek_wecom/config.py:75` `session_queue_maxsize` **代码默认=3**。
   `channel.py:883-895` 在「1 条处理中 + 3 条等待」时对超额消息执行**设计内背压拒绝**
   （`wecom_turn_queue_rejected` + 用户可见「请等待前面的消息完成」提示）。因此对单会话紧凑 5-burst，
   在模型较慢（pending>3）时会拒绝 1–2 条——**这是背压正常工作，非 bug、非 freeze、非迁移回归**
   （运行时 `.py` 与基线逐字节一致）。本轮本地调到 8 以验证 5-in↔5-out exactly-once；maxsize=3 时
   背压路径亦被独立观察到（`wecom_turn_queue_rejected` 触发 + 提示文案）。
   建议：在文档中注明「exactly-once N-burst 需 `session_queue_maxsize≥N`」，或评估默认值是否上调。
2. **Live C 状态文案**：artifact/delivery 状态查询回退为 raw 契约文案而非业务化排版（同历史 finding #2）。
3. **smoke override 仍是活体测试前置**：committed 数字员工属战略发展部，朱春霖=信息技术部，活体需本地
   `-mac-smoke.N` + `owning_org` 覆盖（已知、已记录）。

## 七、企业边界与健康脱敏

- schema **rev11**（0 migration）、Pack `1.13.0` / Profile `1.12.0`（committed 不变）、0 PackSnapshot conflict、
  Work 状态机不变、MCP 受保护 zero-diff。
- 本轮 A–E 窗口（22:37–22:59）：**0 Traceback / 0 SIGBUS / 0 Integrity·UniqueViolation / 0 WorkConflictError /
  0 真 HTTP non-200 / 0 freeze**。（累计日志中的 404 为历史探针、410 为 signed-link 一次性 grant 复用——by-design。）
- **0 泄漏**：回复无凭据值、OA 原文、token、storage_key、grant_hash、宿主路径、隐藏工具名。
- root workspace 仍只有 `agentseek-wecom` 提供一个 `bub/wecom` entry；企业模板 fork-local、不进上游 locked catalog；
  其他上游 templates 保持 Lifecycle v1 compatibility mirror；Windows 路径与含引号 POSIX 路径 render 仍通过。

## 八、结论

**Lifecycle v2 迁移切片 PASS，可关闭并进入 v0.1.2 下一阶段。** 迁移为纯元数据（lifecycle.toml v1→v2 + 文档 + render 测试），
**0 运行时 `.py` 变更**；静态全绿、v2 契约与脱敏通过、info/doctor（含 --live）通过、Live A–E 通过、
企业边界与健康脱敏通过。非阻塞观察（队列默认上限=3 的 burst 容量关系、Live C 文案、smoke override 前置）
均与本次迁移无关，建议另册跟踪。未合入 production、未打 tag。

---

# 默认模型统一为 deepseek-v4-flash-0731（1ff335f）

## 裁定：PASS

`enterprise/v0.1.2-upstream-core-sync` @ `1ff335f10d1de72e6ba6095baa7dee468162267a`
（父基线 `401c36d`，确认为 ancestor）。本轮**仅把企业数字员工模板与仓库示例的默认模型统一为
`deepseek-v4-flash-0731`**，**未修改运行时逻辑、schema、Pack/Profile、Work、MCP、企微消息链路或本机 `.env`**。
GitHub origin + GitLab company-gitlab 双端 `enterprise/v0.1.2-upstream-core-sync=1ff335f1…`（ls-remote 双端一致）。
未合入 production、未打 tag。

## 一、变更范围（6 文件，纯默认值/文档）

`git diff 401c36d..1ff335f --stat` 恰好 6 文件、+8/-4：

1. `templates/deepagents/enterprise-wecom/cookiecutter.json` — `default_model: openai:gpt-4o-mini → deepseek-v4-flash-0731`。
2. `templates/deepagents/enterprise-wecom/README.md` — `default_model` 说明补充实际值。
3. `examples/enterprise_wecom_digital_employee/.env.example` — `AGENTSEEK_MODEL=openai:gpt-4o-mini → deepseek-v4-flash-0731`（`AGENTSEEK_MODEL_PROVIDER=openai` 不变）。
4. `examples/enterprise_wecom_digital_employee/.agentseek/lifecycle.toml` — `[env.AGENTSEEK_MODEL] default` 同步。
5. `examples/enterprise_wecom_digital_employee/DEVELOPER_QUICKSTART.md` — 补注默认模型与本机 `.env` 覆盖说明。
6. `tests/cli_commands/test_templates_render.py` — 渲染断言 `.env.example` 与 lifecycle 默认值。

**0 运行时 `.py` 变更**（唯一 .py 是 render 测试断言）、**0 schema、0 Pack/Profile、0 MCP protected**（`mcp_policy.py`+`tools.py` vs `68d7b25` = 0 diff）。✓

## 二、默认值一致性

- `cookiecutter.json` `default_model=deepseek-v4-flash-0731`。✓
- 示例 `.env.example`：`AGENTSEEK_MODEL_PROVIDER=openai`、`AGENTSEEK_MODEL=deepseek-v4-flash-0731`。✓
- 示例 Lifecycle v2：`[env.AGENTSEEK_MODEL] default="deepseek-v4-flash-0731"`。✓
- **默认参数渲染企业模板**（`cookiecutter --no-input`）：生成项目 `.env.example` 含 `AGENTSEEK_MODEL=deepseek-v4-flash-0731`、
  lifecycle `[env.AGENTSEEK_MODEL] default="deepseek-v4-flash-0731"`；**全项目 `openai:gpt-4o-mini` 残留=0**。✓
- 本机 `.env` 未被覆盖（`AGENTSEEK_MODEL=deepseek-v4-flash-0731`，与本轮默认一致；模板默认仅影响新项目/示例）。

## 三、静态回归

- template render：**55 passed**。✓
- Example：**325 passed, 1 skipped**。✓
- ruff（`test_templates_render.py`）：All checks passed。✓
- `make docs-test`：built。✓
- `git diff --check`：clean。✓
- MCP protected zero-diff（vs `68d7b25`）：0 diff。✓

## 四、最小活体回归（run_gateway.sh，本机 .env）

进程 env 确认 `AGENTSEEK_MODEL=deepseek-v4-flash-0731` 已加载；活体用本地未提交 smoke override
（`owning_org=信息技术部`+`pack_version=1.13.0-mac-smoke.2`，committed 仍 1.13.0/战略发展部）。

- **`你是谁`**：`digital_employee_service_discovery`、**0 model call**、`**数字员工身份**` Job Charter。✓
- **`你好`**：`langchain_model_call` 事件 **`model_name=deepseek-v4-flash-0731`**、回复「朱春霖，你好！👋」、exactly-once（1 in ↔ 1 out）。✓
- **`查看当前报告任务状态`**：direct_response、825ms（确定性）、**0 model call**、`**当前报告任务**`、**0 新 WorkItem**。✓

## 五、企业边界与健康脱敏

- schema **rev11**、Pack `1.13.0`/Profile `1.12.0`（committed 不变）、Work 状态机不变、MCP zero-diff。
- 活体窗口（23:07+）：**0 Traceback / 0 SIGBUS / 0 Integrity·UniqueViolation / 0 WorkConflictError / 0 真 HTTP non-200 / 0 freeze**。
- **0 泄漏**（无凭据值、OA 原文、token、storage_key、grant_hash、宿主路径、隐藏工具 `call_mcp_tool`/`list_mcp_tools`）。
- **账本不变**：items=17/contracts=92/artifacts=8/publications=6/deliveries=11/events=40 全不变；今日 0 新 WorkItem。

## 六、结论

**默认模型统一为 `deepseek-v4-flash-0731` PASS。** 6 文件纯默认值/文档变更，0 运行时/schema/Pack·Profile/MCP 变化；
模板默认值三处一致（cookiecutter/.env.example/lifecycle）、渲染无 `openai:gpt-4o-mini` 残留；静态全绿；
活体确认实际加载与 `langchain_model_call` 模型名均为 `deepseek-v4-flash-0731`，A/B/C 三条消息行为正确、0 WorkItem 变化、健康脱敏通过。
未合入 production、未打 tag。
