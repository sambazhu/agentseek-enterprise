# 统一业务材料生成器——.172 离线复验结果（zcode，2026-09-22）

## 判定：**BUSINESS_MATERIALS_OFFLINE_PASS**（合成生成 + 读回复验 + 39 项测试全通过）；不代表真实材料/服务就绪/业务全链通过

## 一、到货核验

BIZMAT-MANIFEST **6/6 OK**（包目录 4 件含清单自身）；候选 aed0c76a9f7bd1be5d8c02da2232f10865fd0ead。
交付 = **统一生成器（business_materials.py 378 行 + example 103 行 + test 214 行）**——纯 scripts 交付，零 wheel 变化（45 模块/2 入口不变）。

## 二、环境

- 合成生成与读回复验：使用已装业务 wheel 的 50adc3f venv（proxy_port=80 修复版本）
- 39 项测试：使用 cdiag venv（有 pytest + 9d54662 业务 wheel；本轮工具为纯 scripts，不需业务 wheel）
- 零混装、零联网、零升级依赖

## 三、合成生成结果

`business_materials_example.py --directory <新目录>`（不预创建目录——工具自行 mkdir 0700）：
- 输出 `input.json` + `sources/` 目录（9 个合成凭据文件：api_key/broker_token/ca/certificate/private_key/receipt_key/identity-executable/identity-script/identity-unit_file）
- input.json SHA256 = `5cc41418e72ca6995dc6fd895fd45c87d73507cdd4f4aef940aa557bfd06db64`
- 全部合成假数据 ✓

## 四、生成后读回复验

`business_materials.py --input input.json --sha256 <digest>`：
- **status = MATERIALS_READBACK_VERIFIED** ✓
- **files = 16** ✓
- **execution_authorized = false** ✓
- service_sha256 = `1161276086…bb4`

## 五、--verify-only 只读复验

同输入同摘要 `--verify-only`：**MATERIALS_READBACK_VERIFIED、exit 0**——只读成功，文件内容/mtime 前后不变 ✓

## 六、无输出情形 --verify-only BLOCKED

对不存在目录路径测试：**status=BLOCKED、error=io_or_contract、exit 2**——不创建输出 ✓（与生成后成功验证分别记录）

## 七、39 项测试

在已装业务 wheel 的 50adc3f venv 下运行 test_business_materials.py：**39 passed**——覆盖漏 key/错摘要/权限/篡改/漏写/verify-only/exact-readers 等合成破坏测试。

## 八、边界终检

不创建 guest、不探针、不手工 perform、不切 broker/网关、不修改 unit、不重装运行时 wheel、不修改网络、不清理/重置/覆盖旧额度/fence/manifest/账本/回执、不删除或篡改真实文件测试失败路径、零 Git 操作。判定上限 BUSINESS_MATERIALS_OFFLINE_PASS。
