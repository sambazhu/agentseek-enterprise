# 业务窗口部署执行与 BLOCKED 报告（zcode，2026-09-20 窗口 14:45–16:45 UTC）

## 判定：**部署六项中 1-6 完成；第 7 项（创建链材料+服务最终件）BLOCKED_IMPLEMENTATION——候选 3009d82 身份合同硬编码 R1 单元名，无法验证第二监督服务**；创建未执行（guest=0）；窗口不自动顺延

## 一、writer 确认（全部 <14:45 start）

- **zcode-172 本人确认：出具/实收 2026-09-20T14:37:09Z**（覆盖本窗口；除本轮批准范围内一次业务创建由程序执行外，不自行/不经工具创建、不执行未批准探针终止；特权通道披露五条：.172 root 会话通道、/opt 各 venv 创建入口、API key 受控源+物化件、vault key/监督 pins 等 root 可读私有材料、无出站 SSH/无 .171 凭据/无其他 key 副本）。
- 台账 /var/lib/agentseek-m3-business/W0-biz-20260920-confirmations.json（1a5ccae8…）：user-zhuchunlin（工单=六项批准，14:36:20Z）、cc-171（工单载明 14:36:20Z）、zcode-172（14:37:09Z）**3/4 已收**；**codex 待收**（14:45 前未达——W0 记录闭合第二缺口）。

## 二、已执行部署（六项批准范围内；命令即"可执行命令清单"实录）

| # | 步骤 | 结果 |
| --- | --- | --- |
| 1 | iptables 三条（CUBE_POC_GUARD：ACCEPT 192.10.50.171/ACCEPT 127.0.0.1/REJECT 其余，dport 13100） | 已生效（iptables -S 在册）；恢复=-D 三条 |
| 2 | 业务 venv /opt/agentseek-business-releases/3009d82/venv（wheel 3d17d872…+交付 deps 离线） | install_check --profile business **44/2/isolated/exit 0**（工单"36-2"系笔误，实测为既定合同 44/2） |
| 3 | 目录树 /var/lib/agentseek-m3-business/runs/agentseek-m3-biz-20260920/{supervisor,vault,fence,quota,store,workspace,config/{tls,plans}} | 全 0700 |
| 4 | 业务 CA（CN=agentseek-business-ca，180d，私钥 0600 /etc/agentseek-business-ca/）+服务器证书（CN=agentseek-business-node，90d，**SAN=IP:192.10.50.172 实证**，key 0600 config/tls/） | openssl 输出在册 |
| 5 | broker token（64 hex）+principal_sha256=**5111a50c6501b4b3486444788f75edea142c8355b0f326c56ebd7848e38ac946**；受控交接包 /root/m3-biz-creds-handoff-20260920（0700：business-ca.crt d52e0d85… + broker-token.txt + HANDOFF-MANIFEST） | 待 .171 经用户通知只读拉取 |
| 6 | 新 run 登记（agentseek-m3-biz-20260920/agentseek-m0-poc/tpl-c4ba…，guest=0 时刻）+unit agentseek-m3-biz-supervisor.service（UMask=0077/120s 冻结/不 enable）+启动 | **active、disabled、心跳新鲜、无告警**（pid 2278380）；**保持运行**（合法监督，不因停止报告而关闭） |

## 三、BLOCKED_IMPLEMENTATION（步骤 7 冻结根因）

- **候选 3009d82 的 m3_supervisor_identity.py:24 硬编码 `UNIT="agentseek-m3-r1-supervisor.service"`**（安装件与源码闭包双确认）——verify_identity 的 `_show()` 只查该 R1 单元（MainPID=2750111），业务监督进程（2278380）**永不可匹配**；而 R1 单元名被现役 R1 服务占用，第二服务不可同名。
- 连锁影响：创建链 precreate 的 supervisor_identity pins 无法生成/验证 → 创建链材料（approval/precreate/launcher/lifecycle）无法闭合 → 服务 approved=true 最终件（plans 引用 lifecycle/business 文件摘要）无法合法生成。**按合同在创建前停止**：未现场改代码、未重命名规避、未伪造 pins。
- 修复需 Codex 新候选（unit 名参数化或业务专用常量）→ 走离线复验轮 → 再进窗口执行。本窗口（16:45 UTC 截止，不自动顺延）大概率不足以完成该循环。
- 第二缺口：codex 本人 W0 确认未于 14:45 前到达（W0 记录闭合另需其确认时刻 ≤start）。

## 四、现场状态与恢复

- guest=0（全程零创建零探针）；R1/production/M0/M2 零触碰；R1 三服务+新业务监督共四服务 active。
- 已部署项恢复办法：iptables -D 三条；新监督 systemctl stop（留审计）；交接包与证书/私钥目录留存或按作废流程销毁记录；venv/bizroot 留审计。全部变更清单在案。

## 五、建议

1. Codex 尽快出 unit 名修复候选（最小改动：UNIT 常量参数化/按 run 配置）；
2. codex 本人 W0 确认随下轮工单一并给出（新窗口重收集亦可）；
3. 用户裁定：本窗口作废与重开新窗口（不自动顺延），或批准保持已部署基础设施待下一候选（全部可恢复）。
