# 统一生成器候选 aed0c76 .171 复验结果（2026-09-22）

## 判定：BUSINESS_MATERIALS_OFFLINE_PASS（39+合跑 45 与开发端一致）

| 项 | 结果 | 备注 |
| --- | --- | --- |
| Git | aed0c76（父=a5bac60 ✅）；GitLab 补推双端一致 | — |
| 新测试 | **39 passed（11.22s）** | 必须用已装业务 wheel 的 venv（toolvenv 无 wheel→1 fail；源码 PYTHONPATH 不够——精确 readers 测试需安装态模块） |
| 合跑 | **45 passed（10.52s）**（39+清单 6） | 开发端 45 一致 ✅ |
| 示例工具 | `--directory` 生成合成 input+sha256 ✅ | synthetic_only=true |
| verify-only | BLOCKED（io_or_contract，execution_authorized=false） | **正确**——只传入 input 不带完整目录上下文=拒绝（只读不写不创建 ✅） |
| wheel | **零变化**（45 模块/2 入口，纯 scripts 交付） | 无需重建重装 |
| 交接 | 包 7 件→.172 /root/m3-bizmat-handoff-20260922（6/6 OK） | tools×3+docs×2+README+MANIFEST |

## 自检合同核验要点
- 生成后**实际读回**每枚 *_file：存在性/权限/预期摘要/跨文件引用
- **缺预期摘要报"未覆盖"**（不从输出倒算）
- **失败非零退出不 READY 不启服务**
- **物化摘要从受控源计算**（不从生成内容倒算）
- 合成破坏测试全覆盖：漏 key/错摘要/权限/引用/漏写/篡改

## 教训（.171 首跑 1 fail）
**必须用已装业务 wheel 的 venv** 运行测试——精确 readers 测试 import 安装态模块（agentseek_execution.business_service），toolvenv 无 wheel 会 ModuleNotFoundError。工单 §.172.4 原文"在已安装业务wheel的独立venv下运行"=此意。
