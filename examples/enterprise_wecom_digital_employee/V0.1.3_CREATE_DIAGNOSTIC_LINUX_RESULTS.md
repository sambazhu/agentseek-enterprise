# 创建链诊断补丁——.172 离线复验结果（zcode，2026-09-22）

## 判定：**CREATE_DIAGNOSTIC_OFFLINE_PASS**（45/2 + 907 + 1 + 新增 10 项测试覆盖确认）；不切 /opt/broker、不清 W9 状态、零 guest/零 Git

## 一、到货核验

CDIAG-MANIFEST **172/172 OK**；wheel `9f097a974e35e2e7844f8ceb41d95828b9f927b59951541a2fd3b38db6eb621c` 一致；候选 7ea9ef7c93afdb36ab8e24f57bc2afe98bc03538（父=a2ba27e）。

## 二、隔离复验

- 新 scratch+venv-cdiag（纯交付依赖 --offline --no-index）；install_check **45/2/isolated/exit 0**（新增 execution_diagnostic.py → 45 模块）。
- 闭包 pytest **907 passed**（首跑 1 failed=test_secure_ownership 同前五轮瞬态，复跑 **907** 达标；+10 vs 上轮 897——新增 10 项测试在 test_execution_diagnostic.py 中）。
- 业务清单 **1 passed**；零残留。

## 三、新增 10 项测试覆盖核验

execution_diagnostic.py（85 行）提供**父进程阶段诊断**——不捕获 worker stderr 或 payload，只输出标量白名单值。

| # | 合同要求 | 测试（全部 PASSED） | 源码依据 |
| --- | --- | --- | --- |
| 1 | worker PID 捕获+脱敏 | test_real_worker_pid_cleanup_and_redaction | :25-43 emit 的 PID/returncode 字段 + :43-49 worker_id 校验（32hex） |
| 2 | 超时 reaps 子进程 | test_real_timeout_reaps_child | worker_process.py 的 timeout → group kill → reaped |
| 3 | spawn 失败保留原始异常（不伪造清理结果） | test_spawn_failure_preserves_exception_and_no_fake_cleanup | :57-75 phase 的 failed 事件在异常时触发→raise 原始异常 |
| 4 | exchange 失败→清理+异常分类 | test_exchange_failure_cleanup_and_original_category[connect/timeout/contract] | error_kind :24-33 白名单分类（TimeoutError/OSError/ContractError→contract/protocol/…） |
| 5 | 清理失败不被误报为 reaped | test_cleanup_failure_is_not_reported_reaped | :80-84 phase finally 中 token reset，不额外 emit |
| 6 | provider 创建失败与会话缺失区分 | test_provider_creation_failure_and_no_session_are_distinct | stage 区分 provider_create vs provider_run |
| 7 | 并发 scope 隔离 | test_parallel_scopes_are_independent | ContextVar 隔离（:20 _scope）；不同线程不交叉 |
| 8 | 未知字段/logger 失败不泄露 | test_unknown_fields_and_logger_failure_do_not_leak_or_change_result | :54-56 emit 全部 try/except 吞噬（logging 失败不影响结果） |
| 9-10 | 既有 launcher 日志断言 + 脱敏 | test_worker_diagnostic_is_redacted_and_not_success（W5 已有，继续通过） | business_cube_session.py:69 |

**STAGES 白名单**：provider_create/provider_run/provider_destroy/lifecycle/intent_save/result_save/launcher/create_worker/attach_worker/session_reconstruct/closeout/worker_spawn/worker_exchange/worker_cleanup（14 个阶段）
**EVENTS 白名单**：started/complete/failed/skipped/spawned/kill_sent/group_absent/reaped（8 个事件）
**error_kind 白名单**：none/contract/TimeoutError/TimeoutExpired/PermissionError/ProcessLookupError/FileNotFoundError/OSError/ValueError/RuntimeError/CancelledError/KeyboardInterrupt/SystemExit/other

## 四、与本轮真实执行的关联

W9 的 4m44s 间隙与零诊断缺口——此补丁正是为了在**下次真实执行**时提供：
- 父进程每个阶段的开始/完成/失败事件+elapsed_ms
- worker PID+returncode+错误分类（脱敏白名单，无敏感正文）
- 并发 scope 隔离（ContextVar）

**不记 W9 根因已修复**——补丁提供诊断能力，W9 的 guest 消失原因需在下一次真实执行中通过诊断日志确认。

## 五、边界

不切 /opt、不切 broker（当前 broker 仍 09949c8）、不清 W9 状态、不用真实配置运行本地 execute/perform、零创建/零探针/零 Git。仅运行交付的合成测试（临时子进程正常收尾）。

## .171 汇总：安装与批准引用变化（CREATE_DIAGNOSTIC_OFFLINE_PASS 后）

| 项 | 值 |
| --- | --- |
| 节点拟部署版本 | **7ea9ef7c / wheel 9f097a97…eb621c**（907/45-2/清单 1 双端一致；第十三次确定性命中） |
| 清单扩容 | 44→**45 模块**（execution_diagnostic 入闭包）；入口仍 2；测试 42→43 路径 |
| 安装影响 | 落位时新 /opt/agentseek-business-releases/7ea9ef7c/venv+broker unit 切换+service-config 重钉（**引用链须全链重钉至 7ea9ef7c+新 wheel 摘要**——含 approval/precreate/create-installation/launcher 的 candidate_sha256） |
| 批准引用变化 | 下次执行窗口须**新 request_id #10+全链 W10 材料**（W9 授权已耗+reconciling 保留；"额度未耗≠材料可续"已两次验证）；新清单 45 模块影响 install_check 预期值 |
| 诊断能力 | 14 阶段/8 事件/14 error_kind 白名单——**随下次真实执行自然实证**（无需额外创建测试日志） |
| 现场终态 | broker 仍 13f9114+W9 配置（未切）；W9 reconciling 保留；guest=0；生产网关正常 |
