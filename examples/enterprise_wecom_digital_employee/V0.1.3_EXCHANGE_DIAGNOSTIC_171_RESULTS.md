# 交换诊断候选 bf4602f .171 复验结果（2026-09-21）

## 判定：EXCHANGE_DIAGNOSTIC_OFFLINE_PASS

| 项 | 结果 | 开发端对照 |
| --- | --- | --- |
| 网关套件 | **277 passed（26.73s）**=272+5 新增 | 277 一致 |
| HTTP 专项 | **12 passed（1.70s）** | 12 一致 |
| 警告 | 33 条（既有 websockets/Pydantic 类型） | 32（+1 同型，非失败） |
| Git | bf4602f（父=04aa47a ✅）；GitLab 补推双端一致 | — |

## 新增覆盖核验（工单 §5-6）
- 真实 DeepAgent 图+ToolRuntime+asyncio.to_thread+BusinessHttpClient+loopback TLS listener+合成证书，五例：成功回写及诊断脱敏/证书拒绝/**TCP 后 TLS 前 RuntimeError 注入**/已执行后响应超时与只读恢复/取消图任务后原线程继续落盘不重创
- **工单 §6 如实遵守**：本地无故障注入时完整路径通过——**不能预先归因于 asyncio.to_thread/httpcore**；现场根因待实际网关进程内只读诊断（需用户另批）

## 边界
零 guest/零生产切换/零 W6 意图清理/节点零重装；未用 shell 直驱替代图测试。
