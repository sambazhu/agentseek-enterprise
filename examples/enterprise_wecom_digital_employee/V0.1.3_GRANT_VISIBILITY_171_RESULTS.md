# 授权可见性候选 b9d971c .171 复验结果（2026-09-21）

## 判定：GRANT_VISIBILITY_OFFLINE_PASS（网关 272 passed 与开发端一致）

| 项 | 结果 |
| --- | --- |
| Git | b9d971c（父=9b05954 ✅）；GitLab 补推双端一致（Codex 连接未完成由 .171 代推，普通推送） |
| 网关测试 | **272 passed（20.20s）**——工单 glob 范围（files/wecom+test_sandbox*+agent_harness）；.171 首跑 262 系漏三个 glob 命中文件（authorization/deepagent_loop/tools），按原命令复跑对齐 |
| 警告 | 13 条 vs 开发端 12（多一条同型 Pydantic context 序列化，非失败） |
| 节点 | 沿用 9d54662/3efe201b 零重装（本轮纯网关修复） |
| 新增覆盖 | 16 项（无历史/旧失败/新授权可见/过期/跨 scope/缺文件/错指令/单参/篡改/catalog 错/已耗含非最新/未决+其他 owner 阻断/查询后过期执行拒/图内查询→执行→回写） |

## 工具合同核验要点
- get_sandbox_task_result 保留历史 state+新增 grant_available/authorization；无历史=no_task 非异常
- 授权检查复用服务端 grant 解析（可信三键/唯一 catalog/有效期/文件唯一+盘上全核验）；mode=ro 读镜像账本；任意未决阻断；已耗请求（含非最新）不可误判
- 不联网/reserve/写账本；执行仍重新解析+原子 reserve+节点 permit 三重把关
- run_sandbox_task 文档区分历史失败与新授权；无新增模型可控身份/批准参数

## 边界
本轮零创建/零服务切换/零状态清理/不签发授权；真实模型选择、CSV 执行、工作区回写待下一次授权验收（判定上限即本值）。
