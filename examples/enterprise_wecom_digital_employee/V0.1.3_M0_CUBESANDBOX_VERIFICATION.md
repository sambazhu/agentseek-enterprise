---
title: v0.1.3 M0 CubeSandbox 双机部署与复验
type: how-to
audience: [A3, A4]
runs: no
verified_on: 2026-09-08
sources:
  - examples/enterprise_wecom_digital_employee/V0.1.3_EXECUTION_ISOLATION_PLAN.md
  - examples/enterprise_wecom_digital_employee/V0.1.3_M0_SANDBOX_ADMISSION.md
  - pyproject.toml
---

# v0.1.3 M0 CubeSandbox 双机部署与复验

当前结论：**M0 PASS（2026-09-08，用户确认），限定于"单可信 PoC、既定风险豁免"范围。**

- 评审基线 `3d25447`（R4 整改后）：Codex 建议 M0 PASS（同上限定），用户确认；
- 判定材料：本手册 §10–§14 执行与四轮整改记录 + 证据包
  （V0.1.3_M0_R1_EVIDENCE_PACK.md，现场原件 .172 /root/cube-poc-evidence/）；
- **范围边界**：单可信 PoC 身份（O4b 跨身份授权未验证、不计通过，留待
  Broker 验收 §11.7）；镜像漏洞扫描已豁免（用户决定，**豁免≠已评估**，
  §12.5）；存储为同机 MinIO（单故障域，§5 O10 注记）；
- 遗留及责任归属见 §15.3（egress 根因=平台运维跟踪、观察面=preflight
  门禁；创建链不校验 egress=上游反馈项）；
- **不进入 M1**：M1 启动另由用户确认，本 PASS 不授权。

以下 §1–§9 为安装前操作合同（保留原文）；§10–§14 为安装与 R1–R4 整改
实录（全部历史失败与修复保留，不重写）。

## 1. 固定基线与版本

使用功能分支 `enterprise/v0.1.3-cubesandbox`，基线
`b3392285b3eded238a71e37c38c28b2b162f8171`。它包含 GA 后已发布的架构文档，
`enterprise-wecom-v0.1.2-ga` 为其祖先，保留 production 和 tag 原状。

| 项目 | 决定 | 理由与限制 |
| --- | --- | --- |
| Cube 服务端 | 确认 v0.7.0，commit d0081641c59822e4e5653b7462e914410b81910a | 官方 release 可核验；锁版本不代表运行已通过 |
| Python SDK | 确认 cubesandbox==0.7.0 | PyPI 存在，声明 Python >=3.9；3.13 运行与 Volume 仍需 PoC |
| Kernel release | 确认 kernel-release-260812-1 的普通 vmlinux-amd64 | VMware 暴露硬件 KVM 后使用普通 Guest 内核 |
| PVM 宿主内核 | 本轮不采用 | 同一 release 包含 PVM 宿主包，不能将它与普通 Guest 内核混用；不换 Ubuntu 宿主内核 |
| Guest image | 确认 guest-image-260820-1 的 amd64 制品 | 构建时核验与 v0.7.0 bundle 元数据匹配；不匹配立即停止 |
| Volume | 确认 v0.7.0 自带 S3 driver，不自研 | 明确 driver=s3；只读 attachment 通过原生 SDK 验证 |
| 存储 | 确认 PoC 使用内置 MinIO，位于 .172 | 不声称独立故障域或生产 HA；需补实际镜像 digest |
| DeepAgents | 确认维持 0.6.12 | 不改项目依赖锁文件 |
| v0.6.0 回退 | 仅保留候选，不锁为自动回退 | 默认回滚为停用本轮 Cube 组件；降版本需验证数据格式和配置兼容 |

官方元数据摘要（安装前下载后重新计算，不仅相信文件名）：

| 制品 | SHA-256 |
| --- | --- |
| cube-sandbox-one-click-v0.7.0-amd64.tar.gz | d4522eb97fe898dd8ea681a7caaadbfd1f331d98df76a7c08e5865d6b7229ba4 |
| cubesandbox-0.7.0-py3-none-any.whl | 3dc5d452ccb01684de7a2e12f00659c1df2798d484ea66c9e546c76bb459fa05 |
| kernel-release-260812-1 / vmlinux-amd64 | 3ea4a05e2b74e1b4baaafb21875c9ce0dde8fdfc435723f6c39a98735a64a5a4 |
| guest-image-260820-1 / cube-guest-image-amd64.tar.gz | 945d2d7945aea31c18f3f0f4db1cfd80dd6d3b2188c9136a1f1ddbdf06e8dc6f |

完整部署锁定还必须包含依赖镜像、Volume 插件、模板 ID 与内容 digest。
这些尚未下载或构建，不伪造已锁定状态。禁止 latest、浮动安装脚本和静默回退。

## 2. 前置门：管理员完成后补验

1. 管理员在 vSphere 暴露嵌套虚拟化并安排 `.172` 重启。由于原 uvicorn 非
   systemd，业务恢复由其负责人安排；先确认原业务恢复再做 Cube 工作。
2. Linux CC 检查 vmx、/dev/kvm、实际设备权限及 KVM ioctl 可用性；仅加载模块
   或看到设备不足以判完整虚拟机创建通过。失败保持 PARTIAL。
3. 记录 `/data/cubelet` 的文件系统类型及 v0.7.0 本地 CoW 所需配置；安装器
   要求 XFS。核验 eBPF、bpffs 挂载和网络条件。任何格式化或系统调整交管理员，
   不执行 skip-precheck 绕过。
4. 记录 `.172` 既有业务的监听、健康和资源基线，保留 8089、22589 及 /opt
   所有既有业务目录。只记录允许的健康响应，不收集业务正文或凭据。
5. `.171` 双网关保持原样；在独立 PoC 目录/虚拟环境运行客户端，不更新现网 venv。

全部前置符合才标 READY-FOR-POC；这不是 M0 PASS。

## 3. 安装前生成配置与变更清单

1. 仅获取上述固定版本制品，校验摘要，在独立目录审查 bundle 的 install.sh、
   env.example、服务定义和 support Compose；不直接运行在线一键脚本。
2. 生成所有监听端口、绑定地址、配置引用、容器映射和目录的矩阵。
   CubeMaster 默认 8089 与现网冲突；可提议 18089，但先确认空闲，修改所有
   服务端与客户端引用后再核验。22589 同样保留，无论当前是否监听。
3. 检查配置中 8089 是否仍作为活动端点出现，包括 CUBEMASTER_ADDR、
   CUBE_MASTER_ADDR、节点注册与健康检查；注释与历史文本可保留。
   未找到可配置项时暂停，不用停止现有进程解决冲突。
4. 为 Cube/MinIO 预留独立目录、容器名和数据卷，检查是否与现有资源同名。
   不对已有 Docker/containerd 升级、重启、prune 或执行宽范围 compose down。
5. 冻结资源预算：首轮最多 1 个运行 VM，从 1 vCPU/1GiB 及 120s deadline
   的试验档开始；可否满足所选模板需实测。总服务预算先扣除既有业务峰值
   与节点余量。不能仅凭“节点 15Gi”判容量足够。
6. 生成控制面 X-API-Key 与数据面 TrafficAccessToken 的独立认证配置、TLS
   证书/反代配置，密钥只在本机受限文件保存。授权回调失败必须拒绝。
7. 配置默认拒绝出站；首轮不配置真实业务白名单/凭据注入。管理网络仅授权
   `.171` 的客户端与必要管理地址，防止绕过 TLS 直接访问后端监听。
8. Host Mount 默认关闭。若验证白名单，只允许预先确认的
   `/data/shared/agentseek-m0/` 测试子目录，不开放整个 /data/shared 或 /opt。
9. MinIO 数据用专用持久卷，PoC 内容存储与 Cube Volume 使用不同桶/前缀和
   权限；记录镜像版本/digest、数据目录和停服务不删数据的恢复办法。

先提交变更清单及端口/资源矩阵供维护者确认；确认后才执行审查过的部署步骤。
没有可验证的鉴权/TLS/端口避让配置时不能先开放服务“再补安全”。

## 4. 执行安装和最小 SDK PoC

1. 在 `.172` 应用确认后的配置，逐个启动本轮 Cube 与 MinIO 组件。
   每次启动后复核旧业务端口与健康；出现回归立即停止本轮变更。
2. 在 `.171` 独立 PoC 环境固定 cubesandbox==0.7.0，不接入现网 Agent。
   验证控制面 TLS 与数据代理 TLS，禁止关闭证书校验。
3. 建立带唯一测试前缀的 Volume，显式 driver=s3；记录 ID 的受限映射。
   先用可写测试实例写固定 canary 字节并完成存储确认，停止 writer；再以
   VolumeMount(read_only=True) 创建 reader 验证原生 SDK 挂载。
4. 使用已固定模板，默认拒绝网络，运行确定性脚本；分别执行写文件、读文件、
   返回摘要，证明同一实例的连续命令衔接。实际 SDK 参数按 0.7.0 签名核验，
   不从当前主干示例直接照抄。
5. 下载测试输出，对最终字节重算 SHA-256，保存到独立测试内容桶并回读比对。
   销毁 VM 后仍从外部存储读取输出；重建 VM 从同一只读 Volume 读取 canary。
6. 依次执行下表 Oracle，记录逐项结果、耗时、版本、资源基线及清理结果。
   测试只能使用本轮资源和虚构 canary，不加载真实员工数据或模型 Key。

## 5. 确定性 Oracle

| 编号 | 操作与判定 | 失败动作 |
| --- | --- | --- |
| O0 共存 | 安装前后 .172 8089/22589 归属与业务健康不变，/opt 未改；.171 双网关未重启；资源在预算内 | 停止本轮组件，保留现场 |
| O1 VM | 创建 A/B 各有独立实例；A 的临时文件在 B 不存在；删除后资源释放 | 不判隔离通过 |
| O2 原生 Volume | writer 写固定字节；reader 只读读取摘要一致，创建/覆盖/改名/删除均失败 | SDK 或挂载合同 BLOCKED |
| O3 持久化 | 删除 VM 后 Volume 重挂可读，下载输出外部保存可读；无隐式删除卷 | 保留卷，核查提交语义 |
| O4a 控制面（入口认证） | 缺失/错误 X-API-Key 被拒（13000 执行点）；后端 3000 直连不可达；WebUI/SDK 入口凭据不互通 | 不开放 PoC 业务调用 |
| O4b 跨身份授权（**已批准移至 Broker 阶段，本轮未验证、不计通过**） | A/B 有效身份跨资源负向、资源归属映射 | Broker 阶段验收时执行 |
| O5 数据面 | 对同一受保护端点，缺失/错误 token 被拒，合法 TrafficAccessToken 可执行所需文件操作 | 检查代理直连绕过 |
| O6 网络 | 默认出站关闭，测试已授权的探测目标不可达；元数据/控制面/IPv6等绕过失败 | 隔离网络；不扩大允许列表 |
| O7 配额/超时 | 有界测试触发 deadline；进程树可确认终止，资源回收；单 VM 限额实效可读 | 不以 SDK 超时代替取消 |
| O8 Host Mount | 禁止路径和逃逸路径拒绝；若未开启 host-mount，明确记录禁用及拒绝证据 | 不临时开放父目录 |
| O9 未知结果 | 有界模拟客户端断连，查回原资源或进入待对账；不盲目再建竞争实例 | 收集关联记录 |
| O10 计算故障 | 仅停止本轮 Cube 执行服务，MinIO 保持运行，已提交输出仍可读，恢复不重复创建 | 不关闭整机或旧业务 |
| O11 脱敏/收尾 | 凭据不入日志/报告，旧业务回归正常，测试资源逐个登记处理 | 保留受限记录修复 |

资源耗尽测试必须有硬上限和节点预留；本阶段不在共存节点运行无界 fork/OOM。
全机故障不在本轮执行。Cube 与 MinIO 同在 `.172`，O10 不证明存储高可用。
M0 只验证基础设施和 SDK；Broker fencing、Work 提交与消息幂等在后续切片验收，
不能将 PoC 成功计为那些尚未实现的合同通过。

## 6. 故障处理与回滚

| 现象 | 处理 |
| --- | --- |
| 无 KVM / vmx=0 | 管理员处理 vSphere；不安装 PVM 绕过本轮决策 |
| XFS/bpffs 未满足 | 补充系统变更方案，不格式化现有业务盘、不跳过检查 |
| 8089/22589 冲突 | 改 Cube 端点及全部引用并重做矩阵；原进程保留 |
| 内核/Guest 元数据不匹配 | 停止模板构建，报告差异，不自动使用另一个 release |
| SDK 无所需只读/认证行为 | BLOCKED，保留锁版事实，不改为无鉴权或读写卷 |
| Volume 删除 409 / 节点 refcount 可疑 | 核实真实 attachment，不能强删 |
| 安装影响既有业务 | 停本轮新增服务，按记录恢复本轮配置；通知原业务负责人 |
| Cube 创建结果不明 | 查证关联资源；不重复创建；进入受限对账 |

回滚只停止本轮新增的明确服务/容器，恢复经备份的本轮配置。MinIO 数据卷和
测试证据保留为受限归档，不删除共享目录、不清空 Docker、不重写生产仓库。
测试 VM 可按 ID 清理；Volume/桶删除前确认无引用并获准，报告可恢复性。

## 7. 收官报告

报告 branch/HEAD、节点、版本制品摘要、模板 ID/digest、端口矩阵、资源档位、
O0–O11 逐项结果、脱敏计数及旧业务零影响证据。缺项或未执行不得计 PASS。
保留 PARTIAL 历史；READY-FOR-POC 仅是前置阶段，M0 PASS 后仍由用户确认进入 M1。
验证记录提交功能分支，按既有流程推双端；不动 production/tag，不提交运行数据。

## 8. 固定版本依据

- [Cube v0.7.0 release](https://github.com/TencentCloud/CubeSandbox/releases/tag/v0.7.0)
- [SDK 0.7.0](https://pypi.org/project/cubesandbox/0.7.0/)
- [Kernel artifacts](https://github.com/TencentCloud/CubeSandbox/releases/tag/kernel-release-260812-1)
- [Guest artifacts](https://github.com/TencentCloud/CubeSandbox/releases/tag/guest-image-260820-1)
- [v0.7.0 安装前检查](https://github.com/TencentCloud/CubeSandbox/blob/v0.7.0/deploy/one-click/online-install.sh)
- [v0.7.0 配置入口](https://github.com/TencentCloud/CubeSandbox/blob/v0.7.0/deploy/one-click/env.example)

上述来源用于固定版本核查；当前线上文档中的后续新增能力不能自动视为 0.7.0 已有。

## 9. 安装放行与用户确认（2026-09-07 UTC，安装轮授权记录）

> 勘误（R1）：本节原文误写 2026-09-08；git 提交 730857b 实际时间为
> 2026-09-07 10:40:10 UTC（授权在前，安装执行于同日 11:00–15:45 UTC）。

- Codex 复核通过代码 9543073（九轮修复链闭环）；授权执行 M0 安装与
  O0–O11 验证；**此授权不代表 M0 PASS，不授权进入 M1**。
- 用户四项确认（均已完成）：
  1. 同意 .172 启用 UFW，SSH 22589 管理源收敛 192.10.50.0/24；
  2. 同意既有业务 8089 访问源收敛 192.10.50.0/24；
  3. 同意 O4 拆分：O4a 本轮（单可信 PoC 身份入口认证）；O4b 跨身份
     资源归属授权移至 Broker 阶段**届时必须验收**（见 §5 表，O4b 状态
     =已批准移至、未验证，不得计为通过）；
  4. vSphere 控制台可用性已与管理员确认（失联最后回滚通道）。
- 本节为范围调整记录，不表述为"跨身份授权已通过"。

## 10. M0 安装轮执行记录（2026-09-07，.171/.172 实测）

### 10.1 结论

功能项 O0–O11 已取得通过证据（O7 为「修复后 PASS」）；**O4b 按批准
移至 Broker 阶段，未验证、不计通过**。安装轮暴露的缺陷已在当轮修复
复验（§10.4）；Codex 评审（2026-09-08）裁定「需定点整改与补验」，R1
整改轮已完成边界收敛、restricted 补验、Python 版本门禁与收官材料
闭环（§11）。**整体 M0 PASS 待复核与用户判定，未进入 M1**。

### 10.2 版本 / 制品 / 模板登记

- 执行基线：分支 `enterprise/v0.1.3-cubesandbox` 文档基线 730857b，
  本轮代码修复见 git log（node_supervisor 纳秒时间解析）；
- 四制品 sha256 全 MATCH（一键包/内核 vmlinux/guest 镜像/SDK wheel）；
  vmlinux 已按锁定版替换（3ea4a05e…，原版留 vmlinux.orig）；
  guest 镜像安装器落盘字节一致（8262a7eb…）；
- 8 依赖镜像完整 digest 引用部署，容器限额实测=§9.4 冻结值
  （mysql512/redis128/minio768/coredns64/webui64/proxy512/lcm256/egress256
  + master/api/ops 各 512M + cubelet 1536M，MiB 合计与清单一致）；
- **模板（构建 #11 成功）**：
  - template_id `tpl-c4ba4bf8e2fb4668b0b7f22c`，alias `agentseek-m0-poc`，
    status READY，distribution 1/1；
  - artifact `rfs-b5457edef749be99334af6d9`，
    artifact_sha256 `b9e6915560fbaac9885f912adb8f48ea1ee75d3f8313d34d7216b2590af4a412`，
    spec fingerprint `b5457ede…8f09cfa`；
  - **基础镜像偏离说明**：§2 原口径"基于内核+guest 镜像构建"；实测
    `tpl create -f` 为未文档化路径，采用文档化 `create-from-image`：
    ghcr cubesandbox-base 从 .172 拉取慢（17–96KiB/s）且 HTTP/2 断流
    （PROTOCOL_ERROR），且**未镜像到腾讯 registry**；改用文档示例同款
    `cube-sandbox-int.tencentcloudcr.com/cube-sandbox/sandbox-code@sha256:743d264f…`
    （amd64 platform `467494c3…`，int registry 国内快速拉取），含 envd
    49983 与完整工具链；spec：--cpu 1000 --memory 1024（冻结 1C/1G）、
    writable-layer 1G、--expose-port 49983 --probe 49983 /health、
    未开 allow-internet-access（egress 默认拒绝）、backend xfs（reflink）。

### 10.3 O0–O11 结果表（证据：.171 /root/cube-poc-client/*.json、.172 /root/cube-poc-evidence/）

| Oracle | 结果 | 关键证据 |
| --- | --- | --- |
| O0 共存 | PASS | 8089 业务全程 200、22589 SSH 正常、/opt 时间戳未动、.171 双网关进程+外部反代链 200、限额=冻结值 |
| O1 VM 隔离 | PASS | A/B 创建各 0.21/0.23s（快照恢复），A 写读回全链隧道 OK，B 读 marker=not-found + /tmp 空，销毁即回收，终态列表空 |
| O2 原生 Volume 只读 | PASS | writer 盘上 sha 一致；reader 摘要一致；创建/覆盖/改名/删除 exit=2/2/1/1 全失败；负向后原文件完好 |
| O3 持久化 | PASS | 销毁 writer 后卷仍列出；reader 重挂可读；卷保留未隐式删除；R1 补证：VM 销毁后外部保存输出仍可读（o3_external_output.txt，sha bcff8ac4…） |
| O4a 入口认证 | PASS | 13000 缺失/错误 key 401、WebUI Bearer 不能替代；13001 SDK key 401、/sandbox/ 403；3000/3010/13010/12088 从 .171 不可达；过滤代理归一化绕过 9 例全 403（仅白名单 /health 200） |
| O4b 跨身份授权 | 移至 Broker（未验证、不计通过） | — |
| O5 数据面 | PASS | restricted（allow_public_traffic=False）下 token 签发；合法 token 200、缺失/错误 token 403、/health 无 token 亦 403 |
| O6 网络默认拒绝 | PASS | 沙箱内外网 HTTP/HTTPS/DNS、节点 18089/3000/8089、:80 数据面、过滤代理、169.254 元数据、IPv6 全部拒绝（exit7/28、rc=2） |
| O7 配额/超时 | PASS（修复后） | 客户端崩溃断连 → 监督以 marker 一级授权在 120s 截止+轮询容差内击杀（实测 T+122s，HARD DEADLINE 2026-09-07 15:39:42Z）、告警 pending→cleared、shim 进程 0、TAP 0、终态列表空；缺陷见 10.4-①（修复后 PASS） |
| O8 Host Mount | PASS | 沙箱内 /data/shared 不存在、/proc/mounts 无宿主路径（0 条）；R1 补证：宿主路径串作 volume_mounts 被服务端拒（ApiError 130400）、路径穿越卷名被拒（^[a-zA-Z0-9_-]+$ 校验） |
| O9 未知结果 | PASS | 客户端创建后即崩（结果未知）→ list+marker 对账唯一实例 → 监督按期终止 → 无重复创建 |
| O10 计算故障 | PASS | 停 cubelet 期间 MinIO healthy、卷仍列出、外部已存输出可读；恢复后 reader 重挂卷摘要一致、无重复实例；期间监督无误告警 |
| O11 脱敏/收尾 | PASS | 证据文件 x_api_key 命中 0、token 值落盘 0（SSH 口令 1 处已脱敏）；沙箱终态空、卷/模板按策保留；业务/网关回归全绿 |

### 10.4 执行中暴露并修复的缺陷（真实平台首测）

1. **监督无法锚定 deadline（O7 首测失败→修复→复验）**：平台 startedAt
   为纳秒精度（9 位小数）；Python 3.10 的 fromisoformat 仅支持 3/6 位
   →解析 None→只告警不终止（沙箱滞留 9 分钟，超出 120s 合同）。
   修复：_parse_epoch 归一化小数到 6 位；新增 2 回归测试（合计 **49
   passed**、ruff 全绿）；重部署后监督当场击杀滞留沙箱，后续两例均
   T+120~122s 精确终止。教训：**测试环境 py3.13 与部署环境 py3.10 的
   标准库行为差异**逃过九轮评审——部署目标版本必须进测试矩阵。
2. **模板分发下载撞 8089（构建 #1–#9 反复 sha mismatch）**：cubelet
   `cubemaster_http_addr` 为空（安装渲染遗留）→工件下载回落**编译期默认
   127.0.0.1:8089** →命中业务 mcp market（SPA 兜底任意路径 200 回
   26117 字节首页，got-sha 恒等于首页哈希）。定罪链：`.download` 残留
   字节=业务首页 → strace connect 抓到 `connect(127.0.0.1:8089)` →
   二进制 strings 见默认值+yaml 键。修复：dynamicconf 显式设
   127.0.0.1:18089（.poc-orig+sha256 留档）。
3. **cube-egress 容器静默退出（约 12:06，起因未定）**：9091 admin 拒连
   →清理告警刷屏 + 构建 #10 模板创建失败（创建流要推 egress 策略）。
   `docker start` 拉起后 #11 即成功。9091 已恢复监听。
4. **cubelet 重启依赖网关 ARP**：`gateway mac for ens192 … not found`
   崩溃循环（ARP 过期即复现）。修复：systemd drop-in 91-poc-arp.conf
   ExecStartPre ping 网关（幂等）。
5. **LCM compose 8089 残留**：`CUBE_LCM_CUBEMASTER_URL` 安装期渲染为
   192.10.50.172:8089 未随 18089 收敛（host 网络容器）→改
   127.0.0.1:18089（.orig+sha256 留档）。注意：compose 重建会**冲掉
   docker update 限额**，已重施 256M 并复核全部 13 项。

### 10.5 边界偏离与补偿（如实记录）

- **UFW 按用户指示禁用**（"内网环境无需防火墙"），偏离 v10 §6 三向策略；
  补偿：3000/3010/12088/18089 全收 loopback、13010 仅 172.29.0.1+nginx
  allow/deny（默认桥容器实测 403 拒绝）、13000/13001/13080 TLS+凭据。
- **无防火墙的实际暴露面**（对 192.10.50.0/24 可达，逐项实测）：
  cube-proxy 数据面 :80/:443（**token 强制实测 403**，明文可达但无 token
  不可操作；SDK restricted 模式为必选补偿）、9090/8082、cubelet
  *:9998/9999/9966、LCM *:8083、13000/13001/13080（设计入口）。建议
  后续轮将 cubelet/LCM 端口收敛或恢复防火墙。
- 过滤代理隔离依据=nginx allow/deny 实测（非"无路由"）：默认桥 403、
  专用网内 webui 容器放行、/internal/ 及归一化变体全 403。

### 10.6 运行时补丁清单（全部留 .orig/sha256，回滚按此反向）

| 对象 | 变更 | 留档 |
| --- | --- | --- |
| Cubelet/dynamicconf/conf.yaml:52 | cubemaster_http_addr ""→127.0.0.1:18089 | conf.yaml.poc-orig |
| cube-lifecycle-manager/docker-compose.yaml:19 | CUBE_LCM_CUBEMASTER_URL →127.0.0.1:18089 | docker-compose.yaml.orig（重建后须重施 docker update 256M） |
| /etc/systemd/system/cube-sandbox-cubelet.service.d/91-poc-arp.conf | ExecStartPre ping 网关（新增） | drop-in 自身可删 |
| .172 supervisor | node_supervisor.py 纳秒修复版（8b23fb1b…，PID 417568 常驻） | /root/cube-poc-supervisor/ |
| .171 | 转发器 127.0.0.1:13080 常驻；PoC 脚本+证据 /root/cube-poc-client/ | — |

### 10.7 回滚点（仅本轮新增）

默认动作=停用并保留（模板、卷、证据全保留供复核）；删除类操作
（tpl delete / Volume.destroy / 清数据卷）**均为另需用户授权的可选
步骤**。默认顺序（本文档 §10.3 结果表所列沙箱资源已清空）：停 .171
转发器 → 停 .172 supervisor → 按登记清单停 12 单元+8 容器 → 删 nginx
四站点+conf.d apikey map+DOCKER 网络 cube-webui-net → 撤 iptables 源
收敛规则（cube-poc-evidence/iptables-poc-remove.sh）→ 撤 drop-in
91-poc-arp.conf → 恢复 .poc-orig/.orig 五处 → 数据卷保留归档。
SSH 22589 与 .171 双网关全程不动；8089 业务已按用户指示停止
（2026-09-08 R1，恢复命令见 §11.4）。

### 10.8 待办与上游反馈建议

- O4b（Broker 阶段验收）；模板 alias/身份->资源授权留 Broker；
- 建议 CubeSandbox 上游：①one-click 渲染应把 cubemaster_http_addr 随
  master 地址联动（空值回落 8089 默认在共存环境必炸）；②cubelet 启动
  硬依赖网关 ARP 应内置重试/ExecStartPre；③startedAt 精度与 SDK 文档
  对齐；④cubelet *:9998/9999/9966 与 LCM :8083 默认全地址监听宜可配；
- s3lvol 服务 disabled（安装器默认）：cubecow 的 s3 初始化重试为非致命
  噪声（本轮 xfs/reflink 路径不依赖），已实证；如后续用 s3 backend 需
  启用 cube-sandbox-s3lvol.service。

## 11. R1 整改轮记录（2026-09-08，按 Codex 评审执行）

评审输入：Codex 对 5ba23bf/550c592 的八项裁定（1 ACCEPT / 5 REVISE /
1 BLOCKED / Q8 收尾整改），经用户转发。本节逐项对应。

### 11.1 Q4 边界收敛（BLOCKED → 已整改待复核）

用户 2026-09-08 另行指示：8089 业务（mcp market 副本）可停用或换端口
——**已执行停用**（见 11.4）。不启用 UFW/防火墙服务的用户决定不变，
以**绑定收敛+最小 iptables 源规则（等效宿主规则）**满足放行条件：

| 端口 | 归属 | 收敛方式 | 实测（.171 视角） |
| --- | --- | --- | --- |
| 80/443/9090 | cube-proxy nginx | nginx.conf bind 127.0.0.1（挂载文件直改） | REFUSED |
| 9998/9966 | cubelet http/debug | config.toml bind 127.0.0.1 | REFUSED |
| 8083 | LCM | CUBE_LCM_LISTEN_ADDR=127.0.0.1（消费方 cube-proxy 侧 $cube_sidecar_addr 即 loopback） | REFUSED |
| 9999/8082 | cubelet grpc / proxy admin | 保留节点 IP 绑定（master 经 HostIP 拨 9999；8082 有 advertise 契约）+ iptables 仅放行 127.0.0.1/192.10.50.172 源、其余 REJECT tcp-reset | REFUSED |
| 8089 | 业务（已停） | 用户授权停用 | REFUSED |
| 3000/3010/12088/18089/13010/53 | 原有 loopback/专用网绑定 | 不变 | REFUSED |
| 13000/13001/13080/22589 | 设计入口 | 不变（TLS+凭据/SSH） | REACHABLE（预期） |

- 数据面绕过加密入口已被**物理消除**（:80/:443/:9090 仅 loopback，
  转发器链路 13080→127.0.0.1:80 不变）；整改后全链冒烟（create/kill
  经隧道）PASS；master→cubelet（9999 自源）健康。
- **逐端口用途/认证/允许来源**即上表+§10.3 矩阵；负向=15 端口
  REFUSED 实测（.171）；正项=4 设计入口。
- 防回退：preflight 门禁逐次核验（11.5）；iptables 规则与移除脚本
  留档 cube-poc-evidence/iptables-poc-remove.sh（规则不随重启持久，
  重启后由 preflight 检出阻断——失败关闭）。
- UFW 禁用的授权补记：安装执行中（2026-09-07）用户口头指示"内网
  环境无需启动防火墙"，当时未单独成文，R1 按评审要求补记于此；
  原 §6 计划（启用 UFW）未执行，本节为等效技术措施的合同同步。

### 11.2 Q2 时间解析与 Python 版本门禁

- `_parse_epoch` 重写为显式正则：小数 1–9 位（右补零/截断至微秒）、
  时区**必填**（Z/z 或 ±HH:MM/±HHMM；朴素时间一律拒绝，绝不按宿主
  本地时区解释）；数值原样；越界/非法 → None；
- 测试矩阵：`test_parse_epoch_format_matrix` 覆盖 0/1/2/3/4/5/6/9 位、
  Z/显式时区/紧凑偏移、数值、nan/inf、朴素、非法、越界、10 位、None、
  非字符串；
- **双解释器矩阵（部署版本实测）**：py3.10.12（=.172 部署版，
  uv 独立 venv）与 py3.13.14（=.171 venv）均 **54 passed**、ruff
  --no-fix 全绿；`test_sdk_adapter/tunnel_forwarder` 的 `datetime.UTC`
  常量已改 `timezone.utc`（3.11+ 别名不可用于 3.10），并在 pyproject
  为三个沙箱测试文件加 UP017 豁免（附注释：部署矩阵含 3.10）；
- 部署核验：.172 supervisor 已运行新版（sha256 b5d66db5…），心跳/门禁
  正常。教训固化：**部署目标解释器版本进测试矩阵**为分支约定。

### 11.3 Q5/Q6/Q3 补验

- **Q5**：o1/o2 脚本固化 `allow_public_traffic=False`，在 §11.1 终态
  边界下重跑：O1 PASS（A/B restricted、隔离/回收全过）、O2/O3 PASS
  （只读负向 exit=2/2/1/1、卷跨 VM 存活、外部摘要一致）；
- **Q6 egress**：取证=退出码 0（优雅终止信号）、OOMKilled=false、
  **RestartPolicy=no（不自愈根因，已改 unless-stopped）**、退出触发源
  无法定证（docker events 缓冲已轮转，如实记录）；日志驱动 json-file
  未设上限（保留中）。检测与阻断=①preflight egress_alive（实测停机
  即 FAIL/exit 3）；②**有界故障注入（7.4s 停机）实测失败关闭**：存活
  restricted 沙箱出站仍 DENIED（000/rc=7）、恢复后复验 O6 全 DENIED；
  附带发现：egress 宕机期间新建沙箱请求仍可成功（创建链不校验 egress
  存活）——已由 preflight 前置阻断（PoC 流程约束），列为上游反馈项；
- **Q3**：O7 保持「修复后 PASS」表述与完整时间线（含 9 分钟滞留窗口），
  T+122s 统一表述为 120s 截止+轮询容差；
- 观察窗口：egress 自 2026-09-08 00:3x UTC 恢复并启用自愈策略后，
  preflight 每次创建前核验（持续观察中）。

### 11.4 Q1 基线变更登记：模板底座镜像（2026-09-07 生效）

- **变更**：模板底座由 ghcr `cubesandbox-base` 改为
  `cube-sandbox-int.tencentcloudcr.com/cube-sandbox/sandbox-code`
  @`sha256:743d264fad8c9dc9a49f07e931166d24d025363360ae770ca4b70e3f19540944`
  （manifest-list digest；amd64 platform manifest
  `sha256:467494c38f3c335e42d590e23d5cbb00dc15c2627c9da4f7e7f3bd9f4ec18c5e`，
  linux/amd64；tag latest 当时指向）。
- **动因与时间**：ghcr 从 .172 拉取 17–96KiB/s 且 HTTP/2 PROTOCOL_ERROR
  断流、base 未镜像腾讯源（404 实测）；2026-09-07 14:32 UTC 起（构建
  #4）改用文档示例同款镜像。**属执行期基线变更，非原计划**；经用户
  转发 Codex 评审（2026-09-08）认可方向，本节为补登记录。
- **对象与哈希区分**：guest 压缩制品
  `cube-guest-image-amd64.tar.gz`=`945d2d79…`（下载校验对象）；
  解包后 `cube-guest-image-cpu.img`=`8262a7eb…`（安装器落盘内容）；
  二者**均未参与本模板构建**（create-from-image 以 OCI 镜像为源），
  guest 制品保留在位供 ad-hoc/默认路径。锁定表（变更清单 §2）不回溯
  改写，以本节为准确认。
- **镜像事实**：sandbox-code 为官方文档 template-from-image.md 示例
  镜像；含 envd（0.5.11，49983）与完整 Python/CLI 工具链（O6 探针
  依赖 curl/getent 实测可用）；启动入口由镜像 ENTRYPOINT+平台 probe
  （49983 /health）控制；**漏洞扫描：验证环境无镜像扫描工具，未执行
  ——如实记为限制项**；补偿=digest 锁定+restricted token+默认拒绝出站
  +PoC 全程虚构数据；完整工具链≠最小底座，能力面扩大已知悉。
- 建议后续轮：引入离线扫描（trivy/grype）后补扫并登记。

### 11.5 Q7 运行时补丁规程与新增门禁

**脱敏 diff 汇总**（全部有 .poc-orig/.orig+sha256 留档于 .172）：

1. `Cubelet/dynamicconf/conf.yaml`：`cubemaster_http_addr: "" →
   "127.0.0.1:18089"`（一行；键值经 preflight 校验）；
2. `cube-lifecycle-manager/docker-compose.yaml`：
   `CUBE_LCM_CUBEMASTER_URL: http://192.10.50.172:8089 →
   http://127.0.0.1:18089`；`CUBE_LCM_LISTEN_ADDR: 0.0.0.0:8083 →
   127.0.0.1:8083`；**新增 `mem_limit: 256m`（限额进 compose 本体，
   重建不再单靠 docker update）**；
3. `cubeproxy/nginx.conf`：`listen 80/443/9090 → listen 127.0.0.1:…`
   （reuseport/ssl/http2 参数原样保留）；
4. `Cubelet/config/config.toml`：`[http]/[debug] address ":9998/:9966"
   → "127.0.0.1:9998/9966"`；
5. systemd drop-in `91-poc-arp.conf`：`ExecStartPre=/bin/sh -c
   "ping -c 1 -W 2 192.10.50.254 >/dev/null 2>&1 || true"`——目标=
   网关 ARP（192.10.50.254），1 次、2s 超时、失败不阻断（cubelet 自身
   会显式失败重试）；
6. `docker update --restart unless-stopped cube-egress`（RestartPolicy）。

**容器重建规程**（对任何 compose 管理组件）：渲染/重建 → 核对 .orig
基线（防上游改写）→ 重放地址+限额补丁 → `docker compose config` 校验
→ `up -d` → docker inspect 实读 Memory/监听核验 →（PoC 流程）preflight
通过后才恢复创建。

**新增门禁（代码+测试入仓，sandbox_poc/preflight.py，sha 9e486ab8…，
54 测试含 5 项 preflight）**：新建沙箱前在 .172 执行（与 gate-check
并列）：master 活动引用不回落 8089（dynamicconf+LCM compose，数字
边界正则避免「18089 含 8089」误报）/ 8 容器限额=冻结值（重建后未核验
不得创建）/ 端口收敛+iptables 在位 / egress 9091 可达 / 心跳门禁；
exit 0/3 失败关闭。**.172 已部署并实测**：正常=5 项全 PASS、egress 停机
=FAIL exit 3。

### 11.6 O11 敏感事件处置状态（历史如实保留）

- 事件 1：SSH 口令一次内联于编排脚本（2026-09-07）——当日脱敏+chmod
  600；**脱敏不等于凭据失效**，轮换与否留管理员评估（处置状态=待评估）；
- 事件 2：DB 取证时 t_system_setting 两密值（jwt/secret master key）
  曾出现于操作终端（未入任何仓/报告/证据文件）；建议评估轮换
  （处置状态=待评估）；
- 整改后扫描：x_api_key 明文 0、token 值落盘 0、SSH 口令 0（复查）；
- 8089 业务停止（用户授权）：恢复命令留档 cube-poc-evidence/
  8089-restore.txt。

### 11.7 O4b Broker 阶段验收清单（登记）

前置：Broker 阶段建立双身份（A/B 各自 X-API-Key/凭证与资源归属映射）。
必须逐项负向验证：A 创建/连接/执行/文件读写/卷挂载/删除 B 的资源均被
拒（创建=403/404；连接=token 不通；执行/文件/卷=not-found/forbidden；
删除=所有权校验拒）；资源列表互不可见；归属映射可审计。**验收前不得
开放多身份共享控制面**。

### 11.8 R1 终态与遗留

- 终态：模板 tpl-c4ba4bf8… READY；卷 agentseek-m0-o2 保留；沙箱 0；
  supervisor b5d66db5 常驻；preflight 部署；egress 自愈策略在位；
  .171 转发器常驻；8089 停用。
- 遗留（R2 更新）：egress 退出触发源未定证（日志/事件已轮转，后续
  跟踪项：负责人=平台运维，观察=preflight 每次创建前核验）；cubelet
  创建链不校验 egress 存活（**限单可信 PoC 流程，不开放直接业务调用**，
  上游反馈项）；~~iptables 规则不随重启持久~~ → **已闭环**（R2
  portguard systemd 单元先于监听恢复，见 §11.9）；镜像漏洞扫描未执行
  → 表述修正为**「底座镜像风险尚未评估」**（非已安全；若按 PoC 豁免
  需用户明确接受范围，真实数据/业务接入前必须补扫）。
- 最小脱敏证据包（供 Codex 复核索取）：端口矩阵+iptables+preflight
  输出；O7 两例时间线+HARD DEADLINE 行+回收计数；镜像完整引用+补丁
  diff+限额核验；双解释器版本×结果；O3/O8 补证；egress 取证+失败
  关闭实测；敏感事件处置状态。位置：.171 /root/cube-poc-client/、
  .172 /root/cube-poc-{supervisor,evidence}/。

## 12. R2 整改轮记录（2026-09-08，按 Codex R2 复核执行）

### 12.1 preflight 误放行缺陷（Codex 本地复现）——已修复

- R1 版只查"文本存在"（ACCEPT/REJECT 字样），前置全放行+规则在后仍
  PASS——复现确认；
- R2 重写：**专用链 CUBE_POC_GUARD**（INPUT 首条=跳转，先于 ufw 链）；
  链内规则**逐条按序全等校验**（target/source/prot/端口 token 精确比
  较，`dpt:80820` 不误认）；**命令 rc≠0 一律拒绝**；master 引用改
  **活动值级解析**（跳注释、多处活动键=歧义拒绝——注释正确地址不再
  掩盖空/错活动值）；
- 负向测试新增：前置全放行绕过、无跳转、错误来源、端口前缀相似、
  规则缺失、rc≠0、注释掩盖、歧义多活动键——合计 **59 passed**
  （py3.10.12 与 py3.13.14 双解释器）、ruff 全绿。

### 12.2 非持久 iptables → 启动边界闭环（不再是"仅创建前检查"）

- `cube-poc-portguard.service`（oneshot+RemainAfterExit，enabled，
  幂等重建专用链）；四个监听单元（cubelet/cube-proxy/LCM/egress）
  drop-in `Requires=+After=portguard` → **规则未就绪则监听不启动**；
- 重建路径实测：停守卫+拆链→重启 cube-proxy 单元→守卫被 Requires
  拉起、链先恢复（INPUT 首条=跳转），监听后起；.171 复测 REFUSED；
- **整机重启验证待用户授权**（boot 拓扑：control.target enabled、
  compute.target disabled=安装器默认——重启后计算栈不自动起，守卫与
  supervisor 服务 enabled 自动恢复）；
- supervisor 已 systemd 服务化（cube-poc-supervisor.service，
  Restart=always）。

### 12.3 渲染回归事故（检测器实效的意外实证）

重启 cube-proxy 单元触发上游重渲染：proxy nginx.conf 与 LCM compose
回退默认（loopback 丢失、URL 回 8089、限额归零）——**R2 preflight 当场
三项 FAIL 抓获**（检测器活体验证）。根因=R1 补丁只改了渲染产物。修复
=**模板/env 级持久化**：
1. `cubeproxy/nginx.conf.template` 三处 listen 加 127.0.0.1: 前缀；
2. `.one-click.env` 显式 CUBE_LCM_CUBEMASTER_URL/CUBE_LCM_LISTEN_ADDR
   （up 脚本 env 覆盖默认）；
3. 六个 compose 模板补 `mem_limit`（proxy512/coredns64/webui64/
   mysql512/redis128/minio768）——**docker update 不再是任何容器的
   唯一限额来源**；
重渲染后复验：监听全 loopback、LCM 值正确、限额=冻结值、preflight
5/5 PASS、全链冒烟 PASS、.171 矩阵 15 REFUSED+4 入口。

### 12.4 最小脱敏证据包

入仓：`V0.1.3_M0_R1_EVIDENCE_PACK.md`（Q4 终态矩阵/iptables 顺序/
IPv6/启动边界、Q6 时间线与失败关闭、Q2/Q3 双解释器与 O7、O3/O8 补证）。

### 12.5 用户决定（2026-09-08）

1. **整机重启验证：已授权并执行**（见 §12.6，通过）；
2. **底座镜像漏洞扫描：用户决定全部豁免**（理由：内网环境；范围=本
   PoC 全程）。登记口径：豁免≠已评估——底座镜像风险**未评估**状态
   不变，真实数据/业务接入前补扫的建议保留在案（§11.4）。

### 12.6 整机重启验证（2026-09-08 01:11 UTC，用户授权）

重启前新增两处启动边界补丁：docker.service drop-in
`After=cube-poc-portguard`（7/8 容器 RestartPolicy=unless-stopped，防
容器先于守卫监听）；nginx.service drop-in `After=docker`+`Restart=
on-failure`（13010 绑定 172.29.0.1 依赖 docker 网桥，boot 实测
bind 失败一次）。

实测结果（boot 2026-09-08T01:11:42Z）：

- **启动时序（ActiveEnterTimestamp/StartedAt）**：portguard
  01:12:04 → docker 01:12:07 → cube-proxy 容器 01:12:15——**守卫先于
  docker 与全部自启容器，暴露窗口消除**；
- 守卫链 boot 自动恢复：INPUT 首条=CUBE_POC_GUARD 跳转、链内 6 条
  期望序列；
- 8 容器全部自启（unless-stopped）；control.target/compute 栈按
  安装器默认（compute 需手动拉起，已拉起）；
- **重启暴露的新漂移**（preflight 抓获）：cube-egress/coredns 限额
  归零（egress 为 docker start 型容器、coredns 历史渲染路径）——已
  部署 `cube-poc-limits.service`（enabled，boot 后幂等对齐 8 容器
  冻结值；模板型容器已由 compose mem_limit 持久）；nginx 入口经
  drop-in 自愈恢复；
- 终态：preflight 5/5 PASS exit=0；.171 矩阵 15 REFUSED+4 入口；
  全链冒烟（create/commands/kill）PASS；supervisor systemd 服务
  自恢复（心跳新鲜）。

### 12.7 R2 终态清单（.172 新增 PoC 单元/文件）

`/usr/local/sbin/cube-poc-{portguard,limits}.sh`；systemd 单元
cube-poc-{portguard,limits,supervisor}.service（均 enabled）；
drop-ins：docker 93-poc-portguard、nginx 94-poc-order、四监听单元
93-poc-portguard、cubelet 91-poc-arp；模板/env 持久补丁（nginx
template 地址位、.one-click.env LCM 覆盖、六 compose 模板
mem_limit）。全部纳入 §10.7 回滚清单语义（默认停用保留）。

## 13. R3 整改轮记录（2026-09-08，按 Codex R3 复核执行）

### 13.1 preflight 条件跳转误放行（Codex 复现）——已修复

R2 版只比对跳转目标名。R3 改为 **`iptables -S` 完整形态全等**：
- INPUT 第 1 条必须**无条件**跳转（token 全等
  `-A INPUT -j CUBE_POC_GUARD`）——带 -s/-i 等条件跳转=外部流量绕开
  守卫（R3 复现场景），一律拒绝；
- 链内每条规则 **token 级全等**：REJECT 带 -s 限缩、ACCEPT 带 -d 限缩、
  多任何匹配项/动作修饰=结构漂移拒绝；
- 负向新增：条件跳转（-s / -i 两型）、REJECT 限缩、ACCEPT 限缩。
  合计 **63 passed（py3.10.12+py3.13.14 双解释器）**、ruff 全绿。

### 13.2 portguard 无暴露窗口换链——已重写并实测

- apply 重写：`CUBE_POC_GUARD.new` 先建满 6 条规则 → 插入 INPUT 首位
  （新旧双保护并存）→ 才移除旧跳转 → 清旧链 → 重命名规范名；
  EXIT trap 兜底清理临时链。任一步失败：旧保护或新保护至少一个在位，
  set -e 非零退出（preflight 按形态失败关闭）；
- 定向测试（stub iptables 记录调用序列）：重应用顺序（新跳转插入先于
  旧跳转删除、旧链清理更后）、中途失败（建链/加规则/插跳转三位点）
  绝不出现删旧跳转调用、双重 apply 幂等；
- **监听在位场景实测**：9999/8082 正被监听时 apply，结构全程在位
  （rc=0，`iptables -S` 全等，.171 探测持续 REFUSED）。

### 13.3 docker 失败启动边界——Requires 补齐并实证

- docker.service drop-in 升级为 **`Requires=` + `After=`**（脱敏原文
  入 .172 /etc/systemd/system/docker.service.d/93-poc-portguard.conf，
  两行：Requires/After cube-poc-portguard.service）；
- `systemctl show docker -p Requires -p After` 双证含 portguard；
- **失败路径实测**：移走守卫单元文件+daemon-reload 后
  `systemctl restart docker` →
  `Failed to restart docker.service: Unit cube-poc-portguard.service
  not found.`——**依赖缺失阻止新的启动事务**（未运行场景=起不来）。
  措辞收窄（R4，Codex 裁定）：该报错**不能**推导既有监听必然消失——
  运行中的 docker 及其容器在依赖失效后是否继续监听，以实测端口状态为准
  （本 PoC 的持续保障来自 preflight 门禁 + 端口探测，不依赖该推导）；
  恢复单元文件后 docker 正常重启；
- 附带根治：egress/coredns 单元加 `ExecStartPost=cube-poc-limits.sh`
  （单元级容器重建路径的限额丢失——docker restart 周期中再次实测
  出现，现两条路径（boot 后 limits.service + 单元启动后 ExecStartPost）
  均收敛）。

### 13.4 R3 终态

8 容器全在位、preflight 5/5 PASS（含 `-S` 全等）、.171 矩阵
REFUSED（9999/8082/80/8089 抽测）；遗留项不变（egress 触发源未定证、
创建链不校验 egress=上游反馈）。镜像扫描豁免维持 §12.5 口径
（豁免≠已评估）；O4b 保留 Broker 前置。

## 14. R4 整改轮记录（2026-09-08，按 Codex R3 整体评审执行）

Codex R3 整体裁定：项①③配置闭环（③证据表述收窄）、**项②未闭环必须
重构**、IPv6 口径需更正并补证。本轮全部闭环。

### 14.1 IPv6 绕过实证与等效 v6 保护（Codex 项①收口）

- **事实**（R4 实测取证）：9999=cubelet 监听 `[::]:9999` **双栈通配**
  （`/proc/net/tcp6` 全零地址 + `bindv6only=0`）；8082=nginx 仅 v4 绑定
  （`192.10.50.172:8082`）；节点无全局 v6（仅 link-local，ens192=
  `fe80::250:56ff:febe:143d/64`）；ip6tables INPUT 原无任何规则；
- **修复前绕过实证**（.171 → .172 同链路）：
  `curl -6 'http://[fe80::250:56ff:febe:143d%ens192]:9999/'` →
  **TCP 连接成功建立**（`Connected to fe80::... port 9999`，cubelet
  返回非 HTTP 数据致 curl 报 HTTP/0.9）——v4 守卫链对 v6 流量完全不
  生效，Codex 点名的"无全局地址≠无 IPv6 绕过"成立；
- **修复**：portguard 升级双栈——v6 侧 `ip6tables CUBE_POC_GUARD`
  （9999/8082 各两条：`-s ::1/128 ACCEPT` + 无条件
  `REJECT --reject-with tcp-reset`；节点无全局 v6、平台自连走
  v4/v4-mapped，故无节点自源 ACCEPT）；preflight 新增 **firewall_v6**
  门禁（`ip6tables -S` 完整形态 token 全等，负向 3 类：条件跳转
  `-s ::1`、REJECT 带 `-s fe80::/10` 限缩、rc≠0/链缺失）；
- **修复后**：同一 v6 探测 `Connection refused`（0ms，tcp-reset）；
  .172 自身 v4 自连（192.10.50.172:9999 与 127.0.0.1:9999）不受影响
  （rc=0，平台现网连接全为 v4-mapped，`ss -tn6` 无真实 v6 流量）；
  O1 冒烟 PASS（创建 0.21s/读写回/隔离/回收，v6 守卫不破坏平台功能）。

### 14.2 portguard 阶段化重构（Codex 项②，按五点意见）

安全合同重写（v4+v6 双栈同构）：

1. **有效保护**定义 = 跳转在 INPUT **且**链内规则与期望逐行全等
   （"有跳转"≠"有效保护"——回应 Codex 对 `-C` 守卫不足的裁定）；
2. NEW 接管前失败（建链/加规则/插跳转）：仅清理**未挂跳转**的 NEW
   残留，OLD 原样保留；
3. NEW 接管后失败（重命名等晚期失败）：**保留完整 NEW 与其跳转**，
   非零退出，不清理——残留以临时链名提供等效保护，preflight 按规范
   形态失败关闭并暴露；
4. 再次 apply 先识别残留：NEW 有效 → 收敛为规范名（while 循环清尽
   旧跳转，含异常重复；外部链引用致 -X/-E 失败时 NEW 原样在位）；
   NEW 无效且未挂跳转 → 清除重建；**NEW 挂跳转但无效（外部污染态）
   → -N 失败退出，绝不自动拆除可能仍在提供部分拒绝的规则**；
5. apply/remove 以 **flock 互斥**（`/run/cube-poc-portguard.lock`，
   默认等 30s，超时 rc=75）；显式 remove（有意拆除，不经 trap）与
   失败清理严格分离。

**状态化测试**（`tests/test_portguard.py` + 语义忠实模拟器
`tests/iptables_stub.py`：-X 被引用拒绝、-E 目标存在拒绝、-E 同步
更新引用、-D 无匹配失败、-s 规范化 /32|/128、-p tcp+--dport 自动补
-m tcp——与真实 iptables -S 输出一致）：每操作后快照全状态，逐步断言
**"至少一个有效拒绝路径始终在位"**（不止检查是否出现某条命令）。
11 场景：正常双栈/空态构建/接管前失败×3（OLD 原样）/晚期重命名失败
（NEW 保留）/失败后再 apply 收敛/**外部链引用自然晚期失败+解除恢复**/
重复旧跳转收敛/v6 晚期失败（v4 已规范）/污染态拒绝并保留/并发互斥
（快照不交错）/锁竞争 rc=75（**该测试抓获真缺陷：锁超时路径原仍触发
EXIT trap 对持锁实例执行 cleanup_new——已修为锁先行、trap 后设**）/
显式 remove 双栈拆除。

### 14.3 实机验证（.172，2026-09-08 02:04–02:27 UTC）

- **部署**：portguard.sh/preflight.py sha256 与仓内一致（旧版留
  `.poc-orig-r3` 备份）；apply rc=0，双栈结构 `iptables -S` /
  `ip6tables -S` 与规范形态逐 token 一致；监听在位（9999/8082 正被
  监听）重应用 rc=0 幂等；
- **晚期失败注入（不制造裸露）**：`iptables -N CUBE_POC_DUMMY;
  -A CUBE_POC_DUMMY -j CUBE_POC_GUARD`（外部引用）→ apply **rc=1**
  （-X 被引用拒 → -E 目标存在拒）→ INPUT 首条=
  `-A INPUT -j CUBE_POC_GUARD.new`、NEW 6 条规则完整（临时名保护）、
  OLD 空链被引用；**失败状态下 .171 三探测全拒**（v4 9999/8082
  rc=refused + v6 9999 refused）；清除 DUMMY 后再 apply rc=0 →
  收敛为规范名（.new 消失）；preflight 6/6 PASS；
- **Requires × ExecStart 失败隔离验证**（Codex 项③补充，不碰真实
  docker）：临时单元 cube-poc-t-dep（oneshot /bin/false）+
  cube-poc-t-main（Requires+After dep）→ `systemctl start main` =
  **"A dependency job for cube-poc-t-main.service failed"** rc=1
  （main=inactive、dep=failed）——区别于"单元文件不存在"的第二种
  失败形态；对照：dep 改 /bin/true 后 start rc=0；两单元已删除；
- **终态**：preflight 6/6（master_refs/container_limits/
  port_convergence/**firewall_v6**/egress_alive/gate）exit 0；
  8 容器健康；O1 冒烟 PASS。

### 14.4 测试与遗留

- 测试矩阵：4 沙箱测试文件 **77 passed（py3.10.12 部署机 + py3.13.14
  双解释器）**、ruff 全绿；portguard 状态化 11 场景含并发/污染/失败
  恢复路径；
- 遗留不变：egress 退出触发源未定证（owner=平台运维，观察=preflight）；
  创建链不校验 egress（上游反馈）；镜像扫描豁免（豁免≠已评估）；
  O4b Broker 前置（§11.7 清单）；
- systemd drop-in 与 boot 时序事实不变（§12.6）；R4 未改变
  docker/nginx/limits 单元拓扑，仅 portguard 脚本与 preflight 检查项
  升级。

## 15. M0 收官记录（2026-09-08，判定与遗留）

### 15.1 判定

- **M0 PASS，用户于 2026-09-08 确认**；
- Codex R4 评审（基于 `3d25447`）建议：M0 PASS，限定于
  **"单可信 PoC、既定风险豁免"**范围——单可信 PoC 身份入口认证
  （O4a）通过、跨身份授权（O4b）未验证不计通过；镜像漏洞扫描按
  用户 2026-09-08 决定全豁免（内网 PoC 范围，§12.5：豁免≠已评估）；
- 评审与整改链（全部保留于 §10–§14）：安装轮 550c592（O0–O11，
  O7 修复后 PASS，四个真实平台缺陷修复）→ Codex R1→538235a →
  R2→af479a0（+整机重启验证 a823341）→ R3→09ef4ff → R4→3d25447；
  评审基线 = `3d25447`；
- 验证终态快照（2026-09-08T02:27 UTC）：preflight 6/6 PASS exit 0
  （master_refs / container_limits / port_convergence / firewall_v6 /
  egress_alive / gate）；8 容器健康；.171 对 .172 双栈管理面探测
  全拒（v4 9999/8082 + v6 9999 link-local）；O1 冒烟 PASS；
  测试矩阵 77 passed（py3.10.12 部署机 + py3.13.14）、ruff 全绿；
- 复刻手册：本记录为叙事体裁；在新环境复刻部署用
  [V0.1.3_CUBE_DEPLOYMENT_RUNBOOK.md](V0.1.3_CUBE_DEPLOYMENT_RUNBOOK.md)
  （操作体裁：参数表/前置/端口矩阵/与开源默认的 9 项差异/六大坑检查
  清单/PoC→生产差异/升级与回滚）。

### 15.2 历史记录处置

§10–§14 的全部失败、BLOCKED、缺陷与整改过程**原文保留**（模板 11 次
构建失败链、_parse_epoch 纳秒缺陷、R2 渲染回归、R3 条件跳转误放行、
R4 IPv6 绕过与锁超时 trap 缺陷等），不重写不美化；早期 PARTIAL/
READY-FOR-POC 判定为当时事实，仅顶部结论区指向本节。

### 15.3 遗留项与责任归属（不因 PASS 消失）

| 遗留项 | 状态 | 责任/归属 | 后续动作 |
| --- | --- | --- | --- |
| O4b 跨身份资源归属授权 | 未验证、不计通过 | Broker 阶段验收（清单 §11.7：A/B 身份跨资源负向 + 归属映射 + 不共享多身份控制面） | Broker 切片准入前置 |
| 底座镜像漏洞扫描 | 用户豁免（内网 PoC）；**豁免≠已评估，风险尚未评估** | 用户决策记录 §12.5 | 接入真实业务/数据前补扫建议保留在案 |
| cube-egress 静默退出根因 | 未定证（events 已轮转） | 平台运维跟踪 | 观察面=preflight egress_alive 门禁（9091）+ RestartPolicy=unless-stopped 自愈 |
| 创建链不校验 egress 存活 | 平台行为（egress 宕机时新建沙箱仍成功） | 上游反馈项 | preflight 创建门禁兜底（本轮已落地） |
| 上游反馈清单 | 待提交 | 维护者汇总 | cubemaster_http_addr 渲染默认值、cubelet ARP 网关依赖、startedAt 纳秒精度、cubelet/LCM 全接口绑定（9999 [::] 双栈为本轮实证）、创建链 egress 校验 |
| 存储单故障域 | 设计内（PoC） | — | 生产化前需独立存储；O10 不证明存储高可用 |

### 15.4 边界重申

本 PASS 不授权进入 M1（另由用户确认）；不扩大到真实业务数据、多身份
共享控制面或生产部署；production 分支与 tag 未动；运行数据/凭据不入库
（证据为脱敏摘要 + .172 受限现场原件）。
