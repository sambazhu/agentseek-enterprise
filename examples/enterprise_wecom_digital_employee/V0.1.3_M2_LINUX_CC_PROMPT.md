---
title: M2 Broker 部署候选 Linux CC 交接工单
type: how-to
audience: [A4]
runs: no
verified_on: 2026-09-09
sources:
  - examples/enterprise_wecom_digital_employee/V0.1.3_M2_BROKER_RUNTIME.md
  - examples/enterprise_wecom_digital_employee/V0.1.3_M2_BROKER_VERIFICATION.md
---

# M2 Broker 部署候选 Linux CC 交接工单

将以下正文连同 Codex 本轮交接的完整 commit SHA 转交 Linux CC。
未指定完整 SHA 时，先核验双端引用并请使用方确认锚点，不能在部署中途追踪移动 HEAD。

```text
致 Linux CC：请接手 v0.1.3 M2 Broker 安装评审与真实沙箱复验。

分支：enterprise/v0.1.3-execution-broker
基线：M1 收官 4a7e76f；候选 HEAD 使用本次 Codex 交接给出的完整 SHA。
GitLab/GitHub 同名分支已发布；请先核验 ancestry、双端 ls-remote 和工作区。

请完整阅读：
examples/enterprise_wecom_digital_employee/V0.1.3_M2_BROKER_RUNTIME.md
examples/enterprise_wecom_digital_employee/V0.1.3_M2_BROKER_VERIFICATION.md
安装样例：examples/enterprise_wecom_digital_employee/m2_broker/

1. 先在 .171 独立 worktree 做离线门禁并提交本轮安装变更清单。
   Codex 本地：Python3.10/3.13 各157 passed，Files/Work153 passed；
   Ruff --no-fix、format、ty(py310)、严格MkDocs、diff --check通过。
   M2服务的现场安装变更经用户确认后再执行，不直接沿用M0授权。

2. .171 新增独立venv/私有配置/UDS Broker与受限SSH gate；
   .172 仅新增M2 run的监督原代码副本与独立manifest/heartbeat。
   原M0代码、监督、平台、模板、转发器和网关均不改。
   安装前确认模板digest/限额/CA/domain与M0一致，不能使用latest或占位值。

3. 复验B0–B9：合成DirectTurn/Work→真实VM、固定命令与文件、
   A/B双向授权、幂等/容量、SIGTERM/SIGKILL、节点120s监督、
   丢响应/失联对账、TLS/restricted/门禁负向、安全扫描与原服务零变化。
   具体PASS/FAIL、注入范围和回滚以手册为准，不临时降低Oracle。

4. 注意：本轮不接真实Agent/企微/Work库，不加载.env，不迁移共享PG。
   DirectTurn不生成WorkItem；Work为服务端合成引用。
   O4b只记Broker开放能力的API授权，不等于Cube原生多租户；
   Volume关闭不算挂载授权已通过；M0扫描豁免不等于安全已评估。
   B6须仅对M2停用自动重启后kill整个服务组，不能用优雅清理冒充独立监督。
   未知结果保留writer/容量；不得清库、扩大认领范围或盲目再次create。

5. 任一失败即停止新建并回传BLOCKED；不动production/tag，不force push，
   不提交.env、凭据、运行库、日志或原始平台句柄。
   请回传代码HEAD、验证commit、双解释器门禁、B0–B9逐项证据、
   监督/资源收敛时间线、原服务零变化与遗留项；记录追加手册§7并双端推送。

当前为“可交安装评审与实机复验的候选”，不是M2 PASS；最终由用户验收。
```
