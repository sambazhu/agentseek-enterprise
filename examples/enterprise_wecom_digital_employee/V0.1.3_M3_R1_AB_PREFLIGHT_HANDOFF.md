---
title: M3 R1 A/B 窗口前置核对与 B8 勘误交接
type: how-to
audience: [A4]
runs: no
verified_on: 2026-09-16
sources:
  - examples/enterprise_wecom_digital_employee/V0.1.3_M3_R1_OFFLINE_LINUX_RESULTS.md
  - examples/enterprise_wecom_digital_employee/V0.1.3_M2_BROKER_VERIFICATION.md
  - contrib/agentseek-execution/src/agentseek_execution/m3_create_process.py
  - contrib/agentseek-execution/src/agentseek_execution/m3_approved_case.py
  - contrib/agentseek-execution/src/agentseek_execution/m3_case_process.py
  - examples/enterprise_wecom_digital_employee/sandbox_poc/node_supervisor.py
---

# M3 R1 A/B 窗口前置核对与 B8 勘误交接

## 1. 保持范围

沿用代码候选 `07e098e67678ca60f8434cc276fe9c731464b6bd`。
离线验收报告提交 `5a78ef8d186552848cae5c677d7b849aa1c915dc`，判定为
`R1_OFFLINE_INSTALL_PASS`，不再重复已通过的离线测试。

本轮唯一允许的持久变更是执行分支文档提交及普通推送。
现场只读；不创建/终止 guest，不部署或启动 supervisor，不登记新 run/目标，
不写批准文件、密钥、栅栏或额度，不发 envd 探针，不改网关、production、标签、DB、MCP、WORK。
已有监督服务可能自主执行其原授权行为，不将其行为算成本工单操作。

旧 `V0.1.3_M3_R1_LINUX_CC_PROMPT.md` 是历史草案，不是本轮执行入口。
本工单也不授权其 shell、安装或创建步骤。

## 2. 补入 B8 勘误

1. 获取最新 `enterprise/v0.1.3-execution-broker`，核验双远端一致，在独立干净工作树处理。
2. 从原工作树读取本地 `4d0caa9` 的完整 SHA、父提交与 diff；不得修改原工作树。
3. 再确认仅改 `V0.1.3_M2_BROKER_VERIFICATION.md`、仅新增已报告的 7 行，父提交为 `d73ef00`。
4. 在最新执行分支上以保留来源的 cherry-pick（`-x`）补入；若冲突、内容变化或已被覆盖，停止并回报，不重置或强推。
5. 核验新增勘误明确：两次原标“错误 token”的探针使用未被识别的 `X-Access-Token`，
   403 只能支持“缺失所识别凭据”；不能支持识别头中错误值的拒绝。
   原始证据和 M2 结论保留，未测项仍为未测。
6. 执行 diff 空白检查；仅提交/推送该文档变更。保留旧提交及工作树，不清理。

Mac 端没有该未推送本地对象，本工单不声称已经补入；Linux CC 完成后回传新 SHA 与出处。

## 3. 采集当前前置条件

只使用已知端点、已有读取权限和已安装只读工具；不能读取时报告缺口，不安装工具或修复现场。
仅控制面 GET 列表、精确模板/已知目标详情及只读主机检查；不扫描端口或猜测 API。
报告记录采集时间与脱敏摘要，凭据正文不得进入终端回执、日志或 Git。

| 项 | 需要回传的事实 |
| --- | --- |
| 候选与安装 | 固定代码 SHA、已验收 wheel SHA、31 模块安装位置、独立 venv 是否仍在；无需重跑 715 项 |
| 平台 | 当前版本、已知控制面端点与 CA 校验方式、当前 guest 数；非零则只登记，不清空 |
| 模板 | 精确 template ID、artifact 身份/摘要与来源，是否仍对应审批目标；镜像标注不冒充 guest 实测 |
| W0 | 拟议排他窗口、所有已知创建入口及责任人、禁创确认是否齐备；没有平台 capacity=1 保证不得写成已有 |
| 监督 | 现有 M0/M2 服务状态、心跳/告警；拟用 M3 独立副本路径、运行 UID、umask 077、120 秒截止及进程身份/boot 取证方法；缺少副本仅记待部署，不创建 |
| 网络 | 现有 TLS、双栈/出口约束的可核验证据；不调整防火墙、代理、hosts、隧道或路由 |
| 安装材料 | 逐项列出 launch/precreate/approval/template pins/CA/密钥引用/vault/fence/quota/target tracking 所需路径与写者；只列计划，不创建或展示秘密 |
| 入口接线 | 明确哪个可信调用方执行已安装 `launch_isolated`，怎样从密封回执取得 ID、登记并采集目标，再推进 B；若只有测试驱动而无现场入口，明确标 BLOCKED_IMPLEMENTATION，不临时拼有效创建脚本 |
| 恢复与停止 | 如何只停本轮发送端，保留独立监督与私有账本；UNKNOWN 不重试、不删栅栏、不重置额度、不以空列表替代丢失回执 |

当前读取结果只用于准备，不代替创建前实时重验。旧窗口或旧心跳不自动延续。

## 4. 提交下一轮审批材料，不执行

拟定 A/B 两个 restricted guest，串行、最多各一次创建；C/D public 不在本轮方案。
列清 guest 内进程启动、文件读取/Stat、平台创建/终止及节点部署各自需要的授权。
创建单次外层预算 10 秒、清理另最多 1 秒，guest 截止 120 秒；不得自行放宽。

逐行列明拟测端点、凭据状态、预期响应、调用次数与最坏耗时。
先给正确凭据阳性对照；错误值使用被识别的别名，另一个别名必须省略；缺失行两者都省略。
不得把 `X-Access-Token` 的 403 再写成 traffic token 错误值验证。
跨 guest 行只能在 A 已结束后用其本轮私有 token 对 B 验证，不能借历史凭据或声称双活隔离。
若时间预算无法覆盖矩阵，列出明确 NOT_RUN 行，提交用户取舍；不增加 guest 或自动续时。

## 5. 回传与停止

新增 `V0.1.3_M3_R1_AB_PREFLIGHT_RESULTS.md`，分别记录：

- B8 新提交及原提交完整 SHA、diff 核验结果。
- 只读事实、证据时间、缺口及无需重复的已验收项。
- 现场入口是否已具备；需要 Codex 补交的代码/配置契约。
- 待批准的安装对象、窗口、A/B 逐行矩阵和预算。

仅普通推送执行分支并核验双端；失败不强推。
本轮结束停在“前置材料待审”，不能记 `M3_READY`，不能自行进入真实运行。
