# M3 R1 A 槽限定范围执行——结果报告（zcode，2026-09-17）

## 判定：**A_EXECUTION_PARTIAL**——创建/登记/密封回执/materialize/closeout 成功；**固定五行整槽 NOT RUN（合同预算门拒绝，零探针发送）**；B 零执行。只报告实际取得的结果，非 M3_READY。

## 一、最终候选、入口及材料完整摘要

候选 5d3c27bf1d5d15e0ad09f0ff1fcd6e3e8a70cfdc（wheel 188a0d2c…9fceb）；登记入口
/opt/agentseek-m3-releases/5d3c27b/venv/bin/python -I 全程使用；常驻监督未切换（pid=2750111 全程未变）、
M0/M2/production 零接触。执行前 §二.3 全文件完整 SHA256 从文件重读核验 **12/12 引用检查 PASS**；
materialize 最终预批 8bea7dccc04411680865a0b0c6666ed105fef05c1fb233f0a07ab53969e061b7（程序化确认
与待审版仅差 approved/materialize_exact_rows 两布尔）；创建链摘要同前依赖清单（W0-FINAL e6f7aca9…/
approval-FINAL 0f51a213…/request aac0d215…/precreate 08da21aa…/installation d8b9a2fd…/launcher
69b2056d…/lifecycle e1292612…），执行时以文件实测摘要为钉值。

## 二、真实执行起止时间（全部真实时钟）

| 步骤 | 起 | 止 | 结果 |
| --- | --- | --- | --- |
| 真实时钟门禁（7/7） | 09:00:21Z | — | PASS（W0/approval 真实时钟、guest=0、模板 READY、pins、心跳、无告警） |
| lifecycle create | 09:00:38.856Z | 09:00:41.051Z | **exit 0**（{"saved":true,"next_create_authorized":false}） |
| materialize | 09:01:09.736Z | 09:01:09.899Z | **exit 0**（5 行，slot 摘要 afbddd58…e2f29 与 result 一致） |
| m3_slot 五行 | 09:01:43.191Z | 09:01:43.814Z | **exit 2=合同拒绝**（详见 §五） |
| 监督硬截止 | 09:02:41.661Z | — | kill issued（合同自动执行，非手工） |
| lifecycle closeout | 09:03:14.399Z | 09:03:15.038Z | **exit 0**（known_target_absent=true） |

## 三、实际创建次数、监督登记与生命周期证据

- **实际创建 = 1 次**（max_creates=1 已耗；quota create-slot-0 已写、fence create.pending 留存——
  均按合同不重置不删除）。
- 监督登记：lifecycle attach 内完成 register-sandbox（manifest 登记 sandbox
  a934cee2f71546f288f22d8be77f8601）并等到登记后新心跳（合同路径）；平台观测 collect_created
  列表恰为该 guest。
- 生命周期证据：create-intent.json/create-result.json（lifecycle-A/）与 vault 密封回执（intent+
  receipt，AEAD）保存完整；closeout-result.json 落盘。binding 与 approval-FINAL/真实回执逐字段一致
  （intent_sha256=95d174a7…为真实回执产生，approval_sha256=0f51a213…，create_token 为 w2 新值——
  正文不入本报告）。硬截止：09:02:41.661 kill issued（startedAt+120s 换算），告警
  "termination pending confirmation" 出现后经平台确认解除；终检平台列表空、心跳新鲜、无告警文件。

## 四、materialize 完整结果核验

exit 0（0.163s）；materialize-result.json：rows=5、slot_file 摘要 afbddd58…实测一致、
next_create_authorized=false；out-A/ 18 文件（probe/approval/installation ×5 + intent + slot.json +
result）全部落盘 0600。生成审批为已批委托机械展开（approved=true 行级，绑定真实 sandbox/回执引用）。

## 五、五行逐项结果与真实预算（如实：整槽 NOT RUN）

**五行全部 NOT RUN，零探针发送。** m3_slot 于 09:01:43.191 启动，0.62s 内 exit 2（合同拒绝）：
首行前预算门槛要求剩余 ≥5×16+5=85s，而 guest startedAt≈09:00:41.7、120s 截止≈09:02:41.7，
启动时剩余约 58s < 85s → 合同正确拒绝，slot-intent 已独占烧毁（dispatch-A/），无 slot-result。
**停止位置**：五行执行入口之前；未发送任何数据面请求；未压缩矩阵、未延长 guest、未重试、未换目录、
未手工抽取生成文件。根因（如实）：执行链逐步调用间的编排延迟（create 完成 09:00:41→materialize
09:01:09→slot 09:01:43，间隔共约 62s，为工具往返粒度）耗尽窗口余量；预算门按合同工作正常。

## 六、guest 结束/closeout 核验

监督硬截止 kill 于 09:02:41.661 自动执行；lifecycle closeout（09:03:14）经既有结束检查一次有界核验：
**known_target_absent=true、next_create_authorized=false**；平台列表空与之一致。closeout 不构成 B 授权。

## 七、B 零执行与边界遵守

B 零创建、零探针、零额度消耗（quota 仅 create-slot-0；batch_sequence 中 B 计划仅为登记）；本轮零代码
修改、零依赖变更、零临时执行脚本（全程既有显式入口：m3_lifecycle/m3_materialize/m3_slot）、零窗口
顺延、零预算放宽、零额外创建；监督未关闭未重启（服务 disabled 状态保持，不新增手工 kill——硬截止为
监督合同自动行为）；凭据/回执正文零入回传；三服务终检 active；零 Git。

## 八、留给下一轮的事实

1. 五行矩阵未执行——若用户仍需矩阵证据，需新窗口+新材料（B 计划已登记但 B 批准从未存在）；
   编排延迟教训：窗口内执行链需一次性驱动（或用户批准的单一驱动脚本），逐步工具往返不可行；
2. A 槽额度已耗（一次创建）；fence pending 留存按合同不可清除；
3. 全部材料/记录/回执留存审计（A-slot-w2-20260917/ 与 EXECUTION-RECORD-A.json）。
