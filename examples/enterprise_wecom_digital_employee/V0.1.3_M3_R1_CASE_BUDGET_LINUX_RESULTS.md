# M3 R1 单行预算补丁——.172 独立复验与编排缺口盘点结果（zcode，2026-09-17 UTC）

## 判定：离线复验 **R1_CASE_BUDGET_OFFLINE_PASS**；§4 编排缺口盘点 **BLOCKED_IMPLEMENTATION**（两项分开判定）

离线通过不解除 `BUDGET_ADJUDICATION_PENDING`，阶段二仍待批准，非 M3_READY。本轮零创建、零探针、
零真实平台调用（测试内临时 loopback/子进程按既有隔离套件自起自收）、零 Git；现用 aa9e1e5 安装、
监督服务、身份 pins、禁用草稿全部原样保留（/opt 树 mtime 未变，三服务终检 active）。

## 一、候选与到货

| 项 | 值 |
| --- | --- |
| 固定候选 Git SHA | a12bf86b5f10b73db7b67d4eaeed125f688c61dc（基线 2a7d8e5 为祖先，.171 备料侧核对） |
| wheel | agentseek_execution_r1-0.1.3a0-py3-none-any.whl，SHA256 `bc7cce1eac25da76d25af6eabeaabfebda902cc81eb1acf580da60af7f0ee44e`——与工单预期一致（不符即停条款未触发） |
| 到货清单 | `sha256sum -c MANIFEST.sha256` **140/140 OK** |
| 闭包 | 5 入口 / 32 运行模块 / 72 文件（package/R1_INVENTORY.json） |
| 解释器 | 复用 /opt 已装 cpython-3.13.14（包未附带，未重复安装）；deps 仅用包内 19 wheel，`--offline --find-links`，未回找旧包目录、未联网 |

## 二、环境（分离口径）

- scratch=/root/m3-r1-budget-work-20260917（本轮专用，保留供审计）：
  `venv-runtime`（wheel+10 运行时钉版=11 发行版，**无 pytest**，仅用于 installed-wheel 检查）与
  `venv-test`（pytest 栈+运行依赖、**不装 wheel**，模块经 conftest 从闭包 src 导入）严格分开；
  PYTHONPATH 显式指向闭包 src（conftest）与 examples，未从现用 /opt 安装补模块。
- 重建 repo 布局（contrib/examples/scripts/tools）仅从本包闭包复制；不覆盖 /opt、不改系统
  Python/unit/.pth。

## 三、复验结果（HANDOFF §3）

| 步骤 | 结果 |
| --- | --- |
| install_check（venv-runtime `python -I`） | **isolated=true，installed_modules_verified=32，invalid_input_rejected=5**；未用真实配置补测成功路径 |
| 隔离源码完整测试 | **747 passed（45.29s）**，旧基线 741+6 实例对账一致 |
| 清单工具 | **5 passed**（1.45s） |
| 失败与修正过程（如实） | 首次全量收集 27 errors：venv-test 缺模块级导入所需运行依赖（cryptography 等）。按 HANDOFF §3.3"独立测试环境"口径补装运行依赖（仍不装 wheel）后完整通过；与 .171 侧记录的修正过程同构，未改候选绕过失败 |
| 临时子进程收尾 | 测试后 ps 全查：本轮 scratch 派生进程**零残留**；常驻进程仍仅 M0/M2/M3 三个监督 |

### 四个新增边界测试逐项确认（test_m3_case_process.py，15 个参数实例全 PASSED）

| 工单要求 | 测试（实测 PASSED） | 代码依据 |
| --- | --- | --- |
| ①剩余不足 16 秒零启动 | test_insufficient_or_invalid_parent_deadline_never_launches[111/112/115.999/nan/inf/True] | m3_case_process.py:39-43：启动前 `deadline-now ≥ CASE_BUDGET_SECONDS(15)+CLEANUP_RESERVE_SECONDS(1)`，不满足 DENIED，不启动 worker |
| ②同一截止跨行不刷新 | test_next_row_cannot_refresh_the_shared_cutoff | :40-44 调用方传入较早可信截止；stop=now+15 每行独立，不从行启动重造 guest 寿命（注释 :41-42 明示） |
| ③子进程拒绝旧 30 秒载荷 | test_child_rejects_insufficient_or_legacy_budget_before_reading[10.99/15.01/30] | :72-73 子进程接收时 `11 ≤ stop-now ≤ 15`；30s 旧载荷与窗外交集一律 DENIED |
| ④UNKNOWN 不重试 | test_timeout_propagates_unknown_without_retry | run_worker 超时原样 UNKNOWN；单次调用、无循环重试；不删行记录/fence/quota（模块 docstring :3） |
| （附加）单总预算+空环境 | test_parent_has_single_total_budget_and_no_environment[200-15/116-15] | :45-50 唯一 run_worker、environment={} |
| （附加）行内请求前后复核 | test_inline_request_rechecks_budget_after_preparation[9/20] | :57-63 prepare 后剩余复核、observe 截止钳制 |

## 四、REFERENCE 只读核对——**全部一致，无差异**

| REFERENCE 合同 | 代码实测 | 一致性 |
| --- | --- | --- |
| 整行外层 15s+1s 清理 | :28-29 常量、:43-44 预算/清理划分 | ✓ |
| 启动前剩余<16s DENIED | :43（15+1 门槛，115.999 边界实测拒） | ✓ |
| 子进程接收窗 11–15s、拒旧 30s | :72-73 | ✓ |
| 内层约束不放宽 | :60（≥10）、:85（≥11）、approval_deadline 取 min 钳制 | ✓ |
| 超时 UNKNOWN 无重试、不删记录 | worker_process 超时路径+测试④ | ✓ |
| guest 寿命 startedAt+120 单调换算 | node_supervisor.py:53 `DEADLINE_SECONDS=120`（冻结值未动）、:529 单调换算 | ✓ |
| 15+1 非绝对耗时证明 | :34-35 docstring "OS launch is not a realtime guarantee" | ✓ |
| A 5×16=80s / B 6×16=96s 核算 | 算术核对 ✓；且接受 REFERENCE 勘误：80/96 不得直接宣称 120s 内完成（启动消耗/文件准备/收尾余量另计）；转发摘要"压缩至 30 秒"口径无效（旧码已是 30+1） | ✓ |

## 五、§4 跨行驱动能力只读盘点——**BLOCKED_IMPLEMENTATION（四项均无调用方，逐项依据）**

| 能力 | 现状 | 文件/入口依据 |
| --- | --- | --- |
| ① 同机单调钟可信截止（不逐行 now+120） | **未实现（调用方缺位）**。单行入口仅"接受"调用方传入的较早可信截止并做 16s 门槛；运行时无任何代码从 guest startedAt/批准事实构造该截止 | m3_case_process.py:33,39-44（接口存在、注释明示 caller 供给）；全库无构造方 |
| ② 首行前核验全槽 80s(A)/96s(B)+额外余量 | **未实现**。全运行模块检索 `80/96/matrix/orchestr/driver/cross-row/next_row/all-slot/slot-budget` 零命中 | grep 全 32 模块零匹配 |
| ③ 每行检查剩余；阳性对照失败/UNKNOWN/身份审批异常即停后续行与 B 槽 | **部分：仅单行入口有剩余检查**（:43/:73/:85）；"停止后续行与 B 槽"需要跨行循环/编排器——不存在。dispatch_isolated 在运行代码中**无调用方**（仅定义与测试） | m3_case_process.py:32；grep dispatch_isolated 运行时零调用 |
| ④ 保存 A 回执/准备 B 动态字段期间正确核算寿命 | **未实现**。launcher 仅有创建/attach/closeout 各自 10s 预算，无探针行寿命核算，无回执保存期间的预算代码 | m3_launcher.py（全文无 case/row 编排，仅 :24 导入 _decode/config_bytes） |

结论：本补丁只收紧单行入口，未交付矩阵驱动器/批次停止编排/自动创建权限（与 REFERENCE
"阶段二仍待落实的执行条件"自述一致）。**BLOCKED_IMPLEMENTATION 交回 Codex**；未现场拼脚本、
未把操作员目测耗时当作已实现约束。盘点过程零读取现场 key、零物化 api_key、零改批准文件、
零变更现有草稿。

## 六、边界与回退

本轮新增对象仅 /root/m3-r1-budget-work-20260917（scratch+双 venv+repo 重建，保留供审计）与
回传目录两件；回退即整目录留存待审，无外部依赖方。未切换/重启任何现用服务；未接真实平台
（控制面零请求）；.172 零 Git。BUDGET_ADJUDICATION_PENDING 维持待审（离线 PASS 不自动解除），
阶段二执行条件（W0 责任人/writer 确认/窗口/两槽审批/api_key 物化/额外余量数值）仍待用户批准。
