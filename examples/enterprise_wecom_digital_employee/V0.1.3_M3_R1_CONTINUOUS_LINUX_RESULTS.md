# M3 R1 连续驱动入口——.172 独立离线复验与合同核验结果（zcode，2026-09-17）

## 判定：**R1_CONTINUOUS_OFFLINE_PASS**（仅离线/合同结果；非现场矩阵 PASS、非 M3_READY、非已部署；后续安装登记/新窗口/执行授权另发工单）

## 一、到货核验

- MANIFEST **152/152 OK**（本机独立复核）；wheel SHA256 `79531abc26d03c46ef5d95aa3ae8dacea657168e46fb893704ff13bb1a7c2b17` 与工单一致（不符即停未触发）；候选 89be61f71ee6201984259abda0dfe9f059396153（归档基线 2eb7a47d…，.171 侧祖先核对）。
- 闭包 9 入口 / 36 运行模块（新增 m3_continuous）；HANDOFF/README 已完整阅读，无合同冲突。

## 二、隔离安装 36/9 与入口检查

- 全新 scratch=/root/m3-r1-continuous-work-20260917：venv-runtime（wheel+10 钉版=11 发行版，无 pytest）与 venv-test（pytest+运行依赖）分离；已批准独立解释器 cpython-3.13.14；全程 `--offline --find-links deps/`，未借生产 venv。
- install_check（venv-runtime `python -I`，venv/bin/python 调用、未直启共享解释器）：**isolated=true，36 模块来源/摘要匹配，9 入口无效输入拒绝**。
- m3_continuous `--help`：`--config CONFIG --sha256 SHA256`（无其他参数）；无效输入 **exit 2**（契约保持）；未用任何真实配置冒烟。

## 三、测试实际计数

- 隔离源码闭包 execution+node_supervisor 合计 **813 passed（47.79s）**，与 HANDOFF 预期一致（813=799+14 项新增 continuous 单测）；清单工具 **5 passed（1.93s）**。
- 解释器与导入源：Python 3.13.14；源码测试 import 实测解析至本轮闭包 `repo-root/contrib/agentseek-execution/src/agentseek_execution/__init__.py`（非现用安装）。
- 临时子进程按既有用例收尾，测试后**零残留**；真实监督三件原样（未动）。

## 四、连续驱动合同逐项核验（HANDOFF §3，逐条 file:line）

| # | 合同 | 依据（m3_continuous.py） | 结果 |
| --- | --- | --- | --- |
| 1 | schema=1 精确六字段 | :44-45 {schema,lifecycle,lifecycle_sha256,preapproval,preapproval_sha256,directory} | ✓ |
| 2 | 只接受 A；B/未批准预批创建前拒绝 | :51 slot 三重 =="A"；:52 approved 与 materialize_exact_rows 双 true 前置（test_preflight_rejects_without_create） | ✓ |
| 3 | 创建前 binding/plan/监督身份/W0/模板/request/vault-key 路径匹配 | :23-37 _binding_check：launcher→installation(schema2)→precreate 链钉摘要；expected_create==plan.binding(approval)；plan/监督四键/请求与三路凭据路径逐项一致（静态比较，不替代实时门禁——HANDOFF 自述口径） | ✓ |
| 4 | 四个空私有目录互斥不嵌套 | :57-68：directory/life_dir/output/dispatch 四者 distinct+parents 不嵌套+逐一必须为空；:58 create_result_file==life_dir/create-result.json | ✓ |
| 5 | continuous-intent 独占持久化先于创建；重放不再次创建 | :79 O_EXCL intent 在 :81 execute(create) 之前；重复调用目录非空被 :63-68 拒；失败后 intent 留存无清理（:104-105 注释；test_summary_write_failure_never_replays） | ✓ |
| 6 | 单次调用内 create→materialize→slot，无人工/往返/sleep/重试/自动 B | :81→:86→:96 进程内直调 execute/materialize/run_slot；无 sleep/重试/B 路径（test_continuous_order_and_replay_denied） | ✓ |
| 7 | 跨阶段重钉摘要+生成结果与磁盘一致；缺失/篡改不派发 | :73-76 pinned() 每阶段前复核三配置未变；:87-92 materialize-result 对账（摘要/rows==5/slot 路径+摘要）；:97-98 slot 结果核验——任一失败在 run_slot（唯一探针派发点）之前终止（test_changed_preapproval_after_create_stops_before_generation、test_incomplete_generation_never_dispatches） | ✓ |
| 8 | 异常不继续、不删 intent、不换目录、不手工抽取 | 异常即传播 exit 2，finally 仅关 fd；无清理/重试代码（test_failure_stops_chain_and_burns_intent 参数化各阶段） | ✓ |
| 9 | 85s 整槽预算与 120s 监督截止未放宽 | m3_slot.py:111 `len(prepared)*16+reserve` 未变；m3_case_process.py:28 CASE_BUDGET_SECONDS=15 未变；node_supervisor.py:53 DEADLINE_SECONDS=120 冻结值未变 | ✓ |
| 10 | 输出仅结果与耗时；closeout_required=true、next_create_authorized=false | :100-102 summary={schema,config_sha256,rows_observed,next_create_authorized:False,closeout_required:True,timings}——无 token/配置正文 | ✓ |

closeout 独立性：本入口不等待结束、不 kill、不自动 closeout、不赋下次创建权（docstring :1-5 + summary 字段）；未来真实执行仍按原 lifecycle 合同单独结束核验。

## 五、模拟测试与未执行现场项（如实）

- 14 项新增单测为**模拟阶段调用**（monkeypatch 替换 execute/materialize/run_slot）验证编排顺序/失败隔离/重放拒绝——不冒充真实平台全链或性能验收；HANDOFF 自述"不能据本地耗时保证真实平台满足 120s"如实接受。
- NOT RUN：真实创建/探针/平台写请求、真实配置运行 m3_continuous、/opt 安装或入口切换、监督服务变更、旧 A 的 quota/fence/intent/回执/审批材料触碰（全部原样）、新现场 approved=true 材料、联网、Git。

## 六、边界与收尾

本轮新增对象仅 scratch（双 venv+repo 重建+日志）与回传目录两件；三服务终检 active；现场零变更。
若后续采用本候选：需另发安装登记工单（部署至 /opt/agentseek-m3-releases/89be61f 并重验）、新一轮状态/材料/窗口/执行授权——本轮离线通过不含任何执行授权。
