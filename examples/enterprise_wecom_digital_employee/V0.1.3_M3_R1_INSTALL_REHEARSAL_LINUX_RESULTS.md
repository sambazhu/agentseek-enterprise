# M3 R1 版本安装与隔离配置准备演练——.172 执行结果（zcode，2026-09-17 UTC）

## 判定（三项分开，不合写为现场全链 PASS）

1. **新版本安装/入口登记：PASS**（be59e54 独立安装+重验通过，受信调用路径已登记；旧现场零变更）
2. **合成准备计时：完成**（3 轮零失败；机械层 A/B 各 ≤0.036s；关键发现：Agent 逐文件合成模式在 120s 窗口内不可行，阶段二必须工具化动态字段填入）
3. **预算：BUDGET_ADJUDICATION_PENDING 维持**（reserve_seconds 建议值 5s 仅建议，未自行批准；非 M3_READY）

本轮零真实平台调用、零创建、零探针、零有效审批、零凭据物化、零服务重启/切换、零 Git。

## 一、到货与复用复核

- 工单包 /root/m3-r1-rehearsal-handoff-20260917：MANIFEST **2/2 OK**（README+HANDOFF）。
- 复用包 /root/m3-r1-slot-handoff-20260917：本轮重新逐文件自校 **146/146 OK**；
  wheel SHA256 `c85c651768adc4a92b4dd33327a9297b53310d9a640b922968e852c31ed0e6da` 与工单一致。
- 目标安装路径 /opt/agentseek-m3-releases/be59e54 原不存在（净目录新建，零覆盖）。

## 二、新版本安装与重验（第一项判定）

| 项 | 结果 |
| --- | --- |
| 旧引用关系（安装前只读快照） | unit ExecStart 仍指 /opt/agentseek-m3-supervisor/venv/bin/python -I …/node_supervisor.py（UMask=0077）；旧 venv=aa9e1e5（0.1.3a0，wheel c725dc67…）；pins executable=/opt/…/cpython-3.13.14…/bin/python3.13 |
| 安装 | /opt/agentseek-m3-releases/be59e54/venv（目录树 0700）；复用已批准解释器 cpython-3.13.14；uv `--offline --find-links` 装 wheel+10 钉版依赖=11 发行版；未升级依赖、未改 .pth/PATH/PYTHONPATH/systemd |
| 实际解释器/模块来源 | 解释器规范绝对路径 /opt/agentseek-m3-supervisor/python/cpython-3.13.14-linux-x86_64-gnu/bin/python3.13（共享、原样）；模块来自新 venv site-packages（…/be59e54/venv/lib/python3.13/site-packages/agentseek_execution/） |
| install_check（新 venv `python -I`） | **isolated=true，34 模块摘要一致，7 无效入口拒绝**；wheel 摘要即上列 c85c6517… |
| 新入口 --help | m3_slot：--config/--sha256；m3_lifecycle：--config/--sha256/--action{create,closeout}——与 slot 轮合同一致；未提供任何有效配置 |
| 旧现场 | /opt/agentseek-m3-supervisor 安装/解释器/脚本/unit/pins 全部原样（M3 服务 MainPID=2750111 未变，ExecMainStartTimestamp 仍 01:04:55） |

## 三、入口登记（仍属第一项判定）

- 私有安装记录：/opt/agentseek-m3-releases/be59e54/INSTALL-RECORD.json（0600），
  SHA256 `dda133c8499447f2492dd58d5887b3722d7473860048fe403d366a9a7035a68a`。
  内容：候选 be59e54…、wheel 摘要、登记入口=新 venv python 规范绝对路径、解释器路径、模块来源、
  检查结果（34/7/isolated+help+R1_SLOT_OFFLINE_PASS 前验）、旧路径（aa9e1e5 现役明细）、
  回退路径（仅恢复入口选择；不删新目录、不停监督）。
- 语义申明（记录内含）：登记只指**后续显式调用的入口选择**；不切换常驻进程、未建 current 软链、
  不假定导入成功=现有进程已换版本。安装成功后已登记；如需回退按记录执行。

## 四、合成资料准备计时演练（第二项判定）

设置：/root/m3-r1-rehearsal-work-20260917/synthetic/round-{1,2,3} 三轮独立目录（0700）；
run_id=SYNTHETIC-REHEARSAL-20260917-R{n} 等全部 ID/token/key 均带 SYNTHETIC 标注，boot_id 置零、
domain=synthetic.invalid；未复制任何真实凭据/vault/fence/quota/回执/批准，未复用真实 run_id 可执行文件。
一次性计时辅助程序 rehearsal_timing_helper.py（纯 stdlib，只处理合成 JSON 的写盘/摘要/引用核对，
无网络/无执行器/无批准逻辑）随材料留存——**明确声明：非交付候选的一部分**。
每轮模拟"收到创建回执之后"：保存合成回执→填动态字段（A 5 行/B 6 行含 X1 donor 三字段、B 绑 A
create-result/slot-result）→规范 JSON 写盘（canonical sort_keys）→逐文件摘要→引用顺序核对
（slot config→行安装摘要→probe/approval 摘要逐项回验；B 另验 previous_slot=A 五行成功记录）。
全部 approval 合成件 approved=false、w0_accepted=false。

| 轮 | 缓存 | 结果 | A_prep | B_prep | 轮墙钟 | 轮间隔 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 冷（helper 首载） | OK | 0.030s | 0.036s | 0.070s | — |
| 2 | 暖 | OK | 0.036s | 0.036s | 0.074s | 0.036s |
| 3 | 暖 | OK | 0.033s | 0.034s | 0.068s | 0.037s |

分步（三轮一致量级）：rows 写入 16-20ms 为大头（A5/B6 文件），approvals 9-11ms，回执/槽配置/核对
各 1-4ms。冷/暖差异在此规模不可分辨（如实记录）。零失败、零校验删减、无固定 sleep。
轮间隔为同批连续执行（批内无 Agent 往返）；批间 Agent 等待未单独插桩，见"未计入"。

**关键发现（可行性）**：本轮 A/B 各槽准备被一次性辅助程序整体接管，机械层仅 0.03-0.04s；
而本项目补充轮的真实 Agent 逐文件合成锚点为 **01:31:51（凭据就绪）→01:45:49-01:47:24（9 草稿+说明
落盘）≈14-15.5 分钟**（含文档阅读与设计，文件 mtime 可证）。结论：若阶段二"回执后资料准备"由
Agent 逐文件现写，120 秒 guest 窗口内不可行；可行形态必须是**预先批准的模板+一次性辅助程序机械
填入动态字段（回执/binding/摘要链）**，Agent/用户只做批准与核对——本轮已按该形态实测机械层。

## 五、预算建议（第三项判定：仅建议，不自行批准）

账目（按工单格式）：

| 段 | 量值 | 性质 |
| --- | --- | --- |
| 已消耗：create 外层 10s / attach 10s（含登记 3s/心跳 3s）/ 首轮证据采集 ≤10s | 10/10/≤10s | 预算值，真实平台耗时**未测** |
| 已消耗：回执保存+动态资料准备（A/B 全部行/批准/槽配置+核对） | 机械层实测 A 0.036s / B 0.036s（max） | 合成实测；仅覆盖工具化形态 |
| 全槽预算 | A 5×16=80s；B 6×16=96s | 既有合同（不重算 120s、不重复扣算） |
| **建议余量 reserve_seconds** | **5s（建议值）** | 依据：合成机械最大 0.036s×~140 倍裕量，覆盖核对扫描、I/O 抖动、helper 冷启动；**"观测最大值+裕量"仅是建议，非统计上界、非硬实时证明、不自行批准**；合成最大耗时≠reserve |

首行实际检查（以建议值代入示意，待批）：剩余可信寿命 ≥ A 80+5=85s / B 96+5=101s。

**未计入（不得假设为零）**：用户批准与消息往返（W0 放行、两槽审批、X1 确认）；Agent 对机械输出的
逐文件核对（若逐份细核可达分钟级——按补充轮锚点，阶段二须限定为预批模板+抽核，否则不可行）；
真实平台 create/attach/实时证据查询/网络抖动；真实 vault/fence/quota/receipt 物化耗时。
合成演练不能证明完整矩阵可在 120s 内完成；实际证据门不足时仍必须停止。

## 六、边界与回退

新对象仅：/opt/agentseek-m3-releases/be59e54/（venv+安装记录）、scratch（synthetic 三轮+helper+logs）、
回传目录两件。旧安装/unit/pins/草稿/服务零变更（终检三服务 active、MainPID 未变）；控制面零请求；
零 Git。回退：入口选择按 INSTALL-RECORD 恢复旧路径即可；新目录与合成材料留存审计，不删不停。

## 七、勘误（按本工单 §6 补充，不 amend/rebase 历史）

上轮 V0.1.3_M3_R1_SLOT_LINUX_RESULTS.md 中"与 .171 macOS 3.12 记录互证"应更正为
**"与 Codex 开发端 macOS 3.12 记录互证"**（该 macOS 本地验证记录属 Codex 开发侧，非 .171）。
