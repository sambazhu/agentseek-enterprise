# M3 R1 生成器版本安装与阶段二审批清单准备——.172 执行结果（zcode，2026-09-17 UTC）

## 判定（分开记录，不得合写为真实执行获批或 M3_READY）

1. **安装/入口登记：PASS**——5d3c27b 独立安装+重验通过，后续显式入口已登记为新 venv python 规范绝对路径；be59e54 与 aa9e1e5 现场零变更。
2. **阶段二审批清单：已准备（PREPARED，未批）**——四类分层脱敏清单随回传提交，全部确认项留白待用户；BUDGET_ADJUDICATION_PENDING 维持。

本轮零真实平台调用、零创建、零探针、零结束查询、零凭据物化/读取、零现场 true 预批、零 W0 放行、零服务启停、零 Git。未调用 materialize、未改既有禁用草稿。

## 一、到货与复用复核

- 工单包 /root/m3-r1-mi-handoff-20260917：MANIFEST **2/2 OK**（README+HANDOFF）。
- 复用包 /root/m3-r1-materialize-handoff-20260917：本轮重新逐文件自校 **149/149 OK**；
  wheel SHA256 `188a0d2cdcd9705befe916a9b850e8791b81471ca88d455a8c97d9b94df9fceb` 与工单一致。
- 目标路径 /opt/agentseek-m3-releases/5d3c27b 原不存在（净目录新建，零覆盖）。

## 二、安装与入口登记（第一项判定）

| 项 | 结果 |
| --- | --- |
| 安装前只读快照 | 常驻监督（aa9e1e5 现役）：unit ExecStart 指 /opt/agentseek-m3-supervisor/venv、MainPID=2750111、pins exe=共享解释器；be59e54 入口 INSTALL-RECORD 摘要 dda133c8499447f2492dd58d5887b3722d7473860048fe403d366a9a7035a68a（未变） |
| 安装 | /opt/agentseek-m3-releases/5d3c27b/venv（目录 0700）；复用已批准 cpython-3.13.14；uv `--offline --find-links` 装 wheel+10 钉版=11 发行版；未改 .pth/PATH/PYTHONPATH/unit/pins |
| install_check（新 venv `python -I`） | **isolated=true，35 模块摘要一致，8 无效入口拒绝** |
| 三入口 --help | m3_materialize：--preapproval/--sha256；m3_slot：--config/--sha256；m3_lifecycle：--config/--sha256/--action{create,closeout}——均与合同一致，未给任何有效现场配置 |
| INSTALL-RECORD | /opt/agentseek-m3-releases/5d3c27b/INSTALL-RECORD.json（0600），
  SHA256 `e915ec4975457487cda1a76a2ff895cd5add9e20911d8b4b15ef210aabb75a7c`。
  含候选/wheel/登记入口/解释器/模块来源/检查结果（35/8+三 help+R1_MATERIALIZE_OFFLINE_PASS 前验）/回退路径（恢复入口选择至 be59e54，不删新目录不停监督）。**记录内显式警告：启动一律用本 venv 的 bin/python 规范绝对路径，禁止改用共享 Python 路径直接启动（丢 venv 安装上下文）** |
| 既有现场核验 | be59e54 INSTALL-RECORD 摘要未变（dda133c8…）；aa9e1e5 常驻监督 active、MainPID=2750111 未变；无 current 软链；未启动任何任务 |

三候选状态（本轮后）：aa9e1e5=常驻现役监督；be59e54=已安装、保留为回退入口；**5d3c27b=已安装并登记为后续显式入口**（含 materialize 生成器）。登记只改后续工单选版，不表示进程换版。

## 三、阶段二审批清单（第二项判定：PREPARED 未批）

V0.1.3_M3_R1_STAGE2_APPROVAL_CHECKLIST.md（随本回传提交）按四类分层：
- **已知事实 F1-F7**：候选与入口（本轮登记路径+venv 启动警告）、run/template 现值（有效性窗口另验；不新建 run 不重置额度栅栏）、执行链离线结论、凭据现状（受控来源已登记/物化待批）、顺序纪律（A 批准在 A 创建前完成；B 材料在 A 结束后；机械生成无新增授权）、失败纪律（UNKNOWN 即停留痕不重跑）、验证边界（非 M3_READY）。
- **建议 S1-S2**：reserve_seconds=5s（建议非生效；A 首行 85s / B 101s）；真平台各段未测；预算不足整槽 NOT RUN，不缩矩阵不自动重试。
- **待用户批准 A1-A11**：W0 责任人/创建入口与 writer 清单/逐人禁创确认（未确认保持待定，不写 true、不推断默认同意）/拟议窗口（明确时区，建议 UTC）/A 与 B 创建批准及逐行委托（矩阵固定 A5 请求 B6 含 X1、120s 不延长；批准后钉摘要）/预算裁定/api_key 物化/阶段二总放行。
- **动态待填 D1-D5**：A/B 密封回执、实际 sandbox/intent、A 成功记录、各摘要与 confirmed_epoch——全部只能来自真实事实，禁合成冒充禁预造。

清单为脱敏件：不含预批/安装正文（control 含 api_key 正文不在内）、不含密钥内容。未调用 materialize、未写任何 approved=true/materialize_exact_rows=true/w0_accepted=true 文件、未改既有禁用草稿。

## 四、边界与回退

新对象仅 /opt/agentseek-m3-releases/5d3c27b/（venv+记录）、scratch（/root/m3-r1-mi-work-20260917 日志）、
回传目录三件。M0/M2/M3 服务、production/标签/网关/MCP/DB/WORK 全未动。回退：入口选择恢复至
be59e54（按 5d3c27b 记录的回退条目），新目录留存审计；安装/核验若失败本应不登记（本轮一次通过，
未触发）。BUDGET_ADJUDICATION_PENDING 维持；5s 未因本轮安装生效。
