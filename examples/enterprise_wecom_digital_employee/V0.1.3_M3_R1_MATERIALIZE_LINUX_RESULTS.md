# M3 R1 预批准配置生成器——.172 独立离线复验与合同审阅结果（zcode，2026-09-17 UTC）

## 判定：**R1_MATERIALIZE_OFFLINE_PASS**（仅离线/合同结果；不代表部署或真实批准；BUDGET_ADJUDICATION_PENDING 维持，非 M3_READY）

## 一、候选与到货

| 项 | 值 |
| --- | --- |
| 固定候选 Git SHA | 5d3c27bf1d5d15e0ad09f0ff1fcd6e3e8a70cfdc（基线 dbe129d，.171 侧核对） |
| wheel | agentseek_execution_r1-0.1.3a0-py3-none-any.whl，SHA256 `188a0d2cdcd9705befe916a9b850e8791b81471ca88d455a8c97d9b94df9fceb`——与工单一致（不符即停未触发） |
| 到货清单 | `sha256sum -c MANIFEST.sha256` **149/149 OK** |
| 闭包 | **8 入口 / 35 运行模块 / 79 文件**（新增 m3_materialize；tools 用包内本候选版本） |
| 环境 | 复用 /opt cpython-3.13.14；scratch=/root/m3-r1-materialize-work-20260917；venv-runtime（无 pytest）/venv-test（预装运行依赖）分离；全程 offline、零 Git |

## 二、复验结果（§3）

| 步骤 | 结果 |
| --- | --- |
| install_check（venv-runtime `python -I`） | **isolated=true，35 模块摘要一致，8 无效入口拒绝** |
| 闭包完整测试 | **799 passed，47.17s**（实际解释器 Python 3.13.14/.172 Linux；不与 macOS 3.12 基线混记） |
| 清单工具 | **5 passed**（1.71s） |
| 新入口 --help | `--preapproval PREAPPROVAL --sha256 SHA256`；无效输入 exit 2（契约保持）；未传任何现场配置 |
| 合成 fixture 兼容性 | test_exact_approvals_and_probes_accepted_by_existing_readers（A 五行，生成 approval 经既有 read_approval、probe 经既有 prepare_receipted 读取成功；重复运行被 intent 独占拒绝）与 test_b_materialization_uses_real_donor_and_existing_probe_reader（B 六行，X1 末行 donor 凭据可读）**全 PASSED**；全程零请求发送 |
| 临时子进程 | 测试与计时后**零残留**；真实监督三件原样 |

### 失败路径九类逐项核验（定向 -v 全 PASSED，不输出完整成功清单、无自动续写）

| 工单要求 | 测试实例（结果） |
| --- | --- |
| 未批准 | [not_approved] PASSED |
| 未明确委托 | [no_delegation]（materialize_exact_rows=false）PASSED |
| 过期 | [expired]（预批过期）+ [expired_during_write]（写盘中过期）PASSED |
| 跨 boot/创建身份 | [different_boot] + [foreign_create] PASSED |
| 错误 pin | [bad_pin] PASSED |
| 重复 case | [duplicate_cases] PASSED |
| 密封回执缺失 | [receipt_missing] PASSED |
| 途中预批准/回执变化 | [preapproval_changed] + [receipt_output_changed] PASSED |
| 写盘失败 | test_partial_output_never_publishes_slot_and_cannot_resume PASSED（intent 落盘但 slot.json/materialize-result.json 不发布；目录烧毁不可续跑，重跑被 O_EXCL/空目录校验拒绝） |
| （附加）未知字段 | [unknown_field] PASSED（精确字段集拒绝） |

### 交付生成器计时（独立 synthetic fixture，三轮，仅调用本生成器）

fixture 为我方独立构造的合成世界（双密封回执[A donor+B target]、双 fence 记录[create.pending=B、
create.closed-*=A]、B schema-3 successor 链、previous_slot 记录；全部 SYNTHETIC 标注，真实 boot_id
用于通过同机校验），非测试 monkeypatch 环境：

| 轮 | A（5 行） | B（6 行含 X1） |
| --- | --- | --- |
| 1 | 0.162s | 0.172s |
| 2 | 0.166s | 0.174s |
| 3 | 0.168s | 0.191s |

子进程级墙钟（含解释器启动、vault AEAD 双读、donor 链校验、fence 核对、5/6 行×3 文件写盘与摘要）。
**此为交付生成器实测，取代演练轮一次性辅助程序 0.036s 的估计**；量级仍远低于预算关注点。

## 三、预批准合同审阅（§4，逐字段文件:行依据）——全部落实

- **顶层 14 字段精确集**：m3_materialize.py:37-39 {schema, approved, materialize_exact_rows,
  expires_epoch, slot, reserve_seconds, expected_create, create_result_file, receipt_source,
  installation, output_directory, dispatch_directory, case_ids, approval_refs}；B 另加
  previous_slot/donor（:40、:88-90）；多余字段拒绝（[unknown_field]）。
- **approved 与 materialize_exact_rows 必须显式 true**（:41-42）；false 草稿不可运行；本轮未填任何
  现场 true 文件。expires 剩余 ≥16s（:45）；reserve 显式、无默认、0<r<120（:46）。
- **expected_create**：完整 CreateBinding、intent_sha256 全零占位+restricted=true（:47-49）；
  同机 boot 匹配（:50）；实际 intent 只从创建输出经 vault.read_intent 校验（:66）；
  sandbox ID 只取自密封回执 vault.read→receipt.sandbox_id（:67、:113）；其余身份字段逐项一致
  （:59 replace(created,intent=0)==expected）。receipt_source 恰五字段、不允许指定 create/token（:60-63）。
- **installation 七字段精确集**（:70-72）；plan.binding(approval)==expected（:74）；template_pins
  校验且模板一致（:75-77）；control 与 plan/source 逐项一致且 api_key 与受控文件字节相等（:78-81）；
  supervisor_directory/supervisor_identity/exclusive_window/create_request_file 为预先固定上下文透传，
  不自动补网络/身份/W0（§5 由执行门实时验证）。
- **矩阵固定**：MATRIX 常量（m3_slot）+B 附加 ("E1","cross_guest")（:82）；case_ids/approval_refs
  数量=行数且唯一（:83-86）；用户 JSON 不可自定义行选择。
- **B 绑定**：previous_slot{path,sha256} + A 成功记录四项核对（slot=A/rows=5/未授权后续/create==donor）
  （:88-94）；donor 经 donor_receipt 全链复核（:95-96，同 vault 双回执/successor schema-3/双 fence）。
  B 预批在 A 结束后 B 创建前准备；本工具不生成 B 创建安装/创建批准（输出 installation 为行级 case
  安装，非 create 链）。
- **独占写序**：输出目录必须新建空（:104）→ materialize-intent.json 先行独占（:105）→ 逐行
  probe/approval/installation（:107-124）→ 途中五重复核（预批/创建结果/key/回执/有效期不变，:128-130）
  → **slot.json 最后发布**（:131）→ materialize-result.json（:132-134，含 slot 路径+摘要供调用方核验）。
- **部分失败≠交付**：任一步失败即停，残留部分行审批不可抽取执行/拼槽（O_EXCL+空目录校验+intent
  烧毁；docstring :6"Partial output burns the directory and is never resumed or overwritten"）；
  不删 intent 重跑、不自动换目录规避。
- **行 approval 为既有 schema 1**：approved=true、精确 Binding、固定 expiry（:117）——已批准委托的
  机械展开，非草稿翻 true（false 在 :41-42 已被挡）。
- **私密性**：control 含 api_key 正文（:81），预批与输出安装文件均落 0600 私有目录；本轮全部为
  SYNTHETIC 值，**未读取真实凭据，回传不含任何预批/安装正文**（仅摘要与合成计时数据）。

## 四、验证边界与 NOT RUN（如实）

- 生成器零网络/零创建/零探针；不证明 W0/监督/模板/平台现状——仍由执行入口实时验证。
- 兼容性=生成结果可被既有 read_approval/prepare_receipted 读取，**不是 slot→case→真实平台端到端
  证明**；严格 installation 上下文运行合法性仍需完整执行门校验，不把"JSON 生成成功"叫配置验收。
- 一键 create→materialize→slot 未实现（按工单不要求）；不自动批准 B、不绕过 UNKNOWN。
- 现场准备 B create 安装及预批模板的具体流程：本轮未发现新合同缺项（生成器侧闭环）；若阶段二
  编排仍有缺项届时另列。真实生成耗时中的现场侧等待（用户批准/消息往返）仍未计入，reserves 数值
  仍待审（5s 仅为未批准建议值）。
- 未更新现用 be59e54 安装与入口（/opt/agentseek-m3-releases/be59e54 mtime 未变；三服务 active、
  M3 MainPID 未变）；零真实平台调用；零 Git。

## 五、回退与留存

新对象仅 scratch（venv×2/repo 重建/timing 三轮合成世界/驱动与日志）与回传目录两件；全部留存审计，
无外部依赖方。回退=目录留存即可。
