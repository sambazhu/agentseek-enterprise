---
title: M3 R1 Linux 隔离安装核验结果（BLOCKED_DEPENDENCY）
type: how-to
audience: [A4]
runs: yes
verified_on: 2026-09-16
sources:
  - V0.1.3_M3_R1_OFFLINE_HANDOFF.md
---

# M3 R1 Linux 隔离安装核验结果（Linux CC，2026-09-16）

## 判定：**BLOCKED_DEPENDENCY**（唯一缺项 cffi==2.1.0；按工单在安装步停止，未联网/未降级/未借用生产 venv）

## 一、环境（Linux CC）

| 项 | 值 |
| --- | --- |
| OS/架构 | Ubuntu 22.04.5 LTS / x86_64 |
| Python | 3.13.14（uv 托管解释器 `cpython-3.13.14-linux-x86_64-gnu`，离线可用；未借用生产 venv） |
| uv | 0.11.29 (x86_64-unknown-linux-gnu) |

## 二、已完成步骤

| 步骤 | 结果 |
| --- | --- |
| 双端核验 | 分支 `enterprise/v0.1.3-execution-broker` 双远端 HEAD=5a170d1 一致；构建提交 07e098e 为其祖先 |
| 独立干净工作树 | `/root/agentseek-v013-m3-r1` detach 于 07e098e，`git status --porcelain` 为空 |
| inventory 暂存 | `m3_r1_inventory.py` 成功：entry_modules=4（m3_create/case/precreate_process、m3_receipt_probe）、runtime_modules=31、test_paths=35、files=70 |
| wheel 构建 | `uv build --offline --wheel`（pdm-backend==2.4.9 缓存在）成功：`agentseek_execution_r1-0.1.3a0-py3-none-any.whl`，SHA256=`f01c93f98a109a2c57be63a898662c7374b78c22d15f3a539b93437547ca5e39`（容器摘要随构建时间，源码完整性以固定 Git SHA 与安装检查为准） |
| 独立 venv | `uv venv --offline --python python3.13` 于 scratch 目录创建成功（/tmp/agentseek-r1-linux.NZVoi6） |

## 三、阻塞点（安装步）

`uv pip install --offline -r m3_r1_runtime_requirements.txt <wheel>` 解析失败：
**cffi==2.1.0 不在离线缓存**（cryptography==49.0.0 的运行依赖）。

逐包核验（`uv pip install --offline --dry-run`）：

| 包 | 状态 |
| --- | --- |
| **cffi==2.1.0** | **缺失（阻塞）** — 需 linux x86_64 wheel（cp313/abi3 manylinux 兼容） |
| anyio==4.15.1 / certifi==2026.7.22 / cryptography==49.0.0 / h11==0.16.0 / httpcore==1.0.9 / httpx==0.28.1 / idna==3.19 / pycparser==3.0 / typing_extensions==4.16.0 | 缓存在 |
| pytest==9.1.0 / cubesandbox==0.7.0 / pdm-backend==2.4.9（测试与构建侧） | 缓存在 |

缓存中无任何其他版本 cffi。按工单：停止，不联网安装、不降级版本、不去掉 --offline。

## 四、未达步骤（因阻塞）

- `python -I` 安装检查（31 模块来源/摘要、4 无效配置拒绝）——未执行
- 隔离源码复跑（预期 715 passed）与清单工具复跑（预期 5 passed）——未执行

补齐 cffi==2.1.0（linux x86_64）离线缓存后从安装步继续即可；wheel 与 scratch（/tmp/agentseek-r1-linux.NZVoi6，含 inventory/venv 现场保留）待复核，未清理。

## 五、边界遵守与现场事实

- 未接真实 CubeSandbox、未创建/终止 guest、未部署 supervisor、未重启网关；production（b226413）/标签/MCP/数据库/WORK 全未动
- 报告提交于执行分支（基 5a170d1），双端推送后核验
- 现场备注（非本工单范围，仅登记）：`/root/agentseek-v013-m2-broker` 工作树存在一个未推送本地提交 4d0caa9（2026-09-09 docs(m2) B8 措辞澄清，+7 行，不在任何远端分支）——未处理，待维护者裁定（推送或废弃）

## 六、结论

本轮记 **BLOCKED_DEPENDENCY**（cffi==2.1.0 linux x86_64 离线缓存缺失）。非 R1_OFFLINE_INSTALL_PASS，更非 M3_READY；其余前置（环境/工作树/inventory/wheel）均已就绪，补包后可续。
