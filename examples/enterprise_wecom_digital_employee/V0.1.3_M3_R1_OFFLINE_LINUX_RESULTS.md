---
title: M3 R1 Linux 隔离安装核验结果（补包后续测通过）
type: how-to
audience: [A4]
runs: yes
verified_on: 2026-09-16
sources:
  - V0.1.3_M3_R1_OFFLINE_HANDOFF.md
---

# M3 R1 Linux 隔离安装核验结果（Linux CC，2026-09-16；补包后完成）

## 判定：**R1_OFFLINE_INSTALL_PASS**（非 M3_READY；真实创建窗口另行安排）

首轮 BLOCKED_DEPENDENCY（见 git 历史 bc58ff3 版本）：唯一缺项 cffi==2.1.0。经用户批准单包补齐后续测完成。

## 一、环境（Linux CC）

| 项 | 值 |
| --- | --- |
| OS/架构 | Ubuntu 22.04.5 LTS / x86_64 |
| Python | 3.13.14（uv 托管解释器，独立 venv；未借用生产 venv） |
| uv | 0.11.29 (x86_64-unknown-linux-gnu) |
| 固定代码 | 07e098e（独立干净工作树 /root/agentseek-v013-m3-r1，porcelain 空） |
| scratch | /tmp/agentseek-r1-linux.NZVoi6（inventory/wheel/venv 首轮保留，续测前完整性核验通过：wheel SHA 逐字节一致） |

## 二、依赖补齐记录（用户批准的单包联网下载）

| 项 | 值 |
| --- | --- |
| 包 | cffi==2.1.0（唯一缺项；其余 12 项首轮已确认在离线缓存） |
| 来源 | 官方 PyPI（metadata: pypi.org/pypi/cffi/2.1.0/json；文件: files.pythonhosted.org） |
| 文件 | `cffi-2.1.0-cp313-cp313-manylinux2014_x86_64.manylinux_2_17_x86_64.whl` |
| 兼容标签 | cp313 / manylinux2014+manylinux_2_17 / x86_64（匹配现场 Python 3.13.14/Linux x86_64） |
| SHA256 | `799416bae98336e400981ff6e532d67d5c709cfb30afb79865a1315f94b0e224`（下载后与 PyPI 公布摘要**核对一致**） |
| 存放 | 独立目录 /root/m3-r1-deps（0700），经 `--find-links` 仅供本轮独立 venv/临时测试环境解析；未安装到生产、未动其他依赖、未用源码包编译、未替换版本 |

说明：`--find-links` 为本机补包必需的依赖来源参数（工单命令假设缓存齐全）；未去掉任何 `--offline`，未开放联网解析其他依赖，未修改门禁或用例。

## 三、核验结果

| 步骤 | 结果 |
| --- | --- |
| inventory | entry_modules=4（m3_create/case/precreate_process、m3_receipt_probe）、runtime_modules=31、test_paths=35、files=70 |
| wheel | `agentseek_execution_r1-0.1.3a0-py3-none-any.whl`，SHA256 `f01c93f98a109a2c57be63a898662c7374b78c22d15f3a539b93437547ca5e39`（容器摘要随构建时间；源码完整性以固定 Git SHA 与安装检查为准） |
| 独立 venv 安装 | `uv pip install --offline --find-links` 成功；实际安装 11 个发行版：agentseek-execution-r1==0.1.3a0、anyio==4.15.1、certifi==2026.7.22、**cffi==2.1.0**、cryptography==49.0.0、h11==0.16.0、httpcore==1.0.9、httpx==0.28.1、idna==3.19、pycparser==3.0、typing_extensions==4.16.0 |
| `python -I` 安装检查 | **installed_modules_verified=31、invalid_input_rejected=4、isolated=true**（输出仅检查结果与依赖版本） |
| 隔离源码测试 | **715 passed**（48.4s；模拟平台/loopback，supervisor 用例的临时子进程与端口 finally 收尾，跑后无残留进程） |
| 清单工具测试 | **5 passed**（1.7s） |

新缺项：无（安装与两个测试环境全部离线解析成功）。

## 四、M2 本地提交 4d0caa9 只读核对（工单三）

- 提交：4d0caa9（父 d73ef00=M2 收官提交；2026-09-09；docs(m2) B8 traffic-token probe 措辞澄清），仅 +7 行于 V0.1.3_M2_BROKER_VERIFICATION.md
- 内容：B8 勘误块——原标"错误 token"的两次探针实际用 X-Access-Token（非 CubeProxy 识别的 e2b/cube-traffic-access-token 别名），两次 403 应归类"缺失所识别凭据"而非"识别头中错误值"；原始证据与 M2 验收结论不变；错误值/另一别名/跨 guest 复用拒绝待另行授权补测
- **判断：尚有有效补充**——与远端分支当前版本文本逐行比较，差异恰为此 7 行（远端完全缺失该勘误，无部分覆盖）；插入点上下文两侧逐字一致，无冲突、无过时表述
- 处置：按工单只读，未推送/未 cherry-pick/未 reset/未删除；是否推送由维护者裁定

## 五、边界遵守

未接真实 CubeSandbox、未创建/终止 guest、未部署节点 supervisor、未重启网关；production（b226413）/标签/MCP/数据库/WORK 全未动；首轮与本轮 scratch/wheel/报告全部保留未清理；仅结束本轮测试自身的子进程（无宽泛 kill）。

## 六、结论

**R1_OFFLINE_INSTALL_PASS**：隔离安装链（inventory→wheel→独立 venv 离线安装→隔离解释器 31/4 检查→715/5 测试）全过。真实 Linux 创建子进程成功路径、独立 supervisor 联合生命周期、W0/G2 与真实平台矩阵仍待下一张受控工单。
