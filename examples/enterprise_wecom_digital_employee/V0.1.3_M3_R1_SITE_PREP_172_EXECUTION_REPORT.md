# M3 R1 阶段一节点准备——.172 执行报告（zcode，2026-09-17 UTC）

## 判定：**SITE_PREPARED_PENDING_APPROVAL**（安装与身份已核验；批准/动态绑定待阶段二生成）

上轮 .171 判定的 BLOCKED_TOPOLOGY 已解除：launcher 独立执行环境本轮已按批准的变更清单落入 .172，
与节点监督同机，满足本机身份读取合同。§4 禁用配置草稿因凭据（CA/密钥）尚未按授权交接而
**BLOCKED_PENDING_CREDENTIAL_HANDOFF**（子项，见"未执行项"）；不为凑齐草稿伪造任何摘要或绑定。
非 M3_READY；阶段二（真实创建/探针/终止）未批未执行。

执行方：.172 zcode（本轮 .172 安装/启动/配置独占执行；.171 仅备料与传输，未 SSH 写 .172）。
执行窗口：2026-09-17 00:5x–01:0x UTC。全程未联网解析（安装一律 `--offline --find-links`）。

## 一、到货复核与安装来源链条

| 环节 | 结果 |
| --- | --- |
| 交接包完整性 | `sha256sum -c MANIFEST.sha256` **3956/3956 OK，零失败** |
| 候选 wheel | SHA256 `c725dc677375354c287e15e0827947096ed26441437ceb9c931fb5192a80c061`（与工单一致；候选 Git SHA `aa9e1e5`，带外固定） |
| 闭包保真（本轮新增交叉核对） | 由 source-closure 重建 repo 布局后运行清单工具重建 inventory，与出厂 `R1_INVENTORY.json` **逐文件 sha256 完全一致**（72 文件；runtime_modules 32 项一致） |
| 解释器副本 | uv 托管 CPython 3.13.14 复制至 /opt 后对 MANIFEST 子集 **3815/3815 OK**；副本位运行 `--version`/ssl(OpenSSL 3.5.7)/sqlite3/zlib 正常，prefix 自解析=/opt 路径；未覆盖系统 Python（系统仍 3.10.12） |

## 二、安装与移主机重验（工单 §三.3 要求）

布局：`/opt/agentseek-m3-supervisor/`（0700）= 解释器副本 `python/cpython-3.13.14-linux-x86_64-gnu/` +
`venv/`（候选独立 runtime venv）+ `venv-verify/`（仅工具测试用 pytest 栈）+ `node_supervisor.py`（0600）。

| 检查 | 结果 |
| --- | --- |
| runtime venv | uv 离线安装 **11 发行版**（wheel+10 运行时钉版，与 .171 记录一致），全部来自 deps/ 本地 wheel |
| 安装检查（`python -I`） | **isolated=true，installed_modules_verified=32，invalid_input_rejected=5**（5 入口无效输入均按契约拒绝：exit 2 且无输出） |
| 清单工具测试 | **5 passed**（1.41s，Python 3.13.14 + pytest 9.1.0 离线栈） |
| launcher `--help` | 正常列出 `--closeout/--config/--sha256/--previous-config/--previous-sha256/--binding-file/--binding-sha256`；`--attach` 隐藏（内部子进程入口，符合契约） |
| 741 项测试 | **NOT RUN**（工单明示移主机不重复，以 32/5+清单工具为验收） |

## 三、拓扑核对（§2，逐项结论）

1. production SHA：**NOT RUN（本机）**——本轮 .172 无 Git 通道（.171 为唯一 Git 端），production 完整 SHA
   核实属 .171 侧职责，其上轮实测 b226413、笔误已纠正的结论继续有效；本轮 .172 未做任何 Git 操作。
2. 同机条件：**满足**——launcher 独立 venv 与 M3 监督服务现均在 .172；身份核验走本机 /proc 与本地私有文件。
3. 数据面：loopback 代理 0.0.0.0:13080（nginx）在监听；控制面本地 API 127.0.0.1:3000 可达。
4. 模板：API 实查唯一模板 `tpl-c4ba4bf8e2fb4668b0b7f22c`（alias `agentseek-m0-poc`，public=false，
   status=READY，镜像摘要 `743d264f…`）——与 AB_PREFLIGHT_RESULTS 记录一致。
5. guest 实查：登记前、启动后、终检三次 `GET /sandboxes` 均为 `[]`（guest=0）；新 run ID
   `agentseek-m3-r1-20260917` 经全盘检索未见于任何现有 guest/manifest/账本（M0 run=`run-20260907-m0-01`、
   M2 run=`agentseek-m2-20260909`，互不冲突）。

## 四、监督副本、私有目录与服务（§3）

路径清单（全部 root:root）：

| 路径 | 权限 | 内容 |
| --- | --- | --- |
| /opt/agentseek-m3-supervisor/ | 0700 | M3 专用安装树（本轮新建，原路径不存在、零覆盖） |
| /opt/agentseek-m3-supervisor/node_supervisor.py | 0600 | 原样监督脚本副本，SHA256 `b5d66db549f761ce09a046c3e64514eb5bd25989cf55da9eabfde96517b9cbca`（与交接包 MANIFEST 一致；脚本内冻结 120s 硬截止） |
| /opt/agentseek-m3-supervisor/venv/（及 venv-verify/） | 0700 | 候选独立 venv（固定解释器=副本位 python3.13） |
| /var/lib/agentseek-m3/runs/agentseek-m3-r1-20260917/ | 0700 | 本轮专用：supervisor/ vault/ fence/ quota/ tracking-A/ tracking-B/ config/ receipts/ 八目录（0700，互不混用） |
| …/supervisor/manifest.json（+.lock） | 0600 | 独立 manifest：run=`agentseek-m3-r1-20260917`、alias=`agentseek-m0-poc`、template=`tpl-c4ba4bf8e2fb4668b0b7f22c`、sandboxes={} |
| /etc/systemd/system/agentseek-m3-r1-supervisor.service | 0644 | 新建 unit（写前确认不存在）：UMask=0077、Type=simple、Restart=on-failure、MemoryMax=128M、CPUQuota=25%、固定解释器+脚本+独立 manifest/alarm/api-url(127.0.0.1:3000)；**无任何凭据入 unit/argv** |
| /etc/agentseek-m3/ | 0700 | unit 0600 存档副本 + supervisor-identity-pins.json（0600，见下） |

- manifest 登记时 guest=0（登记前即刻实查）；未重登记任何已有 run，未清空任何历史。
- 服务 **active (running)**，MainPID=2750111，**未 enable**（开机不自启）；与 M0/M2 unit 零共享零改动。

## 五、服务心跳与身份核验（§3.5–3.6）

- 心跳：`manifest.json.heartbeat`（0600）由服务每轮原子写入，`run_id=agentseek-m3-r1-20260917`、
  `pid=2750111`（=MainPID），抽查新鲜度 <1s；**无 alarm 文件**（连空文件都没有）；journal 无告警日志。
- 创建门禁（只读）：`--gate-check` → **GATE: ALLOWED**（exit 0）。
- 身份核验（用装好的候选模块自身合同执行 `verify_identity`，非人工比对）：**通过**。
  - IdentityPins（据实际进程生成，已存 /etc/agentseek-m3/supervisor-identity-pins.json，0600，供阶段二 precreate 引用）：
    executable=`/opt/agentseek-m3-supervisor/python/cpython-3.13.14-linux-x86_64-gnu/bin/python3.13`
    sha256=`b200d6c7…be9ff`；script=`/opt/agentseek-m3-supervisor/node_supervisor.py` sha256=`b5d66db5…cbca`；
    unit=`/etc/systemd/system/agentseek-m3-r1-supervisor.service` sha256=`0fa18123…e8b3`；
    cmdline_sha256=`50679354…6b08`。
  - verify_identity 实测：active/running、DropInPaths 空、ControlGroup=/system.slice/agentseek-m3-r1-supervisor.service、
    MainPID=心跳 pid=2750111、argv 含 script 且无 `--once/--gate-check/--register-*`、三文件 uid=0 无 022 位且摘要匹配、
    读窗前后 PID/start_ticks 稳定；**pid=2750111，start_ticks=77719270**，boot_id=`acbe1772-86e3-4261-b7d2-926dc729672d`。

## 六、未执行项（NOT RUN，如实；不写"无需"）

- **§4 A/B 禁用配置草稿与批准草稿：BLOCKED_PENDING_CREDENTIAL_HANDOFF**——CA/密钥未按授权交接
  （工单明示另行交接），创建链（precreate schema-2/3、launcher schema-1）所需的 api_key/ca/approval/
  create_token/endpoint 等"来源可核验的完整摘要"无法成立；按"只用真实安装路径与可核验摘要、保存不确定即停"
  不生成半真草稿，不伪造 B 的 previous_create/binding/intent，approved=false、w0_accepted=false 语义保持。
  已备齐的真实基线：目录骨架（八目录）、监督副本、IdentityPins、candidate_sha256（=wheel 摘要
  `c725dc67…c061`）、登记 run/template——凭据到位后可直接生成。
- vault 密钥未生成（属创建链材料，随阶段二受信安装步骤本地生成，不入 Git 不打印）。
- 阶段二全部动作未执行：无创建/终止 guest、无探针/矩阵发送、无 launcher 有效 `--config/--attach/--closeout`、
  未占用 intent/fence/quota、不触碰 traffic token。
- 741 项测试不重复（见 §二）；production SHA 本机未复核（见 §三.1）。
- 显式 NOT_RUN（探针矩阵覆盖缺口，沿上轮口径）：`cube-traffic-access-token` 别名变体的 E2/E3 等价行；
  envd 层 X-Access-Token 阳性行（SDK v0.7.0 回执从不交付 envd token）；绕过代理的本地直访/其他 UID 行；
  public C/D guest 全部行。M2 勘误继续有效：未识别 X-Access-Token 的 403 不是识别头错误值拒绝证据。

## 七、W0/G2 待确认事项与回退方案

- W0：责任人、所有已知创建入口、拟议时窗、禁创确认——待用户阶段二批准时逐项给出；不齐不得声称有效。
- G2：M2 勘误口径（X-Access-Token 403 不得作为识别头错误值拒绝证据）在下轮矩阵判定中继续生效。
- 回退（每项本轮新增对象）：M3 服务→确认本轮从未创建 guest（当前成立）后 `systemctl stop` 即可
  （未 enable，重启不自来）；私有目录与材料→保留供审计、不批量删除；/opt 安装树→停服后整树可移除，
  无外部依赖方；manifest→不重登记不清空，废弃即停服留档。M0/M2/production/网关/MCP/DB/WORK 本轮零接触，无需恢复。

## 八、下一轮逐行矩阵（精确版，待阶段二批准；沿 SITE_PREP_RESULTS §五）

端点（两 guest 同构）：`http://127.0.0.1:13080`，`Host: {port}-{sandboxID}.cube.app`；每请求实现上限
httpx timeout=2s（阻塞 I/O 可超截止至多一个 I/O 超时）。两 guest 均最多一次创建、串行；B 的 X1 计入 B 预算。

| 行 | 端点 | 头别名/凭据状态 | 预期 | 次数 | 最坏耗时 |
| --- | --- | --- | --- | --- | --- |
| A-E1 阳性对照 | 上 | `e2b-traffic-access-token`=A 正确 traffic token；不送 X-Access-Token | 200/正常 | 1 | 2s+超限余量 |
| A-E2 错误值 | 上 | `e2b-traffic-access-token`=错误值；省略 `cube-traffic-access-token` | 403 | 1 | 同上 |
| A-E3 缺失 | 上 | 两 traffic 别名均省略；不发 X-Access-Token 冒充 | 403 | 1 | 同上 |
| B-E1/E2/E3 | 上（B guest） | 同 A 口径（B 自己的凭据） | 同上 | 各 1 | 各 2s+ |
| X1 跨 guest | 上（B guest） | A 结束后用 A 本轮私有 traffic token | 403 | 1 | 2s+ |

总预算：A 探针 3 次 ≤6s+I/O 余量；B 探针 4 次（含 X1）≤8s+；外层创建 10s、attach 10s（内含登记 3s/心跳 3s）、
清理 1s、guest 硬截止 120s（不放宽）。每次 UNKNOWN 立即停止，不重试创建、不延时、不追加 C/D、不删栅栏。

## 九、边界遵守声明

未创建/终止任何 guest（全程 guest=0）；未动 M0/M2 服务与 unit（终检双双 active）、production/标签/网关/
MCP/DB/WORK；无凭据经手（包内无凭据，亦未另行收到）；无 Git 操作（.171 为唯一 Git 端）；回传目录仅本报告
与 SHA256 清单两件，不含凭据/完整环境变量/密封回执库/原始敏感响应；心跳与 lock 为动态运行态文件，不列入
回传清单（清单含报告+新装静态产物，见 RETURN_MANIFEST.sha256）。
