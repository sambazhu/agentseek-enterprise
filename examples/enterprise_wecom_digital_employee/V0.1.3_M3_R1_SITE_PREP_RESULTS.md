---
title: M3 R1 阶段一现场准备结果（BLOCKED_TOPOLOGY）
type: how-to
audience: [A4]
runs: yes
verified_on: 2026-09-16
sources:
  - V0.1.3_M3_R1_SITE_PREP_HANDOFF.md
  - V0.1.3_M3_R1_LAUNCHER_LINUX_RESULTS.md
---

# M3 R1 阶段一现场准备结果（Linux CC，2026-09-16）

## 判定：**BLOCKED_TOPOLOGY**（拓扑未满足，§3/§4 安装与配置准备未执行；非 SITE_PREPARED_PENDING_APPROVAL，非 M3_READY）

## 一、production SHA 核实与笔误处置

- 实测（只读）：本地 HEAD=`b226413b2e65feeaca2327e8a575a6b0c6d9b234`；GitHub/GitLab `refs/heads/production` ls-remote 同值
- **"b224 613" 为用户回执转发文本中的笔误，实际=b226413**；分支全部已提交文档均记录 b226413 无误（唯一 "b224" 出现处为本工单自身的引述）。处置：本报告更正记录，未切换任何代码

## 二、拓扑核对（§2.2-2.3）——不满足

| 事实 | 证据 |
| --- | --- |
| launcher 独立环境仅装在 **.171** | /tmp/agentseek-r1-launcher.sEmEZp/venv（R1_LAUNCHER_OFFLINE_PASS 环境） |
| 节点监督（M0/M2）与拟议 M3 独立监督均在 **.172** | M0 cube-poc-supervisor 与 M2 agentseek-m2-supervisor systemd 实况（前置报告 §二） |
| 身份合同强制同机 | `verify_identity` 读**本机** /proc（boot_id/start_ticks/exe/cmdline，m3_supervisor_identity.py:105-173）；`SupervisorReader` 读**本地**私有文件（manifest/heartbeat/lock）——代码无任何远程读取路径 |
| 结论 | launcher(.171) 与 supervisor(.172) 不同机 → **BLOCKED_TOPOLOGY**；按工单不搬监督、不桥接、不新增远程协议 |

## 三、在 .172 安装独立执行环境所需变更（待拓扑批准，未执行）

只读侦察 .172 现状：python3=3.10.12、**python3.13 缺**、uv 0.12.5 在、/opt 余 543G、/opt/agentseek-m3-supervisor、/var/lib/agentseek-m3、/etc/agentseek-m3 均不存在（净）、无 agentseek-m3 单元。

获批后需依次：
1. 供给 .172 Python 3.13（离线拷贝 .171 uv 托管 cpython-3.13.14 或 uv python install 后离线核验）
2. 受控传输固定候选产物：wheel `c725dc67…c061`（候选 aa9e1e5）、m3_r1_runtime_requirements.txt、install_check/inventory 工具、已批准 cffi==2.1.0 wheel（独立依赖目录）
3. .172 新建独立 venv 离线安装，**重验安装来源/32 模块摘要/5 无效配置拒绝**（工单 §1 要求，移主机不沿用 .171 结论）
4. 之后再进 §3（M3 监督副本+服务+私有目录）与 §4（禁用配置草稿）

## 四、未执行项（拓扑未满足，全部如实）

- §3 安装（M3 副本/服务/目录/manifest 登记/IdentityPins）：**NOT RUN**
- §4 配置草稿（A schema-2/B schema-3 草稿、approved=false、w0_accepted=false）：**NOT RUN**——将在拓扑批准并完成安装后生成；B 的 previous_create/binding 动态字段留待 A 真实回执，不伪造
- 回退：本轮零现场对象新建，无回退需求；M0/M2/production/网关/MCP/DB/WORK 全未动（guest 实查仍=0）

## 五、下一轮逐行矩阵（精确版，待阶段二批准）

端点（两个 guest 同构）：`http://127.0.0.1:13080`（沙箱节点 loopback 代理），`Host: {port}-{sandboxID}.cube.app`；每请求实现上限 **httpx timeout=2s**（m3_auth_probe.py:149；阻塞 I/O 可超截止至多一个 I/O 超时——实现注释 :140 如实转述）。

| 行 | 端点 | 头别名/凭据状态 | 预期 | 次数 | 最坏耗时 |
| --- | --- | --- | --- | --- | --- |
| A-E1 阳性对照 | 上 | `e2b-traffic-access-token`=**A 正确 traffic token**（被识别别名）；不送 X-Access-Token | 200/正常 | 1 | 2s+超限余量 |
| A-E2 错误值 | 上 | `e2b-traffic-access-token`=错误值；`cube-traffic-access-token` 省略 | 403（识别头错值证据） | 1 | 同上 |
| A-E3 缺失 | 上 | 两个 traffic 别名均省略；**不发送 X-Access-Token 冒充**（B8 勘误口径） | 403（缺失所识别凭据） | 1 | 同上 |
| B-E1/B-E2/B-E3 | 上（B guest） | 同 A 口径（B 自己的凭据） | 同上 | 各 1 | 各 2s+ |
| X1 跨 guest | 上（B guest） | **A 结束后**用 A 本轮私有 traffic token | 403（不借历史凭据/不称双活隔离） | 1 | 2s+ |

总预算：A 探针 3 次 ≤6s+I/O 余量；B 探针 4 次（含 X1，计入 B 预算）≤8s+；外层创建 10s、attach 10s（内含登记 3s/心跳 3s）、清理 1s、guest 硬截止 120s（不放宽）。

**显式 NOT_RUN**（不写"无需"）：`cube-traffic-access-token` 别名变体的 E2/E3 等价行（本轮矩阵仅用 e2b 别名）；envd 层 X-Access-Token 阳性（SDK v0.7.0 回执从不交付 envd token——M2 既有事实）；绕过代理的本地直访/其他 UID 行；public C/D guest 全部行。M2 勘误继续有效：X-Access-Token 的 403 不是识别头错误值拒绝证据。

## 六、结论

**BLOCKED_TOPOLOGY**——等待拓扑变更批准（launcher 环境落 .172 或维护者另定同机方案）；批准后按本报告 §三 完成 .172 安装并重验，再执行阶段一剩余（§3/§4），回传 SITE_PREPARED_PENDING_APPROVAL。阶段二仍需用户另行批准，本次未执行任何创建/探针/终止。
