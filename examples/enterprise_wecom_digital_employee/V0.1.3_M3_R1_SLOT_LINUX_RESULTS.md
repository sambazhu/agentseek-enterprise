# M3 R1 显式分阶段编排（slot+lifecycle+X1）——.172 独立离线复验与合同审阅结果（zcode，2026-09-17 UTC）

## 判定：**R1_SLOT_OFFLINE_PASS**（仅离线/合同结果；BUDGET_ADJICATION_PENDING 保留，非 M3_READY；未发现新增实现缺口，跨阶段仍为显式人工分阶段=设计口径，A/B/X1 真实全链 NOT RUN）

## 一、候选与到货

| 项 | 值 |
| --- | --- |
| 固定候选 Git SHA | be59e54c33ab3e5e3a91d74c32e4f55be3bf4773（基线报告 ca40e7e，.171 侧核对祖先链） |
| wheel | agentseek_execution_r1-0.1.3a0-py3-none-any.whl，SHA256 `c85c651768adc4a92b4dd33327a9297b53310d9a640b922968e852c31ed0e6da`——与工单一致（不符即停未触发） |
| 到货清单 | `sha256sum -c MANIFEST.sha256` **146/146 OK** |
| 闭包 | **7 入口 / 34 运行模块 / 77 文件**（新增 m3_slot、m3_lifecycle 等；tools/ 为本候选更新版，未回找旧轮） |
| 解释器/依赖 | 复用 /opt cpython-3.13.14；仅包内 deps/ 19 wheel，`--offline --find-links`，全程未联网、零 Git |

## 二、环境与复验结果（HANDOFF §3）

scratch=/root/m3-r1-slot-work-20260917（留存审计）：venv-runtime（wheel+10 钉版依赖=11 发行版，
无 pytest）与 venv-test（pytest+运行依赖，**一开始即含 cryptography 等**——上轮 27 errors 教训已吸收，
本轮全量一次通过）分离；测试经 conftest 从闭包 src 导入，未用 /opt 或生产 venv 补模块。

| 步骤 | 结果 |
| --- | --- |
| install_check（venv-runtime `python -I`） | **isolated=true，installed_modules_verified=34，invalid_input_rejected=7**（未用真实配置补测成功路径） |
| 闭包完整测试 | **784 passed，46.69s**（解释器 Python 3.13.14/.172 Linux；与 .171 macOS 3.12 记录互证） |
| 清单工具 | **5 passed**（1.64s） |
| m3_slot `--help` | `--config CONFIG --sha256 SHA256`（无其他参数；无效输入 exit 2 契约保持） |
| m3_lifecycle `--help` | `--config CONFIG --sha256 SHA256 --action {create,closeout}` |
| 临时子进程收尾 | 测试后 ps 全查**零残留**；真实监督未动（M0/M2/M3 三进程原样） |

## 三、显式调用合同审阅（HANDOFF §4，逐条文件:行依据）

**① 显式创建保存（lifecycle create）——落实。**
m3_lifecycle.py:58 以 O_EXCL 独占写 create-intent.json（m3_slot._save :29-40 同机制）→ :60/:62 调既有
launch/launch_successor → :64 途中复核配置未变 → :65 独占写 create-result.json。任何失败 exit 2，
intent/result 的 O_EXCL 使重跑必然失败：**UNKNOWN 不重跑**（测试 test_uncertain_create_never_retries）。

**② 受信安装方独立批准配置——按设计无自动生成器。**
lifecycle docstring :1-5 "never generates approvals or loops"；slot docstring :3 "Receipt provisioning
and approval remain separate trusted installation steps"；slot.run :56-60 注释明示 reserve 的运营批准
与测量是前置条件非推断。准备时间消耗 guest 寿命：可信截止来自首行实时证据（见③），配置准备发生
在其前，不暂停不重算。

**③ 整组执行（m3_slot）——落实。**
- 可信截止构造：:108-110 首行经 collect_gate_isolated 取实时 Evidence（m3_evidence_process.py:119-133，
  guest_deadline=lifetime.stop_mono :306，源自密封寿命/平台 startedAt）→ deadline 单值贯穿；
  **非每行 now+120**（测试 test_slot_calls_real_entry_interface_with_one_cutoff_and_no_b）。
- 全槽预算：:111 `deadline-now ≥ 行数×16+reserve`（A=5×16+余量=80+，B=6×16=96+）；
  reserve :67-68 显式、无默认（0<reserve<120；测试 test_reserve_is_explicit_not_defaulted）。
- 逐行：:113 复核配置未变 → :115 剩余预算 `(剩余行数)×16+reserve` → :116-118 调既有 15s 单行入口
  dispatch_isolated（deadline-reserve 传入，沿用上轮已验合同）。
- 异常停：:106 整组 intent 先独占持久化（注释 :104-105 "Burns the whole slot before any live call"），
  :120 每行判定失败即异常终止，无恢复路径（测试 test_slot_stops_without_resume）。
- 判定标准：:43-52 阳性=status 200 且协议信号 kind 对应（E1 command_success/E2 file_payload/
  E3 stat_payload）；负向=403+http_denial_signal 且 activity=false。不扩展为服务端无副作用证明。
- 成功保存 slot-result（next_create_authorized=false，:121-123）。

**④ 结束检查（lifecycle closeout）——落实。** :37-43 读已存 create-result（config_sha256 必须匹配）
→ 调既有 check_closed 一次有界检查 → 独占存 closeout-result.json。无轮询、无 kill、不启动 B
（测试 test_create_saved_then_explicit_closeout_no_automatic_b）。

**⑤ 显式 B——落实。** lifecycle :45-57 B 的 previous 六字段精确集合 {launcher, launcher_sha256,
create_result, create_result_sha256, slot_result, slot_result_sha256}；逐项钉摘要读 A 的 create-result
（registered_and_observed=true）与 slot-result（slot=A、rows_observed=5、next_create_authorized=false、
create==A binding）后才 :62-63 调 launch_successor（再查 A 当前结束事实+B 独立创建批准）。
**A schema 2 / B schema 3 强制**：:33-34 `installation["schema"]==(2 if slot=="A" else 3)`。
slot 侧 B 的 previous_slot{path,sha256} :93-99 同样要求 A 五行成功记录且 create==donor。
（测试 test_b_requires_a_success_and_calls_successor_not_plain_launch、
test_b_six_rows_require_matching_a_and_stop_on_x1_failure）

**⑥ X1——落实。** m3_slot.py:25-26/:69 X1 恰为 B 末行（matrix=("E1","cross_guest")），:82 E1+e2b 别名；
m3_receipt_probe.py:78 donor 三字段精确集合 {create, successor_file, successor_sha256}；:82-84 同
run/boot/template/candidate/domain 且 create_token 不同；:88-102 B 安装 schema3、previous_create、
plan binding、approval、batch_sequence[0]=A/[1]=B 全验；:103-107 当前 B fence verify_pending +
归档 create.closed-* fence 原文核对；:108-112 donor 凭据取自同 vault 密封回执（不同 sandbox_id/
token，不收调用方 token、无独立 donor vault）；:135-144 probe schema=4 仅 cross_guest、
donor_ref=canonical(donor) 的 SHA256；实时缺席事实由 schema-4 collector 建立：
m3_evidence_process.py:236 collect_created 强制**完整平台列表恰为 B**（:237-240 注释明示"非原子
快照、不声称逐 guest 终止"）；**旧 standalone worker 拒绝 X1**：:178/:207 `require(source.donor is None)`
（X1 必须走完整 case/evidence gate，不得绕过）。错值凭据为新鲜随机（:152，测试
test_random_collision_never_sends_correct_token_as_wrong）。

**配置字段集总核**：lifecycle {schema,slot,launcher,launcher_sha256,directory}(+B previous) ✓
（m3_lifecycle.py:24-25/:47-49）；slot {schema,slot,reserve_seconds,directory,rows}(+B previous_slot)、
每行 {installation,sha256,approval_sha256}、A 序 E1正/E2正/E2错/E3正/E3缺 ✓（m3_slot.py:25-26/:63-75）；
X1 donor/probe 如上 ✓；路径一律 `_path()` 规范绝对（is_absolute+resolve==path）；私有文件 0600/父
0700 由 config_bytes（m3_probe_process.py:40-49）与 DispatchDirectory(O_NOFOLLOW, intent O_EXCL
burns case)强制。

## 四、实现缺口与验证边界（如实，不拼称全链）

- **本轮合同审阅未发现实现缺口：无新增 BLOCKED_IMPLEMENTATION。** 上轮四项编排缺口中：可信截止
  构造/全槽预算核验/逐行异常停/整组 intent 均已由 m3_slot+collect_gate_isolated 落实（见③）；
  "A 回执保存期寿命核算"由"单截止贯穿+配置准备耗寿不重算"的显式分阶段设计覆盖（lifecycle 先存
  回执，slot 截止取自其后首行实时证据）。
- 保留边界一：m3_slot docstring :5 自述 "not yet the create-to-closeout installation orchestrator"
  ——跨阶段总编排（create→配置准备→slot→closeout→B→X1）仍为受信安装方显式逐段调用，无自动流水线
  （与本工单"显式分阶段"口径一致，非缺口）。
- 保留边界二：**分层测试不拼称全链**——slot/lifecycle 测试替换平台采集与部分下游（测试名可证），
  X1 测试用真实私有回执+归档文件但非真实平台；A/B/X1 真实全链、真实探针、动态配置准备端到端时序
  均 **NOT RUN**（阶段二未批，本轮零创建零探针零平台调用）。
- 保留边界三：**BUDGET_ADJUDICATION_PENDING 维持**——reserve_seconds 数值与测量依据、动态配置
  准备耗时、新的现场批准材料仍待审；OS 调度/文件 I/O 非硬实时；监督 120s 冻结值不变。

## 五、现场零变更与回退

本轮新增对象仅 scratch（/root/m3-r1-slot-work-20260917）与回传目录两件；/opt 现用安装仍为 aa9e1e5
（已验 a12bf86 与本轮 be59e54 均未部署）、监督服务/身份 pins/禁用草稿原样未动；零物化凭据、零改
批准文件、零切换重启服务；控制面零请求；零 Git。回退=目录留存待审，无外部依赖方。
