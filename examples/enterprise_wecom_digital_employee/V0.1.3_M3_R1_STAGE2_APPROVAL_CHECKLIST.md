# M3 R1 阶段二审批清单（脱敏，准备稿）— zcode 整理，2026-09-17 UTC

用途：供用户逐项审批的清单。四类分层：**已知事实**（可复核）/ **建议**（未生效待裁定）/ **待用户批准**（不批不执行）/ **动态待填**（只能来自真实事实，禁合成冒充）。**禁代填确认**：所有"确认/同意"字段仅用户或其明确委托人可填写；未获确认一律保持"待定"，不写 true，不推断"无人回应=同意"。

## 一、已知事实（可复核）

| 项 | 内容 |
| --- | --- |
| F1 候选与入口 | 本轮已安装 5d3c27bf1d5d15e0ad09f0ff1fcd6e3e8a70cfdc（wheel SHA256 188a0d2cdcd9705befe916a9b850e8791b81471ca88d455a8c97d9b94df9fceb）于 /opt/agentseek-m3-releases/5d3c27b/venv，重验 35 模块/8 拒绝/isolated=true，三入口 --help 契约一致；**后续显式调用入口已登记为 /opt/agentseek-m3-releases/5d3c27b/venv/bin/python**（规范绝对路径；虽符号链接解析到共享 Python，禁止改用共享 Python 路径直接启动——会丢失 venv 安装上下文）。be59e54 安装与其 INSTALL-RECORD 原样保留（回退目标）；aa9e1e5 常驻监督未动（MainPID 2750111）。production/标签不涉及 |
| F2 run/template | 平台已登记 run_id `agentseek-m3-r1-20260917`、template_id `tpl-c4ba4bf8e2fb4668b0b7f22c`（alias agentseek-m0-poc，public=false）。不新建 run、不重置额度/栅栏；**当前有效性（模板 READY/guest=0/心跳）在真实执行窗口实时另验**（最后实测 2026-09-17 补充轮，平台状态会漂移） |
| F3 执行链版本 | lifecycle/slot/materialize 合同已离线审阅落实（R1_SLOT_OFFLINE_PASS / R1_MATERIALIZE_OFFLINE_PASS）；生成器实测 A≤0.168s / B≤0.191s（合成） |
| F4 凭据现状 | CA 受控交接已登记（/root/m3-r1-creds-20260917/ca.pem）；API key 受控源已登记（/root/cube-one-click-v0.7.0/poc-install.env 的 CUBE_API_KEY）。**物化（key-only 文件落 config/api_key）待批**；vault receipt.key 已本地生成（本轮 run 专用，不复用） |
| F5 顺序纪律 | A 的用户批准→预批审阅与摘要固定→A 创建（这三步全部在 A 创建前完成）；A 结束（closeout 事实）后才准备 B 动态后继材料，B 创建前完成 B 批准与预批固定。创建后的机械生成（materialize）无人工新增授权 |
| F6 失败纪律 | UNKNOWN/身份或审批异常/阳性对照失败即停后续行与后继槽；保留 intent/fence/quota 全部记录；不恢复执行、不手工抽取部分生成审批拼槽、不删记录重跑、不自动换目录规避 |
| F7 验证边界 | 离线通过≠现场通过；生成器合成计时≠真实全链预算证明；M3_READY 尚未达成；BUDGET_ADJUDICATION_PENDING 维持 |

## 二、建议（未生效，待裁定）

| 项 | 内容 |
| --- | --- |
| S1 reserve_seconds=5s | **建议值而非生效值**（本轮安装不使其生效）。依据：交付生成器合成实测最大 0.191s×~25 倍裕量；非统计上界、非硬实时。若批准：A 首行检查=剩余可信寿命 ≥80+5=**85s**；B ≥96+5=**101s** |
| S2 真实未测段 | 真平台 create（外层 10s 预算）/attach（10s）/首轮证据采集（≤10s）均未实测；用户批准/消息往返未计入。**预算不足时整槽记 NOT RUN**（m3_slot 合同行为），不缩矩阵、不自动重试、不创建替代 guest |

## 三、待用户批准（不批不执行；批准后逐项钉摘要）

| # | 项 | 需要用户给出的内容 |
| --- | --- | --- |
| A1 W0 责任人 | window 记录的 creator_id（责任人身份标识） |
| A2 W0 创建入口清单 | 所有已知创建入口及全部 writer 的 ID 清单（inventory 以此定摘要） |
| A3 逐人禁创确认 | 每个 writer 的 responsible/confirmed_epoch/**abstain=true**——**必须逐人实际确认**；未确认者保持"待定"并阻断 W0（read_window 合同要求全员 abstain=true） |
| A4 拟议窗口 | starts_epoch/ends_epoch（**明确时区**；建议以 UTC 表述，机器时钟为 UTC；end−批准时刻 ≥11s 且窗口不延长 guest 120s） |
| A5 A 槽创建批准 | approved=true 的 A approval（schema1，max_creates=1）+ 对应 materialize 预批（approved=true、materialize_exact_rows=true）的逐字段审阅；批准后带外钉 SHA256 |
| A6 A 逐行委托 | 5 个 case_ids + 5 个 approval_refs（唯一）；行矩阵固定 E1正确/E2正确/E2错值/E3正确/E3缺失（不可自定义） |
| A7 B 槽创建批准 | 同 A5 口径（schema-3 创建安装 + B approval + B materialize 预批）；**B 材料在 A 结束后才允许准备** |
| A8 B 逐行委托 | 6 个 case_ids + 6 个 approval_refs；含 X1 行（E1/e2b 别名、A 本轮凭据、donor 绑定） |
| A9 预算裁定 | 对 S1 的 5s 或另定值明确批准；未批准前阶段二矩阵不得执行 |
| A10 api_key 物化 | 授权从受控源物化 key-only 文件至 /var/lib/agentseek-m3/runs/agentseek-m3-r1-20260917/config/api_key（0600）并钉摘要 |
| A11 执行窗口与顺序放行 | 阶段二总放行：主机/窗口/独立监督终态/A 与 B 创建额度各一次/终止权限/停止预算 |

## 四、动态待填（只能来自真实事实；禁合成冒充、禁预造）

| # | 字段 | 来源 |
| --- | --- | --- |
| D1 | A 密封回执（binding 含实际 intent_sha256、sandbox ID、traffic token——token 不入清单） | A lifecycle create 真实成功输出经 vault 密封 |
| D2 | B 预批 expected_create / previous_create / previous_slot / donor 三字段与各摘要 | D1 与 A slot-result/closeout 真实记录 |
| D3 | B 密封回执 | B lifecycle create 真实成功输出 |
| D4 | 各 installation/probe 的最终路径与摘要 | materialize 真实运行产物（materialize-result.json 供核验） |
| D5 | W0 各 writer 的 confirmed_epoch | 逐人实际确认时刻（用户侧事实） |

## 五、批准流程提示（对用户）

按 A1→A4（W0）→A9（预算）→A10（物化）→A5/A6（A 槽）放行 A 侧；A 真实执行结束且 D1/D2 齐备后，再批 A7/A8 放行 B 侧；A11 为总闸。任何一项未批，对应动作保持 NOT RUN；UNKNOWN 即停。
