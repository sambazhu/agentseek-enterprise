---
title: M3 R1 替代窗口 .171 有界只读盘点（脱敏归档版）
type: how-to
audience: [A4]
runs: yes
verified_on: 2026-09-17
sources:
  - V0.1.3_M3_R1_STAGE2_A_CONFIRM_REVIEW.md
---

# .171 有界只读盘点（Linux CC，2026-09-17 08:33:20 UTC；替代窗口 09:00–11:00 UTC 开始前）

范围：已知 CubeSandbox/M2/M3 目录与单元；未全盘扫描、未读进程内存/shell history、未导出凭据正文或摘要。脱敏摘要已于归档前先行提交用户转交 .172。

## 一、凭据副本（路径/权限/用途）

| 副本 | 位置（权限） | 用途 |
| --- | --- | --- |
| cube.key（已披露） | /etc/agentseek-m2-broker/（0700/0600） | 控制面 API key 副本（M2 遗留） |
| x_api_key（**本轮新增披露**） | /root/cube-poc-client/pki/（0600） | 第二份 API key 副本（M0 建链遗留；未与受控源比对摘要，值关系未验证） |
| server.key / server.pem | /root/cube-poc-client/pki/（0600） | .172:13000 服务器密钥对（M0 遗留） |
| client-a/b.key、encryption.key、gate.key、ca.pem、known_hosts、ssh_config | /etc/agentseek-m2-broker/（0600） | M2 principals/账本/m2gate/链路 |
| /root/.ssh172p（0600，13B） | /root | .172 root SSH 口令载体 |
| ~/.ssh/ github/gitlab/weknora 三对密钥（0600） | /root/.ssh | Git 用途，非 CubeSandbox 相关（范围外已知） |

m3-r1 凭据交接目录仅存在于 .172（.171 无副本）；/root/m3-r1-deps 仅含公开 wheel。

## 二、创建入口（capability 与 auto 严格区分）

- **自动任务：零**（root crontab 空；无 cube/m2/broker 相关 systemd timer）——限已检查范围，不推定全局不存在其他创建入口
- **活动服务**：agentseek-m2-broker.service（active running）——具备经 TLS 隧道触发沙箱生命周期的能力；入口=本机 UDS broker.sock（0600，仅本机 root）；无自动创建行为（被动等请求）
- **常驻进程**：tunnel_forwarder.py pid 2584104（loopback:13080→.172:13080 纯转发，不创建；无 systemd 单元=手工常驻）
- **创建能力代码（非自动）**：/root/cube-poc-client/（M0 客户端脚本 o1/o2/o4a…+cubesandbox 0.7.0 SDK venv）、/root/agentseek-v013-m2-broker、/root/agentseek-v013-cubesandbox …/sandbox_poc、生产仓 examples/…/sandbox_poc、M3 worktree 的 agentseek-execution 创建入口代码（离线验证用）

## 三、.171→.172 特权通道与持有主体

通道三条：root SSH（22589，口令载体 /root/.ssh172p）；控制面 HTTPS 直连（.172:13000，iptables 放行 .171 源，配 cube.key/x_api_key 副本）；数据面隧道（loopback:13080）。
持有主体：仅 .171 本机操作者（.171 Linux CC 本 agent）与经其操作的用户 zhuchunlin；无其他人员持有上述凭据——**据实声明，非机器扫描证明**。

## 四、cc-171 禁创确认覆盖确认

原声明（2026-09-17 08:13:05 UTC）载明"适用于用户指定的下一个窗口"。本替代窗口（2026-09-17 09:00–11:00 UTC）即该窗口，**覆盖生效**；范围含上述全部通道与创建能力代码，并保持对 .172 仅协议内只读操作。**未发现新增主体；x_api_key 属已知通道补充披露，非声明冲突**。

## 五、未验证限制

未全盘扫描文件系统；未读取进程内存与 shell history；两份 API key 副本的值关系未验证（按纪律不比对摘要）；git 密钥未深查；"无其他持有者"为责任人声明而非技术证明。

## 六、边界遵守

本工单只盘点 .171 本机；未 SSH 写 .172、未改任何服务/凭据/权限/网络/配置；未读取 .172 私密审批正文；未创建 guest、未执行探针、未动 production/标签/网关/MCP/DB/WORK。本盘点不产生 A/B 执行授权。
