# 方案 D 前置轮——端口 80 只读核对与 business-session 修订（zcode，2026-09-21 14:43 UTC）

## 判定：**端口 80 核对通过（预期 HTTP 数据面确认）+ W5 修订件就绪（仅改 proxy_port，程序化核验一致）；零 guest/零 perform/零直驱**

## 一、127.0.0.1:80 只读核对

| 项 | 结果 | 证据 |
| --- | --- | --- |
| 监听归属 | **cube-proxy Docker（host 网络模式）** | `docker inspect` NetworkMode=host；`ss -tlnp` :80 nginx 进程 |
| 协议 | **HTTP（非 TLS）** | nginx -T 无 ssl 指令；注释明确"HTTP server block (port 80) intentionally proxies to the backend" |
| Host 路由 | **49983-{id}.cube.app 格式有效** | 测试假 ID 得 404（=路由解析已执行，sandbox 不存在） |
| 认证转发 | **Lua 层处理** | admin_phase.lua / backend_cache.lua / balancer_phase.lua 等在 OpenResty 内 |
| 非 sandbox Host | 400（localhost 无路由匹配） | 预期行为 |
| TLS 入口 13080 不受影响 | ✓（本轮未改动 13080） | 上一轮已加 allow 127.0.0.1 |

**结论：127.0.0.1:80 是预期的 cube-proxy HTTP 数据面——与 business_cube_session 的 `http://` 协议匹配。**

## 二、business-session W5 修订件

| 项 | 值 |
| --- | --- |
| 新文件 | `config/business-session-W5.json`（0600） |
| **唯一差异** | `control.proxy_port: 13080 → **80**` |
| 不变项 | endpoint / api_key / ca_file / domain / supervisor_identity / vault_directory / vault_key_file / expires_epoch 等**全部保留** |
| 旧件保留 | `config/business-session-W4.json` 原样未动 |
| SHA256 | `29092da7da35550a…`（见清单全值） |

## 三、上游引用重钉

| 件 | 变更 | 摘要 |
| --- | --- | --- |
| service-config-W5-FINAL.json | **新建**：plans[0].business_file→W5.json + business_sha256 重钉 | 见清单 |
| launcher / lifecycle | **不变**（不直接引用 business-session 摘要；仅 service-config 引用） | — |
| broker unit | 已更新指向 W5-FINAL + 新摘要 | active ✓ |

## 四、程序化核验

| 项 | 结果 |
| --- | --- |
| W5 与 W4 仅 proxy_port 差异 | ✓（diff 程序化确认） |
| service.business_sha256 == sha(W5.json) | ✓ |
| service.lifecycle_sha256 不变 | ✓ |
| broker active + 监听 13100 | ✓ |
| HTTP→:80 协议验证 | ✓（404 = 路由层正常处理，协议匹配） |
| read_plan 代码校验 | **DENIED（expires_epoch 已过）**——预期（W4 窗口 10:13 UTC 已结束）；下一窗口材料生成时 expires_epoch 将更新 |

## 五、恢复方案

```bash
# 恢复 broker 到 W4 配置
sed -i 's|service-config-W5-FINAL.json|service-config-W4-FINAL.json|' /etc/systemd/system/agentseek-business-broker.service
SVC_SHA=$(sha256sum /var/lib/agentseek-m3-business/runs/agentseek-m3-biz-20260921/config/service-config-W4-FINAL.json | awk '{print $1}')
sed -i "s|--sha256 [a-f0-9]\{64\}|--sha256 $SVC_SHA|" /etc/systemd/system/agentseek-business-broker.service
systemctl daemon-reload && systemctl restart agentseek-business-broker.service
# business-session-W5.json 可删除或保留（不影响 W4 配置）
```

## 六、不变项声明

控制面 HTTPS/CA/broker 13100/身份与生命周期规则不变；旧额度/栅栏/意图/回执全保留（未清理）。/health 端点存在性仍为 **PENDING**（本轮零 guest，404 不代表端点不存在——路由层正常返回）。

## 七、边界

零 guest/零链外 perform/零人工直驱/零额度重置/零状态清理/零 Git。仅创建 W5 修订件 + 更新 broker unit 指向。
