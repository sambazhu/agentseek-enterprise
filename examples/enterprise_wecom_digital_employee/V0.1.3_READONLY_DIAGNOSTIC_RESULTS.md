# 实际网关进程内只读诊断结果（用户批准范围执行，2026-09-21 23:01–23:05 UTC）

## 判定：诊断完成——**第 11 号缺陷未复现：网关进程内交换完整成功（7ms 全链 HTTP 200）**

## 一、执行记录（严格按批准范围）
- 诊断前快照：镜像 4 attempts（605e162f=reconciling）+ 摘要 1a9a3088 存档；未清理未重置
- 网关以 `build_diagnostic_spec` 受控加载（14edfa5 树；spec entrypoint 日志实证）；**run_sandbox_task 未挂载**（14edfa5 接线级强制：diagnostic_only→[get_sandbox_task_result]）
- 用户发送一次查询消息（不重传不发执行问题）；模型 1 次工具调用（get_sandbox_task_result）
- .172 零动作；诊断后生产网关恢复（23:05:00）

## 二、脱敏交换阶段日志（business_exchange 14 条，exchange_id=8e889c2c7ce88cc0，attempt=605e162f…，pid 3472718/thread 3472765）
```
prepare started → tcp trace(1ms) → tls complete(4ms) → send_headers(5ms) → send_body(5ms)
→ response_headers(6ms) → response_body(200, 6-7ms) → stage=complete status=succeeded(7ms 总计)
```
**传输层全阶段零异常；HTTP 200。**

## 三、结果解读（如实）
- 工具对模型返回 unavailable_or_rejected：**非传输失败**——节点对 605e162f 返回 not_found（W6 execute 未达节点=无记录）→ 恢复路径按合同不更新镜像（镜像前后摘要一致 1a9a3088，605e162f 仍 reconciling——not_found 未关闭意图未释放额度 ✅）
- **第 11 号缺陷（W6 的 TCP 后 TLS 前弃用）在诊断中未复现**——与 Codex 离线结论一致（本地无法复现）；W6 成因仍无定论（一次性条件？bf4602f 重构后时序变化？）
- **若 W7 执行中复发，现在有 14 行级 trace 实时捕获**（诊断机制已在本轮实证工作）

## 四、统计（按用户要求）
- 实际工具调用次数：**1**（get_sandbox_task_result；零 run_sandbox_task——未挂载）
- 传输阶段：tcp/tls/send_headers/send_body/response_headers/response_body 全 complete
- 异常类别：**传输层无异常**；应用层 not_found（预期合同行为）
- 前后状态：镜像零变化（摘要一致）；节点零接触；guest=0
