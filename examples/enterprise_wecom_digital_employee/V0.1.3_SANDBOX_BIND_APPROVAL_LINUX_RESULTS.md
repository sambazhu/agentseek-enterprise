# 单地址批准补丁业务包——.172 离线复验与 schema 2 草案（zcode，2026-09-20）

## 判定：**BUSINESS_BIND_OFFLINE_PASS**（仅离线/合成配置 PASS；≠现场最终配置 PASS——凭据引用/窗口/permit-plan/摘要仍待批准包补齐核验；非业务全链 PASS、非 M3_READY；更新的是批准包拟部署版本，非实际安装切换）

## 一、到货核验

- BIND-MANIFEST **170/170 payload OK**（工单表述"171 件"与清单行数 170 的差异如实记录：以清单实测为准）；新 wheel `agentseek_execution_business-0.1.3a0` SHA256 `3d17d87245550fb813d0f93d8be633e07286e9c34c35770deda40bc597382ce2` 与工单一致；旧 wheel bceeb6e3 原样保留。README 中"192.10.50.50.172"笔误按其指引以 §配置变化 2 准确值 192.10.50.172 执行。

## 二、隔离安装与检查（新 scratch=/root/m3-bind-work-20260920，新 venv=venv-bind）

- 新业务 venv 独立（与 R1 各 venv 及旧业务 venv-business 零混装；旧安装零覆盖零切换；未动 /opt）；解释器复用原始交接包 python3；`--offline --no-index --find-links` 仅交付依赖，17 发行版（cffi==2.1.0/pytest==9.1.0/agentseek-execution-business==0.1.3a0 断言在册）。
- install_check --profile business：**isolated=true、44 模块摘要、2 入口拒绝、exit 0**。

## 三、闭包测试

- 新闭包隔离 pytest（`-o pythonpath=` 方式，未用 -I+PYTHONPATH）：**853 passed，13.83s**（较上轮 835 +18，定向 test_business_service 23 已含不再单跑）。
- 业务清单测试（环境变量钉根）：**1 passed（0.38s）**；测试后零残留子进程。

## 四、schema 2 禁用修订草案（重钉）

文件：/root/m3-bind-work-20260920/drafts/business-service-schema2-DISABLED-draft.json（0600），
SHA256 **a353954a8c0321aba520ec98db13133100ca571ec3fe31aa8821da25d8177b77**。
- **字段差异**（vs 历史 schema 1 草案）：schema 1→2；新增第 12 字段 `approved_bind_host`；
  `bind_host=approved_bind_host="192.10.50.172"`（双地址同值规范 IPv4，new code :33-34 核对）；
  其余 11 字段与 schema 1 草案语义一致。
- 8/8 值级检查 PASS：12 字段精确集、双地址同值规范 IPv4、拒绝类全过（unspecified/multicast/
  reserved/link-local/0-8 均否）、bind_port 合同区间、路径规范绝对、permits_sha256=null PENDING；
  **双重禁用**（approved=false + plans=[] 空集载入必拒）。
- schema 2 语义（新代码 :24-34 实证）：单地址例外受整个服务配置摘要及 approved=true 约束，
  配置文件不是独立身份认证；TLS/broker token/来源规则/用户批准仍须保留；未单独批准的新地址
  不自动继承放行。
- **PENDING（按批准包实际进度标注，不伪造）**：permits_file/permits_sha256、plans 全部引用
  （待 §1.6 网关绑定）、证书/私钥物化（SAN=IP:192.10.50.172）、broker token、W0/窗口、
  一次创建额度、网络三条规则、服务最终件 approved=true（另建重钉）与启用窗口。

## 五、历史结论与继续有效项

- 历史 schema 1 的"精确 11 字段"结论保留不改写（本轮 schema 2 的 12 字段为新候选新合同；
  上轮 BLOCKED_DESIGN 由本候选方案 a 解除——批准包拟部署版本更新为 3009d82，
  **实际安装未切换**，/opt 各版本原样）。
- /root/m3-final-window-handoff-20260920（§1.2/§1.5）与 /root/m3-binding-handoff-20260920 两轮
  任务继续有效（不冲突范围）：13100 规则/CA/token/bizroot/新 run 监督/生命周期方案维持，
  其中 bind 相关项以本轮 schema 2 草案为拟部署基准。

## 六、边界

未启服务、未创建 guest、未切 /opt、零 Git；旧 R1/业务安装零覆盖。回传仅两件脱敏件。
