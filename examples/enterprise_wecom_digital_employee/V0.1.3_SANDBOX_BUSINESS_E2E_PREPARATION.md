# CSV 业务 E2E 节点方案准备——脱敏报告（zcode，2026-09-19）

## 判定：**BUSINESS_E2E_PREPARATION**（仅方案与材料准备；草案全部禁用；未启动服务/未建 guest/未改网络与任何现场状态；服务启用、网络变更与真实执行由最终工单统一提交用户批准）

## 〇、输入缺口（先列）

1. **GATEWAY_FACTS 脱敏件未收到**（abfd2e9 归档，应由用户/.171 提供）——影响 §二 的"文件进入提问当轮 current_files 方式"与"用户打开输出文件入口"两项核对：**列为缺口 G1/G2**，未由 .172 手工注入状态补过；
2. SITE_PREP_HANDOFF 文档本体（622d68e）未随包到 .172——本报告按用户转述工单指令执行，文档细节如有出入以补交后核对为准（缺口 G3，低风险）；
3. 固定任务三件**文件本体**未交接（摘要已由 Codex 复核，见 §二）——待 .171 受控交接（缺口 G4）。

## 一、两机网络与凭据方案（§一）

**链路核对（只读实测 2026-09-19）**：13100 **空闲**（无监听）；iptables 无任何 13100 规则；INPUT 策略
ACCEPT（CUBE_POC_GUARD 仅守 9999/8082）→ **现状=服务一旦监听即对任意源开放**。拟议链路
.171(192.10.50.171) → https://192.10.50.172:13100 可达性成立，但建议加源限制（房式与 9999/8082 一致）：

- **拟议规则（留待批准，本轮未实施）**：
  `iptables -A CUBE_POC_GUARD -s 192.10.50.171/32 -p tcp --dport 13100 -j ACCEPT`
  `iptables -A CUBE_POC_GUARD -s 127.0.0.1/32 -p tcp --dport 13100 -j ACCEPT`
  `iptables -A CUBE_POC_GUARD -p tcp --dport 13100 -j REJECT --reject-with tcp-reset`
- **恢复方式**：`iptables -D CUBE_POC_GUARD` 对应三条（或按行号删除）；不涉及 NAT/持久化变更，
  如需 netfilter-persistent 保存另列批准项。

**证书方案（业务专用内部 CA，不复用 M0 Cube CA=agentseek-m0-internal-ca）**：
- 签发：获批后 .172 本地生成业务 CA（建议名 agentseek-business-ca）——CA 私钥 0600 存
  /etc/agentseek-business-ca/（0700，root；**私钥保管端=.172**）；服务器证书 CN=agentseek-business-node、
  **SAN=IP:192.10.50.172**，TLS≥1.2（代码合同）；有效期建议：**CA 180 天、服务器证书 90 天**（内部短周期
  实践，可调）；签发与物化待最终批准，本轮零生成。
- 分发：**CA 公共证书**交 .171（网关 BusinessHttpClient CA 校验用，受控交接+SHA256 清单）；服务器证书/
  私钥仅 .172 config/tls/（0600）；有效期到期前重签方案=同一 CA 续签+双向钉新摘要。

**broker token 方案**：获批后 .172 以 secrets 生成**随机十六进制 64 字符**（满足代码合同 32–256、
ASCII 33–126、无空格换行）；单值；受控交接 .171（仅入网关侧私有文件），.172 侧入 permits 私有材料；
轮换=重新生成→双侧同步→重钉摘要；本轮零生成。

**保留 .172 的私有材料**：Cube API key（受控源+已物化件沿用）、vault key、监督 pins/manifest——
不进回传与 Git（.171 仅持 broker token+CA 公共证书，与 REFERENCE 现场分工一致）。

## 二、固定任务与绑定（§二）

| 项 | 值 | 状态 |
| --- | --- | --- |
| 输入 CSV | 41 字节，SHA256 `0ec7d232fcf9e415f07261888d90e21c8cb7d03b30f9932caa254e03b0b576e0` | Codex 已复核；文件本体待交接（G4） |
| 固定问题 | UTF-8、无首尾附加换行，SHA256 `5941d04739dfa492eaf53e30607d47aef1665e992f7d6e33124e72f1bbbf96f5` | 同上 |
| 预期输出 | 28 字节，SHA256 `376b78750aa4f075ee291e117efdf1e015d58c43c0bc61c82215feff26085a32` | 同上 |
| owner_id / session_key / input_ref | — | **待补**（.171 按用户实际上传与可信会话取得、按候选公式计算后受控交接；须在最终批准/钉摘要/创建之前补齐；不预填不猜测） |

**GATEWAY_FACTS 两项核对**：因脱敏件未收到（G1/G2 缺口）——"文件进入提问当轮 current_files 的方式"
与"用户打开输出文件的入口"**未能核对**，维持缺口；不由 .172 手工注入状态补过。

## 三、节点方案（§三；全部为建议值，未物化）

| 项 | 建议值 |
| --- | --- |
| 业务 venv | /opt/agentseek-business-releases/5afe44c8/venv（业务 wheel 生产安装；与 R1 各 venv 隔离；安装+36-2 复验待批准轮执行；现 venv-business 仅 scratch） |
| 业务根目录 | /var/lib/agentseek-m3-business/runs/agentseek-m3-biz-<日期>（run_id 用户批准定名；不复用 R1 run） |
| SQLite store | <业务根>/store/（BusinessStore，任务/结果/reconciling 状态） |
| workspace | <业务根>/workspace/（CsvWorkspace，所有者/请求隔离、0600、原子、读回后销毁） |
| 监督/创建链 | 新 run 专属 supervisor（第二服务设计沿 R2 方案：新 manifest+unit agentseek-m3-biz-supervisor.service+新 pins，UMask=0077、120s 冻结值、不 enable；旧 M3 服务零改动）+ vault/fence/quota/config（permits、lifecycle 链、business_file、tls/）目录树 |
| 服务配置（草案禁用） | business_service 12 字段契约：bind_host=192.10.50.172、bind_port=13100、cert/key=config/tls/、store/workspace 如上、plans 按 (owner_id,request_id) 逐项钉 lifecycle/business 摘要；**approved=false 草稿**，approved=true 最终件待用户批准后另建重钉 |
| 服务单元（建议） | agentseek-business-broker.service：disabled 默认、UMask=0077、ExecStart=<业务 venv python> -I -m agentseek_execution.business_service --config… --sha256…；仅在批准窗口内 start，不 enable |

**所需变更清单（全部留待批准，本轮零实施）**：①13100 三条 guard 规则；②业务 CA 签发+服务器证书+
CA 公共证书交接；③broker token 生成与交接；④新 run 登记+第二监督 unit+新 pins；⑤业务 venv 生产安装；
⑥业务根目录树创建；⑦business_service 草案→最终件；⑧business-broker unit 创建。**恢复办法**：规则
iptables -D 三条；服务 systemctl stop+unit 删除（不 enable 即无自启）；新 run 目录留审计不删；CA/token
作废=私钥/值销毁记录+双侧摘要移除；R1 侧零变更故无需恢复。**旧 A 额度/栅栏/manifest/回执全部原样保留**
（本轮只读核对过 R1 现场：三服务 active、runs 树原样）。

**批准范围建议（一次业务执行全链）**：一次 guest 创建（经新 run 创建链+W0 新窗口确认）→ CSV 执行
（模板 python3/标准库/临时目录能力**纳入本次同一执行验证**，本轮不额外建 guest）→ 节点持久化（读回后
销毁）→ 正常主动终止（业务批准含主动终止许可，REFERENCE 合同）→ 监督 120s 兜底；**创建后各步由程序
连续执行**（RemoteCsvRunner 一次请求驱动全链，无人工审批/Agent 工具往返——上轮 R1 编排延迟教训已由
连续驱动设计吸收）→ 清理确认（closeout，DELETE 不算成功）→ 网关回写 → 用户打开文件验证。

## 四、§四 逐项表（建议值/缺项汇总）

| 项 | 建议值/状态 |
| --- | --- |
| 链路 | https://192.10.50.172:13100；13100 空闲；拟议仅 .171+loopback 限制规则（待批） |
| 证书 | 业务专用内部 CA（180d）+服务器证书（90d，SAN=IP:192.10.50.172）；CA 私钥留 .172；公证交 .171（待批签发） |
| broker token | 64 位随机十六进制；获批后 .172 生成受控交接（未生成） |
| 固定任务 | 三摘要已登记；文件本体待交接（G4） |
| owner_id/session_key/input_ref | 待补（网关侧，批准/钉摘要/创建前必须补齐） |
| current_files 进入方式 | 缺口 G1（GATEWAY_FACTS 未收到） |
| 用户打开输出入口 | 缺口 G2（同上） |
| 业务 venv/根/SQLite/workspace/监督 | §三 建议值（全部待批物化） |
| guest python3 能力 | 纳入同一次业务执行验证（不额外建 guest） |

## 五、边界

本轮零执行零变更：未启动 business_service、未建 guest、未改网络/防火墙/服务/软链//opt/R1 状态；
凭据与批准正文零入回传；零 Git。全部产物为本报告与清单两件。
