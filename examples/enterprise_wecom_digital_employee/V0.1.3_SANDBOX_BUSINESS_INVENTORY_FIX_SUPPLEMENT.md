# 业务清单测试补正——补充报告（zcode，2026-09-18）

## 判定：**BLOCKED_ON_STAGING_SCOPE 已解除；本轮汇总判定 BUSINESS_NODE_OFFLINE_PASS**（44/2、835 等沿用原报告实测，不冒充本次重跑；仍非真实业务全链 PASS、非文件用户可见 PASS、非 M3_READY）

## 一、补正包核验

/root/m3-business-fix-handoff-20260918 恰 **4 件**（FIX-MANIFEST/README-FOR-ZCODE-FIX/
test_business_inventory.py/补正版 HANDOFF）；`sha256sum -c FIX-MANIFEST.sha256` **3/3 payload OK**。
补正提交 2e62f5e0…（父=候选 5afe44c，仅改测试与 HANDOFF）；运行对象不变：候选 5afe44c、
wheel bceeb6e3…fb8f2a（未重装）。

## 二、复跑记录（仅 1 项；完整命令与实际解析路径）

- README 原样命令存在**组合缺陷**（如实报告）：`-I` 隔离模式忽略 `PYTHONPATH` 环境变量，
  首次执行收集失败（ModuleNotFoundError: business_inventory，exit 2，日志在册）。
- **最小忠实调整**：以 pytest 运行时注入 `-o pythonpath=/root/m3-business-handoff-20260918/tools`
  替代 `PYTHONPATH=` 环境变量，其余与 README 完全一致（同测试文件、同 ROOT/TEMPLATE 环境变量、
  同原业务 venv 解释器、保持 -I）。调整依据=上轮 835 项闭包测试同机制（-o pythonpath 在 -I 下有效）。
- 实际执行命令：
  `AGENTSEEK_BUSINESS_INVENTORY_ROOT=/root/m3-business-handoff-20260918/source-closure AGENTSEEK_BUSINESS_PACKAGE_TEMPLATE=/root/m3-business-handoff-20260918/tools/business_package.toml /root/m3-business-work-20260918/venv-business/bin/python -I -m pytest -q -v -o pythonpath=/root/m3-business-handoff-20260918/tools /root/m3-business-fix-handoff-20260918/test_business_inventory.py`
- 结果：**1 passed in 0.36s（exit 0）**。

## 三、执行对象与来源确认

| 项 | 值 | 确认方式 |
| --- | --- | --- |
| 被执行测试 | /root/m3-business-fix-handoff-20260918/test_business_inventory.py，SHA256 53f5d2ba…80aa4 | 与 FIX-MANIFEST 清单值一致；pytest -v 显示同路径 |
| business_inventory | /root/m3-business-handoff-20260918/tools/business_inventory.py（原包，未变） | import 探针实测解析路径 |
| m3_r1_inventory | /root/m3-business-handoff-20260918/tools/m3_r1_inventory.py（原包，未变） | 同上 |
| package 模板 | 原包 tools/business_package.toml（环境变量指定） | 命令显式 |
| 清单根 | 原业务 source-closure（环境变量指定，测试断言双入口/业务文件集/排除网关与无关工作，stage_package 模板校验通过） | 测试通过本身 |
| 解释器/venv | /root/m3-business-work-20260918/venv-business（原业务独立 venv，零重装） | 命令路径 |

## 四、汇总判定依据

- 原报告核心项（170/170 到货、16 发行版严格离线、44/2/isolated/exit 0、835 passed、零残留）
  引用 2026-09-18 原报告实测，本轮未重跑不冒充；
- 本轮补跑 1 项通过 → 原 BLOCKED_ON_STAGING_SCOPE 解除（Codex 裁定：原 5 项失败为工单/工具
  作用域不匹配，通用 R1 完整仓库测试不在业务子闭包复跑，未拼接完整仓库）；
- **汇总判定 = BUSINESS_NODE_OFFLINE_PASS**；仍非真实业务全链 PASS、非文件用户可见 PASS、非 M3_READY。

## 五、下一步（报告注明）

直接进入**真实业务全链准备**：真实用户问题 → DeepAgent 模型路由（脚本模型不代表活体能力）→
受控创建（新授权新窗口，不沿用旧 A 额度）→ 固定 CSV 执行（模板 python3 待活验）→ 节点工作区
持久化 → 清理确认（DELETE 不算成功，须 closeout）→ 网关用户文件区回写 → **用户打开文件验证**
（两层验证不可互代）。所需现场输入见原报告 §五八项。

## 六、边界

未重装 wheel、未重跑 44/2/835/平台测试；未启动 business_service、零 guest、零探针；/opt、R1 状态、
额度、栅栏、回执、服务、软链、防火墙零触碰；零 Git。回传仅两件，无凭据/业务数据/批准正文。
