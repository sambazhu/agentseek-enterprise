# CSV 沙箱业务节点隔离复验——.172 执行结果（zcode，2026-09-18）

## 判定：核心项 PASS（到货/隔离安装/44-2 检查/835 闭包测试/版本核验/零残留）；**清单工具测试子项 BLOCKED_ON_STAGING_SCOPE（6 项中仅 1 项可复现）**——整体不记完整 BUSINESS_NODE_OFFLINE_PASS，待补材料裁定后补跑该子项。非业务全链 PASS、非文件交付 PASS、非 M3_READY。

## 一、到货与解释器核验

- /root/m3-business-handoff-20260918：`sha256sum -c MANIFEST.sha256` **170/170 OK**；
  wheel agentseek_execution_business-0.1.3a0-py3-none-any.whl SHA256
  `bceeb6e372b7dfa8c0d4c2c3a588c5430e94f7b3dbca5b08086ee41087fb8f2a` 与工单/文档一致。
- 解释器复用 /root/m3-r1-handoff-20260917/python/…/bin/python3（只读核验：3.13.14；bin/python3 与
  bin/python3.13 为同内容实际文件，摘要 b200d6c7…9ff 与原始 R1 清单值一致；未修改该解释器）。

## 二、隔离安装与版本核验（与 R1 严格隔离）

- 新 scratch=/root/m3-business-work-20260918 与独立 venv venv-business（uv venv --offline）；
  **未装入任何 R1 venv**；未触碰 /opt、常驻服务、软链。
- 安装命令含 `--offline --no-index --find-links <到货>/deps`：仅从本轮 16 个交付 wheel 解析
  （无现场 uv 缓存/联网）。**实际安装发行版清单（16）**：agentseek-execution-business==0.1.3a0、
  anyio==4.15.1、certifi==2026.7.22、**cffi==2.1.0**、cryptography==49.0.0、h11==0.16.0、
  httpcore==1.0.9、httpx==0.28.1、idna==3.19、iniconfig==2.3.0、
  packaging==26.3、pluggy==1.6.0、pycparser==3.0、pygments==2.21.0、**pytest==9.1.0**、
  typing_extensions==4.16.0。cffi 2.1.0 与 pytest 9.1.0 断言在册 ✓（以 logs/dists.txt 为准）。

## 三、安装检查与闭包测试

| 项 | 结果 |
| --- | --- |
| install_check --profile business | **isolated=true、44 模块摘要通过、2 入口无效输入拒绝、exit 0**；清单 scope="CSV business node closure"（非 R1 矩阵/网关包）、entries=business_service/business_cube_session |
| 闭包隔离测试（源码闭包根 + `-o pythonpath=examples/…`） | **835 passed，12.83s**（Python 3.13.14；与开发端 835 一致）；TLS 用例仅本机临时 loopback listener/合成凭据/假 provider；未连接真实 Cube/控制面/平台、未启动 business_service |
| 清单工具测试 | **1 passed / 5 failed——BLOCKED_ON_STAGING_SCOPE**：test_business_inventory.py 断言**完整候选树**的清单推导（如 entry_modules 末项=m3_continuous 等 R1 模块），而交付 source-closure 为业务子集（44 模块）且未附完整候选树；开发端 6 passed 是在完整候选根目录运行（文档 §1 命令可证）。重建 repo 布局后仍失败（根因同）。**未合并其他轮闭包凑数**（异源提交合并不忠实）。需补交付完整候选树或调整测试范围后补跑 |
| 临时子进程/端口 | 测试后子进程**零残留**；新增监听端口零（8083/3010/12088/8089/22589 均为既有平台/系统服务，归属已核：cube-lifecycle/cubeops/docker-proxy/uvicorn/sshd） |

## 四、§3 五项只读盘点（只报事实与缺项）

1. **网关→节点 HTTPS**：拟议=.172 私网 IPv4（192.10.50.172）TLS 服务（business_service 仅在明确
   批准配置下启动，不 import 即监听）+HTTPS CA 校验+独立 broker token（.171 只持 broker token；
   Cube key/vault/监督私有材料留 .172）。**端口与证书签发方案未定**——开发端仅本地 loopback HTTPS
   验证过；真实两机证书/端口/服务启用尚未批准执行；本轮未改防火墙（工单禁止）。
2. **store/workspace 位置**：节点权威文件区=guest 外私有账本+所有者/请求隔离的 CsvWorkspace
   （0600/原子/读回后销毁）+SQLite 任务结果；网关侧沿用 FilesSettings 的 LocalFileStore 用户/会话
   文件区。**.172 盘上具体根目录未在交付材料中定死**；约束=新业务根不复用旧 A 的 quota/fence/run。
3. **身份映射**：规则已明确——文件 employee_key/session_key 必须与可信 runtime 一致；**企微 userid
   与 OA 账号不一致时不得猜测映射**。真实映射数据属网关侧输入，本轮未交付至 .172（缺项，下轮需）。
4. **CSV 固定程序待活验能力**：guest 模板须提供 python3+所需标准库（csv/decimal 等）——**真实模板
   能力尚未验证，不得凭模板 READY 推定**（当前唯一平台模板 tpl-c4ba…的镜像内 python3 未验）；
   待活验=python3 存在性/版本/标准库、25s worker 预算内完成、summary.csv 输出符合 group,total。
5. **两层验证区分**：节点持久化（沙箱外私有工作区+摘要读回后销毁）与用户文件区可见性（网关镜像+
   前端文件列表）为**两层独立验证，不能互证**；前端可见性尚未实测；最终业务验收必须由用户打开
   文件核对（进度表第 5/6 步）。

## 五、下一轮真实业务全链仍缺的现场输入（汇总）

①两机证书签发与端口/防火墙批准；②.172 业务根目录与 SQLite/工作区位置批准；③网关侧真实
userid↔OA 映射输入；④guest 模板 python3/标准库活体验证；⑤真实 DeepAgent 模型（脚本模型不代表
活体选择能力）；⑥business_service 启用配置与 broker token 受控交接；⑦一次创建授权+有效窗口
（不沿用旧 A 额度/W0/窗口）；⑧用户文件区回写的用户可读性验收安排。

## 六、边界

本轮零创建零探针零平台连接、未启动 business_service、未生成审批/执行材料；R1 全部安装/额度/栅栏/
回执/常驻服务/软链/防火墙/生产配置零触碰；零 Git。回传仅脱敏报告与清单，无凭据/证书私钥/审批
正文/输入数据/输出 CSV 正文。
