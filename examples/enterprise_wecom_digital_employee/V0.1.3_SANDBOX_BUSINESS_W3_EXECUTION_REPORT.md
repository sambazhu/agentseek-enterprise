# 窗口三执行报告（2026-09-21 07:12–09:12 UTC 技术有效期；07:34 提前收口）

## 判定：NOT BUSINESS_E2E_PASS——但模型环与全链前段首次完整贯通

| 层 | 结果 |
| --- | --- |
| W0（.172 唯一台账） | ✅ 4/4（zcode 06:58/codex+cc-171 07:08:16/user 07:11:59；最晚实收<start 07:12） |
| 网关切换 | ✅ **一次到位**（进程 env 压过 .env——09949c8 优先级修复实证生效；.env 零改动） |
| 绑定核对 | ✅ 8/8 程序化（request_id=2497…fbf9 两端一致） |
| 模型真实选工具 | ✅ **首次完整贯通**：经两轮引导（旧终态读数→新授权说明）后真实调用 run_sandbox_task，attempt 8c22bcfe reserve+提交（grants/permits v3 全过） |
| 一次创建 | ✅ 真实发生（sandbox 03a3d1c5…，manifest 创建后 0.36s 登记，vault 密封，额度已耗，closeout 正常） |
| guest 内 CSV | ❌ 未产出（workspace 仅 create.target）；节点终态 failed+cleanup_confirmed=true |
| 工作区回写验收 | ❌ NOT RUN |

## 现场处置记录（如实）
1. 旧 request#2（ede5eccb）镜像 reconciling 意图：permits 已轮换 v3→其结果取回路径不可达（broker 400=permit 不存在）→经用户批准按节点权威终态（W2FIX/法证 545d2e2 归档）一次性 record('failed') 闭合（镜像摘要 95577d08 留痕）——同 request#1 闭合模式。
2. 模型引导两轮：第一轮读旧终态拒绝（新文案正确传达"failed 终态不可重试"）；第二轮获新授权说明后正确提交新授权。

## 剩余缺陷定位（强时序证据，唯一阻断项）
- guest 全生命周期 2.6s（lifecycle create 07:27:41.008→delete 07:27:43.626）；命令尝试窗口≈2s=代码 `httpx.Client(timeout=2)` 硬编码；
- **头号根因**：平台 create 事件≠guest 内 envd(:49999) 就绪——冷启动 guest 创建后 1-2s 服务未必监听，链在 ~1.5s 即发命令→连接失败/超时→failed→应用终止；
- 佐证更正：**cube-proxy access_log off（M0 配置）**——"proxy 零日志"不再构成"命令未发出"的证据（此前两轮推断据此需修正）；
- **修复属代码层**：run 分支需增加 guest 就绪等待（对标 R1 attach 预算 register 3s+heartbeat 3s，或轮询平台 state/envd 端口；客户端超时同步放宽至命令预算级）——交 Codex。

## 恢复终态
生产网关已恢复（07:34:51 标准/0 错误/.env 未动）；下载 disabled、.60 未改、旧账本/额度/栅栏全保留；guest=0。

## 下一步
Codex 修复就绪等待→离线复验→窗口四（模型环已证通，仅剩此一处）直奔验收（终点=.171 工作区回写+376b7875）。
