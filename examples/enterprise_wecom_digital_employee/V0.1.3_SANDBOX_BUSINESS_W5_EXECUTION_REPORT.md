# 窗口五执行报告（2026-09-21 14:53–16:53 UTC 技术有效期；15:06 提前收口）

## 判定：NOT BUSINESS_E2E_PASS——阻断于第 10 号缺陷（授权对模型不可见）；**一次创建授权分文未耗（零 guest）**

## 一、就绪与绑定（全部实证通过）
- W0 4/4（最晚实收 14:52:19<start 14:53）；有效期 14:53–16:53 UTC
- 节点：材料 v5 10/10、broker active（9d54662+W5-v2、proxy_port=80 方案 D 落地）、quota/fence/lifecycle-v5 全空、guest=0
- 网关：grants/配置 v5（request_id #5=5cffc9df…5ca95，grants dc391f75/config ece726b4）切换一次到位；绑定核对 8/8 程序化 PASS（resolver 全链过=模型若调用即通过授权）

## 二、执行记录
- 用户 14:59 发固定问题→模型调用 get_sandbox_task_result（读镜像旧终态 8c22bcfe=failed）→拒绝 run_sandbox_task
- 15:02 引导消息（新授权 5cff 说明）→模型再读旧态→**拒绝且给出决定性自述**：
  > "5cff 新授权单无法被本会话核验到（工具结果中没有任何 5cff 相关的可用状态）……新状态生效后（get_sandbox_task_result 返回 available）我可执行 run_sandbox_task"
- 模型环 exhausted（W3/W4/W5 共 5 种消息变体验证同一死锁）

## 三、第 10 号缺陷（本轮核心产出，精确闭环）
**授权对模型不可见**：get_sandbox_task_result 只反映最近请求终态（镜像旧 attempt=failed）；工具面无任何信号显示"新授权已就绪"。鸡生蛋死锁：模型要先见授权证据才调用，授权可见性只在调用后产生。**四轮窗口的"模型拒发"至此全部归因此缺口。**
**修复建议（Codex）**：状态工具在无活动任务时返回 `grant_available: true`（读 grants 目录状态不执行）——模型即可区分"无授权"与"新授权待首次使用"；辅以 run_sandbox_task 文档句提示"服务端已配置的授权以调用结果为准"。

## 四、zcode 反馈的事实更正（入册）
1. "问题在 .171 网关侧"不准确——网关绑定/配置/连接全部正常（§一实证）；真因=第 10 号缺陷
2. **"store 14:59:55 已清空"待说明**——若为 .172 再次清除旧账本（W4 历史 attempt），违反 W5 批文"旧状态全部保留不重置"且为窗口内第三次状态清除；需 zcode 说明操作与授权依据（且该清除对本轮死锁无影响——清除后工具仍返回 unavailable 而非 available）

## 五、终态
- 生产网关 15:06:25 恢复（0 错误）；下载 disabled、.60 未改；历史额度/账本全保留；guest=0；**W5 一次创建授权未消耗，随 16:53 有效期自然失效**
- 节点基础设施全部实证就绪（网络双层修复+协议匹配+材料 10/10）——Codex 修完 grant 可见性后 W6 按同款条款直通

## 六、五轮窗口累计缺陷清单（终版）
1. CA 缺 KeyUsage 2. batch_sequence 结构 3. lifecycle 残留 intent 4. artifact SHA 笔误 5. business-session schema 6. verify_identity 漏参 7. 无就绪等待 8. 材料复用已耗 quota 9. 链外 perform+额度重置（.172） 10. **授权对模型不可见（本轮）**——前 9 项全部修复实证，第 10 项为唯一现存阻断。
