# 窗口六执行报告（2026-09-21 15:29–17:29 UTC 技术有效期；15:49 提前收口）

## 判定：NOT BUSINESS_E2E_PASS——**但第 10 号修复实战验证成功：模型真实调用了 run_sandbox_task**（五轮首次）；新阻断=第 11 号缺陷（网关进程交换弃用）；**一次创建授权未耗（guest=0）**

## 一、历史性突破
- 授权可见性修复（b9d971c）生效：模型首次在正式流程调用 run_sandbox_task（attempt 605e162f reserve 成功——授权链/grants v6/绑定全过）
- 模型行为 thereafter 完全合同化：精确引用 grant_available/authorization.state 并承诺"信号 true 即执行"——第 10 号缺陷修复**实战闭环**

## 二、第 11 号缺陷（新，网关进程专属）——strace 双轮铁证
```
TCP connect 192.10.50.172:13100 → SO_ERROR=0（成功）→【零 TLS 字节】→ socket 弃用（fd 被复用）→ 线程转做其他任务（WeCom API 等）
```
- 异常未被分类（非 Connect 类）→ attempt 留 reconciling → 节点 not_found 无法终态化 → grant 检查返 false → 模型正确等待（死锁由缺陷 11 而非 10 造成）
- **对照排除全记录**：同机同 venv 同文件 shell 直查=完全正常（200+not_found）；env 无差异；httpx 0.28.1/httpcore 1.0.9 无 shadow；fd 33/1M 充裕；CA v2 加载正确；网关到 13100 的 TCP 可达（strace 实证）
- **为何五轮未见**：W3-W5 网关进程的交换从未真正执行过（皆被 shell 直驱替代）——模型环打通后首次暴露

## 三、修复建议（Codex）
1. 工具层增加异常分类日志（脱敏 stage/error 类别——复用节点侧诊断信封模式）使此类异常可见
2. 排查网关进程内 httpx 在 TCP-connect-后-TLS-前 弃用的成因（疑似 async 工具执行器的超时/取消传播到线程池-httpx 交互；或 httpcore 连接池 deadline 计算）
3. 复现 harness：用 agent 内图测试路径（test_sandbox_deepagent_loop 已有先查询再执行用例）扩展为真实 Client 注入场景

## 四、其余记录
- W0 4/4（最晚 15:28:35<start 15:29）；绑定 8/8；双端材料全 PASS（节点 10/10/网关 v6 2dd2ca7d+66c347e5）
- 用户两轮触发（固定问题+两次只读核验请求）全程配合；模型只读核验行为完全正确
- 生产网关 15:49:13 恢复；下载 disabled、.60 未改、guest=0、历史状态全保留

## 五、六轮窗口累计：11 缺陷清单
1-10 全部修复实证（第 10 号本轮实战闭环）；**11（网关进程交换弃用）唯一现存**——修复后 W7 全绿可期。
