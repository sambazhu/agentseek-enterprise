---
title: CSV 业务窗口二执行报告（.171 视角全记录）
type: how-to
audience: [A4]
runs: yes
verified_on: 2026-09-21
sources:
  - V0.1.3_SANDBOX_E2E_WINDOW_APPROVAL.md
  - V0.1.3_GATEWAY_FINAL_MATERIALS_RECORD.md
---

# 窗口二执行报告（窗口 2026-09-21 04:00–06:00 UTC；.171 Linux CC 归档，05:5x 收口）

## 一、判定：NOT BUSINESS_E2E_PASS（分层如实）

| 层 | 结果 |
| --- | --- |
| 真实模型选工具 | ✅ 两次真实调用（04:29 首次 reserve 成 attempt `2bdb3953`；05:26 受理 attempt `6750b14a`——授权链/绑定/摘要逐项通过） |
| 节点一次创建 | ✅ **实际创建次数=1**：额度 create-slot-0 已耗、vault 密封回执在册 |
| guest 最终状态 | **已终止**：监督 120s 硬截止正常执行；lifecycle 全链 intent→result→closeout 完整 |
| closeout 结果 | 正常（目标消失确认；cleanup_confirmed=true） |
| guest 内 CSV 执行 | ❌ 未产出 summary.csv（工作区仅 create.target 跟踪文件）；节点 store 终态 **failed** |
| 工作区回写验收 | ❌ NOT RUN（无产物；预期 376b7875 无从核对） |

## 二、六项处置过程（含 Codex 复核标注）

| # | 缺陷 | 现场处置 | 定性（含 Codex 2026-09-21 复核意见） |
| --- | --- | --- | --- |
| 1 | 业务 CA 缺 KeyUsage（OpenSSL 3 拒链；04:02 探测曾过=当时服务端出示信任锚本尊的锚点旁路，04:31 起暴露） | zcode 重签 CA v2（684ffebb，critical CertSign+CRLSign+SKI）+重签服务器证书（SAN=IP 不变）+受控交接 .171 | 已修（证书链 04:40 复验 PASS） |
| 2 | batch_sequence=1 违反 BatchQuota∈{2,4} | zcode 补第二计划（同参数不同 token），四级重钉 | 已修（结构层）；**Codex 复核：满足结构要求不代表获准创建两次——第二计划的一次创建授权依据未单独获批，待复核** |
| 3 | lifecycle 目录残留 create-intent.json（O_EXCL 阻断） | zcode 新建 lifecycle-v2 目录并重钉 | 已修（运行层）；**Codex 复核：换目录前须有旧 intent 是否曾发送创建的证据及授权依据——不能仅因 O_EXCL 阻断就换目录，zcode 侧待补证** |
| 4 | precreate artifact_sha256 第 25 位转录笔误 b→f | zcode 修正 | 已修 |
| 5 | business-session schema 不符 12 字段精确集+内嵌 request_id 未随 v2 同步 | zcode 按 ApprovedCsvSessionFactory 合同重建（4ebabf49） | 已修 |
| 6 | **CSV 程序命令=宿主机绝对路径** `/opt/agentseek-m3-supervisor/python/cpython-3.13.14-linux-x86_64-gnu/bin/python3.13`（business-session/plans 三处引用）——guest 内不存在该路径 | 未修 | **下一轮硬前提**。**模板取证（m3-r1-template-response.json createRequest.containers[0].envs）：guest 镜像自带 python——PYTHON_VERSION=3.12.12、PYTHON_SHA256=fb85a134…、PYTHONDONTWRITEBYTECODE/PYTHONUNBUFFERED 在册**。即"缺 python3"不成立，错在命令引用宿主解释器+版本错配（3.13.14 vs guest 3.12.12） |

## 三、.171 侧完整时间线（含自录缺陷）

- 04:02:18 TLS 探测 PASS（当时不知为锚点旁路）；04:02-04:04 **切换首试失败**（候选树 `uv run --offline --with jpype1` 离线解析不出；生产停机 1m39s）→ 改生产 venv 直启成功。
- **spec 覆盖问题（自录缺陷③）**：BUB_LANGCHAIN_SPEC 进程 env 两轮不生效——根因=`bub_gateway.py` 启动把 **.env 每键直接覆盖进 os.environ**（24-25 行）+AGENTSEEK_*→BUB_* 别名再映射（53-58 行）→.env:21 固化的默认 spec 盖掉一切进程 env。改 .env 行解决（备份 .env.bak-20260921-window2-specswitch）。
- 04:20/04:22/04:42 三次重传 CSV（current_files 随重启丢失所致）；04:20:28 spec 确认 sandbox_spec。
- 04:29-04:31 模型首次成功调用（request#1=4d760701，实参完美）→ reserve 成功 → **客户端 TLS 失败（缺陷①）**→ 意图 reconciling；节点零到达（journal/store 双空实证）。
- 04:38-04:42 修复包#1（用户批准）：request_id#2=ede5eccb、grants v2（00ca8f4b）/配置 v2（ceaa9cb7）、CA v2 换件、网关重启。
- 04:43/04:45 模型两次调用均 **unavailable**——真因=镜像 store "unresolved attempt; do not create" 护栏（**产品缺陷①：预发送失败意图永不闭合且阻塞同 owner 新执行**）；当时误判为模型实参问题（05:07 诊断轮证实模型实参完美）。
- 05:20 模型拒调（**产品缺陷②：get_sandbox_task_result 对不可恢复旧意图永远 unavailable→模型认定权威拒绝、拒调新授权工具**——重试毒化）。
- 05:25 **用户批准一次性闭合死意图**（record('failed')，候选方法非 SQL；依据=节点零执行独立证据+批准人裁定；镜像摘要 770a3f56 留痕）。
- 05:26 模型受理 attempt `6750b14a`（reserve 过）→交换 400。05:27/05:32/05:39 三次直接交换均 400——zcode 连修缺陷②③④⑤后**本地全路径驱动**。
- 05:41-05:48（zcode 侧）：**一次创建发生**（额度消耗、vault 密封、lifecycle 全链、监督 120s、closeout 正常）→ guest 内 CSV 程序零输出 → store 终态 failed(cleanup=true)。
- 05:49 结果取回交换仍 400（镜像 attempt#2 留 reconciling——节点 store 权威终态在册；不再重试）。
- 05:50-05:51 **生产网关恢复**（.env 复原 agent:build_spec+标准脚本；05:51:13 LC 订阅、0 错误）。下载 disabled、.60 未动、R1/production/标签全程未动。

## 四、三个产品缺陷（.171 发现，交 Codex）

1. **预发送失败意图死锁**：节点 not_found 无法终态化 → 意图永久 reconciling → 阻塞同 owner 一切新执行；唯一出路=人工裁定闭合（本轮已发生一次）。
2. **模型重试毒化**：状态工具对不可恢复意图返回 unavailable+retry_allowed=false，模型将其当作权威拒绝，拒绝执行结构上是"新请求"的新授权（request_id 已换也不行）。
3. **env 优先级颠倒**：bub_gateway 启动期 .env 全键覆盖 os.environ+别名映射——运行时环境变量对 AGENTSEEK_* 配置失效（本次排障三轮的根因）。

## 五、证据与边界

- 节点侧证据（额度/vault 回执/lifecycle 链/监督/closeout/模板 envs）在 .172，zcode 正式回传后归档；.171 侧：镜像 store 终态（attempt#1 failed[裁定闭合]/attempt#2 reconciling[节点权威 failed 在册]）、grants/配置 v2 私有件（00ca8f4b/ceaa9cb7）、gateway.log 全程在册。
- 凭据与批准正文零入库；一次创建额度已耗不退；本轮不定新窗口、不批准新创建（Codex 接手修复验证后另交完整候选）。

## 六、下一步（Codex 已列）

①business-session 命令生成修正（宿主/guest 解释器区分；guest python 3.12.12 路径经镜像工件先行确认）；②三产品缺陷定因修复；③两项现场处置复核（第二计划授权依据/旧 intent 发送证据）；④完整候选+离线复验后另议窗口与新创建授权。

## 勘正（2026-09-21，依据 W2 法证 545d2e2 归档的 .172 取证）
§一表"guest 最终状态"原记"监督 120s 硬截止正常执行"**不准确**，勘正为：**guest 被应用 cleanup DELETE 终止（存活 1.30 秒：create 05:47:36.730Z→delete 05:47:38.027Z），非监督 120s 硬截止**；supervisor journal 无 HARD deadline/kill/ALARM 记录（监督未触发=符合预期而非"正常执行"）。正常终止路径与监督兜底是两条不同路径，本例走的是前者。原报告其余内容经法证复核无矛盾。

## 审计事项单列（不归入代码修复 PASS，待 Codex/用户审计关闭）
1. **第二 batch 计划授权**：BatchQuota 合同要求 len∈{2,4}，batch[1] 为结构占位（quota 无 slot-1、从未获准从未执行）——占位计划的授权表述需在合同层明确（占位≠授权）或由用户在每次窗口批准中显式知悉。
2. **旧 intent 换目录（lifecycle→lifecycle-v2）**：换目录时旧 intent 未经发送即被绕开；法证已证 v2 路径创建确曾发送成功、旧目录 intent 无对应创建——但"以换目录绕过 O_EXCL 阻断"的操作模式本身需审计定性（是否应先闭合旧 intent 再新建）。
