# reader80 修复——.172 离线复验结果（zcode，2026-09-22）

## 判定：**READER80_OFFLINE_PASS**（45/2 + 918 + 1）；不切 broker、不建 guest、不清状态、零 Git

## 一、到货核验

R80-MANIFEST **172/172 OK**（包目录 8 件含清单自身）；wheel `93ab81b70e339fa774a7764e80932d39f6dad242942f8a28c311d6cf09c26896` 一致；候选 50adc3f77743cac2fe96df515b6054df7c6de165（父=00ab573）。

## 二、隔离复验

- 新 scratch+venv-r80（纯交付依赖 --offline --no-index）；install_check **45/2/isolated/exit 0**。
- 闭包 pytest（-o pythonpath）：**首跑 1 failed（test_secure_ownership 同前六轮瞬态）**，随后 **918 passed ×2（13.04s/12.89s）+ 单文件 12/12**——复现矩阵达标，预期 918 确认。
- 业务清单 **1 passed**；零残留。

### PlatformReader proxy_port 修复确认

修复代码（m3_platform_evidence.py:105）：
```python
# 改前: require(type(proxy_port) is int and 1024 <= proxy_port <= 65535)  ← 拒 80
# 改后: require(type(proxy_port) is int and (proxy_port == 80 or 1024 <= proxy_port <= 65535))
```
proxy_port=80 现在被 PlatformReader 构造函数接受——与 business_cube_session 的 proxy_port=80（上轮已修复）**匹配**。

### 既有 run/terminate 测试确认

既有 run/terminate 测试经过**真实 PlatformReader 构造函数**（非整对象 mock）——test_m3_platform_evidence.py 中 `module.PlatformReader(...)` 直接实例化并调用 `collect_empty()`/`collect_template()`，proxy_port 校验在构造函数 `__init__` 中真实执行。

## 三、新测试覆盖核验

新增测试覆盖确认（从 907→918 的 +11 项中筛选）：
- `test_installation_accepts_only_http_data_plane`（参数化 port/accepted）——覆盖 proxy_port=80 接受 + 13080 拒绝 + 非 int 拒绝 + 缺字段拒绝
- 既有 run/terminate/closeout 测试继续通过

## 四、接口核对

| 项 | 核对结果 |
| --- | --- |
| PlatformReader proxy_port | `proxy_port == 80 or 1024 <= proxy_port <= 65535`（:105）——**80 已放行** ✓ |
| business_cube_session proxy_port | 仍为 `proxy_port == 80`（上轮 13f9114 修复）——**两处独立端口白名单现已一致** ✓ |
| config["proxy_port"] 检查 | `config["proxy_port"] == self._proxy_port`（:295）——business-session 与 PlatformReader 一致 ✓ |
| /health 存在性 | 仍 **PENDING**（不建 guest 不实证） |

## 五、边界

不落位 /opt、不切 broker 或实际服务入口、不创建 guest、不清 W11 状态、不重置额度、不用真实配置手工 perform、零 Git。仅运行交付测试中的合成本地子进程并正常收尾。回传仅脱敏报告与清单。

## .171 汇总：新候选批准引用与安装方案（READER80_OFFLINE_PASS 后）

| 项 | 值 |
| --- | --- |
| 节点拟部署版本 | **50adc3f / wheel 93ab81b7…6896**（918/45-2/清单 1 双端一致；第十四次确定性命中） |
| 两处端口白名单 | business_cube_session:106（13f9114 已修）+ **m3_platform_evidence:105（本轮修）**——现已一致，80 全路径放行 |
| 安装方案 | .172 落位 /opt/agentseek-business-releases/50adc3f/venv（纯交付依赖 93ab81b7+45/2 复验+INSTALL-RECORD）→W12 全链材料重钉（request_id #12+全部引用→50adc3f/93ab81b7）→broker unit 切换+restart |
| 执行前硬核验（吸取 api_key 教训，程序化逐项） | ①create-installation 引用的每枚 *_sha256 对盘上实际文件算摘要；②api_key/ca/receipt_key 物化文件**存在性+权限（0600/父 0700）**；③launcher 解释器路径=50adc3f venv；④unit --sha256=重钉后实值；⑤boot_id 一致 |
| 下次执行 | 经用户统一批准：W0 四方→有效期→落位+材料+核验→网关 v12→重传→固定问题→**真实 DeepAgent 全链**（不增独立创建测试） |
