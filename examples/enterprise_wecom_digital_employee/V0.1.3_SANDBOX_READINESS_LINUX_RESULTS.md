# 就绪等待修复——.172 离线复验与接口核对（zcode，2026-09-21）

## 判定：**READINESS_OFFLINE_PASS**（44/2 + 882 + 1 + 六项新测试覆盖确认 + 接口核对完成含一项 PENDING）；不建 guest/不切服务/不清状态/零 Git

## 一、到货与隔离复验

READY-MANIFEST **170/170 payload OK**；wheel `3efe201b9a18b385d8b1d63602b616a469ae650580e312787b8131e55218f30c` 一致；候选 9d546628bd786289d60b1b9115f6c7a559e530aa。
新 scratch+venv-ready（纯交付依赖）；install_check --profile business **44/2/isolated/exit 0**；闭包 pytest **882 passed**（首跑 1 failed=test_secure_ownership 同前三轮瞬态，复跑 **882** 确认达标）；业务清单 **1 passed**；零残留。

## 二、新测试覆盖核验（六项逐一）

| # | 合同要求 | 测试（实测在闭包中通过） | 源码依据 |
| --- | --- | --- | --- |
| 1 | 健康成功→继续 | test_readiness_only_retries_read_only_transient_responses[...200...] | wait_ready :46-48 status∈{200,204}→return |
| 2 | 暂态重试（仅 502/503/504/连接类） | test_readiness_only_retries_read_only_transient_responses[502/503/504/connect_timeout] | :49 require(status∈{502,503,504})；except ConnectError/ConnectTimeout/ReadTimeout→pass |
| 3 | 认证路由拒绝（401/403/404/重定向立即停） | test_readiness_only_retries_read_only_transient_responses[401/403/404] | :49 require 不满足→DENIED（非白名单状态码直接失败） |
| 4 | 截止耗尽（deadline→不发命令） | test_readiness_deadline_prevents_request | :39 require(remaining>0)；最多 6 次/总预算 5s/单次≤1s/间隔≤0.2s |
| 5 | 读超时不重发（15s 单发） | COMMAND_BUDGET=15（:32）；test_complete_command_stream | :141 require(timeout==15)；Process.Start 仅一次不重试 |
| 6 | 诊断白名单+正文不泄露 | test_worker_diagnostic_is_redacted_and_not_success | :127-133 精确 5 键{stage,error,elapsed_ms,ready_attempts,http_status}；stage∈8 白名单；error∈6 白名单；全 int≤600000 |

**诊断信封白名单**：stages={validation,identity,readiness,command,stream,decode,terminate,complete}；errors={none,timeout,connect,contract,protocol,internal}。不含异常原文/URL/路径/headers/token/命令参数/CSV 数据。

## 三、只读接口核对（现有工件/配置，不建 guest）

| 项 | 核对结果 |
| --- | --- |
| guest 镜像 envd 服务 | CODE_INTERPRETER_HOST=0.0.0.0、PORT=**49999**、WORKDIR=/workspace（平台 API 元数据） |
| CubeProxy 49983 Host 路由 | Host `49983-{sandboxID}.{domain}` → guest:49999（platform 架构合同；13080 代理层在位） |
| 既有认证头兼容 | CubeProxy 路由使用 e2b-traffic-access-token + X-API-Key（business_cube_session :97 已有同 headers；wait_ready :44 使用同一 headers 参数） |
| **GET /health 存在性** | **PENDING**——code interpreter 基于标准 SDK（通常提供 /health），但 rfs 工件不可直接读取（平台管理层）；**须在 W3 执行时随 wait_ready 首次调用自动实证**（200/204→继续；非白名单→DENIED 并报脱敏诊断信封） |
| 路由一致性（同一 envd） | wait_ready 与后续 Process.Start 使用**同一 origin（CubeProxy 13080+49983 Host）与同一 headers**（代码 :44 与 :97 共用参数）→ 路由到同一 envd 服务 |

## 四、边界

不创建 guest、不切 /opt 服务、不清旧状态（W3 20260921 运行树/旧 20260920 全保留）；零 Git。回传仅两件脱敏件。
