# 监督单元身份接线修复——.172 离线复验与现场只读核验（zcode，2026-09-21）

## 判定 A（离线）：**BUSINESS_SUPERVISOR_UNIT_OFFLINE_PASS**；判定 B（现场，单独列）：**只读身份核验 PASS——上轮 UNIT 硬编码阻塞确认解除**。未切实际安装、未启业务服务、零 guest、零 Git；基础设施保留/回退待用户裁定

## 一、到货核验

UNIT-MANIFEST **170/170 payload OK**（工单"171 件"与清单行数差异照例如实记录）；wheel
`2290eaf62df8134f40028bb21d68baaf7143695f490435254f7299754c8ff97b` 一致（旧 3d17d872/bceeb6e3 保留）；
候选 4ebd197514c11988c7b80dd89f5e6eda24a99a16。

## 二、隔离复验（新 scratch=/root/m3-unit-work-20260921，venv-unit 零混装零切换）

- 纯交付依赖安装（--offline --no-index --find-links）；install_check --profile business：
  **44/2/isolated/exit 0**。
- 新合同核验（源码 :85-93）：**IdentityPins=8 字段**（原 7+可选 `unit: str = UNIT`，默认仍 R1 单元，
  业务用显式名称；`_validate_unit` 名称规则 [A-Za-z0-9][A-Za-z0-9_-]*\.service ≤255）；verify_identity
  全程改用 pins.unit（:145 show/:155 cgroup/:180 复核/:184 ProcessIdentity.unit）——硬化问题在此修复。
- 源码闭包 pytest（-o pythonpath 方式）：**首跑 1 failed/866 passed**（test_secure_ownership::
  test_bad_permissions_and_symlink_refused）；**随后全量 867 passed ×2（12.46s/12.26s）+单文件
  12/12 ×3 全过**——首败为不可复现瞬态（测试间状态污染类），如实记录不隐瞒；以复现矩阵为准，
  预期 867 达成。业务清单测试 **1 passed**；零残留子进程。

## 三、单元实况（本轮实采 2026-09-21T01:00:54Z；历史 PID 2278380 仅参考）

单元名 **agentseek-m3-biz-supervisor.service**；MainPID=**2278380**（实采=参考值一致）；
FragmentPath=/etc/systemd/system/agentseek-m3-biz-supervisor.service；DropInPaths 空；
ControlGroup=/system.slice/agentseek-m3-biz-supervisor.service；active/running。

## 四、8 字段 pins 修订与现场只读核验（单独结果）

- **显式 8 字段 pins 修订**：config/supervisor-identity-pins-v2-20260921.json（0600，
  SHA256 `8c1555acc26a5150f634511d64982c268cc75e5a536c3a7f890ee1f52ba9afcd`）——unit=
  完整单元名；executable=业务 venv 解释器解析路径+摘要；script=node_supervisor.py 副本+摘要；
  unit_file+摘要；cmdline 摘要；上游引用（未来业务创建链 precreate 的 supervisor_identity）将在
  最终材料轮按 8 字段 pins 重新生成并逐项核对——旧材料保留历史未原位改（R1 pins 于
  /etc/agentseek-m3/ 原样；上轮失败尝试未留文件）。
- **现场只读身份核验（一次）**：新 venv（venv-unit）+ 真实本机 snapshot（read_empty）+ 显式
  unit pins 调 verify_identity → **exit 0 PASS**：pid=2278380、ProcessIdentity.unit=
  agentseek-m3-biz-supervisor.service。**上轮阻塞（UNIT 硬编码致 MainPID 永不匹配）确认解除**。
  核验全程只读，未调用任何创建入口（终检执行入口=0）。

## 五、流程知悉与下一步

- **W0 新规知悉**：下轮起 .172 为唯一 W0 台账点，全部 writer 确认（含 codex 本人）须于窗口
  start 前实收于 .172 台账再定窗口。
- 流程澄清知悉：**本轮通过仅更新批准包拟部署版本（4ebd197）**——实际安装切换（/opt 落位）、
  最终材料生成（含 8 字段 pins 上游引用闭合）与新窗口仍须用户批准后按序执行，不直接进入执行。

## 六、边界

未切实际安装（/opt 各版本原样；venv-unit 仅 scratch）；未启业务服务（business-broker 未建未启；
四服务状态不变：M0/M2/R1+业务监督）；零 guest 零探针；基础设施（规则/CA/token/第二监督）保留
待裁定；零 Git。回传仅两件脱敏件。
