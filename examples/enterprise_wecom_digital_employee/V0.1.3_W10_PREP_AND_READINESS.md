# W10 准备——落位+材料 v10+日志去向+恢复方案（zcode，2026-09-22 07:4x UTC）

## 判定：**W10_PREPARED（材料就绪，等 user+codex W0 确认后重钉 confirmed_epoch 并设窗口）**；未切实际入口（broker unit 已指 7ea9ef7c+W10 配置但未 start）；零 guest/零探针/零 Git

## 一、W9 意图闭合（本授权下执行）

| 项 | 值 |
| --- | --- |
| 操作 | store attempts 表 UPDATE state: reconciling→failed |
| 对象 | id=3360722073a0…（W9 attempt） |
| 依据 | 用户批准 + guest 666c9e4c 真实创建 + 监督击杀 + 零 artifact + 取证双档（6a6f182/a2ba27e） |
| 时间 | 2026-09-22 07:30:53 UTC |

## 二、7ea9ef7c 落位 + 45/2 复核

- /opt/agentseek-business-releases/7ea9ef7c/venv（净目录；wheel 9f097a97…621c；纯交付依赖 --offline --no-index）
- install_check --profile business：**45/2/isolated/exit 0**
- INSTALL-RECORD 0600（含修复描述：execution_diagnostic.py 新增）
- 既有 09949c8/13f9114/3009d82/4ebd197/9d54662 全保留

## 三、材料 v10 全链（10/10 PASS）

| 件 | 摘要（前 16） |
| --- | --- |
| W0-window-FINAL-W10 | 见清单 |
| request-W10 | 见清单 |
| precreate-W10 | 见清单 |
| approval-W10-FINAL | 见清单 |
| create-installation-W10 | 见清单 |
| launcher-W10 | 见清单 |
| lifecycle-W10 | 见清单 |
| business-session-W10 | 见清单 |
| permits-W10 | 见清单 |
| service-config-W10-FINAL | 见清单 |

核验项（全 PASS）：read_window/read_plan/十级引用/candidate=7ea9ef7c wheel/proxy_port=80/quota-w10 空/fence-w10 空/lifecycle-v10 空/batch=2/intent=0

**W0 台账 2/4**：zcode-172 ✓ + cc-171 ✓；user/codex 待收——4/4 闭合后 confirmed_epoch 重钉为实收时刻（当前 W0 内 confirmed=start 占位，4/4 后更新）

## 四、execution_diagnostic 日志去向确认

- logger name: `agentseek_execution.execution_diagnostic`
- 输出格式: `execution_stage {json}`（stage/event/scope_id/request_sha256/pid/thread_id/epoch_ms/elapsed_ms/error/worker_id/child_pid/returncode）
- 传播: root logger → syslog → **journalctl -u agentseek-business-broker** ✓（实测输出格式正确）
- 14 阶段全在 STAGES 白名单

## 五、恢复方案（具体命令）

```bash
# 恢复 broker 至 13f9114 + W7 配置
sed -i 's|/opt/agentseek-business-releases/7ea9ef7c/venv|/opt/agentseek-business-releases/13f9114/venv|' /etc/systemd/system/agentseek-business-broker.service
sed -i 's|service-config-W10-FINAL.json|service-config-W7-FINAL.json|' /etc/systemd/system/agentseek-business-broker.service
W7_SHA=$(sha256sum /var/lib/agentseek-m3-business/runs/agentseek-m3-biz-20260921/config/service-config-W7-FINAL.json | awk '{print $1}')
sed -i "s|--sha256 [a-f0-9]\{64\}|--sha256 $W7_SHA|" /etc/systemd/system/agentseek-business-broker.service
systemctl daemon-reload && systemctl restart agentseek-business-broker.service
```

## 六、边界

零 guest/零探针/零 Git；旧账本/额度/栅栏/回执全保留；不切 13f9114 入口（broker unit 已写但未 start——start 即部署切换）。等 user+codex W0 确认后重钉 confirmed_epoch + 设 ≤2h 窗口 + start broker = 部署切换完成。
