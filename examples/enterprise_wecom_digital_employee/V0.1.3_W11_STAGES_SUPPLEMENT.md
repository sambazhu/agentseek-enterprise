# W11 完整 execution_stage 补充件（.171，2026-09-22；只读取证，原报告 b5eb4b2 保留）

## 一、完整阶段链（按 epoch_ms 时序，scope/worker/PID 全留）

**provider_create（scope f9da89e2，3229ms 全绿）**：
```
1790067619144 provider_create started → lifecycle started → intent_save(1ms complete)
1790067619147 launcher started → create_worker started → worker_exchange started
1790067619148 worker_spawn started（worker 4bfd320d）→ spawned（child_pid 2082225）
1790067620156 worker_exchange complete 1007ms → worker_cleanup（group_absent→reaped rc=0）
1790067619156→1790067622369 attach_worker started（worker 97289fa2，child 2082339）→ 2212ms complete rc=0
1790067622369 launcher complete 3222ms → result_save(2ms) → lifecycle complete
1790067622372 session_reconstruct started→complete(0ms) → provider_create complete 3229ms ✅
```
**169ms/returncode=0 归属澄清**：rc=0 属于**三个 worker 的 cleanup reaped**（create 1007ms/attach 2212ms/run 168ms/destroy 169ms 各自 reaped）；**provider_run 与 provider_destroy 均 event=failed err=contract**（worker 本身退出码 0 但结果信封含合同拒绝）。

**provider_run（scope 87b9bcdd）**：worker 78cc2225/child 2082389 → 168ms → **failed err=contract**
**provider_destroy（scope 91740b19）**：worker 866f400b/child 2082392 → 169ms → **failed err=contract**

## 二、csv_worker 诊断行（决定性）
```
09:00:22 csv_worker operation=run       stage=validation error=contract elapsed_ms=3 ready_attempts=0 http_status=0
09:00:22 csv_worker operation=terminate stage=validation error=contract elapsed_ms=3 ready_attempts=0 http_status=0
```
**第 14 号缺陷精确定位：会话 worker 在 validation 阶段 3ms 即合同拒绝——readiness 从未开始（ready_attempts=0/http_status=0）**。run 与 terminate 双双同败（同校验同拒）。疑点=ApprovedCsvSession 的 validate_installation/read_plan 校验项（与 broker 侧 validate_request 是不同函数——后者已实证通过）。

## 三、create-result.json（W11/lifecycle-v10）
**存在**（mtime 09:00:22.366）且 **registered_and_observed=true** ✅（binding 完整：candidate=9f097a97[7ea9ef7c]/boot_id/run_id/template/restricted——与预期一致）。W9 时"create-result 不存在"是 lifecycle-v9 的事实（当时未写入），W11 已正常写入。

## 四、时间矛盾修正
guest b57cbcb5：平台 create ts=1790067620.120 → delete ts=1790067741.060 = **120.94 秒**（监督 120s 兜底语义一致）。凡"61 秒"表述均为钟源误读，以平台事件 ts 为准。

## 五、给 Codex 的定位请求
`ApprovedCsvSession`（business_cube_session）的 validate_installation/read_plan 在 W11 配置下具体哪枚校验失败——请对照 business-session-W11.json 十二字段与 create-installation-W11 逐枚（重点：supervisor_identity 八字段 pins、supervisor_directory、control 与 precreate 四元组——proxy_port=80 已放行但同块还有 endpoint/ca_file/api_key 三元）。
