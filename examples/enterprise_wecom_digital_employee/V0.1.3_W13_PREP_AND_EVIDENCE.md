# W13 准备与 register_run 归档（.171，2026-09-22；零创建/暂不执行）

## 一、register_run 重置归档（工单要求）

| 项 | 值 |
| --- | --- |
| 批准依据 | 用户"清+继续"裁定（2026-09-22 ~10:10 UTC，继第六次裁定 A 后同轮） |
| 原残内容 | manifest.sandboxes = {b57cbcb5: {registered_at: 1790067620.49}}（W11 guest 登记） |
| W11 guest 结束证据 | 平台 delete event ts=1790067741.06（09:01:21 UTC，registry_size 归零）；监督 HARD DEADLINE 00:15:08.957 |
| 其他旧 guest 结束证据 | c50998377(W2)/ad72dd23(W3)/51d801bd(W4)/666c9e4c(W9) 全部 delete event 在册 |
| manifest 备份 | register_run fresh 重置（旧条目入册保留——zcode 09:43 register_run 操作日志在 supervisor journal） |
| 非默认操作声明 | **manifest 清空不作为每轮默认**——仅在用户明示批准+旧 guest 结束证据齐备时执行 |

## 二、#13 请求与材料复用评估

| 项 | 状态 | 复用判定 |
| --- | --- | --- |
| #13（10226467）grant | **已过期**（expires=11:53 UTC） | **不可复用**（有效期是 grant 的合同组成） |
| lifecycle-v12 | create-intent.json 在（#12 已烧） | 不可复用（需新 #14+新 lifecycle-v14） |
| quota-w12 / fence-w12 | create-intent 烧毁后 slot 状态未查 | **须核对后决定**——"零 guest≠可复用"（工单要求） |
| permits-W13 | 内容正确（request 10226467） | 不可复用（随 #13 过期） |

## 三、实际物化文件核验（非内存对象，逐枚 sha256 对算）

| 文件 | 存在 | 权限 | 实摘要==钉值 |
| --- | --- | --- | --- |
| precreate-W12.json → api_key | ✅ | 0600 | **True**（ac239eab==ci.api_key_sha256） |
| → approval | ✅ | 0600 | **True** |
| → ca.pem | ✅ | 0600 | **True**（b89f99fa） |
| → request-W12.json | ✅ | 0600 | **True**（c13239db==pre.plan.request_sha256） |
| create-installation → precreate | ✅ | 0600 | **True** |
| → receipt.key | ✅ | 0600 | **True**（47e6159c） |
| **综合** | — | — | **PASS（6/6）** |

## 四、修订后执行方案（准备先完成→用户就绪后才启动有效期）

```
[准备阶段——有效期不启动]
① .171 铸造 #14 + grants/config 生成器预置（不设 expires——待有效期确定后闭合）
② .172 落位 50adc3f（已就位）+ 材料 v14 全链生成+重钉+**执行前五项硬核验**（物化文件逐枚 sha/存在/权限/解释器/unit sha/boot_id）
③ .172 报告"材料就绪+五项 PASS"
④ .171 核验材料摘要链
⑤ **通知用户："材料全部就绪，请确认可测试+完成预上传（发送 CSV 但不发问题）"**
⑥ 用户确认+预上传完成
[启动阶段——此时才闭合]
⑦ .172 W0 四方确认（.171+codex 的 W0 确认可提前在准备阶段出具，user 在此步）
⑧ .172 设 ≤2h 有效期并同步 .171
⑨ .171 grants/config 闭合（expires=有效期末）→ 网关切换
⑩ .171 绑定核对（预上传的 CSV 已在 current_files——**无需再重传**）
⑪ 通知用户发固定问题
⑫ 真实 DeepAgent 全链 → 验收
```

## 五、给 Codex 的流程修复请求（最优先）
**重复漏物化的材料生成流程**——W10（api_key 遗漏）+ W12（api_key 遗漏）两次同模式：zcode 的材料 JSON 生成工具只写配置文件不物化密钥/证书到新 run 目录。请交付：材料生成工具的**运行时物化自检**（生成完毕即读每枚 *_file 路径→存在性+权限+sha256 对算——失败即停止，不允许"生成完毕=就绪"报告）。这是比"下一次一定成功"更重要的修复。
