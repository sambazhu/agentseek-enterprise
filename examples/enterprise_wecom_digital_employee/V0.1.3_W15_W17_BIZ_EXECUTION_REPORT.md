---
title: W15-W17 业务窗口执行报告（六次发问，W17 全链 PASS——首次 BUSINESS_E2E_PASS）
type: how-to
audience: [A4]
runs: yes
verified_on: 2026-09-24
sources:
  - V0.1.3_W14_BIZ_EXECUTION_REPORT.md
  - V0.1.3_SANDBOX_DECODE_HANDOFF.md
---

# W15-W17 业务窗口执行报告（窗口 2026-09-24 13:56:54Z–15:56:54Z；.171 归档）

## 一、终局判定：**BUSINESS_E2E_PASS（W17，第六次发问，15:21:32Z）**

| 验收项 | 证据 |
| --- | --- |
| 真实 DeepAgent 发起 | attempt `768b7435…`（request **#17** `7a181055…c59ef`，owner `fcc5b3` ✓），模型自主调用，零人工直驱 |
| 创建恰一次 | guest `b335a527…`；fence/quota 消耗；lifecycle-v15c 三件完整 |
| 登记 | create-result.json `registered_and_observed=true` |
| readiness | `/health` 200，ready_attempts=1 |
| **CSV 命令（guest 内）** | **`csv_worker run {"elapsed_ms":1225,"error":"none","http_status":200,"ready_attempts":1,"stage":"complete"}`——第 16 号修复实战闭环** |
| 持久化 | 节点 store succeeded + .171 镜像 artifact `artifact_de3b914c…` 双侧 |
| 终止/closeout | terminate complete(155ms)；`known_target_absent=true`、`next_create_authorized=false`；guest 链内销毁、监督兜底未触发 |
| **工作区回写+摘要** | **summary.csv sha256 = `376b78750aa4f075ee291e117efdf1e015d58c43c0bc61c82215feff26085a32` 与合同验收摘要逐字节精确一致**（28B=`group,total\nA,8.25\nB,10\nC,0\n`，表头合同格式 ✓） |
| bot 交付 | "✅ 已通过沙箱生成 summary.csv 交付到工作区"（15:21:54Z）——无本地替代、无伪造 |
| HTTPS 下载 | **NOT RUN（by design）**：本窗口批准链明示下载 disabled、.60 不改；验收终点=工作区回写+摘要（已达成）。下载通道验证留待未来启用轮次 |

## 二、六次发问时间线（同一窗口内三轮修正）

| # | 时刻 | request | 结果 | 阻断/处置 |
| --- | --- | --- | --- | --- |
| 1 | 14:36:23Z | #15 | launcher 0ms DENIED | tracking 目录含 W14 残留 create.target（`m3_launcher.py:46` 空目录要求）；零平台到达 |
| 2 | 14:47:14Z | #15 | 模型拒绝重发 | 镜像 reconciling 未决→grant_available=false；**模型本地计算充当交付①**（越界但透明） |
| 3 | 14:50:33Z | #15 | 仍拒绝 | 节点 store 仍 reconciling（broker 权威视图） |
| 4 | 14:54:44Z | #15 | 仍拒绝 | 双侧闭合后 `retry_allowed=false`——**定罪：failed attempt 消耗 request_id 本身** |
| 5 | 15:07:03Z | #16 | worker_exchange 169ms contract | **zcode 定罪：14:27 manifest 重置未带 umask 077→manifest.json 0644→precreate `_private` 0600 拒**；chmod 600 后 precreate 6/6 过；零平台到达 |
| 6 | **15:21:32Z** | **#17** | **全链 PASS** | 见 §一 |

**修正轮**：①tracking→run 专属目录+lifecycle-v15b+三件重钉；②双侧 record('failed') 闭合（用户批准×2）；③#16 全链+grants v17+网关重启+CSV 重传；④#17 全链（构建器 tracking 缺陷**修到源**）+grants v18+网关重启+CSV 重传。

## 三、窗口内新发现（入缺陷账/观察项）

1. **缺陷 #17（已修）**：manifest register_run fresh 不带 umask 077 → 0644 → `_private` 0600 拒——zcode 现场定罪+修复（chmod 600）；**教训：节点状态操作必须带 umask 合同**。
2. **语义定罪（文档化项）**：failed attempt 消耗 request_id（retry_allowed=false）+未决行阻断 grant_available（双侧 store）——窗口内重试最小闭环=双侧闭合→新 request→十件重建→grants→网关重启→CSV 重传→发问（实测 ~19 分钟）。
3. **观察项（交 Codex）**：模型在拒绝重发后两次以本地计算充当交付（表头 `group,amount` ≠ 程序合同 `group,total`；自标"工作区未就绪"未冒充成功）；子进程信封 stderr=DEVNULL 致 #16 类失败无信封细节（诊断缺口）；launcher 运行时空目录要求不在此前预检清单。
4. 材料链工教训：构建器缺陷两次同型复发（tracking 指向）后第三次修到源；owner 笔误第三次复发后修到源（v9→staging→draft 全链）——**修缺陷必须修到源头文件**。

## 四、证据（.171 `/root/m3-w17-exec-archive/` 0700；.172 `/root/w17-success-evidence-20260924/`）

| 件 | 摘要 | 说明 |
| --- | --- | --- |
| gateway-log-W15-window-full.txt | `749e2d80…` | 全窗口网关日志（21:56 起 26908 行，含六次发问/三轮修正/最终成功） |
| mirror-final.sqlite | `6b0308f3…` | .171 镜像终态快照（14 attempts/1 artifact，cp 时点副本） |
| broker-journal-W17-full.log | `61d4113d…` | zcode 提供节点全阶段日志 |
| summary.csv | **`376b7875…85a32`** | 产物原件（28B） |
| 其余 | EVIDENCE-SHA256 全过 | closeout/create 三结果件/store-final/workspace-manifest |

## 五、边界遵守

- 六次发问均用户本人发送；零 shell 直驱、零重试触发、执行期零重启（重启均发生在发问间隔的材料更替，用户逐次批准）；
- 一次创建授权恰耗一次（b335a527）；监督合同运行（兜底未触发）；下载 disabled、.60 未动、production/标签未动；
- 收口后 .171 按已批准方案恢复生产网关。

## 六、累计账目

- **缺陷：1-16 全修复实战验证（#16 于本轮 W17 闭环）；#17 本轮发现并修复**；
- 架构全链首次端到端贯通：真实 DeepAgent→授权→创建→readiness→guest 内 CSV→持久化→终止→closeout→工作区回写→**摘要精确命中**；
- 下一里程碑（另批）：HTTPS 下载通道启用+用户浏览器下载核验；多用户/真实语料扩展。
