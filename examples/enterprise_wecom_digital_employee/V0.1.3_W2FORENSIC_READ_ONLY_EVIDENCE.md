# 窗口二已结束 guest 只读补证——法证报告（zcode，2026-09-21 06:48 UTC）

## 判定：**FORENSIC_COMPLETE——run 命令从未到达 CubeProxy/guest（日志与代码证据完全吻合）；terminate DELETE 确有记录；guest 存活 ~1.3s**；零创建零重试零配置变更

## 一、代码证据对照（.171 归档 4fd24e9 ↔ .172 安装件双确认）

| 版本 | business_cube_session.py:160 | 效果 |
| --- | --- | --- |
| **4ebd197（窗口二实际运行）** | `verify_identity(IdentityPins(**plan["supervisor_identity"]))` — **单参调用** | `TypeError: verify_identity() missing 1 required positional argument: 'pins'` — **在命令构造与发送之前抛出** |
| **09949c8（修复版）** | `supervisor = SupervisorReader(...).read(...)` → `verify_identity(supervisor, IdentityPins(...))` — **双参** | 正确传入 snapshot + pins |

.172 安装件（/opt/agentseek-business-releases/4ebd197/）与源码闭包第 160 行均为单参版本——与 .171 归档的代码证据**逐字吻合**。

## 二、平台日志取证

### lifecycle-manager（docker logs，sandbox=09824eaa984b44899398672f40e9feb6）

| 事件 | UTC 时间戳 | 内容 |
| --- | --- | --- |
| create event applied | **05:47:36.730Z** | sandbox 创建（registry_size=1） |
| delete event applied | **05:47:38.027Z** | sandbox 删除（registry_size=0） |
| **存活时长** | **1.30 秒** | |

### CubeProxy（docker logs + 13080 access log）

**该 sandbox 零记录**——run 命令（`python3 -I -c …` 的 envd 调用）**从未经过 CubeProxy 到达 guest**。

### supervisor journal（agentseek-m3-biz-supervisor，05:44–05:50）

**无 HARD deadline / kill / ALARM 条目**——guest 不是被 120s 硬截止终止的，而是被**应用自身的 cleanup DELETE** 终止（先于 120s 到来）。

### business-broker journal

05:46:02 与 05:47:33 有 systemd restart 条目（.172 调试期间的修复重启，如实记录）；无 run 请求日志。

### .172 本地直接驱动记录

.172 曾在窗口内多次直接驱动 broker.dispatch / perform / lifecycle execute（详见窗口二报告）——这些是**本地直接调用**（进程内 Python 调用，非经 HTTPS 13100），其产物（vault 回执/quota/fence/lifecycle 链）与日志时间线一致。区分：所有驱动均走同一 business_cube_session 代码路径→同一 TypeError。

## 三、rfs 工件 envd 启动配置（只读，从平台 API）

| 项 | 值 |
| --- | --- |
| 容器名 | cubebox-name-0 |
| CODE_INTERPRETER_HOST | 0.0.0.0 |
| CODE_INTERPRETER_PORT | **49999** |
| CODE_INTERPRETER_WORKDIR | /workspace |
| PYTHON_VERSION | **3.12.12** |
| envd 服务归属 | guest 容器内进程，监听 0.0.0.0:49999 |
| 访问路径 | CubeProxy 13080 → Host: 49983-{sandboxID}.{domain} → guest:49999 |

## 四、综合时间线（全部证据源对齐）

```
05:47:36.730Z  lifecycle-manager: create event（guest 09824eaa… 创建）
05:47:36.730Z  vault: 密封 intent + receipt（创建 POST 成功）
05:47:36.7+    business_cube_session worker 子进程启动（run_worker）
05:47:36.7+      → 读 plan/vault/supervisor 文件
05:47:36.7+      → :160 verify_identity(单参) → **TypeError** ← 命令构造/发送之前
05:47:36.7+      → CubeProxy 13080：零请求记录（run 命令从未到达）
05:47:36.7+      → 异常传播至 broker → store 标记 reconciling
05:47:38.027Z  lifecycle-manager: delete event（terminate DELETE 由 cleanup 路径发出）
05:47:38.0+    supervisor：无 kill/HARD deadline（应用 DELETE 先于 120s 到来）
05:47:38.0+    closeout-result.json 落盘（lifecycle-v2 目录）
```

## 五、结论

1. **run 命令（CSV python3 程序）从未到达 CubeProxy/guest**——CubeProxy 零记录 + 代码在命令构造前抛 TypeError。
2. **terminate DELETE 确有记录**——lifecycle-manager delete event 在创建后 1.30s。
3. **guest 被应用 cleanup 终止（非 120s 硬截止）**——supervisor journal 无 kill 条目。
4. **根因确认**：4ebd197 的 business_cube_session.py:160 单参 verify_identity 调用在命令发送前抛 TypeError，导致 CSV 执行从未开始。修复版 09949c8 已改为双参。
5. **Python 3.12.12 在模板中确认存在**（PYTHON_VERSION env）——CSV 程序本身应可运行，但本轮从未获得执行机会。
6. 缺日志项：business_cube_session worker 子进程的 stderr 未被 systemd 捕获（run_worker 以 DEVNULL stderr 运行）——TypeError 的直接 stderr 不可复原，但代码逻辑与全部间接证据（CubeProxy 零记录+时间线+lifecycle-manager delete）构成完整闭环。

## 六、边界

零创建零重试零配置变更零 Git；全部取证为只读（docker logs/journalctl/API GET/platform 文件 stat）；凭据/请求正文/业务数据零输出。
