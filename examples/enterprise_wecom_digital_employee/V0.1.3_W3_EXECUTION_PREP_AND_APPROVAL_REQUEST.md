# 窗口三执行准备与统一批准请求（zcode，2026-09-21 07:0x UTC）

## 判定：**EXECUTION_PREPARED_PENDING_APPROVAL**；零 guest/零探针/零 Git/旧账本额度栅栏全保留

## 一、终止方式勘正（入册）

窗口二报告曾将 guest 终止笼统记为"监督 120s 硬截止"。法证轮证实：**guest 被应用 cleanup DELETE
终止**（lifecycle-manager delete 05:47:38.0Z，距创建 1.30s），**非** 120s 截止（supervisor 无 kill）。
120s 硬截止作为兜底在位但未触发。

## 二、审计事项单列（不归入代码修复 PASS）

| # | 事项 | 审计关注 |
| --- | --- | --- |
| A | 第二 batch 计划授权 | batch[1] 仅为 BatchQuota 合同占位（len∈{2,4}），从未获准从未执行（quota 无 slot-1）；下一轮同结构，slot 递增机制防越权已确认（m3_batch_quota.py:43-56） |
| B | 旧 intent 换目录 | lifecycle/ → lifecycle-v2/ 系因旧链含 artifact SHA 笔误（已知缺陷）而非规避失败；O_EXCL 烧毁正确阻止了基于错误链的重试；定性权在用户 |

## 三、两端拟部署版本核对

| 端 | 版本 | 状态 |
| --- | --- | --- |
| .172 节点 | **09949c8**（候选 Git SHA 09949c8a1f098071486fea264aa255da3ffd7c05；wheel ab8ace00…f1dacc） | **已落位** /opt/agentseek-business-releases/09949c8/venv，44/2/isolated 复核 exit 0；:160 已确认双参修复版；4ebd197 保留回退；INSTALL-RECORD 0600 |
| .171 网关 | 0595b47d（用户批准包 v2 基线） | .171 侧确认（非本轮 .172 范围） |

## 四、实际命令与恢复方案

| 动作 | 命令（批准后执行） | 恢复 |
| --- | --- | --- |
| 安装切换 | business-broker unit ExecStart 改用 09949c8 venv python + 新 service-config 摘要 | unit ExecStart 恢复 4ebd197 路径；systemctl restart |
| 服务配置 | 新 service-config-FINAL（permits v3 新 request_id + plans 引用新链） | 旧 v2 文件保留，恢复=指回旧文件+restart |
| 防火墙 | 无变更（13100 三条规则已在位，.171 源 ACCEPT） | 不变 |
| CA/证书 | 无变更（CA v2 + 服务器证书 v2 已就位） | 不变 |
| broker token | 无变更（已交接 .171） | 不变 |

## 五、新 request_id 与配置引用

- **拟议 request_id**：`249714095a3842d78c722c4308567ef2287891367a6d903fd1681da13082fbf9`（预分配 0600；待批准后入 permits v3）
- 其余绑定值不变：owner_id/input_ref/instruction/input_sha256/principal_sha256/expires 同前
- **一次创建额度**：新 run（或沿用 agentseek-m3-biz-20260920 新 slot？——**需用户裁定**：新 run 需新监督/manifest，或旧 run 新额度目录）；推荐=新 run `agentseek-m3-biz-20260921`+新目录树（旧 20260920 全保留审计）
- **旧账本/额度/栅栏全保留**：20260920 的 quota slot-0/fence pending/vault/lifecycle 链不动不清

## 六、W0 下一窗口台账（.172 唯一台账点）

台账 `/var/lib/agentseek-m3-business/W0-w3-window-confirmations.json`（40d47073…）：
- **zcode-172 ✓**（本人直接出具，见 JSON 内时刻，覆盖"下一授权窗口"）
- user-zhuchunlin **待收**（应随统一批准请求批复或窗口指定一并给出）
- cc-171 **待收**（旧窗口确认不沿用）
- codex-dev **待收**（需本人补发）
- **暂不定 start**；全部实收且声明≤start 后方可定窗口

## 七、统一批准请求（一项打包）

**请用户批准以下三件事（一次批复）**：

1. **候选安装切换**：business-broker 切换至 09949c8（ExecStart 路径+新 service-config 摘要+restart）
2. **最终材料生成**：permits v3（新 request_id 2497…fbf9）+ 创建链材料（新 run 或旧 run 新 slot）+ service 最终件 v3 + plans 重钉
3. **一次业务创建授权**：真实 DeepAgent→CSV 沙箱执行→正常终止/closeout→.171 用户工作区回写→用户核对预期摘要 376b7875…

**未批准不创建。** 批准后 .172 依序执行：切换→材料→通知用户重传 CSV→绑定核对→通知用户发固定问题→真实执行→回写→验收。下载保持 disabled、.60 不改。

## 八、边界

零 guest/零探针/零 Git/旧账本额度栅栏全保留；本轮仅安装落位（独立目录）/审计文档/W0 台账/预分配 ID/报告——无任何执行动作。
