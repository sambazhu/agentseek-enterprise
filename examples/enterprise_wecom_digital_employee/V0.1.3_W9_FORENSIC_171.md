# W9 只读取证报告（.171，2026-09-22；零创建/零重试/零服务变更/零清账）

## 一、统一时间线（四钟源对齐，全部 UTC）

| 时刻 | 事件 | 钟源 |
| --- | --- | --- |
| 00:08:10.8 | 网关身份缓存命中（turn 开始） | gateway.log |
| ~00:08:1x | **execute 交换完成：HTTP 200 / elapsed 198ms / attempt 33607220** | 网关 trace（business_exchange） |
| 00:08:24.32 | create-intent.json 写入（O_EXCL，lifecycle-v9 唯一文件） | 文件 mtime |
| **00:08:24 → 00:13:08** | **【4 分 44 秒未解释间隙】** | 两侧无任何事件 |
| 00:13:08.27/72 | vault 密封 intent→receipt（间隔 0.44s=create POST 成功） | 文件 mtime |
| 00:13:08.72 | 平台 create event（sandbox 666c9e4c） | lifecycle-manager 事件时间 |
| 00:15:08.96 | **监督 HARD DEADLINE 击杀**（startedAt+120s 精确）+alarm pending | supervisor journal |
| 00:15:10.96 | alarm cleared | supervisor journal |
| 00:15:18.01 | 平台 delete event；**同刻** manifest registered_at=00:15:18.01 | lifecycle-manager/supervisor manifest |

## 二、各证据单列（工单逐项）

| 项 | 结果 |
| --- | --- |
| lifecycle-v9 create-result.json | **不存在**（目录仅 create-intent）→ 创建链在 intent 后未完成闭环 |
| registered_and_observed | **无法评估**（create-result 缺失） |
| vault receipt | ✅ 在册（5cdcfdb7 intent+receipt）——但 **receipt≠create 阶段成功返回**（provider.create 要求 execute 返回 saved=true+create-result 落盘，均未发生） |
| 监督登记 | manifest registered_at=00:15:18.01——**晚于击杀 9 秒**（登记与击杀次序异常，如实记录；击杀锚定平台 startedAt 非登记） |
| tracking（workspace） | **空**（mtime 09-21 14:59 旧）——创建后登记步骤从未执行 |
| 业务 session 建立 | **零证据**（无 csv_worker 诊断、无命令、无产物） |
| closeout | **未执行**（无 closeout-result.json；不用 guest 消失代替） |
| broker journal 00:07–00:16 | **python 行计数=0**；unit 无 StandardOutput/Error 覆盖=默认 journal——**零行与"worker 未调用"不能互证**（但 W4 同机制曾出 csv_worker 行→本次会话段确实未达诊断发射点） |
| 00:08–00:13 是否同一次调用 | **网关侧仅一次 execute**（trace 单一 attempt 33607220；198ms 完成即返回）。**推论（最强解释）**：broker 同步 dispatch 在 ~198ms 内即返回 reconciling=创建子进程被**放弃等待**；**孤儿子进程继续运行**至 00:13:08 完成 POST（间隙=子进程自身耗时）；无人再消费其结果 → guest 无人接管 → 120s 兜底击杀。**PIDs/子进程 stderr 不可复原**（进程已消亡，如实标注） |

## 三、独立勘误（对 zcode W9 报告）
1. "guest 存活 ~20s" → 实为 **120.2s**（00:13:08.7→00:15:08.96，监督硬截止）
2. "00:14:48 创建 / 00:15:18 register / 00:15:08 delete" → 实值：**创建 00:13:08.7 / delete 00:15:08.96 / manifest 登记 00:15:18.01**（其时间线三处钟源混淆）
3. "closeout：未执行（guest 提前消失）" → guest 非提前消失（活满 120s）；closeout 未执行是因**创建链父进程早已放弃**，非 guest 消失所致

## 四、待 Codex 裁定的补丁方向（.171 不定因）
- 创建子进程的**放弃等待与孤儿化**：provider.create/lifecycle execute 的等待预算 vs 子进程实际运行（10s 预算 vs 4m44s 实际——预算为何没约束/或约束后为何子进程未被终止）
- 198ms 即返 reconciling 的分类依据（哪一层判"uncertain"）
- 监督登记滞后 9s（击杀先于登记写入）
- 旧 reconciling 保留未动；不开启下一轮
