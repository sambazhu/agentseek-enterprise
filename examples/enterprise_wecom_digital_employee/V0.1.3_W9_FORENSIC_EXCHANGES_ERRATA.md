# W9 交换清单勘正版（.171，2026-09-22；对 6a6f182 的增补与措辞降级）

## 一、按 exchange_id 全部交换清单（W6 网关进程以来）

| exchange_id | op | attempt | 墙钟（锚定） | elapsed | 终态 |
| --- | --- | --- | --- | --- | --- |
| 8e889c2c… | result | 605e162f（W6） | （诊断轮） | 7ms | 200/succeeded |
| b65560bb… | execute | 439c77c7（W7） | （W7 时段） | 10ms | **无 complete 记录**（response_validation unknown/400，W7 硬编码期） |
| 1fd72dbb… | result | 439c77c7 | （W7 时段） | 6ms | 200/succeeded |
| 8ba96a8d… | result | 439c77c7 | （W7 时段） | 6ms | 200/succeeded |
| 584c230d… | execute | 2e3b7375（**W8 请求 68cca518**） | **23:55:01.9** | 2ms | **无 complete 记录**（W8 材料不完整期的模型调用，pid=3484766=v8 网关） |
| **440474cf…** | **execute** | **33607220（W9 请求 662c3ab9）** | **00:08:24.278 起，198ms 完成即 ~00:08:24.5** | 198ms | **200/succeeded（首次 execute）** |
| 0832cbc0… | result | 33607220 | 00:08:27.489 | 6ms | 200/succeeded |

**Codex 三问的回答**：
1. **198ms 那笔是 attempt 33607220 的首次 execute**（该 attempt 仅此一笔 execute + 3 秒后一笔 result，无重复）。
2. 单一 attempt 不作单次调用证明——**以 exchange 清单为证**：W9 请求的 execute 交换仅一笔。
3. **新发现**：W8 请求（2e3b7375）在 23:55:01 有一笔独立 execute（无 complete 记录）——此前的 W8 分析未含此交换。

## 二、三个监督/平台时间精确区分

| 事件 | 时刻（UTC） | 来源 |
| --- | --- | --- |
| **kill-issued** | 00:15:08.**957** | supervisor journal（HARD DEADLINE） |
| **平台 delete event** | 00:15:08.**956** | lifecycle-manager 事件 ts=1790036108.956 |
| **manifest registered_at** | 00:15:**18.013** | supervisor manifest（ts=1790036118.013，晚于击杀 9.06s） |

## 三、措辞降级（按 Codex 指示）
- "孤儿子进程继续运行""父进程早已放弃"：**从勘误/确定性表述降级为推测**（最强解释但 PID/子进程 stderr 不可复原，无法实证）。**可实证的仅是**：execute 交换 00:08:24.5 完成 + create-intent mtime 00:08:24.322（交换窗口内）+ vault 密封/平台 POST 00:13:08.27——**POST 发生于交换完成 4 分 43.8 秒之后**这一墙钟事实。
- 日志缺失项（PIDs/子进程 stderr/broker python 行）一律**不可复原**；不追加现场复现、不清 reconciling、零创建。

## 四、下一补丁诉求（按 Codex 方向）
创建/attach 父进程阶段 + worker PID + 异常分类 + 清理结果的**脱敏诊断**（现有 trace 只覆盖网关侧交换；节点侧创建链父进程无阶段日志——这正是 4m44s 间隙不可复原的原因）。
