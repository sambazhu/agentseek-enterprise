# Vite development server bind address and port used by `agentseek dev`.
FRONTEND_HOST=127.0.0.1
FRONTEND_PORT={{ cookiecutter.frontend_port }}

# Public HTTPS or reverse-proxy backend URL; leave blank to derive it from the browser host.
# VITE_LANGGRAPH_API_URL=
