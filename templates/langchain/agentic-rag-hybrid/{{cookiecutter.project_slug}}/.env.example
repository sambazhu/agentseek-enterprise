# --- Model + provider connection ------------------------------------------
# Choose the chat provider and model below.
# AgentSeek lifecycle checks AGENTSEEK_MODEL_PROVIDER and AGENTSEEK_MODEL.
AGENTSEEK_MODEL_PROVIDER={{ cookiecutter.default_model_provider }}
# The generated agent defaults to the scaffold-time model, but you can
# override it here without editing code.
AGENTSEEK_MODEL={{ cookiecutter.default_model }}
# If you switch providers, switch this model too. Examples:
# - openai: gpt-4.1-mini
# - anthropic: claude-3-5-sonnet-latest
# - google_genai: gemini-2.5-pro
# Compatibility alias for AGENTSEEK_MODEL also works: BUB_MODEL.

# SiliconFlow / OpenAI-compatible
# AGENTSEEK_API_KEY remains the shared default SiliconFlow embedding/VLM credential.
# It also authenticates OpenAI-compatible chat. Existing SILICONFLOW_API_KEY and
# OPENAI_API_KEY shell exports remain compatibility aliases.
AGENTSEEK_API_KEY=
AGENTSEEK_API_BASE=https://api.siliconflow.cn/v1

# Anthropic / Anthropic-compatible
# ANTHROPIC_API_KEY overrides chat authentication when AGENTSEEK_MODEL_PROVIDER=anthropic.
# Leave ANTHROPIC_API_URL empty to use the official Anthropic endpoint.
ANTHROPIC_API_KEY=
ANTHROPIC_API_URL=

# Gemini / Google-compatible
# GOOGLE_API_KEY overrides chat authentication when AGENTSEEK_MODEL_PROVIDER=google_genai.
# Leave GOOGLE_API_BASE empty to use the official Gemini Developer API endpoint.
GOOGLE_API_KEY=
GOOGLE_API_BASE=

# --- Embedded SeekDB via langchain-oceanbase -------------------------------
# OceanbaseVectorStore opens this local path through embedded SeekDB.
# Keep mutable runtime data outside the project tree so `langgraph dev`
# file watching does not reload the backend while indexing sample images.
SEEKDB_PATH={{ cookiecutter.seekdb_path }}
SEEKDB_DB_NAME={{ cookiecutter.seekdb_db_name }}
IMAGE_TABLE_NAME={{ cookiecutter.image_table_name }}

# --- SiliconFlow image embedding + captioning -----------------------------
# EMBEDDING_API_KEY and VLM_API_KEY can override AGENTSEEK_API_KEY when you
# need separate credentials for those calls. EMBEDDING_BASE_URL and
# VLM_BASE_URL default to AGENTSEEK_API_BASE.
EMBEDDING_TYPE=siliconflow
EMBEDDING_API_KEY=
EMBEDDING_BASE_URL=
EMBEDDING_MODEL={{ cookiecutter.embedding_model }}
EMBEDDING_DIMENSION={{ cookiecutter.embedding_dimension }}
VLM_API_KEY=
VLM_BASE_URL=
VLM_MODEL={{ cookiecutter.vlm_model }}

# --- Hybrid behavior --------------------------------------------------------
HYBRID_DEFAULT_MODE=balanced
HYBRID_RECALL_MULTIPLIER=5
HYBRID_MAX_TOP_K=20
HYBRID_AUXILIARY_CANDIDATE_LIMIT=250

# --- Local services ---------------------------------------------------------
MEDIA_DATA_DIR={{ cookiecutter.media_data_dir }}
MEDIA_MAX_UPLOAD_MB=50
# The frontend derives the LangGraph/custom-routes origin from the browser host.
# For remote development, start with:
# LANGGRAPH_HOST=0.0.0.0 FRONTEND_HOST=0.0.0.0 agentseek dev

# --- LangSmith (optional tracing) -----------------------------------------
# Get an API key at https://smith.langchain.com/settings/apikeys
LANGSMITH_API_KEY=
LANGSMITH_TRACING=false
# LANGSMITH_PROJECT={{ cookiecutter.project_slug }}

# --- OpenTelemetry / Phoenix (optional local tracing) ---------------------
# Run `agentseek task phoenix`, then set AGENTSEEK_OTEL_ENABLED=true before
# starting `agentseek dev`. Phoenix opens at http://127.0.0.1:6006 and stores
# traces in OceanBase seekdb through the compose stack.
AGENTSEEK_OTEL_ENABLED=false
AGENTSEEK_OTEL_SERVICE_NAME={{ cookiecutter.project_slug }}
AGENTSEEK_OTEL_PROJECT_NAME={{ cookiecutter.project_slug }}
AGENTSEEK_OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://127.0.0.1:6006/v1/traces
AGENTSEEK_PHOENIX_IMAGE=ghcr.io/agentseek-ai/agentseek-phoenix:main
OCEANBASE_SEEKDB_IMAGE=quay.io/oceanbase/seekdb:latest
